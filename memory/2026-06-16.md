## 01:59 Codex
-完成：修复 BTC 数据层 fallback/预算问题和报告数据边界误导问题；资金费率、OI、CVD、ETF、项目供应字段在全链中均为 ready。第二次 BTC 全链 `run-20260616-053649-f17f12a8` 完成，证据目录 `docs/evidence/fullchain-btc-20260616-after-evidence-boundary-fix`，25 个数据结果全部 ready，gap=0。
-决策：DataNeed 工具预算改为 180s；限流调度在付费源剩余执行窗口不足时跳过迟到候选并尝试 fallback；report_polisher 不得在已有供应字段时写“未返回供给与发行材料”；数据证据边界必须携带样本行数/粒度/覆盖区间，避免把“最新摘要”误读成“只有单点数据”。
待办：最终报告仍保留非当前接口覆盖的细粒度限制：订单簿 L2、高频 CVD 阈值、OI 币本位/U 本位拆分、事件影响分类、社交刻度定义；另发现 final report 有读者表达问题（“布特” typo、自设 1.5 倍/3 根 K/5 个交易日阈值），这不是数据源缺口，但后续需按报告表达质量单独修。

## 02:14 Codex
-完成：修复 report_polisher 的报告表达源头：终稿不得保留未由数据结果明示的成交量倍数、连续 K 线数量、连续天数、价格行为确认等条件；合约计价术语固定为“币本位 / U 本位”；prompt 中不再喂入具体坏数字或错字。
-决策：不手改历史 evidence final_report，保持 live 证据原样；只修源 prompt 和合同测试，后续新报告生效。
待办：如需证明 live 输出已清理，需要按固定 runtime 再跑报告或全链；本轮已通过 `uv run pytest tests/contracts/test_crypto_prompt_truthfulness.py`。
