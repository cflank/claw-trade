## 21:14 [codex]
-完成：复查 `/report AR/USDT` Coinglass 缺口复发原因，确认不是未配置 key，也不是前台多个 worker 重复调用，而是 market pack 内部 `onchain_signals` 对 Coinglass 链上数据连续拆成多个 HTTP 子请求。
-决策：此前 memory 中的修复主要覆盖“worker 工具收敛”和“run plan/cache/single-flight 合同”；当前 Coinglass 链上适配器内部子请求仍绕过该限流/单飞层，且 AR/USDT 符号修复后真实打到 Coinglass，暴露了稳定币净流等子接口 429 问题。
待办：修复时应把 Coinglass 链上子接口纳入统一预算/缓存/限流，或在首个链上子接口失败/限流后停止继续打稳定币净流等额外请求，并补 focused 测试防止同一 onchain 调用内连续触发多次 Coinglass 请求。

## 21:30 [codex]
-完成：按人类要求写入 `docs/限流修改方案.md`，将问题从 Coinglass 单点升级为所有外部接口统一出网闸门方案。
-决策：方案采用两层控制：provider call spec 级 `ProviderExecutionGate` 处理 cache/rate-limit/single-flight，HTTP 子请求级 `ManagedHttpClient` 处理 provider 内部多次请求、429 冷却和子请求缓存；明确禁止 fallback 或把限流伪装成成功。
待办：实施时先做公共执行层，再迁移显式 HTTP 出网路径，并用 AR/USDT、BTC/USDT、600519 等 live/fresh 样本验收。

## 21:41 [codex]
-完成：实现 T5 pypandoc 兼容渲染组件（wkhtmltopdf -> weasyprint -> default）与逐次失败诊断记录，并新增 fallback 单测验证顺序、全失败无 artifact、以及 validator 不可绕过。
-决策：保持 T4 主路径不变，仅提供可组合 `PdfKitWithPandocFallbackRenderer`；不把 WeasyPrint 提升为主路径，也不在全失败时返回占位 PDF。
待办：如需在生产路径启用兼容 fallback，由 T4/T7 调用方显式注入该组合 renderer 并做端到端验证。

## 21:48 [Codex]
-完成：按 T5 返工要求把默认导出渲染器接入 `PdfKitWithPandocFallbackRenderer`，使主路径 pdfkit 失败后自动进入 pandoc 兼容路径，并补充默认服务路径下 fallback 产物仍走 validator 的单测。
-决策：保持 `PdfKitRenderer` 为第一路径，仅在其抛错后触发 pandoc 三引擎尝试；validator 失败仍返回 failed 且不写 PDF artifact。
待办：无。

## 21:42 [Codex]
-完成：完成 T7 完整报告文件请求集成门禁：`request_full_report_file` 在 PDF failed、artifact path/read 失败、channel 显式失败时统一返回 `FILE_SEND_UNSUPPORTED` 且不发送文件；补齐 messageId-only 成功与 `sent:false`/`ok:false`/空返回失败矩阵；修复受本机缺 `font:noto-cjk` 影响的 T7 单测为注入 renderer/runtime capabilities + validator 可通过 `%PDF-` 测试替身。
-决策：不改 OpenClaw 与主产品门禁，仅在通知服务做窄判定加固（包含 `messageId/message_id` 兼容）；保持 HTTP 返回不暴露 `localPath`。
待办：无。

## 22:05 [Codex]
-完成：完成 T8 live 证据收集：总管复核现有 runtime preflight，使用真实 PDF artifact 通过 OpenClaw 微信通道发送，返回 `messageId=openclaw-weixin:1779760699881-bfc4f618`，并落盘 T8 preflight/send payload 证据。
-决策：复用已健康的固定 runtime，不重启、不手工拆启 OpenViking/OpenClaw、不使用 mock/capture-only 替代；T8 只补证据文档和 memory，不改实现代码。
待办：无。

## 06:55 [T00-audit-subagent]
-完成：完成 T00 源文档与现有落点审计，新增 `docs/evidence/a-share-worker-mention-implementation-notes-2026-05-26.md`，记录红线、现状落点、冲突与 BLOCKED 项、dirty worktree 无关文件范围和后续派发建议。
-决策：按 fail-closed 审计口径把“selection 基础能力缺位（无 `src/claw_trade/selection/**`、无 selection 上下文/stage 实现）”标记为后续实现阻塞，不猜测补全路径；明确 T00 不修改 `third_party/openclaw`。
待办：等待人类确认 selection 基础能力前置来源与 T01+启动策略（先合同壳还是等待前置落地）。

## 07:05 [codex]
-完成：按主线程只读评审意见修订 `docs/限流修改任务清单.md`，收敛 Phase 1 共享 gate 合同依赖、修正 fake/local proof 与 live/fresh 口径，并补强 T-00/T-10/T-11 的实施与验收要求。
-决策：本次仅修改任务清单文档，不实现或改动任何源码逻辑；`mock/stub/fake/capture-only` 明确不作为 live/fresh 证明。
待办：按更新后的任务依赖与验收口径启动 AFK/HITL 实施派发。

## 07:14 [codex]
-完成：完成 SEL-00 三仓库算法审计，新增 `docs/evidence/a-share-selection-algo-equivalence-2026-05-26.md`，落盘三仓库 commit 锚点、函数级参数/阈值/排序规则与 claw-trade `filter/strategy/score/top20` 映射，并标注未闭合项。
-决策：将 `go-stock` 标记为关键未闭合来源（自然语言选股逻辑在东财/问财外部服务，仓库仅透传/封装）；按任务要求输出 `BLOCKED`，不做经验补参或猜测闭合。
待办：如需解除 BLOCKED，需补充外部服务算法可审计证据（固定查询条件、返回排序语义、稳定性与参数版本锁定）后再闭合映射。

## 07:32 [sel-00-rework-subagent]
-完成：按人类确认口径返工 `docs/evidence/a-share-selection-algo-equivalence-2026-05-26.md`：将 `go-stock` 从“本地等价算法来源”改为“仅外部 provider 模式参考”，补齐 Human approval、三仓库对照、新等价映射表与 SEL-03 承接合同。
-决策：保留 `myhhub/stock` 与 `Sequoia-X` 作为本地可复现算法参数来源；`go-stock` 仅在用户配置东财/问财 provider 时可调用，未配置即 fail closed/data gap，且不得 fallback/不得反推本地参数。
待办：SEL-03 需基于 `approved_strategy_config_ref` 明确统一 `weights` 与 `stable_top20_rule`，否则继续按 `strategy_config_unapproved` 失败关闭。

## 07:36 [sel-00-testing-subagent]
-完成：按 SEL-00 强制命令逐条执行 3 条 `git ls-remote`，三条命令退出码均为 0；核对 `docs/evidence/a-share-selection-algo-equivalence-2026-05-26.md` 中三仓库 HEAD/分支(tag)锚点与远端输出一致。
-决策：本次只确认“远端版本锚点可取且可复核”，不将 `ls-remote` 结果当作算法正确性证明；并复核 evidence 仍声明 no fallback/no fake，且 `go-stock` 仅为外部 provider 模式参考、非本地等价算法来源。
待办：无。

## 07:47 [sel-01-implementation-subagent]
-完成：完成 SEL-01 选择域 DTO 与双状态机骨架落地（CN_A-only、`select_command` 入口、失败态显式枚举），新增模型/状态机单测并通过指定必跑命令。
-决策：仅在 `src/claw_trade/workflow/models.py` 做最小扩展（新增 `WorkflowEntryPoint.SELECT_COMMAND`），其余实现集中在 `src/claw_trade/selection/models.py` 与两份 SEL-01 指定单测，未触碰 report workflow 业务逻辑与 OpenClaw。
待办：后续 SEL-02+ 任务在此 DTO/状态机壳上填充 store/controller/dispatch 细节。

## 07:54 [sel-01-rework-implementation-subagent]
-完成：完成 SEL-01 review 三项返工：`ReportHandoffRequest` 改为 `report_request: RunRequest` 且强校验 `report_command`，`SelectionWorkflowRun.created_at/updated_at` 改为必填非空，新增 `candidate_pack_hash_mismatch` 显式失败态并补齐单测与证据文档。
-决策：保持改动仅在 SEL-01 指定模型/单测/证据范围内，不触碰 `third_party/openclaw`、report workflow 既有业务逻辑、selection 的 store/controller/dispatch 等 SEL-02+ 内容。

## 07:59 [sel-01-testing-subagent]
-完成：按 SEL-01 要求逐字执行 `uv run pytest tests/unit/selection/test_models.py tests/unit/selection/test_state_machine.py`，退出码 0，40 项全部通过，并确认该证据仅覆盖 DTO/状态机单测。
-决策：本次严格限制为测试结论任务，不修改 `src/**`、`tests/**`、`third_party/openclaw/**`，不将 unit 结果冒充 runtime/live/provider 证明。
待办：无。

## 08:11 [sel-02-implementation-subagent]
-完成：完成 SEL-02 最小闭环实现：新增 selection run store 与 latest completed 只读门，落地 stale/approved/readback/hash/manifest/lineage 校验，`/select` 读取路径在 unavailable 时直接返回并保持零 side effect。
-决策：stale 判定先按 `candidate_pack_ref.expires_at`（SEL-03 再补交易日历细化）；完整性门控在 store 层用显式 integrity 元数据 + manifest/hash/readback 合同校验，不触发 provider/scheduler/data job/OpenClaw。
待办：SEL-03 接入后台 data job 后补齐真实 latest pointer/backfill/rerun/交易日历新鲜度细节。

## 08:17 [sel-02-testing-subagent]
-完成：按 SEL-02 强制命令执行 `uv run pytest tests/integration/selection/test_latest_completed_gate.py`，退出码 0，`collected 8` 且 `8 passed`；并确认 spy 断言仅证明 provider/scheduler/data job 未调用，不作为 runtime/live/provider 成功证明。
-决策：将 `PytestUnknownMarkWarning: integration` 记录为非阻断警告，不扩改 `pyproject.toml`；测试中存在 spy/test double，但仅用于副作用防护断言，未作为 fake runtime/live/provider 验收证据。
待办：无。

## 08:27 [sel-03-implementation-subagent]
-完成：完成 SEL-03 最小闭环实现：新增收盘后调度配置（默认北京时间 16:00，可配置）、确定性 data job 状态链路（fetch->normalize->features->filter/score->stable top20->completed/failed）、fail-closed 约束（approved strategy config/weights/stable_top20_rule/provider batch plan 缺失即失败并落 data gaps），并新增两份 integration 测试与 SEL-03 证据文档。
-决策：严格沿用 SEL-00 口径，将算法参数来源收敛到注入的 approved strategy config；`go-stock/东财` 不做默认路径，不设置 fallback；provider attempts 缺失与策略配置缺失都以 failure status + data gaps 终止。
待办：SEL-04 再接正式 candidate pack approval（OpenViking readback/hash/manifest 全链路），本任务未实现用户 backfill/rerun 入口。

## 08:37 [sel-03-rework-implementation-subagent]
-完成：完成 SEL-03 review 三项返工：top20 严格合同（<20 fail closed + data gap）、stable_top20_rule 强制非空 tie-break（缺失 fail closed）、data job 撤出 approved/readback verified 语义（仅保留 candidate draft/score/top20 证据，不再写 manifest/readback verified）。
-决策：在不改 SEL-04 文件边界下，保持 data run 可完成但 `record.manifest=None` 且 evidence 标记 `candidate_pack_stage=draft_only`，正式 approval/readback 明确留给 SEL-04。
待办：无。

## 08:42 [sel-03-testing-subagent]
-完成：按 SEL-03 要求逐条执行两条必跑命令：`uv run pytest tests/integration/selection/test_data_job_pipeline.py` 与 `uv run pytest tests/integration/selection/test_scheduler_default_time.py`，两条命令退出码均为 0（共 `collected 8`、`passed 8`）。
-决策：将 `PytestUnknownMarkWarning: integration` 记录为非阻断警告且不修改 `pyproject.toml`；测试中的 fixture/注入 callable 仅作为 deterministic contract/integration 证明，不作为 live/provider 验收证据。
待办：无。

## 08:57 [sel-04-implementation-subagent]
-完成：完成 SEL-04 candidate pack 正式审批闭环：新增 candidate pack 构建与合同校验、file-backed artifact write/readback/sha256 验证日志、manifest/lineage 生成，并把 `SelectionDataJob` 从 `draft_only` 接线到 approved pack；新增 SEL-04 unit+integration 测试并通过必跑命令。
-决策：严格执行 top20=20 才可 approved，少于 20 直接 fail closed；model-visible pack 仅保留事实表与机器可复算结果，禁止主观投资判断与机器协议字段；lineage 强制覆盖 provider plan/attempts/normalized/feature/score。
待办：后续 SEL-05 接入 `claw_get_selection_candidate_pack` 工具时复用本次 approved pack ref 与 manifest/readback/hash/lineage 门控。

## 09:06 [sel-04-rework-implementation-subagent]
-完成：完成 SEL-04 review 两项返工：candidate pack manifest/evidence 显式补齐 `stage/target`，并把 `tests/integration/selection/test_data_job_pipeline.py` 从旧 `draft_only/manifest is None` 口径同步到 SEL-04 `approved` 口径，SEL-03 依赖链回归通过。
-决策：仅在允许范围内做最小改动（`models/candidate_pack/data_job` + 指定测试 + 两份 evidence 文档），保持真实 file-backed write->readback->sha256 与 lineage 校验链，不引入 mock/fake/fallback 旁路。
待办：无。

## 09:15 [sel-04-evidence-rework-subagent]
-完成：完成 SEL-04 复审 evidence 一致性返工：用当前实现真实重生成并覆盖 `docs/evidence/sel-04-candidate-pack-artifacts-2026-05-26/**` 的 candidate pack 快照，`candidate-pack-manifest.json` 现已包含 `stage/target`，并更新 `docs/evidence/sel-04-candidate-pack-approval-2026-05-26.md` 明确旧版不含字段快照失效与新证据路径。
-决策：不改 `src/**` 与 `tests/**`，只通过现有 `build_candidate_pack + approve_candidate_pack` 产物修复证据；以 file-backed manifest/readback/hash 一致性作为验收依据，不使用手写或运行期口头断言替代。
待办：无。

## 09:24 [sel-04-testing-subagent]
-完成：按 SEL-04 要求逐字执行 `uv run pytest tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_candidate_pack_approval.py`，退出码 0（collected 5，passed 5），并确认测试覆盖真实 file-backed write/readback/hash 校验链且未使用 fake approved/readback/hash 作为验收证据。
-决策：将 `PytestUnknownMarkWarning: integration` 记录为非阻断警告且不修改 `pyproject.toml`；本次仅执行测试与证据核查，不实现代码、不处理其他 SEL 任务。
待办：无。

## 09:30 [sel-05-implementation-subagent]
-完成：落地 SEL-05：新增 `claw_get_selection_candidate_pack` backend 与独立 selection plugin wrapper，增加 `selection_candidate_pack -> claw_get_selection_candidate_pack` canonical 映射，补齐 SEL-05 合同测试并全部通过。
-决策：采用独立 `openclaw_plugins/claw-trade-selection-tools/`，避免改动既有 frontline/report 工具合约；tool 严格无业务参数、仅允许 strategist/skeptic 在 `selection_review` 读取 approved pack，并执行 readback/hash/manifest/lineage fail-closed 校验。
待办：SEL-06 再接入 selection worker STAGES/prompt 后，补 provider payload focused run 证据闭环。

## 09:43 [sel-05-rework-implementation-subagent]
-完成：完成 SEL-05 rework 复核：复现 `tests/contracts/test_frontline_tool_protocol.py` 失败（1 failed, 30 passed），确认失败点为旧 frontline contract 对工具清单仍断言 4 个，而当前 frontend plugin 实际注册 7 个（含 policy/hot-money/lockup）；SEL-05 必跑测试 `tests/contracts/test_selection_tool_contract.py tests/contracts/test_selection_tool_import_block.py` 仍 6/6 通过。
-决策：按 SEL-05 禁止范围不修改 `openclaw_plugins/claw-trade-frontline-tools/index.js` 与 `tests/contracts/test_frontline_tool_protocol.py`；将该失败判定为 pre-existing/unrelated dirty state，不归因于 SEL-05 selection tool 改动（SEL-05 仅触达 `src/claw_trade/config/tool_names.py` 新增 selection 映射与新增独立 selection plugin）。
待办：由人类确认是否将旧 frontline contract 更新到 7 工具口径，或在独立任务中回退对应旧工具扩展；确认前 SEL-05 可继续按自身验收证据进入复审。

## 09:56 [sel-06-implementation-subagent]
-完成：完成 SEL-06 四个 selection worker 的最小配置落地（`IDENTITY.md`、`prompts/CN_A.md`、`STAGES.yaml`、`SKILLS.md`），并新增 selection stage policy 与 prompt material boundary 两份 contract 测试及 SEL-06 证据文档。
-决策：严格保持 CN_A-only（不加 US/HK/CRYPTO fallback）；`selection_strategist/selection_skeptic` 仅 `selection_candidate_pack`，`selection_manager/selection_portfolio_manager` 工具为空；prompt 明确 manager/PM 不重查数据、PM 仅三分类（进入 `/report` / 观察 / 放弃）并禁止买卖建议模板。
待办：执行 SEL-06 指定命令 `uv run pytest tests/contracts/test_selection_stage_policy.py tests/contracts/test_selection_prompt_material_boundary.py` 并记录结果。

## 09:57 [sel-06-testing-subagent]
-完成：执行 SEL-06 指定命令 `uv run pytest tests/contracts/test_selection_stage_policy.py tests/contracts/test_selection_prompt_material_boundary.py`，退出码 0，`15 passed`。
-决策：修正一处测试文案断言为同义语义匹配（不改业务边界），其余实现保持不变并通过全部 contract。
待办：无。

## 10:20 [sel-07-implementation-subagent]
-完成：完成 SEL-07 固定 4 worker OpenClaw single-turn dispatch 落地与 selection provider payload contract 校验：新增 `selection/dispatch.py`、`selection/evidence.py`，在 `selection/controller.py` 增加最小 dispatch 组装接线，并新增 `tests/contracts/test_selection_dispatch_order.py`、`tests/contracts/test_selection_provider_payload.py`；必跑命令通过（10 passed）。
-决策：dispatch 顺序由 claw-trade 固定（strategist→skeptic→manager→portfolio_manager），每次 command snapshot 固定 `system_context_policy=single_worker_minimal`；工具矩阵固定为 strategist/skeptic 仅 `claw_get_selection_candidate_pack`、manager/PM 无工具；provider request/visible-tools/runtime markers 与 run_id/call_id(worker dispatch_id)/worker/stage 强绑定，并在 manager/PM payload 增加协议污染词阻断。
待办：SEL-11/后续 focused live run 补真实 OpenClaw provider payload 验收证据（当前 SEL-07 证据以 contract 测试为主，非 live/runtime 通过证明）。

## 10:29 [sel-07-rework-implementation-subagent]
-完成：完成 SEL-07 review rework 两项：`build_openclaw_command_for_selection_dispatch` 现将 manager/PM（含 skeptical）所需 approved L1 + candidate_pack_summary 以 `upstream_materials` 引用下发进 OpenClaw command；`execute_selection_dispatches` 现对每次 `run_worker` 结果执行 `validate_selection_dispatch_evidence`，验证失败立即中止后续 dispatch 并返回明确 failed 结果。
-决策：保持最小改动，不改 selection 业务算法与 live 路径；模型可见材料仍来自既有 `model_visible_materials` 文本，Python 不改写内容，仅生成 MaterialReadRef 语义引用与 sha256；证据阻断采用 fail-fast（首个验证失败即停止）。
待办：SEL-11 需在真实 runtime focused run 上补 provider payload live 证据，本次 contract 测试不替代 live 验收。

## 10:41 [sel-07-second-rework-implementation-subagent]
-完成：修复 SEL-07 re-review 剩余问题：manager/PM 通过现有 prompt 变量语义将 approved L1 + `candidate_pack_summary` 正文真实带入模型可见正文（受控标记 `[模型可见已批准材料]`），并把 evidence 校验升级为“必须命中材料标记+正文片段”；补齐“空正文/only refs 不通过、正文存在通过”的 contract 测试并全量通过。
-决策：不改 `third_party/openclaw`；保留 `upstream_materials` 作为审计链，同时新增正文入模路径；正文仅搬运已批准材料，不做总结或改写。
待办：SEL-11 仍需真实 runtime focused run provider payload 证据作为 live 验收。

## 10:59 [sel-08-implementation-subagent]
-完成：完成 SEL-08 最小接线：`/select` 在 chat 层直接进入 selection controller，失败路径只读 latest completed run 并 fail-closed，成功路径按 SEL-07 固定四 worker dispatch 执行并把 PM 决策机械解析为“进入 `/report` / 观察 / 放弃”三分类聊天文本，且每次 `/select` 都落 `selection-workflow-evidence.json` 绑定 workflow/evidence。
-决策：不改 `/report` 业务与 queue/handoff；`/select` completed 仅返回等待确认语义，不自动 enqueue report；PM 文本解析坚持机械规则，歧义直接 `blocked_ask_human`，不做 Python 推断改写。
待办：SEL-09 再接用户确认后的 `/report` handoff 幂等与二次门控（本次未实现）。

## 11:07 [sel-08-rework-implementation-subagent]
-完成：完成 SEL-08 review rework 两项：PM 三分类解析改为强制显式出现“进入/report、观察、放弃”三个段标题（任一缺失即 `selection_result_invalid:missing_required_sections`），并恢复 chat 普通 report intent（不要求显式 `/report`）仍走确认卡；`/select` 仍优先路由到 selection 分支。
-决策：仅改 `selection/controller.py` 与 `ui_backend/chat_controller.py` 的最小判定逻辑；新增回归测试覆盖“缺段失败”“普通 report intent 回归”“/select 优先不被 report intent 吃掉”，不触碰 report queue/handoff/PM 决策业务。
待办：无。

## 11:20 [sel-09-implementation-subagent]
-完成：实现 SEL-09 用户确认与 `/report` handoff：新增 `selection/confirmation.py` 与 `selection/report_handoff.py`，在确认时执行 workflow 可确认态+PM 决策存在+ticker enter_report+candidate pack freshness/readback/hash/manifest/lineage/绑定一致性复验；实现同 idempotency key 返回既有结果与同 `select_workflow_run_id+ticker` 全局去重，并通过现有 `ReportTaskQueue` 创建真实 report task/run（无 fake id）。
-决策：保持 `/report` 内部逻辑与自动触发路径不变，仅复用既有 queue 入口；handoff request 固定 `selection_stage_marker=selection_report_handoff` 与 `report_request.entry_point=report_command`，`selection_context_ref` 仅保留审计追溯，不注入 selection 评级/目标价/建议到 report。
待办：无。

## 11:30 [sel-09-rework-implementation-subagent]
-完成：完成 SEL-09 rework 两项：将 confirmation/handoff 幂等与 workflow+ticker 去重下沉到共享 `SelectionRunStore`（跨 controller 实例生效），并在确认时强校验 PM decision 为 approved material 且绑定当前 workflow；handoff `selection_context_ref` 改为 PM decision material。
-决策：保持 `/report` 队列与业务逻辑不改，仅新增 store 级 confirmation/handoff 记录与查找接口；确认复验失败统一拒绝，不做 fallback。
待办：无。

## 12:18 [sel-09-second-rework-implementation-subagent]
-完成：修复 SEL-09 re-review 剩余问题：`/select` completed evidence 改为写入并绑定 PM decision approved material（不再使用 candidate pack material_id），新增 `material_target/material_type/approval_status/select_workflow_run_id` 元数据；confirmation 增加 PM decision target/type 强校验与“decision material 不得等于 candidate pack material”拦截；补充“candidate pack material 冒充应拒绝”与“真实 `/select` evidence 可通过 confirmation”测试并通过。
-决策：为兼容旧 evidence，若未携带 `material_target/material_type`，仅在 `approved_material_id` 命中 `selection-pm-decision-` 前缀时允许继续；新 evidence 路径仍要求 target/type 明确指向 PM decision 语义。
待办：无。

## 11:45 [sel-09-third-rework-implementation-subagent]
-完成：完成 SEL-09 third rework：移除 confirmation 中基于 `approved_material_id` 前缀的 PM decision 兜底，改为 `material_target=selection_portfolio_decision` 与 `material_type=pm_decision` 精确配对；补齐缺字段/交换值/错误值拒绝测试，并把 idempotency 合法 evidence 更新为显式完整 target/type。
-决策：保持 `/report` workflow 与 PM 业务逻辑不变，只在 selection confirmation gate 做 fail-closed 收紧；`/select` 真实 evidence 路径通过 `test_select_command_chat_flow` 回归确认未受破坏。
待办：无。

## 12:13 [sel-10-implementation-subagent]
-完成：完成 SEL-10 测试矩阵补齐：新增 unit/contract/integration 关键缺口测试（PM 决策解析负例、`/select` vs `/report` 入口与 `single_worker_minimal` 合约、tool runtime context 失败路径、worker 失败停止下游路径），并补充 fake/test double 仅状态机/控制流证明声明；必跑三组测试均通过。
-决策：严格限制在测试与 evidence 范围内，不修改生产代码；将 runtime/live/provider payload 最终验收明确保留给 SEL-11，避免用 fake 集成测试冒充 live 结论。
待办：SEL-11 执行真实 provider payload focused run 与 live 验收闭环。

## 12:15 [sel-11-implementation-live-subagent]
-完成：执行 SEL-11 必跑命令后，`tests/contracts/test_selection_provider_payload.py` 通过，但 `tests/integration/selection/test_select_live_acceptance.py` 在首个 strategist dispatch 失败并触发 payload 缺失；补充 focused runtime 复核确认根因为 selection dispatch command 与 `agent.runSingleWorker` 参数契约不兼容，已产出 `docs/evidence/a-share-select-live-2026-05-26.md`，当前结论为 `BLOCKED_ASK_HUMAN`。
-决策：不改 `src/**` 与 `third_party/openclaw/**`（超出本任务允许范围），不使用静态 render/log/exporter 或 fake pass 伪造 SEL-11 通过；按 stop condition 终止并上报真实失败证据。
待办：需人类确认是否允许进入 SEL-07/SEL-11 生产修复（`agent.runSingleWorker` command payload 契约对齐）后再重跑 live 验收。

## 13:18 [sel-11-runtime-registration-subagent]
-完成：完成 SEL-11 当前 rework：把四个 `selection_*` worker 纳入 `start-control-runtime.sh` 自动注册，并把 `claw-trade-selection-tools` 纳入 runtime 启动插件加载；重跑后 `openclaw.mjs agents list --json` 已出现四个 selection agent，live run 也已生成四个 dispatch 的 provider payload/visible tools/tool calls，不再出现 `Unknown agent id "selection_strategist"`。
-决策：保持最小范围，不改算法/provider/hard gate/OpenClaw 源码；`tests/integration/selection/test_select_live_acceptance.py` 去掉运行期 `agents add` 兜底，改为缺失即失败，防止手工注册伪通过。
待办：SEL-11 仍未全绿，当前新阻塞为 `selection_result_invalid:missing_required_sections`（PM 输出语义未通过），需后续任务在非本次注册范围内处理。

## 13:57 [sel-11-enter-report-empty-rework-subagent]
-完成：完成 SEL-11 `enter_report empty` 取证与重跑验收：读取旧 completed run（`2dc85225`、`70df318f`）的 workflow/provider/raw/tool 证据确认空 `enter_report` 根因是候选包不可读导致上游评审失败链；在统一 runtime 脚本下重跑两条必跑命令均通过，最新 completed run `9826dccf` 已产出 `enter_report=["600519.SH"]`、reader artifact 与 confirmation handoff evidence。
-决策：按来源行判定“产品合同允许 enter_report=0-3，但 SEL-11 focused live 验收场景要求 confirmation handoff 证据”，因此测试在该场景断言 non-empty 合法；本轮不改 `src/**` 或 `third_party/openclaw/**`，仅更新 SEL-11 evidence 文档与 memory。
待办：无。

## 14:16 [sel-11-review-rework-subagent]
-完成：完成 SEL-11 review rework 三项：移除 `test_select_live_acceptance.py` 中 fake `_WorkflowRunnerForHandoffEvidence` 并改用真实 `ReportTaskQueue + ReportWorkflowBridge + _ControlWorkflowRunner` 产出 handoff；补齐 `docs/evidence/a-share-select-live-2026-05-26.md` fixed runtime preflight table；补充“用户已批准在 runtime 注册四个 selection worker”记录。两条 SEL-11 必跑命令均通过（`10 passed`、`1 passed`）。
-决策：handoff 证据边界明确为“真实创建 report task/run + handoff request”，不把完整 `/report` 终态当作 SEL-11 handoff 必要条件；拒绝回退 fake task id/fake runner。
待办：无。

## 14:24 [sel-11-evidence-only-rework-subagent]
-完成：按 SEL-11 剩余 review 问题补全文档证据：在 `docs/evidence/a-share-select-live-2026-05-26.md` 的 Fixed Runtime Preflight 表新增两组 AGENTS 12.1 必填 unset 行（MCP module/cwd、server bin/cwd），并标注该表为总管 preflight 复验证据、非测试替代。
-决策：本次仅做证据文档补全，不改业务代码、不改测试、不运行 live。
待办：无。

## 14:31 [sel-11-evidence-only-rework-subagent]
-完成：继续补齐 SEL-11 最终 review 缺口：在 `docs/evidence/a-share-select-live-2026-05-26.md` 新增 `runtime.env` 存在性检查、既有 runtime health 先验检查（1933/18789 均 exit 7）、不可复用后改用 `scripts/start-control-runtime.sh -- <command>` 的重启/启动决策链，并明确最终验收 run 仅认 `select-20260526T120000-sel-11-live-acceptance-5e084927`，旧 run 仅作中间排障证据。
-决策：严格限定为 evidence-only 文档补全；不改 `src/**`、`agents/**`、`tests/**`、`scripts/**`、`third_party/**`，不运行 live，不引入 mock/stub/fake/fallback 叙述。
待办：无。

## 14:44 [sel-11-independent-testing-subagent]
-完成：按 SEL-11 仅执行两条必跑命令并收集证据：`test_selection_provider_payload.py` 通过，`test_select_live_acceptance.py` 失败于 `enter_report empty`；最新 live run 为 `select-20260526T120000-sel-11-live-acceptance-f836730c`，4 个 worker dispatch 的 provider payload/visible tools/tool calls/raw output 文件均存在。
-决策：严格不改代码与测试；仅在沙箱本地监听失败后按同一命令升级 `require_escalated` 重跑；以最新 run 证据判定 reader artifact 与 confirmation handoff evidence 未生成（因断言提前失败）。
待办：需实现侧修复 PM 决策阶段使 `decision.enter_report` 非空后重跑 SEL-11 live 验收。

## 14:59 [sel-11-enter-report-instability-rework-subagent]
-完成：完成 SEL-11 enter_report instability 修复并通过必跑命令：最小收紧 `selection_skeptic/selection_manager/selection_portfolio_manager` 提示词的数据缺口与分组边界契约，重跑后 live acceptance 通过；最新验收 run `select-20260526T120000-sel-11-live-acceptance-af420e37` 产出非空 `enter_report`、final reader artifact 与 confirmation handoff evidence。
-决策：根因判定为上游提示词输出契约不闭合（数据缺口被放大为全体降级），不是 OpenClaw payload/tool 边界问题；保持不改 `third_party/openclaw/**`、不改算法/provider/hard gate/PM 决策权边界，仅做 selection prompt 最小修复并更新 SEL-11 证据文档。
待办：无。

## 15:31 [sel-11-upstream-material-instability-rework-subagent]
-完成：完成 SEL-11 upstream material/provider prompt 注入修复：skeptic provider-visible prompt 现已稳定包含 `【approved_strategist_l1】` 与已批准正文；两次 live acceptance 连续通过，最终验收 run 为 `select-20260526T120000-sel-11-live-acceptance-646664fc`（另一次通过 run：`32c33d74`）。
-决策：不改 `third_party/openclaw/**`，仅在 selection dispatch/evidence contract 做最小修复；为消除 `enter_report empty` 不稳定，补充 manager/PM 提示词“无明确逐条反证不得输出 enter_report=0”的约束，保持 Python 不代写投资结论。
待办：无。

## 15:40 [sel-11-evidence-only-rework-subagent]
-完成：按 review 要求仅补全文档证据：重写 SEL-11 preflight 必填表（含 runtime.env、既有 1933/18789 health 先验检查、复用/重启决策、unset 检查、固定配置路径、`CLAW_TRADE_OPENVIKING_MCP_STARTED=0`、启动后双 health=200），并补充最终验收 run 口径、manager/PM 非空倾向约束作用域、`third_party/openclaw` dirty 无关性说明。
-决策：本次严格 evidence-only，不改业务代码、不改测试、不运行 live；仅编辑 `docs/evidence/a-share-select-live-2026-05-26.md` 与本 memory 条目。
待办：无。

## 15:52 [sel-11-final-independent-testing-subagent]
-完成：按 SEL-11 仅执行两条必跑命令并取证；contract 命令通过，live acceptance 命令失败，最新 run `select-20260526T120000-sel-11-live-acceptance-02a91b63` 因 `selection_result_invalid:forbidden_trade_term:持有` 未完成，导致本次 run 未产出 final `/select` artifact 与 confirmation handoff evidence。
-决策：严格不改代码/测试/evidence；启动阶段在沙箱下退出码 1 后，按同一 `scripts/start-control-runtime.sh -- ...` 命令使用 `require_escalated` 复跑取到有效 pytest 结果；结论标记为 `FAILED_REWORK_REQUIRED`。
待办：实现侧修复 PM 决策输出语义（避免触发 `forbidden_trade_term`）后重跑 SEL-11 live acceptance。

## 17:49 [sel-11-gate-cleanup-implementation-subagent]
-完成：完成 SEL-11 gate cleanup 最小修复：`selection/controller.py` 将 PM 决策校验从全文禁词扫描改为“显式交易建议模板”检测，放行“无法证明同时持有两支的合理性”这类普通语境；补充/更新 `tests/unit/selection/test_decision_parser.py` 回归与越界失败样本；同步 `docs/A股选股总体设计.md` 与 `docs/A股选股详细设计.md` 验收口径为“结构/语义边界，不扫普通词面”。
-决策：保持 `/select` 三分类产品边界与 fail-closed，不改 `/report` 逻辑，不改 selection 算法/provider/data gateway，不引入 fallback/mock/fake；只拦“建议买入/卖出/持有、投资建议、最终交易建议、目标价、止损价、仓位、交易计划”等明确模板。
待办：由 testing subagent 基于本次修复重跑 SEL-11 live acceptance（含 provider payload 与 handoff 证据链）。

## 18:03 [sel-11-gate-cleanup-testing-subagent]
-完成：按 SEL-11 规定仅执行两条必跑命令并取证；contract `11 passed`、live acceptance `1 passed`，最新 run `select-20260526T120000-sel-11-live-acceptance-9f43decf` 已产出 4 worker provider payload、final `/select` reader artifact 与 confirmation->`/report` handoff evidence，结论 `PASSED`。
-决策：严格不改业务代码；沙箱下 runtime 启动失败后按同命令 `require_escalated` 重跑，最终证据仅采用真实 provider payload/workflow/handoff 路径，不使用 mock/stub/fake/fallback 或 capture-only 结案。
待办：无。

## 19:13 [sel-12-ui-completed-selection-run-implementation-subagent]
-完成：完成 SEL-12 最小实现：`selection/store.py` 新增 completed run 持久化与启动恢复（persisted record + data job completed evidence 兼容恢复）；`selection/data_job.py` 补全 completed evidence 的 run/pack 关键字段；`web/state.py` 与 `chat_controller.py` 接入 restored selection store，避免 UI `/select` 因空内存 store 必然 no-run；新增 `tests/integration/selection/test_store_persistence_restore.py` 覆盖 evidence/record round-trip 与 stale/hash/readback/manifest/lineage fail-closed，并通过 focused 回归。
-决策：保持 fail-closed，不在 `/select` 请求内触发 data job/provider；不改 `third_party/openclaw/**`、不改 `/report` PM 逻辑、不改 selection 算法参数；旧 evidence 缺少部分字段时仅按可审计路径恢复，无法构造真实 completed record 时继续不可用。
待办：由总管在真实 Chrome MCP UI 验收 `/select` 是否已不再因空内存 store 固定报 `no_completed_selection_run`。

## 19:19 [sel-12-ui-completed-selection-run-testing-subagent]
-完成：按 SEL-12-UI-COMPLETED-SELECTION-RUN-TESTING 要求执行 6 条必跑命令并全部通过（`test_store_persistence_restore`、`test_latest_completed_gate`、`test_chat_controller`、`test_data_job_pipeline`、`test_selection_provider_payload`、`test_select_command_chat_flow`），并更新 `docs/evidence/sel-12-ui-completed-selection-run.md` 补齐完整测试证据。
-决策：本次仅做测试与证据汇总，不修改 `src/**`、`agents/**`、`third_party/**`、report workflow、selection 算法；保留 fail-closed 口径，且不启动真实 Chrome MCP（按任务要求由总管执行）。
待办：总管执行 Chrome MCP 验收并汇总 UI 实机证据。

## 19:28 [manager-real-chrome-select-check]
-完成：按人类要求启动真实 Windows Chrome（非 headless）并由 MCP 接管，访问 `http://127.0.0.1:5175/` 后发送 `/select`；真实 UI 返回“`/select` 当前不可用：没有可用的已完成选股批次。”，截图证据为 MCP 输出 `claw-trade-select-no-completed-run-20260526.png`。
-决策：该结果证明 UI 入口和 fail-closed 表现真实存在，但不构成 `/select` 成功验收；当前 `runs/selection` 没有可由 UI 恢复的真实 completed selection data run，SEL-03 证据也明确是 integration fixture/合同证明，不是 live/provider 生产批次。不得用测试夹具或手工假 record 冒充真实 completed run。
待办：需要补齐真实 selection batch provider/data job 生产入口并产出 completed run 后，再做 Chrome 成功路径验收。

## 20:59 [sel-13-implementation-subagent]
-完成：完成 SEL-13 范围阻塞审计并新增 `docs/evidence/sel-13-removed_data_gateway-data-gateway-batch-blocked-2026-05-26.md`，确认当前仓库在不绕过 已删除数据网关/data_gateway 的前提下尚无已批准可执行的 A 股全市场 batch 能力闭环，结论 `BLOCKED`。
-决策：不做猜测性代码接线；在 `docs/A股选股实施任务清单.md` 追加 SEL-13 条目并明确 stop conditions 与 fail-closed 口径，保持 `third_party/openclaw/**`、`/report` PM 逻辑、selection 算法参数不变。
待办：需人类确认并批准 selection batch provider capability、approved strategy config 来源、交易日历来源后，才能继续 SEL-13 实装与测试。

## 21:16 [sel-13-rework-subagent]
-完成：按人类批准三条口径完成 SEL-13 实装：新增 CN_A selection batch capability（akshare/eastmoney spot batch）、`selection/provider_batch.py`（batch plan + data_gateway fetch + trade date resolver + approved strategy loader）、SEL-13 focused tests，并补 `docs/evidence/sel-13-removed_data_gateway-data-gateway-batch-2026-05-26.md`。
-决策：selection batch 通过 data_gateway registry+adapter 执行；字段不足/空结果/remote error 全部 fail-closed，不生成 completed run；`/select` 继续 latest-completed-only，不在请求路径现场拉全市场。
待办：真实 provider smoke 目前记录为 `selection_batch_remote_error`（远端断连），需后续在可用网络/源稳定窗口补 live 成功证据。

## 21:27 [sel-13-independent-review-subagent]
-完成：完成 SEL-13 rework 独立审查（只读），已核对 AGENTS、SEL-03/08/11/12/13 任务条款、SEL-13 两份 evidence、指定实现与测试文件，并复跑 `tests/unit/selection/test_provider_batch_strategy_and_trade_date.py`、`tests/integration/selection/test_sel13_provider_batch_glue.py`、`tests/integration/selection/test_latest_completed_gate.py` 全通过。
-决策：判定当前实现满足“registry/adapter 接线、字段缺失 fail-closed、`/select` latest-only 不现场拉数”，但不满足 SEL-13 对 Mongo/OpenViking/raw-http 证据链与 AGENTS live preflight 验收口径，结论应为 `REWORK_REQUIRED`。
待办：按审查清单补齐 data_gateway 证据链落库与 preflight 合规 live 验收后再提交复审。
## 21:41 [sel-13-review-rework-subagent]
-完成：完成 SEL-13 review 指定返工：`provider_batch` 改为走 data_gateway ProviderExecutionEvidenceHelper + Mongo raw/http/attempt/normalized 证据链，`attempt_refs/normalized_refs` 改为可追溯 Mongo 的 selection refs；`market_adapters.py` 已切片仅保留 CN_A selection batch 必需变更并移除 HK/US/crypto 非 SEL-13 变更；补充 smoke CLI 接口 `uv run python -m claw_trade.selection.provider_batch ...`（可被 `scripts/start-control-runtime.sh --` 调用）并更新 evidence 文档口径为“smoke 仅调试证据”。
-决策：为兼容既有 candidate pack lineage 合同，selection 侧引用保持 `attempt://`/`normalized://` 前缀但内含 Mongo collection/document 路径（`attempt://mongo/removed_data_gateway_provider_attempts/...`、`normalized://mongo/removed_data_gateway_normalized/...`）；Mongo 未配置时返回 `selection_batch_evidence_store_unavailable` 并 fail-closed，不伪造 completed run。
待办：若后续 review 要求 SEL-13 也闭环 OpenViking material 写入，需在 selection batch 层补最小 OpenViking evidence glue 后再重跑 preflight/live 验收。

## 21:46 [sel-13-independent-review-subagent]
-完成：完成 SEL-13 第二轮独立审查并复跑 focused 测试/命令，确认当前 rework 仍存在“OpenViking 证据链未闭环 + attempt ref 可伪造”两项阻断，结论 `REWORK_REQUIRED`。
-决策：按 SEL-13 明确“provider registry/Mongo/OpenViking 必须闭环”与“禁止 fake/fallback 冒充 runtime/live”口径，判定本轮仅可作为 Mongo 接线进展，不能作为通过结案；`/select` latest-completed-only 与 CN_A-only diff 则判定通过。
待办：补齐 OpenViking material 写入与 attempt ref 真实可追溯性（含 evidence-store-unavailable/attempt-write-failed 路径）后，使用 preflight runtime 命令重跑 live 验收并更新 evidence 文档。

## 22:01 [sel-13-implementation-rework-subagent]
-完成：完成 SEL-13 二审两项 P0 返工：`provider_batch.py` 移除占位 refs，新增 Mongo attempt/normalized/raw/http 可读校验，只返回真实可读 ref；新增 selection batch OpenViking evidence material 写入 + stat/readback/hash/size 校验，并把真实 `viking://...` ref 并入 evidence；指定 4 组 pytest 命令全部通过。
-决策：Mongo 或 OpenViking evidence 不可用时统一 fail closed（含 `selection_batch_openviking_evidence_unavailable`），不生成 completed run；不改 `/select` latest-completed-only、不改算法权重/阈值/排序、不改 report 逻辑。
待办：按 AGENTS preflight gate 走 `scripts/start-control-runtime.sh -- ...` 做 live/provider 验收并补 live 证据（当前文档仅 unit/integration + smoke）。

## 22:09 [sel-13-third-review-rework-subagent]
-完成：完成 SEL-13 三审 P0 返工：修复 `provider_batch.py` 中“REMOTE_SUCCESS 但 evidence chain/readback 失败仍可能返回 rows”的缺陷；现已强制 fail closed（`rows=()`）并落 blocker data gap；新增 focused contract test `test_sel13_fetch_selection_batch_remote_success_but_evidence_chain_failed_is_fail_closed`，并重跑指定 4 组 pytest 全通过。
-决策：保持最小改动，仅收敛成功分支条件与失败路径，不新增 fallback/占位 ref，不改 `/select` latest-only、算法参数、report 逻辑；测试中 fake store/backend 仅用于 contract 证明，不作为 live/runtime 证据。
待办：仍需按 preflight gate 执行 live/provider 验收补齐最终 runtime 证据。

## 22:21 [sel-13-live-provider-testing-subagent]
-完成：执行 SEL-13 live/provider smoke（`sel13-live-provider-20260526`）并完成只读证据核查；真实 runtime 下已产出可读 `attempt://mongo/...` 与 `viking://...selection-batch-evidence.json`，但 provider attempt 状态为 `remote_error`，`rows=0`、`normalized_refs=[]`，结论 `FAILED_REWORK_REQUIRED`。
-决策：严格不改 `src/**`/`tests/**`/`third_party/**`；不使用 mock/stub/fake/fallback/capture-only；将沙箱内端口受限结果与提权后真实执行结果分开记录，最终结论仅基于提权后真实 readback 证据。
待办：需要实现侧排查并修复 `akshare_eastmoney_selection_batch` 远端断连（`RemoteDisconnected`）后重跑同一 live/provider 命令，目标是 `rows>0` 且产出可读 `normalized://mongo/...` 证据。

## 23:52 [sel-13-provider-adapter-approval-rework-subagent]
-完成：按人类批准口径完成 SEL-13 最小返工：selection batch 命名改为 EastMoney 口径（`eastmoney_selection_batch` / `project.cn_a.eastmoney_selection_batch`），`industry/f100` 从阻断字段降级为质量字段，保留有效行；在 normalize compact_facts 写入 `source_row_count/valid_row_count/dropped_row_count/dropped_row_ratio`，并在 `selection/provider_batch.py` 将真实 dropped rows 落为 `selection_batch_rows_dropped`（WARN）可审计 data gap；4 组指定 pytest 全通过。
-决策：保持 `/select` 只读 latest-completed-run，不在请求链路直连 EastMoney；`selection/provider_batch.py` 继续只走 data_gateway provider registry + adapter + Mongo/OpenViking evidence 链；有效行=0、关键字段全缺、远端或证据链失败仍 fail-closed。
待办：本轮未重跑 live/provider preflight 命令，后续若需 live 通过证据需以新命名再跑 `scripts/start-control-runtime.sh -- ... provider_batch` 并更新 `docs/evidence/sel-13-live-provider-20260526.json`。

## 00:00 [sel-13-smoke-cli-exit-semantics-rework-subagent]
-完成：完成 SEL-13 smoke CLI 退出语义最小修正：`selection/provider_batch.py` 的 `_smoke_cli_run` 改为“有有效 rows 且无 BLOCKER data_gap 返回 0；无 rows 或有 BLOCKER 返回 2”，并保留 `data_gap_codes` 输出且新增 `data_gap_severities`；新增 3 个 unit 测试覆盖 WARN/BLOCKER/空行分支。
-决策：遵循人类批准口径，WARN（如 `selection_batch_rows_dropped`）不再把合规 smoke 判为失败；仍保持 BLOCKER 一票否决与 fail-closed 边界，不改 data_gateway adapter、不改算法/排序、不改 report/OpenClaw 路径。
待办：无。

## 00:10 [sel-13-final-testing-live-provider-subagent]
-完成：按 SEL-13 final testing 要求完成 4 组必跑 pytest（全通过），并执行新命名 live/provider smoke（`sel13-live-provider-eastmoney-20260526`）与只读证据回查：提权后拿到真实 `attempt://mongo/...project.cn_a.eastmoney_selection_batch...` 与 `viking://...selection-batch-evidence.json`，但 attempt `status=remote_error`、`rows=0`、`normalized_refs=[]`，结论 `FAILED_REWORK_REQUIRED`。
-决策：严格不改 `src/**`、`tests/**`、`third_party/**`；只在证据文件追加本次 live/provider 章节并记录沙箱失败与提权真实结果分离口径；结论仅基于真实 runtime + Mongo/OpenViking 只读 readback，不使用 mock/stub/fake/fallback/capture-only。
待办：实现侧需排查并修复 EastMoney batch 远端断连（`ConnectionError: RemoteDisconnected`），修复后重跑同一 live/provider 命令，目标 `rows>0` 且产出可读 `normalized://mongo/...`。
