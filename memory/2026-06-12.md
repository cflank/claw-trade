## 数据源能力释放与限流重组进展

- 完成：补齐 SDK internal HTTP 风险清单 `src/claw_trade/data_gateway/providers/sdk_risk_inventory.py`，所有 `sdk_internal_unknown` provider/catalog endpoint 都必须登记残余风险、迁移优先级和可见性口径。
- 完成：A 股实时 `quote_snapshot` 的 DataNeed resolver 不再只走 mootdx，已接入 EastMoney `eastmoney.push2.spot_quote_batch`、AkShare `akshare.stock_zh_a_spot_em`、mootdx `mootdx.quote_snapshot`；`order_book_snapshot` 接入 mootdx `mootdx.order_book_snapshot`。
- 验证：`tests/unit/data_gateway/test_data_need_planner.py` 13 passed；限流重组合同组 35 passed；前线数据工具相关回归 440 passed, 1 skipped；`git diff --check` 通过。
- live 证据：固定 runtime 提权运行 `scripts/start-control-runtime.sh -- uv run python -c ...`，`quote_snapshot` planned=3/scheduled=3，EastMoney 与 AkShare 成功返回 600519.SH 实时行情，mootdx 超时；`order_book_snapshot` planned=1/scheduled=1，但 mootdx `mootdx_fetch_timeout:6s`，盘口仍缺。
- 决策：A 股行情快照现在不是“没源”或“没规划”，而是部分可用；盘口快照仍是真实 provider 失败缺口。后续要么增加非 mootdx 盘口源，要么把 mootdx SDK internal unknown 迁到可控 managed_http/可诊断实现。

## 官方接口目录修正与 Coinglass funding 入库

- 完成：删除假的 Tushare Pro HTTP 实时接口目录/路由。live 直连已证明 `api_name=realtime_quote` 对 `https://api.tushare.pro` 返回 `40101 请指定正确的接口名`；Tushare Python 包里的实时函数是 SDK 路径，不能冒充 Pro raw API。
- 完成：A 股 `quote_snapshot` 现在 planned=3：AkShare、EastMoney、mootdx；live 中 AkShare/EastMoney 成功入库，mootdx 超时。A 股 `order_book_snapshot` planned=1：mootdx；live 仍超时。
- 完成：Coinglass official funding parser 接入 `/api/futures/funding-rate/oi-weight-history`，把返回的 `time/open/high/low/close` 标准化为 `crypto_derivative_metric.funding_rate`，`close` 为资金费率值。
- 完成：official planner 对 Coinglass `start_time/end_time` 使用毫秒，并补 `limit=1000`；`end_time` 对 date 使用当天结束毫秒，和既有结构化 Coinglass provider 口径一致。
- 完成：official batch 把 `1h`/`60m` 规范为 `hourly`，避免 worker 写 `1h` 时 normalizer 因粒度不一致丢行。
- 防坏数据：Tushare daily parser 现在跳过开高低收量核心字段为空/不可解析的行，避免无效 OHLCV 写入标准库。
- 验证：`uv run pytest ...` 数据层/前线宽回归 278 passed；`git diff --check` 通过。
- live 验证：fixed runtime `scripts/start-control-runtime.sh -- uv run python -c ...` 跑 BTC funding，结果 `status=ready`，planned=1/scheduled=1，Coinglass raw ref 有，normalized refs 有，gaps 为空。
- 决策：Tushare 实时能力不是删除，而是尚未以正确 SDK/managed_http 方式接入；当前禁止的是已证伪的 Pro HTTP 假接口。

## A 股 Tushare SDK 实时行情/盘口接入

- 完成：新增 `cn_a_tushare_realtime` provider，通过 Tushare 官方 SDK `realtime_quote` 获取 A 股实时行情；同一个 SDK 返回里解析五档盘口，标准化为 `quote_snapshot` 和 `order_book_snapshot`。
- 完成：DataNeed resolver 的 A 股 `quote_snapshot` 现在尝试 Tushare SDK、EastMoney、AkShare、mootdx；`order_book_snapshot` 现在尝试 Tushare SDK、mootdx。Tushare SDK 与 `official_api_tushare` 共用 `ratelimit:tushare` 和 settings 里的 `tushare` 凭证/限流归属。
- 完成：删除/避免使用被 live 证伪的 Tushare Pro HTTP 实时假接口；SDK internal HTTP 风险按 `sdk_internal_unknown` 记录，不宣称子请求级 managed_http 已完成。
- 完成：`claw_request_data` 的 provider attempt summary 补 `provider_id`，排查时能直接看到哪个数据源成功/失败。
- 验证：`uv run pytest tests/unit/data_gateway/test_provider_registry.py tests/unit/data_gateway/test_provider_selector.py tests/unit/data_gateway/test_data_service_flow.py tests/contracts/test_provider_capabilities.py tests/contracts/test_provider_adapter_inventory.py tests/contracts/test_data_need_tool_contract.py tests/unit/data_gateway/test_cn_a_tushare_realtime_sdk_provider.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/reports/test_data_pack_bridge.py -q -p no:cacheprovider`，结果 217 passed, 1 warning。
- live 证据：fixed runtime `scripts/start-control-runtime.sh -- uv run python -c ...`，600519.SH `quote_snapshot` planned=4/scheduled=4；Tushare SDK、EastMoney、AkShare 成功入库，mootdx `mootdx_fetch_timeout:6s`。`order_book_snapshot` planned=2/scheduled=2；Tushare SDK 成功入库，mootdx 超时。
- 当前真实状态：A 股盘口不再“只有 mootdx”；Tushare SDK 已能提供盘口。剩余问题是 mootdx SDK internal unknown 仍超时，需要另做 managed_http/诊断迁移，不能算本轮已解决。
- review 修正：Tushare SDK 返回的 `DATE+TIME` 已按 `Asia/Shanghai` 写入时区；SDK internal unknown 成功入库时不再标 `remote_success=True`，attempt 记录新增 `http_visibility=sdk_internal_unknown` 和 `http_audit_status=sdk_internal_unknown`，避免把 SDK 内部 HTTP 成功误读成 managed_http 可审计成功。

## DataNeed resolver 删除中央接口白名单

- 完成：删除 `src/claw_trade/data_gateway/planner/need_resolver.py` 里的 `_NEED_ENDPOINT_INDEX` 中央“需求到接口 ID”路由表。
- 完成：新的 resolver 不再按 report/select/worker/consumer 或固定 endpoint 清单决定候选；它按接口自身输出事实和 provider capability 匹配市场、数据类型、粒度。
- 完成：官方 catalog 通过 `response_shape.outputs` 登记接口产出事实。已补 Tushare、Coinglass、Finnhub、FRED、Glassnode、CoinGecko Pro 当前 catalog 端点的市场、数据类型、粒度、字段、优先级。
- 完成：保留 provider capability 对公共/SDK provider 的动态匹配，避免再维护另一张 resolver 路由表。
- 防回潮测试：`test_resolver_has_no_central_need_to_endpoint_route_table` 禁止 `_NEED_ENDPOINT_INDEX` 回来；`test_catalog_endpoint_enters_candidates_from_endpoint_output_fact_without_resolver_route` 证明新增 catalog 接口只要声明输出事实就自动进入候选。
- 验证：`uv run pytest tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_official_api_plugin.py -q -p no:cacheprovider`，48 passed。
- 修正：`exchange_netflow` 匹配逻辑收紧为“需求 token 必须出现在接口字段/端点 token 中”，避免因为需求名包含 `exchange` 误匹配 `open-interest/exchange-list`。
- 验证：数据层/前线相关回归含 BTC no-fixed-17-pack 集成测试 `324 passed, 1 warning`。

## 固定资料包远端请求入口删除

- 完成：删除 `src/claw_trade/reports/data_pack_bridge.py` 里的旧 `_build_requests` 固定请求生成器，以及旧 `DataRequest` / `build_data_api_from_env` 预取路径。
- 完成：删除旧报告预取 worker 路径 `_fetch_report_prefetch_domain`、`_report_prefetch_domain_worker`、`_DomainPrefetchOutcome`、`_report_prefetch_domain_timeout_seconds`。报告预取现在显式返回 `report_prefetch_disabled_use_data_need`。
- 完成：`run_frontline_data_pack` 旧入口已删除。
- 完成：`_CN_A_DOMAIN_DATASETS`、`_US_DOMAIN_DATASETS`、`_HK_DOMAIN_DATASETS`、`_CRYPTO_DOMAIN_DATASETS` 等固定包定义表已删除。
- 防回潮测试：BTC 集成测试改为断言 `run_frontline_data_pack` 不存在；前线插件合同断言只注册 `claw_request_data`。
- 验证：`uv run pytest tests/unit/reports/test_data_pack_bridge.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_data_need_tool_contract.py tests/contracts/test_frontline_tool_protocol.py -q -p no:cacheprovider`，97 passed；`git diff --check` 通过。

## DataNeed 执行边界修复

- 问题：把 DataNeed 执行逻辑放在 `reports/data_pack_bridge.py` 后，报告层直接 import 了 scheduler、planner、runtime、official_api、crypto_lens 等数据层内部模块，违反数据访问边界。
- 修复：新增/扩展数据层公共出口 `src/claw_trade/data_gateway/report_evidence.py`，由它承接 DataNeed 规划、调度、fetch、ingest、official/structured batch 构造和 CryptoLens 代理调用。
- 修复：`reports/data_pack_bridge.py` 现在只调用 `report_evidence.run_data_need_tool_request`，再做图表补充和 worker 可见文本拼装；不再直接 import 数据层内部模块。
- 修复：BTC no-fixed-pack 集成测试改为 monkeypatch 新的数据层公共出口，不再要求 reports 模块保留旧 runtime builder。
- 验证：`uv run pytest tests/contracts/test_data_access_boundary.py tests/unit/reports/test_data_pack_bridge.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py -q -p no:cacheprovider`，49 passed。
- 验证：限流重组相关回归 `134 passed`；`git diff --check` 通过。

## Legacy AlphaEar 残留删除

- 完成：删除 `agents/market_analyst/skills/alphaear-stock/` 和 `agents/market_analyst/skills/alphaear-techlab/`，包括旧脚本、测试、本地 `signal_flux.db` 和 pycache 残留。
- 完成：删除旧 worker facade `src/claw_trade/data_gateway/agent_tools.py`，以及对应 `tests/unit/data_gateway/test_agent_tools.py`、`tests/contracts/test_market_skill_entrypoint_invocation.py`。
- 保留能力：把纯指标/图表计算从 legacy skill 迁到正式模块 `src/claw_trade/reports/market_indicators.py` 和 `src/claw_trade/reports/market_charts.py`；它们只消费已返回 OHLCV 行，不出网、不读旧本地库。
- 修复：`reports/data_pack_bridge.py` 不再动态加载 `agents/.../alphaear-techlab`，改用正式 reports 模块生成指标和图表。
- 合同：`test_market_analyst_does_not_mount_legacy_alphaear_data_skills` 现在同时断言 legacy skill 目录不存在；`test_data_access_boundary.py` 不再允许 `claw_trade.data_gateway.agent_tools`。
- 文档：更新 `docs/架构设计.md`、`docs/详细设计.md`、`docs/data-layer-unification-plan.md`、`docs/data-access-boundary-audit.md`，非 legacy 文档不再把 AlphaEar/agent_tools 写成现状。
- 验证：`uv run pytest tests/unit/reports/test_data_pack_bridge.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_data_need_tool_contract.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_rate_limit_policy.py tests/contracts/test_frontline_tool_contract.py tests/contracts/test_data_access_boundary.py -q -p no:cacheprovider`，131 passed。
- 验证：`uv run python -m compileall -q src/claw_trade/reports src/claw_trade/data_gateway/report_evidence.py` 通过；`git diff --check` 通过。

## 旧 pack 工具注册删除

- 完成：OpenClaw 前线插件只注册 `claw_request_data`；`claw_get_market_pack`、`claw_get_fundamental_pack`、`claw_get_news_pack`、`claw_get_social_pack`、`claw_get_policy_pack`、`claw_get_hot_money_pack`、`claw_get_lockup_pack` 不再注册。
- 完成：`policy_analyst`、`hot_money_tracker`、`lockup_watcher` 的 CN_A stage intent 改为 `cn_a_policy_data`、`cn_a_hot_money_data`、`cn_a_lockup_data`，最终可见工具都解析为 `claw_request_data`。
- 完成：插件和 guard 的 worker 可见错误文案从“资料包工具”改为“数据工具”；审计字段从 `data_pack_status` 改为 `data_result_status`。
- 合同：前线插件合同、provider payload 合同、CN_A 新 worker prompt 对齐合同都要求模型可见数据工具只有 `claw_request_data`。
- 验证：`uv run pytest tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_frontline_tool_contract.py tests/contracts/test_tool_registry_contract.py tests/contracts/test_provider_payload_visible_tools.py tests/contracts/test_prompt_alignment_cn_a_workers.py tests/contracts/test_frontline_prompt_write_contract.py tests/contracts/test_stage_tool_policy_contract.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/unit/reports/test_data_pack_bridge.py tests/unit/data_gateway/test_provider_selector.py tests/unit/test_openclaw_client.py -q -p no:cacheprovider`，170 passed。

## 旧 pack 命名和运行残留清理

- 完成：`reports/data_pack_bridge.py` 改名为 `reports/data_need_bridge.py`，测试文件同步改为 `tests/unit/reports/test_data_need_bridge.py`；OpenClaw 前线插件 embedded Python 现在 import `claw_trade.reports.data_need_bridge.run_claw_request_data`。
- 完成：CRYPTO downstream/report_polisher/market skill 等模型可见提示词里的“资料包”统一改成“数据结果”，不再让模型按固定 pack 概念理解数据材料。
- 完成：删除旧 `frontline_data_pack_domain_timeout` 兼容识别和旧英文错误映射；删除旧专用脚本 `scripts/validation/diagnose_frontline_data_pack_timeout.py`。
- 完成：`scripts/run_cn_a_social_fresh_acceptance.py` 旧验收工具检查从 `claw_get_social_pack` 改为 `claw_request_data`。
- 扫描：`src/`、`agents/`、`openclaw_plugins/`、`scripts/` 已无 `data_pack_bridge`、`run_frontline_data_pack`、`frontline_data_pack_domain_timeout`、旧 `claw_get_*_pack` 运行入口或模型提示词“资料包”残留；旧名只保留在测试防回潮断言和历史文档/历史 memory。
- 验证：`uv run pytest ...` 前线/数据需求/边界重点回归分别为 `180 passed`、`210 passed, 1 skipped`；`uv run python -m compileall -q src/claw_trade/reports src/claw_trade/guards src/claw_trade/config`、`uv run python -m py_compile scripts/run_cn_a_social_fresh_acceptance.py src/claw_trade/reports/data_need_bridge.py`、`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、`git diff --check` 均通过。
- 完成：active docs 中的旧 `claw_get_*_pack`、`data_pack_bridge`、`test_data_pack_bridge`、中文“资料包”口径已清理为 `claw_request_data` / `data_need_bridge` / “数据结果”；legacy 文件 `frontline工具资料包改造说明.md` 改名为 `frontline工具数据结果改造说明.md` 并同步引用。
- 扫描：除 `docs/限流重组.md` 的“已删除清单”、superseded 历史文档、历史 memory、测试防回潮断言外，当前运行目录和 active docs 不再命中旧 pack 入口名。
- 验证：相关文档合同 `2 passed`，前线/data_need 回归 `144 passed`，`git diff --check` 通过。
- 修复：从 git 历史恢复真实 TradingAgents-CN baseline evidence 到 `docs/evidence/**/tradingagents_cn_*`，没有创建假 evidence，也没有放松测试；`find docs/evidence -type f -name 'tradingagents_cn_*' | wc -l` 为 47。
- 验证：`uv run pytest tests/contracts/test_cn_a_social_coverage_matrix_contract.py -q -p no:cacheprovider`，4 passed。
- 完成：前线 OpenClaw 工具证据目录从 `pack-tool-evidence` 改为 `data-need-tool-evidence`；final exporter、合同测试、active docs 同步改名，不再保留旧目录兼容。
- 验证：`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、`uv run python -m py_compile ...`、`git diff --check` 通过；`uv run pytest tests/contracts/test_final_report_exporter.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_frontline_tool_contract.py tests/contracts/test_tool_registry_contract.py tests/contracts/test_provider_payload_visible_tools.py tests/contracts/test_frontline_prompt_write_contract.py tests/contracts/test_stage_tool_policy_contract.py tests/contracts/test_tool_calls_guard.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/unit/reports/test_data_need_bridge.py -q -p no:cacheprovider`，146 passed。
- 当前残留说明：运行目录和 active docs 已无前线固定 pack 入口；旧前线工具名只保留在测试防回潮断言。

## 选股候选缓存命名清理

- 完成：worker 可见选股工具从 `claw_get_selection_candidate_pack` 改为 `claw_get_selection_candidate_cache`，stage intent 从 `selection_candidate_pack` 改为 `selection_candidate_cache`。
- 完成：OpenClaw selection 插件证据目录从 `selection-pack-tool-evidence` 改为 `selection-candidate-cache-tool-evidence`；selection prompt、provider payload guard、tool registry、active selection docs 同步改成“候选缓存”。
- 完成：生产端新写出的选股候选产物 URI/文件名改为 `candidate-cache/approved/candidate-cache.*`，material id 前缀改为 `selection-candidate-cache-`，manifest schema version 改为 `sel-04-candidate-cache-v1`。
- 完成：selection 内部 schema/状态/错误码/测试从 `candidate_pack`、`CandidatePack`、`CANDIDATE_PACK`、`pack_body`、`pack_summary` 迁移为 `candidate_cache`、`CandidateCache`、`CANDIDATE_CACHE`、`cache_body`、`cache_summary`；源文件从 `candidate_pack.py` 改为 `candidate_cache.py`。
- 完成：从 git 历史恢复真实 selection 证据后迁移为 cache 夹具，只保留 `docs/evidence/sel-04-candidate-cache-artifacts-2026-05-26`；旧 `sel-04-candidate-pack...` 副本已删除。
- 扫描：selection 运行入口、selection active docs、selection tests、cache 证据目录已无 `claw_get_selection_candidate_pack`、`selection_candidate_pack`、`selection-pack-tool-evidence`、`candidate-pack`、`candidate pack`、`candidate_pack`、`CandidatePack`、`CANDIDATE_PACK`、`pack_body`、`pack_summary`、`候选包` 命中。
- 验证：selection 全套 `uv run pytest tests/contracts/test_selection_tool_contract.py tests/contracts/test_selection_tool_import_block.py tests/contracts/test_selection_stage_policy.py tests/contracts/test_selection_provider_payload.py tests/contracts/test_selection_dispatch_order.py tests/unit/selection tests/integration/selection -q -p no:cacheprovider`，240 passed, 1 skipped；前线数据工具回归 146 passed；`py_compile`、`node --check`、`git diff --check` 通过。

## 旧 pack 文档残留和 source role 假缺口清理

- 完成：active docs 中旧 `*_data_pack` / `candidate pack` / “候选包” / “资料包”口径改为 `claw_request_data` / data result / candidate cache；`docs/crypto_news_social_data_pack_design.md` 改名为 `docs/crypto_news_social_data_result_design.md` 并同步引用。
- 完成：`scripts/validation/run_real_select_full_chain.py` 从旧 `candidate_pack_ref` 改为 `candidate_cache_ref`，输出字段改为 `candidate_cache_*`。
- 完成：`src/claw_trade/data_gateway/warehouse/freshness.py` 不再因 `source_role_required` 生成 `warehouse_missing`；`src/claw_trade/data_gateway/warehouse/warehouse.py` 不再用 `source_role_required` 缩小可用 records，也不再因它跳过来源优先级。
- 合同：`test_freshness_checker_treats_source_role_required_as_metadata`、`test_warehouse_treats_source_role_required_as_non_denying_metadata`、`test_warehouse_source_role_required_does_not_override_source_priority` 覆盖 source role 只能是元数据/排序事实，不能制造缺口或过滤可用数据。
- 扫描：除反回归测试里的旧工具名禁止清单外，`src/`、`agents/`、`openclaw_plugins/`、`scripts/`、active `docs/`、selection tests 已无旧 pack/candidate_pack/candidate-pack/候选包/资料包运行口径命中。
- 验证：selection 全套 240 passed, 1 skipped；前线数据工具回归 146 passed；freshness/warehouse/provider selector 76 passed；`py_compile` 和 `git diff --check` 通过。

## 18:28 crypto_lens_domain_status_worker
- 完成：审查 CryptoLens `_DOMAIN_DATASETS` 残留；确认它不参与远端 DataNeed 请求、provider 候选或调度，只在已返回 `DataResult` 上做分析域覆盖状态和 section gap id 归类。
- 完成：将误导性 `_DOMAIN_DATASETS` / `_dataset_from_request_id` 命名改为 `_DOMAIN_STATUS_DATA_TYPES` / `_data_type_from_request_id`，避免把结果状态分类误读成固定资料包或远端白名单。
- 完成：补 CryptoLens 源树合同，禁止旧 `_DOMAIN_DATASETS` 名称以及 `DataRequest`、`QueryPlan`、`candidate_endpoints`、`planned_calls` 等请求规划/候选选择符号进入 CryptoLens。
- 验证：`uv run pytest tests/unit/data_gateway/test_crypto_lens_import_block.py tests/unit/data_gateway/test_crypto_lens_technical_patterns.py -q -p no:cacheprovider`，4 passed；`uv run python -m py_compile src/claw_trade/data_gateway/analysis/crypto_lens/input_contract.py` 通过；`git diff --check -- src/claw_trade/data_gateway/analysis/crypto_lens/input_contract.py tests/unit/data_gateway/test_crypto_lens_import_block.py` 通过。
待办：如果未来要让 CryptoLens 接受新的归一化数据类型或字段形态，需要人类决策批准对应分析域的语义映射；本轮没有新增 provider、没有删除真实能力、没有改变远端请求候选。

## 18:28 [codex-sdk-rate-limit-worker]
- 完成：复查 SDK internal HTTP 风险清单、source 共享限流、provider 等待字段；清单覆盖 registry/catalog 全部 `sdk_internal_unknown`，Tushare/Coinglass 官方 raw 与 structured/SDK provider 共享 source 限流。
- 完成：彻底移除 `ProviderBatchPlan.wait_timeout_seconds` 字段；`batch_planner` 不再生成该字段，`execution/gate` 的 single-flight 等待只看本次任务 `deadline_at` 剩余预算。
- 完成：UI/env 数据源设置不再暴露或写入 `rate_limit_wait_timeout_seconds`；旧输入被忽略，旧 env 不再投影到 source record。
- 完成：旧 `docs/限流修改方案.md` 和 `docs/限流修改任务清单.md` 改为废弃说明，避免恢复“统一预提取”和 provider 等待秒数；`docs/数据层详细设计.md` 的伪代码同步改为 deadline 等待。
- 验证：`uv run pytest tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_provider_adapter_inventory.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_selector.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_sdk_internal_http_risk_inventory.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_rate_limit_policy.py tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_batch_planner.py tests/unit/data_gateway/test_crypto_lens_import_block.py tests/unit/data_gateway/test_crypto_lens_technical_patterns.py tests/contracts/test_frontline_tool_contract.py tests/contracts/test_frontline_tool_protocol.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/unit/reports/test_data_need_bridge.py tests/unit/ui/test_data_source_settings.py tests/unit/runtime/test_settings_projection.py -q -p no:cacheprovider`，258 passed, 1 warning。

## 19:30 [codex-official-catalog-raw-release]
- 完成：新增官方 catalog 生成入口 `scripts/update_official_catalog_snapshots.py` 和 generated loader；生成源全部来自官方文档/OAS/llms，不再手猜 provider 能力。
- 完成：官方 raw snapshot 已接入保留付费源：Coinglass 164 个 REST endpoint、CoinGecko Pro 85 个 endpoint、Finnhub 114 个 endpoint、FRED 32 个 endpoint、Glassnode 884 个 endpoint。生成项只登记接口、参数、文档链接、响应字段样例，统一 `parser_missing`，不写 `outputs`，避免把脚本猜测变成新白名单。
- 完成：Coinglass 生成器删除了早期错误的启发式 outputs 推断；`supported-exchange-pairs` 这类接口不再被误标为资金费率候选。
- 完成：`finnhub.py`、`fred.py`、`glassnode.py`、`coingecko_pro.py`、`coinglass.py` 改为 curated endpoint 优先，generated raw endpoint 补齐同源剩余官方接口。
- 完成：DataNeed resolver 不再依赖旧 `_NEED_ENDPOINTS` 路由表；raw official endpoint 即使没有 `outputs`，也能用官方 endpoint id、path、summary、description、response_fields 做内部语义匹配并生成 planner call。worker 仍不能传 provider/path/api_name/url/header/token。
- 完成：resolver 不再用 `coverage_fields` 作为 endpoint 候选否决依据；capability 匹配改为 endpoint 索引，DataNeed planner 单测从 87 秒降到 7 秒。
- 合同：新增/扩展测试，断言官方 catalog 不再是小手写表；Coinglass/Finnhub/FRED/Glassnode/CoinGecko Pro generated snapshot 必须存在且 generated 项不含 outputs；raw endpoint `coinglass.raw_borrow_interest_rate_history` 可从官方元数据进入 planner。
- 合同：新增 `test_tushare_catalog_is_explicitly_manual_until_official_machine_catalog_exists`，明确 `official_api_tushare` 仍是手工官方目录；这个测试只覆盖官方 API catalog，不把 `cn_a_tushare_realtime` SDK 实时行情项误判为官方目录。
- 合同：`scripts/update_official_catalog_snapshots.py --provider tushare` 现在只写 `generated/tushare_blocked_no_public_machine_catalog.json` blocker，不生成假 raw endpoint；`test_tushare_generated_snapshot_is_blocker_not_fake_catalog` 锁住这个状态。
- 修复：旧 `docs/数据源修改实施方案.md` 已移入 `docs/reference/legacy/数据源修改实施方案.md`，active A股任务清单不再把它的 10-14 章当当前验收来源；旧 pack 名称在运行目录和 active docs 只剩禁止恢复/反回归测试命中。
- 修复：official raw capability 市场映射补上 `tushare -> CN_A`，避免未来拿到 Tushare 全量 raw 目录后被默认当成 US。
- 修复：删除 `provider_official_catalog.py` 的 `documented_groups` 范围表；该文件只保留 source、官网、调用方式，不再维护“该 provider 覆盖哪些范围”的手工清单。
- 验证：`uv run pytest tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_data_need_planner.py -q -p no:cacheprovider`，56 passed。
- 验证：`uv run pytest tests/contracts/test_official_provider_catalog_contract.py -q -p no:cacheprovider`，8 passed。
- 验证：`uv run pytest tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_official_provider_catalog_contract.py -q -p no:cacheprovider`，39 passed。
- 验证：`uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py -q -p no:cacheprovider`，398 passed。
- 验证：`git diff --check`、`uv run python -m py_compile scripts/update_official_catalog_snapshots.py src/claw_trade/data_gateway/planner/need_resolver.py src/claw_trade/data_gateway/official_catalog/generated_loader.py src/claw_trade/data_gateway/official_catalog/coinglass.py src/claw_trade/data_gateway/official_catalog/coingecko_pro.py src/claw_trade/data_gateway/official_catalog/finnhub.py src/claw_trade/data_gateway/official_catalog/fred.py src/claw_trade/data_gateway/official_catalog/glassnode.py src/claw_trade/data_gateway/official_catalog/tushare.py` 通过。
- 当前差异：Tushare 仍没有找到可公开机器生成的官方完整接口目录；当前 `official_api_tushare` 仍是手工 official catalog。不能声称 Tushare 官方全接口已释放。
- 当前差异：Glassnode 官方 `https://docs.glassnode.com/basic-api/endpoints/pit.md` 在本轮返回超时/500；脚本记录 warning 后生成其它官方分类，不能声称该单页已成功抓取。

## 20:05 [codex-data-need-mapping-removal-correction]
- 纠正：上一段里“official raw capability 市场映射补上 `tushare -> CN_A`”是错误方向，已删除。`official_api.py` 不再维护 `RAW_OFFICIAL_MARKETS` source-to-market 表；raw 官方能力只能读取 catalog endpoint 自身的 `response_shape.markets/market` 事实。
- 完成：`need_resolver.py` 删除固定需求路由残留：`_need_data_types`、`_NEED_FIELD_ALIASES`、`_RAW_SOURCE_MARKETS`、`_CONTAINER_DATA_TYPES`、`_raw_catalog_facts`、`_metadata_matches_need_kind` 均不存在。
- 完成：resolver 不再把 `need_kind` 先翻译成固定 `data_type` 清单；现在只做市场事实、粒度事实、catalog/provider endpoint 文字事实匹配。完整短语优先；拆词至少两个实质词同时命中，避免 `open/snapshot/need` 这类泛词误命中。
- 完成：缺口原因从 `resolver_mapping_missing` 改为 `catalog_match_missing`。新流程不再产生旧 mapping reason；测试保留旧字符串只是为了防止它们重新泄露到 worker 可见文本。
- 完成：generated official catalog loader 支持 `markets` catalog metadata；Coinglass/CoinGecko Pro/Glassnode 标 CRYPTO，Finnhub/FRED 标 US。这个事实在 catalog 装载层，不在 resolver 里推断。
- 合同：`test_resolver_has_no_central_need_to_endpoint_route_table` 禁止上述固定表和旧缺口名回到 resolver；`test_raw_official_endpoint_without_catalog_market_is_not_registered` 禁止无市场事实的 raw endpoint 被默认注册。
- 验证：`uv run pytest tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_data_gap_reason_contract.py tests/unit/reports/test_data_need_bridge.py tests/unit/data_gateway/test_models.py tests/contracts/test_worker_provider_payload_no_provider_leak.py -q -p no:cacheprovider`，41 passed。
- 验证：`uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py -q -p no:cacheprovider`，399 passed。
- 验证：限流相关 `uv run pytest tests/unit/data_gateway/test_rate_limiter.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_rate_limit_policy.py -q -p no:cacheprovider`，27 passed；`test_rate_limiter_uses_sliding_window_across_fixed_minute_boundary` 覆盖任意连续 60 秒窗口，不是自然分钟。
- 扫描：运行源码和活跃设计文档不再保留这些旧路由符号；memory 只保留本段历史纠正记录。

## 21:15 [codex-official-parser-public-source-output-contracts]

- 完成：`official_api.py` 不再只有 `tushare.daily` 和 `coinglass.futures_funding_rate` 两个特殊标准解析。保留特殊解析，同时新增基于 catalog `response_shape.outputs` 的通用解析：支持常见 JSON list、`data/items/result/results/rows/observations`、Tushare `fields/items`、列式数组；按 catalog 输出合同写 `dataset/market/granularity/fields/source_roles`。
- 完成：`report_evidence.py` 的官方接口 batch 形状不再只认两个硬编码接口；优先读取 official catalog output，生成标准 `data_type/granularity/fields`。没有 output 的 raw endpoint 仍走 `official_api_response`，按文档保留 raw，不伪造标准表。
- 完成：`IngestPipeline` 修复 raw-shape batch 硬跳过标准化的问题。只要 fetch result 里已有带 `dataset` 的标准行，即使 batch 还是 `official_api_response` 或 `parser_missing`，也会进入 normalizer 和 normalized store；真正 raw-only 响应才记录 `parser_missing`。
- 完成：`Normalizer` 修复 `official_api_response` 覆盖行内真实 `dataset` 的问题，避免标准行被改坏。
- 完成：`public_sources.py` 为 65 个 public/free catalog endpoints 补齐 output contracts：AkShare、EastMoney、mootdx、Tushare realtime SDK、BaoStock、CNInfo、Yahoo、Binance、SEC、Stocktwits、Sina HK、HKEX、DeFiLlama、Alternative.me、GitHub、Google News、同花顺、百度。该表只描述接口产物，不含 worker/domain/report_section 限制。
- 官方/全网核查：AKShare 官方文档确认 A 股实时行情、历史行情、个股新闻、人气、个股资金流、板块资金流等接口；BaoStock、mootdx、Binance 等保留官方文档链接。未把“测试失败”当成否定能力。
- 合同：新增 `test_public_source_catalog_endpoints_declare_data_outputs`，防止 public/free 目录退回只有接口名；新增 official generic parser、official batch shape、official normalized ingest 测试。
- 验证：`uv run pytest tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_ingest_pipeline.py tests/unit/data_gateway/test_report_evidence.py tests/unit/reports/test_data_need_bridge.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py -q -p no:cacheprovider`，80 passed。
- 验证：`uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py -q -p no:cacheprovider`，404 passed。
- 验证：限流专项 `uv run pytest tests/unit/data_gateway/test_rate_limiter.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_rate_limit_policy.py -q -p no:cacheprovider`，27 passed；`git diff --check` 通过。
- 未执行：未跑 live 全链；原因是人类明确要求实现完缺口和技术债后再跑 live。当前这段只提供非 live 代码和合同测试证据。
- 当前边界：付费 generated raw catalog 里仍有大量没有 output contract 的接口，主要是 Glassnode/Finnhub/FRED/Coinglass/CoinGecko Pro。它们仍可由 planner 作为 raw endpoint 尝试并保存 raw；没有官方/代码可验证标准输出合同的，不硬猜成标准 normalized dataset。
