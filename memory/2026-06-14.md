# 2026-06-14

## Contract failures fixed after data-layer cleanup

- 根因 1：`tests/contracts/test_cn_a_fundamental_acceptance_assets_contract.py` 要求的 `docs/evidence/cn_a_fundamental/` 证据目录不存在；失败先卡在 `fresh_final_acceptance_template.md`，实际一并缺模板、5 样本 manifest、same-ticker compare index。
- 修复 1：新增 `docs/evidence/cn_a_fundamental/fresh_final_acceptance_template.md`、`t_fnd_046_five_sample_manifest_2026-05-07.json`、`same_ticker_compare_index_600519_2026-05-06.md`。manifest 中 5 个样本的 10 个证据槽位全部标为 `pending_t_fnd_047_real_run`，不伪造真实路径。
- 根因 2：collect-first 合同测试还按旧期望要求 hard early-stop 后继续收集同批所有 worker；当前 AGENTS 合同和 runner 实现要求遇到会污染证据链的硬早停立即停止，只记录触发早停的异常证据。
- 修复 2：`tests/contracts/test_runner_batch.py::test_collect_first_early_stop_writes_exception_evidence` 改为断言只执行 1 个 worker、只收集 1 条 failure/exception evidence。
- 验证：`uv run pytest tests/contracts/test_cn_a_fundamental_acceptance_assets_contract.py tests/contracts/test_runner_batch.py::test_collect_first_early_stop_writes_exception_evidence -q -p no:cacheprovider` 为 `2 passed`。
- 验证：`uv run pytest tests/unit/data_gateway tests/contracts -q -p no:cacheprovider` 为 `1442 passed, 1 skipped, 1 warning`；warning 为既有 multiprocessing fork 警告。
- 验证：`python3 -m json.tool docs/evidence/cn_a_fundamental/t_fnd_046_five_sample_manifest_2026-05-07.json`、`python3 -m py_compile ...`、`git diff --check` 全部通过。

## Public data API contract plan superseded

- 废弃：本日早先 `api_id + params` 公开工具计划已被后续人工纠正，不能再作为当前实施合同。
- 当前合同：worker/OpenClaw 只通过 `claw_request_data` 提交业务数据项 `item`、`purpose` 和正常 runtime selectors；不能提交 `api_id`、`need_kind`、provider、path、api_name、url、header、token、fields、coverage_fields、source_role_required 或自由 `params`。
- 数据层内部仍可使用 `DataNeed.api_id`、`ProviderCallSpec`、catalog endpoint、provider path/api_name 和 provider params。
- 相关计划文档已改为 superseded 说明：`docs/superpowers/plans/2026-06-14-public-data-api-contract.md`、`docs/superpowers/plans/2026-06-14-public-data-api-contract-spec.md`。

## 06:19 [codex-data-tool-boundary-correction]
- 完成：纠正 2026-06-14 早先 `api_id + params` 公开工具方案；该方案已标为 superseded。当前共识是 worker/public tool 只提交业务数据项 `item`、`purpose`、标的、市场、时间和粒度，不能提交 `api_id`、`need_kind`、provider、path、api_name、url、header、token、fields、coverage_fields、source_role_required。
- 完成：OpenClaw 前线插件和合同测试保持单一 `claw_request_data`，schema 必填 `item + purpose`；公开 `DataAPI.request_data` 接收业务数据项请求，数据层内部再推导 `api_id/DataNeed/ProviderCallSpec`。
- 完成：清理 worker 可见数据结果文本，不再暴露计划接口数、调度数、尝试记录数、候选尝试限制和证据文件路径；审计 payload 仍保留内部 refs/attempts 供工程排查。
- 完成：同步 `docs/限流重组.md`、数据层总体/详细设计、架构设计、任务清单、A股扩展详细设计，并把旧 `docs/superpowers/plans/2026-06-14-public-data-api-contract*.md` 标记为废止。
- 验证：`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、`uv run python -m py_compile ...` 通过；聚焦回归 `uv run pytest tests/contracts/test_public_data_api_contract.py tests/contracts/test_data_api_contract.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_frontline_tool_contract.py tests/contracts/test_crypto_prompt_truthfulness.py tests/contracts/test_worker_prompt_alignment_policy.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/unit/data_gateway/test_models.py -q -p no:cacheprovider` 为 254 passed, 1 skipped。
待办：继续跑更大范围非 live 回归和边界复审；不跑 full-chain/live，直到代码和文档边界通过。

## 06:41 [codex-data-tool-boundary-finalization]

- 完成：公开数据请求继续使用业务数据项 `item`，不让 worker/OpenClaw 看到 `api_id`、`need_kind`、provider、path、api_name、url、header/token、fields、coverage_fields、source_role_required。
- 完成：删除 OpenClaw `claw_request_data` schema 里的自由 `params`；`PublicDataRequest` 也不再接收 `params`，并设置未知字段直接拒绝。provider 出网参数仍只由数据层 planner 根据接口目录生成。
- 完成：旧 `tests/unit/data_gateway/test_data_need_planner.py`、`tests/unit/data_gateway/test_report_evidence.py`、`tests/unit/reports/test_data_need_bridge.py` 已迁到当前合同：工具输入是 `item/purpose`，内部才有 `DataNeed.api_id` 和 `ProviderCallSpec`。
- 完成：`docs/superpowers/plans/2026-06-14-public-data-api-contract*.md` 已改为废弃说明，明确 `api_id + params` 方案不得实施，避免历史 plan 被误读成当前方案。
- 完成：按 AGENTS runtime preflight 用固定脚本跑了最小 live proof：`scripts/start-control-runtime.sh -- uv run python -m claw_trade.cli.run_control ... --stop-point first_response --target-worker-id market_analyst --target-stage frontline`，run_id=`run-20260614-103635-9e1f1f2c`。这是单 worker provider-payload proof，不是全链。
- 证据：新 provider payload 已复制到 `.runtime/data-need-provider-payload-proof/provider-request.json`；模型可见工具只有 `claw_request_data`，schema required=`item,purpose`，无 `params/api_id/need_kind/provider/path/api_name/url/header/token/fields/coverage_fields/source_role_required`。
- 验证：相关合集 `uv run pytest ... -q -p no:cacheprovider` 为 `439 passed, 1 skipped`；`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、`uv run python -m py_compile ...`、`git diff --check` 均通过。
- 未声称：没有跑 A 股/BTC 全链；当前 live 只证明 OpenClaw worker 可见 prompt/tool schema 边界。

## 07:18 [codex-data-tool-boundary-review-fixes]

- 完成：按人工共识和子代理复审意见继续收敛边界。公开数据工具仍只有 `claw_request_data`；worker 可见输入保持 `item`、`purpose`、`instrument`、`market`、`time_range`、`granularity`、`priority`，不暴露 provider/API/path/token/fields/source_role 或自由 `params`。
- 完成：插件错误文本不再把 `params.*`、禁用字段名、provider/API 执行细节回显给 worker；错误只说数据请求缺少业务字段、格式无效或包含不支持内容。
- 完成：`PublicDataRequest` 明确拒绝自由 `params` 和未知字段；旧 `get_data_needs` cutover 合同改为禁止旧入口，要求 `DataAPI.request_data`。
- 完成：`src/claw_trade/reports/data_need_bridge.py` 不再构造或依赖内部 `DataNeed`/`ProviderCallSpec`/`api_id` 来处理运行时请求或生成 worker 文本；它只消费数据层返回的公开 payload、数据结果和图表输入。
- 完成：`docs/superpowers/plans/2026-06-12-data-need-rate-limit-rework.md` 替换为 superseded 短文档，避免旧 `DataNeed/need_kind` worker 合同再次被执行。
- 完成：清理 `agents/market_analyst/skills/crypto-trading-analysis/**` 的旧 market pack、canonical domain、direction tool input、build_trade_context、MCP output/envelope 口径，改为业务数据结果口径。
- 子代理复审：边界监控子代理二次复审结论为 `No P0/P1`；未跑 full-chain/live。
- 验证：`uv run pytest ... -q -p no:cacheprovider` 聚焦合集为 `445 passed, 1 skipped`；后续文案清理后相关合集为 `250 passed, 1 skipped`；`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、`uv run python -m py_compile src/claw_trade/data_gateway/public_api.py src/claw_trade/reports/data_need_bridge.py src/claw_trade/data_gateway/report_evidence.py`、`git diff --check` 均通过。
- 未声称：本轮没有跑 A 股/BTC 全链；这是代码/文档/合同边界修复，不是 live 数据可用性验收。

## 08:05 [codex-project-data-item-first-hk-fixes]

- 完成：按人工最新共识清理文档口径。当前方向是“项目需要的数据项 -> 数据层内部 DataNeed -> 精确 provider API -> parser/normalizer -> 标准输出”，不是抓 provider 的所有接口，也不是按 `domain/source_role/coverage_group/fields` 做候选白名单。
- 完成：同步 `docs/限流重组.md`、`docs/数据层总体设计.md`、HK/CRYPTO/选股/A股扩展相关文档；worker/public tool 只提交业务数据项和用途，不能看到 provider、path、api_name、api_id、need_kind、params、fields。
- 完成：Tushare HK 项目必需接口补齐到官方目录和 planner 测试：`hk_mins`、`rt_hk_k`、`hk_income`、`hk_balancesheet`、`hk_cashflow`。`hk_daily_adj` 只保留 daily bar，不再冒充 HK 估值接口。
- 完成：HK Yahoo 拆成财务与估值两个明确 adapter endpoint；HK 估值走 valuation adapter，不走 financial adapter。HKEX 公告/日历 catalog path 改成实际 RSS adapter 路径。
- 完成：前线工具、选股工具和 `report_evidence` 的错误文本改为 worker 安全文案；provider/path/api_name/token 等内部执行细节只进 audit，不进模型可见文本。
- 验证：`uv run pytest tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_selection_tool_contract.py -q` 为 `192 passed, 1 warning`。
- 验证：更宽合同集合 `uv run pytest tests/contracts/test_public_data_api_contract.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_selection_tool_contract.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_provider_payload_visible_tools.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_provider_capabilities.py -q` 为 `263 passed, 1 warning`。
- 验证：`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、`node --check openclaw_plugins/claw-trade-selection-tools/index.js`、`uv run python -m compileall -q src/claw_trade/data_gateway openclaw_plugins`、`git diff --check` 均通过。
- 未声称：没有跑 full-chain/live；这是代码、文档和合同边界修复。

## 08:28 [codex-review-p1-closure]

- 子代理只读评审发现并已修复：旧 `docs/A股扩展方案.md`、`docs/架构设计.md` 仍有“同作用范围/同 coverage_group/market-domain-source_role-coverage_group 内优先”的白名单口径。已改为“同一项目数据项、同一标准输出口径的等价接口候选内排序”；这些标签只做审计/展示，不能否决 provider 候选。
- 子代理只读评审发现并已修复：旧 HK structured plugin 还保留 `hk_daily_adj_valuation`，把 Tushare `hk_daily_adj` 解析成 `valuation_metric`。已删除能力声明、fetch 分支和 valuation 解析分支；合同测试改为断言该端点不存在。
- 子代理只读评审发现并已修复：对外话术“目录未命中”会误导成 catalog 驱动。内部原因码 `catalog_match_missing` 保留兼容，但 report/worker 人话改为“项目数据项未绑定可调用接口”，planner human_readable 也改成项目数据项接口绑定失败。
- 验证：聚焦测试 `uv run pytest ... -q` 为 `6 passed`。
- 验证：宽合同集合 `uv run pytest tests/contracts/test_public_data_api_contract.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_selection_tool_contract.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_provider_payload_visible_tools.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_provider_capabilities.py tests/unit/reports/test_data_need_bridge.py -q` 为 `287 passed, 1 warning`。
- 验证：`node --check` 两个 OpenClaw 插件、相关 Python `py_compile`、`git diff --check` 均通过。
- 复查：子代理最后复查结论 `P0 无，P1 已关闭，P2 无新增`。`docs/A股扩展方案.md` 原“同组/覆盖组优先”已改成“同一项目数据项的等价接口候选内优先”；剩余 `market/domain/source_role/coverage_group` 命中只存在于“不是白名单”的反向说明。

## 09:40 [codex-unattended-data-layer-closure]

- 完成：继续无人值守修复子代理复审剩余缺口。CRYPTO frontline 工具调用守卫从旧 `claw_get_*_pack` 彻底迁到 `claw_request_data`；守卫内部也不再保留旧 pack 工具兼容列表。冻结测试哈希已同步，未新增 guard 类别。
- 完成：限流执行顺序改为缓存优先、single-flight 合并优先、只有 owner 才消耗真实 HTTP 配额。重复同一 provider call 的 waiter 不再先占滑动窗口；owner 因任务预算限流失败时会发布 single-flight 失败结果，避免等待方挂住。
- 完成：provider 调用不再使用默认 8 秒 provider 私有 cap；`_provider_call_timer` 只按当前任务 `deadline_at` 剩余预算计时。`CLAW_TRADE_DATA_NEED_PROVIDER_CALL_TIMEOUT_SECONDS` 运行代码已删除。
- 完成：provider call `batch_key` 加入 `rate_limit_bucket` 和 `http_visibility`，同计划 merge key 改为基于真实 batch key，避免不同真实执行路径误合并。
- 完成：selection 专用数据批处理把同一 merged provider call 的结果 fan-out 回填给全部 `need_ids`，不再只写第一个 need。
- 完成：规划器修复加密“当前读数”与官方小时/日频序列的粒度兼容。`crypto.long_short_ratio`、`liquidation_heatmap`、`cvd`、`taker_buy_sell`、`options_*`、`etf_flow`、`borrow_interest_rate`、`exchange_balance` 等已能按 public contract 规划到 provider API。
- 完成：补测试证明 69/69 个 public API contract 都能规划到至少一个 provider call；补 A 股盘口 paid/free 候选、Coinglass 借贷利率、宏观、事件日历 planner/parser 测试；补限流 single-flight 顺序和 selection fan-out 测试。
- 完成：把 `docs/HK数据层修改方案.md`、`docs/港股详细设计.md`、`docs/数据层实施任务清单.md`、`docs/A股扩展任务清单.md`、`docs/A股扩展详细设计.md` 重写为当前有效合同摘要，旧固定资料包、provider selector、默认预取、pack builder 只以“已废弃/不得使用”形式出现。
- 复查：`rg` 扫描真实运行代码、插件和 agent 配置，不再命中 `claw_get_*_pack`、`data_pack_bridge`、`ProviderSelector`、`RequestCoalescer`、`ProviderBatchPlanner`、`RunProviderPlan`、`PackRequest`；旧 pack 名只剩在禁止出现的合同测试和文档废弃说明里。
- 验证：聚焦测试 `uv run pytest tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_single_flight.py tests/unit/data_gateway/test_official_call_batch_keys.py tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_selection_data_need_batch.py tests/unit/reports/test_data_need_bridge.py tests/unit/data_gateway/test_rate_limit_policy.py tests/unit/data_gateway/test_rate_limiter.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/contracts/test_tool_calls_guard.py tests/contracts/test_guard_change_requires_approval.py -q` 为 `211 passed`。
- 验证：宽合同集合 `uv run pytest tests/contracts/test_public_data_api_contract.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_selection_tool_contract.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_provider_payload_visible_tools.py tests/contracts/test_tool_calls_guard.py tests/contracts/test_guard_change_requires_approval.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_provider_capabilities.py tests/unit/reports/test_data_need_bridge.py tests/unit/data_gateway/test_rate_limit_policy.py tests/unit/data_gateway/test_rate_limiter.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_selection_data_need_batch.py tests/unit/data_gateway/test_official_call_batch_keys.py -q` 为 `377 passed, 1 warning`；warning 为既有 mootdx multiprocessing fork warning。
- 验证：`uv run python -m py_compile src/claw_trade/data_gateway/execution/gate.py src/claw_trade/data_gateway/planner/call_planner.py src/claw_trade/data_gateway/report_evidence.py src/claw_trade/data_gateway/_selection_batch.py src/claw_trade/guards/tool_calls.py tests/unit/data_gateway/test_selection_data_need_batch.py`、`git diff --check` 均通过。
- 未声称：本轮没有跑 live/full-chain；这是代码、文档和非 live 合同闭环。下一步若人类要求，按 AGENTS live preflight 后再跑 A 股和 BTC frontline/full-chain。

## 10:35 [codex-legacy-field-pack-cleanup]

- 完成：删除运行模型中的旧请求字段 `source_role_required` 和旧 provider 能力字段 `coverage_fields`；`DataRequest`、`WarehouseCheck`、`ProviderCapability` 设置未知字段拒绝，避免旧字段继续静默进入运行层。
- 完成：provider 能力统一使用内部 `fields`；registry 不再从 `coverage_fields` 兼容回退；adapter inventory、测试 fixture 和 live scope 测试同步改名。
- 完成：删除 `DataRunScheduler` 对旧 provider batch 输入的排序/等待/skip 兼容路径；scheduler 只调度 `NeedPlan` / provider call spec，非新合同输入原样返回。
- 完成：selection 证据输出不再写 `source_role_required`；selection store 删除旧 `provider_batch_plan_ref` 回退兼容；selection 代码和测试里的旧 pack 口径改成 candidate cache。
- 完成：仓库和 freshness 测试删除“旧字段只是 metadata”的断言；旧字段现在只允许出现在公开工具禁止名单、payload 泄露防护测试和“不得出现旧工具”的合同测试中。
- 完成：同步当前有效文档，把 report/select/crypto/HK/A 股扩展中的正常 `pack` 口径改为数据工具、数据结果或 candidate cache；legacy 文档仍保留在 `docs/reference/legacy`，只作历史归档。
- 扫描：`rg source_role_required|coverage_fields src tests` 只剩禁止旧输入/断言不出现；`rg ProviderSelector|RequestCoalescer|ProviderBatchPlanner|PackRequest|RunProviderPlan|data_pack_bridge|claw_get_.*_pack ...` 只剩旧工具禁止测试。
- 验证：聚焦回归 `uv run pytest tests/unit/data_gateway/test_models.py tests/unit/data_gateway/test_provider_registry.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_warehouse.py tests/contracts/test_freshness_contract.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_crypto_prepackaged_data_contract.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py tests/contracts/test_fundamental_claim_gate.py tests/contracts/test_cn_a_fundamental_skill_manifest_contract.py tests/contracts/test_data_layer_implementation_order.py -q` 为 `238 passed, 12 warnings`；warnings 为既有 pytest integration mark 和 multiprocessing fork warning。
- 验证：相关 Python `py_compile` 通过；`git diff --check` 通过。
- 未声称：本轮没有跑 live/full-chain；没有声明 legacy 文档内容被删除，只声明当前有效文档口径已同步。

## 16:17 [codex-thorough-legacy-field-pack-removal]

- 完成：继续按人工“做彻底”要求，把旧字段名 `source_role_required`、`coverage_fields`、`need_kind(s)`、`capability_ids` 从当前代码、插件、测试和当前有效文档里清掉；公开工具仍只接受业务数据项、用途、标的、市场、时间、粒度和优先级。
- 完成：把旧 `claw_get_*_pack`、`*_data_pack`、`pack_path`、`RunProviderPlan`、`PackRequest`、`ProviderSelector`、`RequestCoalescer`、`ProviderBatchPlanner`、`data_pack_bridge` 口径从当前代码、插件、agent 配置、测试和当前有效文档中清掉；旧计划文档已删除或改写。
- 完成：OpenClaw 前线插件删除旧字段的专门分支和回显；公开 schema 通过允许字段集合拒绝未知输入，内部执行字段如 provider/path/api_name/api_id/data_type/fields/token 仍禁止从 worker 传入。
- 完成：选股全市场日线补数 bug 修复：迁到公开数据请求后，刷新日志仍按旧内部对象读取 `need_id`，导致补数分支抛异常并跳过二次本地读取；已改为公开请求的 `request_id`。
- 完成：guard 文件中只做旧 `pack` 命名迁移，不改 guard 行为；冻结哈希已同步记录。
- 扫描：`rg source_role_required|coverage_fields|need_kind|need_kinds|capability_ids src tests openclaw_plugins agents` 无命中；旧 pack/selector/plan 名称扫描当前代码、测试、插件、agent 无命中；当前有效 docs 同类扫描无命中。
- 说明：`pack` 这个词仍存在于 OpenViking `/api/v1/pack/import/export` 协议和对应测试，这是 OpenViking 文件包接口，不是数据层旧资料包。
- 验证：聚焦回归 `uv run pytest tests/contracts/test_public_data_api_contract.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_report_evidence.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_frontline_tool_protocol.py tests/contracts/test_provider_payload_visible_tools.py tests/contracts/test_frontline_prompt_write_contract.py tests/contracts/test_stage_tool_policy_contract.py tests/contracts/test_guard_change_requires_approval.py tests/contracts/test_official_provider_catalog_contract.py tests/unit/selection/test_data_need_refresh_strategy_and_trade_date.py tests/integration/selection/test_data_job_pipeline.py -q` 为 `189 passed, 11 warnings`。
- 验证：`node --check openclaw_plugins/claw-trade-frontline-tools/index.js`、相关 Python `py_compile`、`git diff --check` 均通过。
- 未声称：没有跑 live/full-chain；没有物理删除 `docs/reference/legacy` 历史归档，只保证当前有效代码/插件/agent/测试/文档不再引用旧数据层口径。

## 16:32 [codex-legacy-archive-physical-removal]

- 根因修正：上一轮没有做彻底，是因为我自行保留了“历史归档不算产品路径”的边界；这个边界没有得到人工批准，和“产品必须纯净”冲突。
- 完成：物理删除 `docs/reference/legacy` 整个历史归档目录，包括旧 frontline/provider 矩阵资料层、旧数据源修改方案、旧 prompt/reference、旧 Mongo/Tushare setup、旧 control 迁移资料。
- 复查：`test ! -e docs/reference/legacy` 通过。
- 扫描：在 `src tests openclaw_plugins agents docs` 中排除 `docs/evidence/**` 后，`source_role_required|coverage_fields|need_kind|need_kinds|capability_ids` 无命中。
- 扫描：在同一范围内，`claw_get_.*_pack|market_data_pack|fundamental.*pack|news.*pack|social.*pack|pack_path|_DOMAIN_DATASETS|RunProviderPlan|PackRequest|ProviderSelector|RequestCoalescer|ProviderBatchPlanner|data_pack_bridge` 无命中。
- 说明：`pack` 字样只剩 OpenViking `/api/v1/pack/import/export` 协议和测试，这是 OpenViking 文件包接口，不是数据层资料包。
- 验证：`git diff --check` 通过。
- 未声称：没有删除 `docs/evidence/**`，那是运行证据；没有跑 live/full-chain。

## 17:16 [codex-frontline-live-data-layer-check]

- 按人工要求只跑 frontline，不跑全链。
- BTC frontline：`run-20260614-204931-b0ab0446` 正常完成，路径 `.runtime/frontline-runs/crypto-btc-20260614/run-20260614-204931-b0ab0446`。4 个 worker 均 succeeded，工具调用 23 次数据结果、全部工具层 success；数据层内部仍有缺口：CRYPTO 日线未绑定可调用 provider API，Glassnode 缺 key，Coinglass AHR999 返回空，资金费率/清算地图/CVD 存在 Coinglass request_error、pair 不匹配、粒度不匹配或工具预算截断。
- A 股 600519.SH frontline：第一次手动停早，不作为干净证据；重跑 `run-20260614-205649-0d9e552f` 正常完成，路径 `.runtime/frontline-runs/cn-a-600519-20260614-rerun/run-20260614-205649-0d9e552f`。CN_A 当前 frontline 实际跑 7 个 worker：market、fundamental、news、social、policy、hot_money、lockup，全部 succeeded。
- A 股工具层：23 个数据结果，15 ready、2 partial、6 missing。主要缺口：日线部分可用但 Tushare rate_limited，AkShare/EastMoney request_error，Baostock 网络接收错误，mootdx 6s 超时；财报 Tushare income request_error，后续免费源被工具预算截断；一个“财务指标”项目数据项未绑定接口但同轮另一个 financial_metric 已由 EastMoney 成功；资金流/板块部分接口可用但 Tushare moneyflow/moneyflow_ind_dc request_error 或 rate_limited，AkShare/EastMoney 部分报错；政策宏观官方原文不足，Tushare raw_cctv_news request_error，Google News 兜底返回搜索/媒体线索；社交只拿到 AkShare/东方财富热度，细粒度情绪、话题、用户画像不足；解禁 EastMoney 返回空。
- 工具边界确认：所有 worker 可见工具均为 `claw_request_data`，没有直接暴露 provider/path/api_name/api_id/token。provider 尝试、限流、raw refs、dataset refs 只在数据层证据中。
- 运行方式：使用 `scripts/start-control-runtime.sh -- uv run python -m claw_trade.cli.run_control ... --stop-point frontline_ready --target-stage frontline`，脚本自动启动并停止 MongoDB/OpenViking/OpenClaw。沙箱内 MongoDB socket 权限失败，按权限规则用 escalated 重跑。

## 20:18 [codex-cn-a-hot-money-data-gap-fix]

- 完成：修复本轮 A 股 hot_money 单 worker 测试暴露的数据层问题。`北向资金`、`融资融券` 已成为公开数据项并接入可调用 provider；仓库允许写入 `northbound_flow` 和 `margin_trading`，不再因 unsupported dataset 造成 evidence_write_failed。
- 完成：EastMoney 融资融券明细从默认 20 条改为 500 条，解决本轮 live 中两融只能 partial 的问题。
- 完成：`板块资金` 单独建公开数据项，要求真实资金字段 `main_net`；Baostock 行业分类不能再冒充板块资金 ready。缺真实资金字段时，即使有 dataset ref 也不能算满足合同。
- 完成：Tushare official raw 调用形状改为跟 SDK 一致：POST 到 `/{api_name}`，参数里带 `ts_type_name`，不再只打根路径 `/`。
- 完成：同一 worker 同一轮内，如果模型已经拿到某个业务数据项 ready/partial/missing 结果，后续同 item/purpose 的变体请求直接复用已保存结果，不再重新进入规划和 provider 调用；避免模型把 `日线` 换成 `lookback_days=60` 又打一遍。
- Live 结果：最新 A 股 600519.SH hot_money 单 worker 路径 `.runtime/frontline-runs/cn-a-600519-20260614-hot-money-sector-flow-contract/run-20260615-000009-9e76e2dd`。日线、游资、资金流、北向资金、融资融券 ready；板块资金 missing。
- 根因：板块资金 missing 不是代码白名单或伪缺口。本轮真实外部返回为：Tushare proxy 对 moneyflow_ind_* 返回 522/空，EastMoney 板块资金接口 502，AkShare 板块资金底层同 EastMoney 导致 JSON 解析失败。
- 验证：相关回归 `uv run pytest tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_public_data_api_contract.py tests/contracts/test_provider_capabilities.py::test_cn_a_free_source_plugins_declare_source_backed_interfaces_without_legacy_runtime tests/contracts/test_provider_capabilities.py::test_cn_a_social_signal_declares_text_and_heat_sources_without_full_sentiment_claim tests/contracts/test_provider_capabilities.py::test_cn_a_astock_fetches_northbound_flow_from_hsgt_api_shape tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_margin_detail_uses_margin_trading_dataset tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_ingest_pipeline.py::test_normalized_store_accepts_cn_a_northbound_and_margin_datasets tests/unit/reports/test_data_need_bridge.py -q` 为 `146 passed`；`git diff --check` 通过。
- 未声称：没有跑全链；本轮按要求只围绕 frontline/hot_money 暴露的数据缺口修复和验证。

## 20:55 [codex-sector-flow-api-doc-alignment]

- 完成：复查板块资金接口。Tushare 官方目录确认资金流向数据下存在板块资金流向 THS、行业资金流向 THS、板块资金流向 DC；AkShare 官方文档确认 `stock_sector_fund_flow_rank(indicator="今日", sector_type="行业资金流")`。
- 修复：Tushare 结构化 provider 的 `moneyflow_ind_dc`、`moneyflow_ind_ths`、`moneyflow_cnt_ths` 不再把日期发成 `end_date`，统一按单日接口发 `trade_date`；`moneyflow_ind_dc` 继续带 `content_type="行业"`。
- 说明：official raw planner 原本已按 Tushare catalog 发 `trade_date`，本次错误只在 CN_A 结构化 Tushare provider helper。
- 完成：删除当前有效文档中的 `docs/reference/legacy` 失效链接；把当前代码/测试/文档里的旧 `pack/domain_pack/selection_pack` 静态命名清掉，保留合法的 `package/ovpack` 和历史 memory 记录。
- 验证：`uv run pytest tests/contracts/test_provider_capabilities.py::test_cn_a_tushare_sector_fund_flow_does_not_send_stock_code_filter tests/contracts/test_provider_capabilities.py::test_cn_a_tushare_sector_ths_fund_flow_uses_trade_date_not_range_dates tests/contracts/test_provider_capabilities.py::test_cn_a_akshare_fund_flow_sdk_is_registered_as_public_fallback tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_sector_snapshot_uses_short_http_timeout tests/unit/data_gateway/test_data_need_planner.py::test_cn_a_sector_flow_plans_only_to_sector_fund_flow_interfaces tests/contracts/test_public_data_api_contract.py::test_public_result_contract_requires_payload_fields_not_only_dataset_ref tests/settings/test_enhanced_sources.py tests/unit/ui/test_data_source_settings.py tests/contracts/test_selection_tool_import_block.py tests/contracts/test_worker_prompt_alignment_policy.py::test_hk_frontline_prompts_reuse_existing_data_tools tests/contracts/test_tool_registry_contract.py::test_hk_frontline_stage_policy_declares_approved_data_tools tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_bundle.py tests/unit/data_gateway/test_data_need_planner.py::test_crypto_detail_items_plan_only_requested_items_not_fixed_market_bundle tests/unit/test_control_runner.py::test_crypto_market_worker_reaches_openclaw_with_compact_market_data_result -q` 为 `56 passed`；相关 `py_compile` 和 `git diff --check` 通过。
