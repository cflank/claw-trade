- 完成：按“目标机当新机”重新部署到 `192.168.1.21`。部署前清理旧 `claw-trade*` systemd unit、`/opt/claw-trade`、`/usr/local/lib/claw-trade`、sudoers、`clawtrade/clawkiosk` 用户和相关端口；确认新机状态后安装 `claw-trade-production-0.1.4-20260702T051203Z`。远端 sha256 为 `3208e71b612f7f2b36db7dde75834de4d8cea9530b338f1065a9639b5b307e6c`，安装后 `claw-trade-control.service`、`claw-trade-ui.service`、`claw-trade-auto-update.timer` 均 active/enabled，`systemctl --failed` 为 0，UI `http://192.168.1.21:5175/`、OpenViking `1933/health`、OpenClaw `18789/health` 均返回 200。
- 根因修复：上一包的新机 CRYPTO seed 只从 `current-seed-20260626.tar` 恢复出 BTCUSDT，导致启动维护误走 `download_binance_public_data.py --start-date 2025-06-01 --all-symbols` 的慢全量下载。本轮生产包改为携带完整只读加密历史种子 `data/crypto-history-full/normalized-columnar-usdt-only`，生产 control/UI/定时维护统一通过 `CLAW_TRADE_CRYPTO_HISTORY_COLUMNAR_ROOT` 指向 release 内该目录；生产 preflight/audit/契约测试覆盖此路径和 daily parquet 存在性。
- 验证：本地 focused 回归 `uv run pytest tests/unit/production/test_production_paths_contract.py tests/unit/data_gateway/test_scheduled_data_maintenance_runner.py tests/unit/ui/test_web_state.py tests/unit/data_gateway/test_ingest_pipeline.py -q` 为 `97 passed`，`git diff --check` 通过。生产包审计 `OK: audited 185971 entries`。目标机包内 CRYPTO seed 为 `355791` 行、`596` 个交易对、范围 `2025-06-01` 到 `2026-06-06`。
- 运行结果：新机启动后 CRYPTO 原始行情维护没有再触发 2025-06-01 全量下载；实际规划 `424` 个增量缺口，覆盖 `2026-06-07` 到 `2026-07-02`，结果 `status=succeeded`，`remote_success=424`，`dataset_refs=11005`，`raw_refs=424`，`attempt_refs=424`。A 股种子恢复成功：`242646` 行、`5522` 只股票、范围 `2025-01-20` 到 `2026-06-25`；后续补 `2026-06-26` 等 4 个交易日失败，根因是新机没有 Tushare 凭证，错误码 `credential_missing:data_source:tushare`。
- 真实 Chrome 验证：`scripts/mcp-chrome.sh --endpoint http://127.0.0.1:9223 --start` 可发现 Chrome 146，但 9223 被旧会话卡住，wrapper 初始化超时；按脚本建议手工启动 Windows Chrome 空 profile 9224 后，用 `scripts/playwright-win-chrome-mcp-call.mjs` 打开 `http://192.168.1.21:5175/`，页面标题为 `claw-trade 投研工作台`，右侧任务栏显示 A 股补数据失败并带具体 Tushare 凭证错误，console `0 errors / 0 warnings`。截图输出名：`claw-trade-production-192168121-20260702.png`。
- 完成：修复并复验新机模型配置保存失败。第一次新机实测 `claw-trade-production-0.1.4-20260702T071410Z` 时，Chrome 保存报告模型配置返回 `503 / 助手服务暂不可用`；OpenClaw 日志给出真实错误 `config.patch ... INVALID_REQUEST config base hash required; re-run config.get and retry`。根因是前一轮为减少耗时，把 UI opaque settings version `v_*` 直接传给 `config.patch` 时跳过了 `baseHash`，但生产 OpenClaw 强制要求 `baseHash`。
- 修复：`OpenClawGatewayRpcClient.config_patch()` 遇到无有效 hash 的 opaque version 时重新取当前 config hash，避免无 hash patch；`LlmSettingsBridge.save_llm_config_via_openclaw()` 更进一步复用保存前已有的 `config.get` 返回 `revision` 作为 `baseHash`，所以 UI 保存仍只需要一次 `config.get`，不会回到多余读取。另修 `scripts/production/uninstall_factory_test_ubuntu.sh` 删除 `/tmp/claw-trade-ui.pid` 和 `/tmp/claw-trade-ui.log` 未用 sudo 导致新机清理中断的问题。
- 验证：本地 `uv run pytest tests/unit/ui/test_openclaw_gateway_rpc_client.py tests/unit/ui/test_llm_settings_bridge.py -q` 为 `37 passed`，`bash -n scripts/production/uninstall_factory_test_ubuntu.sh` 通过。新生产包 `claw-trade-production-0.1.4-20260702T080609Z.tar.gz` sha256 `bc0a9ff37347bb9ba12f77189ea86ab7b32a0f4799f778407066e3f1ddd81294`，包审计 `OK: audited 185971 entries`。
- 真实新机部署验证：目标机 `192.168.1.21` 先用修复后的卸载脚本 `--purge-shared` 清到 `/opt/claw-trade` 仅剩空根目录，再安装 `20260702T080609Z`；`claw-trade-control.service`、`claw-trade-ui.service`、`claw-trade-auto-update.timer` 均 active，UI/OpenViking/OpenClaw health 均 200，`/opt/claw-trade/shared/config` 初始为空。用真实 Windows Chrome 146 的 9224 profile 打开 `http://192.168.1.21:5175/settings?fresh=20260702T080609`，填写测试 API key 后点击“保存报告模型配置”，页面 `7645ms` 显示 `模型配置已保存。`，状态为 `已保存未验证`，未出现 `模型配置暂不可保存` 或 `助手服务暂不可用`；OpenClaw 日志显示 `config.patch 1045ms` 且 `hot reload applied`。随后通过正式 `reset-settings-to-defaults` 清除测试 key，`load-llm-settings` 返回 `state=unconfigured` 且 `apiKeyMasked=null`。
- 生产机 A 股补数复验：`/opt/claw-trade/shared/config/claw-trade.env` 不存在，旧 startup job 失败发生在 Tushare 设置可用前；当前 Mongo `ui_data_source_settings` 有 `builtin-tushare`，enabled/validated，secret 存在。用生产 runtime 环境直接验证 `data_source:tushare` 为 `available`，随后手动运行 `ScheduledDataMaintenanceRunner(CN_A,eod, cron_run_id=manual-20260702-tushare-rerun)` 成功：`input_total=5`，覆盖 `2026-06-26..2026-07-02`，`remote_success=5`，`dataset_refs=27559`，`raw_refs=5`，`attempt_refs=5`，`gaps=0`。UI `/api/ui/get-selection-refresh-snapshot` 返回 `{"selectionProgress":null}`，旧失败提示已被新成功 job 压过。
- 完成：按用户要求再次把目标机 `192.168.1.21` 清到新机状态并重装最新生产包。先确认本地最新包为 `claw-trade-production-0.1.4-20260702T080609Z.tar.gz`，本地原始 sha256 `bc0a9ff37347bb9ba12f77189ea86ab7b32a0f4799f778407066e3f1ddd81294`；因 scp 传 1.8G 包过慢且临时 HTTP 暴露被安全拒绝，目标机从当前同版本 release 本地重建 `/tmp/claw-trade-production-0.1.4-20260702T080609Z.tar.gz`，通过 `validate_production_archive.py` 校验后清机。清理范围：停止/disable `claw-trade-*` systemd，删除 `/opt/claw-trade`、`/usr/local/lib/claw-trade`、`/etc/systemd/system/claw-trade-*`、`/etc/sudoers.d/claw-trade-update`、临时 pid/log，并删除 `clawtrade/clawkiosk` 用户和组；验证清理后 `/opt/claw-trade` 不存在、claw-trade unit 不存在、关键端口 `27017/1933/18789/5175` 无监听。
- 重装验证：用 `/tmp/install_production_package.sh /tmp/claw-trade-production-0.1.4-20260702T080609Z.tar.gz` 正式安装，随后 `systemctl enable --now claw-trade-control.service claw-trade-ui.service claw-trade-auto-update.timer`。`/opt/claw-trade/current` 指向 `claw-trade-production-0.1.4-20260702T080609Z`；control/UI/timer 均 `active/enabled`，`systemctl --failed` 为 0；`127.0.0.1:5175`、`192.168.1.21:5175`、OpenViking `1933/health`、OpenClaw `18789/health` 均 200。新机配置确认：`/opt/claw-trade/shared/config` 只有 gateway token/openviking，Mongo `ui_data_source_settings=0`、`ui_secret_settings=0`、`ui_report_model_config=0`。启动补数结果：CRYPTO `status=succeeded`，`remote_success=424`，`dataset_refs=11005`；A 股按新机无 Tushare 凭证预期失败，错误 `credential_missing:data_source:tushare`。
