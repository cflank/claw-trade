- 完成：定位 A股 `/report` 数据源被禁用根因。`src/claw_trade/data_gateway/coordination/provider_selector.py` 在 2026-06-06 提交 `67d3c2e4` 中加入了 `consumer=report` 时硬过滤 `cn_a_mootdx_market` 的分支，并由 `test_report_prefetch_skips_mootdx_public_provider` 锁住。这与 `docs/数据层详细设计.md` 中“consumer 只是审计标签，不能改变 provider 选择框架”冲突，也与 2026-05-22 memory 中 mootdx quote/orderbook 已接入并有 live/provider 复证冲突。
- 修复：删除 report 专用 mootdx 过滤；测试改为 `test_report_prefetch_keeps_mootdx_public_provider` 和 `test_consumer_label_does_not_change_cn_a_realtime_quote_providers`，保证 report 与 maintenance 不再因 consumer 标签产生不同 provider 候选。
- 事实边界：AkShare 没有在 CN_A data_gateway 全局禁用；被限制的是旧 worker skill/非数据层直连路径和部分 SDK HTTP 不可审计路径。CN_A report 当前仍会选择 AkShare/EastMoney 等免费 provider；Mootdx 被 report 特判过滤才是本次明确实现错误。
- 系统性原因：此前多次修的是“某个 provider 为什么没进候选”的单点（字段声明漏、批量 planner 退化、旧路径/SDK 可审计边界），但缺少一条覆盖全部产品请求的合同：业务调用方标签不得改变 provider 候选。因此 2026-06-06 的大提交能把 `report` 专用过滤混进选择器，并且错误测试还锁住了该行为。
- 验证：`env UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/data_gateway/test_provider_selector.py -q` -> `16 passed`；选择器探针显示 CN_A report 实时报价候选为 `cn_a_eastmoney_market_data:spot_quote_batch`, `cn_a_mootdx_market:quote_snapshot`, `cn_a_akshare_social_news:stock_zh_a_spot_em`。

## A股 report provider live proof after mootdx filter removal

- Fixed runtime profile used for the live data proof: `scripts/start-control-runtime.sh -- <command>`, repo `uv`, OpenViking `127.0.0.1:1933`, OpenClaw gateway `127.0.0.1:18789`, no MCP sidecar, no external OpenViking server, A-share runtime Mongo write DB `claw_trade_runtime_ashare_mootdx_fix_20260609`, seed Mongo DB `claw_trade_a_share_seed_300td_mootdx_fix_20260609`, columnar root `/tmp/claw-trade-a-share-mootdx-fix-20260609/a-share-cn-required-300td-20260608/data/normalized`.
- Restored factory package metadata into the seed DB from `data/a-share-cn-required-300td-20260608.tar`: `raw_payloads=1`, `provider_attempts=1`, `dataset_manifests=2`, `normalized_datasets=0`. Normalized rows are in Parquet, not Mongo.
- Focused report-consumer proof for `000001.SZ` on `2026-06-09`:
  - `cn_a_eastmoney_market_data:spot_quote_batch` executed and succeeded, writing quote row `dataset:quote_snapshot:CN_A:032801cb7a8bf888`; row includes `price=11.09`, `change_pct=0.54`, `company_name=平安银行`.
  - `cn_a_mootdx_market:order_book_snapshot` executed under `consumer=report` and wrote attempt `attempt:cn_a_mootdx_market:order_book_snapshot:9607afe9e9e1`; result was `empty_result`, so the remaining盘口 gap is provider empty result, not report-side provider suppression.
  - `cn_a_akshare_social_news:stock_individual_fund_flow` executed and produced raw payload, but final status still carries `date_range_missing`.
  - `cn_a_cninfo_events:official_filing` executed and succeeded.
- Runtime DB after proof: `raw_payloads=3`, `provider_attempts=14`, `dataset_manifests=3`, `normalized_datasets=0`, `provider_result_cache=4`.
- Evidence file: `runs/run-ashare-mootdx-focused-20260609/data-layer/focused-proof.json`.
- Full `run_report_data_prefetch` did not return after about 8 minutes and was killed with exit `143`; it did not write `runs/run-ashare-mootdx-fix-20260609/data-layer/report-prefetch.json`. New gap: report prefetch needs a hard total timeout / per-domain collect-first timeout so one stuck provider cannot hang the whole prefetch.
- Provider attempt documents currently store only `provider_error` for Tushare failures and do not persist the concrete exception text. New gap: attempt logging must retain provider error reason/message, otherwise live root-cause analysis is blind.

## Report prefetch domain timeout fix

- Clarification after reading memory/docs/code: previous completed work covered worker-stage collect-first, rate-limit wait timeout, and one report prefetch manifest before worker dispatch. It did not implement a hard deadline around the report prefetch call itself.
- Fix: `run_report_data_prefetch()` now splits report prefetch by domain (`market`, `fundamental`, `news`, `social`) instead of sending all requests in one `get_data_batch()` call. Each domain runs with a hard timeout, default `90s`, configurable via `CLAW_TRADE_REPORT_PREFETCH_DOMAIN_TIMEOUT_SECONDS`; `0` disables subprocess timeout for tests/local debugging.
- Behavior: if one domain errors or times out, that domain writes `DataResultStatus.ERROR` rows with `provider_error` gaps such as `report_prefetch_domain_timeout:market:0.2s`; other domains continue and the manifest is still written with `status=partial`.
- Verification:
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/reports/test_data_pack_bridge.py -q` -> `32 passed`.
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/test_control_runner.py::test_report_command_prefetches_data_before_controller_dispatch tests/unit/test_control_runner.py::test_report_command_prefetch_failure_fails_before_worker_dispatch tests/unit/data_gateway/test_provider_selector.py -q` -> `18 passed`.
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run python -m py_compile src/claw_trade/reports/data_pack_bridge.py tests/unit/reports/test_data_pack_bridge.py` -> exit `0`.

## A股 report prefetch timeout live proof after fix

- Fixed runtime preflight: old `.runtime/dev-services/runtime.env` existed, but `127.0.0.1:1933/health` and `127.0.0.1:18789/health` were unreachable, so the proof used `scripts/start-control-runtime.sh -- <command>`. Startup reported services ready, `CLAW_TRADE_OPENVIKING_MCP_STARTED=0`, `OPENVIKING_CONFIG_FILE=/home/frank/src/claw-trade/.runtime/dev-services/openviking/ov.conf`, and `OPENVIKING_DATA_DIR=/home/frank/src/claw-trade/.runtime/dev-services/openviking/data`.
- Live command: A-share `/report` data prefetch only, ticker `000001.SZ`, run id `run-ashare-prefetch-timeout-fix-20260609`, seed Mongo DB `claw_trade_a_share_seed_300td_mootdx_fix_20260609`, runtime write DB `claw_trade_runtime_ashare_prefetch_timeout_fix_20260609`, `CLAW_TRADE_REPORT_PREFETCH_DOMAIN_TIMEOUT_SECONDS=20`.
- Result: command exited `0` and wrote `runs/run-ashare-prefetch-timeout-fix-20260609/data-layer/report-prefetch.json`. Manifest status is `partial`, request count is `19`, domains are `market/fundamental/news/social`.
- Domain statuses: `market` 6 requests timed out at about `20.037s`; `fundamental` 4 requests timed out at about `20.04s`; `news` 3 requests returned `partial` in `10.73s`; `social` 6 requests returned `partial` in `8.74s`.
- Runtime Mongo write counts: `provider_attempts=18`, `raw_payloads=10`, `dataset_manifests=10`, `normalized_datasets=0`, `provider_result_cache=13`.
- Providers observed in attempt records: `cn_a_akshare_social_news`, `cn_a_astock_signal_social`, `cn_a_baostock_market`, `cn_a_cninfo_events`, `cn_a_eastmoney_market_data`, `cn_a_google_news`.
- Conclusion: the prefetch no longer blocks the whole report on one stuck domain. Remaining data quality gaps are real provider/coverage gaps, not a global prefetch hang.

## A股 report prefetch final timeout root-cause fix

- Root cause after single-request probes:
  - `quote_snapshot` and `valuation_metric` realtime requests were incorrectly carrying the full report date window, so successful point-in-time rows were reclassified as `date_range_missing` and the service kept falling through to more providers.
  - `cn_a_mootdx_market` socket calls could hang despite the SDK socket timeout.
  - EastMoney snapshot HTTP calls inherited the global `30s` HTTP timeout, too slow for report prefetch.
  - The report-domain subprocess returned large market payloads through `multiprocessing.Queue`; child `Queue.put()` could block while the parent was waiting in `join()`, causing false domain timeout even after providers finished.
- Fixes:
  - `run_report_data_prefetch()` still runs `market/fundamental/news/social` separately, but domain worker output now goes through a temp JSON file instead of a queue.
  - CN_A/HK/US report realtime requests now use `date_range_start=None` and `date_range_end=None`; non-crypto intraday uses the target day only; sector/social snapshots do not require a one-year range.
  - Mootdx fetch now has a provider hard timeout via forked process, controlled by `CLAW_TRADE_MOOTDX_FETCH_TIMEOUT_SECONDS` and defaulting to `6s`.
  - EastMoney A-share snapshot/report-critical HTTP calls now set `timeout_seconds=6.0`.
- Focused verification:
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/reports/test_data_pack_bridge.py -q` -> `34 passed`.
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/data_gateway/test_provider_selector.py tests/contracts/test_provider_capabilities.py::test_cn_a_migrated_provider_matrix_declares_business_dataset_capabilities tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_market_data_fund_flow_uses_source_request_shape tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_spot_quote_uses_target_secids_not_first_page_scan tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_sector_snapshot_uses_short_http_timeout tests/contracts/test_provider_capabilities.py::test_mootdx_fetch_returns_provider_error_when_sdk_hangs -q` -> `21 passed, 1 warning`.
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run python -m py_compile src/claw_trade/reports/data_pack_bridge.py src/claw_trade/data_gateway/providers/plugins/cn_a/provider_matrix.py tests/unit/reports/test_data_pack_bridge.py tests/contracts/test_provider_capabilities.py` -> exit `0`.
- Final live proof: fixed runtime command mode, ticker `000001.SZ`, run id `run-ashare-prefetch-final-fix-20260609`, seed DB `claw_trade_a_share_seed_300td_mootdx_fix_20260609`, runtime write DB `claw_trade_runtime_ashare_prefetch_final_fix_20260609`, domain timeout `90s`, mootdx timeout `3s`.
- Final live result: command exited `0`, wrote `runs/run-ashare-prefetch-final-fix-20260609/data-layer/report-prefetch.json`, manifest status `partial`, request count `19`.
- Final live domain statuses: `market` `partial` in `36.959s`, `fundamental` `partial` in `30.504s`, `news` `partial` in `7.372s`, `social` `partial` in `6.969s`; no domain had `timed_out=true`.
- Final live runtime DB counts: `provider_attempts=26`, `raw_payloads=11`, `dataset_manifests=11`, `normalized_datasets=0`, `provider_result_cache=16`.
- Remaining gap: the final proof intentionally left `DATA_GATEWAY_COLUMNAR_ROOT` empty, so `daily_bar` and daily `valuation_metric` still show `data_integrity_failed` instead of reading the factory Parquet package. Next fix should restore/mount `data/a-share-cn-required-300td-20260608.tar` as the A-share factory seed for runtime.

## crypto-advanced-indicators-truthfulness-hardening

- Context: user required CRYPTO advanced indicators to use available data sources/interfaces and not self-calculate or hallucinate historical/statistical claims unsupported by data.
- Implemented:
  - CryptoLens local indicator output is integrated into CRYPTO market packs through `src/claw_trade/reports/data_pack_bridge.py`.
  - CryptoLens JSON serialization now handles pydantic models in `src/claw_trade/data_gateway/analysis/crypto_lens/output_contract.py`.
  - CRYPTO prompts for all frontline/debate/trader/risk/PM/report-polisher workers now contain explicit unsupported-history/statistics/model-memory constraints and output lexical bans.
  - A CRYPTO L1 runtime rejection for unsupported historical/model-memory terms was added in `src/claw_trade/artifacts/approval.py`, but this was later identified as invalid under AGENTS Runtime Guard Freeze because it added a new expression-sensitive runtime gate without explicit approval for that exact gate.
  - The invalid `approval.py` runtime rejection was removed later on 2026-06-09; do not restore it. Historical/model-memory wording should be handled by prompt alignment, source-grounded evidence checks, and existing fake/unsupported-claim guards, not by a broad lexical runtime gate.
- Live evidence:
  - BTC rerun succeeded: `run-20260609-005434-ba845919`, evidence `docs/evidence/live-crypto-advanced-indicators-20260609/report-btc-rerun`.
  - SOL promptfix4 succeeded but was rejected by content review: `run-20260609-021212-8fb1c194`, final report still contained unsupported `历史上`/`经典`/`聪明钱` wording.
  - SOL promptfix5 reached final_report but failed at `report_polisher` because configured `deepseek-chat` returned billing/insufficient-balance error: `run-20260609-023008-1240c74a`.
  - Partial promptfix5 scan showed remaining banned `bull_researcher` wording (`往往`); new approval hard check will prevent such L1 from entering approved materials in future runs.
- Evidence summary:
  - `docs/evidence/live-crypto-advanced-indicators-20260609/summary.md`.
- Verification:
  - Earlier verification before the rollback: `uv run pytest tests/contracts/test_material_approval_pipeline.py tests/contracts/test_crypto_prompt_truthfulness.py tests/contracts/test_guard_change_requires_approval.py::test_guard_like_rejection_call_surface_is_frozen_without_human_approval -q` -> `8 passed`.
  - `uv run pytest tests/contracts/test_material_approval_pipeline.py tests/contracts/test_crypto_prompt_truthfulness.py tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_pack_adds_crypto_lens_indicator_coverage_from_prefetch tests/unit/test_start_control_runtime_script.py::test_start_control_runtime_script_contains_required_guards tests/unit/test_start_control_runtime_script.py::test_start_control_runtime_script_preauthorizes_gateway_cli_admin_scope_with_approve_retry tests/unit/test_start_control_runtime_script.py::test_start_control_runtime_script_does_not_write_secrets_or_remove_runs_root tests/unit/data_gateway/test_crypto_lens_import_block.py tests/unit/data_gateway/test_data_service_flow.py tests/unit/data_gateway/test_ingest_pipeline.py tests/unit/data_gateway/test_provider_selector.py tests/contracts/test_provider_payload_visible_tools.py tests/contracts/test_frontline_tool_contract.py tests/contracts/test_frontline_tool_protocol.py -q` -> `150 passed`.
  - `python3 -m compileall -q src/claw_trade/data_gateway/analysis/crypto_lens src/claw_trade/reports/data_pack_bridge.py src/claw_trade/artifacts/approval.py` -> exit `0`.
  - `bash -n scripts/start-control-runtime.sh` -> exit `0`.
  - active deleted-source-name scan over `src tests agents scripts docs web/research-ui` excluding `docs/归档/**` returned no matches.
  - `git diff --check` -> exit `0`.
- Blocker:
  - Final live `/report SOL` truthfulness acceptance cannot be completed until the configured LLM provider account is topped up or a different report model/provider is explicitly approved.

## approved-provider-scope-contract

- Context: user pointed out this is not a Coinglass-only issue. The systemic failure is agents repeatedly narrowing approved data-source scope based on current implementation or fear of provider failure.
- Root cause fixed at contract level:
  - Added `src/claw_trade/data_gateway/approved_provider_scope.py` as a code-readable approved source/scope matrix.
  - The matrix classifies every fixed UI source catalog entry as `implemented` or `not_implemented`; no catalog source may remain implicit.
  - Implemented report scopes must have all three links: provider capability, report data request, and provider selector candidate.
  - Approved but not implemented scopes are explicit implementation gaps, not data-source absence.
- Current audit result:
  - UI catalog source types: `82`.
  - Classified source types: `82`.
  - Implemented source types: `20`.
  - Not-yet-implemented source types: `62`.
  - Implemented report scopes audited: `29`.
  - Explicit report-relevant implementation gaps: `7`.
  - Missing implemented capability/request/selector paths: `0`.
- CRYPTO report-relevant implementation gaps now explicitly recorded:
  - Coinglass `liquidation_heatmap`, `options`, `etf_flow`, `order_flow_cvd`.
  - LunarCrush `platform_social`.
  - Glassnode `deep_onchain`.
  - Token Terminal `protocol_revenue`.
- Added audit command:
  - `uv run python scripts/validation/audit_approved_provider_scope.py`
  - It exits non-zero if a fixed catalog source is unclassified, or if an implemented scope loses provider capability, report request routing, or selector reachability.
- Verification:
  - `uv run pytest tests/contracts/test_approved_provider_scope.py -q` -> `3 passed`.
  - `uv run python scripts/validation/audit_approved_provider_scope.py` -> exit `0`.
  - `uv run pytest tests/contracts/test_approved_provider_scope.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_selector.py -q` -> `85 passed`.
  - `git diff --check` -> exit `0`.
  - `uv run pytest tests/contracts/test_approved_provider_scope.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_selector.py tests/unit/reports/test_data_pack_bridge.py -q` -> `118 passed`.
- Remaining boundary:
  - This does not make all unimplemented approved sources live. It prevents silent narrowing: future work must either implement the adapter/report scope or keep an explicit implementation-gap entry.

## crypto-advanced-provider-gaps-implemented

- Context: user rejected stopping at the approved-provider-scope contract because the 7 CRYPTO advanced report gaps needed implementation, not just classification.
- Root cause:
  - The approved sources and some earlier CRYPTO provider work existed in memory/code history, but the unified data-layer migration only carried the basic CRYPTO provider endpoints into report request/provider capability/selector paths.
  - This made advanced indicators appear as report data gaps even though approved source families existed.
- Implemented:
  - Coinglass advanced endpoint capabilities and parsers:
    - `futures_liquidation_heatmap` for liquidation heatmap rows.
    - `options_open_interest` for options open interest and volume.
    - `spot_cvd_history` for spot CVD and taker buy/sell volume.
    - `etf_flow_history` for BTC/ETH/XRP ETF flow history; unsupported assets return explicit `not_applicable` instead of fallback.
  - New paid-source plugins:
    - `crypto_lunarcrush_social` for platform social metrics.
    - `crypto_glassnode_onchain` for active addresses, MVRV, STH/LTH SOPR, and NUPL rows.
    - `crypto_token_terminal_fundamentals` for protocol revenue/fees rows.
  - Report request specs now include protocol revenue, ETF flow, liquidation heatmap, options OI, CVD, deep on-chain metrics, and platform social metrics.
  - Rate-limit source mapping now groups these plugins under their final data-source types.
  - Approved provider scope audit now reports `not_implemented_scope_count: 0`, `capability_missing: []`, `report_request_missing: []`, and `selector_missing: []` for report scopes.
- Verification:
  - `uv run pytest tests/contracts/test_approved_provider_scope.py -q` -> `3 passed`.
  - `uv run pytest tests/contracts/test_provider_capabilities.py -q` -> `72 passed`.
  - `uv run pytest tests/unit/data_gateway/test_provider_selector.py -q` -> `16 passed`.
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py -q` -> `34 passed`.
  - `uv run python scripts/validation/audit_approved_provider_scope.py` -> exit `0`, key output: `implemented_scope_count=36`, `not_implemented_scope_count=0`, no missing capability/request/selector links.
  - Combined data-layer/report regression: `uv run pytest tests/contracts/test_approved_provider_scope.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_selector.py tests/unit/reports/test_data_pack_bridge.py tests/unit/data_gateway/test_data_service_flow.py tests/unit/data_gateway/test_ingest_pipeline.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py -q` -> `177 passed, 9 warnings`.
  - Legacy provider keyword cleanup: source-tree `rg` had no content hits; `find` found one prunable `.git/worktrees` metadata module path; `git worktree prune` removed it; follow-up source-tree, `.git/worktrees`, and filename scans returned no hits.
- Minimal live probes:
  - Evidence summary: `docs/evidence/live-crypto-advanced-provider-gaps-20260609/summary.md`.
  - Market BTC probe wrote `docs/evidence/live-crypto-advanced-provider-gaps-20260609/market-btc.json`, exit `0`, status `partial`.
    - Coinglass `futures_liquidation_heatmap` fetched and ingested successfully.
    - Coinglass `options_open_interest` and `spot_cvd_history` were requested but returned empty rows for the BTC/window probe.
    - Existing Coinglass `futures_coin_netflow` hit `rate_limited`.
  - Fundamental BTC probe wrote `docs/evidence/live-crypto-advanced-provider-gaps-20260609/fundamental-btc.json`, exit `0`, status `partial`.
    - Coinglass `etf_flow_history` was requested but returned empty rows for the BTC/window probe.
    - Current `.env.local` has no `GLASSNODE_API_KEY` or `TOKEN_TERMINAL_API_KEY`, so those credential-required providers were not selected.
  - Social BTC probe wrote `docs/evidence/live-crypto-advanced-provider-gaps-20260609/social-btc.json`, exit `0`, status `partial`.
    - Current `.env.local` has no `LUNARCRUSH_API_KEY`, so the credential-required provider was not selected.
  - Runtime support fixes made to complete the probes:
    - `scripts/validation/diagnose_frontline_data_pack_timeout.py` now wraps `build_data_api_from_env`.
    - `scripts/start-control-runtime.sh` now treats a normal `update.status` response as preauthorization success even if the OpenClaw CLI exits non-zero, and keeps one transient retry.
    - `scripts/start-control-runtime.sh -- true` verified fixed runtime startup, command execution, and cleanup.
- Remaining boundary:
  - A full `/report` with LLM workers was not rerun in this slice. The current proof is data-layer live probing plus code-level contracts. A fresh `/report` still depends on provider credentials, rate limits, provider account state, and configured LLM billing.

## A股 factory seed restore and live prefetch proof

- Restored system A-share seed package from `data/a-share-cn-required-300td-20260608.tar`.
- Added one-command restore script: `scripts/selection/restore_a_share_factory_seed.py`.
- Important fix: the restore target is `.runtime/factory-seeds/a-share-cn-required-300td-20260608/normalized`, not `.runtime/dev-services/...`, because `scripts/start-control-runtime.sh` cleans `.runtime/dev-services` at startup.
- Restore output: `runs/a-share-factory-seed-restore-20260609/restore.json`.
- Restored seed contents:
  - Parquet files: `2`.
  - Parquet bytes: `467,209,271`.
  - Mongo seed DB: `claw_trade_a_share_factory_seed`.
  - Mongo counts: `dataset_manifests=2`, `provider_attempts=1`, `raw_payloads=1`, `normalized_datasets=0`.
  - Datasets: `daily_bar` rows `1,626,559`; `valuation_metric` rows `1,626,559`; date range `2025-03-03` to `2026-05-27`; symbols `5,522`.
- The restore script now rewrites imported manifest `path` values to the real factory seed directory, so runtime writes can use a separate overlay root without breaking seed reads.
- Direct data-layer proof for `000001.SZ` on `2026-05-27`:
  - `daily_bar` read returned `close=10.76`, `company_name=平安银行`, `identity_source_ref=baostock://query_all_stock/2026-05-27`.
  - `valuation_metric` read returned `turnover_rate=0.4327`, `pe_ttm=4.8492`, `pb=0.4499`.
- Fixed two runtime startup blockers found during proof:
  - OpenClaw CLI config now receives the same generated gateway token in `gateway.auth.token` and `gateway.remote.token`.
  - Startup removes stale OpenViking `.openviking.pid` only when the referenced PID is not alive; live locks still fail closed.
- Live `/report` data prefetch proof, not full LLM report:
  - Fixed runtime entry: `scripts/start-control-runtime.sh -- <command>`.
  - Run id: `run-ashare-prefetch-restored-seed-20260609d`.
  - Manifest: `runs/run-ashare-prefetch-restored-seed-20260609d/data-layer/report-prefetch.json`.
  - Runtime write DB: `claw_trade_runtime_ashare_prefetch_restored_seed_20260609d`.
  - Seed DB: `claw_trade_a_share_factory_seed`.
  - Runtime write columnar root: `.runtime/dev-services/data-gateway/normalized`.
  - Result: `ok=true`, overall `status=partial`, domains did not time out.
  - `daily_bar` status `ready`, `dataset_refs=243`, gaps `[]`.
  - daily `valuation_metric` status `ready`, `dataset_refs=243`, gaps `[]`.
  - realtime `valuation_metric` status `ready`, `dataset_refs=1`, gaps `[]`.
  - `data_integrity_failed` disappeared from the prefetch gap reasons.
- Seed purity after live proof:
  - Factory seed directory still has exactly 2 Parquet files:
    - `.runtime/factory-seeds/a-share-cn-required-300td-20260608/normalized/market=CN_A/dataset=daily_bar/granularity=daily/partition-factory-300td-daily_bar.parquet`
    - `.runtime/factory-seeds/a-share-cn-required-300td-20260608/normalized/market=CN_A/dataset=valuation_metric/granularity=daily/partition-factory-300td-valuation_metric.parquet`
  - Live provider output went to `.runtime/dev-services/data-gateway/normalized`, not the factory seed directory.
- Verification:
  - `uv run pytest tests/unit/data_gateway/test_a_share_factory_seed_restore_cli.py tests/unit/test_start_control_runtime_script.py -q` -> `36 passed`.
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_cn_a_report_requests_do_not_force_snapshots_to_history_window tests/unit/reports/test_data_pack_bridge.py::test_report_data_prefetch_writes_large_subprocess_payload_without_deadlock -q` -> `2 passed`.
  - `uv run python -m py_compile scripts/selection/restore_a_share_factory_seed.py tests/unit/data_gateway/test_a_share_factory_seed_restore_cli.py tests/unit/test_start_control_runtime_script.py` -> exit `0`.
  - `bash -n scripts/start-control-runtime.sh` -> exit `0`.
  - `git diff --check -- scripts/selection/restore_a_share_factory_seed.py tests/unit/data_gateway/test_a_share_factory_seed_restore_cli.py scripts/start-control-runtime.sh tests/unit/test_start_control_runtime_script.py data/README.md README.md memory/2026-06-09.md` -> exit `0`.
- Remaining gaps:
  - No daily incremental job yet.
  - No scheduler binding yet.
  - Incremental merge command was added later in this file.
  - Full live `/report` LLM chain was not rerun here; this proof covers the data prefetch layer.

## A股 300 trading-day factory seed incremental merge command

- Added command: `scripts/selection/merge_a_share_factory_seed_incremental.py`.
- Purpose: merge runtime overlay Parquet into the restored A-share factory seed, keep exactly the latest N trading days, and replace seed Mongo metadata.
- Default scope:
  - market: `CN_A`
  - datasets: `daily_bar`, `valuation_metric`
  - granularity: `daily`
  - default seed root: `.runtime/factory-seeds/a-share-cn-required-300td-20260608/normalized`
  - default incremental root: `DATA_GATEWAY_COLUMNAR_ROOT` or `.runtime/dev-services/data-gateway/normalized`
  - default trading days: `300`
- Safety behavior:
  - write mode requires `--confirm-merge`;
  - `--dry-run` writes nothing and imports no Mongo metadata;
  - daily and valuation retained date windows must match by default;
  - `normalized_datasets` remains `0`; Mongo seed metadata is replaced with `raw_payloads=1`, `provider_attempts=1`, `dataset_manifests=2` in write mode.
- Current real dry-run:
  - command: `uv run python scripts/selection/merge_a_share_factory_seed_incremental.py --seed-columnar-root .runtime/factory-seeds/a-share-cn-required-300td-20260608/normalized --incremental-columnar-root .runtime/dev-services/data-gateway/normalized --trading-days 300 --dry-run --output-json runs/a-share-factory-seed-merge-dry-run-20260609/merge.json`
  - exit `0`.
  - current runtime overlay has no `CN_A` daily/valuation incremental Parquet, so no new trade date was merged.
  - dry-run output still reads both factory seed files successfully:
    - `daily_bar`: `row_count=1,626,559`, `retained_trading_days=300`, date range `2025-03-03` to `2026-05-27`, `incremental_parquet_files=0`.
    - `valuation_metric`: `row_count=1,626,559`, `retained_trading_days=300`, date range `2025-03-03` to `2026-05-27`, `incremental_parquet_files=0`.
- Tests:
  - `tests/unit/data_gateway/test_a_share_factory_seed_incremental_merge_cli.py` covers rolling to latest trading days, incremental row replacement by symbol/date, Mongo metadata replacement, default rejection of half-updated daily/valuation windows, and dry-run non-mutation.
- Remaining gaps after this command:
  - The daily incremental fetch job still needs to produce new `CN_A` daily/valuation overlay Parquet after each trading day.
  - The scheduler still needs to run fetch first, then merge.
  - Full live `/report` LLM chain is still separate from this data seed merge proof.

## CRYPTO BTC/SOL live data-source and missing-indicator probe

- User asked to run BTC and SOL live tests and focus on missing data/indicators plus data-source calls.
- Scope actually run:
  - Frontline data-pack live probes only, not the full 12-worker LLM `/report`.
  - Runtime entry: `scripts/start-control-runtime.sh -- uv run python scripts/validation/diagnose_frontline_data_pack_timeout.py ...`.
  - Market: `CRYPTO`.
  - Symbols: `BTCUSDT`, `SOLUSDT`.
  - Date window: `2025-06-09` to `2026-06-09`.
  - Evidence directory: `docs/evidence/live-crypto-btc-sol-data-sources-20260609/`.
- Enhanced diagnostic script:
  - `scripts/validation/diagnose_frontline_data_pack_timeout.py` now writes compact `data_results_summary`, `gaps_summary`, and `source_calls_summary`.
  - This only changes diagnostic output; it does not change provider selection, fetching, ingest, warehouse, or report logic.
- Probe outputs:
  - `btc-market.json`: `partial`, `elapsed_ms=74030`, `gap_count=19`, `source_call_count=19`.
  - `btc-fundamental.json`: `partial`, `elapsed_ms=6564`, `gap_count=5`, `source_call_count=6`.
  - `btc-news.json`: `partial`, `elapsed_ms=2092`, `gap_count=2`, `source_call_count=3`.
  - `btc-social.json`: `partial`, `elapsed_ms=631`, `gap_count=1`, `source_call_count=1`.
  - `sol-market.json`: `partial`, `elapsed_ms=34798`, `gap_count=31`, `source_call_count=15`.
  - `sol-fundamental.json`: `partial`, `elapsed_ms=886`, `gap_count=8`, `source_call_count=2`.
  - `sol-news.json`: `partial`, `elapsed_ms=2152`, `gap_count=2`, `source_call_count=3`.
  - `sol-social.json`: `partial`, `elapsed_ms=672`, `gap_count=1`, `source_call_count=1`.
- Data sources actually called:
  - Binance spot market: `order_book_depth`, `ticker_24hr`, `spot_intraday_bar`, all successful for BTC/SOL.
  - Daily spot source: `spot_daily_bar`, successful for BTC/SOL.
  - Coinglass derivatives/on-chain: many endpoints successful, but `options_open_interest` and `spot_cvd_history` returned empty for BTC/SOL; `futures_coin_netflow` hit rate limit for SOL and caused later cooldown skips.
  - CoinGecko market: `coins_markets`, successful for BTC/SOL.
  - DeFiLlama: `protocol`, successful for BTC/SOL but did not satisfy all required report fields.
  - Project official news: `github_releases`, successful.
  - News search source: company and macro news successful.
  - Alternative.me: `fear_greed`, successful.
- Main missing indicators/data:
  - BTC/SOL daily and 1h spot K-lines remain missing in final data-pack despite successful fetch calls; warehouse validation says there are no usable spot rows and USD-M futures rows cannot substitute spot market rows.
  - BTC/SOL derivatives hourly metrics are mostly partial because returned rows do not cover the full requested year.
  - BTC/SOL options open interest/options volume missing because Coinglass returned empty.
  - BTC/SOL CVD missing because Coinglass returned empty.
  - SOL Coinglass `futures_coin_netflow` rate-limited; subsequent order-book/on-chain requests were skipped by cooldown.
  - BTC ETF flow returned empty and had local integrity failure evidence.
  - DeFi TVL field missing; protocol revenue/fees missing.
  - News company/macro rows exist but do not cover the full requested year.
  - Basic sentiment exists, but extended social metrics `social_dominance`, `num_posts`, and `interactions` are missing.
- Interpretation:
  - This is not a provider-scope restriction problem in this run; multiple sources were called.
  - The remaining failures are split between post-fetch warehouse validation mismatch, provider empty/rate-limited responses, and requested-field mismatch.

## CRYPTO live K-line spot marker bug fix

- User challenged the live BTC/SOL result that said "本地报告包没有可用现货行情行".
- Root cause:
  - Binance live Kline fetch succeeded for BTCUSDT/SOLUSDT.
  - Report-layer crypto market bar filter only accepted rows marked with `universe_ref=binance_spot_all_symbols` or `source_market_segment=spot`.
  - Live Binance Kline rows did not carry either marker, so report-layer filter incorrectly treated spot endpoint rows as not proven spot and dropped them.
  - This was a false negative, not a Binance no-data problem.
- Fix:
  - `src/claw_trade/data_gateway/providers/plugins/crypto/__init__.py`: live Binance Kline rows now include `universe_ref=binance_spot_all_symbols` and `source_market_segment=spot`.
  - `src/claw_trade/reports/data_pack_bridge.py`: `_is_crypto_spot_row` also accepts spot provider lineage (`spot_daily_bar`, `spot_intraday_bar`, `crypto_binance_spot_market`) so existing normalized rows with provider lineage are not misclassified solely due to missing marker fields.
- Tests:
  - `tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_bar_pack_accepts_spot_provider_lineage_without_extra_marker`.
  - Existing no-futures-fallback test remains: `test_crypto_market_bar_pack_does_not_fallback_to_futures_when_spot_missing`.
  - `tests/contracts/test_provider_capabilities.py::test_crypto_plugin_fetches_daily_bar_through_managed_http_without_environment_token` now asserts the marker fields.
  - Focused command: `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_bar_pack_accepts_spot_provider_lineage_without_extra_marker tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_bar_pack_does_not_fallback_to_futures_when_spot_missing tests/contracts/test_provider_capabilities.py::test_crypto_plugin_fetches_daily_bar_through_managed_http_without_environment_token -q` -> `3 passed`.
- Live recheck through fixed runtime:
  - `btc-market-after-spot-fix.json`: `daily_bar` `partial`, `row_count=366`; `intraday_bar` `partial`, `row_count=43`; no "没有可用现货行情行" gap on these requests.
  - `sol-market-after-spot-fix.json`: `daily_bar` `partial`, `row_count=366`; `intraday_bar` `partial`, `row_count=43`; no "没有可用现货行情行" gap on these requests.
  - Overall market packs remain `partial` because of other gaps: local dataset metadata integrity, 1h date range coverage, and Coinglass empty/rate-limit paths.

## CRYPTO BTC full-chain failed on invalid lexical runtime gate

- User clarified the requested test was full-chain `/report`, not only data-pack probing.
- BTC full-chain command attempted through fixed runtime:
  - `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py --ticker BTC --company-name Bitcoin --market CRYPTO --profile CRYPTO --currency USDT --currency-symbol USDT --current-date 2026-06-09 --start-date 2025-06-09 --end-date 2026-06-09 --output-dir docs/evidence/fullchain-crypto-btc-sol-after-spot-fix-20260609/btc`
- Result:
  - Failed before export; no final report generated.
  - Run id: `run-20260609-082922-b88f6621`.
  - Failure record: `runs/run-20260609-082922-b88f6621/failures/20260609T083239258174Z-stage_batch.json`.
  - Failure text: `stage_batch: claims: workers=['fundamental_analyst']; reasons=['CRYPTO L1 含未证据化历史/模型记忆话术，不能进入 approved materials: 历史案例']`.
- Root cause:
  - Fundamental L1 contained the safe negation sentence `缺少可引用历史案例，不能作为当前证据。`
  - The runtime lexical gate only matched the token `历史案例` and rejected the L1 even though the sentence explicitly said the evidence must not be used.
  - This was an invalid runtime guard under AGENTS Runtime Guard Freeze. It was not an existing fake/unsupported-claim guard; it was an added expression-sensitive lexical gate.
- Fix:
  - Removed `validate_crypto_unverified_history_language` and its call from `src/claw_trade/artifacts/approval.py`.
  - Restored `tests/contracts/test_guard_change_requires_approval.py` guard-like rejection count for `src/claw_trade/artifacts/approval.py` from `4` to `3`.
  - Do not restore this lexical runtime gate. Existing fake/fabricated/unsupported-source guards remain. Unsupported historical/model-memory wording should be handled by prompt alignment and source-grounded evidence, not a broad token ban at material approval.
- SOL full-chain rerun did not execute because the user interrupted before the command started.

## A股 full-chain live report data-source and missing-data audit

- User asked to run one A-share full-chain test and focus on missing data/indicators plus data-source calls.
- First attempt reused an already-running runtime and failed after data prefetch because the OpenClaw gateway was SIGTERM'd before worker dispatch. Evidence: `runs/run-20260609-053011-41b5bb1b`; failure category `openclaw_runtime`, not a data-source failure.
- Reran through fixed command-mode runtime:
  - command entry: `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py ...`
  - ticker: `000001.SZ`, company `平安银行`, market/profile `CN_A`, date window `2025-06-09` to `2026-06-09`.
  - runtime write DB: `claw_trade_runtime_ashare_fullchain_20260609b`.
  - seed DB: `claw_trade_a_share_factory_seed`.
  - runtime overlay root: `.runtime/dev-services/data-gateway/normalized`.
- Full-chain result:
  - run id: `run-20260609-053352-1d947d18`.
  - command exit: `0`.
  - evidence export: `docs/evidence/live-ashare-fullchain-data-gaps-20260609/report-000001-sz-run2`.
  - `capture_summary.json`: `worker_count=22`, final report chars `23936`, assets `2`, worker appendix files `16`.
  - run state: `completed`.
- Data prefetch:
  - manifest: `runs/run-20260609-053352-1d947d18/data-layer/report-prefetch.json`.
  - overall `status=partial`, `request_count=19`, `dataset_refs=865`, `raw_refs=12`, `attempt_refs=36`.
  - domain status: market `partial` in `52.431s`; fundamental `partial` in `41.465s`; news `partial` in `8.907s`; social `partial` in `8.34s`; no domain timeout.
- Ready / usable data:
  - `daily_bar`: `ready`, `478` rows, coverage `2025-06-09` to `2026-06-08`.
  - realtime `quote_snapshot`: `ready`, one row on `2026-06-09`, EastMoney source.
  - realtime `valuation_metric`: `ready`, one row on `2026-06-09`.
  - daily valuation history: `partial`, `235` rows, coverage `2025-06-09` to `2026-05-27`.
  - `company_news`: `partial`, `8` rows; `macro_news`: `partial`, `14` rows; `official_filing`: `partial`, `5` rows.
  - social hot keyword/rank sources returned three ready one-row `social_signal` requests.
  - worker evidence includes market charts: `assets/market-01-000001.SZ_indicator_panels.png` and `assets/market-02-000001.SZ_market_structure.png`.
- Missing or partial data/indicators:
  - `intraday_bar`: missing. Mootdx timed out at `3s`; Baostock returned empty. Missing fields: timestamp/open/high/low/close/volume.
  - `order_book_snapshot`: missing. Mootdx timed out at `3s`. Missing bid/ask price/size/timestamp.
  - `sector_snapshot`: missing. Tushare permission error, AkShare parse error, EastMoney HTTP 502. Missing sector_name/main_net/timestamp.
  - `capital_flow`: partial. AkShare returned `120` rows covering `2025-12-05` to `2026-06-08`, not full requested year; Tushare token/permission and EastMoney connection errors.
  - `financial_statement`: missing. Tushare token expired; Baostock network receive error. Missing period/revenue/net_income/assets/liabilities/cash_flow.
  - `financial_metric`: missing. Tushare token expired; Baostock empty. Missing roe/roa/gross_margin/debt_ratio/eps.
  - daily `valuation_metric`: partial because seed covers only to `2026-05-27`; Tushare valuation endpoint failed with expired token.
  - company/macro/news/official filing are partial because the returned rows do not cover the full requested window and news/filing bodies are not fully available.
  - social signal remains weak: investor interaction requires Tushare permission; THS hot reason empty; concept blocks provider error; missing question/answer, topic/reason, concept/industry/region/description, large-order fields.
  - lockup/supply worker output says core lockup, shareholder behavior, block trade, margin financing, dividend/split evidence is not available enough for quantitative supply-pressure analysis.
- Provider calls from runtime Mongo:
  - `provider_attempts=41`, `raw_payloads=16`, `dataset_manifests=17`, `normalized_datasets=0`, `provider_result_cache=21`, `provider_rate_limits=22`.
  - Provider status totals: AkShare social/news `6 success / 2 provider_error`; AStock social `1 empty / 1 provider_error`; Baostock `2 success / 2 empty / 1 provider_error`; CNInfo `3 success`; EastMoney events `2 success`; EastMoney market `2 success / 2 provider_error / 1 empty`; Google News `2 success`; Mootdx `2 provider_error`; Tushare fundamental `11 provider_error`; CN_A primary `1 provider_error`.
  - Interpretation: this run did call the free fallback providers. The remaining gaps are not "provider not used"; they are provider failure, empty result, field mismatch, permission/token problems, or incomplete date coverage.

## Tushare official endpoint configuration

- User provided a new Tushare token and requested switching the URL to the official Tushare URL.
- Updated local-only `.env.local`:
  - `TUSHARE_HTTP_URL=https://api.tushare.pro`
  - `TUSHARE_TOKEN` replaced; token is not recorded here.
- Synced env data-source settings into local Mongo settings via `export_runtime_settings_from_mongo(import_env_data_sources=True)`.
- Verification probe against official Tushare endpoint:
  - API: `trade_cal`
  - request: SSE calendar for `20260609`
  - HTTP status `200`, provider code `0`, row count `1`, first row `['SSE', '20260609', 1]`.

## A股 full-chain rerun after Tushare official endpoint switch

- User asked to rerun the A-share full-chain test after changing Tushare settings.
- Runtime preflight:
  - Existing `.runtime/dev-services/runtime.env` existed, but `127.0.0.1:1933` and `127.0.0.1:18789` were unreachable.
  - Used fixed command-mode runtime: `scripts/start-control-runtime.sh -- <command>`.
  - No MCP sidecar, no invest sidecar, OpenViking `1933`, OpenClaw gateway `18789`, runtime files under `.runtime/dev-services`.
- Run scope:
  - ticker `000001.SZ`, company `平安银行`, market/profile `CN_A`.
  - date window `2025-06-09` to `2026-06-09`.
  - runtime write DB `claw_trade_runtime_ashare_fullchain_20260609c`.
  - seed DB `claw_trade_a_share_factory_seed`.
  - evidence export `docs/evidence/live-ashare-fullchain-data-gaps-20260609/report-000001-sz-run3`.
- Full-chain result:
  - run id `run-20260609-085923-716c2d23`.
  - command exit `0`.
  - state `completed`.
  - `capture_summary.json`: `worker_count=21`, final report chars `22170`, assets `2`, worker appendix files `16`.
- Data prefetch result:
  - manifest `runs/run-20260609-085923-716c2d23/data-layer/report-prefetch.json`.
  - status `partial`, request count `19`, dataset refs `1697`, attempt refs `28`.
  - domain statuses: market `partial` in `34.256s`, fundamental `partial` in `55.709s`, news `partial` in `7.222s`, social `partial` in `9.368s`; no domain timeout.
- Main improvement versus the previous A-share full-chain run:
  - Runtime Mongo provider attempts: `provider_attempts=33`, `raw_payloads=23`, `dataset_manifests=25`, `normalized_datasets=0`, `provider_result_cache=29`, `provider_rate_limits=21`.
  - Tushare fundamental attempts changed from previous run's all-failed state to `9 success`.
  - Successful Tushare endpoints included `moneyflow`, `moneyflow_ind_dc`, `moneyflow_ths`, `financial_metric`, `financial_statement`, `valuation_metric`, `anns_d`, and `investor_interaction`.
  - `capital_flow` became `ready` with `487` rows.
  - `sector_snapshot` became `ready` with `11` rows.
  - historical `valuation_metric` became `ready` with `479` rows.
  - `financial_statement` became `partial` with `4` rows instead of missing.
- Remaining gaps after Tushare fix:
  - `intraday_bar` still missing: Mootdx timed out at `3s`, Baostock returned empty.
  - `order_book_snapshot` still missing: Mootdx timed out at `3s`.
  - `financial_metric` still partial: Tushare returned rows but required report fields/date coverage are still incomplete; Baostock financial metric empty/network path still not enough.
  - News/social domains remain partial because coverage, fields, and source specificity are still incomplete.

## Provider scope recurrence guard

- User pointed out the recurring failure mode: agents keep narrowing provider scope and then treat "not implemented/currently not selected" as "source cannot provide data."
- Root cause found in current code shape:
  - Runtime `ProviderSelector` no longer has the old `consumer=report` Mootdx hard filter; selector still chooses by market/data_type/granularity/fields/source_role/credentials.
  - The newer `approved_provider_scope` audit table was dangerous because it mixed UI-visible configured sources with internal default providers and did not list `mootdx` / `google_news`; this could again be misread as an allowlist.
- Fix:
  - `ApprovedSourceStatus` now has `visible_in_settings`; settings catalog and internal default sources are separated.
  - Added internal source statuses for `mootdx` and `google_news`.
  - Added contract tests that every CN_A runtime provider used by report data requests is accounted for in source inventory, and that `approved_provider_scope` is not imported by `src/` runtime code as a whitelist.
  - Audit output now reports `internal_non_settings_sources` separately.
- Verification:
  - `uv run pytest tests/contracts/test_approved_provider_scope.py -q` -> `5 passed`.
  - `uv run python scripts/validation/audit_approved_provider_scope.py` -> exit `0`; `internal_non_settings_sources=["google_news","mootdx"]`; no missing capability/request/selector links.
  - `uv run pytest tests/unit/data_gateway/test_provider_selector.py tests/contracts/test_approved_provider_scope.py -q` -> `21 passed`.
- Remaining gap:
  - Tushare official docs show realtime quote and minute data exist, but claw-trade has not implemented Tushare realtime/order-book/minute adapters yet. That must be described as "adapter not implemented", not "Tushare cannot provide it."

## Provider adapter inventory replaces approved scope

- User rejected the Tushare-centered framing. Fix was expanded to the whole provider layer.
- Removed the code-level approved/scope inventory:
  - Deleted `src/claw_trade/data_gateway/approved_provider_scope.py`.
  - Deleted `scripts/validation/audit_approved_provider_scope.py`.
  - Deleted `tests/contracts/test_approved_provider_scope.py`.
- Added registry-derived adapter inventory:
  - `src/claw_trade/data_gateway/provider_adapter_inventory.py` now reads implemented capabilities from `build_minimal_provider_registry().list_all_capabilities()`.
  - `ProviderRegistry.list_all_capabilities()` was added so inventory comes from actual registered adapters instead of a hand-written allowlist.
  - `scripts/validation/audit_provider_adapter_inventory.py` outputs source catalog, all implemented adapters, and known missing adapters.
  - `tests/contracts/test_provider_adapter_inventory.py` verifies every registered provider has a source mapping, every registry endpoint appears in inventory, and runtime `src/` code does not import inventory as a selector gate.
- Current audit counts:
  - source catalog `86`: settings-visible `82` plus internal sources `baidu_finance`, `google_news`, `mootdx`, `project_official`.
  - implemented adapters `126` across `36` providers.
  - known missing adapters currently recorded `4`: Tushare realtime quote, Tushare order book, Tushare intraday, Mootdx trade tick.
  - unmapped provider ids `[]`; stale mappings `[]`; unknown source types `[]`.
- Verification:
  - `uv run pytest tests/contracts/test_provider_adapter_inventory.py -q` -> `6 passed`.
  - `uv run python scripts/validation/audit_provider_adapter_inventory.py` -> exit `0`.
  - `uv run pytest tests/contracts/test_provider_adapter_inventory.py tests/unit/data_gateway/test_provider_selector.py -q` -> `22 passed`.
  - `uv run pytest tests/contracts/test_provider_adapter_inventory.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_selector.py -q` -> `95 passed, 1 warning`.
- Incidental verification fix:
  - Combined provider regression exposed an existing Coinglass contract failure: open-interest/funding rows lacked unit/source-field metadata.
  - Fixed `crypto_coinglass_derivatives` generic metric normalization to preserve `{metric}_unit` and `{metric}_source_field`.
  - Focused proof: `uv run pytest tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_open_interest_and_funding_keep_unit_metadata -q` -> `1 passed`.
- Boundary:
  - This change does not add new provider adapters. It removes the hand-written allowlist/scope trap and makes all current providers visible from registry-derived inventory.
  - Remaining data-source expansion work must add real adapters provider by provider; missing official/source capability must be reported as `adapter_not_implemented`, not "provider cannot do it."

## BTC crypto final-report low-level error audit

- User asked to inspect the generated BTC final report for low-level mistakes, not to blame data sources blindly.
- Evidence inspected:
  - Final report: `docs/evidence/fullchain-crypto-btc-bnb-20260609/btc/final_report.md`.
  - Prefetch pack: `runs/run-20260609-092633-61ce83e1/data-layer/report-prefetch.json`.
  - Replayed model-visible packs with current code for market/news/social domains.
- Confirmed root causes:
  - News rows had titles/summaries in the data pack, but `_row_summary` did not expose them clearly enough to workers; GitHub Bitcoin Core releases and macro-news rows were later written as missing.
  - Social rows had Alternative.me Fear & Greed `score=10.0`, `sentiment=Extreme Fear`, but model-visible summary omitted score/sentiment, leading worker/final report to write neutral sentiment.
  - Coinglass heatmap parser treated timestamp-like arrays as liquidation prices, producing impossible `1780996500.0` price; downstream then had no usable cluster price.
  - Funding/OI normalization dropped source field/unit metadata. Funding can be shown as `percent`; old OI rows cannot be unit-confirmed after the fact because the old row did not preserve the source field.
  - CVD standard field was missing, but taker buy/sell delta was promoted too easily as full CVD. Model-visible label must say CVD proxy / active buy-sell volume.
  - Technical materials did not spell out close-vs-MA, KD threshold, and TD setup meaning. The final report wrote price below all MAs, K/D both oversold, and TD down-count exhaustion incorrectly.
- Fixes applied:
  - `src/claw_trade/reports/data_pack_bridge.py`: news row summaries now include title/summary; social summaries include score/sentiment; market pack now adds MA position and KD threshold text; CryptoLens coverage labels CVD as proxy; macro row tells workers to consult news/macro pack instead of writing market macro missing.
  - `src/claw_trade/data_gateway/providers/plugins/crypto/__init__.py`: Coinglass OI/funding now preserve source field/unit; heatmap arrays no longer treat timestamp as price; heatmap rows carry price/size units; exchange-balance `data_map.Binance` branch returns value plus source field.
  - `src/claw_trade/data_gateway/analysis/crypto_lens/input_contract.py`: derivative rows carry attempt refs; funding unit inferred as percent for old rows; liquidation evidence filters abnormal heatmap prices and records invalid heatmap count; onchain netflow can carry unit/source field.
  - `src/claw_trade/data_gateway/analysis/crypto_lens/engine/*`: derivatives evidence includes units/source fields and CVD proxy source; liquidation evidence includes cluster units and invalid count; technical evidence includes KD state, TD setup rule, price sequence, and human interpretation.
  - `agents/market_analyst/skills/alphaear-techlab/scripts/indicator_engine.py`: technical summary includes latest close so the model-visible pack can state close-vs-MA directly.
- Verification:
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_crypto_social_model_visible_text_includes_score_and_sentiment tests/unit/reports/test_data_pack_bridge.py::test_crypto_news_model_visible_text_includes_titles_not_only_source tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_pack_adds_crypto_lens_indicator_coverage_from_prefetch tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_open_interest_and_funding_keep_unit_metadata -q` -> `4 passed`.
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_crypto_social_model_visible_text_includes_score_and_sentiment tests/unit/reports/test_data_pack_bridge.py::test_crypto_news_model_visible_text_includes_titles_not_only_source tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_pack_adds_crypto_lens_indicator_coverage_from_prefetch tests/unit/reports/test_data_pack_bridge.py::test_crypto_bare_asset_requests_usdt_pair_even_when_display_currency_is_usd tests/unit/reports/test_data_pack_bridge.py::test_crypto_explicit_non_usdt_pair_requests_usdt_pair tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_bar_pack_prefers_spot_rows_over_futures_rows tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_open_interest_and_funding_keep_unit_metadata tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_liquidation_heatmap_fetches_and_normalizes_points tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_liquidation_heatmap_array_payload_does_not_treat_timestamp_as_price tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_options_cvd_and_etf_flow_fetch_and_normalize -q` -> `10 passed`.
  - `uv run pytest tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_exchange_balance_data_map_returns_value_and_source_field tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_open_interest_and_funding_keep_unit_metadata tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_liquidation_heatmap_array_payload_does_not_treat_timestamp_as_price tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_pack_adds_crypto_lens_indicator_coverage_from_prefetch -q` -> `4 passed`.
  - `uv run python -m py_compile ...` for changed report bridge, crypto provider, CryptoLens engines, and indicator engine -> passed.
  - Replayed old BTC market pack now shows latest close above MA5, K near oversold / D oversold, funding `0.00379 percent`, CVD proxy source, and invalid heatmap sample count.
  - Replayed old BTC social pack now shows `score 10.0`, `sentiment Extreme Fear`.
  - Replayed old BTC news pack now shows GitHub release and macro-news titles.
- Remaining not fixed:
  - Trader/PM/polisher self-generated probabilities (`52%`, `55%/35%/10%/<10%`) and data-gap-driven forced exit/forced clearing rules remain a generation-boundary issue. Existing prompts already forbid self-derived probabilities, so adding more prompt text is not enough.
  - No new runtime guard/hard gate was added because runtime guard creation is frozen.
  - Full live BTC/BNB chain has not been rerun after these fixes.
  - Forbidden-provider string scan over `AGENTS.md agents src tests docs/evidence/fullchain-crypto-btc-bnb-20260609/btc runs/run-20260609-092633-61ce83e1/data-layer/report-prefetch.json` returned no matches.

## CRYPTO downstream probability and forced-exit generation boundary fix

- User asked what "self-made probabilities" and forced exits meant in the BTC final report.
- Root cause evidence:
  - Scenario probabilities first appeared in the trader report, then were copied into risk moderator, portfolio manager, polisher, and final report materials.
  - Forced exit / forced clearing language first appeared in the risk moderator report, then was copied into portfolio manager and final report materials.
  - `portfolio_manager` CRYPTO prompt inlined `{trader_plan}` inside an instruction sentence; runtime filled `trader_plan` from the research manager material, making upstream prose look like part of the instruction.
  - `risk_moderator` CRYPTO prompt placed the full trader decision before task instructions, making unsupported trader numbers overly salient.
  - `trader` CRYPTO prompt asked for 0-1 confidence/risk numbers, which invited self-made numeric scoring.
  - CRYPTO prompts also contained model-visible literal ban lists; those lists seed the exact words they are trying to avoid and conflicted with the prompt boundary contract.
- Fixes applied:
  - `agents/trader/prompts/CRYPTO.md`: replaced 0-1 confidence/risk score requirements with qualitative confidence/risk-grade wording.
  - `agents/risk_moderator/prompts/CRYPTO.md`: moved `{trader_decision}` into a separate material block after the instructions.
  - `agents/portfolio_manager/prompts/CRYPTO.md`: removed inline `{trader_plan}` and split research manager plan, trader decision, and risk debate history into separate material blocks.
  - All CRYPTO prompts: removed model-visible literal ban-list wording and changed those sections to generic evidence-boundary language.
  - `tests/contracts/test_worker_prompt_alignment_policy.py`: added a contract that CRYPTO downstream prompts do not inline large upstream materials into instruction sentences and that trader no longer asks for 0-1 self scoring.
  - `tests/contracts/test_crypto_prompt_truthfulness.py`: changed the truthfulness contract to check evidence-boundary wording plus absence of model-visible literal ban lists.
- Verification:
  - `rg` over CRYPTO prompts for the removed literal list terms and `输出禁词` / `没有例外` returned no matches.
  - `uv run pytest tests/contracts/test_worker_prompt_alignment_policy.py -q` -> `165 passed, 1 skipped`.
  - `uv run pytest tests/contracts/test_crypto_prompt_truthfulness.py -q` -> `1 passed`.
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_crypto_social_model_visible_text_includes_score_and_sentiment tests/unit/reports/test_data_pack_bridge.py::test_crypto_news_model_visible_text_includes_titles_not_only_source tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_pack_adds_crypto_lens_indicator_coverage_from_prefetch tests/unit/reports/test_data_pack_bridge.py::test_crypto_bare_asset_requests_usdt_pair_even_when_display_currency_is_usd tests/unit/reports/test_data_pack_bridge.py::test_crypto_explicit_non_usdt_pair_requests_usdt_pair tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_bar_pack_prefers_spot_rows_over_futures_rows tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_exchange_balance_data_map_returns_value_and_source_field tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_open_interest_and_funding_keep_unit_metadata tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_liquidation_heatmap_fetches_and_normalizes_points tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_liquidation_heatmap_array_payload_does_not_treat_timestamp_as_price tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_options_cvd_and_etf_flow_fetch_and_normalize -q` -> `11 passed`.
- Boundary:
  - No runtime guard, hard gate, output validator, exporter rejection, or data-source scope limit was added.
  - Full live BTC/BNB chain has not been rerun after this prompt/material-boundary fix.

## CN_A report data evidence boundary fix

- User asked to modify after reviewing the 000001.SZ final report issues. Goal was to stop the report chain from inventing or copying unsupported A股 numbers/news/execution language, without adding a new hard exporter guard.
- Root cause confirmed by review:
  - Upstream fundamental prompt only saw field coverage, not actual revenue/net income/assets/liabilities/cash-flow/PE/PB/PS values.
  - Downstream trader/portfolio manager/report polisher did not receive a plain-language data evidence boundary.
  - Google News company-news discovery for `000001` could pick up 上证指数 pollution.
  - A股 execution language allowed unrealistic "immediate market clearing" style wording.
- Fixes applied:
  - Added `src/claw_trade/reports/data_evidence_summary.py` to turn `report-prefetch.json` into worker-visible Chinese evidence/gap summary.
  - `data_pack_bridge.py` now exposes actual financial/valuation values and CN_A financial statement basis/units to workers; CN_A statement rows include period-cumulative vs period-end-point-in-time guidance.
  - `runner.py` injects `data_evidence_summary` into trader, portfolio manager, and report polisher for `/report` data-gateway runs.
  - CN_A trader/portfolio manager/report-polisher prompts now require conflicts with data evidence to be disclosed and forbid US-style immediate market-clearing execution wording.
  - CN_A Google News query now includes known company-name hints such as `000001 -> 平安银行`, and filters `000001` company-news rows that mention 上证指数 without 平安银行.
  - News model-visible text keeps macro-news titles but hides Google/discovery `company_news` titles/summaries so discovery headlines are not promoted to formal facts.
- Verification:
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_cn_a_fundamental_model_visible_text_includes_values_and_statement_basis tests/unit/reports/test_data_pack_bridge.py::test_report_prefetch_summary_preserves_run3_style_gaps_and_available_data tests/contracts/test_provider_capabilities.py::test_cn_a_google_news_filters_000001_index_pollution tests/unit/test_control_runner.py::test_report_polisher_receives_report_data_evidence_summary tests/unit/test_prompt_materials.py::test_cn_a_report_polisher_prompt_materials_inline_full_final_report_inputs -q` -> `5 passed`.
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py tests/contracts/test_provider_capabilities.py::test_cn_a_google_news_discovery_fetches_rss_without_formal_fact_claim tests/contracts/test_provider_capabilities.py::test_cn_a_google_news_filters_000001_index_pollution tests/unit/test_control_runner.py::test_report_command_prefetches_data_before_controller_dispatch tests/unit/test_control_runner.py::test_final_report_batch_runs_report_polisher_dynamic_serial_section_turns tests/unit/test_control_runner.py::test_report_polisher_receives_report_data_evidence_summary tests/unit/test_prompt_materials.py::test_cn_a_report_polisher_prompt_materials_inline_full_final_report_inputs tests/contracts/test_worker_prompt_alignment_policy.py -q` -> `211 passed, 1 skipped`.
  - `git diff --check` -> passed.
- Boundary:
  - No new runtime guard, hard gate, output validator, or exporter rejection rule was added.
  - A fresh 000001.SZ live `/report` run has not been rerun after this fix, so final user-facing report quality is not yet proven by live output.

### CN_A evidence boundary code review follow-up

- Subagent code review found one Important issue:
  - The new CN_A unit/basis values were added to provider rows and model-visible summaries, but not consistently added to report data request fields or provider capability fields.
  - Real risk: field projection could drop `amount_unit`, financial statement basis fields, or `market_cap_unit`, causing worker prompts to see naked values such as Tushare `total_mv` in `CNY_10K`.
- Follow-up fix:
  - CN_A report requests now include `amount_unit`, financial statement basis fields, and `market_cap_unit`.
  - Tushare, EastMoney, AkShare, Baostock, Mootdx capability declarations and actual rows were aligned for these fields where the provider returns amount/market-cap/statement values.
  - Tushare daily bar capability now includes `amount_unit`; daily amount remains normalized from Tushare thousand CNY to CNY.
  - Added regression tests for `_build_requests()` field lists, provider capability field lists, and `/report` data evidence summary injection into `trader` and `portfolio_manager`.
- Verification:
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_cn_a_report_requests_do_not_force_snapshots_to_history_window tests/contracts/test_provider_capabilities.py::test_cn_a_migrated_provider_matrix_declares_business_dataset_capabilities tests/contracts/test_provider_capabilities.py::test_cn_a_free_source_plugins_declare_source_backed_interfaces_without_legacy_runtime tests/unit/test_control_runner.py::test_trader_and_portfolio_manager_receive_report_data_evidence_summary -q` -> `4 passed`.
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py tests/contracts/test_provider_capabilities.py::test_cn_a_migrated_provider_matrix_declares_business_dataset_capabilities tests/contracts/test_provider_capabilities.py::test_cn_a_free_source_plugins_declare_source_backed_interfaces_without_legacy_runtime tests/contracts/test_provider_capabilities.py::test_cn_a_baostock_valuation_does_not_claim_market_cap_without_source_field tests/contracts/test_provider_capabilities.py::test_cn_a_plugin_uses_settings_credential_and_endpoint_through_managed_http tests/contracts/test_provider_capabilities.py::test_cn_a_tushare_registers_and_fetches_paid_fund_flow_before_public_sources tests/contracts/test_provider_capabilities.py::test_cn_a_tushare_sector_fund_flow_does_not_send_stock_code_filter tests/contracts/test_provider_capabilities.py::test_cn_a_akshare_fund_flow_sdk_is_registered_as_public_fallback tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_market_data_fund_flow_uses_source_request_shape tests/contracts/test_provider_capabilities.py::test_cn_a_eastmoney_spot_quote_uses_target_secids_not_first_page_scan tests/contracts/test_provider_capabilities.py::test_cn_a_google_news_discovery_fetches_rss_without_formal_fact_claim tests/contracts/test_provider_capabilities.py::test_cn_a_google_news_filters_000001_index_pollution tests/unit/test_control_runner.py::test_report_command_prefetches_data_before_controller_dispatch tests/unit/test_control_runner.py::test_report_polisher_receives_report_data_evidence_summary tests/unit/test_control_runner.py::test_trader_and_portfolio_manager_receive_report_data_evidence_summary tests/unit/test_prompt_materials.py::test_cn_a_report_polisher_prompt_materials_inline_full_final_report_inputs -q` -> `55 passed`.
  - `uv run python -m py_compile src/claw_trade/reports/data_evidence_summary.py src/claw_trade/reports/data_pack_bridge.py src/claw_trade/workflow/runner.py src/claw_trade/data_gateway/providers/plugins/cn_a/provider_matrix.py src/claw_trade/data_gateway/providers/plugins/http_daily_bar.py` -> passed.
  - `git diff --check` -> passed.
  - Subagent re-review verdict: previous Important is fixed; no new blocking CN_A issue found.
- Not included:
  - Full `tests/contracts/test_worker_prompt_alignment_policy.py` currently has an unrelated CRYPTO prompt assertion failure from existing dirty CRYPTO prompt/test changes; it was not caused by the CN_A field-contract patch and was not fixed here.
