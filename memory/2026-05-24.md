## 08:07 [codex-ui-qrcard-20260524]
-完成：完成 UI-QRCARD-20260524 前端收口：首页微信弹窗显示后立即补拉真实二维码（includeQr=true）、新增“刷新二维码”与弹窗内“稍后设置”，保留 LLM 弹窗“稍后配置”后工作台可见；设置页主 section 补齐 `box-shadow: var(--ct-shadow-panel)` 卡片阴影，并补齐对应前端测试断言。
-决策：保持“微信非硬阻断、报告模型 gate 不放松、无 fake/mock/static QR”边界；首页仍先走轻量状态请求保障工作台首屏速度，再在微信弹窗可见且未连接时异步拉取真实二维码并支持 refreshQr=true 主动刷新。
待办：如需 live/fresh/provider 级别证据，需由 manager 按 fixed runtime preflight 执行，不在本任务内伪造。

## 08:28 [codex]
-完成：将 `docs/evidence/cryptolens-btc-fullchain-internal-clean-20260519T042344Z/exported/final_report.md` 转换为同目录 `final_report.pdf`，并通过内嵌资源方式保留报告中的两张图表。
-决策：本机无 LaTeX PDF 引擎，使用 `pandoc` 渲染 HTML 并用 `wkhtmltopdf` 生成 PDF；因标准输入路径会丢失相对图片，改用 `--embed-resources` 内嵌图片后重新生成。
待办：无。

## 08:32 [codex]
-完成：修复 `final_report.pdf` 中文显示为方块的问题，重新生成 PDF 并确认嵌入 `NotoSansSC` 中文字体与 2 个图片对象。
-决策：根因是 WSL fontconfig 没有中文字体，`wkhtmltopdf` 默认嵌入 `DejaVuSans` 导致 CJK glyph 缺失；改用自定义 fontconfig 指向 Windows 字体目录，并在 PDF CSS 中指定 `Noto Sans SC`/微软雅黑/黑体。
待办：无。

## 08:53 [codex]
-完成：调研 `myhhub/stock` 与 `ArvinLovegood/go-stock` 的选股功能，重点梳理综合选股、策略选股、自然语言选股、热门/自定义策略、形态/指标筛选和回测链路。
-决策：判断 `myhhub/stock` 更适合作为可验证批处理选股架构参考，`go-stock` 更适合作为交互式自然语言选股和 AI 工具入口参考；不建议直接搬 GPL-3.0 代码。
待办：如要落到 claw-trade，需要先定义选股产物、证据边界和数据源授权策略。

## 09:08 [codex]
-完成：补充调研 `sngyai/Sequoia-X`，重点拆解 A 股日频策略包：均线放量、海龟突破、高窄旗形、涨停洗盘、上升趋势跌停、RPS 突破、定增事件。
-决策：Sequoia-X 适合作为战术策略库参考，不适合作为完整选股 workflow；claw-trade 应将其策略转成可审计 strategy hit artifact，再由 OpenClaw 负责候选解释、反驳、排序和报告交接。
待办：若进入设计落地，需先确认第一版市场、数据源、候选数量和是否做回测回填。

## 09:16 [codex]
-完成：结合本地 `data_gateway`、已删除数据网关/OpenViking/Mongo 证据链和前述三个开源项目，形成选股 workflow 复用现有数据源机制的架构建议。
-决策：建议选股扩展现有 provider plan、Mongo evidence、OpenViking lineage，而不是新建孤立数据仓；策略引擎只消费标准化特征快照并输出可审计命中证据。
待办：如进入实现，需先确认第一版市场范围、日频/盘中频率、候选数量和是否新增不在设置白名单内的数据源。

## 09:23 [codex]
-完成：细化选股 workflow 的候选压缩方法，明确从全市场到候选池的多层漏斗：硬过滤、便宜因子粗筛、策略命中、证据打分、agent 复核、报告交接。
-决策：不让 LLM 扫全市场，不对全市场跑完整报告；先用批量数据和确定性规则把范围压到几十只，再让 OpenClaw 对候选包做解释、反驳和排序。
待办：若进入设计文档，需要把各层阈值按市场和频率参数化。

## 09:26 [codex]
-完成：回答选股候选压缩后的执行归属问题，明确 300-500 只股票仍由确定性筛选/评分引擎处理，不发给 LLM 或报告 worker。
-决策：大规模筛选归 `claw-trade` 数据/策略层，OpenClaw 只接收压缩后的 top 候选包并做解释、反驳和最终排序；这避免 Python 冒充 analyst，也避免 agent 扫全市场导致成本和证据失控。
待办：如进入设计文档，需要定义 selection engine 的输入特征表、输出候选包和 agent 可见材料边界。

## 09:27 [codex]
-完成：明确选股 workflow 的执行归属：全市场数据由 `claw-trade` 的批量数据采集/标准化作业通过现有数据源获取，选股由本地确定性策略评分引擎完成，OpenClaw 只处理最终候选。
-决策：新增概念应是 selection data job / universe data builder，而不是新增一个 OpenClaw worker 去拉全市场数据；OpenClaw 不负责全市场 ETL。
待办：后续设计需定义全市场数据作业的调度、增量更新、水位线、失败重试和数据新鲜度合同。

## 09:30 [codex]
-完成：按 `myhhub/stock`、`go-stock`、`Sequoia-X` 源码重新核对全市场数据获取方式，并给出 A 股选股所需数据量级、接口选择和 claw-trade 落地边界。
-决策：第一版不臆造全市场 provider；参考 Sequoia-X 的 baostock 全市场日线批处理、myhhub 的 EastMoney/AkShare 全量表和历史缓存、go-stock 的 EastMoney 选股接口，但在 claw-trade 中优先落为 Tushare/AkShare/EastMoney provider adapter + Mongo normalized 快照。
待办：如实现，需实测目标数据源的全市场吞吐、字段覆盖、限流和失败率。

## 09:35 [codex]
-完成：明确选股 workflow 的 worker/执行单元分工：批量数据和评分不作为 OpenClaw worker，OpenClaw 只从压缩候选包开始介入。
-决策：推荐第一版使用 3 个确定性执行单元加 4 个 OpenClaw 选股 worker；这样既保留全市场批处理能力，又不让 LLM 扫全市场或承担数据 ETL。
待办：若写设计文档，需要定义 4 个选股 worker 的 prompt/profile、输入材料边界和输出 artifact。

## 09:43 [codex]
-完成：解释“3 个确定性执行单元”的工程含义：它们是普通 Python 批处理/库模块/队列任务，不是 OpenClaw agent，不调用 LLM。
-决策：第一版建议先作为同一个 `/select` 后台任务中的三个阶段实现，后续吞吐需要再拆成独立队列 worker 或定时进程。
待办：无。

## 09:45 [codex]
-完成：评估是否应把选股能力写成 skill 给 worker 使用，明确 skill 适合承载候选评审方法，不适合承载全市场数据获取和批量筛选计算。
-决策：全市场 ETL、因子计算和排序仍归 `claw-trade` 确定性执行层；OpenClaw worker 的 skill 只定义如何阅读 candidate pack、如何质疑策略命中、如何输出排序和 `/report` 交接建议。
待办：若进入设计文档，需要定义 selection worker skills 的可见输入、禁止能力和输出格式。

## 09:54 [codex]
-完成：核对本地 OpenClaw single-worker 运行机制和 claw-trade 入口策略，给出 `/select` worker prompt、system prompt 处理和 OpenClaw 复用边界建议。
-决策：`/select` 应新增独立 entry point 并复用 `single_worker_minimal`、provider payload capture、per-turn tool narrowing、material target 等通用机制；prompt 本身不能复用 report 12 worker，需要新增 selection worker profile 和共享候选评审 skill。
待办：若实施，需要补 `SELECT_COMMAND`、selection stages/workers、agent prompts/skills、候选 artifact 合同和 focused tests。

## 10:00 [codex]
-完成：按 A 股 report prompt 风格重新细化 `/select` 四个 worker 的 CN_A prompt 草案，补足角色定位、A 股约束、输入材料、写作边界、输出结构和禁止项。
-决策：select prompt 应继承 TradingAgents-CN 的中文报告感和辩论感，但结论限定为“进入 /report / 观察 / 淘汰”，不得输出买入/持有/卖出、目标价或最终交易建议。
待办：若进入实现，需要将草案落到 `agents/selection_*/prompts/CN_A.md` 并做 provider payload 证明。

## 10:02 [codex]
-完成：按已确认方案新增 `docs/A股选股总体设计.md`，覆盖 A股选股总体结论、确定性批处理层、4 个 OpenClaw selection workers、A股 prompt 草案、`select_command`、artifact、调度、测试、停止条件和实施切片。
-决策：文档明确 `/select` 复用 OpenClaw single-worker 机制但新增独立入口，不复用 `/report` 语义；全市场数据与评分归 Python 确定性层，worker 只评审 top 20 候选。
待办：如进入实现，需要先把文档拆成任务清单或实施计划。

## 08:38 [codex-wechat-qr-runtime-rootcause]
-完成：完成 WECHAT-QR-RUNTIME-ROOTCAUSE-20260524：`start-control-runtime.sh` 清理 runtime 时保留 OpenClaw state，避免删除已安装微信插件、设备 pairing 和微信登录凭据；CLI scope 预授权改为 `operator.admin` 的 `update.status` 无写入检查，命中 pairing/scope upgrade 时提取 requestId、调用 `openclaw devices approve <requestId>`、必要时批准 approve 命令自身生成的当前 requestId 后重试；ChannelBridge 将内部授权/配对错误翻译为“请在设备界面批准连接权限后刷新二维码。”。
-决策：根因是脚本原先 `find "${RUNTIME_DIR}" ... -exec rm -rf` 未排除 `${OPENCLAW_STATE_DIR}`，每次启动都会删掉用户已调好的 OpenClaw 插件安装、devices pairing 与微信登录状态；同时旧预授权只请求 `operator.read/operator.write`，且把 `agent.runSingleWorker` 的参数错误当成功，未覆盖微信 `web.login.start` 所需 `operator.admin`。本轮不改 OpenClaw source、不伪造二维码、不清空用户 runtime 状态。
待办：真实 probe 显示插件已 loaded 且 channelIds 为 `["openclaw-weixin"]`，但 raw `web.login.start(force=True)` 仍返回 `web login provider is not available`，未返回真实二维码；后续需查外部微信插件为何未提供 web login provider。

## 08:54 [codex-wechat-qr-runtime-rootcause]
-完成：追加修复 WECHAT-QR-RUNTIME-ROOTCAUSE-20260524 第二层根因：启动脚本生成 OpenClaw config 时写入真实微信通道配置 `channels.openclaw-weixin.replyProgressMessages=true`，并保留既有 `meta`，避免 OpenClaw 因 `missing-meta-vs-last-good` 自动回滚到无 channels 的旧配置；live probe 已返回真实 `data:image/png` 微信登录二维码。
-决策：插件安装、loaded、channelIds 和 `operator.admin` 预授权都通过后仍无二维码的根因，不是插件缺失，而是 `channels.openclaw-weixin` 没有有意义配置，gateway 启动时未把外部微信通道 full-load，`web.login.start` 因而找不到 `loginWithQrStart`；只使用插件 schema 已有字段 `replyProgressMessages`，不 invent 假字段、不改 OpenClaw source、不向普通 UI 暴露内部通道/插件细节。
待办：真实二维码已返回；剩余风险是外部微信服务后续扫码/会话状态仍需真实手机微信完成端到端登录验证。

## 09:18 [codex-wechat-qr-no-llm-first-start]
-完成：完成 WECHAT-QR-NO-LLM-FIRST-START-20260524 最小修复：`preauthorize_openclaw_gateway_cli_scopes()` 去掉“无 LLM 跳过 CLI scope 预授权”分支，保持 `operator.admin` 预授权/批准/重试始终执行；单测同步改为断言无 LLM 也不跳过预授权。按要求完成语法、unit、live 及“空 LLM + 全新 OPENCLAW_STATE_DIR”真实 probe，fresh-state 下返回真实 `data:image/png;base64,`，`qrLength=4010`。
-决策：只改 `scripts/start-control-runtime.sh` 与 `tests/unit/test_start_control_runtime_script.py`，不改 OpenClaw source、不改 UI、不放松报告模型 gate；无 LLM 启动仍保留“仅设置/诊断 UI 可用，报告执行由模型 gate 阻断”。
待办：无。

## 09:20 [codex-wechat-qr-no-llm-first-start-r1]
-完成：完成 WECHAT-QR-NO-LLM-FIRST-START-20260524-R1 最小返工：删除 `scripts/start-control-runtime.sh` 中未被调用的 `openclaw_report_llm_configured()` 死代码，并更新单测不再断言该函数存在，继续断言不存在“跳过 CLI scope 预授权”和 `if ! openclaw_report_llm_configured` 分支；语法和单测通过。
-决策：本轮仅做死代码清理，不改变运行行为；按任务要求未重跑 live/fresh direct probe。
待办：无。

## 09:29 [codex-wechat-qr-state-preserve-override]
-完成：完成 WECHAT-QR-STATE-PRESERVE-OVERRIDE-20260524：脚本清理 `${RUNTIME_DIR}` 时新增同时保留默认 state 目录 `${RUNTIME_DIR}/openclaw-state`（即使当前 `OPENCLAW_STATE_DIR` override 到 `/tmp/...`）；单测新增可执行契约，模拟外部 override 后执行同一条清理命令，验证默认 state 文件保留、非保留文件删除。语法与 unit 全通过；额外快速复验中，`OPENCLAW_STATE_DIR=/tmp/... scripts/start-control-runtime.sh -- true` 后默认 state 哨兵文件仍在（`PRESERVED`）。
-决策：仅改 `scripts/start-control-runtime.sh`、`tests/unit/test_start_control_runtime_script.py`、`memory/2026-05-24.md`；不改 OpenClaw source/UI，不改外部 state 实际使用路径，不新增字段能力。
待办：无。

## 09:39 [codex-manager]
-完成：完成微信二维码根因复查与总管验收：确认反复丢二维码的根因是 fixed runtime 清理 `.runtime/dev-services` 时删除 OpenClaw state，连同已安装微信插件、device pairing、scope approval 和微信登录凭据一起清掉；另补查出外部 `OPENCLAW_STATE_DIR` 验证会误删默认 state、无 LLM 首启会跳过 `operator.admin` 预授权、以及缺少 `channels.openclaw-weixin.replyProgressMessages=true` 导致插件 loaded 但 `web.login.start` 找不到 provider。已由 subagent 修复并复审通过，manager 独立 live/Chrome 复验真实二维码 `data:image/png;base64`、无 LLM 弹模型窗口不退出、微信独立弹窗显示二维码、设置页三块卡片、数据源编辑和 OpenClaw Control 入口。
-决策：保留默认和外部 OpenClaw state，始终执行 `operator.admin` 预授权；不改 OpenClaw source、不伪造二维码、不放松报告模型 gate、不向普通 UI 暴露 scope/gateway/channel/plugin 包名。
待办：真实扫码登录后的端到端消息收发仍需手机微信完成验证。

## 10:02 [codex-wechat-ui-poll-login]
-完成：完成 WECHAT-UI-POLL-LOGIN-20260524：首页微信弹窗拿到真实二维码后新增 2 秒登录状态轮询，调用 `getChannelStatus({ includeQr: true, pollLogin: true })`，收到 `connected` 后由现有弹窗条件自动隐藏；首页测试新增二维码后轮询并关闭弹窗覆盖。
-决策：只改首页前端轮询和首页测试，不改后端/OpenClaw/设置页；轮询失败只显示用户可理解的连接状态提示并保留工作台，连接成功完全来自后端状态响应，不伪造二维码或在线状态。
待办：真实手机微信扫码后的端到端连接仍需 live/UI 验证；本轮单测 mock 只证明前端请求参数和状态切换。

## 10:12 [codex]
-完成：补强 `docs/A股选股总体设计.md` 的 `/select` 工具设计，按 `/report` 的 stage policy、tool intent、canonical provider-visible tool、allowed_tools、provider payload 验收方式重写工具边界。
-决策：第一版只让 `selection_strategist` 和 `selection_skeptic` 看见 `claw_get_selection_candidate_pack`；manager/PM 不见工具；candidate pack tool 只读 approved selection artifact，不出网、不查 raw、不重新排序。
待办：如进入实现，需要新增 selection tool intent 映射、candidate pack tool、selection worker STAGES.yaml，并补 provider payload contract tests。

## 10:18 [codex]
-完成：补齐 `docs/A股选股总体设计.md` 的总体设计骨架，新增模块清单、模块说明、模块边界、模块架构图、数据流程图、分阶段数据流和关键接口依赖。
-决策：明确 `/select` 架构分为控制层、数据入口与证据层、确定性选股计算层、selection 工具层、OpenClaw worker 层、报告交接层；第一版新增 `src/claw_trade/selection/` 作为确定性选股模块建议落点。
待办：如进入实现，需要把第 3 节模块拆成实施任务并补对应 contract tests。

## 10:25 [codex]
-完成：补充 `docs/A股选股总体设计.md` 中从用户输入 `/select` 发起后的端到端流程，覆盖命令识别、completed selection run 校验、candidate pack 审批、4 个 worker 顺序唤醒、失败状态、用户确认和 `/report` 交接。
-决策：`/select` 触发后只读取已完成候选池并运行 selection worker DAG；没有 completed run、run 过期或 candidate pack 不可信时停止提示，不现场拉全市场；`/report` 必须等用户确认后启动。
待办：实现阶段需要补 `/select` 状态机、selection run store、用户确认卡片和对应 contract/integration tests。

## 10:24 [codex-wechat-ui-real-connect-poll-20260524-r2]
-完成：完成 WECHAT-UI-REAL-CONNECT-POLL-20260524-R2：`ChannelBridge` 改为“二维码会话后台长等待 + UI 轮询只读结果”，不再在 UI 轮询路径用 8 秒 `web.login.wait` 短超时销毁会话；新增会话代号隔离与刷新失效机制，防止旧 session 的等待结果覆盖新二维码会话。
-决策：仅改 `src/claw_trade/ui_backend/channel_bridge.py` 与 `tests/unit/ui/test_channel_bridge.py`，不改 OpenClaw source/微信插件；连接态只在真实 `web.login.wait connected` 或真实 `channels.status connected` 时返回 connected，未连接保持 disconnected 且不阻断工作台。
待办：需 manager 按 preflight 跑 live/UI 人工扫码，验证真实连接链路；本轮仅证明“不再短轮询销毁会话”与“旧会话结果不污染新会话”。

## 10:32 [codex-wechat-ui-real-connect-poll-20260524-r2a]
-完成：完成 WECHAT-UI-REAL-CONNECT-POLL-20260524-R2A：将后台二维码等待超时抽成常量 `_QR_LOGIN_BACKGROUND_WAIT_TIMEOUT_MS` 并从 `90_000` 提升到 `480_000`，覆盖微信插件 5 分钟登录会话有效期且贴近插件默认 wait 超时；同步 focused 单测断言为 `480000`。
-决策：仅改 `channel_bridge.py`、`test_channel_bridge.py`、`memory/2026-05-24.md`；不改 OpenClaw/微信插件源码，不伪造连接成功。
待办：真实扫码连通仍需 manager preflight + 人工扫码做 live 证据，本轮仅完成后端等待窗口修正与单测证明。

## 10:35 [codex-architecture-review]
-完成：完成 `docs/A股选股总体设计.md` 架构评审，逐条核对 `/select` 共识并识别实现验收风险。
-决策：总体方向可接受但需修改；重点风险是 selection 状态机/请求模型未落到现有 runner、candidate pack 自然语言边界不够清晰、prompt 禁词测试会误杀、下游无工具 worker 的材料注入合同需收紧。
待办：按评审建议修改总体设计后再进入实施拆票。

## 10:47 [codex-doc-edit-subagent]
-完成：按架构评审意见修改 `docs/A股选股总体设计.md`，补齐 `/select` 第一版新增/扩展合同、candidate pack 边界、worker 材料边界、失败语义和验收证据。
-决策：只改总体设计和追加 diary；把 top20、16:30/17:00、顺序评审、评分权重、策略集合、CLI/UI/Stage 命名等自作主张内容分类为已确认共识、合理推断、待确认或需人工确认。
待办：接受复审后再进入实施拆票；实现前仍需确认评分权重、策略阈值、最终 UI 形态和非 CN_A 市场策略。

## 10:48 [codex-wechat-homepage-poll-fix]
-完成：完成 WECHAT-HOMEPAGE-POLL-FIX-20260524：首页 4 秒后台刷新改为只查微信连接状态，不再携带 `includeQr` 触发二维码登录；首页测试补充后台刷新不带 `includeQr/pollLogin/refreshQr`，并保留弹窗拉二维码与二维码存在后登录轮询覆盖。
-决策：只改 HomePage、首页测试和 memory；不碰后端、runtime、OpenClaw 或微信插件，连接成功仍只来自真实后端状态。
待办：重新启动前端后用真实 Chrome/手机扫码验证 UI 是否能消费已连接状态并自动关闭微信弹窗。

## 10:52 [codex-wechat-homepage-poll-fix-rework]
-完成：返工 WECHAT-HOMEPAGE-POLL-FIX-20260524 测试稳定性：把“周期刷新不带二维码登录参数”的断言从较长的模型弹窗测试中拆出，改为捕获 workspace 定时器回调并手动触发，不再真实等待 4.3 秒。
-决策：生产代码不动；只修 `home-page.test.tsx` 与 memory，保留周期刷新不得携带 `includeQr/pollLogin/refreshQr` 的断言。
待办：无。

## 10:52 [codex-doc-edit-subagent]
-完成：按复审意见继续修改 `docs/A股选股总体设计.md`，修正 3.5 workflow owner、strategist/skeptic 工具取材边界、Guard source 和 `/select` 最终分类措辞。
-决策：OpenClaw 只返回 raw output/provider payload/tool evidence；approved L1 由 `claw-trade` ArtifactApproval 写入；`/select` 最终分类统一为“进入 /report / 观察 / 放弃”。
待办：等待复审确认；实现前仍需避免把 `/select` 输出限制误做成 `/report` 投资表达 guard。

## 10:55 [codex-manager]
-完成：完成 `docs/A股选股总体设计.md` 修改-复审-返工闭环；复审代理最终判定可接受，主代理用 focused `rg` 复核 workflow owner、candidate pack 取材边界、Guard source、fail-closed 和最终分类语义。
-决策：本轮只修改总体设计文档和 diary，不进入代码实现；保留评分权重、策略阈值、UI 形态、非 CN_A 市场策略等为实现前人工确认项。
待办：如进入实现，先拆分 selection request/state/run store、candidate pack artifact contract、tool payload contract、用户确认幂等和后台 scheduler 任务。

## 11:06 [codex-wechat-homepage-poll-fix-rework2]
-完成：二次返工 WECHAT-HOMEPAGE-POLL-FIX-20260524：删除队列/历史轮询测试中的真实 `setTimeout(4300)`，改为捕获首页 workspace 定时器回调并手动触发；保留周期刷新不带二维码登录参数的独立断言。
-决策：生产代码不动；只修改 `home-page.test.tsx` 与 memory。剩余 `setTimeout(2100)` 属于微信二维码登录轮询测试，不是 4 秒 workspace 刷新等待。
待办：无。

## 11:44 [codex-wechat-qr-provider-regression-20260524]
-完成：完成 WECHAT-QR-PROVIDER-REGRESSION-20260524 窄修：`start-control-runtime.sh` 生成 OpenClaw 配置时改为保留 `existingConfig.channels` 并仅强制 `channels.openclaw-weixin.replyProgressMessages=true`；`runtime.env` 改为逐项 `printf %q` shell-safe 写入，支持中文/空格/路径/空字符串可被 `source`；单测新增配置保留与 shell-safe 证明，live QR 测试收紧为“插件 ready 时必须真实二维码或 connected”，不再把 provider unavailable 映射成功路径。
-决策：不改 OpenClaw source、不改微信插件源码、不改 UI 暴露面；live/fresh 验证严格走 `scripts/start-control-runtime.sh -- <command>` 命令模式，并在首次沙箱受限后按规则提权重跑。
待办：若线上再次出现 `web login provider is not available`，需以当前收紧后的 live 测试与 runtime config/state 证据继续做 root cause，不做 fallback 成功判定。

## 12:21 [codex]
-完成：产出 `docs/A股选股详细设计.md`，按总体设计逐条覆盖 `/select` A股选股的模块落点、数据模型、状态机、candidate pack/tool contract、4 个 OpenClaw worker dispatch、用户确认与 `/report` handoff、provider/data gateway、artifact authority、测试设计和实施顺序。
-决策：未把总体设计未确认的评分权重、策略集合、硬过滤阈值、CLI 权限、UI 形态、非 CN_A 市场策略写成决定；这些路径在详细设计中均要求 approved config 或 fail closed。不修改 `third_party/openclaw`，不写实现代码。
待办：实现前需人工确认文档第 15 章列出的策略参数、阈值、交易日历、provider 矩阵和 UI/CLI 形态。

## 12:41 [codex-wechat-inbound-rootcause]
-完成：排查“设置显示微信已连接但工作台/微信消息无反应”的当前根因，确认本轮问题不是二维码链路，而是 `claw-trade-frontline-tools` 使用 `api.registerHook("reply_dispatch", ...)` 导致 OpenClaw 插件注册失败；已改为 typed hook `api.on("reply_dispatch", ...)` 并补回归测试。重启后 Chrome 显示工作台微信已连接，状态接口返回 `connected`，OpenClaw gateway 日志显示 `claw-trade-frontline-tools` 与 `openclaw-weixin` 均已加载。
-决策：保留 5/24 之前已修好的微信二维码/runtime state 方案，不回滚；本轮只修微信入站消息桥接注册 API 与契约测试，不伪造微信消息、不伪造二维码、不把状态轮询当入站消息证明。
待办：需要真实手机微信再发送 `/report BTC` 验证端到端入站：日志应出现微信入站、`reply_dispatch` 触发、UI 后端 `/api/ui/channel-inbound-message` POST 与最终回复。

## 13:55 [codex-wechat-channel-enable-restart]
-完成：继续排查“微信登录服务未就绪，请刷新二维码重试”，确认当前 runtime 的 `channels.openclaw-weixin.enabled` 被写成 `false`，导致插件存在但登录 provider 不挂载；手动重新启用后 OpenClaw 重启，接口恢复 `connected`。同时修复 `ChannelBridge.save_channel_config_via_openclaw()`：启用微信后等待 gateway 重启稳定再返回状态，禁用微信时直接返回断开状态，不再立刻请求二维码造成 provider unavailable。
-决策：不改 OpenClaw source、不伪造二维码；保留微信通道真实启用/禁用语义，只修 claw-trade 后端在配置变更重启窗口内过早判错的问题。
待办：真实手机微信发送 `/report BTC` 的入站端到端回复仍需现场验证。

## 14:46 [codex-wechat-channel-registry-rootcause]
-完成：查明“之前好了现在又坏”的当前根因：OpenClaw 启动时微信通道已加载，但后续工具发现加载 `claw-trade-frontline-tools` 的 tool-only 注册表时，把已 pin 的微信 channel registry 覆盖成空表，导致 `channels.status` 与 `web.login.start` 看到无微信 provider；已修为只有加载结果包含 channel 时才更新 channel registry，并补聚焦回归测试。
-决策：这是 OpenClaw 通用 runtime registry seam 修复；不改微信插件、不伪造二维码、不放松 UI 状态。源码测试通过，`pnpm build` 在非沙箱通过，fixed runtime 真实验证 `channels.status` 返回 `openclaw-weixin` 且 UI 后端返回 `connected/canSendText=true`。
待办：真实手机微信发送 `/report BTC` 的入站端到端回复仍需现场验证；Chrome MCP 当前 9222 端口拒绝连接，未取得浏览器截图。
## 16:01 [codex]
-完成：修复并复验微信扫码后 UI 继续刷新二维码的问题。根因是微信插件在“已连接过此 OpenClaw”场景返回 `alreadyConnected=true` 且 `connected=false`，OpenClaw gateway 与 claw-trade UI bridge 只认 `connected=true`，导致通道不重启、UI 不消耗成功状态。已改为两层都把 `alreadyConnected=true` 视为真实连接成功；OpenClaw 已重新 build，fixed runtime 已重启，Chrome 实测工作台/设置页显示已连接且无二维码刷新。
-决策：这是 OpenClaw 通用 QR 登录结果语义修复，不改微信插件、不伪造登录、不放松硬门禁；UI 连接态仍只来自真实 gateway/通道状态或真实 QR wait/start 返回。
待办：如需验证“微信发消息触发 UI/报告流程”，还要在当前已连接状态下由真实手机微信再发一条消息并看 OpenClaw 入站、UI bridge POST 与工作台响应日志。

## 16:30 [codex-wechat-fresh-scan-verify]
-完成：完成从断开微信到重新扫码的真实复验；扫码后 API 返回 `state=connected/canSendText=true/qrCodeImageDataUrl=null`，Chrome 设置页和工作台均显示“微信 ClawBot / 已连接”，工作台不再显示 unknown，OpenClaw health 与 OpenViking health 均通过。
-决策：补修 OpenClaw `web.login.start/wait` 在 `alreadyConnected=true` 且未返回账号 id 时使用 runtime 默认微信账号重启通道；已跑 `pnpm exec vitest run src/gateway/server-methods/web.start.test.ts`、`pnpm build`，并重启 `scripts/start-research-ui.sh` 后验证真实 UI。
待办：若要证明微信消息进入聊天/报告流程，还需在当前已连接状态下由真实微信发送一条消息，再核对 OpenClaw 入站日志、UI bridge POST 和工作台聊天响应。

## 17:18 [codex-wechat-qr-refresh-loop]
-完成：修复解除连接后微信弹窗反复刷新二维码的问题。根因有两层：前端把“没拿到二维码”误当成“需要重新启用微信配置”，反复写 `enabled=true` 触发 OpenClaw 重启微信通道；后端在 OpenClaw 返回二维码但没有登录会话 ID 时，每次轮询都重新发起 `web.login.start`，导致二维码持续换新。已改为只有明确“请先启用微信 ClawBot 插件”才写配置；有二维码就复用二维码，只有用户手点刷新才重新开二维码。
-决策：不改微信插件、不伪造二维码、不伪造连接；连接态仍来自真实 `channels.status` 或真实 QR 登录结果。已跑 `uv run pytest tests/unit/ui/test_channel_bridge.py`（31 passed）、`pnpm exec vitest run src/__tests__/home-page.test.tsx src/__tests__/settings-page.test.tsx`（29 passed）、`pnpm build`，并重启 `scripts/start-research-ui.sh`。
待办：Chrome MCP 当前 `127.0.0.1:9222` 拒绝连接，未能补新截图；接口复验显示微信当前 `state=connected/canSendText=true`，17:14 重启后的 OpenClaw 日志未再出现持续 `config.patch`/`web.login.start` 刷新循环。

## 17:43 [codex-wechat-disconnect-restart-state]
-完成：修复“解除连接后重启又显示已连接”的根因：启动脚本不再把用户保存的 `channels.openclaw-weixin.enabled=false` 强行改回 true；解除连接会清掉 OpenClaw 微信账号与 sync 登录态，并在禁用配置写入后再清一次，覆盖插件禁用过程中回写 sync 文件的情况；首页弹窗自动检查不再自动启用微信，用户点击“刷新二维码”才重新启用并取二维码。
-决策：不改微信插件、不伪造二维码、不伪造连接；重启后的状态必须来自真实 OpenClaw config/state。已跑后端/启动脚本测试、前端页面测试、前端 build，并重启真实 UI 后用 API + Chrome MCP 验证：配置保持 `enabled=false`，账号/sync 文件为空，工作台显示“待连接/微信已解除连接，请点击刷新二维码重新扫码”，没有自动恢复已连接。
待办：如需完整闭环，还需要用户点击“刷新二维码”后用真实手机重新扫码，验证 `connected/canSendText=true` 与微信消息入站。

## 18:47 [codex-wechat-qr-stability]
-完成：修复微信二维码“很久不出、随后不停刷新”的当前根因：工作台 4 秒后台刷新会在前一个微信状态请求未完成时继续发新请求，且默认使用深探测 `probe=true`，导致 OpenClaw `channels.status` 堆积 30 秒级并发，二维码登录请求被挤压；首页二维码刷新也缺少互斥，可能叠出多个 `web.login.start`。已改为首页后台微信状态只用浅查询、同一时间只允许一个状态请求；二维码刷新请求同一时间只允许一个；首页二维码相关请求也不再带 `probe=true`。
-决策：不改微信插件、不伪造二维码、不伪造登录；只修首页请求节流与探测强度。已跑 `uv run pytest tests/unit/ui/test_channel_bridge.py`、`pnpm exec vitest run src/__tests__/home-page.test.tsx src/__tests__/settings-page.test.tsx`、`pnpm build`，并重启真实 UI；Chrome MCP 复验显示二维码已出现，10 秒内二维码 hash 保持一致，网络请求不再堆积 `probe=true` 状态查询，OpenClaw 日志本轮只有一次 `web.login.start`。
待办：用户扫码后还需验证连接状态是否变成 `connected/canSendText=true`，以及微信消息入站是否触发工作台。

## 18:59 [codex-doc-edit-subagent]
-完成：按复审意见修订 `docs/A股选股详细设计.md`：补齐 `select_command` 固定 `single_worker_minimal`（模型/dispatch/伪码/测试/live 验收）、`selection_report_handoff` 等价阶段标识、确认去重键 `select_workflow_run_id+ticker`、确认前再校验门、关键 DTO 字段级合同、PM 决策机械解析边界、非持久返回码边界，以及对应测试矩阵补全。
-决策：严格以 `docs/A股选股总体设计.md` 为唯一主设计来源；未把评分权重/策略阈值/CLI 权限/UI 形态/非 CN_A 策略写成已决定；仅做文档修改，不改代码、不跑 live/provider。
待办：等待复审；若有新评审意见，继续在详细设计文档内最小增量修订。

## 19:03 [codex-selection-design-review-loop]
-完成：完成 A股选股详细设计的子代理修订与独立复审闭环；复审 verdict 为“符合 / 可进入实现”，并补掉两个非阻塞建议：公司名只能从候选池确定性映射补齐、确认伪码使用显式 ticker 集合比较。
-决策：复审已通过，未继续扩大范围；保留第 15 章待人类确认项，不把评分权重、策略阈值、UI 形态或非 CN_A 策略写成决定。
待办：无。

## 19:02 [codex-wechat-ordinary-inbound-chat]
-完成：查明“微信已连接但发 hi 无反应”的两层根因并修复：后端旧逻辑只处理 `/report`，普通微信文字返回 `handled=false` 被插件吞掉；同时微信插件入站 UI 等待默认只有 5 秒，而真实 DeepSeek 普通聊天本轮耗时 26.218s，插件会先超时。已改为普通微信文字走普通聊天并返回 `replyText`，入站等待默认改为 60 秒。
-决策：不把普通聊天偷偷转成报告流程，不伪造微信消息、不伪造 LLM 回复；`/report` 仍走确认卡。已跑后端/插件/启动脚本聚焦测试，并重启真实 UI；`runtime.env` 显示 `CLAW_TRADE_UI_INBOUND_TIMEOUT_MS=60000`，直接打 `/api/ui/channel-inbound-message` 返回 `handled=true/state=replied`，回复 `Hi. How can I help you?`，HTTP 200，耗时 26.218s。
待办：如需证明真实手机微信端已收到回复，需要用户在当前已连接状态下再发一条 `hi`，再核对 OpenClaw 入站日志、UI bridge POST 和微信最终回复。

## 19:14 [codex-wechat-inbound-latency-ack]
-完成：针对“微信有反应但很慢”补上真实即时反馈：普通微信文字在进入 UI/模型链路前先排一条 `收到，正在处理。`，最终模型回复回来后再排第二条；报告类文本仍不插入即时反馈，避免 `/report` 确认卡前多一条消息。
-决策：没有伪造最终回复，只发送真实处理中的状态；根因证据显示上一轮真实手机消息从入站到 agent session start 约 14.5s，模型本身约 2.7s，最终出站总约 24s。已跑 `uv run pytest tests/contracts/test_frontline_tool_protocol.py::test_reply_dispatch_hook_forwards_wechat_inbound_and_uses_ui_reply_text tests/contracts/test_frontline_tool_protocol.py::test_reply_dispatch_hook_sends_immediate_ack_for_ordinary_wechat_text tests/contracts/test_frontline_tool_protocol.py::test_reply_dispatch_hook_does_not_send_when_ui_returns_unhandled`（3 passed），测试证明即时反馈在 UI fetch 返回前入队。已重启 `scripts/start-research-ui.sh`，健康检查 `18789/health` 与 `1933/health` 均通过，Chrome MCP 打开工作台无 console error。
待办：等待用户手机发送新消息，核对真实微信端第一条即时反馈与第二条模型回复的出站时间。

## 19:28 [codex-settings-doc-card-visual-fix]
-完成：按设置视觉文档收敛设置页卡片/面板实现：`ct-*` token 改为文档精确值，增强数据源列表行改成“名称 / 元信息 / 操作”的三列结构，去掉增强源行的卡片化圆角和额外内边距，保留设置页三块主面板、模型区 Embedding 弱分隔、微信二维码区和任务卡片样式。
-决策：不新增未批准字段、不改设置模块功能语义、不做全站视觉重构；补强前端测试断言，防止只查 class 不查文档 CSS。已跑 `pnpm --dir web/research-ui test -- settings-css-onboarding settings-css-sections settings-page settings-enhanced-sources report-task-card`（15 files / 62 tests passed）与 `pnpm --dir web/research-ui build`；Chrome MCP 打开真实 `/settings`，console error 0；截图落点 `.runtime/test-artifacts/settings-doc-card-fix/settings-page.png`。
待办：如果还要继续提高“卡片感”，需要先更新视觉文档，因为当前文档要求的是浅色、细边框、低阴影、无嵌套卡片。

## 21:31 [codex-settings-card-visual-correction]
-完成：按用户现场反馈重新用 Chrome 查看设置页，确认上一版只是“白块”，卡片层级不够明显；已增强设置三块主面板的可见边界和阴影，标题区加入底部分隔线，增强源选中行加入左侧强调线，让设置页肉眼呈现为三张独立卡片。
-决策：仍不新增字段、不做嵌套卡片、不把增强源做成大卡片堆叠；只改视觉层级。已跑 `pnpm --dir web/research-ui test -- settings-css-onboarding settings-css-sections settings-page settings-enhanced-sources report-task-card`（15 files / 62 tests passed）与 `pnpm --dir web/research-ui build`，并用 Chrome MCP 打开真实 `/settings?card-audit=2`，console error 0；截图落点 `.runtime/test-artifacts/settings-doc-card-fix/settings-page-card-audit.png`。
待办：无。

## 21:42 [codex-settings-source-category-cards]
-完成：按用户要求把增强数据源从 12 行长列表改成按功能分组的卡片：A股数据、基本面、行情、新闻与情绪、加密货币；每张卡片内用下拉列表选择该功能的数据源，当前编辑区只编辑被选中的具体源。
-决策：保留固定 12 个已批准源，不新增未批准 provider，不开放任意 HTTP/JSON、优先级或适用范围；改掉前端测试里“必须 12 行长列表”的锁死断言，改为验证 5 个功能卡片、12 个批准源在对应下拉中、未批准源不出现。已跑 `pnpm --dir web/research-ui test -- settings-enhanced-sources settings-css-sections settings-page settings-responsive-motion settings-forbidden-surface`（15 files / 62 tests passed）与 `pnpm --dir web/research-ui build`；Chrome MCP 打开真实 `/settings?source-card-group=1`，console error 0，并实测 A股数据下拉切到 AKShare 后当前编辑变为 AKShare；截图落点 `.runtime/test-artifacts/settings-source-category-cards/settings-page.png`。
待办：无。

## 22:08 [codex-settings-global-source-market-tabs]
-完成：按用户最新要求把增强数据源改成两级结构：第一层是市场 tab（A股、港股、美股、全球、加密货币），第二层是功能卡片（行情、基本面、新闻公告、社交舆情、宏观、链上等），每张卡片内用下拉列表选择具体数据源；同一个数据源只保存一份配置，因此同源出现在多个市场/功能时会共享“已设置/启用/测试结果/掩码密钥”状态。
-决策：固定内置主流目录已扩展到 A股、港股、美股、全球宏观、新闻社交和加密货币常用源，包含 Tushare、AKShare、东方财富/股吧、雪球、同花顺、巨潮、BaoStock、Wind、Choice、iFinD、HKEX、AAStocks、富途、LongPort、Yahoo Finance、已删除数据网关、Polygon、FMP、Finnhub、SEC EDGAR、NewsAPI、X.com、Reddit、Stocktwits、FRED、CoinGecko、CoinMarketCap、Binance、OKX、CCXT、Coinglass、Glassnode、Santiment、Messari、LunarCrush 等；仍不开放任意 HTTP/JSON、自定义 mapping、用户调整优先级或适用范围。新增源先作为固定可填写目录出现；当前只有原已接入源有真实 probe-backed 测试，新源若没有真实探测器必须失败，不能假测试通过。
待办：如要让所有新增源的“测试连接”和报告链路都真实可用，需要逐个补真实健康探测、凭据校验和数据适配器，并分别做 live/provider 证据。

## 22:17 [codex-settings-global-source-catalog-expansion]
-完成：继续补齐明显漏掉的全球主流源，并验证真实页面的两级目录：A股补腾讯财经、中证指数、CSMAR、RESSET、JoinQuant、RiceQuant；全球/美港股补 Bloomberg、LSEG Refinitiv、FactSet、Morningstar、S&P Capital IQ、TradingView、Barchart、Finazon、Reuters、Dow Jones、GDELT；宏观补 IMF、BIS、Eurostat、ECB、BLS、BEA、EIA、US Treasury；加密补 Coin Metrics、Dune、Nansen、Token Terminal、The Graph、Kaiko、Amberdata。
-决策：仍然是固定主流目录，不开放任意 HTTP/JSON 或用户自定义数据源；测试里新增 Reddit 共享状态用例，证明同一个源出现在港股社交和全球新闻时共享同一份已填写状态。已跑 `uv run pytest tests/settings/test_enhanced_sources.py tests/settings/test_enhanced_source_health_probes.py tests/unit/ui/test_settings_security.py -q`（67 passed）、`pnpm --dir web/research-ui test -- settings-enhanced-sources settings-css-sections settings-page settings-responsive-motion settings-forbidden-surface`（15 files / 62 tests passed）、`pnpm --dir web/research-ui build`（通过）。Chrome/Playwright DOM 读取确认 A股、港股、美股、全球、加密货币五个 tab 的下拉列表均包含新增源；截图落点 `web/research-ui/.runtime/test-artifacts/settings-source-market-tabs/settings-page-global-catalog-final.png`。
待办：`uv run pytest tests/settings/ui/test_settings_sections_visual.py -q` 当前卡在本机 `reportModelStatus.state=unconfigured` 的前置条件，未跑到数据源断言；如要跑该 live 证据，需要先配置并测试通过报告模型。

## 22:22 [codex-settings-source-doc-sync]
-完成：同步 `docs/设置模块设计文档.md` 与 `docs/设置模块任务清单.md`，把旧的“固定 12 源 / 不做 tab / 增强源列表行”更新为 2026-05-24 人类覆盖确认的“固定全球主流源目录 / 市场 tab / 功能卡片 / 数据源下拉 / 同源共享配置状态”。
-决策：文档仍保留禁止任意 HTTP/JSON、自定义 provider、provider 优先级和适用范围选择；只把已经获人类确认的数据源目录与 UI 结构同步到设计和任务清单，防止后续 agent 按旧 12 源要求回滚。
待办：无。

## 22:23 [codex-doc-edit-subagent]
-完成：新增 `docs/A股选股实施任务清单.md`，按可派发格式拆解 SEL-00~SEL-11（含前置“原项目算法审计与等价映射”），并最小修订 `docs/A股选股详细设计.md`：将 `fake OpenClaw success` 测试改为“真实 focused run 证据优先”，重写第 15 章仅保留真实未知项，移除本轮已确认事项的“待确认”表述。
-决策：保持改动只在文档与 memory，不改源码/测试/AGENTS；任务清单明确 OpenClaw 只跑 single worker turn、`/select` 只读 latest completed run、candidate pack 仅事实可复算、provider payload 为最终验收证据、第一版不做手动 backfill/rerun 用户入口。
待办：等待实现 agent 按 SEL-00 前置审计先锁定三仓库最新算法映射，再进入代码实现。

## 22:32 [codex-manager]
-完成：根据独立复审的两个非阻塞建议，补强 `docs/A股选股实施任务清单.md`：SEL-01 明确覆盖详细设计 §3 全量模型合同，SEL-03 明确默认北京时间 16:00 口径优先于旧 16:30/17:00 背景描述。
-决策：复审 verdict 已为“可交给实现 agent”，本轮仅消除实现歧义，不改源码、不新增测试、不放松 fake/mock/fallback 禁令。
待办：补强后再做一次只读复核，确认任务清单仍可派发。

## 23:05 [codex-settings-coingecko-pro-source-ui]
-完成：按人类确认把普通设置页增强数据源收敛为“需要用户填写密钥或凭证的外部增强源”，移除前端设置目录中的默认免费源/免密源/内部接入层展示，CoinGecko 改为 CoinGecko Pro，并把切换市场/选择下拉后下方“配置详情”同步到当前源。
-决策：CoinGecko Pro 按官网 Pro API 口径使用 `https://pro-api.coingecko.com/api/v3` 与 `x-cg-pro-api-key`；CoinGecko 免费/公共接口、已删除数据网关、AKShare、Yahoo Finance、SEC EDGAR、Binance、OKX、CCXT 等不进入普通设置页或 S-05 provider list。已跑 `uv run pytest tests/settings/test_enhanced_sources.py tests/settings/test_enhanced_source_health_probes.py -q`（61 passed）、`pnpm --dir web/research-ui test -- settings-enhanced-sources settings-css-sections settings-page settings-forbidden-surface`（15 files / 62 tests passed）、`pnpm --dir web/research-ui build`（通过）；已重启 `scripts/start-research-ui.sh` 到新代码，`/api/ui/list-data-sources` 返回 51 个密钥型增强源、包含 `coingecko_pro`、默认/内部源交集为空；Chrome MCP 打开真实 `/settings`，切到“加密货币”后确认下拉显示 CoinGecko Pro，配置详情同步为 CoinGecko Pro，截图 `settings-data-source-coingecko-pro-after-restart.png`。
待办：`tests/contracts/test_ui_http_routes.py::test_extended_ui_api_routes_return_json_not_spa_html` 仍因本机模型连接测试返回 503 未通过，和本轮数据源改动无关，后续需要单独处理模型测试环境或该合同测试预期。
