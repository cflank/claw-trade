## A 股 seed-to-Mongo 规则收口
- 人类决策：11 个指数 `sh.000816`、`sh.000833`、`sh.000842`、`sh.000939`、`sh.000940`、`sh.000950`、`sh.000951`、`sh.000957`、`sh.000958`、`sh.000963`、`sh.000975` 明确不下载，不纳入 `index_daily` 覆盖失败条件。
- 人类决策：单个股票行业映射缺失只记录为数据质量缺口，不阻断入库或 `/select`。
- 人类决策：A 股日线起止日期缺口只记录为数据现象，不作为失败理由；停牌、退市、无交易、上市日或 provider 历史范围差异不能被直接写成失败。
- 代码变更：`selection_seed_importer.py` 排除上述 11 个指数；行业代码缺失为 warn；qfq/index daily 起止日期不再作为 blocker；仍保留 manifest、必要文件、空文件、字段、重复冲突、复权口径、Mongo/evidence 写入失败等真实性阻断项。
- 检查脚本变更：`audit_baostock_selection_seed.py` 对上述三类情况只记录 observation 或排除，不再返回失败。
- 文档变更：`docs/数据层总体设计.md`、`docs/数据层详细设计.md`、`docs/数据层实施任务清单.md`、`todolist.md` 和 `scripts/validation/data_layer_acceptance_plan.py` 已同步；T9A 不再作为 blocker，T9B 仍按人类决定暂不做。
- 真实 seed 检查：`uv run python scripts/selection/audit_baostock_selection_seed.py --root .runtime/selection-data/raw/baostock --start-date 2015-01-01 --trade-date 2026-05-27 --run-label t9a-approved-nonblockers-20260530 --max-samples 20` -> exit 0，`status=completed`；仍记录 `missing_industry_observation_count=1` 和 `incomplete_daily_observation_count=189`，但不阻断。
- Mongo readback：本机 Mongo `ping ok`；`removed_data_gateway_raw_payloads=12623`、`removed_data_gateway_normalized=12644`、`removed_data_gateway_provider_attempts=12875`；run `t9a-real-20260529-reconcile-v2` 有 `attempts_for_run=12490`、`normalized_for_run=12450`。

## 数据层文档/代码一致性二审收口
- 人类要求：按“总管 AI”方式修改，并派 subagent 专门检查代码与文档一致性；不要写一个小功能就马上测试，全部写完后统一测试。
- 已闭环：`DataRequirement` 现在显式包含 `data_type`、`granularity`、`date_range`、`lookback_window_days`、`source_role_required`、`field_set`；`report_plan` 和 `select_plan` 会写入这些字段。
- 已闭环：`/report` DomainPack 先查 Mongo 主仓库；A 股 Baostock 日线仓库只覆盖 `cn_a_market_kline`，不能冒充实时 quote/orderbook；fundamental/news/social 使用各自 domain 的 `removed_data_gateway_normalized` 仓库结果。
- 已闭环：`/select` data job 先生成 `SelectDataPlan`，再取 provider/仓库结果；candidate pack 接受 `mongo://removed_data_gateway_normalized/...` lineage，但仍要求 provider attempts、normalized refs、feature refs、score refs 和 blocker gap fail-closed。
- 已闭环：Crypto `/select` 在历史包未批准下载并入 Mongo 时产出 `mongo_missing` blocker gap；Mongo 主仓库未配置时 DomainPack 产出 `mongo_missing` 缺口，不再静默跳过主仓库检查。
- 仍未完成：`/report` requirement 仍由 `RunProviderPlan.call_specs` 反推，不是完整的 worker 显式需求收集；`SelectDataPlan.provider_call_specs` 仍为空，表示 selection provider spec 迁移未完成。不得把这两项说成完成。
- 验证：`uv run pytest tests/contracts/test_data_layer_models.py tests/unit/data_gateway/test_models.py tests/contracts/test_report_data_plan_cutover.py tests/unit/data_gateway/test_domain_pack_service.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_coverage_ledger.py` -> exit 0，`79 passed, 8 warnings`；warnings 为未注册 `pytest.mark.integration`。

## 数据层剩余 1-3 项实现
- 人类追问：除真实 Chrome `/select` 验收外，其它缺口为什么不实现；本轮按要求继续实现，不把 Chrome 验收用单测冒充。
- 已闭环：`/report` ReportDataPlan 改为按 worker 显式需求生成 consumer/requirement。frontline 四个 worker 映射到 market/fundamental/news/social domain；有 provider spec 时继承 spec 的 data_type/granularity/source_role/field_set，缺 provider spec 时写 missing-provider requirement。
- 已闭环：`/select` CN_A 仓库缺失时 `SelectDataPlan.provider_call_specs` 列出 mootdx、腾讯、新浪、baostock、eastmoney、akshare、tushare 七个补缺口候选；仓库 fresh 时为空；Crypto 历史包未批准时仍只返回 `mongo_missing` blocker gap，不生成假补取候选。
- 已闭环：DomainPackService 非 market 仓库命中必须同时匹配 market/domain/schema/ticker/freshness/required field groups。fundamental 缺 `valuation.pb` 或 `financial_indicators.roe` 不会成为 warehouse_hit；news/social 接受 `title/headline/summary` 字段组。
- 文档同步：`docs/数据层详细设计.md` 已把 select provider_call_specs 语义改成“CN_A 缺仓库时列 provider 候选；fresh 时可为空；Crypto 未批准历史包只返回 blocker gap”。
- 验证：`uv run pytest tests/contracts/test_data_layer_models.py tests/unit/data_gateway/test_models.py tests/contracts/test_report_data_plan_cutover.py tests/unit/data_gateway/test_domain_pack_service.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_coverage_ledger.py` -> exit 0，`81 passed, 8 warnings`；warnings 为未注册 `pytest.mark.integration`。
- 未完成：真实 Chrome 输入 `/select` 的 UI 验收仍未跑；不能把本轮单测/集成测试说成 `/select` 真实 UI 完成。

## 数据层一致性三审补丁
- subagent 复核指出上一版仍是“worker 名字 + provider spec 派生需求”，不算完整 worker 显式需求；还缺 Crypto data job 级 fail-closed 测试和仓库 stale/ticker/schema 负例。
- 已修：`report_plan.py` 增加显式 frontline worker needs 表。CN_A `market_analyst` 明确需要 quote/kline/orderbook；`fundamental_analyst` 明确需要 financials/estimates/research；`news_analyst` 明确需要 announcement/company/flash/macro；`social_analyst` 明确需要 concept/search discovery。provider spec 只绑定 requirement_id、granularity、required_fields。
- 已修：`workflow/runner.py` 写入 run provider plan 前，用 ReportDataPlan 的 bound provider specs 替换 `plan.call_specs`，保证 pack runtime 读到同一批 requirement_id/required_fields。
- 已修：`selection/data_job.py` 在初始 SelectDataPlan 有 blocker gap 且没有批准 provider 补取计划时直接 fail closed；Crypto 历史包未批准不会调用 provider_fetch。
- 已修：`FundamentalPackBuilder` 使用 `spec.params.required_fields` 作为 required field gap 来源；仓库字段不足、stale、ticker mismatch、schema mismatch 均有负例测试。
- 验证：`uv run pytest tests/contracts/test_data_layer_models.py tests/unit/data_gateway/test_models.py tests/contracts/test_report_data_plan_cutover.py tests/unit/data_gateway/test_domain_pack_service.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_coverage_ledger.py` -> exit 0，`84 passed, 8 warnings`；warnings 为未注册 `pytest.mark.integration`。
- 未完成：真实 Chrome 输入 `/select` 的 UI 验收仍未跑。

## 数据层一致性四审补丁
- subagent 三审指出 4 个残缺：A 股 extension pack 仍可能由 provider spec 生成 requirement；`removed_data_gateway_run_provider_plans` Mongo roundtrip 丢 `ProviderCallSpec.requirement_id/data_type`；A 股日线仓库未校验 doc.domain；缺 Mongo roundtrip 测试。
- 已修：CN_A policy/hot_money/lockup 增加显式 report pack needs；`report_plan.py` 主路径不再从 provider spec 生成 requirement，provider spec 只用于绑定已有 requirement。
- 已修：`MongoRunProviderPlanStore` 序列化/反序列化保留 `ProviderCallSpec.requirement_id` 和 `data_type`；新增 roundtrip 测试确认 `params.requirement_id`、`params.required_fields` 保留。
- 已修：A 股 qfq daily 仓库命中必须满足 doc.domain 为 `market` 或 `select_feature`；新增 wrong-domain 负例测试。
- 验证：`uv run pytest tests/contracts/test_data_layer_models.py tests/unit/data_gateway/test_models.py tests/contracts/test_report_data_plan_cutover.py tests/unit/data_gateway/test_domain_pack_service.py tests/unit/data_gateway/test_run_provider_plan.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_coverage_ledger.py` -> exit 0，`100 passed, 8 warnings`；warnings 为未注册 `pytest.mark.integration`。
- 未完成：真实 Chrome 输入 `/select` 的 UI 验收仍未跑，不能宣称 `/select` UI 完成。

## CN_A /report runtime 测试（仅测试，不改代码）
- 按 fixed runtime 脚本执行单市场 `CN_A 600519.SH`：`scripts/start-control-runtime.sh -- bash -lc '... uv run python scripts/run_claw_trade_fresh_report.py --ticker 600519.SH --company-name 贵州茅台 --market CN_A --profile CN_A --currency CNY --currency-symbol RMB ...'`；sandbox 内 Mongo 启动权限失败后改为 `require_escalated` 重跑。运行在 `run-20260530-172848-055edc22` 卡在 OpenClaw gateway 子进程等待（`openclaw_local_runner.py -> subprocess.run(... communicate/select)`），人工中断后退出 `130`；状态停在 `investment_debate_running`，无 `final-report.md`、无 `capture_summary.json`，因此 已删除数据网关 evidence-chain audit 与 OpenViking runtime evidence 本次未产出；无 fake/mock/fallback 修改。

## 数据层一致性五审补测
- subagent 四审确认代码层面 1-4 基本闭口，剩余为测试覆盖：extension provider specs 未逐项证明绑定显式 requirement；Mongo roundtrip 还不是真实 BSON 行为。
- 已补测试：`test_report_command_binds_cn_a_extension_provider_specs_to_explicit_requirements` 通过完整 `/report` 初始化路径验证 policy/hot_money/lockup provider specs 全部绑定显式 requirement，并保留 `params.requirement_id/granularity/required_fields`。
- 已补测试：`test_mongo_run_plan_roundtrip_keeps_requirement_binding` 改用 `bson.BSON.encode/decode` 模拟 Mongo BSON 往返，验证 `ProviderCallSpec.requirement_id/data_type/params.required_fields` 仍保留，且 `required_fields` 以 Mongo list 形态返回也可用。
- 验证：`uv run pytest tests/contracts/test_data_layer_models.py tests/unit/data_gateway/test_models.py tests/contracts/test_report_data_plan_cutover.py tests/unit/data_gateway/test_domain_pack_service.py tests/unit/data_gateway/test_run_provider_plan.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_coverage_ledger.py` -> exit 0，`101 passed, 8 warnings`；warnings 为未注册 `pytest.mark.integration`。
- 未完成：真实 Chrome 输入 `/select` 的 UI 验收仍未跑，不能宣称 `/select` UI 完成。

## /report 四市场重跑与 provider 复盘
- 人类要求：重新开始四市场 `/report`，不要因为无输出中断；监测 provider 获取数据情况，有失败就记录供复盘。
- 固定 runtime：均通过 `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py ...` 串行执行；OpenViking 1933、OpenClaw 18789、本地 Mongo；无并行报告 run。
- CN_A `600519.SH` run `run-20260530-175427-559024c2`：命令 exit 1；13 个 worker/报告润色阶段跑完，`runs/.../reports/final-report.md` 存在，但状态 failed。失败原因为 `openviking_lineage` 拒绝 blocked DomainPack；provider attempts 35 条，状态分布 `warehouse_hit=2, remote_success=5, empty=18, remote_error=8, not_applicable=2`；已删除数据网关 evidence-chain audit exit 0/pass。证据：`docs/evidence/report-four-market-restart-20260530/cn_a_600519/`。
- HK `00700.HK` run `run-20260530-180715-6c876235`：命令 exit 1；13 个 worker/报告润色阶段跑完，`runs/.../reports/final-report.md` 存在，但状态 failed。失败原因为 `openviking_lineage` 拒绝 blocked DomainPack；provider attempts 13 条，状态分布 `remote_success=5, field_missing=1, remote_error=6, empty=1`；已删除数据网关 evidence-chain audit exit 0/pass。证据：`docs/evidence/report-four-market-restart-20260530/hk_00700/`。
- US `AAPL` run `run-20260530-181815-e74aa215`：命令 exit 1；12 个核心 worker 跑完，状态 failed。失败原因为 `export_report_assets: 未找到可复制的图表资产`；provider attempts 9 条，状态分布 `remote_success=6, remote_error=3`；已删除数据网关 evidence-chain audit exit 0/pass。证据：`docs/evidence/report-four-market-restart-20260530/us_aapl/`。
- CRYPTO `BTC` run `run-20260530-182722-46599e29`：命令 exit 1；13 个 worker/报告润色阶段跑完，`runs/.../reports/final-report.md` 存在，但状态 failed。失败原因为 `openviking_lineage` 拒绝 blocked DomainPack；provider attempts 29 条，状态分布 `warehouse_hit=12, remote_success=10, cache_hit=4, field_missing=1, rate_limited=1, remote_error=1`；已删除数据网关 evidence-chain audit exit 0/pass。证据：`docs/evidence/report-four-market-restart-20260530/crypto_btc/`。
- 汇总文件：`docs/evidence/report-four-market-restart-20260530/summary.md` 与 `summary.json`。本轮没有 fake/mock/fallback；失败都按真实 attempts 和 run state 记录。

## /report 缺数据不阻断最终报告收口修正
- 人类纠正：`/report` 里的“假成功”不是指缺数据就禁止最终报告；正确口径是最终报告继续生成，缺数据、provider 失败、图表缺失必须记录和注明，不能冒充全量数据成功。`/select` 的 fail-closed 策略字段规则不受本修正影响。
- 代码修正：`已删除数据网关MongoLineageWriter` 不再把普通 `blocked DomainPack`（remote_error/empty/field_missing 等数据源失败）当作最终报告失败；现在会继续写 final-report -> PM -> worker -> provider attempts lineage，并在 `openviking/lineage-relations.json` 里新增 `pack_statuses`，保留每个 pack 的 approval_status、source_summary、attempt_statuses、data_gaps。仍保留证据写入失败 `evidence_write_failed` 为阻断。
- 代码修正：final report 导出遇到没有可复制图表资产时，不再直接失败；如果报告没有声称图表已生成，则插入“本次未附图表”说明并继续导出。若报告声称图表已生成但资产缺失，仍按真实性失败。
- 测试：`uv run pytest tests/unit/data_gateway/test_openviking_lineage_writer.py tests/contracts/test_final_report_exporter.py tests/unit/test_control_runner.py` -> exit 0，`45 passed`。
- 真实 CN_A `/report` 复验：`scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py --ticker 600519.SH --company-name 贵州茅台 --market CN_A --profile CN_A --currency CNY --currency-symbol RMB --output-dir docs/evidence/report-lineage-nonblocking-20260530/cn_a_600519` -> exit 0；run `run-20260530-195309-6aaca01f`；state `completed`；export `passed`；workers `21`；final_report_assets `2`；provider attempts `35`，状态分布 `empty=18, remote_error=8, remote_success=5, warehouse_hit=2, not_applicable=2`；lineage `relation_count=117`，`pack_statuses` 为 `blocked=6, approved=1`；已删除数据网关 evidence-chain audit `passed=true`。
- 真实 US `/report` 复验：`scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py --ticker AAPL --company-name Apple --market US --profile US --currency USD --currency-symbol '$' --output-dir docs/evidence/report-lineage-nonblocking-20260530/us_aapl` -> exit 0；run `run-20260530-200306-443c0912`；state `completed`；export `passed`；workers `18`；final_report_assets `0`；final report 写入 `本次未附图表`；provider attempts `12`，状态分布 `warehouse_hit=5, remote_error=4, remote_success=3`；lineage `relation_count=49`，`pack_statuses` 为 `blocked=3, approved=1`；已删除数据网关 evidence-chain audit `passed=true`。
- 未跑：本修正后没有重跑 HK/CRYPTO。CN_A 覆盖 blocked DomainPack 不阻断，US 覆盖缺图表不阻断；HK/CRYPTO 同类 lineage 行为由 focused tests 与 CN_A live proof 覆盖，但没有新的 HK/CRYPTO live run。

## 四市场 /report subagent 测试复盘
- 人类要求：用 `gpt-5.3-codex`、`high` 子代理依次测试 CN_A/HK/US/CRYPTO 全链路 `/report`，记录所有数据源问题供复盘；本轮不修代码。
- 总管 preflight：固定 runtime `scripts/start-control-runtime.sh`，repo `uv`，OpenViking `1933`，OpenClaw `18789`，no invest sidecar；`OPENVIKING_CONFIG_FILE=.runtime/dev-services/openviking/ov.conf`，`OPENVIKING_DATA_DIR=.runtime/dev-services/openviking/data`，MCP/外部 OpenViking server unset，`CLAW_TRADE_OPENVIKING_MCP_STARTED=0`，`1933/health` 与 `18789/health` 均通过。
- CN_A `600519.SH`：run `run-20260530-211749-4f0aebbd`，命令 exit 0，state `completed`，final report 存在；provider attempts 35 条，状态分布 `warehouse_hit=2, remote_success=5, not_applicable=2, empty=17, remote_error=9`。主要问题：`eastmoney_datacenter` 多 endpoint 空返回/404、`cninfo` 多 endpoint 空返回、`eastmoney_company_news`/`ths_concept_hot`/`baidu_concept` adapter 未接入、`ths_hot` 401、`eastmoney_push2his` 断连。证据：`docs/evidence/report-four-market-subagents-20260530/cn_a_600519/`。
- HK `00700.HK`：子代理关闭前留下半截 run `run-20260530-213046-d59e2b1c`，state 停在 `frontline_running`，未生成 final report/证据目录；本轮不能算 HK 全链路通过。
- US `AAPL`：子代理关闭前留下半截 run `run-20260530-213512-3398e05f`，state 停在 `investment_debate_running`，未生成 final report/证据目录；本轮不能算 US 全链路通过。
- CRYPTO `BTC`：默认 sandbox 在启动 MongoDB 后 exit 1；提权重跑未返回完成行，未拿到可确认 run_id，`crypto_btc` 证据目录为空；本轮不能算 CRYPTO 已启动完成。
- 汇总证据：`docs/evidence/report-four-market-subagents-20260530/summary.md` 与 `summary.json`。Collect-first：四项都记录；early-stop exception 未使用；本轮无代码修复。
