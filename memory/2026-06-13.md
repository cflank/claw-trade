# 2026-06-13

## DataNeed / 限流重组收口

- 按 `docs/限流重组.md` 继续收口数据层：报告 worker 远端取数入口保持 `DataNeed -> planner -> ProviderCallSpec -> execution gate / rate limit / ingest`，不恢复固定 pack。
- `run_data_need_tool_request` 接入运行时 `ExecutionGate`，支持 cache hit、shared result、cooldown、rate limited、owner fetch；同一 semantic need 可跨 worker 复用。
- 修复 batch key：包含参数值指纹，避免不同标的/参数误共享。
- raw-only 官方接口不再伪装 normalized；Coinglass/Glassnode generated raw 只保存原始证据，缺 parser 时保留 `parser_missing`。
- raw-only 且真实出网成功时允许共享 raw refs，但仍返回 `parser_missing`，不冒充结构化数据 ready。
- `provider_empty` / `permission_denied` 闭环：空结果统一为 `provider_empty`；HTTP 401/403 和 Tushare 权限/套餐/token 类业务错误归为 `permission_denied`；Tushare 频次类业务错误归为 `rate_limited`。
- Scheduler 接入 runtime `RateLimiter`，用已有滑动窗口状态做预算判断；同优先级保持 planner 原始顺序。
- managed_http 子请求走同一滑动窗口限流；测试证明配置 10/min 时第 11 次不出网并返回 `rate_limited_by_tool_budget`。
- resolver 不再用 output fields / response_fields 作为唯一候选命中依据；字段只作结果/解析参考。CoinGecko Pro `/derivatives` 增补为明确官方目录语义，不靠字段碰巧命中。
- 删除旧 fixed report pack 入口残留：`data_pack_bridge.py`、旧 social fresh acceptance 脚本等已移除；旧 `claw_get_*_pack` 只剩文档说明和防回潮测试。
- Selection 旧“候选事实包”文案改为“候选缓存”；`select_workflow_run_id` 只保留 ID，新增 `selection_prompt_context` 承载模型可见材料，避免身份字段污染工具 metadata。
- active docs 中旧 prefetch/domain pack/RunProviderPlan/PackRequest/market pack 目标态已加 2026-06-13 状态修正，明确被 `docs/限流重组.md` 取代。

## Review / 测试

- Subagent 只读审查 1：未发现 DataNeed/限流/官方目录 P0；指出 raw-only 共享、permission_denied、scheduler、字段匹配、selection ID 复用等问题，均已处理。
- Subagent 只读审查 2：未发现固定 pack 主路径回流；指出 active docs 旧口径，已加 superseded 状态修正。
- Focused 验证：`uv run pytest ... -q -p no:cacheprovider` 覆盖 DataNeed、官方目录、限流、ingest、selection、工具边界，最终 `378 passed`，仅剩既有 unknown integration mark warnings。
- `git diff --check` 通过。
- 按人类要求，本轮没有跑 live/full-chain。

## 02:11 [codex-data-layer-unattended-manager]
- 完成：按无人值守口径继续收口数据层缺口：公共 `DataAPI.get_data/get_data_batch` 已禁止非 `warehouse_only` 的旧 `DataRequest` 远端出网，validation market probe 改为 `DataNeed -> get_data_needs`，maintenance 仍走 `DataNeed`，UI source probe 为 config-only 不直连远端。
- 完成：业务范围字段防回潮已补到四层：official catalog、planner、`ProviderCallSpec.params` 模型、official executor 均递归拒绝 `allowed_*` 和 `only_for_*` 通配；`fields/coverage_fields/consumer/domain/worker/source_role_required` 不作为 provider 候选否决的合同测试已补。
- 完成：active docs 与技能文案收口：架构运行顺序、数据层总体/详细设计、数据层任务清单、A 股选股、加密历史数据、crypto source boundaries 均改为 `DataNeed -> planner -> ProviderCallSpec -> scheduler/gate/fetch/ingest` 口径；旧 `ProviderSelector/ProviderBatchPlan` 仅保留为明确旧兼容/背景章节。
- 验证：focused 回归和宽回归通过；最终宽回归 `uv run pytest tests/unit/data_gateway ... tests/settings/test_enhanced_sources.py -q -p no:cacheprovider` 为 `532 passed, 1 warning`，warning 是既有 unknown `integration` mark；`py_compile` 和 `git diff --check` 通过。
- 评审：代码复审、测试覆盖复审、文档代码一致性复审 subagents 最终均未发现 P0/P1/P2。未跑 live/full-chain，遵守“代码缺口完成前不反复全链测试”。

## 02:34 [codex-data-layer-completion-audit]
- 完成：收尾审计发现 `docs/A股扩展详细设计.md` 和 `docs/数据层详细设计.md` 仍有旧目标态话术残留，已改成当前 `DataNeed -> ProviderCallSpec -> scheduler/gate/fetch/ingest` 合同；旧 `source_role_matches` 伪码已删除，A 股扩展中的旧 pack builder 目标态已替换为 DataNeed 数据结果合同。
- 复扫：`src/scripts/openclaw_plugins/agents` 中未发现 `_CN_A_DOMAIN_DATASETS`、`_CRYPTO_DOMAIN_DATASETS`、`run_frontline_data_pack`、`claw_get_*_pack`、`data_pack_bridge`、`candidate_pack`、`SelectionProviderBatchPlan` 残留。
- 复扫：公共 `api.get_data_batch` 远端入口已被阻断；剩余产品调用只在 selection 本地仓库检查，且请求为 `warehouse_only`。数据层的 `allowed_*` / `only_for_*` 只作为拒绝输入的防回潮字段；其它 `allowed_tickers/allowed_tools` 属于选股候选或工具权限边界，不是 provider 能力白名单。
- 验证：非 live 宽回归重新通过，命令为 `uv run pytest tests/unit/data_gateway ... tests/settings/test_enhanced_sources.py -q -p no:cacheprovider`，结果 `532 passed, 1 warning`；warning 仍是既有 unknown `integration` mark。`py_compile`、`git diff --check` 通过。未跑 A 股/BTC 全链 live。

## official catalog source evidence / parser status

- 完成：官方目录入口统一补来源证据。所有 catalog endpoint 都必须有 `official_doc_ref`、`response_shape.source`，并有 `observed_from_url` 或 `sdk_module`。
- 完成：有 `response_shape.outputs` 的接口默认标为 `normalized`；不再出现“已经有输出结构但仍标成 parser_missing”的矛盾。`raw_only` 仍保持 raw-only，不伪装结构化。
- 完成：生成目录加载器不再只给 Coinglass/Glassnode 的 outputs 标 normalized；所有带显式输出合同的生成 endpoint 一视同仁。
- 完成：SDK 内部 HTTP catalog endpoint 必须记录 `sdk_module`，保留“无法证明子请求级 managed_http 限流”的残余风险。
- 当前统计：endpoint 1429；`official_doc_ref` 缺失 0；`response_shape.source` 缺失 0；`observed_from_url/sdk_module` 缺失 0；有 outputs 但 parser_missing 0；parser 状态为 normalized 115、parser_missing 1306、raw_only 8；SDK internal 18 且 `sdk_module` 缺失 0。
- 验证：`uv run pytest tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/unit/data_gateway/test_models.py -q -p no:cacheprovider`，结果 `98 passed`。

## A股/BTC full-chain live and DataNeed trace review

- 完成：按固定 runtime 入口执行 A股 600519.SH 全链 live。真实 run 为 `run-20260613-080502-1085dd7d`，状态 completed；第一次导出因预创建输出目录导致 export 命令 exit 1，随后成功导出到 `docs/evidence/fullchain-a-share-crypto-20260613/cn_a_600519_live_exported/`，最终报告为 `final_report.md`，worker 22。
- 完成：按固定 runtime 入口执行 BTC 全链 live。真实 run 为 `run-20260613-082231-37a77611`，命令 exit 0；导出目录 `docs/evidence/fullchain-a-share-crypto-20260613/crypto_btc_live_report/`，最终报告为 `final_report.md`，worker 19。
- 数据层探针证据：A股 `data-layer-cn-a-probe-rerun4` 为 32 needs，28 ready / 1 partial / 2 error / 1 missing；BTC `data-layer-btc-20260613-rerun` 为 19 needs，8 ready / 4 partial / 6 missing / 1 error。
- A股真实缺口：`market_price` 有 Tushare 权限/分钟线 parser 缺口；`sector_snapshot` 主要为 Baostock 行业接口超时；`lockup_event` 为 EastMoney cached_empty。Tushare realtime quote/order-book 有成功证据，不是“盘口只有 mootdx”。
- BTC真实缺口：CoinGecko Pro key 为空、Glassnode key 缺失导致估值/链上/部分衍生品缺口；Coinglass 有多条成功 raw/structured 尝试，但部分 funding/OI/long-short 仍有 parser_missing、timeout 或 tool budget 限流。不能写成“Coinglass 完全没数据”。
- 修复：只读复审指出“多个 DataNeed 合并成一次 provider call 后，失败/无数据路径缺少每个原始 need 的 gap/request trace 覆盖”。已在 `DataService` 中把 DataNeed 结果和 gap request_id 归一到当前原始 need，并补 `test_data_need_merged_failed_call_fans_out_gap_trace_to_each_need`。
- 修复：`DataService._merge_data_need_result` 在所有候选都失败且无数据时保留全部 `attempt_refs` 和 `gaps`，避免只留下最后/最高优先级失败。
- Guard 收口：`tests/contracts/test_guard_change_requires_approval.py` 的 hash allowlist 与当前 DataNeed migration 后的 guard 文件同步，并把来源说明改成真实的 `docs/限流重组.md DataNeed migration`；旧“format-only”说明已移除。未新增新的报告表达 guard。
- 验证：DataNeed focused 4 tests passed；数据层主回归 `170 passed`；限流回归 `52 passed`；官方目录/白名单/provider payload 合同 `17 passed`；tool_calls/guard freeze 合同 `18 passed`；`git diff --check` 通过。
- 未做：按人类“3 不要实现”的指令，没有把 `sdk_internal_unknown` provider 迁移成 managed_http；这些路径仍是风险清单，不能声称 SDK 内部子请求级限流已完成。

## 14:29 BTC frontline derivatives/on-chain parser follow-up

- 完成：针对 `runs/run-20260613-142952-6cce13d5` 只做非 live 根因修复，未改 `runs/` 证据，未跑全链 live，未编辑 `src/claw_trade/data_gateway/report_evidence.py`。
- 证据：provider payload 显示 market analyst 真实请求了 `funding_rate`、`long_short_ratio`、`liquidation`、`onchain_metric`、`exchange_netflow`；Mongo 在 `2026-06-13 14:29-14:34` 窗口有 97 条 `provider_attempts`、0 条 `dataset_manifests`。
- 根因：部分 Coinglass official API 已返回 raw，但 catalog/generated outputs、通用 tabular/nested array 解析、字段别名和 funding interval 粒度归一不足，导致 `parser_missing`、`field_missing`、`granularity_mismatch`；另有 Glassnode/CoinGecko Pro `credential_missing`、Coinglass tool budget 限流和 `onchain_whale_transfer` timeout/provider_error。
- 修复：补 official generated catalog 基于官方 `response_fields` 推断 long/short 与 netflow 输出；补 official_api 通用 tabular records 解析、long/short/netflow/liquidation/exchange balance 字段别名和 funding interval 粒度；补 normalizer 对 `1h/1d` 等接口粒度的规范化。
- 验证：`uv run pytest tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_ingest_pipeline.py tests/contracts/test_official_provider_catalog_contract.py` 为 97 passed；`uv run pytest tests/unit/data_gateway/test_data_need_planner.py` 为 29 passed；相关文件 `git diff --check` 通过。

## 16:09 BTC frontline granularity gap fix

- 根因：BTC frontline 中 `fundamental_analyst` 会补充请求 `market_price` 的 `granularity=weekly/1w/1mo`；计划器、批次构造和报告证据层的粒度归一化不一致，导致可用的 Binance 日线行情没有被用于周/月需求，工具返回过“接口凭证缺失”缺口。
- 修复：新增共享 `canonical_granularity()`；`need_resolver`、`call_planner`、`report_evidence` 共用同一粒度别名表；周/月行情需求允许用日线行情源满足，真实请求 Binance 仍用 `interval=1d`，批次粒度保持 `daily`，不伪装成周/月原生数据。
- 验证：桥接最小验证中 `weekly/1w/monthly/1mo/wk/mon` 均为 `ready`，366 行，模型可见文本无 `数据缺口`。
- Live：最终 BTC frontline run `run-20260613-160934-b1733b8a` completed，stop_point=`frontline_ready`；工具返回统计为 company_news、exchange_netflow、funding_rate、liquidation、long_short_ratio、market_price、onchain_metric、open_interest、social_signal 全部 `可用`；负向检查未命中 `状态：缺失`、`状态：部分可用`、`数据缺口：`、`接口凭证缺失`、限流或 parser/field/provider 空结果。
- 回归：`uv run pytest tests/unit/data_gateway/test_official_api_plugin.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_report_evidence.py tests/unit/data_gateway/test_ingest_pipeline.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_single_flight.py tests/unit/data_gateway/test_rate_limiter.py tests/unit/data_gateway/test_rate_limit_policy.py tests/unit/reports/test_data_need_bridge.py tests/integration/data_gateway/test_btc_market_data_need_no_fixed_17_pack.py tests/integration/data_gateway/test_sliding_window_rate_limit.py tests/contracts/test_official_provider_catalog_contract.py -q` 为 236 passed；相关文件 `git diff --check` 通过。

## 16:20 A 股 / BTC full-chain live rerun

- A 股全链：按 fixed runtime command mode 跑 `600519.SH` / 贵州茅台，run `run-20260613-162005-d9df5ec3`，状态 completed，`stop_point=completed`，最终报告在 `runs/run-20260613-162005-d9df5ec3/reports/final-report.md`。
- A 股数据状态：`market_price`、`capital_flow`、`company_news`、`official_filing`、`social_signal`、`event_calendar`、`hot_money_event` 可用；`financial_metric` 缺失 2 次，原因 `接口权限不足；来源调用失败`；`financial_statement` 缺失 1 次，原因 `接口权限不足`；`lockup_event` 缺失 1 次，原因 `缓存记录为空`。
- BTC 全链：按 fixed runtime command mode 跑 `BTC` / Bitcoin，run `run-20260613-163558-e5d63058`，状态 completed，`stop_point=completed`，最终报告在 `runs/run-20260613-163558-e5d63058/reports/final-report.md`。
- BTC 数据状态：`market_price`、`funding_rate`、`open_interest`、`long_short_ratio`、`liquidation`、`exchange_netflow`、`onchain_metric`、`company_news`、`social_signal` 全部可用；负向检查未命中 `状态：缺失`、`状态：部分可用`、`状态：错误`、`数据缺口：`、`接口凭证缺失`、限流、timeout、parser/field/provider 空结果。

## DataNeed exact capability mapping

- 完成：数据层 resolver 删除 endpoint 名、path、描述、字段名的候选猜测；`DataNeed.need_kind` 只按 official catalog 输出中的 `need_kinds/capability_ids` 合同精确匹配。
- 完成：official catalog `output_contract()` 增加 `need_kinds`；Coinglass curated endpoint 和 generated raw endpoint 覆盖 funding、OI、多空比、清算、清算图、CVD、交易所净流、AHR999、盘口、期权等明确能力 ID。
- 完成：Tushare generated weborder raw endpoint 只对明确能力 ID 进入候选；实时 K 线/分钟线、集合竞价、新闻、研报、货币政策、政策法规按 exact output 合同释放，不再靠 `response_fields` 或 endpoint 名猜。
- 完成：同一 API 多个 output 时，report evidence 和 official API executor 先按 `need_kinds` / `data_type` 选输出，再按粒度选，避免新闻/宏观等多能力接口拿错 output。
- 完成：planner 内部合并键命名从 `semantic_need_key` 改为 `need_request_key`；文档 `docs/限流重组.md` 同步为“能力 ID / 数据请求”口径。
- 验证：`uv run pytest tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_provider_selector_no_business_whitelist.py -q -p no:cacheprovider` 为 `115 passed`。
- 验证：非 live 宽回归 `uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_freshness_contract.py tests/contracts/test_provider_capabilities.py -q -p no:cacheprovider` 为 `619 passed, 1 warning`。
- 验证：成功即停和限流聚焦回归 `uv run pytest tests/unit/reports/test_data_need_bridge.py::test_data_need_stops_after_first_structured_ready_result tests/unit/data_gateway/test_rate_limiter.py tests/unit/data_gateway/test_rate_limit_policy.py tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_fetch_engine.py -q -p no:cacheprovider` 为 `49 passed`。
- 未跑：按人类要求，本轮没有跑 live/full-chain。

## DataNeed exact capability mapping follow-up

- 修正：删除 resolver 中最后残留的 `data_type -> capability_id` 默认能力表；catalog output 没有显式 `need_kinds/capability_ids` 时不再进入候选。
- 修正：report evidence 选择 official output 时，没有命中当前 `need_kind` 的 output 不再退回第一个 output。
- 补齐：Tushare、Coinglass、Finnhub、FRED、Glassnode、CoinGecko Pro 和公共源 catalog 的所有 `output_contract()` 都有显式 `need_kinds`；当前 catalog output 缺显式能力统计为 0。
- 修正：`DataRunScheduler` 同优先级请求按 `deadline_at` 再按原始顺序调度，避免限流预算紧时晚截止请求先占槽导致早截止请求被误跳过。
- 防回归：新增 catalog output 必须声明 `need_kinds/capability_ids` 的合同测试；resolver 禁止 `_DATA_TYPE_CAPABILITY_IDS`；scheduler 覆盖同优先级 deadline 排序和紧预算场景。
- 验证：`uv run pytest tests/unit/data_gateway/test_data_need_planner.py tests/contracts/test_official_provider_catalog_contract.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py -q -p no:cacheprovider` 为 `127 passed`。
- 验证：`uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_freshness_contract.py tests/contracts/test_provider_capabilities.py -q -p no:cacheprovider` 为 `621 passed, 1 warning`，warning 为既有 multiprocessing/fork 警告。
- 验证：`git diff --check` 通过。
- 未跑：本轮只做代码/合同回归，没有跑 live/full-chain。

## DataNeed exact capability mapping review follow-up

- 复审发现并修复：`DataRunScheduler` 的旧 provider-batch 分支也必须按 `deadline_at` 调度；已补 `_provider_batch_sort_key()` 和 provider-batch 紧预算测试，避免旧路径继续制造 `rate_limited_by_tool_budget` 假缺口。
- 复审发现并删除：resolver 不再接受 endpoint-level `response_shape.need_kinds/capability_ids`；能力必须挂在具体 `outputs[]` 上，否则不进入 DataNeed 候选。
- 复审发现并删除：official API provider 不再给无 `outputs` 的 raw endpoint 注册通用 `official_api_response` capability；没有 output 合同就不宣称能力。
- 验证：`uv run pytest tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_official_provider_catalog_contract.py -q -p no:cacheprovider` 为 `125 passed`。
- 验证：`uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_freshness_contract.py tests/contracts/test_provider_capabilities.py -q -p no:cacheprovider` 为 `623 passed, 1 warning`，warning 为既有 multiprocessing/fork 警告。
- 验证：`git diff --check` 通过。
- 未跑：本轮没有跑 live/full-chain。

## DataNeed scheduler provider-batch priority follow-up

- 复审发现并修复：provider-batch 兼容分支实际使用 `priority_rank`，不是 `priority`；`_provider_batch_sort_key()` 已改为先读 `priority_rank`，没有时才回退 `priority`。
- 防回归：新增 provider-batch 不同 `priority_rank` 测试，证明高优先级 batch 先占限流槽；同优先级再按 `deadline_at` 排。
- 验证：`uv run pytest tests/unit/data_gateway/test_dynamic_rate_limit_scheduler.py tests/unit/data_gateway/test_data_need_planner.py tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_official_provider_catalog_contract.py -q -p no:cacheprovider` 为 `126 passed`。
- 验证：`uv run pytest tests/unit/data_gateway tests/unit/reports/test_data_need_bridge.py tests/contracts/test_official_provider_catalog_contract.py tests/contracts/test_provider_selector_no_business_whitelist.py tests/contracts/test_data_gap_reason_contract.py tests/contracts/test_worker_provider_payload_no_provider_leak.py tests/contracts/test_freshness_contract.py tests/contracts/test_provider_capabilities.py -q -p no:cacheprovider` 为 `624 passed, 1 warning`，warning 为既有 multiprocessing/fork 警告。

## Data layer old remote path deletion and event structuring

- 完成：删除公开旧入口 `DataAPI.get_data/get_data_batch`；`DataAPI` 只保留 `get_data_needs()` 和公司名解析。
- 完成：`DataService` 删除旧远端链路入口 `get_data/get_data_batch/plan_batch/execute_plan`；report/select 本地缓存只走 `read_warehouse_batch()`，该方法只查仓库，不出网。
- 完成：运行时不再构造旧 provider selector、request coalescer、provider batch planner；对应源码文件和旧测试已删除；`ProviderBatchPlan/DataPlan/MergeGroup/MergeItem` 模型和导出已删除。
- 完成：三份主文档 `docs/限流重组.md`、`docs/数据层总体设计.md`、`docs/数据层详细设计.md` 改成当前合同版；旧计划文档 `docs/data-layer-unification-plan.md` 删除。
- 完成：Coinglass 文章/经济日历、Finnhub 新闻/日历/公告/filing、FRED release、CoinGecko Pro news 补结构化 event output；official API 通用 parser 补 `event_id/event_type/title/published_at/event_date/source/summary/url` 派生。
- 完成：report 层 evidence 汇总字段从存储名样式改为 `data_attempts_summary`，避免上层 report 文件暴露数据层 collection 名。
- 扫描：`src/claw_trade/data_gateway`、三份主文档和相关测试中，旧 selector/batch/coalescer/model 名称无命中；旧入口/旧兼容/固定 pack/白名单/语义口径无命中。
- 验证：聚焦回归 `209 passed, 1 warning`，命令覆盖 DataAPI cutover、数据层文档合同、official catalog、official API parser、report evidence、selection warehouse read。
- 验证：宽回归 `tests/unit/data_gateway tests/contracts` 为 `1440 passed, 1 skipped, 1 warning, 2 failed`；剩余失败是非本轮数据层项：缺 `docs/evidence/cn_a_fundamental/fresh_final_acceptance_template.md`，以及 collect-first hard early-stop 测试期望 2 个 exception evidence 但当前实现硬早停只记录 1 个。
- 验证：`git diff --check` 通过；相关 Python 文件 `py_compile` 通过。
