- 完成：纠正真实 Chrome 验收路径误判。必须先按仓库固定入口运行 `scripts/mcp-chrome.sh --start`，再用 `mcp__playwright_win_chrome__` 控制 Win11 Chrome。当前机器脚本提示 WSL localhost forwarding unavailable，因此 Windows Chrome 打不开 `http://127.0.0.1:5175/`；应从 Windows 侧执行 `wsl.exe hostname -I` 获取 WSL 地址，本轮为 `172.27.36.34`，真实 Chrome 成功打开 `http://172.27.36.34:5175/`。
- 完成：真实 Chrome 验证 `/select` 右侧进度显示。页面右侧“选股任务进度”显示 `/select 补数据 2026-06-04`、状态 `补数据失败`，失败原因为服务启动恢复发现旧任务仍处于运行态并按 fail closed 标记失败；不再显示旧任务卡在“补数据中”。截图由 MCP 保存为 `select-refresh-failed-20260604.png`。
- 完成：按用户要求重跑真实测试后发现第二个实装缺口：Web 端使用的 `claw_trade.selection.provider_batch` facade 未把 `progress_callback` 透传到底层 data gateway，导致真实 UI 任务有 provider attempts 但右侧进度仍固定。已修 facade 并补 `test_selection_batch_facade_forwards_progress_callback`。重启后真实 Win11 Chrome 发送 `/select`，新 run `sel-run-80ba7dc745774f79b8487d1378d3e48f` 右侧显示 `补齐全市场日线数据：已完成 64/5502`，截图 `select-refresh-progress-64-of-5502-20260604.png`。相关回归 `83 passed`，`git diff --check` 通过。
- 完成：定位“右侧仍显示补数据中”的根因。不是前端渲染，也不是微信；是测试/默认 ChatController 路径触发 `restore_selection_run_store()` 默认写真实 `runs/selection`，把 UI 正在运行的 data-run 批量标成 `selection_data_run_interrupted`，但当前 UI 后端内存仍持有 running 状态，造成 API/UI 与落盘状态不一致。修复：`restore_selection_run_store()` 默认只读恢复，只有 UI 服务启动接管时显式 `fail_interrupted_active=True`；`ChatController` 默认 selection controller 改用内存 `SelectionRunStore()`，避免测试污染真实 store。回归：相关 37 tests 通过，扩大 selection/UI 相关 84 tests 通过，`git diff --check` 通过。真实 Chrome 重启后旧 run 显示 `补数据失败`，重新发送 `/select` 创建 `sel-run-273ec9636bc54527a74fb3688028f820`，右侧从 `64/5502` 推进到 `128/5502`；截图 `select-refresh-failed-after-state-fix-20260604.png`、`select-refresh-progress-128-of-5502-20260604.png`。
- 完成：定位“已分配 Tushare 但 `/select` 补日线仍打东方财富”的根因。UI Mongo 设置中 Tushare 已启用、endpoint 与 secret 均存在；本次 00:07 后 provider attempts 中没有 `cn_a_primary` 日线尝试，说明不是 token 缺失或 Tushare 先失败，而是选择器把它过滤掉。直接原因是 `/select` 日线请求要求字段 `date/open/high/low/close/volume/amount`，但 `TushareDailyBarPlugin` 能力声明漏了 `date`；选择器按字段全覆盖匹配，所以 Tushare 未进入候选，后续才走 EastMoney/AkShare/BaoStock/Mootdx。修复：Tushare 日线能力字段补 `date`，解析结果 row 显式写 `date`。验证：同一选择器探针从修复前 `['cn_a_eastmoney_market_data:daily_bar', ...]` 变为修复后 `['cn_a_primary:daily_bar', 'cn_a_eastmoney_market_data:daily_bar', ...]`；聚焦测试 `38 passed`；`git diff --check` 通过。注意：当前已运行的 Python UI 服务不会热加载此修复，需重启后新 `/select` 才生效。
- 完成：按系统性漏检复盘 provider 声明层。根因不是没有做源检查；此前 memory 中已多次记录 Tushare、CNInfo、EastMoney、AkShare、BaoStock、mootdx、Finnhub、Coinglass 等源/字段 probe，但这些 probe 没有形成“产品请求字段 -> provider 能力声明字段 -> 选择器候选”的合同测试，所以 Tushare 日线 `date` 这种声明漏项能通过源检查却在选择器层被静默过滤。修复：新增 `test_product_request_field_filters_have_explicit_provider_exclusions`，覆盖 report/select 全部产品请求，要求每个请求至少有候选；官方/付费 provider 只要因字段声明被过滤，就必须出现在显式原因表中。修复后当前显式原因表有 20 个字段过滤差异，均是已知不同接口/不同指标字段边界；新的 Tushare 类漏声明不列原因会直接测试失败。验证：`tests/unit/data_gateway/test_provider_selector.py -q` 为 `7 passed`；相关 provider/selection 回归为 `39 passed`；`git diff --check` 通过。
- 完成：定位“为什么还是一直补数据中”。真实 API 返回的不是前端缓存，而是 active data-run `sel-run-3239267d89e54d50b91d80de6bea9f46`，状态 `fetching_data`，一度停在 `128/5502`，后续推进到 `256/5502`。Mongo provider attempts 证明 Tushare 已被调用且成功，连接目标是配置的 `111.170.140.159:8020`，不是东方财富；但补齐流程把全市场缺口展开为 5502 个单票日线请求，每个单票还可能继续试 EastMoney/AkShare/BaoStock/Mootdx，导致看起来长期卡住。根因是 Tushare 日线只声明/实现了按 symbol 批量，没有实现 Tushare `daily` 按 `trade_date` 拉全市场；batch planner 对 universe request + `batch_by=symbol` 会产出 0 个 batch，于是流程退化成逐票补齐。修复：Tushare 日线新增 `daily_bar_by_trade_date` endpoint，`batch_by=date`，无 symbol 时按 `trade_date` 请求全市场；selection universe refresh 改为按缺失交易日生成 all_a_shares 请求，例如 2026-06-04 只补对应日期，不再默认生成 5502 个单票请求。验证：新增 Tushare trade_date universe fetch 合同测试；更新 selection stale all-share 测试；相关回归 `116 passed`，`git diff --check` 通过。注意：当前已运行的旧 UI 后台不会热加载，必须重启后新 `/select` 才走该修复。
