## 21:33 [codex]
-完成：实现 Task 1 后端报告保留时间设置持久化、API、重置接入和聚焦测试。
-决策：Mongo 存在时使用单记录 store；未配置 Mongo 时只使用 `runs/.ui-report-cleanup-settings.json`，重置通过清除保存记录回到默认 7 天。
待办：后续任务再实现实际报告清理服务、删除接入和调度。

## 21:41 [codex]
-完成：修复报告保留时间设置校验，拒绝小数、字符串等非真实整数值，并拒绝非对象 JSON 持久化内容。
-决策：只有缺失设置文件或缺失 Mongo 记录回默认 7 天；存在但格式错误的持久化内容抛 `INVALID_INPUT`。
待办：无。

## 21:44 [codex]
-完成：修复 Mongo 报告保留时间设置坏 payload 被误判为缺失设置的问题，并新增回归测试。
-决策：Mongo store 负责区分无记录和坏记录；service 将坏记录转换为 `INVALID_INPUT`。
待办：无。

## 21:56 [codex]
-完成：实现 Task 2 报告硬删除清理服务、运行保护、发送中保护、OpenViking/OpenClaw 文件清理和聚焦测试。
-决策：发送队列清理采用保守策略；唯一绑定文件删除，共享或混合 run 文件跳过并记录 warning。
待办：后续任务再接 UI 删除路由和自动调度。

## 22:03 [codex]
-完成：修复 Task 2 清理服务评审问题，先做目录 containment 校验再读候选年龄，并把仓库/tombstone 清理异常纳入 failedRunIds/warnings。
-决策：symlink 逃逸目录直接不进入候选；文件系统已删除但索引清理失败时按失败结果返回，保证批次继续。
待办：无。

## 22:06 [codex]
-完成：修复 Task 2 剩余路径安全问题，拒绝 report run 目录 symlink，即使 symlink 目标仍在 run_root 内。
-决策：run 清理只接受目录入口本身为真实目录且名称等于 run id；内部 symlink 与外部 symlink 都不进入候选。
待办：无。

## 22:16 [codex]
-完成：修复 Task 2 代码质量复审问题，OpenViking/OpenClaw 清理不跟随 symlink 目录，session 删除限定为 `single-worker-*` 文件，并恢复队列慢路径无锁行为。
-决策：workflow symlink 只 unlink 链接；sessions/delivery-queue symlink 目录直接跳过；队列只保留清理保护快照的短锁。
待办：无。

## 22:23 [codex]
-完成：修复 Task 2 提交前复审问题，队列内存状态读写改用同一短锁且不包住 runner/state/writer 慢调用，并补齐 OpenClaw session sidecar 清理。
-决策：只有匹配目标 run 的 `single-worker-*` primary 文件确认后才删除同 stem `trajectory-path` sidecar；孤立 sidecar 保留。
待办：无。

## 22:32 [codex]
-完成：实现 Task 3，将手动删除报告路由接入硬删除清理服务，并返回稳定 cleanup 响应结构与测试覆盖。
-决策：路由只公开聚合 cleanup 字段，不泄漏清理服务内部逐 run 结果；选中报告只有进入 `deletedRunIds` 时才返回 `deleted: true`。
待办：无。

## 22:38 [codex]
-完成：修复 Task 3 评审遗漏，前端删除契约改为 cleanup 响应，并让单删/批量清空只移除 `deleted: true` 的报告。
-决策：保留现有批量循环；跳过或失败的报告继续留在历史列表，提示沿用后端 `userMessage` 的最小处理。
待办：无。

## 22:43 [codex]
-完成：修复 Task 3 硬删除后前端残留误导文案，确认框和历史列表 tooltip 都改为真实硬删除含义。
-决策：只替换 stale copy，不做 Task 6 的完整删除 UX 文案打磨。
待办：无。

## 22:51 [codex]
-完成：修复 Task 3 批量删除部分成功时跳过/失败消息被静默吞掉的问题，并补充前端回归断言。
-决策：保留现有逐报告删除循环，只记录并展示第一条非空 `deleted:false` 响应消息，不做完整 Task 6 UX。
待办：无。

## 22:57 [codex]
-完成：实现 Task 4 报告清理启动/每日后台调度器，接入 UI 服务生命周期并补充确定性单元/e2e 覆盖。
-决策：调度器只读取当前清理设置后调用现有清理服务；异常只记录日志并继续循环，注入的测试服务不自动启动后台线程。
待办：无。

## 23:00 [codex]
-完成：修复 Task 4 调度器测试探针质量问题，导入 `ReportCleanupResult` 并让成功路径断言探针未记录异常。
-决策：保留调度器吞异常的生产契约，在测试探针内记录异常以避免后台线程静默失败被误判为成功。
待办：无。

## 23:07 [codex]
-完成：修复 Task 4 生命周期复审问题，FastAPI 启动失败会回滚已启动调度器，清理调度器 stop 超时后再次 start 会明确报错直到旧线程退出。
-决策：重复 start 在活跃运行时仍保持幂等；只有 stop 已请求但旧线程仍存活时拒绝重启，避免清掉 stop event 后没有新调度器。
待办：无。
