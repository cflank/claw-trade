## 2026-05-30 correction [a-share-seed-nonblocking-rules]
- 2026-05-30 人类决策废止本文件中关于 T9A 的过严阻断口径。
- 11 个指数 `sh.000816`、`sh.000833`、`sh.000842`、`sh.000939`、`sh.000940`、`sh.000950`、`sh.000951`、`sh.000957`、`sh.000958`、`sh.000963`、`sh.000975` 明确不下载，也不纳入 `index_daily` 覆盖失败条件。
- 单个股票行业映射缺失只记录为数据质量缺口，不阻断入库或 `/select`。
- A 股日线起止日期缺口只记录为数据现象，不作为失败理由；停牌、退市、无交易、上市日或 provider 历史范围差异不能被直接写成失败。
- 因此，本文件中 “missing_index_count=11 / missing_industry_count=1 / incomplete_daily_count=189 导致 T9A 未完成或代码阻断” 的旧结论不再有效。

## 02:55 [a-share-select-local-seed-live-validation]
- 任务范围：为 A 股 `/select` 使用本地历史种子数据完成真实候选包与 UI 验证；不使用单日行情快照冒充策略选股。
- 数据：
  - Baostock 本地种子目录：`.runtime/selection-data/raw/baostock`，约 `224M`。
  - AkShare 私募/定增事件目录：`.runtime/selection-data/raw/akshare_stock_add_stock`，约 `21M`。
  - 前复权日线 CSV：5207 个，对齐 universe 5207 只。
  - 私募/定增事件 CSV：5207 个，对齐 universe 5207 只。
  - Baostock manifest：`.runtime/selection-data/raw/baostock/manifest/baostock_files.sha256`。
  - AkShare manifest：`.runtime/selection-data/raw/akshare_stock_add_stock/manifest/akshare-stock-add-stock-full-20260527-persistent.json`，`success_count=5207`，`failure_count=0`。
- 代码变更：
  - `src/claw_trade/data_gateway/selection_local_baostock.py`：新增本地 Baostock qfq 日线 provider，读取本地 universe/daily/history 和 AkShare 私募事件；缺私募事件、全空历史、重复 ticker / 名称冲突均 fail closed。
  - `src/claw_trade/data_gateway/selection_batch.py`：存在 `CLAW_TRADE_SELECTION_LOCAL_BAOSTOCK_ROOT` 时走本地历史 provider。
  - `src/claw_trade/selection/candidate_pack.py`：candidate pack 遇 blocker data gap 不能 approved。
  - `scripts/selection/download_baostock_selection_seed.py`：Baostock 持久化下载脚本。
  - `scripts/selection/download_akshare_private_placement_events.py`：AkShare 私募/定增事件并发分片下载脚本。
  - `tests/unit/selection/test_local_baostock_provider.py`、`tests/unit/selection/test_candidate_pack_contract.py`：补本地 provider 与 fail-closed 合同测试。
- 验证：
  - `uv run python -m py_compile scripts/selection/download_baostock_selection_seed.py scripts/selection/download_akshare_private_placement_events.py` -> exit 0。
  - `uv run pytest tests/unit/selection/test_local_baostock_provider.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/integration/selection/test_select_command_chat_flow.py` -> exit 0，`34 passed`。
  - 数据任务 run：`sel-local-baostock-persistent-20260529-01`，证据 `runs/selection/2026-05-27/sel-local-baostock-persistent-20260529-01.json`。
  - 数据任务结果：`status=completed`，`candidate_pack_stage=approved`，`feature_rows=5119`，`top20_count=20`，`score_ref=score://sel-local-baostock-persistent-20260529-01`。
  - data gap：仅 `selection_local_baostock_history_insufficient`，severity=`warn`，88 只新股历史不足 260 日并被剔除；无 blocker。
  - per-strategy raw hits 已写入证据，17 个 approved strategy variant 均有 hit_count 记录。
  - fixed runtime：`scripts/start-research-ui.sh` -> `scripts/start-control-runtime.sh`，OpenViking `1933` health OK，OpenClaw gateway `18789` health OK，UI `5175` OK，`CLAW_TRADE_OPENVIKING_MCP_STARTED=0`。
  - 真实 Chrome `/select`：`runs/selection/workflows/select-20260529T025226-e0d36c91-b3d6-439c-bb87-6ee1255d0e03/selection-workflow-evidence.json`，`status=completed`，4 个 selection worker dispatches 均有 provider request / visible tools / raw output 证据。
  - Chrome UI 结果：进入 `/report` 为 `301377.SZ`、`000636.SZ`；观察 7 只；放弃 9 只；候选事实包和确认按钮均显示。Playwright 截图 `select-result-20260529T0254.png`，snapshot `select-result-20260529T0254.snapshot.md`。
- 决策：
  - 当前 `/select` v1 主链路完成真实运行验证。
  - 不能宣称“出厂全历史数据完成”：当前大多数 Baostock 日线覆盖为 2025-01-01 至 2026-05-27，足够 260 日策略窗口，但不是全市场 2015 至今全量补库；指数/复权因子完整补库也未完成。

## 04:55 [a-share-select-current-local-history-final-check]
- 范围：在本地历史种子补齐到日线/基础/行业/复权因子/指数后，重新生成当前代码版本的持久化 selection data run，并用真实 Chrome 验证 `/select`。
- 数据现状：
  - `.runtime/selection-data/raw/baostock` 当前约 `1.2G`；其中 `daily/qfq` 约 `890M`、`index_daily` 约 `233M`、`adjust_factor` 约 `21M`。
  - `.runtime/selection-data/raw/akshare_stock_add_stock` 当前约 `21M`，用于 Sequoia-X 私募/定增事件字段。
  - 审计文件：`.runtime/selection-data/raw/baostock/manifest/baostock-selection-audit-full-seed-20260527-after-factor-index.json`。
  - 审计结果：universe `5207`、qfq daily files `5207`、daily rows `10584050`、factor files `5207`、index files `2029`、basic rows `8762`、industry rows `5528`；daily 缺失 `0`、空文件 `0`、adjustflag mismatch `0`、factor 缺失 `0`。
  - 当时记录：按旧口径把 11 个指数缺失写成失败。2026-05-30 人类决策已废止该口径：这 11 个指数明确不下载，不作为缺口。
- 代码/数据 run：
  - 当前 data run：`sel-local-baostock-current-20260529-01`。
  - 证据：`runs/selection/2026-05-27/sel-local-baostock-current-20260529-01.json`。
  - 结果：`status=completed`、`failure_code=None`、`candidate_pack_stage=approved`、`feature_rows=5102`、`top20_count=20`。
  - data gaps：`selection_local_baostock_history_insufficient` warn（88 只历史不足 260 日已剔除）；`selection_local_baostock_history_stale` warn（17 只最后日线早于 `2026-05-27` 已剔除）。
  - provider attempt refs：`attempt://local-baostock/sel-local-baostock-current-20260529-01/2026-05-27/28742b6ecbe40c71` 与 `baostock_files.sha256`。
  - normalized ref：`normalized://local-baostock/qfq/2026-05-27/eef3569537ffed11`；feature ref：`feature://sel-local-baostock-current-20260529-01`；score ref：`score://sel-local-baostock-current-20260529-01`。
  - 17 个 approved strategy variant 均写入 `per_strategy_raw_hits`，0 命中策略也保留 hit_count=0。
- 固定 runtime / Chrome 验证：
  - 重启路径：`CLAW_TRADE_SELECTION_LOCAL_BAOSTOCK_ROOT=.runtime/selection-data/raw/baostock CLAW_TRADE_SELECTION_LOCAL_PRIVATE_PLACEMENT_ROOT=.runtime/selection-data/raw/akshare_stock_add_stock RESEARCH_UI_PORT=5175 scripts/start-research-ui.sh`。
  - preflight：OpenViking `1933/health` OK；OpenClaw gateway `18789/health` OK；UI `5175/api/ui/get-report-queue-snapshot` OK；`CLAW_TRADE_OPENVIKING_MCP_STARTED=0`；OpenViking config/data 均在 `.runtime/dev-services/openviking`。
  - 真实 Chrome 输入 `/select`，workflow evidence：`runs/selection/workflows/select-20260529T043059-c5177a79-f79e-4159-9466-b02526d944a7/selection-workflow-evidence.json`。
  - workflow 结果：`status=completed`、`selection_run_id=sel-local-baostock-current-20260529-01`、4 个 worker dispatch 均有 `provider-request.json`、`visible-tools.json`、`raw-output.md`。
  - UI 显示：数据质量提示 `2` 条 warn；标准化股票 `5102` 行；确认进入 `/report` 为 `301377.SZ 鼎泰高科`、`300308.SZ 中际旭创`。
- 验证命令：
  - `uv run pytest tests/unit/selection/test_local_baostock_provider.py tests/unit/selection/test_candidate_pack_contract.py tests/integration/selection/test_data_job_pipeline.py tests/integration/selection/test_select_command_chat_flow.py` -> exit `0`，`35 passed, 17 warnings`。
  - `uv run python -m py_compile scripts/selection/download_baostock_selection_seed.py scripts/selection/audit_baostock_selection_seed.py scripts/selection/download_akshare_selection_daily.py scripts/selection/download_akshare_selection_indices.py src/claw_trade/data_gateway/selection_local_baostock.py tests/unit/selection/test_local_baostock_provider.py` -> exit `0`。
- 结论：当前 `/select` 主链路可用且使用当前本地历史数据，不是单日快照。2026-05-30 人类决策后，11 个明确不下载指数不再作为完整性缺口。

## 设计讨论 [codex]
- 完成：根据当天关于 OpenBB/data_gateway、A 股 provider、`/report`、`/select`、Mongo/OpenViking 分工、provider 优先级和限流去重的讨论，新增 `docs/数据层总体设计.md`。
- 决策：文档明确为目标设计，不宣称当前代码已全部实现；核心方向是上层只声明数据需求，provider 选择、缓存、限流、single-flight、证据写入由数据层统一负责。
- 待办：后续如进入实施，应先把设计拆成任务清单，并区分 `/report` 单标的资料包预取、`/select` 全市场数据仓库增量维护、UI probe/价格提醒统一闸门三类工作。

## 02:45 [codex]
- 完成：按同一数据层思路，把 Crypto provider 定位、UI 设置页瘦身、历史包、`/report` 单币资料包、`/select` 选币池数据仓库和 provider 优先级补入 `docs/数据层总体设计.md`。
- 决策：Crypto 不照搬 A 股 provider；按 K 线、盘口、合约、链上、稳定币流、基本面、DeFi、新闻、社交、事件预期分别定 provider 优先级。UI 只暴露真正需要用户配置且会影响运行链路的源，预留但未接入的付费源先隐藏。
- 待办：继续按同一口径讨论港股和美股 provider、用户配置源、历史包和优先级。

## 03:00 [codex]
- 完成：把港股、美股 provider 定位、设置页瘦身、历史包和优先级补入 `docs/数据层总体设计.md`，并补充社交源设置页显示规则。
- 决策：HKEXNews、SEC、Yahoo/OpenBB 等内置或官方源不作为普通用户配置项；社交源只有在真实调用、入库、进入 social pack、worker 可见且有 live/fresh 证据时才显示在设置页。
- 待办：如继续细化，需要逐一确认港股和美股 UI 现有设置项哪些只是 probe/预留，哪些已经影响 `/report` 或 `/select`。

## 03:05 [codex]
- 完成：修正 `docs/数据层总体设计.md` 的历史包和 `/select` 范围：港股、美股暂不支持 `/select`，暂不按 A 股方式下载历史包；A 股补充已下载历史数据包的清洗、整理、入 Mongo/OpenViking 审计引用流程；Crypto 标记为历史包待下载。
- 决策：A 股当前重点是已下载数据入库和增量维护；Crypto 后续任务清单必须加入下载、清洗、整理、入 Mongo 和审计任务；港股/美股当前只服务 `/report` 单标的资料包。
- 待办：后续拆任务时，把 A 股入库任务和 Crypto 历史包下载入库任务列为独立数据层任务。

## 05:29 [codex-doc-subagent]
- 完成：按主代理评审意见修订 `docs/数据层总体设计.md`，补齐全局架构边界、UI/probe 通用判定原则、A 股 Baostock 核心口径、HK/US 历史包 03:05 收敛口径，并收紧“当前代码和历史 evidence”类表述。
- 决策：文档继续保持“目标设计”定位，不宣称当前代码已完成 Mongo 主路径，不把 UI probe/mock/stub/预留配置项当作 provider 主链路证据。
- 约束：本次仅文档改写与 memory 追加；未执行 live/fresh/provider/UI 运行验证。

## 03:38 [codex-data-detail-doc]
- 完成：根据 `docs/数据层总体设计.md` 新增 `docs/数据层详细设计.md`，包含编号需求、核心数据结构、模块边界、函数级伪码、四市场设计、失败语义、Coverage Matrix 和 Deviation Audit。
- 决策：详细设计只作为目标编码合同，不宣称当前 Mongo 主路径已完成；provider 优先级按市场和数据类型拆分，OpenViking 仍只做材料和证据引用，不做 provider cache。
- 验证：已执行关键词自检、Coverage Matrix 状态检查、Deviation Audit 阻断项检查和关键结构/函数出现性检查；未执行源码测试或 live/fresh/provider/UI 运行验证。

## 03:50 [codex-data-detail-doc-current-code-revision]
- 完成：按用户要求阅读当前数据层源码后修订 `docs/数据层详细设计.md`，把“provider 当前能否 live/fresh 跑通”改为非编码前阻断；运行时按现有优先级尝试，失败记录 attempt/gap/evidence 后尝试同一数据类型下一个候选。
- 决策：Mongo collection 命名沿用当前代码的 `openbb_*` store 常量；`/select` 当前沿用 `runs/selection/store/data-runs` 和 data job evidence 文件，不新增平行 `select_data_plans` 主表；限流沿用 `RateLimitPlanMetadata`、`MongoRateLimitStore`、`openbb_rate_limits`，默认 60 秒窗口，未配置 hard limit 时只计数。
- 源码事实：核对 `src/claw_trade/data_gateway/store/mongo.py`、`src/claw_trade/data_gateway/store/*.py`、`src/claw_trade/data_gateway/providers/run_plan.py`、`src/claw_trade/data_gateway/providers/execution.py`、`src/claw_trade/selection/store.py`、`src/claw_trade/selection/data_job.py`。
- 验证：已执行文档关键词检查、Coverage Matrix 状态检查和旧表名/禁用语义检查；本次仍为文档修订，未修改源码，未执行 live/fresh/provider/UI 运行验证。

## 05:05 [codex-data-task-list-review-fix]
- 完成：根据数据层实施任务清单评审 findings 修订 `docs/数据层实施任务清单.md`，新增 T9A A 股 seed-to-Mongo 入库任务和 T9B Crypto 历史包下载/清洗/入库任务。
- 决策：A 股 CSV/Baostock/AkShare 只能作为 seed/import/audit 来源；`/select` 新入口必须依赖 Mongo normalized rows 和 warehouse marker。Crypto 历史包因首批 universe、source、history range、interval 和许可边界未批准，实施状态写 `BLOCKED`，不能写成覆盖完成。
- 切割：任务清单已把 `src/claw_trade/selection/store.py` 的 legacy data job evidence restore 路径纳入 cutover 合同；缺少 new `SelectDataPlan` / warehouse marker / warehouse_check_ref 的 legacy completed evidence 只能作为 read-only reference 或 migration input。
- Store Contract：T3/T10/T11 已改为明确使用现有 `openbb_provider_manifests`、`openbb_provider_validation_receipts`、`openbb_run_provider_plans`、`openbb_provider_attempts`、`openbb_raw_payloads`、`openbb_normalized`、`openbb_provider_http_evidence` 和 selection evidence 文件；未新增平行 Mongo 主表。
- 约束：本次只改文档和 memory；未修改源码，未运行 live/fresh/provider/UI，未实现代码。

## 05:46 [codex-data-layer-t0-contract]
- 完成：新增 T0 coverage ledger 合同测试，锁定任务清单 T0-T14/T9A/T9B、DG-* 与 §5-§19/§22 映射、BLOCKED 项、Deviation Audit、Stop Conditions、既有 collection 名和 anti-fake 禁止语义。
- 约束：本次只新增合同测试和 memory 记录；未实现数据层功能，未修改源码、设计文档或运行时文件。

## 06:10 [codex-data-layer-t1-models]
- 完成：补齐 T1 数据层模型和状态语义，新增 `DataRequirement`、`RequirementBatch`、`ProviderCallResult`、证据/ref 记录、资料包、report/select plan 和 UI provider display decision 合同；`cache_hit`、`shared_result`、`rate_limited`、`cached_empty`、`cooldown_skipped`、`sdk_http_unknown`、`evidence_write_failed` 均不能写成 `remote_success`。
- 验证：`uv run pytest tests/unit/data_gateway/test_models.py tests/contracts/test_data_layer_models.py` -> exit 0，17 passed；已执行 T1 要求的状态/anti-fake/collection grep 检查。
- 约束：本次只改模型和测试；未实现 store、provider 出网、gate、OpenViking 写入、report/select 入口或 runtime 行为。

## 06:18 [codex-data-layer-t1-review-fix]
- 完成：按 T1 spec review 收紧 `remote_error` 缺口要求，补 ProviderCapability T1 字段语义测试，并新增 `HttpEvidence(sdk_internal_unknown=True)` 拒绝伪 HTTP evidence 的单元/合同断言。
- 验证：`uv run pytest tests/unit/data_gateway/test_models.py tests/contracts/test_data_layer_models.py` -> exit 0，21 passed；已重跑 T1 grep 扫描，anti-fake 命中均为负例/禁止语境。

## 06:35 [codex-data-layer-t2-store-contract]
- 完成：加固 T2 store 合同：cache success 必须带 raw/normalized refs；single-flight 状态写失败统一 `evidence_write_failed`；evidence chain 追 raw 与 normalized refs；新增 store contract 测试锁定既有 collection、非远端状态、selection evidence 文件边界和禁止平行 Mongo 表。
- 验证：`uv run pytest tests/unit/data_gateway/test_cache_state_machine.py tests/unit/data_gateway/test_evidence_chain.py tests/integration/data_gateway/test_mongo_attempts.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_store_contract.py` -> exit 0，25 passed, 1 skipped（缺 Mongo URI）, 9 warnings。
- 复核修正：撤回 `tcp://` HTTP evidence 成功行缺 status/header 仍通过的放松；设计未明确批准该豁免，现按 evidence 缺口 fail closed。

## 11:04 [codex-data-layer-t3-provider-catalog-policy]
- Task ID：T3。
- 完成：
  - `src/claw_trade/data_gateway/providers/registry.py`：新增 capability policy 归一化 `normalize_provider_capability_policy()`，统一填充 T1 字段（`data_type`、`coverage_fields`、`coverage_symbols`、`freshness_supported`、`user_config_key`、`can_be_formal_fact_source`、`can_enter_worker_pack`、`http_visibility`、`live_fresh_required_for_ui`）；新增 `capabilities_for_data_type()`；priority 解析改为按 `data_type + source_role + coverage_group` 分桶后排序；`apply_user_preferred()` 增加 `data_type` 作用域，禁止跨 data_type 误提升。
  - `src/claw_trade/data_gateway/providers/defaults.py`：默认 catalog 加载时统一走 capability policy 归一化；`default_provider_config_version()` 纳入 T1 字段，确保 capability 策略变化进入 config hash。
  - `src/claw_trade/data_gateway/providers/catalog.py`：catalog snapshot 版本输入增加 `data_type`（基于 manifest `expected_schema_id`）以满足 T3 integrity 口径。
  - `tests/contracts/test_provider_catalog_policy.py`（新增）：覆盖“同 data_type 内排序”“user preferred 不能替代官方披露”“discovery/event_expectation 非正式事实源”“默认 catalog 边界字段”和“禁止平行主表 token”。
  - `tests/unit/data_gateway/test_provider_registry.py`：补充 data_type 作用域排序单测，并给 capability helper 显式 data_type。
  - `tests/unit/data_gateway/test_default_provider_catalog.py`：补充默认 capability 边界断言（key 与 discovery 边界）；并把 3 处旧 catalog 断言更新到当前实际 provider 实现（`chart_yahoo`/`daily_sina` 不在当前实现，crypto slash symbol 当前为 `AR/USDTUSD`）。
- 验证命令与退出码：
  - `uv run pytest tests/contracts/test_provider_catalog_policy.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_provider_registry.py` -> exit `0`（24 passed）。
  - `rg -n "global|rank|fallback success|fallback|remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/providers tests/contracts/test_provider_catalog_policy.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_provider_registry.py` -> exit `0`（命中为既有命名/测试文本；无新增 fallback success）。
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" src/claw_trade/data_gateway/providers/catalog.py src/claw_trade/data_gateway/providers/registry.py src/claw_trade/data_gateway/providers/defaults.py tests/contracts/test_provider_catalog_policy.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_provider_registry.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/providers/catalog.py src/claw_trade/data_gateway/providers/registry.py src/claw_trade/data_gateway/providers/defaults.py tests/contracts/test_provider_catalog_policy.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_provider_registry.py` -> exit `1`（无命中）。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" src/claw_trade/data_gateway/providers/catalog.py src/claw_trade/data_gateway/providers/registry.py src/claw_trade/data_gateway/providers/defaults.py tests/contracts/test_provider_catalog_policy.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_provider_registry.py` -> exit `0`（仅命中新增合同测试中的 forbidden token 列表，源码无新增平行主表语义）。
- 变更文件：
  - `src/claw_trade/data_gateway/providers/catalog.py`
  - `src/claw_trade/data_gateway/providers/defaults.py`
  - `src/claw_trade/data_gateway/providers/registry.py`
  - `tests/contracts/test_provider_catalog_policy.py`
  - `tests/unit/data_gateway/test_default_provider_catalog.py`
  - `tests/unit/data_gateway/test_provider_registry.py`
  - `memory/2026-05-29.md`
- 偏差状态：无设计越界；仅对既有单测中的过期断言做了与当前实现一致的收敛，未扩展到 T4-T8。
- mock/stub/fake/fallback：未新增。

## 11:22 [codex-data-layer-t3-provider-catalog-policy-review-fix]
- Task ID：T3（复核修正，仅限允许路径）。
- 修正点：
  - `src/claw_trade/data_gateway/providers/registry.py`
    - 修正 `http_visibility` 默认策略：不再把非 `OPENBB_NATIVE` 默认成 `managed_http`；改为 fail-closed 语义：已知 `mootdx` 标记 `no_http`，其余未显式声明能力默认 `sdk_internal_unknown`。
    - 修正 `apply_user_preferred()`：当未传 `data_type` 且匹配到多个 data_type 时直接抛错 `user_preferred_requires_data_type:<adapter_id>`，避免跨 data_type 生效。
  - `tests/contracts/test_provider_catalog_policy.py`
    - 新增 `user_preferred` 多 data_type 必须显式传 `data_type` 的合同断言；
    - 新增默认 `http_visibility` 不得猜测为 `managed_http` 的合同断言。
  - `tests/unit/data_gateway/test_provider_registry.py`
    - 新增单测：未传 `data_type` 且匹配多个 data_type 时 `apply_user_preferred` 抛错。
  - `tests/unit/data_gateway/test_default_provider_catalog.py`
    - 新增默认 `http_visibility` 行为断言（无 `managed_http` 猜测）；
    - 新增 `mootdx` 能力为 `no_http` 断言；
    - 按复核意见撤回无关断言改动：`AR/USDTUSD` -> `ARUSD`。
- 验证：
  - `uv run pytest tests/contracts/test_provider_catalog_policy.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_provider_registry.py` -> exit `1`。
  - 失败点：`tests/unit/data_gateway/test_default_provider_catalog.py::test_run_provider_plan_parses_slash_crypto_pair_for_provider_symbols`，当前运行时代码返回 `AR/USDTUSD`，断言期望 `ARUSD`。
  - 该失败对应 `run_plan/market_adapters` 的 crypto symbol 归一化行为，不属于 T3 provider catalog/policy 约束本身；本次按复核要求未做非 T3 逻辑修复，记录为 `BLOCKED`。
- 扫描：
  - provider policy grep：exit `0`（命中为既有命名/测试文本，未新增 fallback success 语义）。
  - changed-path anti-fake scan（mock/stub/fake/...）：exit `1`（无命中）。
  - changed-path anti-fake scan（remote_success/shared_result/cache_hit/...）：exit `1`（无命中）。
  - changed-path anti-parallel-table scan：exit `0`（仅命中合同测试中的 forbidden token 列表字符串）。

## 11:30 [codex-data-layer-t4-gate-subagent]
- Task ID：T4。
- 完成：
  - 新增 `src/claw_trade/data_gateway/providers/gate.py`，实现最小统一 gate `run_provider_call_gate(...)`：
    - cache hit 直接返回 `cache_hit`，写 `cache_hit` attempt，`remote_success=False`。
    - cached empty 直接返回 `cached_empty`，写 `cached_empty` attempt + gap，`remote_success=False`。
    - `cache_miss/cache_stale/cache_error` 不当成功，继续走 rate-limit + single-flight。
    - rate-limit blocked 返回 `rate_limited`，写 `rate_limited` attempt + gap，不出网，`remote_success=False`。
    - 仅 reserve 成功后进入 single-flight；owner 才执行 `owner_call`。
    - consumer 仅返回 `shared_result`（引用 owner attempt），不写 `remote_success`。
    - owner `remote_success` 走 cache success 写入；owner empty 走 cache empty 写入并返回 `cached_empty`。
  - 新增 `tests/contracts/test_data_layer_gate_semantics.py`，覆盖 T4 必要分支语义：
    - `cache_hit` 分支不触发 owner call。
    - `cached_empty` 分支不触发 owner call且有 gap。
    - stale cache 继续走 owner 并刷新 cache。
    - rate-limit blocked 不出网且写 `rate_limited` attempt。
    - single-flight consumer 返回 `shared_result`（非 `remote_success`）。
    - owner `remote_success` 缺 refs 会被模型约束拒绝。
- 验证：
  - `uv run pytest tests/contracts/test_data_layer_gate_semantics.py` -> exit `0`（`6 passed`）。
- anti-fake scan（changed paths 三个扫描）：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|兜底" src/claw_trade/data_gateway/providers/gate.py tests/contracts/test_data_layer_gate_semantics.py` -> exit `1`（无命中）。
  - `rg -n "cache_hit|shared_result|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/providers/gate.py tests/contracts/test_data_layer_gate_semantics.py` -> exit `0`（仅命中分支语义实现与合同断言）。
  - `rg -n "remote_success=True|status=ProviderStatus\\.REMOTE_SUCCESS" src/claw_trade/data_gateway/providers/gate.py tests/contracts/test_data_layer_gate_semantics.py` -> exit `0`（仅 owner 远端成功必要路径）。
- 偏差状态：无越界；未改 T5/T6/T7/T8、未改 store、未改 execution wrapper。
- mock/stub/fake/fallback success：未新增。

## 11:42 [codex-data-layer-t5-managed-http-subagent]
- Task ID：T5。
- 完成：
  - 新增 `src/claw_trade/data_gateway/providers/managed_http.py`：
    - 提供显式 HTTP 统一语义入口 `run_managed_http(...)`（本阶段不出网，由注入 fetcher 返回 response）。
    - 提供稳定 key `stable_http_cache_key(...)`（`provider/method/host/path/sorted_query_hash/body_hash/config_version`），并确保 secret 不进入 key。
    - 提供 `redact_url(...)`、`redact_headers(...)`、`detect_quota_signal(...)`、`classify_http_response(...)`、`has_visible_http_exchange(...)`。
  - 修改 `src/claw_trade/data_gateway/providers/execution.py`：
    - 当 `spec.managed_http_required=True` 且 HTTP 证据不可见时，返回 `sdk_http_unknown`，不写伪造 HTTP evidence，不给 `remote_success`。
    - 增加 HTTP 状态闸门：`429/quota -> rate_limited`，`5xx -> remote_error`，并在 attempt/evidence 层保留非成功语义。
    - `fetch.is_empty=True` 时禁止写成 `remote_success`。
    - `_http_evidence_records(...)` 在没有可见 HTTP 元数据时返回空记录，避免补造 capture-only evidence。
  - 修改 `src/claw_trade/data_gateway/store/http_evidence.py`：
    - 持久化前统一做 `source_url` 与 header 脱敏，避免 secret 进入 evidence。
  - 测试：
    - 新增 `tests/contracts/test_managed_http_semantics.py`（稳定 key、secret 脱敏、429/quota、empty/remote_error 分类）。
    - 更新 `tests/unit/data_gateway/test_evidence_chain.py`（`sdk_http_unknown` 不伪造 HTTP evidence 仍通过证据链审计）。
    - 更新 `tests/unit/data_gateway/test_provider_execution.py`（`managed_http_required + 不可见 HTTP -> sdk_http_unknown`；`429 -> rate_limited`）。
- 验证：
  - `uv run pytest tests/contracts/test_managed_http_semantics.py tests/unit/data_gateway/test_evidence_chain.py` -> 首次 exit `1`（断言 URL 编码细节不匹配，已修复）。
  - `uv run pytest tests/contracts/test_managed_http_semantics.py tests/unit/data_gateway/test_evidence_chain.py` -> exit `0`（8 passed）。
  - `uv run pytest tests/unit/data_gateway/test_provider_execution.py` -> exit `0`（8 passed）。
- anti-fake 扫描：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|兜底|伪造" <changed paths>` -> exit `1`（无命中）。
  - `rg -n "requests\\.|urllib\\.request|httpx\\.|aiohttp\\.|curl_cffi" src/claw_trade/data_gateway/providers/managed_http.py src/claw_trade/data_gateway/providers/execution.py src/claw_trade/data_gateway/providers/http_capture.py` -> exit `0`（仅 `http_capture.py` 既有捕获补丁命中；本次 `managed_http.py`/`execution.py` 未新增直接出网实现）。
- 偏差状态：无越界；未改旧 adapter 产品路径（`market_adapters.py`/`fundamental.py`/`news.py`/`social.py`/`hot_money.py`/`policy.py`/`lockup.py`/`polymarket.py`）的大扫阻断逻辑。
- BLOCKED：
  - “阻断所有 legacy provider 直接 HTTP 产品路径”需要改上述旧 adapter 文件，超出 T5 本次允许写入范围，按范围约束标记 BLOCKED。
- mock/stub/fake/fallback success：未新增。

## 11:45 [codex-data-layer-t5-managed-http-review-fix]
- Task ID：T5（复核修正）。
- 问题：`has_visible_http_exchange()` 之前把 `source_url` 视为“可见 HTTP 证据”，导致 `managed_http_required=True` 时在“只有 URL 无 status/headers”的情况下仍可能继续成功路径，形成 capture-only success 风险。
- 修复：
  - `src/claw_trade/data_gateway/providers/managed_http.py`
    - 收紧 `has_visible_http_exchange(...)`：必须同时满足 `response_status_code` 存在且 `response_headers` 非空；仅 `source_url` 不再算可见证据。
  - `tests/unit/data_gateway/test_provider_execution.py`
    - 新增 `_ManagedHttpUrlOnlyAdapter` 与 `test_managed_http_required_with_url_only_does_not_remote_success`，覆盖“只有 `source_url`、无 status/headers”时 `managed_http_required` 必须返回 `sdk_http_unknown`、不得 `remote_success`。
- 最小验证：
  - `uv run pytest tests/contracts/test_managed_http_semantics.py tests/unit/data_gateway/test_evidence_chain.py tests/unit/data_gateway/test_provider_execution.py` -> exit `0`（17 passed）。
- anti-fake 扫描：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|兜底|伪造" src/claw_trade/data_gateway/providers/managed_http.py tests/unit/data_gateway/test_provider_execution.py` -> exit `1`（无命中）。
- 偏差状态：无越界；仅修复 T5 指定语义。

## 12:10 [codex-guard-narrowing-subagent]
- 范围：human 最新批准的 guard 收窄；结构/格式问题只保留为 evidence/diagnostic，不作为 runtime guard 或 hard fail；不修改 `src/claw_trade/guards/**`、T3、USDT/USD、`third_party/**`、lockfile 或数据层 provider 逻辑。
- 完成：
  - `src/claw_trade/reports/exporter.py`：`final-report-structure.json` 仍写结构诊断，但移除 `guard_source` 和导出失败返回；新增 `hard_fail=false` 与 diagnostic source，最终报告缺章节/顺序等格式问题不再阻断导出。
  - `src/claw_trade/workflow/runner.py`：`report_polisher` 分段结构检查仍写 `final-report-segment-structure.json`，但不再把失败转成 worker failure/early stop；移除旧的结构失败格式重试分支。
  - `tests/contracts/test_final_report_exporter.py`：缺最终报告章节的用例改为导出通过并记录诊断。
  - `tests/unit/test_control_runner.py`：分段结构不合格的用例改为继续材料批准，并断言诊断 payload 不含 `guard_source`。
- 验证：
  - `uv run python -m py_compile src/claw_trade/reports/structure.py src/claw_trade/reports/exporter.py src/claw_trade/workflow/runner.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_final_report_exporter.py tests/unit/test_control_runner.py` -> exit `0`（36 passed）。
- 扫描：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" src/claw_trade/reports/exporter.py src/claw_trade/workflow/runner.py tests/contracts/test_final_report_exporter.py tests/unit/test_control_runner.py` -> exit `1`（无命中）。
  - `rg -n "guard_source|hard fail|guard_failed|GuardResult|early_stop" ...` -> exit `0`；命中解释：`guard_source` 只在测试负向断言中出现；`GuardResult`/`early_stop` 为既有运行时 guard 和批次控制逻辑；本轮结构诊断只新增 `hard_fail=false`，未新增 guard/hard fail/early-stop category。
- 确认：造假/真实性红线未放松；unsupported concrete target price、fake source/tool/chart/data success 等仍由既有 truthfulness guards/export guards 处理。本轮未碰 USDT/USD 逻辑或测试，未碰 T3/provider symbol 文件。

## 11:47 [codex-data-layer-t5-managed-http-review-fix-2]
- Task ID：T5（第二轮复核修正）。
- 修复点：
  - `src/claw_trade/data_gateway/providers/execution.py`
    - 调整判定顺序：先执行 `_status_from_http()` 的明确失败分类，再执行 `managed_http_required` 的证据可见性校验。
    - 结果：`429/quota -> rate_limited`、`5xx -> remote_error` 不会被 `sdk_http_unknown` 抢先覆盖。
  - `tests/unit/data_gateway/test_provider_execution.py`
    - 新增 `test_managed_http_required_429_without_headers_is_still_rate_limited`。
    - 新增 `test_managed_http_required_5xx_without_headers_is_remote_error`。
- 最小验证：
  - `uv run pytest tests/unit/data_gateway/test_provider_execution.py` -> exit `0`（11 passed）。
- 偏差状态：无越界；未改旧 adapter，未跑大回归。

## 11:49 [codex-data-layer-t5-managed-http-review-fix-3]
- Task ID：T5（第三轮复核修正）。
- 问题：429/5xx 无 headers 时虽然已分类失败，但 `_http_evidence_records()` 因“可见证据需 status+headers”过滤，导致失败 HTTP evidence 丢失。
- 修复：
  - `src/claw_trade/data_gateway/providers/execution.py`
    - 在 `_http_evidence_records()` 增加失败例外：若 `response_status_code` 为 `429` 或 `>=500`，即使 headers 为空也保留 HTTP evidence 记录。
    - 新增 `_is_explicit_http_failure_without_headers(fetch)` 仅用于失败证据保留，不影响 success 证据门槛。

## 12:10 [codex-data-layer-t9a-seed-importer-subagent]
- Task ID：T9A。
- 完成：
  - 新增 `src/claw_trade/data_gateway/selection_seed_importer.py`：显式 A 股 seed-to-Mongo importer，读取 Baostock manifest、本地 qfq 日线/seed CSV 和 AkShare 私募定增 CSV；写 existing `openbb_raw_payloads`、`openbb_normalized`、`openbb_provider_attempts`；返回 manifest hash、raw refs、normalized refs、attempt refs、data gaps、readback counts 和 warehouse marker。
  - 新增 `scripts/selection/import_a_share_seed_to_mongo.py`：人工执行真实导入的 CLI，连接 `DATA_GATEWAY_MONGODB_URI`/`CN_A_MONGODB_URI`，只写 existing `openbb_*` collection。
  - `src/claw_trade/data_gateway/selection_local_baostock.py`：只暴露 importer 复用的只读解析 helper，未改变 `/select` runtime。
  - 新增 `tests/contracts/test_a_share_seed_to_mongo_import.py`：小样本合同覆盖成功写入 refs/readback、缺 manifest、空文件、缺字段、K 线重复冲突、私募事件重复冲突。
- 验证：
  - `uv run python -m py_compile src/claw_trade/data_gateway/selection_seed_importer.py scripts/selection/import_a_share_seed_to_mongo.py src/claw_trade/data_gateway/selection_local_baostock.py tests/contracts/test_a_share_seed_to_mongo_import.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_a_share_seed_to_mongo_import.py` -> exit `0`（6 passed）。
  - `uv run pytest tests/contracts/test_a_share_seed_to_mongo_import.py tests/contracts/test_select_warehouse_cutover.py tests/integration/selection/test_data_job_pipeline.py` -> exit `0`（18 passed, 8 warnings）。
- anti-fake scan：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底|hard-coded completed" <changed paths>` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" <changed paths>` -> exit `1`（无命中）。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" <changed paths>` -> exit `0`，仅命中 `data_gaps` 结果字段/既有 selection_local_baostock 字段；未新增 Mongo data gap 主表或其它平行主表。
- 偏差状态：
  - 未改 `/select` runtime、workflow、UI、provider 或 OpenClaw。
  - importer 成功 attempt 使用现有 `ProviderStatus.NOT_APPLICABLE` 并在 `source_metadata.local_seed_import_status=imported` 标记本地导入状态，避免把 local seed import 写成 `remote_success`；未伪造 HTTP evidence。
  - 真实 1.2G 全量 seed 入库未执行；本次只验证 importer 机制。最终全量验收待 T14/人工命令运行并检查真实 Mongo readback。

## 12:24 [codex-data-layer-t9a-review-fix]
- Task ID：T9A。
- 修复：`scripts/selection/import_a_share_seed_to_mongo.py` 移除固定输出 `real_full_seed_import=executed_by_this_command`，改为 `execution_scope=operator_supplied_paths` 和 `full_seed_completion_claimed=false`，避免小样本 CLI 运行被误读成真实全量 seed 已完成。
- 测试：`tests/contracts/test_a_share_seed_to_mongo_import.py` 增加 focused 断言，锁定 CLI 输出不声明真实全量完成。
- 验证：`uv run python -m py_compile scripts/selection/import_a_share_seed_to_mongo.py tests/contracts/test_a_share_seed_to_mongo_import.py` -> exit `0`；`uv run pytest tests/contracts/test_a_share_seed_to_mongo_import.py` -> exit `0`（7 passed）。
- anti-fake：CLI 脚本扫描 `real_full_seed_import|full seed imported|production full seed|真实全量|全量.*完成|hard-coded completed|fallback success|placeholder|skeleton|stub|mock|fake|假成功|伪造|兜底` -> exit `1`（无命中）。
  - `tests/unit/data_gateway/test_provider_execution.py`
    - 对已有 `429 no headers`、`5xx no headers` 两个用例新增断言：HTTP evidence doc 存在，`status` 分别为 `rate_limited` / `remote_error`，并保留 `response_status_code`。
- 最小验证：
  - `uv run pytest tests/unit/data_gateway/test_provider_execution.py` -> exit `0`（11 passed）。
- 偏差状态：无越界；未改旧 adapter，未跑大回归。

## 12:22 [codex-data-layer-t6-provider-execution-wrapper]
- Task ID：T6。
- 完成：
  - 新增 `src/claw_trade/data_gateway/providers/fetcher.py`：
    - 增加 `ensure_call_spec_bound_to_adapter(...)`，执行 `ProviderCallSpec` 与 adapter 绑定校验（`adapter_id + provider + endpoint + market + domain`）。
    - 对 `capabilities()` 为空的测试替身 adapter，增加兼容校验分支（`adapter_id + provider_id`），避免误判并保持单测可运行。
  - 新增 `src/claw_trade/data_gateway/providers/execution_wrapper.py`：
    - 提供 `ProviderExecutionWrapper`，集中做 spec 绑定校验、凭证状态读取、受约束的 fetch/normalize 调用。
  - 修改 `src/claw_trade/data_gateway/providers/execution.py`：
    - 接入 `ProviderExecutionWrapper`，执行层先做凭证判定，缺凭证时直接返回 `credential_missing`，不进入 fetch/normalize。
    - fetch/normalize 异常分类增加 `credential_missing`、`rate_limited` 映射；`ProviderCallSpec` 绑定错误显式返回 `remote_error + provider_call_spec_mismatch`。
    - 保持 `remote_success` 证据写入约束不变（仍要求 raw/normalized refs）。
  - 新增 `tests/contracts/test_provider_execution_wrapper.py`：
    - 合同验证 spec 绑定失败时 adapter fetch 不执行；
    - 验证 A provider 失败后不会自动触发 B，必须由 coordinator 显式再调 B spec。
  - 更新 `tests/unit/data_gateway/test_fundamental_adapters_live_contract.py`：
    - 新增 Tushare 缺 token 显式报错断言；
    - 新增 CN_A fundamental 上游异常不吞并断言（不得伪装 empty/success）。
  - 更新 `tests/unit/data_gateway/test_news_social_adapters_live_contract.py`：
    - 新增 social X 缺 token 显式报错断言；
    - 新增 CNInfo 异常不吞并断言（不得伪装 empty/success）。
  - 更新 `tests/unit/data_gateway/test_provider_execution.py`（同面 sanity）：
    - 覆盖凭证缺失前置判定、fetch 凭证异常映射、spec 绑定错误映射。
- 验证：
  - `uv run pytest tests/contracts/test_provider_execution_wrapper.py tests/unit/data_gateway/test_fundamental_adapters_live_contract.py tests/unit/data_gateway/test_news_social_adapters_live_contract.py` -> exit `0`（33 passed）。
  - `uv run pytest tests/unit/data_gateway/test_provider_execution.py` -> 首次 exit `1`（wrapper 对 capabilities 为空替身过严 + frozen 异常类问题），修复后 exit `0`（14 passed）。
  - 修复后复跑最小集：`uv run pytest tests/contracts/test_provider_execution_wrapper.py tests/unit/data_gateway/test_fundamental_adapters_live_contract.py tests/unit/data_gateway/test_news_social_adapters_live_contract.py` -> exit `0`（33 passed）。
- anti-fake 扫描结果：
  - 本次未新增 mock/stub/fake/fallback success 路径；
  - adapter 失败分支保持显式失败，不把异常吞成 empty/success。
- 偏差状态：无越界；仅在 T6 允许路径改动。
- BLOCKED：
  - “全面阻断 legacy adapter 直接 HTTP 产品路径”仍是后续范围；本次仅完成 execution wrapper / adapter 绑定边界，不做旧 adapter 全量拆分与清扫。

## 13:08 [codex-data-layer-t7-evidence-materializer-subagent]
- Task ID：T7。
- 完成：
  - 新增 `src/claw_trade/data_gateway/packs/materializer.py`，统一把 `DomainPackResult` materialize 为 worker 可读 `DomainPack`：
    - 统一 `approval_status` 判定：`evidence_write_failed` / blocker gap / required failure -> `blocked`；warn gap -> `warn`。
    - `approved` 且缺 `normalized_refs` 强制转 `blocked`（fail closed），并补充 blocker gap。
    - 统一生成 `source_summary`，并对 worker 文本做 ref 脱敏（移除 `mongo://`、`viking://` 等协议 ref 暴露）。
  - `src/claw_trade/data_gateway/openviking/lineage.py` 接入 materializer 预判：
    - lineage 写入前先 materialize pack；若任一 pack 为 `blocked`，返回失败并阻断 OpenViking available lineage 写入。
  - `src/claw_trade/data_gateway/store/evidence_chain.py` 加固 fail-closed：
    - 新增 `remote_success_attempts_missing_http_evidence` 审计项；
    - `remote_success` attempt 无对应 HTTP evidence 时审计失败；
    - 同时保留 raw/normalized/doc 可追踪校验。
  - `src/claw_trade/data_gateway/packs/service.py` 增加 `materialize_for_worker()` 入口，复用统一 materializer。
  - 测试更新：
    - `tests/unit/data_gateway/test_evidence_chain.py` 补充 missing HTTP evidence fail-closed 断言。
    - `tests/unit/data_gateway/test_openviking_lineage_writer.py` 新增 blocked pack 阻断 lineage 写入用例。
- 必跑最小验证：
  - `uv run pytest tests/unit/data_gateway/test_evidence_chain.py tests/integration/data_gateway/test_openviking_lineage.py tests/contracts/test_openviking_contract.py` -> exit `0`（11 passed）。
- 同面 sanity：
  - `uv run pytest tests/unit/data_gateway/test_domain_pack_service.py tests/unit/data_gateway/test_openviking_lineage_writer.py` -> exit `1`。
  - 失败集中在 `test_domain_pack_service.py`（8 failed），主要是当前工作区既有 path 下 `remote_success` 要求 raw/normalized refs 的不一致；`test_openviking_lineage_writer.py` 全部通过（含新增用例）。
- anti-fake：
  - changed paths 扫描 `mock|stub|fake|skeleton|placeholder|fallback success|兜底|伪造` 无命中（`rg` exit `1`）。
  - 未引入把 `cache_hit/shared_result/rate_limited/cached_empty/cooldown_skipped` 写成 `remote_success` 的新路径。
- 偏差状态：无 T7 范围外实现；未修改禁止路径；未引入 mock/stub/fake/fallback success。

## 13:22 [codex-data-layer-t7-review-fix]
- Task ID：T7（复核修正）。
- 修复：
  - `src/claw_trade/data_gateway/packs/materializer.py`
    - 删除按 `attempt_id` 合成 `mongo://openbb_provider_http_evidence/{attempt_id}:http` 的逻辑；
    - `http_evidence_refs` 仅从显式输入收集（`DataGap.evidence_refs`、`attempt.source_metadata.http_evidence_ref(s)`），无真实 ref 时保持空。
  - `src/claw_trade/data_gateway/store/evidence_chain.py`
    - `remote_success` 缺 HTTP evidence 的失败检查改为“仅对明确期望 HTTP evidence 的 attempt 生效”；
    - 期望来源：`attempt.source_metadata.http_evidence_expected=True` 或 `managed_http_required=True`；
    - 保留原有 fail-closed：HTTP evidence row 若 `status=remote_success` 且缺 `response_status_code`/`response_headers_summary`/`raw_ref` 仍失败。
  - `src/claw_trade/data_gateway/providers/execution.py`
    - 写 attempt `source_metadata` 时新增 `http_evidence_expected` 标记；
    - 标记条件：`remote_success` 且（`spec.managed_http_required` 或存在可见 HTTP 元数据/captured_http）。
  - 测试补充：
    - `tests/unit/data_gateway/test_evidence_chain.py`
      - 新增：`remote_success + http_evidence_expected=True + 无 HTTP evidence` -> 审计失败；
      - 新增：`remote_success + 无 marker + 有 raw/normalized` -> 不因缺 HTTP evidence 失败。
    - `tests/unit/data_gateway/test_openviking_lineage_writer.py`
      - 新增：materializer 不再合成不存在的 HTTP evidence ref。
- 验证：
  - `uv run pytest tests/unit/data_gateway/test_evidence_chain.py tests/unit/data_gateway/test_openviking_lineage_writer.py` -> exit `0`（13 passed）。

## 12:26 [codex-data-layer-t8-report-plan-cutover]
- Task ID：T8。
- 完成：
  - 新增 `src/claw_trade/data_gateway/report_plan.py`：把 `/report` 启动时的 `RunProviderPlan` 映射为 `ReportDataPlan` / `RequirementBatch` / `ProviderCallSpec` / planned DomainPack 证据快照，并写入 `report_data_plan.v1` payload。
  - 更新 `src/claw_trade/workflow/runner.py`：
    - `report_command` 增加硬切约束：仅允许 `data_gateway=openbb`；
    - run plan 写入后同步生成并落盘 `runs/<run_id>/data_gateway/report-data-plan.json`。
  - 新增合同测试 `tests/contracts/test_report_data_plan_cutover.py`：
    - 校验 `/report` 初始化会写 ReportDataPlan 快照；
    - 校验 `data_gateway!=openbb` 直接 fail closed；
    - 校验 workflow/UI/CLI 报告路径不直连 provider adapter/import。
  - 新增集成测试 `tests/integration/data_gateway/test_pack_runtime_report_plan.py`：
    - 校验 pack runtime 读取并使用 run plan，不走旧路径；
    - 校验 materialized worker 可见文本会脱敏协议 ref（`mongo://...`）。
- 最小验证：
  - `uv run pytest tests/contracts/test_report_data_plan_cutover.py tests/integration/data_gateway/test_pack_runtime_report_plan.py` -> exit `0`（5 passed）。
- 同面 sanity：
  - `uv run pytest tests/unit/data_gateway/test_run_provider_plan.py` -> exit `0`（13 passed）。
- anti-fake 扫描：
  - `rg` 扫描 workflow/ui/cli 旧 provider token 与直连 provider adapter import，均无命中（exit `1`）。
- 偏差状态：无；未改禁改 provider adapter 路径；未引入 mock/stub/fake/fallback success。

## 12:44 [codex-data-layer-t8-manager-review-fix]
- Task ID：T8（manager review 修正）。
- 修复：
  - `tests/contracts/test_report_data_plan_cutover.py` 新增“旧路径 patch/raise 证明”：
    - 注入 import blocker，阻断 `frontline_data_pack`/`provider_executor`/`crypto_market_data_pack`/`claw_trade.providers`；
    - 同时 poison `DomainPackService.get_pack` 为抛错，证明 `/report` entry 初始化阶段不会触发旧 pack/provider 直连路径。
  - `src/claw_trade/data_gateway/report_plan.py` 增加显式字段 `domain_pack_generation.mode=deferred_to_pack_runtime`，说明 DomainPack 物化延后到 pack runtime tool call。
  - `tests/integration/data_gateway/test_pack_runtime_report_plan.py` 增加 `plan_store.load_calls` 断言，证明 pack runtime 路径确实从 `run_provider_plan_store.load(run_id)` 读取并使用已存 plan。
- 最小验证：
  - `uv run pytest tests/contracts/test_report_data_plan_cutover.py tests/integration/data_gateway/test_pack_runtime_report_plan.py` -> exit `0`（6 passed）。
- anti-fake/边界扫描：
  - workflow/ui/cli 旧 provider token 扫描无命中（`rg` exit `1`）。
  - changed-path `mock|stub|fake|skeleton|placeholder success|fallback success` 扫描无命中（`rg` exit `1`）。

## 13:55 [codex-data-layer-t10-four-market-provider-policy]
- Task ID：T10。
- 完成：
  - 新增 `src/claw_trade/data_gateway/providers/market_policy.py`：
    - 四市场 `/report` 支持状态集中为 policy 决策。
    - `/select` 范围按市场返回：CN_A 默认等待 warehouse cutover，完成后才 `supported`；Crypto 历史包缺口保持 `target_design`；HK/US 返回 `unsupported` blocker gap，且不恢复历史包。
    - provider policy 按 `market + domain + data_type` 解析候选，返回 official lock、user-config scope、discovery-only scope 和 gaps。
    - official-required 数据类型在 official source 缺失时返回 official gap，不允许 paid/vendor/search 替代。
    - UI provider display 只在完整 report/select 主链路证据存在时 `show`；key-only、provider-name-only、probe-only 均隐藏。
  - 新增 `tests/contracts/test_four_market_provider_policy.py`：
    - 覆盖四市场 report/select 范围、CN_A CSV seed/import 角色、Crypto history missing gap、HK/US select unsupported、CNInfo/HKEXNews/SEC official lock、search/Polymarket 非正式事实源。
  - 新增 `tests/contracts/test_ui_provider_display_policy.py`：
    - 覆盖 key-only/probe-only 隐藏、完整主链路证据才显示、官方/内置源不进普通 key 设置页、默认无主链路证据时不显示任何 provider。
- 验证：
  - `uv run pytest tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `0`（12 passed）。
  - `python3 -m compileall -q src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `0`。
- anti-fake / 状态 / 平行表扫描（T10 code/test changed paths）：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `1`（无命中）。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `1`（无命中）。
- 偏差状态：无；未修改 provider adapter、selection、workflow、UI backend、docs、OpenClaw 或 pyproject/lock；policy resolution 不写 Mongo，未新增 provider，未新增平行 Mongo 主表。
- BLOCKED：无 T10 内部 blocker。Crypto 历史包完成仍等待 T9B approval；CN_A select `supported` 仍取决于 T9A warehouse cutover evidence。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 18:10 [codex-data-layer-doc-code-consistency-fix]
- Task ID：数据层代码-文档一致性纠偏。
- 人类决定：T9B 暂不做；`AR/USDT -> ARUSD` 不接受，USDT 与 USD 必须保持两回事。
- 完成：
  - 撤回未批准的 T9B Binance/Crypto history 实现：删除 `src/claw_trade/data_gateway/crypto_history_importer.py`、`scripts/crypto/import_binance_public_klines_to_mongo.py`、`tests/contracts/test_crypto_history_ingest_scope.py`。
  - `docs/数据层实施任务清单.md` 将 T9B 改为 `BLOCKED / TODO`，写明等待 universe、source/exchange、history range、interval、license boundary 单独批准；`todolist.md` 记录 T9B 暂不做。
  - Crypto 默认 quote 改为 USDT；`AR/USDT`、`AR.USDT`、`AR-USDT`、`AR_USDT`、`ARUSDT` 都规范为 `AR/USDT` / `ARUSDT`，不再变成 `ARUSD`。
  - 移除行情适配器中把缺失 OHLCV/date/volume 人工补成成功数据的路径；缺 provider 字段就显式失败或保留缺口。
  - 价格提醒默认 quote provider 不再返回 `current_price=0` 的假报价，改为显式数据源失败。
  - pack 层无 `ProviderExecutionEvidenceHelper` 时 fail closed，不能在没有 raw/normalized refs 的情况下写 `remote_success`。
- 验证：
  - `uv run pytest tests/contracts/test_data_layer_coverage_ledger.py tests/contracts/test_data_layer_final_acceptance_plan.py tests/unit/data_gateway/test_domain_pack_service.py tests/integration/data_gateway/test_market_pack_crypto.py tests/integration/data_gateway/test_fundamental_pack_crypto.py tests/unit/ui/test_confirmation_controller.py tests/integration/data_gateway/test_news_pack_cn_a.py tests/integration/data_gateway/test_social_pack_cn_a.py tests/integration/data_gateway/test_policy_pack_cn_a.py tests/integration/data_gateway/test_hot_money_pack_cn_a.py tests/integration/data_gateway/test_lockup_pack_cn_a.py` -> exit `0`（66 passed）。
  - `uv run pytest tests/unit/test_instrument_resolver_hk.py tests/unit/data_gateway/test_default_provider_catalog.py tests/unit/data_gateway/test_fundamental_adapters_live_contract.py tests/unit/data_gateway/test_market_adapters_live_contract.py -k "crypto or tencent_quote_row or mootdx_quote_row or baidu"` -> exit `0`（33 passed, 72 deselected）。
  - `uv run pytest tests/contracts/test_data_layer_failure_semantics.py tests/contracts/test_data_layer_gate_semantics.py tests/contracts/test_data_layer_store_contract.py tests/unit/data_gateway/test_provider_execution.py -k "remote_success or rate_limited or cache_hit or cached_empty or shared_result or evidence"` -> exit `0`（20 passed, 14 deselected）。
  - `rg -n "crypto_history_importer|import_binance_public_klines_to_mongo|test_crypto_history_ingest_scope|APPROVED_SYMBOLS|build_binance_public_klines_manifest" src/claw_trade tests scripts docs/数据层总体设计.md docs/数据层详细设计.md docs/数据层实施任务清单.md todolist.md` -> exit `1`（无命中）。
  - `rg -n "\bARUSD\b|\bBTCUSD\b|\bETHUSD\b" src/claw_trade tests scripts docs/数据层总体设计.md docs/数据层详细设计.md docs/数据层实施任务清单.md todolist.md` -> exit `0`，仅命中总体设计里的负向规则 `不能规范化成 ARUSD`。
- 偏差状态：T9B 已恢复为未批准/未实现；USDT 未当 USD；未新增 runtime guard；未新增平行 Mongo 主表、OpenViking provider cache、旧路径兜底或未批准 provider。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增；测试替身只用于 focused tests，不作为产品成功证据。

## 14:08 [codex-data-layer-t10-manager-review-fix]
- Task ID：T10（manager review 修正）。
- 问题：`resolve_select_market_policy(Market.CRYPTO, crypto_history_ingest_approved=True)` 原先会返回 `gaps=()`，把“批准导入”误表达成“已有 Mongo/warehouse 覆盖”，形成 fake-readiness 风险。
- 修复：
  - `src/claw_trade/data_gateway/providers/market_policy.py`
    - 保留 `crypto_history_ingest_approved`，但 approval-only 仍返回 `crypto_select_history_missing` blocker gap。
    - 新增显式 `crypto_history_ingest_complete`，只有该完成/覆盖证据式开关为 true 时才清空 Crypto history gap。
    - approval-only 的 `history_package_scope` 改为 `approved_but_ingest_not_complete`。
  - `tests/contracts/test_four_market_provider_policy.py`
    - 新增断言：approval-only 不移除 gap；complete flag 才允许 `gaps=()`。
- 验证：
  - `python3 -m compileall -q src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `0`（12 passed）。
- anti-fake / 状态 / 平行表扫描（T10 changed paths）：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `1`（无命中）。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" src/claw_trade/data_gateway/providers/market_policy.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_ui_provider_display_policy.py` -> exit `1`（无命中）。
- 偏差状态：无；未修改 selection、workflow、provider adapters、UI backend、docs、OpenClaw、pyproject/lock。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 12:40 [codex-data-layer-t9-select-warehouse-cutover]
- 完成：T9 最小切换已实现：`/select` 读门禁要求 completed run 携带 `select_data_plan_ref`、`warehouse_check_ref`、provider attempt refs 和 `openbb_normalized` refs；`restored://` legacy run、缺 marker、缺 normalized/attempt refs、本地 Baostock normalized ref 均返回 `selection_warehouse_check_missing`，不会恢复为可用 run。
- 决策：SelectDataPlan/warehouse marker 继续落在现有 selection store/evidence 文件字段中，不新增 `select_data_plans` Mongo collection；旧 selection provider batch 只在 data job 路径返回 warehouse check ref，本地 Baostock 仍不提供 runtime 可用 warehouse marker。
- 验证：`uv run pytest tests/contracts/test_select_warehouse_cutover.py tests/integration/selection/test_data_job_pipeline.py tests/integration/selection/test_select_command_chat_flow.py` -> exit 0，23 passed, 19 warnings；anti-fake 扫描未发现 mock/stub/fake/skeleton/placeholder/fallback success。

## 12:51 [codex-data-layer-t11-ui-provider-display]
- Task ID：T11。
- 完成：
  - `src/claw_trade/ui_backend/data_source_settings.py`：`DataSourceSettingsService` 新增可注入 display decision source，默认复用 T10 `ui_display_decisions(default_market_policy_registry(), evidence_by_provider={})`，因此无主链路证据时设置页 `supportedTypes/instances` 为空。
  - 设置页列表、测试、保存入口均只接受 `ProviderDisplayStatus.SHOW` 且需要用户凭证的 provider；旧 source list 只保留为候选映射，不再决定显示或保存权限。
  - `probe_not_supported` 不再允许启用保存，避免把 UI probe 缺口或本地配置写成 provider success。
  - 测试补充 key-only/provider-name-only/probe-only 隐藏、五项主链路证据缺一不可、默认空显示、未集成 provider 保存拒绝、probe success 不提升设置页显示。
- 验证：
  - `uv run pytest tests/contracts/test_ui_provider_display_policy.py tests/settings/test_settings_forbidden_surface.py tests/settings/test_enhanced_source_health_probes.py tests/settings/test_enhanced_sources.py tests/unit/ui/test_data_source_settings.py` -> exit `0`（89 passed）。
- anti-fake / 状态 / 平行表扫描：
  - 产品路径 `src/claw_trade/ui_backend/data_source_settings.py` 三组扫描均 exit `1`（无命中）：`mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底|fake validated|fake online`；`remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped`；`market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps`。
  - changed test paths 的 anti-fake 扫描 exit `0`，命中仅为测试替身函数名、负向断言标签和覆盖矩阵文字；未新增产品假成功语义。
- 偏差状态：无；未修改 selection/workflow/provider adapter/OpenClaw/web/docs/pyproject/uv.lock；未新增 provider、display cache、provider cache 或 Mongo collection。
- BLOCKED：无。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 12:58 [codex-data-layer-t13-old-new-cutover]
- Task ID：T13。
- 完成：
  - 新增 `tests/contracts/test_old_new_cutover.py`，聚合旧新切割合同：旧 select modules 可 import 但必须标注 archived/read-only/explicit fetcher；`/select` read entry 在旧 batch/local fetch 函数 patch-to-raise 时仍只读 warehouse-marked completed run；`restore_selection_run_store()` 从 legacy data job evidence 恢复的 `restored://...` completed payload 只能留作 read-only reference，不能成为 latest usable completed run；workflow/select entry AST/grep 不直接 import 或调用旧 select/provider path。
  - `src/claw_trade/selection/provider_batch.py`、`src/claw_trade/data_gateway/selection_batch.py`、`src/claw_trade/data_gateway/selection_local_baostock.py` 增加 T13 模块标注：保留为显式 data-job/fetcher 或 seed/import/audit reader，不作为 `/select` read-entry availability fallback。
  - `tests/contracts/test_report_data_plan_cutover.py` 将 `claw_trade.data_gateway.providers.market_policy` 作为允许的 policy import；它是 UI/provider display policy，不是旧 provider adapter 执行路径。
- 设计来源满足：
  - `docs/数据层实施任务清单.md` T13：旧 provider adapter 可保留为 fetcher；旧 batch/local path 入口层阻断；contract tests import old modules but production entry does not call them；`restored://...` legacy evidence 不可被 `/select` resolver 当作可用 run。
  - `docs/数据层总体设计.md` DG-GEN-003/DG-SELECT-003：不做 report/select 旧新兜底，本地文件只做 seed/import/audit。
- 验证：
  - `uv run pytest tests/contracts/test_old_new_cutover.py tests/contracts/test_report_data_plan_cutover.py tests/contracts/test_select_warehouse_cutover.py` -> exit `0`（13 passed）。
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" tests/contracts/test_old_new_cutover.py tests/contracts/test_report_data_plan_cutover.py src/claw_trade/selection/provider_batch.py src/claw_trade/data_gateway/selection_batch.py src/claw_trade/data_gateway/selection_local_baostock.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" <changed paths>` -> exit `0`，仅命中既有 `reason="no_remote_success"`，不是新增成功路径。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" <changed paths>` -> exit `0`，命中均为既有 `data_gaps` 变量/字段；未新增平行 collection。
  - `rg -n "fetch_selection_batch_from_data_gateway|build_selection_provider_batch_plan|fetch_selection_batch_from_local_baostock|data_gateway\\.providers\\.(market|fundamental|news|social)" src/claw_trade/workflow/runner.py src/claw_trade/selection/controller.py` -> exit `1`（无命中）。
- 偏差状态：无 T13 越界；未改 provider adapter、OpenClaw、UI/web、docs；未实现 T9A importer 或新 provider 功能。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 14:32 [codex-data-layer-t12-failure-semantics]
- Task ID：T12。
- 完成：
  - 新增 `tests/contracts/test_data_layer_failure_semantics.py`，把 §18 状态表固定成合同：`remote_success`、`remote_error`、`empty`、`field_missing`、`credential_missing`、`rate_limited`、`cooldown_skipped`、`cache_hit`、`cached_empty`、`cache_stale`、`shared_result`、`not_applicable`、`evidence_write_failed`、`sdk_http_unknown` 全部覆盖。
  - 合同测试明确只有 `remote_success` 可为远端成功，且必须有 attempt/raw/normalized refs；cache/shared/rate-limit/cached-empty/cooldown/sdk-unknown 不可伪装成远端成功。
  - 合同测试明确 failure 状态必须携带 attempt 和 gap/evidence refs；`sdk_http_unknown` 不允许伪造 HTTP evidence；capture-only / literal completed rows 不构成成功。
  - `src/claw_trade/data_gateway/models.py` 最小补约束：`ProviderCallResult` 必须有 `attempt_ref`；`evidence_write_failed` gap 会阻止 `DomainPackApprovalStatus.APPROVED` material。
  - 未修改 `src/claw_trade/guards/**`，未更新 runtime guard allowlist/early-stop allowlist，未新增 runtime guard。
- 验证：
  - `uv run pytest tests/contracts/test_data_layer_failure_semantics.py tests/unit/data_gateway/test_models.py` -> exit `0`（22 passed）。
  - `uv run pytest tests/contracts/test_data_layer_models.py` -> exit `0`（7 passed）。
  - `uv run pytest tests/contracts/test_data_layer_failure_semantics.py tests/contracts/test_guard_change_requires_approval.py tests/unit/data_gateway/test_models.py` -> exit `1`；T12 新测试和 model 单测通过，失败来自既有 guard 冻结测试发现 `src/claw_trade/selection/evidence.py` 有 39 个 guard-like rejection calls 不在 allowlist。该文件不在 T12 允许写入范围，本任务未改 allowlist，记录为外部 dirty-worktree blocker。
- anti-fake / 状态 / 平行表扫描（T12 touched paths）：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底|capture-only|hard-coded completed" src/claw_trade/data_gateway/models.py tests/contracts/test_data_layer_failure_semantics.py` -> exit `0`，命中均为负向合同、guard redline 名称或模型拒绝伪 HTTP evidence 文案。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/models.py tests/contracts/test_data_layer_failure_semantics.py` -> exit `0`，命中均为状态枚举、模型约束或负向断言。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" src/claw_trade/data_gateway/models.py tests/contracts/test_data_layer_failure_semantics.py` -> exit `0`，命中均为既有模型字段或测试变量；未新增平行主表或 provider cache。
- 偏差状态：T12 内无越界；未实现未分配任务，未修改 workflow/selection/ui/provider/store/OpenClaw/docs/pyproject/lock。
- BLOCKED：完整 suggested focused batch 受 `src/claw_trade/selection/evidence.py` 既有/外部 guard-like rejection call surface 影响失败；T12 不能在允许范围内修复。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 15:08 [codex-data-layer-t14-final-acceptance-plan]
- Task ID：T14。
- 完成：
  - 新增 `scripts/validation/data_layer_acceptance_plan.py`，定义轻量结构化最终验收计划 artifact；计划只描述未来验收批次，不执行 live/fresh/provider/UI。
  - 计划顺序固定为 focused unit/contract first，再 scoped integration；runtime proof 必须先过 AGENTS §12.1 preflight，再使用 `scripts/start-control-runtime.sh -- <command>`。
  - 计划保留 `/report`、A 股 `/select`、UI provider display、价格提醒接入项的 future runtime proof 槽位。
  - 计划写入 collect-first 规则与 early-stop 例外：数据真实性、架构边界、PM authority、provider evidence chain、旧路径 fallback、runtime preflight 任一不可信时停止。
  - 计划写入 old-new cutover、evidence-chain audit、状态红线、Mongo collection audit、OpenViking role audit；Mongo collection 只列 existing `openbb_*`，OpenViking 只允许 materials/lineage/readback/summary。
  - T9B Crypto history warehouse 保持 `blocked`，原因是 universe/source/exchange/history range/interval/license boundary 未批准；T12 guard-freeze blocker 保持 stop condition，未更新 guard allowlist。
  - 新增 `tests/contracts/test_data_layer_final_acceptance_plan.py`，验证计划结构、runtime preflight、future proof 槽位、collect-first/early-stop、audit checks、Mongo/OpenViking 边界和 T9B/T12 blocker。
- 验证：
  - `uv run python -m py_compile scripts/validation/data_layer_acceptance_plan.py tests/contracts/test_data_layer_final_acceptance_plan.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_data_layer_final_acceptance_plan.py` -> exit `0`（8 passed）。
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" scripts/validation/data_layer_acceptance_plan.py tests/contracts/test_data_layer_final_acceptance_plan.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" scripts/validation/data_layer_acceptance_plan.py tests/contracts/test_data_layer_final_acceptance_plan.py` -> exit `0`，仅命中负向状态红线和测试断言。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" scripts/validation/data_layer_acceptance_plan.py tests/contracts/test_data_layer_final_acceptance_plan.py` -> exit `1`（无命中）。
- 偏差状态：无 T14 越界；未修改 `src/**`、`docs/**`、`third_party/**`、`web/**`、`agents/**`、pyproject/lock/package 文件或 guard allowlist；未运行 live/fresh/provider/UI。
- BLOCKED：T9B 仍 blocked；T12 组合验证仍受 guard-freeze blocker 阻断。T14 本身只定义验收计划，不把这些 blocker 伪装为完成。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 15:42 [codex-t12-selection-evidence-guard-cleanup-subagent]
- Task ID：T12 blocker 清理。
- 完成：
  - `src/claw_trade/selection/evidence.py` 从 `claw_trade.guards.common` 剥离，不再导入或调用 runtime guard API。
  - 新增本地结果类型 `SelectionEvidenceValidationResult`，字段保持 `ok/category/reason/paths`，现有调用方可继续按 `.ok/.reason/.paths` 消费。
  - provider request capture、visible tools、dispatch command snapshot、runtime markers、candidate pack material boundary、provider request sequence 等失败语义保持 fail closed；只是 selection 本地证据合同不再统计为 runtime guard。
  - 未修改 `src/claw_trade/selection/dispatch.py`、`tests/contracts/test_selection_provider_payload.py`、`tests/contracts/test_guard_change_requires_approval.py`、`src/claw_trade/guards/**`、T3 `run_plan.py`/`market_adapters.py`/`test_default_provider_catalog.py`。
- 验证：
  - `uv run python -m py_compile src/claw_trade/selection/evidence.py src/claw_trade/selection/dispatch.py tests/contracts/test_selection_provider_payload.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_guard_change_requires_approval.py tests/contracts/test_selection_provider_payload.py` -> exit `0`（`22 passed`）。
- anti-fake / anti-guard 扫描：
  - `rg -n "guard_failed|guard_passed|GuardResult|ApprovalResult" src/claw_trade/selection/evidence.py` -> exit `1`（无命中）。
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" src/claw_trade/selection/evidence.py tests/contracts/test_selection_provider_payload.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/selection/evidence.py tests/contracts/test_selection_provider_payload.py` -> exit `1`（无命中）。
- 偏差状态：无；未更新 guard allowlist，未新增 runtime guard/early-stop/hard gate/claim dictionary/exporter rejection rule。
- T3/USDT：未修改 T3 产品代码或 `AR/USDT` symbol 逻辑；USDT 未被当成 USD。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 16:20 [codex-t9b-crypto-binance-public-data-seed-import]
- Task ID：T9B。
- 完成：
  - 新增 `src/claw_trade/data_gateway/crypto_history_importer.py`：Binance Public Data spot monthly klines `1d` seed/import 机制。默认批准范围固定为 `BTC/USDT`、`ETH/USDT`、`SOL/USDT`、`BNB/USDT`、`XRP/USDT`、`DOGE/USDT`、`ADA/USDT`、`AVAX/USDT`、`LINK/USDT`、`AR/USDT`，provider raw symbol 固定为对应 `*USDT`，窗口固定为 `2021-05` 到 `2026-05`。
  - importer 支持本地 Binance monthly zip/CSV 文件和注入 URL fetcher；生成 expected URL/checksum URL manifest；校验 approved symbol/month/interval、checksum、zip 内 CSV、字段数、UTC 月份、重复冲突；缺失文件、缺 checksum、checksum mismatch、空文件、字段缺失均写 blocker gap 和非远端 attempt。
  - normalized rows 写入现有 `openbb_normalized`，字段包含 `market=CRYPTO`、`ticker=BTC/USDT` 格式、`provider_raw_symbol=BTCUSDT` 格式、`interval=1d`、UTC `open_time/close_time`、OHLCV、`quote_volume`、`trade_count`、source URL/file/checksum metadata。
  - raw/normalized/attempt 只落现有 `openbb_raw_payloads`、`openbb_normalized`、`openbb_provider_attempts`；成功导入状态为 `not_applicable`，不写 `remote_success`。
  - 新增 `scripts/crypto/import_binance_public_klines_to_mongo.py`：operator CLI，支持 `--source-dir` 或显式 `--download-from-urls --download-dir`，输出 `full_history_completion_claimed=false`。
  - 新增 `tests/contracts/test_crypto_history_ingest_scope.py`：锁定批准 source/scope、USDT symbol 保留、local zip import 证据链、缺当前月 gap/attempt、checksum 缺失/不匹配 fail closed、拒绝未批准 `BTCUSD` 和窗口外月份。
- 验证：
  - `uv run python -m py_compile src/claw_trade/data_gateway/crypto_history_importer.py scripts/crypto/import_binance_public_klines_to_mongo.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_crypto_history_ingest_scope.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_select_warehouse_cutover.py` -> exit `0`（17 passed）。
  - anti-fake scan 1：`rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底" src/claw_trade/data_gateway/crypto_history_importer.py scripts/crypto/import_binance_public_klines_to_mongo.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `1`（无命中）。
  - anti-fake scan 2：`rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" ...` -> exit `0`，仅命中 `remote_success=False` 和测试断言，未写远端成功。
  - anti-parallel-table scan：`rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" ...` -> exit `0`，仅命中 result/CLI 的 `data_gaps` 字段。
  - USDT scan：`rg -n "ARUSD|BTCUSD|ETHUSD|USD" ...` -> exit `0`，命中为批准的 `*USDT`、`BTC/USDT`/`AR/USDT`、`currency=USDT`，以及拒绝未批准 `BTCUSD` 的负向测试；未把 USDT 转成 USD。
- 偏差状态：无 T9B 越界；未改 provider catalog/policy、`/select` runtime、guards、OpenClaw、docs、pyproject/lock、T3 相关文件或 worker/report/web 文件。
- BLOCKED：未执行真实全量下载或生产 Mongo 入库；当前交付只证明 seed/import 机制和 scope contract，不宣称生产数据完成。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增；USDT 保留，未转换为 USD。

## 16:38 [codex-t9b-review-fix-remove-direct-http-download]
- Task ID：T9B 复核修正。
- 修正：
  - `scripts/crypto/import_binance_public_klines_to_mongo.py` 删除 CLI 直接联网下载能力：移除 `--download-from-urls`、`--download-dir`、`urlopen`、`Request`、`HTTPError`、`_download_approved_files`、`_download_one`。
  - CLI 改为只接受 `--source-dir` 导入本地已下载 Binance Public Data monthly zip/CSV。
  - CLI 输出 `execution_scope="local_binance_public_data_seed_import"`，仍输出 `full_history_completion_claimed=false`。
  - `tests/contracts/test_crypto_history_ingest_scope.py` 增加源码断言：CLI 不包含 `urlopen` / `download-from-urls`。
  - 未实现 managed HTTP downloader，未改 provider runtime；manifest 里的 expected URL/checksum URL 仍由 importer 生成，供人工或后续 approved managed HTTP downloader 使用。
- 验证：
  - `uv run python -m py_compile src/claw_trade/data_gateway/crypto_history_importer.py scripts/crypto/import_binance_public_klines_to_mongo.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `0`。
  - `uv run pytest tests/contracts/test_crypto_history_ingest_scope.py tests/contracts/test_four_market_provider_policy.py tests/contracts/test_select_warehouse_cutover.py` -> exit `0`（17 passed）。
  - anti-fake scan（T9B code/test paths）-> exit `1`（无命中）。
  - remote/state scan（T9B code/test paths）-> exit `0`，仅命中 `remote_success=False` 和测试断言。
  - table scan（T9B code/test paths）-> exit `0`，仅命中 `data_gaps` 字段。
  - USD scan（T9B code/test paths）-> exit `0`，命中为批准的 `*USDT`、`*/USDT`、`currency=USDT`，以及拒绝未批准 `BTCUSD` 的负向测试。
- 偏差状态：无；没有直接 HTTP 下载路径，没有 provider/runtime 改动；USDT 未变 USD。

## 17:05 [codex-t9b-binance-timestamp-unit-fix]
- Task ID：T9B。
- 阻断证据：本地 Binance Public Data spot monthly klines 1d 导入批次 `t9b-binance-public-202105-202604-r2` 中，2025-01 到 2026-04 的 160 个文件均因 `binance_public_kline_timestamp_invalid` 失败；真实首字段从 2024-12 的毫秒 `1733011200000` 变为 2025-01 的微秒 `1735689600000000`。
- 完成：
  - `src/claw_trade/data_gateway/crypto_history_importer.py`：新增 Binance kline timestamp 解析 helper，只接受 13 位毫秒或 16 位微秒量级，统一输出 UTC `datetime` 和 canonical millisecond 字段；秒级等明显无效量级继续返回 `binance_public_kline_timestamp_invalid` blocker gap，不吞失败、不写成功。
  - `tests/contracts/test_crypto_history_ingest_scope.py`：新增 focused contract，证明 2025-01 微秒时间戳可正常入库并保留 `BTC/USDT` / `BTCUSDT` / `USDT`，同时秒级时间戳仍阻断且无 normalized refs。
- 设计来源满足：
  - `docs/数据层总体设计.md:360-367`：校验历史 K 线原始文件，统一交易对、时间戳、UTC 时区、周期和计价货币，写入 Mongo normalized rows。
  - `docs/数据层详细设计.md:1752-1755`、`1781`、`1805-1808`：Crypto 历史包运行时主查询是 Mongo，Binance 公共行情可作真实交易对来源，Crypto `/select` 历史包仍需清洗入 Mongo。
  - `docs/数据层实施任务清单.md:330-345`：校验 manifest/覆盖/时间范围/字段完整性，统一时间戳，生成 normalized rows，缺口保留为 explicit gaps/attempts，不能写 remote_success。
- 验证：
  - 先新增 focused contract 后运行 `uv run pytest tests/contracts/test_crypto_history_ingest_scope.py -q` -> exit `1`，复现微秒时间戳被当作毫秒解析导致 `year 56971 is out of range`。
  - 修复后 `uv run pytest tests/contracts/test_crypto_history_ingest_scope.py -q` -> exit `0`（7 passed）。
  - `uv run python -m py_compile src/claw_trade/data_gateway/crypto_history_importer.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `0`。
- anti-fake / 状态 / 平行表 / USDT 扫描：
  - `rg -n "mock|stub|fake|skeleton|placeholder|fallback success|假成功|伪造|兜底|hard-coded completed" src/claw_trade/data_gateway/crypto_history_importer.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `1`（无命中）。
  - `rg -n "remote_success|shared_result|cache_hit|rate_limited|cached_empty|cooldown_skipped" src/claw_trade/data_gateway/crypto_history_importer.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `0`，仅命中 `remote_success=False` 和测试断言。
  - `rg -n "market_bars|select_data_plans|provider_cooldowns|http_cache|provider_cache|data_gaps" src/claw_trade/data_gateway/crypto_history_importer.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `0`，仅命中 result 的 `data_gaps` 字段。
  - `rg -n "ARUSD|BTCUSD|ETHUSD|USD" src/claw_trade/data_gateway/crypto_history_importer.py tests/contracts/test_crypto_history_ingest_scope.py` -> exit `0`，命中为批准的 `*USDT`、`*/USDT`、`currency=USDT` 和拒绝 `BTCUSD` 的负向测试；未把 USDT 转成 USD。
- 偏差状态：无 T9B 越界；未修改 `third_party/openclaw/**`、`agents/**`、`docs/**`、`scripts/**`、Mongo 主表定义、provider policy、OpenViking 或旧路径 fallback。
- BLOCKED：未重跑 600 个真实文件的本地导入批次，也未执行生产 Mongo 全量入库；本次只完成并验证时间戳单位缺陷修复。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 17:34 [codex-testclient-hang-diagnosis-subagent]
- Task ID：TestClient hang 诊断。
- 范围：只诊断 `tests/contracts/test_ui_http_routes.py`、`tests/integration/data_gateway/test_pack_runtime_report_plan.py` 和直接 `TestClient(wrapper.create_pack_fastapi_app()).post(...)` 卡住问题；未改业务代码。
- 根因：
  - 在默认沙箱内，空 FastAPI app 的 `TestClient(app).get("/ping")` 也会卡住，说明不是 FastAPI startup/lifespan、route dependency、monkeypatch fixture、app state、pack service 或最近业务代码改动。
  - 更小复现显示 `anyio.from_thread.start_blocking_portal().call(lambda: 42)` 卡住；再缩小到纯 `asyncio` 后，后台线程 `loop.call_soon_threadsafe(...)` 已把 callback 放入 `_ready`，但 epoll/select 没被 self-pipe 唤醒，后台 loop 停在 `selectors.py:468 select`。
  - 给 loop 预置 `call_later(0.5, ...)` 后，跨线程 callback 会在定时器超时醒来时执行，证明任务已入队，问题是跨线程事件循环唤醒被当前执行沙箱限制/破坏。
  - 非沙箱复测纯 asyncio 和空 FastAPI TestClient 均通过；两个 focused pytest 也通过。
- 验证命令：
  - 沙箱内直接 pack app 最小复现：`timeout 12s env UV_CACHE_DIR=.runtime/dev-services/uv-cache UV_LINK_MODE=copy uv run python -X faulthandler -c '...'` -> exit `124`，卡在 `starlette.testclient.py:344 handle_request` / `anyio.from_thread.py`，无业务路由栈。
  - 沙箱内空 FastAPI TestClient：`timeout 8s env UV_CACHE_DIR=.runtime/dev-services/uv-cache UV_LINK_MODE=copy uv run python -X faulthandler -c '...'` -> exit `124`。
  - 沙箱内 `anyio.run()`：exit `0`，输出 `42`。
  - 沙箱内 `anyio.from_thread.start_blocking_portal().call(lambda: 42)`：exit `124`。
  - 沙箱内纯 asyncio `call_soon_threadsafe` 无 timer：exit `1`，`TimeoutError`；有 `call_later(0.5)` timer：exit `0`，callback 执行。
  - 非沙箱纯 asyncio `call_soon_threadsafe`：exit `0`，输出 `callback running` / `42`。
  - 非沙箱空 FastAPI TestClient：exit `0`，输出 `after 200 {"ok":true}`。
  - 非沙箱 `timeout 30s env UV_CACHE_DIR=.runtime/dev-services/uv-cache UV_LINK_MODE=copy uv run pytest tests/contracts/test_ui_http_routes.py -vv` -> exit `0`，`8 passed in 11.78s`。
  - 非沙箱 `timeout 30s env UV_CACHE_DIR=.runtime/dev-services/uv-cache UV_LINK_MODE=copy uv run pytest tests/integration/data_gateway/test_pack_runtime_report_plan.py -vv` -> exit `0`，`2 passed in 0.90s`。
- 是否需要修代码：不需要。修复方向不是 claw-trade 代码；需要让使用 TestClient/anyio blocking portal 的 focused tests 在非沙箱执行，或调整执行环境的跨线程 epoll/self-pipe 权限。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增。

## 18:25 [codex-data-layer-doc-code-alignment-closeout]
- Task ID：数据层文档-代码一致性收口；T9B 按人类决定不纳入本轮。
- 完成：
  - `/report` 数据计划从真实 provider call spec 汇总 requirement，不再只按粗粒度 pack 伪覆盖。
  - `/select` 数据计划改为 snapshot/warehouse check ref，不新增 `select_data_plans` Mongo 主表。
  - CN_A selection batch 改为统一 data_gateway gate：cache/rate-limit/single-flight/attempt/evidence store 缺失直接 fail closed，`helper.execute` 只在 gate owner call 内执行。
  - `cache_hit/shared_result/rate_limited/cached_empty/cooldown_skipped` 不会被写成 `remote_success`；可用数据只接受 `remote_success/cache_hit/shared_result` 且必须有可读 raw/normalized/attempt/openviking 证据。
  - provider/UI HTTP 调用统一走 `managed_requests` -> `run_managed_http`；旧 AlphaEar skill 从 market analyst 挂载和 manifest 中移除。
  - A 股本地 seed importer 加真实性检查：股票/指数 universe、basic、qfq `adjustflag=2`、adjust_factor、批准范围内 index_daily、AkShare private placement manifest/file/字段必须覆盖；单个行业映射缺失、qfq daily 起止日期缺口和 11 个明确不下载指数不阻断。
  - selection partial source 只在完整源失败后执行；若缺 approved strategy / stable top20 必需字段，不再产出 completed rows，只保留 evidence gap。
- 真实 A 股 seed 检查：
  - 当时命令 `uv run python scripts/selection/audit_baostock_selection_seed.py --root .runtime/selection-data/raw/baostock --start-date 2015-01-01 --trade-date 2026-05-27 --run-label t9a-post-hardening-20260530 --max-samples 20` -> exit `3`，该失败口径已在 2026-05-30 被废止。
  - 2026-05-30 人类决策后，`incomplete_daily_count=189`、`missing_index_count=11`、`missing_industry_count=1` 不再作为 T9A 阻断理由；11 个指数明确不下载，单个行业映射缺失和日线起止日期缺口只记录。
- 验证：
  - `uv run pytest tests/integration/selection/test_sel13_provider_batch_glue.py` -> exit `0`（27 passed）。
  - `uv run pytest tests/contracts/test_a_share_seed_to_mongo_import.py tests/contracts/test_managed_http_semantics.py tests/contracts/test_frontline_tool_contract.py::test_market_analyst_does_not_mount_legacy_alphaear_data_skills tests/contracts/test_frontline_tool_contract.py::test_frontline_skill_manifest_only_exports_worker_profile_tools tests/integration/data_gateway/test_news_pack_cn_a.py tests/contracts/test_report_data_plan_cutover.py tests/integration/selection/test_data_job_pipeline.py tests/contracts/test_data_layer_models.py` -> exit `0`（45 passed）。
  - `uv run ruff check ...`（修改过的数据层源码、脚本和 focused tests）-> exit `0`。
  - `rg -n "\b(ARUSD|BTCUSD|ETHUSD)\b" src/claw_trade tests scripts` -> exit `1`；没有把 `AR/USDT`、`BTC/USDT`、`ETH/USDT` 写成 USD 交易对。
  - `rg -n "fallback|兜底" src/claw_trade/data_gateway/selection_batch.py tests/integration/selection/test_sel13_provider_batch_glue.py` -> exit `1`。
- 偏差状态：
  - 已收口本轮确认的代码/文档偏差：粗 requirement、`/select` 计划证据、selection batch 绕 gate、A 股 seed 硬门缺口、AlphaEar 挂载、HTTP 直连、partial source 缺字段成功。
  - 2026-05-30 人类决策后，真实 A 股 seed 不再因 11 个明确不下载指数、单个行业映射缺失、日线起止日期缺口被判未完成。
- mock/stub/fake/fallback/skeleton/placeholder success：未新增为产品成功路径；测试内 fake 仍只用于 contract/integration fixture。
