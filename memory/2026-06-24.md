## 16:42 codex
-作废：这一条错误地把 HK 当前状态判断为未批准。见 17:10 纠正记录。

## 17:10 codex
-纠正：复查 git history 和 memory 后确认 HK 已在 `d835e80 启用港股报告链路` 后批准并多次完整 `/report` 成功；上一条把“缺策略时 fail closed”误读为“HK 当前未批准”。
-完成：恢复 HK profile approved；`/report 00700.HK` 确认卡显示 `腾讯控股`，不再显示 `名称未查到`。
待办：未知港股名称仍应接入正式名称解析数据源；只有 profile/prompt 真实未批准时才 fail closed。
