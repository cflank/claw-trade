- 完成：按 `memory/` 纠正真实 Chrome 验收路径。9222 当前由 `wmux.exe` 监听，`scripts/mcp-chrome.sh --check` 虽显示 ready，但 `@playwright/mcp` 访问 `/json/version/` 会 404，且 browser/page websocket 都报 `Browser webContents not found`；按 2026-05-20 记忆“不杀占用 9222 的既有 Chrome”，改用 9223 上真实 Win11 Chrome 146。`scripts/playwright-win-chrome-mcp-call.mjs` 增加 `PLAYWRIGHT_WIN_CHROME_CDP_ENDPOINT` 覆盖，只用于让同一个 `playwright-win-chrome` MCP server 指向 `mcp-chrome.sh --check` 打印的 websocket，不再使用临时 raw CDP helper。
- 完成：真实 Chrome MCP 打开 `http://172.27.36.34:5175/`，输入并提交 `/select`。页面先显示用户 `/select` 与系统“正在运行选股工作流”，完成后真实页面显示“`/select` 已完成，本轮仅进入等待确认，不会自动启动 `/report`”，左侧出现选股报告，最新结果时间 2026/6/5 23:30:21；进入 `/report`：无；观察：603045.SH、600869.SH、300835.SZ 等 20 只；放弃：无。Chrome console 为 0 errors / 0 warnings。
- 完成：最新 workflow evidence 为 `runs/selection/workflows/select-20260606T032813-req-1780716493474-a6087cf46c2f4/selection-workflow-evidence.json`，`status=completed`，`reason=waiting_report_confirmation`，4 个 selection worker dispatch 均有 provider request、visible tools、raw output 与 OpenViking receipt。
- 完成：本次 `/select` 绑定数据 run `sel-run-eabf7bf15262409d85da8f4466834a14`，data-run 记录 `columnar_manifest_ref=columnar://selection/market_data/market=CN_A/profile=CN_A/trade_date=2026-06-05/manifest.json`，manifest sha256 为 `a3e74a5e604ac82f724fa2b9cbef072db13563b6178c8dbec119434dbe1d8cc0`；本地列式目录 `.runtime/selection/columnar/market_data/market=CN_A/profile=CN_A/trade_date=2026-06-05` 有 44 个 Parquet 文件，总大小约 113MB，manifest coverage 为 `verified`，日线分区 38 个、特征分区 6 个，日线行数 1,854,035，特征行数 5,373。
- 完成：内存复查未见项目进程 GB 级暴涨。测试时 Mongo RSS 约 138MB，UI 后端约 238MB，OpenViking 约 224MB，OpenClaw 最大约 400MB；GB 级进程来自 VS Code/CloudCode，不是 `/select` 数据链路。
- 验证：`git diff --check` 通过；`uv run pytest --import-mode=importlib ... -q` 覆盖数据层、选股、报告、UI store 与 agent skill 相关 277 passed、1 skipped、41 warnings。
