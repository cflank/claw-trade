## 01:58 [review_subagent_B]
-完成：评审 CRYPTO 原始日线补数逻辑，发现主路径可从真实 spot USDT parquet 规划 430 个 symbol，但 fallback 仍可能退化为 BTC-only 或把 universe_ref 当 instrument。
-决策：不修改代码；建议阻断合并直到 CRYPTO 主路径缺失时 fail closed，且 manifest/row fallback 边界补齐测试。
待办：修复 scheduled_runner fallback 退化与 manifest 中间漏日识别后再复审。

## 03:30 [manager]
- 完成：修复“启动补原始行情时右侧任务栏看不到正在补充数据”。根因是 UI 右侧栏只消费 `get-selection-refresh-snapshot` 的选股刷新进度；原始行情补数任务写在 `maintenance_jobs`，`/select` 已能据此拦截旧缓存，但快照接口没有把 running 的原始补数任务转换成前端 `selectionProgress`。
- 决策：不新增轮询接口、不改下载流程；复用现有右侧栏进度结构。当 CN_A/CRYPTO 的 `daily_bar` maintenance job 处于 running 且锁未过期时，快照接口返回“系统启动自动补数据 / 原始行情补数据中”，worker 列表分别显示 A股和加密币 job id；前端标题显示“原始行情补数据”，且该任务不显示“停止选股”按钮。
- 验证：`uv run pytest tests/e2e/ui/test_research_ui_server.py` 33 passed；`pnpm --dir web/research-ui exec vitest run src/__tests__/home-page.test.tsx` 73 passed；`pnpm --dir web/research-ui exec vitest run src/__tests__/right-rail.test.tsx` 6 passed；`pnpm --dir web/research-ui build` 通过；`git diff --check` 通过。真实 Windows Chrome 146 通过 9223 MCP 打开 `http://172.27.36.34:5175/`，右侧栏显示“原始行情补数据 / 原始行情补数据中”，A股和加密币两个临时 running job 均可见，console warning/error 为 0；临时 job 已标记 succeeded 并停止本轮 UI/runtime。
待办：无。

## 11:15 [manager]
- 完成：修复 CRYPTO/A股启动与定时原始行情补数、补完后选股缓存刷新、以及 `/select` 与原始补数冲突处理的回归问题。
- 根因：
  1. CRYPTO 原始补数曾失败在旧/已 BREAK 的币对上，例如 `COSUSDT`，维护计划没有按 Binance 当前 `TRADING` spot USDT universe 过滤，导致下载端返回缺区间。
  2. A股原始补数曾失败在 Mongo 更新文档过大；维护 audit 把巨大 `dataset_refs/raw_refs` 全量写入 job update。
  3. CRYPTO 补数成功后，选股仍读 2026-06-06，是因为选股批处理只读 seed 包 `data/crypto-history-full/normalized-columnar-usdt-only`，没有合并 `.runtime/dev-services` 里新下载的 Parquet。
  4. 更深一层字段根因：真实 runtime CRYPTO row_json 字段是 `source_market_segment=spot` 且没有 `ticker`；旧读路径请求 `market_segment/ticker`，`query_normalized(fields=...)` 因字段不完整把 10600 行真实数据过滤成 0 行。
  5. 自动刷新曾假报 completed；`run_automatic_refresh_once` 只看 data job 是否抛异常，没有读取 data-run 最终状态，所以底层 `failed/no_candidate` 会被 UI 误显示为完成。
  6. `/select 2` 曾复用旧 2026-06-06 缓存；CRYPTO 无显式日期时没有先解析当前可用交易日。
  7. subagent 评审又发现 runtime provider attempt refs 会丢：代码用规范化后的 `source_ref` 去查 provider attempt，而仓库保存的是原始 `dataset_ref`。
- 修复：
  - CRYPTO 维护计划只纳入 Binance 当前 `TRADING` spot USDT symbol；旧/断供币对 fail closed，不污染全量任务。
  - 维护 job audit 对大 ref 列表写 count/sample/hash，避免 Mongo update 文档过大。
  - CRYPTO 选股交易日解析取 seed 与 runtime Parquet manifest 的最大日期；特征生成合并 seed+runtime，runtime 覆盖同 ticker/date 的 seed 行。
  - CRYPTO runtime 读字段改为真实 `source_market_segment`，并从 `record.symbol_id` 补出 `ticker`。
  - provider attempt 查询使用原始 runtime `dataset_ref`，candidate lineage 仍使用规范化 ref；runtime-only 数据没有 seed 文件时也可用 runtime 仓库生成特征。
  - `/select` 对 CRYPTO 也先解析当前交易日，再查 completed cache；原始行情维护 running/failed 时在 cache 读取前拦截。
  - 自动刷新完成后读取 `SelectionDataJobExecution.record` 或 store 最终 data-run 状态；`FAILED/NO_CANDIDATE` 不再报 completed。
- 验证：
  - 单测/集成：`uv run pytest tests/unit/data_gateway/test_maintenance_jobs.py tests/unit/data_gateway/test_scheduled_data_maintenance_runner.py tests/unit/ui/test_scheduled_work_runner.py tests/integration/selection/test_select_command_chat_flow.py tests/unit/selection/test_data_need_refresh_strategy_and_trade_date.py tests/unit/selection/test_refresh_service.py -q` -> 184 passed, 21 warnings；`git diff --check` 通过。
  - 真实运行时字段验证：当前 `.runtime/dev-services` 仓库 `repo.query_normalized(... fields=(source_market_segment,...))` 读到 CRYPTO 10600 行；`_crypto_seed_feature_rows` 生成 424 个 2026-07-01 CRYPTO 特征行，attempt refs 包含 `attempt:crypto_primary:spot_daily_bar...`，不再只有 seed。
  - Chrome 验证：通过 `scripts/mcp-chrome.sh --endpoint http://127.0.0.1:9223 --start` 和 `scripts/playwright-win-chrome-mcp-call.mjs` 操作 Windows Chrome 146，访问 `http://172.27.36.34:5175/`，发送 `/select 2`，UI 生成 2026-07-01 CRYPTO 结果；workflow evidence `runs/selection/workflows/select-20260701T105904-req-1782903543726-60211b0bcca78/selection-workflow-evidence.json` 显示 `market=CRYPTO`、`trade_date=2026-07-01`、`selection_run_id=sel-run-6951b452c2ba4c26b6a9277483c86de6`、data-run completed。
  - 冲突验证：临时将 2026-07-01 最新 raw manifest 置为 inactive，仅模拟原始行情缺口，不删选股缓存；触发 CRYPTO maintenance 后在 Chrome 发送 `/select 2`，UI 显示“原始行情正在补数据，补完后会再计算候选池”，右侧显示 “原始行情补数据中”，未复用旧缓存。
  - 补数验证：受控缺口下 CRYPTO maintenance `inputTotal=424`、status `succeeded`、selectionRefresh completed 到 `sel-run-65017a8c584b4ce8bfe10e12db2a99b3`；A股 maintenance `inputTotal=1`、status `succeeded`、selectionRefresh completed 到 `sel-run-0d9a96df35c2413fadccd7263f22ac34`。Mongo active manifest 恢复：CN_A latest `2026-07-01` row_count=5511；CRYPTO latest `2026-07-01` active manifests=424。
- 待办：不要再用 seed-only 或旧缓存判断 CRYPTO 当前交易日；任何确认必须同时看 memory、运行时 manifest/data-run、Chrome/workflow evidence。

## 12:00 [manager]
- 完成：继续修复 CRYPTO 历史底包缺失/半截时普通启动不会自动初始化、以及原始行情补数失败原因 UI 不可见的问题。
- memory 依据：
  - `memory/2026-06-08.md` 明确规定 `data/crypto-history-full` 和 `claw_trade_crypto_history_*` 是出厂 seed，只读隔离；普通运行时不能写 seed 目录或 seed Mongo。
  - `memory/2026-07-01.md` 11:15 已确认 CRYPTO 选股必须合并 seed + runtime，不能再用 seed-only 或旧缓存判断当前交易日。
- 新根因：
  1. CRYPTO seed 是否可用只按“spot USDT 数量 >= 300”判断；如果 seed 数量够但漏了很多 Binance 当前 `TRADING` spot USDT 币对，普通启动会选择 seed 根目录，后续 coverage 检查直接失败，不会触发 runtime bootstrap 下载。
  2. bootstrap 的代码层只靠 `scripts/start-control-runtime.sh` 阻止写 seed Mongo；如果有人绕过 fixed runtime 直接启动后端，仍可能把 runtime bootstrap 写到 `claw_trade_crypto_history_*`。
  3. 右侧任务栏只把 raw maintenance 的 `running` 状态转成 selectionProgress，`failed` 状态被过滤，用户看不到补数据失败原因。
- 修复：
  - `src/claw_trade/web/state.py`：CRYPTO factory seed 完整性改为“本地行数完整 + 覆盖 Binance 当前 `TRADING` spot USDT universe”；覆盖不足或 exchangeInfo 不可用时走 runtime columnar root，由 bootstrap 写 `.runtime/crypto-history-full/binance` 和 runtime Mongo，不写 `data/crypto-history-full`。
  - `src/claw_trade/web/state.py`：runtime bootstrap 明确拒绝 `DATA_GATEWAY_MONGODB_DATABASE/CN_A_MONGODB_DATABASE/CRYPTO_MONGODB_DATABASE` 解析为 `claw_trade_crypto_history_*`。
  - `src/claw_trade/web/routes_ui.py`：raw maintenance failed 会显示“原始行情补数据失败”和具体 reason/error，不再被右侧任务栏吞掉。
  - `tests/unit/ui/test_web_state.py`：覆盖完整 seed、当前 Binance 覆盖不足走 runtime、runtime bootstrap 不需要 factory 写权限、runtime bootstrap 拒绝 seed Mongo、factory bootstrap 仍需显式写权限。
  - `tests/e2e/ui/test_research_ui_server.py`：覆盖 raw maintenance failed reason 可见。
- subagent 评审：
  - Sartre：普通 fixed runtime 启动不写 seed；指出代码层 seed Mongo 拒绝缺口，已修复。
  - Maxwell：指出半截 seed coverage 判断和右侧 failed 不可见两个阻断点，已修复。
- 验证：
  - `uv run pytest tests/unit/ui/test_web_state.py tests/e2e/ui/test_research_ui_server.py tests/unit/data_gateway/test_scheduled_data_maintenance_runner.py tests/unit/ui/test_scheduled_work_runner.py tests/integration/selection/test_select_command_chat_flow.py tests/unit/selection/test_data_need_refresh_strategy_and_trade_date.py tests/unit/selection/test_refresh_service.py -q` -> `212 passed, 21 warnings`。
  - `git diff --check` -> exit 0。
  - 真实 UI 启动：`scripts/start-research-ui.sh` 通过 fixed runtime 启动；首次沙箱内失败为 Mongo `set_option: Operation not permitted`，提权后启动成功；启动自动补数 job：CN_A `input_total=0 succeeded`，CRYPTO `input_total=0 succeeded`。
  - 受控缺口：仅把最新 raw manifest 标记 inactive，不删缓存；CRYPTO latest `2026-07-01` inactive `424` 个，CN_A inactive `1` 个。
  - Chrome 真实测试：Windows Chrome 146 通过 `scripts/mcp-chrome.sh --endpoint http://127.0.0.1:9223 --start` 和 `scripts/playwright-win-chrome-mcp-call.mjs` 打开 `http://172.27.36.34:5175/`，console `0 errors / 0 warnings`。
  - 补数中 `/select 2`：CRYPTO maintenance `data-maintenance:CRYPTO:kline-refresh:live-gap-crypto-20260701T1139` running 时，Chrome 发 `/select 2`，UI 返回“`/select` 当前不可用：原始行情正在补数据，补完后会再计算候选池。”右侧显示“加密币：原始行情补数据中”。
  - 补数结果：CRYPTO `inputTotal=424 succeeded`，selectionRefresh `completed`，`selection_run_id=sel-run-18af200a82b3491d9859a1f94060c73e`，trade_date `2026-07-01`；CN_A `inputTotal=1 succeeded`，selectionRefresh `completed`，`selection_run_id=sel-run-1d19653a36f246a08905dbd256fb4520`。
  - active manifest 恢复：CN_A latest `2026-07-01` active_count `1`, row_count `5511`; CRYPTO latest `2026-07-01` active_count `424`, row_count sample `25`。
  - 补完后 Chrome 再发 `/select 2`，UI 正常完成选股报告；workflow evidence `runs/selection/workflows/select-20260701T114932-req-1782906570421-3d70fcfed7f02/selection-workflow-evidence.json`：`status=completed`, `market=CRYPTO`, `profile=CRYPTO`, `trade_date=2026-07-01`, `selection_run_id=sel-run-18af200a82b3491d9859a1f94060c73e`, `failure=None`。
- 待办：这次没有真实删除 `data/crypto-history-full` 去全量重下，因为会触发大规模 Binance 历史包下载；缺包/半截包路径用单元测试和启动链路 mock 验证，真实 Chrome 验证覆盖的是启动维护、受控原始行情缺口、下载补数成功和 `/select` 冲突。

## 12:47 [manager]
- 完成：按人工要求重测“只让原始行情缺口可见、不清选股缓存；启动项目；真实 Chrome 输入 select”。
- 数据缺口：
  - 第一次重测：A股最新 3 个交易日 `2026-07-01/2026-06-30/2026-06-29` 的 active raw manifest 标为 `inactive_retest_20260701_restart_select`；CRYPTO 因运行时增量文件是一批 `2026-06-07..2026-07-01`，无法只去掉 3 天而不重写 parquet/hash，所以把最新 `2026-07-01` 的 424 个 active manifest 标为 inactive；选股缓存未清。
  - 启动后自动补数：CN_A `data-maintenance:CN_A:eod:startup-20260701T162611306292Z` running -> succeeded，`input_total=3`，`dataset_refs=16529`；CRYPTO `data-maintenance:CRYPTO:kline-refresh:startup-20260701T162611307227Z` running -> succeeded，`input_total=424`，`dataset_refs=10600`。
  - Chrome 裸 `/select` 在 CRYPTO 仍 running 时启动并完成 A股选股，原因是裸 `/select` 默认市场是 CN_A，而 CN_A 已补完；这不是 CRYPTO 同市场冲突验证。
  - 第二次重测只重造 CRYPTO 缺口，重启项目后 CRYPTO `data-maintenance:CRYPTO:kline-refresh:startup-20260701T163833820868Z` running，真实 Chrome 9224 打开 `http://172.27.36.34:5175/` 并提交 `/select 2`；UI 返回“`/select` 当前不可用：原始行情正在补数据，补完后会再计算候选池。”，右侧显示“系统启动自动补数据 / 加密币：原始行情补数据中（data-maintenance:CRYPTO:kline-refresh:startup-20260701T163833820868Z）”。
  - 第二次 CRYPTO 补数最终 succeeded：`input_total=424`，`audit_counts={request_ids:424,dataset_refs:10600,raw_refs:424,attempt_refs:424,gaps:0}`，`failure_reason=None`。
  - 最终 active raw manifest：CN_A latest `2026-07-01`, active_count `1`, rows `5511`；CRYPTO latest `2026-07-01`, active_count `424`, rows_on_latest `10600`, sample_rows `25`。
- Chrome：使用真实 Windows Chrome 146；9223 会话因确认弹窗导致 wrapper 初始化超时，未强杀；改用独立 profile 的 9224 Chrome，console `0 errors / 0 warnings`。
- 结论：启动自动补数成功；同市场 CRYPTO `/select 2` 在补数中被拦截，不复用旧缓存；裸 `/select` 是 A股命令，A股补完后会正常跑。
