## 03:13 [codex-manager]
-完成：修复 OpenViking lineage 关系写入断点；focused CN_A `600519.SH` live `/report` run `run-20260518-030324-02645c3c` completed，OpenBB evidence audit passed，OpenViking relations 非空并可查到 final report -> PM -> worker -> OpenBB attempt/raw/normalized/chart refs。
-决策：OpenViking relations API 只能从已存在目录资源写 `.relations.json`，因此 API 写入 source 改为 worker/call 目录，真实语义 from/to/kind/hash 保留在 relation reason 与本地 lineage audit 中；不把本地 audit 当作 OpenViking 关系成功。
待办：用修复后的 lineage 重新跑四市场/五样本 canonical T16 collect-first，并更新 T17 closeout 证据。

## 03:24 [codex-manager]
-完成：去掉最终链路写入时重复推送 provider relations 的问题；latest focused CN_A live proof `run-20260518-031458-fb894ea6` completed，OpenBB audit passed，OpenViking `relations_non_empty=true`，关系计数去重后为 final->PM 2、PM->worker 11、worker->L2 4、audit->attempt 11、attempt->raw 8、attempt->normalized 8、chart 2。
-决策：保留 OpenViking API source 为已存在 call 目录，语义边仍写入 relation reason，避免把 `mongo://` 或 `audit://` 当成 OpenViking 关系表源路径。
待办：重新跑 canonical T16 五样本 collect-first，替换旧的空 relations 批次后再做 T17 完成判断。

## 11:22 [codex-manager]
-完成：按 fixed runtime command mode 重跑 canonical T16 五样本 collect-first，证据目录 `docs/evidence/openbb-canonical-t16-lineage-20260518T104114Z/`；CN_A 600519、HK 00700、HK 00005、US AAPL、CRYPTO BTC 均 completed/export passed，run/audit/OpenViking collect exit 均为 0，OpenBB audit 全部 passed，OpenViking relations 全部非空，每个 run 有 13 份 provider prompt、2 张图表、ovpack export/import 证据。
-决策：本批次替代旧 `openbb-canonical-t16-20260518T013557Z` 作为 lineage 修复后的 canonical T16 证据；semantic index 保持 `blocked`，不伪装向量队列可用。
待办：继续 T17 closeout，复核旧路径 import-block、RunProviderPlan no-prefetch、single-flight、Mongo samples、worker/exporter 边界和最终文档收敛后再判断是否完成。

## 09:41 [codex-manager]
-完成：修复 OpenBB provider attempt 重复写入冲突、OpenViking lineage 关联同一 worker 下多次 tool call 的映射、provider_request guard 对自然语言 `profile:` 的误杀，以及 US 前线/终稿 prompt 的过程性英文开场；最终 canonical 五样本证据目录 `docs/evidence/openbb-canonical-t16-final-20260518T125112Z/` collect-first 全通过，failed_cases=0，五个 run 均 export/audit/OpenViking collect passed、OpenBB audit passed、relations 非空、ovpack export/import ok、chart=2、provider prompts=13、evidence_write_failed=0。
-决策：保留 `TOOL_PARAMS_INVALID` 冗余工具调用为可见非阻断残余，不把失败调用伪装成成功；最终报告无该错误词，成功证据链来自后续成功 OpenBB pack 调用。`provider_request.py` guard 改动是误报修复，不新增/收紧 runtime guard，并同步冻结哈希。
待办：后续可继续做 model-visible schema/prompt hardening，减少模型提交 `market=NASDAQ` 或非正 freshness 的冗余失败调用。

## 10:08 [codex-manager]
-完成：收紧 OpenBB 前线资料包工具边界：model-visible schema 不再暴露 ticker/market/profile/date/currency/freshness 等运行字段，工具包装层改为只使用 Python runtime context，并补充合同测试证明模型传入 `market=NASDAQ` 或负数 freshness 不会覆盖 runtime；同步清理前线 prompt 中要求模型填写工具参数的文案。
-决策：市场/profile/date/freshness 属于控制层运行上下文，不属于 worker/prompt 决策；保留模型乱传字段的工具调用证据名列表，但不让其进入 OpenBB provider 请求。
待办：下一轮 live/fresh 验收可复查 provider prompt/tool call 中是否还出现冗余运行参数。

## 10:38 [codex-manager]
-完成：新增 `docs/BB接入方案.md`，固化 BB 接入新口径：OpenBB/data_gateway 负责 CRYPTO 外部 provider 取数与 evidence，BB 只作为离线加密指标分析引擎消费 OpenBB normalized bundle，`market_analyst` 仍只通过 `claw_get_market_pack` 获取自然语言 market pack。
-决策：不恢复旧 BB MCP 直连 provider 路径；`bb_crypto_analysis_evidence` 必须独立于 `openbb_provider_http_evidence`/`openbb_provider_raw_evidence`，当前 BTC canonical final 只能作为迁移断链证据，不能作为 BB 接入完成证据。
待办：按方案拆 T-BB-1 起的实现任务，先盘点 BB 代码中的出网取数路径并定义 OpenBB normalized crypto bundle 合同。

## 10:42 [codex-manager]
-完成：补充 `docs/BB接入方案.md` 的配置迁移规则：Win11 BB 项目已跑通的 provider key 可迁移到本项目 `.env.local`/secret store，但读取方必须是 OpenBB/data_gateway adapter，BB analysis engine 不得读取 key 或出网取数。
-决策：只比较变量名不记录密钥值；当前本项目 `.env.local`/`.env.example` 已包含 BB `.env.example` 的主要 CRYPTO provider key 名称，因此未复制任何真实密钥值。
待办：实现时补 credential status evidence，证明 provider key 只被 OpenBB/data_gateway 使用且不会进入 worker material、docs、memory 或 evidence 明文。

## 10:45 [codex-manager]
-完成：按人类纠正更新 `docs/BB接入方案.md`：BB 代码目标态必须移植进 claw-trade 仓库，成为本项目内部 CRYPTO 分析模块；目标 report runtime 不依赖 Win11 BB 项目目录、`/mnt/d/src/BB`、`D:\src\BB` 或外部 BB MCP。
-决策：Win11 BB 只作为一次性迁移来源、配置参考和历史对照；保留“OpenBB 取数 + 本项目 BB analysis engine 分析 + worker 只看 `claw_get_market_pack` 自然语言资料包”的边界。
待办：实现时先做 T-BB-1 代码盘点，列出可迁入纯分析模块和必须剔除的 provider fetch/key/cache/rate-limit 逻辑。

## 10:47 [codex-manager]
-完成：准备将当前 OpenBB 前线数据迁移、canonical T16 证据、BB 接入方案和相关测试/文档改动提交并推送到 GitHub。
-决策：本次按人类“提交git，并同步到github”的指令提交当前工作区完整改动；不额外创建 PR。
待办：提交前运行基础 Git 校验，提交后推送 `main` 到 `origin`。

## 10:51 [codex-manager]
-完成：已将 OpenBB data gateway/T16 evidence 工作区改动提交为 `09064c6` 并推送到 GitHub `origin/main`。
-决策：原始 `docs/evidence/**` 中的 trailing whitespace 保留为运行证据文本；提交前只对非证据文件运行空白检查并修复源代码/测试 EOF 空行。
待办：`third_party/openbb` 子模块内部仍有 OpenBB 自动生成文件的本地 dirty 状态，未擅自清理。

## 11:23 [codex-manager]
-完成：只读检查 US/HK/CRYPTO `/report` final prompt system context 路径，确认当前代码和本地运行配置下与 CN_A 一样不注入 OpenClaw 默认 system prompt。
-决策：结论限定为代码与现有聚焦测试证据；未启动 fresh/provider live run，因此不把本次检查当作真实 provider payload 证明。
待办：若要做最终运行证明，按 live runtime preflight gate 跑 fresh run 并检查新的 provider_request payload。

## 11:23 [codex-manager]
-完成：按架构评审意见将旧 BB 接入口径统一改名为 CryptoLens，并同步主设计文档、OpenBB 迁移合同、CRYPTO 数据源方案、总体架构、`market_analyst` CRYPTO prompt/skill；第四轮只读 sub-agent 复审结论为“符合、无 blocker”。
-决策：CryptoLens 定位为迁入 claw-trade 的 CRYPTO 离线指标分析引擎，不是数据源/provider/外部 MCP/worker/PM/trader；worker-visible 工具统一为 canonical `claw_get_*_pack`，历史工具名和旧 BB MCP 只保留为迁移盘点、历史对照、禁止项或 import-block 证据。
待办：实现阶段按 CryptoLens 方案先盘点旧 BB 纯分析代码，剔除 provider fetch/key/cache/rate-limit 逻辑后再接入 OpenBB normalized crypto bundle。

## 11:31 [codex-manager]
-完成：新增 `docs/CryptoLens实施任务清单.md`，把 CryptoLens 接入方案拆成 T-CL-1 至 T-CL-12，覆盖任务图、覆盖矩阵、禁止路径/import-block、验收门和停止条件。
-决策：不新增未批准架构；把设计文档里的 T-CL-0 口径冻结折叠为 Coverage Contract 与 T-CL-1 基线任务，明确旧 BB MCP、旧 provider executor、OpenBB atomic/admin/discovery tools 只能作为禁止项和验证对象。
待办：实施时先跑 T-CL-1/T-CL-2/T-CL-3，完成旧 BB 只读盘点和 import-block 基线后再迁入纯分析代码。

## 11:51 [codex-worker]
-完成：按评审意见修订 CryptoLens 文档实施计划，补齐 provider 许可/费用/raw export 记录、RunProviderPlan 边界断言、CryptoLens 输入污染与输出全字段合同、worker skill 审计、OpenViking relation 口径、collect-first 与 live/fresh 必收证据字段，并同步更新接入方案 §16 的人类已批准状态。
-决策：仅改 `docs/CryptoLens实施任务清单.md` 与 `docs/CryptoLens接入方案.md` 的局部条目，不扩大设计范围；保留旧 BB 迁移审计与只读盘点要求，同时把“禁用旧 BB 运行路径”与“OpenBB 缺口真实表达”拆分为独立验收口径。
待办：后续实现阶段按新清单落测试与证据，重点先关闭 T-CL-2/T-CL-3/T-CL-5/T-CL-7/T-CL-10 的新增合同项。

## 11:55 [codex-manager]
-完成：根据人类 4/5/6 批准口径完成 CryptoLens 计划文档二次修订与复审；修正残留 TypeScript 选项为 Python 重写口径，并由只读 reviewer 复审为“符合、无剩余 blocker、可进入实施”。
-决策：本轮只关闭文档计划覆盖和人类决策状态，不声称代码实现、provider 接入或 live/fresh 运行已完成。
待办：下一步进入实施时先按 T-CL-1/T-CL-2/T-CL-3 做口径基线、旧 BB 只读盘点和 import-block，再做 OpenBB provider 与 CryptoLens Python 重写。

## 12:06 [codex-worker]
-完成：完成 T-CL-1 口径冻结与当前偏差基线取证，新增 `docs/evidence/cryptolens-tcl1-baseline-20260518.md`；确认当前 canonical BTC 仅证明数据缺口诚实披露，不构成 CryptoLens 接入完成证据。
-决策：保持“CryptoLens 是内部离线分析引擎、OpenBB 是唯一外部取数入口、后续不得恢复旧 BB 直连”作为后续任务硬边界；本次不改任何运行代码。
待办：进入 T-CL-2/T-CL-3 时先做旧 BB 只读盘点与 import-block 基线，防止迁移中出现旧路径回流。

## 12:09 [codex-worker]
-完成：完成 T-CL-2 旧 BB 只读盘点并新增 `docs/evidence/cryptolens-tcl2-old-bb-audit-20260518.md`，给出可迁入纯分析模块清单、禁止运行路径清单、`src/claw_trade/data_gateway/analysis/crypto_lens/**` 目标映射，以及旧 BB `.env.example` 与本仓库 `.env.example` 的脱敏差异决策。
-决策：将 `/mnt/d/src/BB/mcp/crypto-data-mcp/src/domains/live.ts` 及其 provider fetch/key/cache/rate-limit/fallback/MCP runtime 依赖整体判定为“不得进入 report runtime”；只允许函数级拆分迁入纯计算逻辑。
待办：T-CL-6 执行前先按本清单把 `live.ts` 中纯计算与取数路径彻底解耦，避免整文件复制带入禁止逻辑。

## 12:08 [codex-worker]
-完成：完成 T-CL-4，新增 OpenBB normalized crypto bundle 合同（`NormalizedCryptoMarketBundle` / `CryptoDomainBundle` / `DomainReadiness` / `ProviderSourceRef`）与 focused 合同测试，覆盖完整 bundle、缺 derivatives、缺 liquidation_map、OHLCV 样本不足、cache stale、provider conflict、raw refs 不进入 worker material、输入污染字段拒绝。
-决策：合同仅落在 `models.py` 数据层并显式拒绝 raw/debug/protocol/prompt 污染字段；`raw_refs` 仅保留为证据引用，不进入 `to_worker_material_contract()` 输出。
待办：后续 T-CL-5/T-CL-7 实现时对齐该合同接入真实 OpenBB normalized 数据与 CryptoLens analysis adapter。

## 12:09 [codex-worker]
-完成：完成 T-CL-3 禁止路径与 import-block 基线：扩展旧模块/旧别名/`bb_crypto_data` 负面断言，补齐 report worker payload 扫描禁止项，并把 `start-control-runtime` 改为默认不注入旧 BB MCP（仅在显式提供 `BB_MCP_SERVER_PATH` 与 `BB_MCP_CWD` 时才注入）。
-决策：不新增 provider/CryptoLens engine/MarketPackBuilder 实现，不改 T-CL-4/5/6 代码面，仅在允许范围内做最小测试与启动配置修正。
待办：等待总管串联后续 T-CL-5/T-CL-6/T-CL-7 实施与 live/fresh 验收。

## 12:13 [codex-worker]
-完成：按只读复审补齐 T-CL-3 “Python 出网边界阻断”静态合同测试：新增对 `src/claw_trade/workflow/**`、`src/claw_trade/cli/**`、`src/claw_trade/reports/**` 的全量扫描，允许 `providers.registry/run_plan/defaults` 与 plan/store 控制面导入，阻断 provider fetch/adapter 实现模块与旧 `claw_trade.providers`/`provider_executor`/`frontline_data_pack` 路径引用。
-决策：本轮仅增测试，不改业务实现；扫描结果未发现禁止直连，因此不触发 BLOCKED。
待办：后续若 provider fetch 边界变更，需先经人类批准再调整该合同测试。

## 12:14 [codex-manager]
-完成：建立 `docs/evidence/cryptolens-orchestration-20260518.md` 总管任务总表并完成第一轮 subagent 协调：T-CL-1、T-CL-3、T-CL-4 已有本地复核证据，T-CL-2 旧 BB 盘点完成但旧本地 `.env` 增量缺口未闭合。
-决策：按只读复审把 OpenBB 开工冻结门前移为第一轮硬门；现有 `docs/evidence/openbb-submodule-version.md` 可作为 submodule/version/license/extension 证据，但 provider-specific license/raw export、OpenViking relation kind、CryptoLens evidence collection 与旧 BB 本地 `.env` 增量仍需人类口径。
待办：等待人类确认 T-CL-2 `.env` 缺口闭合方式及 T-CL-5/T-CL-7/T-CL-10 决策门后，再继续分派后续实现。

## 12:22 [codex-manager]
-完成：记录人类最新批准口径到总管清单：`.env.local` 旧 BB 设置可作为脱敏变量名盘点来源并可清理；旧 BB runtime/MCP 要彻底删除；CryptoLens evidence 名称固定为 `crypto_lens_analysis_evidence`；OpenViking 复用现有证据链关系；raw export 按 A 股口径默认 `metadata_only`。
-决策：后续 T-CL-5 provider 接入可继续，但每个 provider 仍要记录 license/cost/raw export；若具体 provider 许可或费用不清楚，写真实缺口或 `license_blocked`，不得伪装成功。
待办：分派 T-CL-2/T-CL-3 follow-up 关闭旧 BB 配置/runtime，再分派 T-CL-5/T-CL-6/T-CL-7 实现。

## 12:34 [codex-worker]
-完成：完成 T-CL-5 第一阶段最小实现：CRYPTO market provider plan 从 yfinance-only 扩展为 7 个域 call specs（market/OHLCV、derivatives、liquidation_map、onchain、macro、events、ahr999），并在 call spec 合同中补齐 `coverage_group/quorum`、`license_policy_id`、`provider_config_version` 与 `raw_export_policy=metadata_only`；新增 phase1 placeholder 显式缺口路径（credential_missing/remote_error/skipped_not_configured），保留现有 yfinance OHLCV 成功路径。
-决策：对许可/费用不清楚的 provider 使用 `*.pending_review` license policy id 并在证据文档记为真实缺口，不伪造 live 成功；本轮不实现 CryptoLens engine/adapter/evidence。
待办：下一阶段把 phase1 placeholder 分域替换为真实 OpenBB/data_gateway fetch，并补 provider-specific license/cost 批准闭环证据。

## 12:31 [codex-worker]
-完成：完成 T-CL-2 follow-up + T-CL-3 follow-up：`start-control-runtime` 删除旧 `bb_crypto_data` 注入分支与 `BB_MCP_SERVER_PATH`/`BB_MCP_CWD` 读取，`.env.local` 清理旧 BB runtime/MCP 变量，补充 `.env.local` 变量名脱敏盘点证据，并通过指定回归测试 `55 passed`。
-决策：仅在允许范围内做最小改动；保留 OpenBB provider key 变量，不记录任何 secret 值；不实现 OpenBB crypto provider、不实现 CryptoLens engine。
待办：进入 T-CL-5/T-CL-6/T-CL-7 时继续沿用“OpenBB 唯一外部取数 + CryptoLens 离线分析”边界闭环。

## 12:31 [codex-subagent]
-完成：完成 T-CL-6 第一阶段 Python 骨架：新增 `src/claw_trade/data_gateway/analysis/crypto_lens/**`（input/output/engine），实现离线 deterministic 分析输出（market_structure、technical_patterns、derivatives_context、liquidation_context、onchain_context、macro_context、ahr999_context）并通过 `tests/unit/data_gateway/test_crypto_lens_*.py` 全量回归。
-决策：第一阶段仅实现纯分析模块与 no-network/no-key/no-Mongo/no-OpenBB import-block 测试，不实现 T-CL-7 evidence store、不接 MarketPackBuilder、不引入旧 BB runtime 或 Win11 构建依赖。
待办：下一阶段按 T-CL-7 接入 `analyze_openbb_crypto_lens_bundle` 与 `crypto_lens_analysis_evidence`，并保持当前 input/output 合同不扩权。

## 14:13 [codex-manager]
-完成：记录人类最新决策：CRYPTO 六个缺口域不能以 phase1 占位作为完成态，目标是把旧 BB provider 取数逻辑迁入 OpenBB/data_gateway provider 合同；工具 `ok:false` 且 exit 0 的严重失败语义需改成真正失败。
-决策：迁移只允许复用旧 BB 的接口参数、字段映射和错误语义；不得恢复旧 BB MCP/runtime/cache/fallback/Win11 路径。raw export 人话口径为“原始接口大包只留证据仓库可追踪，默认不进报告、不暴露给 worker、不导出全文”。
待办：等待 provider worker 和工具失败语义 worker 返回，随后做设计复审和 focused tests。

## 14:15 [codex-worker]
-完成：完成 frontline 工具失败语义修正：`ok:false` 即使子进程 `exit 0` 也标记为 `isError=true`；新增 focused 合同测试覆盖 `pack_runtime_blocked` 失败路径，并把“readiness 不足但调用成功”样例改为 `ok:true` 语义；同步补充编排证据文档记录本次决策与测试结果。
-决策：写作用域仅限 `openclaw_plugins/claw-trade-frontline-tools/index.js`、`tests/contracts/test_frontline_tool_protocol.py`、`docs/evidence/cryptolens-orchestration-20260518.md`、`memory/2026-05-18.md`；不改 provider adapter，不改 CryptoLens engine，不新增 gate/fallback。验证命令与结果：`uv run pytest tests/contracts/test_frontline_tool_protocol.py`（exit 0，24 passed）；`uv run pytest tests/contracts/test_frontline_tool_contract.py tests/unit/data_gateway/test_tool_schema.py`（exit 0，10 passed）。关键输出：`ok:false + error` 现在返回 `isError=true`，自然语言错误信息保留在 model-facing 文本中，未泄露 raw/debug/protocol 细节。
待办：后续可在真实 `/report` provider payload 里抽样核验上层失败分类是否已按 `isError=true` 正确计入。

## 14:24 [codex-worker]
-完成：完成 T-CL-5 六域 provider 接入：`derivatives/liquidation_map/onchain/macro/events/ahr999` 从 phase1 placeholder 切换到真实 fetch + 归一化；缺凭证保持 `credential_missing` 且不发请求；`raw_export_policy` 维持 `metadata_only`；指定测试命令通过（22 passed）。
-决策：沿用既有 call spec 与 provider 边界，不恢复旧 BB MCP/runtime/cache/fallback；`ahr999` 对非 BTC 显式阻断为 not applicable runtime error。
待办：继续补齐 provider 许可/费用审批闭环与 live/fresh collect-first 运行证据。

## 14:32 [codex-worker]
-完成：完成 T-CL-5 复核修正边界：1) `src/claw_trade/data_gateway/providers/http_capture.py` 新增 captured URL query 脱敏（`api_key/apikey/key/token/access_token/secret/authorization/auth`）；2) `src/claw_trade/data_gateway/providers/market_adapters.py` 将 Coinglass/Glassnode/FRED/Tavily 读取切到 adapter 注入 env（含 `COINGLASS_API_BASE`、`COINGLASS_API_HEADER_NAME`）；3) 补 focused tests（`tests/unit/data_gateway/test_provider_execution.py`、`tests/unit/data_gateway/test_market_adapters_live_contract.py`）；4) 更新 `docs/evidence/cryptolens-tcl5-provider-plan-20260518.md` 测试结果与边界说明。执行命令：`uv run pytest tests/unit/data_gateway/test_provider_execution.py tests/unit/data_gateway/test_market_adapters_live_contract.py tests/integration/data_gateway/test_market_pack_crypto.py tests/unit/data_gateway/test_run_provider_plan.py`，exit code 0，关键输出 `29 passed`。
-决策：不新增 fallback，不改 CryptoLens engine，不改工具失败语义；缺 key 仍显式 missing/credential_missing，并通过断言证明缺 key 时不触发 HTTP 请求。
待办：provider 许可/费用闭环仍是 `*.pending_review`（仍有 provider 许可缺口）；secret 明文 query 泄漏缺口已闭合；raw export 仍按 `metadata_only`，后续进入 live/fresh collect-first 验收。

## 14:43 [codex-worker]
-完成：按 T-CL-5 blocker 修正只改最小范围：`market_adapters` 新增安全错误脱敏并替换 `_http_get_json/_http_post_json` 异常摘要；CRYPTO 非 BTC 在 `build_call_specs` 跳过 `ahr999_index`（不再走 `REMOTE_ERROR/provider_unavailable`）；补 focused tests 覆盖“异常 URL 含 secret 脱敏”与“非 BTC AHR999 跳过”；清理集成测试过时 `phase1` 文案；更新证据文档。修改文件：`src/claw_trade/data_gateway/providers/market_adapters.py`、`tests/unit/data_gateway/test_market_adapters_live_contract.py`、`tests/unit/data_gateway/test_provider_execution.py`、`tests/integration/data_gateway/test_market_pack_crypto.py`、`docs/evidence/cryptolens-tcl5-provider-plan-20260518.md`、`memory/2026-05-18.md`。执行命令：`uv run pytest tests/unit/data_gateway/test_market_adapters_live_contract.py tests/unit/data_gateway/test_provider_execution.py tests/integration/data_gateway/test_market_pack_crypto.py tests/unit/data_gateway/test_run_provider_plan.py`，exit code 0，关键输出 `31 passed in 1.74s`。
-决策：不新增 guard/gate，不恢复旧 BB，不改 OpenClaw 插件，不改 CryptoLens engine；AHR999 非 BTC 采用“plan 阶段跳过”最小合同语义实现不适用。
待办：当前这两个 blocker 均已关闭；剩余非本任务 blocker 未处理。

## 14:48 [codex-worker]
-完成：完成 T-CL-5 窄修复（密钥脱敏边角）：扩展 provider/http evidence 与 provider error message 的 URL/query 脱敏变体，覆盖 `accessToken`、`x-api-key`、`api-key`，并新增 URL userinfo（`user:pass@host`）脱敏；补 focused tests 证明 `http_capture` 写入 `source_url` 与 `market_adapters` 抛出的错误文本都不会泄漏这些 secret 形式。修改文件：`src/claw_trade/data_gateway/providers/http_capture.py`、`src/claw_trade/data_gateway/providers/market_adapters.py`、`tests/unit/data_gateway/test_provider_execution.py`、`tests/unit/data_gateway/test_market_adapters_live_contract.py`、`memory/2026-05-18.md`。执行命令：`uv run pytest tests/unit/data_gateway/test_market_adapters_live_contract.py tests/unit/data_gateway/test_provider_execution.py tests/integration/data_gateway/test_market_pack_crypto.py tests/unit/data_gateway/test_run_provider_plan.py`，exit code 0，关键输出 `31 passed in 2.57s`。
-决策：本轮仅做脱敏覆盖，不更改 provider 许可/费用或 raw export 策略，不新增 fallback/guard/mock。
待办：本任务范围内 blocker 已闭合；其余任务保持原计划推进。

## 14:49 [codex-worker]
-完成：完成脱敏键归一化收口（`http_capture` 内部敏感键集合只保留规范化值）并复跑同一回归命令确认无回归：`31 passed in 2.38s`（exit code 0）。
-决策：不新增功能、不扩 scope，仅做实现一致性修正。

## 14:57 [codex-manager]
-完成：总管复核 T-CL-5/工具失败语义/旧 BB import-block 当前状态，复跑组合回归 `146 passed in 14.49s`，并更新 `cryptolens-orchestration` 与 `cryptolens-tcl5-provider-plan` 证据文档。
-决策：六域 provider 代码合同可视为本地通过，但 `coinglass/glassnode/fred/tavily` 许可/费用与 live/fresh 证据未闭合，因此 T-CL-5 总状态仍按 blocked 记录，不声明 CryptoLens 接入完成。
待办：等待人类确认 provider 许可/费用口径后，再按 fixed runtime preflight gate 进入 live/fresh 取证。

## 15:00 [codex-manager]
-完成：记录人类回复“可以，继续”为 Coinglass/Glassnode/FRED/Tavily 可继续进入验收推进，并将 T-CL-5 本地代码/合同状态从 blocked 调整为 completed。
-决策：该批准只解除许可/费用推进阻塞；raw export 仍按 `metadata_only`，live/fresh 仍必须后续真实取证，不能提前宣称完成。
待办：分派 T-CL-7 CryptoLens adapter/evidence，随后接 T-CL-8 market pack 和 T-CL-10 evidence chain。

## 15:06 [codex-worker]
-完成：完成 T-CL-7 CryptoLens adapter/input/output/evidence：新增唯一入口 `analyze_openbb_crypto_lens_bundle`，结果合同补齐 engine 元数据、readiness、coverage、input/output hash、normalized refs、data gaps/conflicts；新增外部注入 collection 的 `crypto_lens_analysis_evidence` 写入能力，并注册 Mongo collection 常量/index。
-决策：CryptoLens 分析逻辑保持离线，只读 normalized bundle；analysis module 不创建 Mongo 连接、不读 provider key、不出网、不调用 OpenBB；证据 collection 独立命名，不伪装为 OpenBB HTTP/raw provider evidence；结果不输出 BUY/HOLD/SELL、仓位、执行建议、PM rating 或最终投资裁决。
待办：T-CL-8 接入 MarketPackBuilder、T-CL-10 继续闭合 evidence chain/OpenViking lineage。

## 15:19 [codex-worker]
-完成：完成 T-CL-8 + T-CL-10 本地合同层并补 T-CL-11 关键测试：CRYPTO market pack 通过 OpenBB normalized bundle 调用 CryptoLens，worker brief 只渲染中文 summary/status/notes/gaps；pack audit 记录 `analysis_evidence_refs`；OpenViking 复用 `worker_l1_to_l2_evidence` 链接 `crypto_lens_analysis_evidence`；Mongo lineage export 后可重建 analysis evidence ref；指定测试集 `53 passed`。
-决策：不新增 CryptoLens 专用 relation kind，不把 analysis evidence 挂到 `pack_audit_to_provider_attempt`；无 Mongo 时允许分析运行但 evidence ref 为空；CryptoLens 失败时保留 OpenBB attempts/refs 并写真实 gap/readiness 降级，不伪造指标分析。
待办：后续 T-CL-9/T-CL-12 仍需 provider payload/tool schema 与 fixed runtime live/fresh 证据；本轮未跑 live/fresh。

## 15:29 [codex-worker]
-完成：只处理只读复审 3 个 blocker：CryptoLens engine 异常会写 `crypto_lens_analysis_evidence` 失败态并把 ref 接入 pack audit；adapter missing / credential missing 在有 evidence helper 时只持久化 provider attempt，不写 raw/http 伪证据；包级 API 移除 `analyze_crypto_lens` 公开入口；reader brief 旧称 `BB/CoinGlass` 改为 CryptoLens/OpenBB 口径。指定测试集 `57 passed in 2.14s`。
-决策：失败 evidence 使用 `status=failure`、`failure_reason`、`input_hash`、`referenced_normalized_refs`、no-network assertion 和 `failure_result_ref`，不构造成功 result；非远端 provider 失败不触发 HTTP、不写 raw/http success。
待办：未跑 live/fresh；后续 T-CL-9/T-CL-12 仍需独立验收。

## 15:36 [manager]
-完成：复跑 CryptoLens/OpenBB 本地闭环测试并完成只读复审，T-CL-7/T-CL-8/T-CL-10/T-CL-11 代码/合同层 PASS；focused `57 passed`、宽回归 `183 passed`、T-CL-9 本地 schema 合同 `49 passed`。
-决策：不把本地合同通过说成 live/fresh 完成；T-CL-9 仍等真实 OpenClaw provider payload/visible tool schema，T-CL-12 仍等 BTC fresh/live 全链证据。
待办：按 AGENTS fixed runtime preflight 进入 live/fresh gate 准备，先读取 memory runtime guidance 并输出 runtime profile/preflight 表。

## 15:59 [manager]
-完成：按 fixed runtime preflight 执行 BTC CRYPTO `/report` fresh/live，run `run-20260518-194156-ea87cf5f` completed；证据目录 `docs/evidence/cryptolens-btc-live-20260518T193938Z/` 含 preflight、13 worker provider payload/report、OpenBB audit passed、CryptoLens analysis evidence 正文、provider visible tools、OpenViking relations/ovpack、final report 与 validation passed。
-决策：live wrapper exit code `1` 仅来自 post-run summary `tools=null` 收口 bug，report/audit/OpenViking evidence 已先落盘；补齐自包含证据后 evidence-validation exit `0` 且只读复审 PASS。OpenViking semantic/vector queue 仍按 blocked 如实记录，不声明 semantic runtime 全绿。
待办：后续非阻塞修复 post-run summary `tools=null`，并考虑把 evidence export 的 status 命名从 `success` 调整为更精确的 `analysis_execution_success`。

## 16:18 [manager]
-完成：修复 Coinglass 覆盖缺口：CRYPTO 链上 provider 改为 Coinglass 进 OpenBB/data_gateway；删除未使用 Glassnode 链上 runtime helper；扩展 Coinglass funding/OI/多空比/CVD/链上字段映射，并让 CryptoLens brief 输出 CVD、MVRV、SOPR/NUPL、活跃地址和大额转账上下文。
-决策：不恢复旧 BB runtime/MCP/cache/free fallback；旧 BB 只作为只读 endpoint/字段映射证据；所有外部取数仍只走 OpenBB/data_gateway provider adapter。
待办：重跑 BTC live/fresh 前必须按 fixed runtime preflight gate 重新取证，不能用本地测试替代 live 证据。

## 17:40 [codex-subagent]
-完成：完成本轮 Coinglass 只读复核（含指定 6 文件与 3 份设计文档相关边界），并执行 `uv run pytest tests/unit/data_gateway/test_market_adapters_live_contract.py tests/integration/data_gateway/test_market_pack_crypto.py`（22 passed）；结论为“部分符合”：Coinglass→OpenBB/provider adapter→market pack→CryptoLens 输入链路已打通，未发现旧 BB runtime/MCP/provider executor/crypto_market_data_pack 回流，也未发现 CryptoLens 出网取数；但测试覆盖对 onchain 正向映射与 provider 边界 import-block 仍有缺口。
-决策：本轮不改实现代码，仅记录复核证据；把缺口留给后续补充测试处理。
待办：补充 focused 覆盖（onchain 正向进入 market pack 与 CryptoLens 文本断言、旧路径 import-block/worker schema 边界断言、失败分支 collect-first 证据断言）后再做下一轮复核。

## 17:51 [codex-manager]
-完成：只读梳理 `/home/frank/src/claw-invest` 的 Research UI 模块，确认其由 `web/research-ui` React/Vite 前端与 `src/claw_invest/web` FastAPI 展示层组成，正式 `/report` 入口仍经 app gateway 和 canonical workflow queue。
-决策：本轮仅做代码阅读与结构理解，不修改 invest 实现、不启动 live runtime、不运行测试。
待办：如需迁移到 `claw-trade`，下一步先对齐 Research UI 的读模型、`/report` 入口语义、设置页范围与 `start-control-runtime.sh` 固定 runtime 约束。

## 18:18 [manager]
-完成：完成 Coinglass 修复闭环：链上/衍生品 provider 走 OpenBB/data_gateway Coinglass，真实字段映射覆盖 funding、OI、多空比、CVD、活跃地址、STH/LTH SOPR、NUPL、大额转账；移除 `.env.local` 残留空 `GLASSNODE_API_KEY` 与旧 BB 注释；最终 BTC fresh run `run-20260518-221025-da4b29a7` completed，final report 位于 `docs/evidence/cryptolens-btc-coinglass-fix4-20260518T220957Z/exported/final_report.md`，OpenBB audit 和 OpenViking collect 均 exit 0。
-决策：链上同源 Coinglass 子接口按 collect-first 收集，部分成功时标为“部分覆盖”并写缺口，不用 fallback、不伪造成功；Glassnode/旧 BB runtime 未恢复。
待办：仍需明确 Coinglass 资金费率/OI/CVD 单位口径，链上交易所净流量与稳定币净流量仍为非阻断缺口。

## 18:38 [manager]
-完成：修复 CryptoLens 技术形态覆盖缺口：按旧 BB 纯分析口径在 Python engine 内补维加斯通道、双线反转、FVG、KD(9,3,3)、TD Sequential；本地 focused `10 passed`、CryptoLens/pack/import-block 回归 `39 passed`、market adapter+pack+engine 回归 `29 passed`；fixed runtime live/fresh BTC run `run-20260518-223145-ca0e68d3` completed，final report 位于 `docs/evidence/cryptolens-btc-technical-fix-20260518T223046Z/exported/final_report.md`，status 为 REPORT/AUDIT/OV 全 0。
-决策：旧 BB 只作只读算法对照，不恢复旧 BB runtime/MCP；TD 输出改为 `bullish_reversal`/`bearish_reversal` 指标语义，避免 CryptoLens 输出 BUY/HOLD/SELL 决策词；维加斯紫带因本次 366 根日线不足 676 根，报告如实标为样本限制，蓝带仍可用。
待办：若要完整维加斯紫带，需要扩大 CRYPTO 技术 K 线历史窗口到至少 676 根并重新 live/fresh 取证；链上交易所净流量、稳定币净流量与 Coinglass 单位口径仍需后续处理。

## 19:06 [manager]
-完成：修复本轮报告缺陷入口：Polymarket 改用零 key public-search 并同时接入 news/social 事件预期资料包；CRYPTO 下游提示词明确“数据缺口不是看涨或看跌事实”；终稿提示词要求指标推导表格化且不得原样粘贴散文指标块；run provider plan 同步扩大 CRYPTO 日线 lookback，避免维加斯紫带在 live 计划里被裁回请求窗口。
-决策：本轮按用户要求只跑 focused 编译/单测/合同测试，不启动整轮报告链路或 live/fresh 验收；Polymarket live 真实返回和新 final report 仍待后续按 fixed runtime gate 取证。
待办：需要重生成 BTC final report 时，先按 runtime preflight gate 启动 fresh run，再检查 Polymarket 样本、指标表格、维加斯紫带行数和“信息空白”措辞是否真实进入最终报告。

## 19:10 [manager]
-完成：修复 CRYPTO 终稿提示词的结尾闭环要求，明确终稿必须完整写到第八节，不能停在中间章节、半句或列表项；补充合约测试覆盖该要求。
-决策：旧 technical-fix final_report 已确认在第五节中途截断，本次只修生成规则和焦点测试，未重跑整轮报告。
待办：用户批准后重跑 BTC CryptoLens live/fresh，确认新 final_report 是否完整生成第六、七、八节并以完整自然段收尾。

## 19:29 [manager]
-完成：按 fixed runtime preflight 重跑 BTC CryptoLens fullcheck，run `run-20260518-231610-19dba142` 完成且 REPORT/AUDIT/OV 均为 0；完成主线程与两个只读 subagent 全面检查。
-决策：本轮验收结论为“不通过”：final_report 仍在第五节半句截断，根因是 report_polisher provider 输出上限 `max_completion_tokens=8192` 且终稿 prompt/上游材料过长；同时发现读者报告暴露 `OpenBB/CryptoLens/tavily` 等内部名、图表状态自相矛盾、CryptoLens analysis evidence 未随证据包导出可复验正文/哈希、OpenViking runtime health 为 blocked。
待办：需要先决定修复路线：压缩/预算化 report_polisher 输入输出，或调整模型/多段终稿生成；修完后再重跑 live/fresh，不能把本轮报告当完成态。

## 19:24 [codex-subagent]
-完成：只读复核 `docs/evidence/cryptolens-btc-fullcheck-20260518T231541Z/exported/`；确认终稿仍在第五章半句截断，未到第八章最终结论；Polymarket 已进入 social/news/final；指标表格已覆盖 FVG/KD/TD/清算/衍生品/链上部分材料，但维加斯紫带和双线反转展开不足，且终稿有英文状态字段与内部来源名泄漏。
-决策：不修改 evidence 文件；按内容质量复核记录问题，mock/stub/fake/fallback 未在导出报告正文中发现，但本轮未做 live runtime 或 provider evidence 复验。
待办：重跑或修复终稿生成后，优先检查第八章完整收尾、维加斯紫带/双线反转具体推导、内部字段清洗，以及 Coinglass/链上缺口的一致表述。

## 19:27 [codex-readonly-review-subagent]
-完成：只读复核 BTC CryptoLens fullcheck 证据链；fixed runtime preflight、13 个 provider payload/visible-tools/tool-calls、OpenBB audit、OpenViking lineage/ovpack 基本证据齐备，未发现旧 BB runtime/MCP/provider executor/crypto_market_data_pack 或 worker 可见 OpenBB 原子/admin/debug 工具回流。
-决策：证据链结论为部分通过；CryptoLens analysis 只有 Mongo URI 关系且 evidence_hash 为 null，证据包内未导出可复验的 analysis evidence 正文；OpenViking runtime_health 为 blocked，原因除 semantic/vector 已知禁用外还包含 metrics endpoint unavailable 和 recovery API 未确认，不能表述为完全健康。
待办：补充 CryptoLens analysis evidence 可复验导出/哈希，明确 OpenViking metrics/recovery unavailable 的验收口径，必要时重跑 fullcheck 后复核终稿截断与内部字段泄漏问题。

## 19:57 [codex]
-完成：新增 docs/UI需求分析.md，整理已确认的 UI 布局、任务、通知、设置、数据源和 PDF 导出需求。
-决策：文档只记录产品需求和边界，不进入实现；真实密钥不写入文档。
待办：等待人工 review UI 需求文档，确认后再进入实现计划。

## 20:00 [codex-subagent]
-完成：完成任务 B：final report exporter 对多个 approved report_polisher L1 按 material/turn 顺序机械拼接，并补 focused contract test 覆盖三段终稿到第八节。
-决策：导出器只拼接已批准 L1 原文，不总结、不写投资内容、不改 PM 结论；claim mapping 继续从全部 ordered materials 生成，worker appendix 继续排除 report_polisher。
待办：无。

## 20:01 [codex-subagent]
-完成：完成任务 C：report_polisher 四个 profile 增加 `{final_report_section_instruction}` 分段写作规则，USER.md 增加可用变量说明，focused prompt contract 覆盖四个 profile 与 CRYPTO 关键要求；`uv run pytest tests/contracts/test_worker_prompt_alignment_policy.py -q` 通过。
-决策：只把分段指令作为写作范围，不写入读者正文；CRYPTO 空范围仍要求完整八节和自然结尾，给定范围时只写指定章节，避免每段都强制完整八节。
待办：runner/exporter 由后续 worker 接入实际分段调用。

## 20:02 [codex-subagent]
-完成：完成任务 A：final_report 阶段将 report_polisher 串行拆成 turn 0/1/2 三次 OpenClaw 唤醒，并为每次调用注入不同 `final_report_section_instruction`；普通非分段 report_polisher 调用保留完整八节语义。
-决策：分段只在 runner 的 final_report batch 调度层生效，Python 只传写作范围说明并继续读取同一批 approved L1，不写投资正文、不改模型、不改 max_tokens、不新增 fallback 或 runtime guard。
待办：无。

## 20:15 [codex-manager]
-完成：完成 final_report 分段生成总管收口：controller 要求 report_polisher turn 0/1/2 全部 approved 才导出；exporter 只按 turn 顺序机械拼接 report_polisher L1，不再把 market_analyst 原文硬插回最终报告主体；相关 focused/file-level tests 通过。
-决策：本轮不启动 live/fresh，不换模型、不改 max_tokens、不改 OpenClaw 源码；真实 provider payload 和新 final_report 仍需后续按 fixed runtime preflight 单独验证。
待办：如需证明真实报告已不截断，需要重新跑 BTC `/report` fresh/live 并检查三段 provider payload、三段 report_polisher L1、合并后的 final_report 与证据链。

## 20:24 [codex]
-完成：根据 `docs/UI需求分析.md` 梳理可生成工业级 UI 详细设计文档的 Prompt，覆盖模块、接口、状态机、函数伪码和验收标准。
-决策：Prompt 要求先读取现有需求与项目边界，再输出设计；避免在未读代码结构时臆造实现文件。
待办：用户确认 Prompt 后，可用它生成 `docs/UI详细设计.md`。

## 20:31 [codex]
-完成：新增 `docs/UI详细设计.md`，覆盖 UI 产品边界、三栏信息架构、视觉规范、核心状态机、数据模型、API 契约、模块设计、函数伪码、错误文案、安全隐私、测试计划、实施切分和未决问题。
-决策：按当前仓库事实将前端技术栈标为未确认，采用 TypeScript 风格合同和 Python 后端可映射模型；OpenClaw 的 LLM/Channel/普通聊天能力只通过桥接调用，不迁入 claw-trade；`.env.local` 只记录配置项名称和分组，不输出真实值。
待办：进入实现前需确认微信 ClawBot Channel ID、文件发送 API、LLM 专用配置 API、数据源实例持久化位置和完成摘要结构化来源。

## 20:40 [codex]
-完成：先单独验证 `report_polish`：复用上次已验哈希的 12 个上游 L1 正文，重新种入新 OpenViking run，仅跑 3 次 `report_polisher`，导出 `docs/evidence/cryptolens-btc-report-polish-replay-20260519T003935Z/exported/final_report.md`；三段 provider 调用均成功，终稿有唯一 H1、八章完整、自然收尾，未再出现“信息空白”“图表资产缺失/未生成独立图表文件”等问题表述。
-决策：先修正分段写作范围、图表说明和导出拼接的标题归一化，再跑单项 live replay；本轮只证明 `report_polish` 单项通过，不把它等同于全链通过。
待办：按用户要求继续跑全链，另行生成 fresh/live 证据后再判断全链是否通过。

## 20:56 [codex]
-完成：在 `report_polish` 单项通过后跑 BTC/CRYPTO 全链 fresh `/report`，run_id=`run-20260519-004450-284a039c`，证据目录 `docs/evidence/cryptolens-btc-fullchain-after-polish-20260519T004421Z/`；`REPORT_STATUS=0`、`AUDIT_STATUS=0`、`OV_STATUS=0`，15 次 worker 调用全部成功，最终报告唯一 H1、八章完整、图片 2 张、三段 report_polisher 证据独立导出。
-决策：发现旧导出脚本会覆盖同名 `report_polisher` 多段证据后，修正为重复 worker 自动带 `t00/t01/t02` 文件名前缀，并用同一真实 run 原始证据重导出；未重跑 LLM，未使用 mock/stub/fake/fallback。
待办：OpenViking runtime health 仍报告 `blocked`，原因为 metrics/recovery unavailable 与 semantic/vector queue 禁用；这是残余运行时健康口径，不影响本次 `/report` 命令、OpenBB audit、OpenViking collect 退出码均为 0 的事实。

## 21:04 [codex]
-完成：把 OpenViking runtime health 后续处理写入 `todolist.md`：拆分 `/report` 链路健康与 OpenViking 增强能力健康，明确 metrics/recovery/semantic 的接通或禁用口径，并要求 UI 不把 enhanced health 的 `blocked` 展示成全局报告失败。
-决策：本轮只登记待办，不改 runtime、不启动服务、不改变 semantic/vector/recovery 策略。
待办：后续若实现该项，先决定是否接 `/metrics`、是否存在稳定 recovery API、是否需要恢复 semantic/vector indexing。

## 21:32 [codex]
-完成：按 fixed runtime preflight 重跑 A/HK/US 三市场 `/report` 回归，证据目录 `docs/evidence/regression-three-markets-after-polish-20260519T010935Z/`；A 股 `run-20260519-011002-2e57d81a`、港股 `run-20260519-011654-d3c77111`、美股 `run-20260519-012347-fda039c0` 均为 `REPORT_STATUS=0`、`AUDIT_STATUS=0`、`OV_STATUS=0`，每个市场 15 次 provider request，三段 `report_polisher` 均 succeeded，未发现 mock/stub/fake/fallback 或禁止 provider payload 泄漏。
-决策：本轮验收结论为“链路通过、报告结构部分不通过”：A 股与港股终稿均缺少 `## 四、`、`## 五、` 二级章节标题，港股另有重复的 `## 腾讯控股（00700.HK）港股投资研究报告` 标题；美股八节标题完整。OpenViking enhanced runtime health 仍为 `blocked`，但 lineage、ovpack export/import 均成功。
待办：修正 `report_polisher` 分段 1 的结构遵循问题，确保 A/HK 也强制生成第四、第五节并清理重复标题；修完后先跑 focused report_polish/section 检查，再决定是否重跑三市场。

## 22:10 [codex]
-完成：按用户批准移除固定 3 段终稿假设，新增按 OpenClaw 模型配置和上游材料大小动态拆分 `report_polisher` 写作范围；新增每段章节结构验收和最终报告八节结构验收，缺章、非首段 H1、重复报告标题会失败，不再导出成成功报告。
-决策：Python 只计算章节范围和验收结构，不写投资正文、不改模型、不改 provider、不新增 mock/stub/fake/fallback；终稿结构 hard fail 的来源为本轮人类明确批准。
待办：用户确认后再跑 report_polish replay 或全链 fresh/live 验证真实 provider 输出；本轮只跑 focused/file-level tests。
