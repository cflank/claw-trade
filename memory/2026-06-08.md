# 2026-06-08

## crypto-history-full-columnar-final

- Context: user required the data layer to use the same columnar mechanism as A股/other markets and to continue unattended until unfinished crypto data-layer tasks are complete unless a human decision is required.
- Final import run:
  - command: `uv run python scripts/crypto/import_crypto_prepackaged_to_mongo.py ... --import-run-id crypto-full-columnar-final-20260608 --columnar-root .runtime/crypto-history-full/normalized-columnar-final --mongo-uri mongodb://127.0.0.1:27017/claw_trade_crypto_history_full_20260607 --output-json .runtime/crypto-history-full/import-result-columnar-final-20260608.json`
  - scope: Binance current active spot + USD-M perpetual symbols, `1d` + `1h`, 2025-06-01 through 2026-06-06.
  - result: command exit 0.
- Verified counts:
  - provider attempts for run: `3872`.
  - raw payload metadata rows for run: `64162`.
  - seed import manifests for run: `3872`.
  - active Parquet manifests: `3872`.
  - physical Parquet files under `.runtime/crypto-history-full/normalized-columnar-final`: `3872`.
  - Mongo `normalized_datasets`: `0`.
  - active Parquet unique rows: `15633535`.
  - parsed CSV rows: `15633546`.
  - duplicate Klines collapsed by `dataset_ref`: `11`.
- Warehouse readback with Mongo normalized fallback disabled:
  - `BTCUSDT`, `ETHUSDT`, `SOLUSDT` spot `daily_bar` each returned `742` records.
  - actual range: `2025-06-01` through `2026-06-06`.
  - `date_range_missing_count=0`; `gaps=[]`.
- Evidence/doc updates:
  - added `docs/evidence/crypto-history-full-columnar-import-20260608.md`.
  - updated `docs/evidence/crypto-history-full-binance-manifest-20260607.md`.
  - updated `docs/加密层历史数据设计.md`.
  - updated `docs/加密层历史数据实施计划.md`.
- Still pending:
  - focused test rerun after doc/code updates.
  - true live `/report BTC` and `/report SOL` provider-payload proof through fixed runtime preflight.

## crypto-live-report-btc-sol-proof

- Context: after the full columnar import, live `/report BTC` first exposed a report-path mapping bug: bare `BTC` with display currency `USD` was resolved as `BTCUSD` instead of the local Binance seed pair `BTCUSDT`.
- Code fix:
  - `src/claw_trade/reports/data_pack_bridge.py` now maps bare CRYPTO assets through the local asset mapping (`BTC` -> `BTCUSDT`, `SOL` -> `SOLUSDT`) even when display currency is USD.
  - CRYPTO market `daily_bar` / `intraday_bar` report packs now filter model-visible rows to spot rows; USD-M futures rows remain warehouse/audit evidence but do not mix into spot market charts.
- Live BTC proof:
  - command used fixed runtime entry `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py ...`.
  - run id: `run-20260608-154751-fd320952`.
  - evidence dir: `docs/evidence/live-crypto-btc-sol-20260608/report-btc-rerun`.
  - data prefetch market rows: daily `371`, hourly `8904`, symbol `BTCUSDT`, universe `binance_spot_all_symbols`, range `2025-06-01` through `2026-06-06`, gaps `0`.
  - final report chart assets: `market-01-BTCUSDT_indicator_panels.png`, `market-02-BTCUSDT_market_structure.png`.
- Live SOL proof:
  - command used fixed runtime entry `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py ...`.
  - run id: `run-20260608-160147-f6bbe938`.
  - evidence dir: `docs/evidence/live-crypto-btc-sol-20260608/report-sol`.
  - data prefetch market rows: daily `371`, hourly `8904`, symbol `SOLUSDT`, universe `binance_spot_all_symbols`, range `2025-06-01` through `2026-06-06`, gaps `0`.
  - final report chart assets: `market-01-SOLUSDT_indicator_panels.png`, `market-02-SOLUSDT_market_structure.png`.
- Negative content scan:
  - `rg -n "BTCUSD([^T]|$)|SOLUSD([^T]|$)|日线行情最早记录|缺少2025年6月至12月|日线行情覆盖2025-12|约6个月" docs/evidence/live-crypto-btc-sol-20260608/report-btc-rerun docs/evidence/live-crypto-btc-sol-20260608/report-sol`
  - result: no matches.
- Evidence/doc updates:
  - added `docs/evidence/live-crypto-btc-sol-20260608/summary.md`.
  - updated `docs/加密层历史数据设计.md`.
  - updated `docs/加密层历史数据实施计划.md`.
  - updated `docs/evidence/crypto-history-full-columnar-import-20260608.md`.
- Still pending:
  - final focused tests after the doc/code updates.
  - repo-wide stale wording scan and doc-code consistency review.

## crypto-doc-code-consistency-review

- Subagent read-only review found three issues:
  - `docs/evidence/crypto-history-full-columnar-import-20260608.md` described unfiltered warehouse readback `742` rows as spot-only proof.
  - docs implied arbitrary Binance active bare-asset mapping was already covered, while code only proves BTC/ETH/SOL static seed mapping plus manifest candidate utilities.
  - live summary did not explicitly explain why the superseded failed `report-btc` evidence directory remains.
- Fixes applied:
  - clarified the 742-row readback as unfiltered spot + USD-M warehouse coverage, and added report-visible spot proof rows: BTC/SOL daily `371`, hourly `8904`.
  - updated design/plan docs to state current `/report` bare-asset proof is BTC/ETH/SOL only; arbitrary active bare-asset runtime mapping still needs discovery manifest + liquidity ranking source.
  - added a superseded failed-run section to `docs/evidence/live-crypto-btc-sol-20260608/summary.md`.
  - changed CRYPTO report market bar filtering so futures rows cannot silently substitute for missing spot rows; missing spot rows now return an explicit `warehouse_missing` gap.
- Verification:
  - `uv run pytest tests/unit/reports/test_data_pack_bridge.py tests/unit/data_gateway/test_a_share_prepackaged_cli.py tests/unit/data_gateway/test_a_share_prepackaged_importer.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_crypto_asset_mapping.py tests/contracts/test_crypto_prepackaged_data_contract.py tests/unit/data_gateway/test_data_service_flow.py tests/unit/data_gateway/test_warehouse.py -q` -> `146 passed in 1.59s`.
  - `git diff --check` -> exit `0`.

## crypto-usdt-only-cleanup-monitoring

- Context: user changed the effective CRYPTO historical package scope to USDT-only and required subagent execution/monitoring without manual status checks.
- Subagent handling:
  - Original execution subagent James did not return a final report after repeated waits and a queued status probe. It was closed while still `running` to avoid silent duplicate deletes/writes.
  - Replacement subagent Gauss was started, then later interrupted only for status after long silence. It reported that the core USDT-only state was already present and stopped without further deletes/writes.
- Current storage state verified by manager:
  - Raw root `data/crypto-history-full/binance/data`.
  - USDT ZIP packages: `32452`.
  - checksum files: `32452`.
  - unique USDT symbols: `596`.
  - non-USDT ZIP packages / 非 USDT ZIP 包: `0`.
  - raw ZIP bytes: `358514812` (`341.9 MiB`); raw directory including checksums/metadata: about `571M`.
  - USDT-only columnar root `data/crypto-history-full/normalized-columnar-usdt-only`: about `735.6 MiB`, `2126` physical Parquet files.
  - Mongo catalog database: `claw_trade_crypto_history_usdt_20260608`.
  - Mongo `normalized_datasets`: `0`; normalized rows are in Parquet, Mongo keeps catalog/evidence/attempt/raw metadata.
  - Mongo stats: `raw_payloads=32452`, `provider_attempts=2079`, `dataset_manifests=4001`, total Mongo DB size about `944.5 MiB`.
  - Current Mongo database list contains only `claw_trade_crypto_history_usdt_20260608` for crypto history; the old mixed crypto catalog DB is not present.
- Evidence/doc state:
  - Added `docs/evidence/crypto-history-usdt-only-cleanup-20260608.md`.
  - Updated `docs/加密层历史数据设计.md` and `docs/加密层历史数据实施计划.md` to state the current effective Phase 1 warehouse is USDT-only and columnar-first.
  - The prior mixed all-quote evidence remains historical/superseded and is not the current effective warehouse.
- Verification:
  - `uv run pytest tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_asset_mapping.py tests/contracts/test_crypto_prepackaged_data_contract.py tests/unit/reports/test_data_pack_bridge.py -q` -> `83 passed in 1.08s`.
  - old deleted-source name scan over `docs src scripts tests memory pyproject.toml uv.lock .gitmodules`, excluding `docs/归档/**`, returned no matches.
  - `git diff --check` on touched crypto docs/evidence/code/test paths and the final cleanup text edits returned exit `0`.
- Remaining caveat:
  - Current USDT-only root has warehouse readback proof and focused tests. The earlier live `/report BTC` and `/report SOL` provider-payload proof was generated before the root switch. If acceptance requires provider-payload proof after changing to the USDT-only root, rerun live `/report` through the fixed runtime preflight.

## crypto-mongo-lineage-compaction

- User pushed back that Mongo was too large and asked to find why the mechanism stored so much there, not merely clean it up.
- Root cause found:
  - seed import manifests stored full row-level `dataset_refs`;
  - `output_refs.dataset_refs` duplicated the same full list;
  - provider attempts stored full row-level `dataset_refs`;
  - Parquet manifests stored full row-level `dataset_refs`;
  - `provider_attempts.dataset_refs` had a large multi-key index.
- Mechanism change:
  - `crypto_prepackaged_importer` now compacts large Mongo lineage lists to count + 20-row sample + SHA256 + dataset-ref prefix.
  - `NormalizedColumnarWarehouse` now compacts large Parquet manifest lineage the same way.
  - `DatasetRepository.find_provider_attempt_refs_by_dataset_ref()` can recover local seed attempt refs from `dataset_ref_prefixes` when full refs are not stored inline.
  - `provider_attempts.dataset_refs` is no longer an ensured index; `provider_attempts.dataset_ref_prefixes` is the indexed compact lookup field.
- Current Mongo migration:
  - dry-run showed provider attempts would shrink from about `904713889` BSON bytes to `6189343`, and seed manifests from about `1664770784` to `11178832`.
  - updated `1033` provider attempts, `955` seed manifests, and `1033` Parquet manifests.
  - dropped `provider_attempts__dataset_refs` index and created `provider_attempts__dataset_ref_prefixes`.
  - Mongo `compact` freed `347348992` bytes from `dataset_manifests` and `123318272` bytes from `provider_attempts`.
- Final current crypto Mongo DB stats for `claw_trade_crypto_history_usdt_20260608`:
  - `dataSize=244602985`.
  - `storageSize=59887616`.
  - `indexSize=2822144`.
  - `totalSize=62709760`.
  - `normalized_datasets=0`.
- Readback after compaction:
  - BTCUSDT/ETHUSDT/SOLUSDT spot daily rows each returned `371`.
  - local seed attempt refs were still recoverable through compact prefix lookup.
- Verification:
  - `uv run pytest tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_asset_mapping.py tests/contracts/test_crypto_prepackaged_data_contract.py tests/unit/reports/test_data_pack_bridge.py tests/unit/data_gateway/test_warehouse.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py -q` -> `125 passed in 1.67s`.

## crypto-raw-packages-moved-out-of-runtime

- User required the CRYPTO raw download packages to live inside the project tree, not under `.runtime`, to avoid cleanup loss.
- Moved raw Binance USDT-only packages from `.runtime/crypto-history-full/binance/data` to `data/crypto-history-full/binance/data`.
- Added `.gitignore` entry `data/crypto-history-full/` so the 571M local data directory is not accidentally committed.
- Regenerated complete path lists under `data/crypto-history-full/`:
  - `data/crypto-history-full/usdt-raw-package-files-20260608.txt`: 64904 files, including ZIP and CHECKSUM.
  - `data/crypto-history-full/usdt-raw-zip-files-20260608.txt`: 32452 ZIP files.
- Updated Mongo `claw_trade_crypto_history_usdt_20260608.raw_payloads` path metadata: 32452 `object_uri` and 32452 `payload_summary.package_path` values now point to `data/crypto-history-full/binance/data`.
- Verified non-USDT ZIP count remains 0.

## crypto-factory-raw-data-project-local

- User clarified not to move CRYPTO factory raw packages to `/opt`; keep them under the project directory.
- Final raw package root: `data/crypto-history-full/binance/data`.
- Verified `.runtime/crypto-history-full/binance/data` does not exist; removed empty `.runtime/crypto-history-full/binance` directory.
- Verified raw ZIP count `32452`, checksum count `32452`, non-USDT ZIP count `0`.
- Verified Mongo raw payload path metadata points to `data/crypto-history-full/binance/data` for all `32452` raw payloads and has `0` old raw path refs.

## crypto-columnar-warehouse-moved-out-of-runtime

- Continued project-local factory data cleanup after user confirmed not to use `/opt`.
- Moved USDT-only normalized columnar warehouse from `.runtime/crypto-history-full/normalized-columnar-usdt-only` to `data/crypto-history-full/normalized-columnar-usdt-only`.
- Updated Mongo `claw_trade_crypto_history_usdt_20260608.dataset_manifests.path`: `2079` Parquet manifest paths now point to `data/crypto-history-full/normalized-columnar-usdt-only`; old columnar path refs are `0`; active new-path manifests are `1921`.
- Project-local storage now includes raw packages and columnar warehouse under `data/crypto-history-full/`: about `1.3G` total; raw package root `571M`; columnar root `741M`; Parquet files `2126`.
- Verified warehouse readback from the new project-local columnar path: BTCUSDT/ETHUSDT/SOLUSDT spot daily rows `371` each and intraday rows `8904` each for 2025-06-01 through 2026-06-06.
- Added `DATA_GATEWAY_COLUMNAR_ROOT` propagation to `scripts/start-control-runtime.sh` runtime env and child commands; `.env.example` documents data gateway storage override keys without real values.

## crypto-project-local-live-report-proof

- Ran fresh live `/report BTC` and `/report SOL` after moving both raw packages and the USDT-only columnar warehouse under project-local `data/crypto-history-full/`.
- Fixed runtime profile used: `scripts/start-control-runtime.sh -- <command>`, OpenViking `127.0.0.1:1933`, OpenClaw gateway `127.0.0.1:18789`, no invest sidecar, runtime config/data under `.runtime/dev-services`.
- Explicit data gateway binding for both runs:
  - `DATA_GATEWAY_MONGODB_URI=mongodb://127.0.0.1:27017/claw_trade_crypto_history_usdt_20260608`
  - `DATA_GATEWAY_MONGODB_DATABASE=claw_trade_crypto_history_usdt_20260608`
  - `DATA_GATEWAY_COLUMNAR_ROOT=data/crypto-history-full/normalized-columnar-usdt-only`
- BTC live run: `run-20260608-221724-ac41997c`, evidence `docs/evidence/live-crypto-btc-sol-usdt-project-path-20260608/report-btc`, command exit `0`.
- SOL live run: `run-20260608-222702-7eeaa3e7`, evidence `docs/evidence/live-crypto-btc-sol-usdt-project-path-20260608/report-sol`, command exit `0`.
- Provider-visible market prompts for both BTC and SOL show `日线行情：371 行` and `小时行情：8904 行`; report-prefetch files include `local_crypto_prepackaged` attempt refs and symbols `BTCUSDT` / `SOLUSDT`.
- Added live proof summary: `docs/evidence/live-crypto-btc-sol-usdt-project-path-20260608/summary.md`.

## crypto-factory-columnar-write-guard

- The project-local live proof initially set `DATA_GATEWAY_COLUMNAR_ROOT=data/crypto-history-full/normalized-columnar-usdt-only`.
- That proved the seed Parquet files were readable, but it also exposed a bad mechanism: runtime provider increments can write into the configured columnar root.
- Cleaned the accidental factory contamination from that run: removed 37 provider-generated Parquet files and 37 matching Mongo `dataset_manifests`; the USDT-only seed root returned to 2126 physical Parquet files.
- Added a guard in `scripts/start-control-runtime.sh`: live/runtime startup now fails if `DATA_GATEWAY_COLUMNAR_ROOT` points under `data/crypto-history-full/*`, unless `CLAW_TRADE_ALLOW_FACTORY_COLUMNAR_WRITE=1` is explicitly set for maintenance import work.
- Current runtime rule: write runtime evidence to a runtime Mongo database, read seed catalog through `DATA_GATEWAY_SEED_MONGODB_DATABASE=claw_trade_crypto_history_usdt_20260608`, leave `DATA_GATEWAY_COLUMNAR_ROOT` blank or use a runtime overlay; seed Parquet files are discovered from Mongo manifest paths, and provider increments must not write into the factory seed directory.

## crypto-seed-mongo-runtime-isolation

- The same live proof also wrote runtime provider evidence into the CRYPTO seed Mongo DB because `DATA_GATEWAY_MONGODB_DATABASE` pointed at `claw_trade_crypto_history_usdt_20260608`.
- Root cause: `build_data_gateway_runtime_from_env()` built one `DatasetRepository`, so seed manifest reads and runtime raw/attempt/cache/rate-limit/single-flight writes shared the same Mongo database.
- Mechanism change:
  - `DatasetRepository.from_database(..., seed_database=...)` now overlays read-only seed collections for `raw_payloads`, `provider_attempts`, and `dataset_manifests`.
  - Runtime writes still go to the primary runtime database.
  - `build_data_gateway_runtime_from_env()` reads `DATA_GATEWAY_SEED_MONGODB_URI` / `DATA_GATEWAY_SEED_MONGODB_DATABASE` for the seed catalog.
  - `scripts/start-control-runtime.sh` exports and records the seed Mongo vars and blocks `DATA_GATEWAY_MONGODB_DATABASE=claw_trade_crypto_history_*` unless `CLAW_TRADE_ALLOW_FACTORY_MONGO_WRITE=1`.
- Cleaned accidental seed Mongo runtime records:
  - deleted `raw_payloads=37`;
  - deleted `provider_attempts=40`;
  - deleted `provider_rate_limits=15`;
  - deleted `single_flight_calls=40`;
  - deleted `provider_result_cache=38`.
- Post-cleanup seed Mongo state:
  - `raw_payloads=32452`;
  - `provider_attempts=2079`;
  - `dataset_manifests=4001`;
  - `normalized_datasets=0`;
  - runtime cache/rate-limit/single-flight collections are `0`.
- Verification:
  - `uv run pytest tests/unit/data_gateway/test_warehouse.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py tests/unit/test_start_control_runtime_script.py -q` -> `73 passed in 1.36s`.

## crypto-seed-overlay-live-proof

- Ran fresh live `/report BTC` and `/report SOL` with runtime Mongo writes separated from the CRYPTO seed Mongo catalog.
- Runtime binding:
  - `DATA_GATEWAY_MONGODB_DATABASE=claw_trade_runtime_live_seed_overlay_20260608`;
  - `DATA_GATEWAY_SEED_MONGODB_DATABASE=claw_trade_crypto_history_usdt_20260608`;
  - `DATA_GATEWAY_COLUMNAR_ROOT=` blank.
- BTC live run: `run-20260608-230023-0e01f1ea`, evidence `docs/evidence/live-crypto-seed-overlay-20260608/report-btc`, command exit `0`.
- SOL live run: `run-20260608-231051-570a5cdf`, evidence `docs/evidence/live-crypto-seed-overlay-20260608/report-sol`, command exit `0`.
- Provider-visible market prompts for both BTC and SOL show `日线行情：371 行` and `小时行情：8904 行`.
- Data prefetch files include `local_crypto_prepackaged` refs and contain no `local_warehouse_empty`.
- Seed Mongo remained unchanged after both runs: `raw_payloads=32452`, `provider_attempts=2079`, `dataset_manifests=4001`, runtime cache/rate-limit/single-flight collections `0`, `normalized_datasets=0`.
- Runtime Mongo captured live provider evidence separately: `raw_payloads=37`, `provider_attempts=40`, `dataset_manifests=37`, `provider_result_cache=38`, total size about `1.3M`.
- Runtime columnar increment root `.runtime/dev-services/data-gateway/normalized` is `336K`; seed columnar root stayed `2126` Parquet files.
- Added final live proof summary: `docs/evidence/live-crypto-seed-overlay-20260608/summary.md`.

## a-share-factory-seed-gap-followup

- User asked for the remaining A-share data-layer gaps beyond incremental update.
- Fixed path portability for normalized Parquet manifests:
  - New manifests write `relative_path`.
  - Legacy manifests whose `path` points to an old workspace are rebased under the current `DATA_GATEWAY_COLUMNAR_ROOT` / default columnar root when the same partition layout exists.
- Root cause for the company-name gap:
  - This had been solved before in the old prepackaged importer by loading real identity from local roots or approved online sources (`baostock`, `akshare`, `tushare`).
  - The new reduced 300-trading-day factory package was rebuilt with only `daily_bar` and `valuation_metric`; it did not carry that identity enrichment.
  - Direct inspection of `data/a-share-cn-required-300td-20260608.tar` showed `daily_bar.row_json` has no `company_name`, `name`, `stock_name`, or `industry` fields.
- Prevented repeat on rebuild:
  - `scripts/selection/import_a_share_required_to_columnar.py` now accepts identity inputs and writes real `company_name` / `industry` / `identity_source_ref` into `daily_bar` rows when provided.
  - It records missing identity as a warning instead of fabricating names.
- Clarified selection cache boundary:
  - `/report` remains live/current computation.
  - `/select` candidate packs are daily runtime outputs with expiry; they should not be fixed factory seed data because market data changes every trading day.
  - Factory seed should provide the underlying data and identity coverage needed to build the current day's candidate pack.
- Verification:
  - `env UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run python -m py_compile scripts/selection/import_a_share_required_to_columnar.py src/claw_trade/data_gateway/a_share_prepackaged_importer.py src/claw_trade/data_gateway/warehouse/normalized_columnar.py` -> exit `0`.
  - `env UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/data_gateway/test_a_share_prepackaged_cli.py tests/unit/data_gateway/test_a_share_prepackaged_importer.py tests/unit/data_gateway/test_a_share_required_columnar_importer.py tests/unit/data_gateway/test_normalized_columnar_warehouse.py tests/unit/selection/test_provider_batch_strategy_and_trade_date.py -q` -> `64 passed in 3.06s`.

## a-share-factory-identity-columnar-test-tightening

- Scope: A股出厂恢复包缺口第4点 only; no incremental job work and no `/select` candidate cache work.
- Confirmed implementation state:
  - `scripts/selection/import_a_share_required_to_columnar.py` already reuses `load_a_share_identity_facts()` from `src/claw_trade/data_gateway/a_share_prepackaged_importer.py`.
  - Factor-derived `daily_bar` rows and daily-only CSV `daily_bar` rows both apply real identity fields when supplied.
  - Missing identity remains a warning path; no guessed company names were added.
- Test change:
  - Tightened `tests/unit/data_gateway/test_a_share_required_columnar_importer.py` to prove `company_name`, `industry`, `list_date`, and `identity_source_ref` are present in factor-derived and daily-only `daily_bar` rows.
- Verification:
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run python -m py_compile scripts/selection/import_a_share_required_to_columnar.py src/claw_trade/data_gateway/a_share_prepackaged_importer.py` -> exit `0`.
  - `UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/data_gateway/test_a_share_required_columnar_importer.py -q` -> `2 passed in 0.22s`.
- Remaining gap:
  - Existing `data/a-share-cn-required-300td-20260608.tar` still needs a rebuild with real identity inputs before the factory package itself contains these fields.


## a-share-factory-identity-package-rebuilt

- Rebuilt `data/a-share-cn-required-300td-20260608.tar` as a clean CN_A 300-trading-day factory seed.
- Rebuild command used real identity sources with `--require-company-identity`; no guessed company names were written.
- Final package facts:
  - package size: `446M`;
  - sha256: `0436bc038cbe580a290129182cb5320561a3be416331c1f33c454f2eb2200579`;
  - `daily_bar`: `1,626,559` rows, `5522` symbols, `2025-03-03` to `2026-05-27`;
  - `valuation_metric`: `1,626,559` rows, `5522` symbols, same date range;
  - Parquet files: `2`;
  - Mongo JSONL metadata: `dataset_manifests=2`, `provider_attempts=1`, `raw_payloads=1`;
  - `daily_bar` company-name missing rows: `0`.
- Skipped 8 symbols without real company identity: `002231.SZ`, `300344.SZ`, `300379.SZ`, `300391.SZ`, `600200.SH`, `600355.SH`, `603056.SH`, `920680.BJ`.
- Important boundary: `/select` top-20 candidate packs are daily runtime outputs, not factory seed. The seed now supplies underlying 300-day daily/valuation data plus company names so current candidates can be recomputed.
- Verification:
  - `sha256sum -c a-share-cn-required-300td-20260608.tar.sha256` -> `OK`.
  - Package extraction + DuckDB check -> `daily_bar` rows `1,626,559`, company missing `0`; `valuation_metric` rows `1,626,559`.
  - `env UV_CACHE_DIR=/tmp/claw-trade-uv-cache uv run pytest tests/unit/data_gateway/test_a_share_required_columnar_importer.py -q` -> `3 passed`.
