# 2026-06-07

## data-layer-columnar-supplement

- Context: user pointed out that the live `/select` + four-market `/report` validation summary described Mongo/provider attempts but omitted the columnar warehouse layer, despite the previous 2026-06-06 work separating Mongo from columnar storage.
- Corrected evidence written:
  - `docs/evidence/live-select-four-market-report-20260606T214515/data-layer-columnar-supplement.md`
  - Updated `docs/evidence/live-select-four-market-report-20260606T214515/summary.md` section `Data Layer / Columnar + Mongo Observations`.
- `/select` confirmed columnar evidence:
  - data run `sel-live-20260606T214515-v2`
  - `warehouse_check_ref=warehouse-check://selection-columnar/CN_A/CN_A/2026-06-05`
  - `columnar_manifest_ref=columnar://selection/market_data/market=CN_A/profile=CN_A/trade_date=2026-06-05/manifest.json`
  - sha `a3e74a5e604ac82f724fa2b9cbef072db13563b6178c8dbec119434dbe1d8cc0`
  - physical path `.runtime/selection/columnar/market_data/market=CN_A/profile=CN_A/trade_date=2026-06-05`, size `113M`, daily rows `1,854,035`, feature rows `5,373`.
- Four-market `/report` normalized columnar audit:
  - queried local Mongo `dataset_manifests` for `storage=parquet,status=active` and matched every prefetch `dataset_ref`.
  - CN_A `1009/1009` refs matched active Parquet manifests.
  - US `582/582` refs matched active Parquet manifests.
  - HK `553/553` refs matched active Parquet manifests.
  - CRYPTO `244/244` refs matched active Parquet manifests.
- Corrected model:
  - Mongo is ledger/index: provider attempts, raw refs, cache/rate/singleflight state, `dataset_manifests`.
  - Normalized report records live in `.runtime/dev-services/data-gateway/normalized` Parquet.
  - `/select` uses separate `.runtime/selection/columnar` Parquet warehouse.
- Remaining evidence gap:
  - current report artifacts still do not emit one ordered per-request timeline of `query planner -> warehouse manifest hit/miss -> exact Parquet manifest -> provider selector -> provider attempt`.
  - This is an evidence-reporting gap, not proof of Mongo-only behavior.
- HK clarification:
  - HK failure was not columnar absence. Its `553` prefetch refs matched Parquet manifests for prefetched domains.
  - Failure was domain coverage mismatch: HK prefetch had `market,fundamental,news`, while `social_analyst` still called `claw_get_social_pack`.

## hk-social-live-rerun

- Context: after implementing HK social data-layer fix, user requested one live HK `/report` validation.
- Branch/runtime:
  - branch: `fix-hk-social-data-layer`
  - fixed runtime entry: `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py --ticker 00700.HK --company-name 腾讯控股 --market HK --profile HK --currency HKD --currency-symbol 'HK$'`
  - existing `.runtime/dev-services/runtime.env` was present but stale; `1933/health`, `1933/healthz`, and `18789/health` were not reachable.
  - first restart failed at Mongo bind because 27017 was already occupied while `.runtime/mongodb/run/mongod.pid` was stale. Verified existing Mongo with PyMongo ping, then reran fixed runtime entry with loopback integer URI `mongodb://2130706433:27017/claw_trade` to reuse the same local Mongo and avoid a second local bind.
  - OpenViking `1933` and OpenClaw gateway `18789` became ready; no OpenViking MCP sidecar was started.
- Result:
  - command exit code: `0`
  - run id: `run-20260607-052437-e10fd015`
  - run state: `completed`
  - export result: `passed`
  - final report: `runs/run-20260607-052437-e10fd015/reports/final-report.md`
  - markdown evidence export: `docs/evidence/trading_claw_trade_hk_fresh_live_run-20260607-052437-e10fd015_md/`
- HK social evidence:
  - `report-prefetch.json` domains: `market,fundamental,news,social`
  - social request: `run-20260607-052437-e10fd015:report-prefetch:social:1:social_signal`
  - social status: `partial`
  - social rows: `20`
  - provider attempt: `attempt:hk_google_news:social_signal_news_heat:b901ebc3678e`
  - columnar path: `.runtime/dev-services/data-gateway/normalized/market=HK/dataset=social_signal/granularity=event/partition-a9a259b7cb8a42b0.parquet`
  - DuckDB readback confirmed `source_roles_json=["discovery"]`, row `quality_flags=["not_formal_fact_source"]`, provider lineage `hk_google_news/social_signal_news_heat`.
- Worker/tool boundary:
  - `social_analyst` visible tools only contained `claw_get_social_pack`.
  - `social_analyst` tool call `claw_get_social_pack` status: `success`.
  - no `manifest does not contain domain: social` hit in the run/evidence.
  - `social_analyst` hard gate passed.
- Report content check:
  - social report and final report describe Google News/public search lines as limited evidence.
  - final report says it cannot judge June 7 HK social discussion heat or sentiment direction from the returned lines.
  - final report states no social platform volume, post sentiment, interaction, or discussion data was available.

## 02:01 [hk-data-doc-code-consistency-subagent]
- 完成：核对 HK 数据层修复的文档-代码一致性，确认 HK frontline intent/skill、统一工具映射、HK social provider capability、data_pack_bridge social 请求、模型可见文案、测试覆盖和 live 证据基本一致。
- 决策：修正 `docs/HK数据层修改方案.md` 中 HK social model-visible 文案的实现描述，使其匹配当前代码：有 `social_signal` row 时说明 Google News/公开搜索线索边界，`partial` 缺口由“数据缺口”段呈现；无 row 时才输出“社交证据视为缺口”。同时在 `tests/unit/reports/test_data_pack_bridge.py` 的 HK prefetch 测试中补充四域 `market/fundamental/news/social` 精确断言，使测试清单与实际断言一致。
- 命令：`uv run pytest tests/contracts/test_tool_registry_contract.py tests/contracts/test_stage_tool_policy_contract.py tests/contracts/test_worker_prompt_alignment_policy.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_selector.py tests/unit/reports/test_data_pack_bridge.py`，退出码 0，结果 301 passed / 1 skipped；`git diff --check -- docs/HK数据层修改方案.md tests/unit/reports/test_data_pack_bridge.py`，退出码 0。
- 偏差状态：DONE_WITH_CONCERNS。未发现 HK 数据层代码违反方案；live report 文本仍把 HK social 覆盖期概括为 2026 年 1-5 月，而本次数据层证据含 2026-06-01、2026-06-02、2026-06-03、2026-06-05 的 Google News rows，属于报告解读/摘要口径残留，需要主代理决定是否另起报告输出修正任务。两个加密层文档本地改动未触碰。
- 待办：主代理如要关闭 live 报告文本口径问题，应基于同一 run 的 provider payload 和 data-layer parquet 另行审查报告生成/摘要逻辑；本次 HK 数据层一致性检查不扩大到加密层文档。
