# 2026-06-07

## data-layer-columnar-supplement

- Context: user pointed out that the live `/select` + four-market `/report` validation summary described Mongo/provider attempts but omitted the columnar warehouse layer, despite the previous 2026-06-06 work separating Mongo from columnar storage.
- Corrected evidence written:
  - `docs/evidence/live-select-four-market-report-20260606T214515/data-layer-columnar-supplement.md`
  - Updated `docs/evidence/live-select-four-market-report-20260606T214515/summary.md` section `Data Layer / Columnar + Mongo Observations`.
- `/select` confirmed columnar evidence:
  - data run `sel-live-20260606T214515-v2`
  - `warehouse_check_ref=warehouse-check://selection-columnar/CN_A/CN_A/2026-06-05`
  - `columnar_manifest_ref=columnar://selection/market_data/market=CN_A/profile=CN_A/trade_date=2026-06-05/manifest.json`
  - sha `a3e74a5e604ac82f724fa2b9cbef072db13563b6178c8dbec119434dbe1d8cc0`
  - physical path `.runtime/selection/columnar/market_data/market=CN_A/profile=CN_A/trade_date=2026-06-05`, size `113M`, daily rows `1,854,035`, feature rows `5,373`.
- Four-market `/report` normalized columnar audit:
  - queried local Mongo `dataset_manifests` for `storage=parquet,status=active` and matched every prefetch `dataset_ref`.
  - CN_A `1009/1009` refs matched active Parquet manifests.
  - US `582/582` refs matched active Parquet manifests.
  - HK `553/553` refs matched active Parquet manifests.
  - CRYPTO `244/244` refs matched active Parquet manifests.
- Corrected model:
  - Mongo is ledger/index: provider attempts, raw refs, cache/rate/singleflight state, `dataset_manifests`.
  - Normalized report records live in `.runtime/dev-services/data-gateway/normalized` Parquet.
  - `/select` uses separate `.runtime/selection/columnar` Parquet warehouse.
- Remaining evidence gap:
  - current report artifacts still do not emit one ordered per-request timeline of `query planner -> warehouse manifest hit/miss -> exact Parquet manifest -> provider selector -> provider attempt`.
  - This is an evidence-reporting gap, not proof of Mongo-only behavior.
- HK clarification:
  - HK failure was not columnar absence. Its `553` prefetch refs matched Parquet manifests for prefetched domains.
  - Failure was domain coverage mismatch: HK prefetch had `market,fundamental,news`, while `social_analyst` still called `claw_get_social_pack`.

## crypto-history-legacy-platform-cleanup

- Context: user corrected that the deleted legacy unified data platform must not be restored or used while implementing CRYPTO historical data packages.
- Completed cleanup in branch `crypto-history-phase1`:
  - removed the deleted platform submodule entry from `.gitmodules` and the `third_party` gitlink;
  - removed editable dependency entries from `pyproject.toml` and regenerated `uv.lock` offline;
  - deleted obsolete setup/audit scripts, stale unit test, old migration design documents, and obsolete TODO file tied to the deleted platform;
  - replaced remaining historical references with neutral legacy-data-gateway wording in docs and memory;
  - removed stale provider/tool/test labels from active Python and UI tests.
- Verification:
  - Full-repo case-insensitive search for the deleted platform name returned no matches after cleanup.
  - `git diff --check` exited 0.
- Decision: CRYPTO historical-data implementation must proceed through `src/claw_trade/data_gateway`, local normalized warehouse, and explicit provider attempts; do not restore the deleted legacy platform, submodule, runtime, editable dependency, or worker-visible atomic tools.

## crypto-history-phase1-implementation

- Context: implemented the approved CRYPTO historical data Phase 1 plan after the review requested explicit `date_range_missing`, no hidden provider fallback, Binance CSV evidence, CRYPTO normalized schema, CLI specs, and local seed attempts.
- Implemented:
  - `src/claw_trade/data_gateway/crypto_prepackaged_importer.py`
  - `src/claw_trade/data_gateway/crypto_symbols.py`
  - `scripts/crypto/download_binance_public_data.py`
  - `scripts/crypto/import_crypto_prepackaged_to_mongo.py`
  - CRYPTO importer, downloader, CLI, asset mapping, data-service local-empty gap preservation, and Mongo intraday date-range query support.
- Important fixes found by real sample import:
  - USD-M futures CSV can include a header row; parser now supports optional Kline headers with alias mapping.
  - spot and USD-M daily rows initially collided on dataset ref; CRYPTO rows now include `source_market_segment` in explicit `dataset_ref` and write the Binance universe ref.
  - Mongo-backed intraday rows use datetime fields; repository normalized queries now include a datetime range branch instead of only date strings.
- Evidence:
  - `docs/evidence/crypto-history-phase1-preflight-20260607.md`
  - `docs/evidence/crypto-history-phase1-sample-import-20260607.md`
  - `.runtime/crypto-history-phase1/import-result-r3.json`
  - `.runtime/crypto-history-phase1/mongo-coverage-r3.json`
- Real sample proof:
  - downloaded Binance 2025-01 monthly Kline ZIPs for BTCUSDT/ETHUSDT/SOLUSDT, spot and USD-M, 1d and 1h.
  - all 12 ZIP checksums verified.
  - imported into scratch Mongo database `claw_trade_crypto_phase1_20260607_r3`.
  - Mongo counts: `normalized_datasets=4650`, `raw_payloads=12`, `provider_attempts=12`, `dataset_manifests=12`.
  - every symbol/segment has `daily_bar=31` rows and `intraday_bar=744` rows for 2025-01.
  - provider attempts are `local_crypto_prepackaged` / `binance_public_data_import` with status `local_seed_imported`.
- Verification passed:
  - `uv run pytest tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_asset_mapping.py tests/unit/data_gateway/test_data_service_flow.py tests/unit/data_gateway/test_warehouse.py tests/contracts/test_crypto_prepackaged_data_contract.py -q` -> 84 passed.
  - `uv run pytest tests/unit/data_gateway/test_warehouse.py tests/unit/data_gateway/test_models.py tests/unit/data_gateway/test_ingest_pipeline.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_asset_mapping.py tests/unit/data_gateway/test_data_service_flow.py tests/contracts/test_crypto_prepackaged_data_contract.py -q` -> 110 passed.
  - `uv run pytest tests/contracts/test_data_access_boundary.py tests/contracts/test_data_gateway_cutover.py tests/contracts/test_provider_capabilities.py tests/contracts/test_selection_tool_import_block.py tests/contracts/test_tool_registry_contract.py tests/unit/data_gateway/test_crypto_lens_import_block.py tests/unit/selection/test_models.py tests/unit/test_workflow_store.py tests/unit/test_run_control_cli.py -q` -> 122 passed.
  - full-repo content scan for the deleted platform name returned no matches.
  - `git diff --check` exited 0.
  - `git submodule status` lists only OpenClaw and OpenViking.
- Verification not completed:
  - live `/report BTC` and `/report SOL` provider-payload proof was not run in this slice.
  - a broader UI/settings/Node plugin regression batch still has unrelated environmental/pre-existing blockers: missing OpenClaw dist plugin SDK, missing settings UI evidence artifacts, and existing data-source runtime-check test assumptions.

## crypto-history-full-binance-correction

- Context: user corrected that the target was Binance all-symbol historical packages, not only the BTC/ETH/SOL sample.
- Correction:
  - The BTC/ETH/SOL January 2025 import is only a proof sample and must not be described as the full target.
  - Added downloader support for `--all-symbols`, active symbol discovery from Binance spot and USD-M exchange info, existing-file skipping, missing-package recording, and progress JSON writes.
  - Added importer support for `--symbol-manifest`, so full symbol metadata from download manifests can drive base/quote normalization instead of the Phase 1 BTC/ETH/SOL static map.
- Full one-year completed-month manifest generated under `.runtime/crypto-history-full/manifests`:
  - spot 1d: 1365 symbols, 16380 monthly package URLs.
  - spot 1h: 1365 symbols, 16380 monthly package URLs.
  - USD-M 1d: 571 symbols, 6852 monthly package URLs.
  - USD-M 1h: 571 symbols, 6852 monthly package URLs.
  - total: 46464 monthly package URLs for 2025-06-01 through 2026-05-31.
- A one-item foreground probe succeeded and recorded an early missing package for `0GTRY`, proving `--ignore-missing` records unavailable symbol/month packages instead of failing the whole batch.
- Superseded later on 2026-06-07: the full foreground download and Mongo normalization were completed; see `crypto-history-full-binance-import-complete`.
- Verification:
  - `uv run pytest tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/contracts/test_crypto_prepackaged_data_contract.py -q` -> 34 passed.
  - `git diff --check` exited 0.
  - full-repo content scan for the deleted platform name returned no matches.

## crypto-history-code-doc-consistency-followup

- Context: user requested sub-agent review until code and documents are consistent after clarifying that the expected target is Binance all-active-symbol packages, not only BTC/ETH/SOL.
- Sub-agents launched:
  - code/design consistency review found daily/monthly mismatch, missing CLI docs, implicit BTC/ETH/SOL import scope fallback, and a provider_lineage field-name mismatch.
  - evidence consistency review confirmed at that time that the four full manifests were planned-only and that no all-active-symbol download or full Mongo import was complete.
- Changes made:
  - downloader now generates monthly package URLs for complete months and daily package URLs for incomplete date ranges such as the current month.
  - import CLI now requires explicit symbol scope: `--symbol`, `--symbol-file`, `--symbol-manifest`, or `--phase1-sample`; it no longer silently falls back to BTC/ETH/SOL for an unscoped run.
  - sample import evidence now includes a current CLI rerun command with `--phase1-sample`.
  - `replace-existing` now scopes deletion to the same source market segment, so replacing spot rows cannot delete USD-M rows for the same symbol and interval.
  - design and implementation docs were updated to distinguish completed sample import, all-active-symbol URL manifest generation, and then-unfinished full download/full Mongo import/live `/report` proof.
  - evidence added: `docs/evidence/crypto-history-full-binance-manifest-20260607.md`.
  - sample evidence now explicitly says it does not prove all-active-symbol download or full Mongo import.
- Superseded later on 2026-06-07:
  - Full all-active-symbol package download, checksum verification, normalization, and Mongo import are complete.
  - True UI/live `/report` provider-payload proof remains incomplete.
  - See `crypto-history-full-binance-import-complete`.

## crypto-history-full-binance-import-complete

- Context: user required Binance all-active-symbol packages, not only BTC/ETH/SOL samples, and asked to continue without interruption until download, normalization, and code/doc consistency review.
- Completed full Binance active-symbol batch:
  - Scope: 2025-06-01 through 2026-06-06.
  - spot active symbols: 1365.
  - USD-M perpetual active symbols: 571.
  - intervals: `1d`, `1h`.
  - planned URLs: 69696.
  - processed URLs: 69696.
  - checksum verified ZIPs: 64162.
  - Binance missing ZIP/CHECKSUM packages: 5534.
  - checksum failures: 0.
  - local files: 128324 ZIP/CHECKSUM files.
  - local directory: `.runtime/crypto-history-full/binance`, about 1.1G.
- Mongo import completed:
  - database: `claw_trade_crypto_history_full_20260607`.
  - import jobs: 3872.
  - raw payload metadata rows: 64162.
  - provider attempts: 3872.
  - dataset manifests: 3872.
  - normalized unique rows: 15633535.
  - parsed CSV rows: 15633546.
  - duplicate Kline rows collapsed by `dataset_ref`: 11 across 9 jobs.
- Code fixes made during full run:
  - Binance URLs now percent-encode non-ASCII symbols such as Chinese-named active trading pairs.
  - import CLI now indexes packages once per root instead of rescanning the full tree for every symbol.
  - importer now distinguishes `parsed_row_count` from unique `row_count`; manifest audit also records `duplicate_row_count`.
- Coverage proof:
  - BTCUSDT, ETHUSDT, and SOLUSDT spot + USD-M `1d` cover 2025-06-01 through 2026-06-06 with `date_range_missing=0`.
  - BTCUSDT, ETHUSDT, and SOLUSDT spot + USD-M `1h` cover 2025-06-01 00:00 UTC through 2026-06-06 23:00 UTC with `hour_range_missing=0`.
  - Warehouse API checks for BTCUSDT and SOLUSDT `daily_bar` returned `ready`, no gaps, and local seed attempt refs.
  - BTC/SOL provider attempts are `local_crypto_prepackaged` / `binance_public_data_import` / `local_seed_imported`, with `remote_attempted=false` and `remote_success=false`.
- Evidence updated:
  - `docs/evidence/crypto-history-full-binance-manifest-20260607.md`.
  - `docs/加密层历史数据设计.md`.
  - `docs/加密层历史数据实施计划.md`.
- Verification:
  - `uv run pytest tests/unit/data_gateway/test_binance_public_data_downloader.py tests/unit/data_gateway/test_crypto_prepackaged_cli.py tests/unit/data_gateway/test_crypto_prepackaged_importer.py tests/unit/data_gateway/test_crypto_asset_mapping.py tests/unit/data_gateway/test_data_service_flow.py tests/unit/data_gateway/test_warehouse.py tests/contracts/test_crypto_prepackaged_data_contract.py -q` -> 94 passed.
  - full-repo case-insensitive content scan for the deleted platform name returned no matches; git status may still show deleted legacy filenames until the cleanup commit lands.
- Still not completed:
  - true UI/live `/report BTC` and `/report SOL` provider-payload proof through the fixed runtime preflight.
  - Phase 2-4 datasets: paid depth, chain/on-chain, news/social history, COIN-M, `1m`, trades, tick/order book, and delisted-pair complete history.

## crypto-history-main-worktree-sync-and-legacy-cleanup

- Context: current active workspace is `/home/frank/src/claw-trade`; crypto implementation evidence also existed in `/tmp/claw-trade-worktrees/crypto-history-phase1`, so the crypto implementation and evidence were synchronized into the main workspace.
- Synced implementation files:
  - `scripts/crypto/download_binance_public_data.py`
  - `scripts/crypto/import_crypto_prepackaged_to_mongo.py`
  - `src/claw_trade/data_gateway/crypto_prepackaged_importer.py`
  - `src/claw_trade/data_gateway/crypto_symbols.py`
  - CRYPTO data-service / warehouse query support and focused tests.
- Cleanup:
  - removed the deleted legacy data platform dependency set from `pyproject.toml` and `uv.lock`.
  - removed the deleted legacy data platform submodule entry, local submodule metadata, dedicated setup/audit scripts, and stale audit test.
  - full working-tree text scan and filename scan, excluding Git history, returned no matches for the deleted platform name.
- Current verification:
  - crypto history focused tests: `98 passed`.
  - legacy cleanup related Python contract/unit subset: `191 passed`.
  - frontend tests touched by the cleanup: `4 passed`.
  - `git diff --check` passed.
- Known verification limits:
  - broader settings Python tests still need their own UI evidence and configured report model preconditions; the failure is not used as crypto history acceptance.
  - true UI/live `/report BTC` and `/report SOL` provider-payload proof is still pending.

## crypto-history-doc-code-consistency-review

- Context: user asked what remains unfinished and requested a doc/code consistency check after the full Binance active-symbol import.
- Review result:
  - no high-risk doc/code mismatch found.
  - data-layer status is consistent: Binance current active spot/USD-M `1d` and `1h` packages were downloaded where available, checksum verified, normalized, and imported to Mongo.
  - report-chain status is still incomplete: true UI/live `/report BTC` and `/report SOL` provider-payload proof remains pending.
- Documentation fixes:
  - split data-layer completion criteria from report-chain final acceptance in `docs/加密层历史数据实施计划.md`.
  - added task status: CRH-00 through CRH-09b complete, CRH-10 and CRH-11 pending.
  - clarified `docs/evidence/crypto-history-full-binance-manifest-20260607.md` so the 64162 package count is the sum of all import jobs' `package_count`, not a top-level audit field.
  - clarified `docs/加密层历史数据设计.md` that BTC/SOL live proof is still pending and current proof is warehouse-interface level only.
- Verification after doc fixes:
  - `git diff --check` passed.
  - doc phrase scan for stale completion wording returned no matches.
  - working-tree content and filename scans for the deleted legacy data platform name returned no matches, excluding Git history.
  - crypto focused tests passed: `100 passed`.
  - Mongo count check returned: normalized rows `15633535`, raw payload metadata rows `64162`, provider attempts `3872`, dataset manifests `3872`.

## crypto-history-columnar-normalized接入

- Context: user asked why Mongo was used instead of a columnar database, then required completing columnar database integration without creating a CRYPTO-specific mechanism. Requirement: data-layer mechanism must be the same across markets, with one upward interface and workers fetching only what they need.
- Decision:
  - keep Mongo as catalog/evidence storage for raw metadata, provider attempts, seed manifests, Parquet manifests, cache/rate-limit evidence.
  - write new normalized CRYPTO bars through the existing unified `DatasetRepository` columnar path, backed by `NormalizedColumnarWarehouse` Parquet partitions.
  - do not add a CRYPTO-specific reader or collection; upward access remains `Warehouse` / `DatasetRepository`.
- Code changes:
  - `scripts/crypto/import_crypto_prepackaged_to_mongo.py` now constructs `DatasetRepository.from_database(..., normalized_columnar=NormalizedColumnarWarehouse.from_env(...))`.
  - added `--columnar-root`; default follows `DATA_GATEWAY_COLUMNAR_ROOT` / runtime default.
  - CLI output now records `catalog_storage=mongo` and `normalized_storage=parquet`.
  - fixed package discovery when `--spot-root` or `--futures-root` points at a shared Binance root: spot imports now ignore `data/futures/um/**`, and USD-M imports now require the futures/um marker.
- Tests/evidence:
  - added CLI test proving normalized rows are written to Parquet manifests while Mongo `normalized_datasets` remains empty.
  - added CLI test proving a shared root does not mix spot and USD-M packages.
  - crypto + warehouse focused tests passed: `111 passed`.
  - smoke import against existing local Binance SOL package wrote Mongo normalized count `0`, raw `1`, attempts `1`, manifests `2`, Parquet manifests `1`, seed manifests `1`.
  - smoke Warehouse readback returned `ready` for `dataset:daily_bar:CRYPTO:spot:SOLUSDT:daily:2026-06-01:2026-06-01`.
  - evidence file: `docs/evidence/crypto-history-columnar-smoke-20260607.md`.
- Known remaining work:
  - the prior all-active import in `claw_trade_crypto_history_full_20260607` remains historical Mongo normalized data until rerun or migrated with the columnar-enabled path.
  - true UI/live `/report BTC` and `/report SOL` provider-payload proof remains pending.
