## 05:15 [codex]
-完成：按固定 runtime 脚本入口执行 CN_A 600519.SH `/report`，显式设置 `CLAW_TRADE_REPORT_MAX_DEBATE_ROUNDS=1` 与 `CLAW_TRADE_REPORT_MAX_RISK_DISCUSS_ROUNDS=1`；run_id=`run-20260513-090433-5d81143b`，13 个 worker（含 report_polisher）全部 succeeded，最终报告导出 passed，图表资产 2 张。
-决策：复核 request.json/state.json 确认轮数均为 1、frontline 为 parallel；运行后 1933/18789 已关闭，run/log 未命中 `resource is busy`、`TOOL_SUBPROCESS_TIMEOUT`、MiniMax、embedding、re-enqueue、openviking probe timeout 或 Mongo 失败相关模式。
待办：无。

## 06:42 [codex]
-完成：按用户要求清理工作区，删除 `docs/evidence` 下除 `trading/` 之外的 evidence，删除 `runs/`、`.runtime/`、`.pytest_cache` 和 Python `__pycache__`。
-决策：完整保留 `docs/evidence/trading/` 及其 `_original_results/`，用于后续美股测试参考。
待办：无。

## 06:37 [codex]
-完成：新增 `docs/evidence/compare/final_report_latest_vs_cn_20260513.md`，对比本项目最新 final report 与 CN 版本在结构、字数、图表、技术指标、基本面、风格和差异原因上的区别。
-决策：判断本项目最新报告更适合产品化读者版最终报告，CN 版本更接近风险委员会裁决备忘录；同时记录本项目报告中批价来源层级需要复核。
待办：无。

## 06:34 [codex]
-完成：将 report workflow 轮数产品硬上限从 5 收紧为 3，并同步配置测试与设计文档。
-决策：`CLAW_TRADE_REPORT_MAX_ROUNDS_HARD_LIMIT` 仍保留为 env 设置，但只能取 1/2/3，不能通过 env 提高到 4 或 5。
待办：无。

## 06:14 [codex]
-完成：修复 OpenClaw gateway 大参数通过命令行传输触发 `Argument list too long` 的问题，并重跑 CN_A 600519.SH `/report`，debate/risk 均为 2 轮；run_id=`run-20260513-100224-c2577736`，18 次 worker 调用全部 succeeded，最终报告导出 passed。
-决策：OpenClaw CLI 增加 `--params-file`，claw-trade runner 仅在参数超过安全阈值时使用文件传参，不压缩 worker 材料、不改变 workflow；OpenClaw 已 `pnpm build`，实际 dist 已更新。
待办：OpenViking probe 对缺失 probe report 的 stat 仍记录 500；本轮不影响正式材料读写，但后续应改为可预期的 not-found 处理。

## 05:31 [codex]
-完成：复查 run-20260513-090433-5d81143b 的 Mongo 与材料流；Mongo 实际创建并保留 CN_A 行情、基本面、新闻、社媒、provider cache、provider attempts 集合，前线 4 个数据包工具均成功调用并写入/读取缓存证据。
-决策：确认当前共享边界是“前线 worker 读数据源矩阵并产出 approved L1 报告，后续 worker 读取 approved L1 文本”，不是让所有 worker 直接读取 Mongo 原始数据。
待办：无。

## 07:00 [codex]
-完成：按固定 runtime 脚本和 OpenClaw gateway 入口执行 US AAPL 完整 `/report` workflow；run_id=`run-20260513-105326-d64378c1`，12 个核心 worker 均被唤醒并产出 final provider prompt、LLM raw output、OpenViking report，证据包写入 `docs/evidence/trading/claw_trade_us_aapl_full_run-20260513-105326-d64378c1/`。
-决策：workflow 在 `portfolio_manager` 后触发 PM-owner gate 失败，原因是 `pm-decision.json` 缺失；按规则未继续到 `report_polisher`，只整理真实已产生证据，不补造最终报告。
待办：排查 US `portfolio_manager` 产出为何没有生成 `pm-decision.json`，并复核前线 US 工具返回的数据缺口。

## 07:07 [codex]
-完成：定位 US AAPL run 中 `pm-decision.json` 缺失根因：US profile 的 PM gate 明确要求结构化 `pm-decision.json`，但 `portfolio_manager` 的 US prompt/STAGES 只允许自然语言输出且 tools 为空，生产代码中也没有从 PM 输出生成该 sidecar 文件的实现。
-决策：判定为 PM-owner 结构化决策产物的实现断层，不是 DeepSeek/OpenClaw/report 写入失败；不能通过让 PM 在正文手写 JSON 解决，因为 L1 guard 会拒绝 `control.pm_decision.v1` 正文机器块。
待办：讨论并实现一个不改写 PM 结论的 sidecar 生成路径，或重新评估 US 是否仍应强制 `pm-decision.json`。

## 07:25 [codex]
-完成：只读检查原版 TradingAgents 本地克隆与最新上游 v0.2.5，确认默认运行需要所选 LLM provider 的 API key；默认 yfinance 数据源不需要额外 key，切 Alpha Vantage 才需要 `ALPHA_VANTAGE_API_KEY`。
-决策：远端 HEAD 已不同于本地 `/home/frank/src/TradingAgents`，因此结论按 `/tmp/TradingAgents-upstream-api-key-check` 最新上游克隆为准，并注明本地克隆少了 MiniMax、CN 区域 key 映射。
待办：无。

## 07:27 [codex]
-完成：解释 `FINNHUB_API_KEY` 与 `POLYGON_API_KEY` 的用途，区分它们分别偏财务/新闻/估值数据与行情/交易报价/期权等市场数据。
-决策：结论按 Finnhub 与 Polygon 官方文档口径，并明确它们不是原版 TradingAgents 最新上游默认必需 key。
待办：无。

## 07:29 [codex]
-完成：确认 yfinance 默认不需要配置 API key；它访问 Yahoo Finance 公开接口，适合默认/原型数据路径。
-决策：同时提示 yfinance 非 Yahoo 官方背书服务，存在限流、接口变化、个人使用条款和无 SLA 风险。
待办：无。

## 07:29 [codex]
-完成：确认原版 TradingAgents 默认配置除所选 LLM provider key 外，不强制要求其他数据 API key。
-决策：保留例外口径：切换到 Alpha Vantage、企业 provider 或其他自定义数据源时，需要对应 key；默认 yfinance 路径不需要。
待办：无。

## 07:31 [codex]
-完成：评估 yfinance 数据是否足够用于原版 TradingAgents，结论为默认演示/低频研究够用，但高可信生产级报告不够。
-决策：按原版上游代码核对 yfinance 覆盖价格、技术指标、基础财务三表、公司新闻和宏观搜索，同时指出其非官方、限流、新闻/财务深度不足、缺少正式 filings/一致预期/实时 SLA 等边界。
待办：无。

## 07:54 [codex]
-完成：给出原版 TradingAgents 若要产出专业、正确、严谨投研报告所需的数据源/API 配置建议，按行情、财报/filings、新闻事件、预期评级、宏观和可选资产类别分层。
-决策：推荐美股最低生产级组合为 SEC EDGAR + Polygon + Finnhub 或 FMP + Benzinga/高质量新闻源 + FRED；强调原版默认代码只内置 yfinance/Alpha Vantage 路由，接入 Polygon/Finnhub/Benzinga/FRED 需要扩展数据层而不是只填 key。
待办：无。

## 07:56 [codex]
-完成：将专业投研报告数据源建议压缩为可执行配置清单：美股核心为 SEC EDGAR、Polygon、Finnhub、FRED，Benzinga 可选增强。
-决策：明确 `yfinance` 只作为备用，不作为专业报告主数据源；同时提示原版 TradingAgents 需要扩展代码才能实际使用这些新增 key。
待办：无。

## 07:59 [codex]
-完成：核对并解释专业投研数据源的免费/收费状态：SEC EDGAR 免费无 key，FRED 免费但要 key，Polygon/FMP 有免费层但生产级付费，Finnhub 有免费 key 但专业使用通常付费，Benzinga 属商业付费数据源。
-决策：建议先用免费层验证链路，正式专业报告最低预算优先投 Polygon + Finnhub 或 FMP，Benzinga 作为新闻/评级增强项。
待办：无。

## 08:02 [codex]
-完成：解释 Polygon.io 是金融市场数据 API 平台，并给出官网与文档地址。
-决策：定位为专业行情主数据源，主要覆盖美股、期权、指数、外汇和加密货币的历史/实时市场数据。
待办：无。

## 08:03 [codex]
-完成：确认 `https://massive.com/` 就是原 Polygon.io 重品牌后的主站，Polygon.io/Massive 是同一金融市场数据服务。
-决策：后续称呼优先使用 Massive，必要时注明 formerly Polygon.io；旧 `polygon.io` 与 `api.polygon.io` 仍按官方说明兼容一段时间。
待办：无。

## 08:21 [codex]
-完成：新建仓库根目录 `README.md`，写入专业投研报告数据源配置清单。
-决策：根目录原本没有 `README.md`，因此按用户“写入 readme”要求创建最小 README，而不改动 `docs/` 或 `third_party/` 下的 README。
待办：无。

## 07:31 [codex]
-完成：删除 US PM 结构化决策单链路，覆盖审批、导出、OpenClaw 工具 schema、portfolio_manager prompt、测试和活跃文档引用；活跃代码、dist、测试和文档扫描无旧结构化 PM 决策标识命中。
-决策：PM 最终裁决回到原版 TradingAgents 的自然语言 portfolio_manager 报告；Python/exporter 只搬运已批准 PM L1 原文，不要求、不生成、不校验结构化决策单。
待办：无。

## 07:40 [codex]
-完成：按全市场口径复扫 US/CN_A/HK/CRYPTO 的 worker prompt、stage policy、OpenClaw 工具、活跃文档和 legacy 文档，删除 portfolio_manager 全局 prompt/skill 中残留的旧结构化裁决字段要求，并清理旧方案文字残留。
-决策：HK/CRYPTO 未批准即 fail 的保护未删除，因为它不是机器裁决约束，而是防止 silent fallback 到 US/CN_A 的市场策略边界；同时补回 stage policy 合同：`tools: []` 只能搭配 `openviking_access: none`。
待办：无。

## 07:42 [codex]
-完成：复查 US 非 PM worker 的 prompt/USER/skill，未发现要求提交结构化投资裁决、JSON 决策单或 PM sidecar 字段的同类问题；命中项主要是自然语言表格、`FINAL TRANSACTION PROPOSAL` 文本标记、禁止机器字段进入正文，以及 `canonical artifact` 运行时产物词。
-决策：将“结构化决策单问题”和“canonical artifact 等运行时词污染 prompt 的原版对齐问题”分开处理；本次只检查不改代码。
待办：如按最严格原版对齐标准，需要单独清理 provider-visible prompt/skill 里的 `canonical artifact` 运行时词，并确认保存报告的职责仍由运行时承担。

## 07:55 [codex]
-完成：按本项目 `/report` 入口重跑 US AAPL live run `run-20260513-114841-fc44c941`，13 个 OpenClaw worker call 均 succeeded，并将每个 worker 的 final prompt / LLM back / report 及 raw evidence 导出到 `docs/evidence/trading`。
-决策：本轮未被旧 `pm-decision.json` gate 阻断，PM 和 report_polisher 均已完成；workflow 终态仍 failed，根因是 US `market_analyst` 仍暴露 CN_A-only `market_market_data_pack`，工具拒绝 AAPL/US 后没有图表资产，最终 exporter 在 `export_report_assets` 阶段失败。
待办：单独修复 US market data/tool profile 映射或为 US 明确无图导出策略；修复前不要把该 run 当作完整成功报告。

## 07:59 [codex]
-完成：追溯 US AAPL 拿到 A股工具的链路：US `market_analyst` 的 `market_data` intent 被全局映射到 `market_market_data_pack`，OpenClaw frontline plugin 又把该工具硬连到 `cn-a-market-data/scripts/market_data_pack.py`，并在插件层硬编码 `FRONTLINE_MARKET=CN_A`。
-决策：判定不是市场识别错，而是工具 intent 没有按市场分流；底层 CN_A provider 矩阵包含 Tushare `pro_bar`、AkShare `stock_zh_a_hist/stock_zh_a_daily/stock_zh_a_hist_tx`、EastMoney `push2his_kline` 等 A股接口，本轮 US 调用实际在 provider 前被参数校验拦下。
待办：设计 US market data tool/profile 分流，不能继续让 US 复用 CN_A-only market pack。

## 08:04 [codex]
-完成：核对 CN_A market data pack 的最底层行情接口：正常 CN_A 路径先查 MongoDB normalized OHLCV cache，再按 provider plan 调 Tushare `pro_bar`、AkShare `stock_zh_a_hist`、EastMoney `push2his_kline`、AkShare/Sina `stock_zh_a_daily`、AkShare/Tencent `stock_zh_a_hist_tx`。
-决策：将 provider_specs 中 baostock/efinance 视为配置候选；当前默认 market call registry 实际注册的是 Tushare、AkShare/Sina/Tencent 与 EastMoney direct。
待办：无。

## 08:24 [codex]
-完成：只读核对当前 A 股前台资料包与 Tushare 接入是否支持港股，确认项目运行路径不支持 HK；Tushare 官方本身有港股接口但本项目未接入。
-决策：港股不能复用 CN_A 工具隐式处理；需先批准 HK profile/prompt/provider 策略，再新增独立港股资料包或 provider 映射。
待办：无。

## 08:25 [codex]
-完成：实施前线资料包按市场分流：US/CN_A 的 market/fundamental/news/social worker 分别解析到各自工具；US 新增 yfinance-based 资料包，CN_A 保持原资料包。
-决策：删除含糊的共用 frontline intent，插件层强制 US 工具只接受 `market=US`、CN_A 工具只接受 `market=CN_A`；US social 仅提供新闻代理情绪并明确缺少直接社交 API。
待办：下一轮 live US `/report` 前需按 live runtime preflight 启动并确认 provider payload 中 US worker 可见工具已变为 `us_*_data_pack`。

## 08:38 [codex]
-完成：删除上一轮 `docs/evidence/trading` 抓取文件后，按固定 runtime 口径重跑 US AAPL `/report`，run `run-20260513-123123-044d0c18` completed，并重新导出 13 个 worker 的 final prompt / LLM back / report / raw evidence、最终报告和两张图表资产到 `docs/evidence/trading`。
-决策：首次启动因 sandbox 限制 MongoDB bind 失败，按规则提升权限重跑同一 `scripts/start-control-runtime.sh -- <command>`；preflight 确认 no-sidecar、1933/18789 health 通过，前线 provider-visible tools 已变为 `us_*_data_pack` 且未暴露 CN_A pack。
待办：无。

## 08:43 [codex]
-完成：恢复误删误写的原版 TradingAgents 证据目录：将 claw-trade US run `run-20260513-123123-044d0c18` 的误写输出整体移到 `docs/evidence/trading_claw_trade_us_run_20260513_123123_044d0c18_wrong_location/`，并从 Git 对象恢复 `docs/evidence/trading` 的 38 个原版 markdown。
-决策：`git restore` 因 `.git/index.lock` 只读失败，改用 `git archive` 只读导出 HEAD 内容再解包恢复工作区，避免碰 Git index。
待办：如果原目录曾有未跟踪原版文件，需要从外部备份或重新跑原版抓取恢复；Git 只能恢复已提交的 markdown。

## 08:52 [codex]
-完成：生成 US 原版 TradingAgents 与 claw-trade run `run-20260513-123123-044d0c18` 的详细对比分析报告：`docs/evidence/trading_us_original_vs_claw_trade_comparison.md`。
-决策：报告放在 `docs/evidence/` 根下，不写入 `docs/evidence/trading`，避免污染原版证据；结论为部分对齐但不可认定完成 parity，P0 问题是前台 final prompt 导出 sequence 错误、US market/fundamental data pack 覆盖不足。
待办：先修 evidence 导出和 US market/fundamental pack，再做同日同缓存重跑对比。

## 08:58 [codex]
-完成：核对 frontline 调度根因，确认 claw-trade US run 的 4 个 frontline worker 实际并发唤醒；修正文档中“前台顺序”的误导表述为“展示/保存顺序”。
-决策：以 `request.json` 的 `frontline_execution_mode=parallel`、call_id 毫秒级创建时间和 tool-call 重叠时间作为运行证据；不把报告表格排序解释为串行执行。
待办：无。

## 09:16 [codex]
-完成：补强 US 原版 TradingAgents 与 claw-trade 对比报告，新增第 12 节，按 `final_prompt`、`LLM back`、`report` 三件套分析字数、结构、风格、口吻、角色声线和 worker 级偏差。
-决策：把专业文本对比补进原报告 `docs/evidence/trading_us_original_vs_claw_trade_comparison.md`，不触碰原版证据目录；结论更新为输出总字数接近，但 prompt 证据、数据口径、角色声线和 PM action 仍未达到 parity。
待办：无。

## 11:10 [codex]
-完成：从每个 worker 的最后一次 `provider-requests.jsonl` 重新导出 claw-trade US run 的 `*_final_prompt.md` / raw JSON / 全量 provider request，并据此修正 US 原版 vs claw-trade 对比报告。
-决策：把“当前 final_prompt 导出错”的结论改为“证据层已重导出可比，但前台 data pack 粒度、research_manager 材料边界、数据口径和 PM action 仍不对齐”；原版证据目录未改动。
待办：把最后 sequence 导出逻辑固化到正式抓取脚本/导出链路，避免下次回归。

## 11:11 [codex]
-完成：只读追溯 US market prompt 禁止最终组合决策/`FINAL TRANSACTION PROPOSAL` 的来源，确认禁令来自初始 control migration prompt，当前未提交 diff 只改了 US 工具名。
-决策：该禁令把 PM owner 边界和 TradingAgents prompt parity 混在一起；US 原版证据允许 market 输出 actionable 技术建议并出现 `FINAL TRANSACTION PROPOSAL: BUY`，CN_A 现版也只禁止“最终交易建议”前缀而允许技术面建议。
待办：按人工确认修改 US market prompt，删除/改写过宽禁令，并补合同测试防止再次把前台建议禁掉。

## 11:17 [codex]
-完成：删除 prompt 中与原版不相符的过宽禁令：US 前台不再禁止工具前简短 planning prose，US market 允许有证据时给 `FINAL TRANSACTION PROPOSAL`/BUY-HOLD-SELL 技术建议，CN_A market 删除禁 emoji/强制技术分析报告标题并恢复 TradingAgents-CN 风格分节。
-决策：保留不编造数据、来源、工具成功、指标、图表和 unsupported claims 的真实性红线；保留 PM/report_polisher 最终决策边界，不把真相 hard gate 当作“莫名其妙禁令”删除。
待办：后续需 fresh live run 检查 provider payload 和 worker 输出是否实际恢复原版口吻。

## 11:22 [codex]
-完成：定位 US market 缺 50/200 日均线、VWMA 以及 fundamental 缺季度/TTM 口径的根因，确认主要是 US data pack 实现覆盖不足且 provider-visible reader brief 只暴露压缩摘要。
-决策：该问题属于工具数据覆盖/parity 缺口，不是单纯 prompt 文案问题；market 复用了只产出 MA5/10/20 的 techlab 指标引擎，fundamental 只取 yfinance 年度 statement 最新列，未取 quarterly_* 或计算 TTM。
待办：补齐 US market 指标集、fundamental 季度+TTM 数据表，并加 provider payload 合同测试后做 fresh run 对比。

## 11:26 [codex]
-完成：对比原版 TradingAgents yfinance 工具与 claw-trade US pack，确认“同用 yfinance 但输出不一样”的原因是工具形态和后处理不同。
-决策：原版 market 通过 `get_stock_data` + `get_indicators` 按指标名调用 stockstats；原版 fundamental 用独立工具读取 quarterly balance/income/cashflow；claw-trade 当前只暴露 `us_*_data_pack` 的 reader brief，且 pack 未实现这些原版口径。
待办：修复时应恢复原版可见语义和数据覆盖，而不是只在 prompt 里追加要求。

## 11:37 [codex]
-完成：按原版 TradingAgents yfinance 语义修复 US market/fundamental pack：market reader brief 暴露原版指标名窗口和最新值，fundamental reader brief 暴露 TTM 字段与 quarterly income/balance/cashflow CSV。
-决策：保留 OpenClaw 现有 `us_*_data_pack` 工具名，只把工具内部材料恢复成原版语义；market 下载改回复权价口径以贴近原版指标计算。
待办：后续做 fresh `/report` live run，检查真实 provider payload 和最终报告是否不再缺 50/200/VWMA、TTM/季度口径。

## 11:42 [codex]
-完成：定位 prompt 材料边界漂移根因：前台漂移来自 OpenClaw 工具桥只把 data pack 的 `reader_brief` 放进模型可见 tool result；research_manager 漂移来自 manifest/runner 把 frontline+debate 全部作为 investment_decision 输入，并由 US prompt 显式展开四份 research inputs。
-决策：这不是技术上不能像原版，而是迁移时把审计完整性、材料批准链和模型可见 prompt parity 混在一起；审计材料应保留在 manifest/evidence，模型可见材料应按原版 worker turn 精确裁剪。
待办：下一步应把前台恢复为原版多工具 transcript 或等价 transcript，research_manager 改为只看 debate history，同时保留后台审计引用。

## 11:55 [codex]
-完成：把 US 前台四个 worker 的模型可见工具从 `us_*_data_pack` 改回原版工具形态：market 用 `get_stock_data`/`get_indicators`，fundamental 用四个财务工具，news 用 `get_news`/`get_global_news`，social 用 `get_news`；同时把 US research_manager prompt/material vars 改为只接收 debate history。
-决策：CN_A 继续保留 pack 工具；旧 US pack Python 函数仅留内部兼容，不再出现在 agent prompt、stage policy、tool registry 或 plugin registration。
待办：需要 fresh `/report` live run 抓真实 provider payload，确认运行时 prompt 和工具 schema 已按新边界生效。

## 12:17 [codex]
-完成：更新 `AGENTS.md` 与活跃设计/对齐文档，把“原版对齐优先于未经批准的审慎/合规/memo 化”写成硬规则，并明确 truthfulness guard 只能防编造、不能禁止强观点、BUY/HOLD/SELL、final proposition 或辩论声线。
-决策：不改历史 evidence 快照；只改活跃规则文档和 prompt/material 边界文档，避免把事实证据改成事后说明。
待办：下一步按这些规则清理实际 worker prompts，并做 fresh provider-payload 验证。

## 12:22 [codex]
-完成：修复 US 后半段 worker prompt 的角色风格漂移，把 bull/bear/research_manager/trader/risk/portfolio_manager 末尾泛化的 limitation/memo 安全句改成窄真实性红线，并显式保留 bull/bear/risk 的强立场与直接反驳，以及 research_manager/trader/portfolio_manager 的明确 Buy/Sell/Hold、最终交易建议和风险/收益判断。
-决策：只改 US 实际 prompt 和 prompt alignment 合同测试；CN_A 已保持 TradingAgents-CN 强辩论口吻，本次不做无关改写。
待办：后续 fresh `/report` live run 需检查真实 provider payload 和 worker 输出是否按新 prompt 生效。

## 12:27 [codex]
-完成：定位 PM `Hold` 动作语义漂移问题，确认漂移不是词面评级，而是上游 research_manager/trader 把 `Hold` 写成 sell existing longs/go to cash，随后 PM 在最终裁决中继承该执行语义。
-决策：该问题应按“评级与交易动作语义一致性缺口”处理；不能靠 exporter 改写 PM 结论，只能在 prompt/合同测试/必要 guard 中防止 Hold 被解释成清仓动作。
待办：修复 PM/trader/research_manager prompt 与合同测试，明确 Hold/Underweight/Sell 的执行边界并做 fresh provider-payload 验证。

## 12:34 [codex]
-完成：修复 Hold 动作语义漂移：US/CN_A 的 research_manager、trader、portfolio_manager prompt 明确 Hold/持有 只能表示维持当前仓位或暂不新增交易，不能表示卖出现有多头、清仓、转为空仓或 0% exposure；新增 approval hard gate 拒绝 Hold/持有 与退出/空仓动作并存的 L1 材料。
-决策：不让 exporter 改写 PM 结论；在模型输入和材料批准层阻断矛盾语义，保持 PM 权威和证据链一致。
待办：后续 fresh `/report` live run 抓 provider payload 与 PM 输出，验证运行时不再把 Hold 写成清仓。

## 12:45 [codex]
-完成：调用 subagent 对本轮四类 prompt/guard 修改做独立评审，并本地复核其关键发现；确认存在 PM owner guard 被误删、测试接受缺少结构化 PM 决策、Hold 动作语义 guard 扫全文导致误报/漏报等问题。
-决策：当前修改不能进入 live 验证，应先恢复 PM 最终决策权保护并收窄 action semantics guard 的判定范围。
待办：恢复 `pm_owner` 保护与测试，修正 `action_semantics` 的 final-decision 解析和边界样例，再跑 focused tests 与 fresh provider-payload 验证。

## 12:58 [codex]
-完成：恢复 `pm_owner` guard、PM owner 合同测试、approval/exporter/export-claims 中的 PM 决策证据校验；收窄 Hold 动作语义 guard 为最终决策行判定，允许正文引用 Hold 和无持仓时 maintain cash。
-决策：不禁止 `Hold` 关键词本身，只禁止最终 Hold/持有 与卖出现有仓位、清仓、转空仓或 0% exposure 执行动作并存。
待办：后续 fresh `/report` live run 需检查真实 provider payload、PM decision evidence 与最终报告导出链路。

## 13:07 [codex]
-完成：撤销 12:58 对旧 PM 结构化 sidecar 与 Hold runtime hard gate 的恢复；活跃 `src/`/`tests/`/`agents/`/`docs/` 扫描不再命中 `pm-decision`、`pm_owner` 或 `action_semantics`。
-决策：原版 TradingAgents 没有 `pm-decision.json` 或 Hold 动作 runtime hard gate；PM 权威回到已批准自然语言 L1 report，Hold 语义只保留在 prompt 对齐层，未经人类批准不再加原版没有的运行时禁令。
待办：fresh `/report` live run 仍需验证真实 provider payload、PM 自然语言裁决与最终报告导出是否对齐原版。

## 13:15 [codex]
-完成：把“runtime guard/gate 默认冻结”写入 `AGENTS.md` 和 `docs/详细设计.md`，并强化 `test_guard_change_requires_approval.py`：当前批准的 guard 文件集合与 early-stop 类别集合被合同测试锁定。
-决策：不靠 Codex 记忆防偏离；新增/恢复 runtime guard、hard gate、early-stop category、输出语义 validator 或 exporter 拒绝规则必须先有原版证据、活跃设计条款或人类明确批准，并显式留下 `Guard source:`。
待办：后续涉及输出漂移时，默认走 prompt/material/workflow 对齐与 provider-payload 证明，不走 runtime gate。

## 13:19 [codex]
-完成：重新评审 runtime guard freeze 与 PM sidecar/Hold gate 撤销改动；确认活跃 `src/tests/agents/docs` 中仅剩 disallow 文档提及 `pm-decision`，但 guard freeze 合同仍有覆盖缺口。
-决策：主要风险是 allowlist 只锁住 guard 文件名和 early-stop 类别，尚未锁住现有 guard 文件内容、claim dictionary 内容或 `src/claw_trade/guards/` 之外的 guard-like rejection 逻辑。
待办：如继续修复，应补强合同测试以扫描/锁定 guard-like rejection surface，并把 PM 自然语言通过测试的 fixture 改成真实 PM 裁决文本。

## 13:30 [codex]
-完成：按复评意见补强 guard freeze 合同测试，锁定现有 guard 文件内容和 guards 外部拒绝调用面；PM 通过测试改用真实 Hold 自然语言裁决文本；完成 US/CN_A active prompt、工具映射、PM sidecar/gate、旧 pack 残留扫描。
-决策：旧 `us_*_data_pack` 问题主要残留在历史证据报告和兼容测试/内部函数中；当前运行面已映射回原版工具名，PM sidecar 和 Hold/Buy/Sell hard gate 只作为禁止恢复项存在。
待办：后续若重跑 live US 对比，需要用新运行证据刷新旧 comparison 文档里的历史结论。

## 13:50 [codex]
-完成：按 live runtime preflight 先读 `AGENTS.md`/memory，再执行 US AAPL fresh `/report` run `run-20260513-173747-4d13dcce`，导出 13 个 worker 的 final prompt / LLM back / report Markdown-only 证据，并生成原版对比与手工结论文档。
-决策：本轮不保存新的 JSON 交付证据；运行内部 JSON 只作为 runtime 原始证据留在 `runs/`。run 终态 failed，原因是 `export_report_assets` 找不到可复制 chart asset；分析结论为 prompt/tool 风格已明显回到原版方向，但 market 未纳入 VWMA、图表资产链路断、action-semantics prompt 仍是非原版残留。
待办：下一步修 US market VWMA/tool-call parity、chart asset 生成/复制链路，并决定是否删除非原版 action-semantics prompt 说明。

## 14:40 [codex]
-完成：修复上一轮 US fresh run 残留：删除 US/CN_A decision prompts 中非原版 action-semantics 说明；US market prompt 引导成交量确认时纳入 `vwma`；US `get_stock_data` 生成本地技术图表资产。focused tests 126 passed，fresh `/report` run `run-20260513-183108-040445c6` completed，final export passed，并生成 43 个 Markdown-only 证据文件到 `docs/evidence/trading_claw_trade_us_fresh_live_run-20260513-183108-040445c6_md/`。
-决策：不新增 runtime gate/guard，不恢复 PM sidecar；用 prompt/tool/output parity 和真实 provider payload 证明修复。运行内部 JSON 仅留在 `runs/`，交付证据目录只保存 Markdown。
待办：继续评审最终报告与原版差异时，应以本轮 completed run 的 `original_vs_claw_fresh_live_detailed_analysis.md` 和正式 `final_report.md` 为准。

## 14:44 [codex]
-完成：按用户要求新增 `final_prompt_llm_report_comparison.md`，针对 US fresh run 与原版逐项对比 final prompt、LLM back、worker report 和最终导出报告，明确已修复项与剩余篇幅/形态差异。
-决策：报告指出当前主要残留是 market final prompt 过长、bull/bear 输出偏长、最终报告仍是 reader-facing report_polisher 层；不是 PM sidecar、Hold gate 或 action-semantics 残留。
待办：如继续追求更严格原版形态，下一步应控制 market 工具结果窗口/体积和 bull/bear verbosity。

## 17:53 [codex]
-完成：修复 US 最终报告图片位置：导出器新增识别 `Technical Market Analysis` 标题，测试覆盖 US 标题下图表插入；同步把当前 `run-20260513-183108-040445c6` 报告和 evidence copy 的图表块移动到技术分析段。
-决策：这是 exporter 标题识别/排版问题，不新增任何 guard/gate，不改 PM 结论或报告正文。
待办：后续 fresh/live 导出会自动按该标题插入图表；如旧历史报告需要同步，需要逐份按同一规则迁移。

## 18:02 [codex]
-完成：按 profile 固化 US/CN_A 模型可见材料边界差异：将误导性的 `build_cn_a_prompt_vars` 改名为 `build_profile_prompt_vars`，补强 CN_A research_manager 测试断言，并在 `AGENTS.md` / prompt 对齐手册写明 US research_manager 只围绕 bull/bear debate history，CN_A 保留综合前台报告 + debate。
-决策：这是 prompt/material 边界加固，不新增 guard/gate；audit `upstream_materials` 可以比模型可见 prompt vars 更宽，但不能据此把所有上游报告暴露给 LLM。
待办：如继续压缩 US market transcript，应在 US 工具结果窗口/体积上对齐原版，不触碰 CN_A 材料边界。

## 21:28 [codex]
-完成：按复评意见补强 US/CN_A research_manager placeholder 边界合同，重新跑 US AAPL fresh `/report` run `run-20260514-011505-506c5fb4` 并完成 Markdown-only 证据抓取与 final prompt / LLM back / report 对比分析。
-决策：继续用 prompt/material 边界和真实 provider payload 证明对齐，不新增 runtime gate/guard；本轮证据目录为 `docs/evidence/trading_claw_trade_us_fresh_live_run-20260514-011505-506c5fb4_md/`，运行内部 JSON 只留在 `runs/`。
待办：若继续追求极限原版 parity，主要剩余决策是最终报告层保留 reader-facing polisher，还是改成原版式 analyst reports 串联导出。
