## 00:19 [codex]
-完成：修复 fundamental claim gate 将“缺失毛利率/净利率/资产负债率/ROA”等证据缺口表述误判为 unsupported claim，并补齐相关 focused tests。
-决策：仅在 claim gate 的 metric 解析阶段增加“缺口语境”过滤，且保留数值/强断言语境下的拦截，避免放宽 hard gate。
待办：如后续出现“无法判断是否改善”类边界句误判，再补充更细的否定-断言句法区分测试并迭代规则。

## 00:20 [codex]
-完成：完成 T-FRONTLINE-FUNDAMENTAL-MISSING-FIELDS-RCA-001 调查，确认毛利率/净利率/资产负债率缺失的主因是 Tushare 字段未进入当前 canonical 映射与 quality 核心集合，而非上游接口空值。
-决策：在不改代码前提下先做 provider payload+本地 Tushare 直连 probe 双证据核对，优先排除“上游没返回”再定位为映射/口径边界问题。
待办：如需修复，下一步应明确新增字段 canonical 命名（含 grossprofit_margin→gross_margin 是否统一）并同步 provider 映射、mapper 别名与 claim gate 字段路径合同。

## 00:24 [codex]
-完成：完成 T-FRONTLINE-FUNDAMENTAL-TUSHARE-FINANCIAL-FIELDS-001，修复 Tushare `fina_indicator` 的 ROA/毛利率/净利率/资产负债率在 provider→mapper→fundamental 资料包链路中的丢字段问题，并通过指定 focused 单测。
-决策：仅补字段透传与映射（含 `grossprofit_margin -> financial_indicators.gross_margin`），不改 quality complete 核心口径，避免越权扩大判定面。
待办：后续如要把新增字段纳入 complete 核心，需要单独设计与验收任务，不在本任务内。

## 00:31 [codex]
-完成：完成 T-FRONTLINE-LIVE-FUNDAMENTAL-AFTER-GUARD-AND-FIELDS-001 实跑复验，CN_A frontline `fundamental_analyst` 单 worker 已完成且 guard 通过，provider payload 中确认新增财务字段与来源映射已生效。
-决策：首次运行受沙箱网络限制失败后按规则申请 loopback/network 提权重跑，避免使用任何 mock/fallback 绕过真实链路。
待办：无。

## 01:17 [codex]
-完成：完成 T-FRONTLINE-4W-LIVE-AFTER-FUNDAMENTAL-FIX-001，真实串行跑通 CN_A frontline 四个 worker，并在证据目录输出 final prompt / llm_back / report / tool calls / guard / provider requests 扁平证据与汇总索引。
-决策：market 首次因沙箱 loopback 权限失败后提权重跑；`llm_back` 严格按 `openviking.write_material` 的 `input.content` 抽取；记录四 worker final prompt 均命中 `viking://` 作为 concern。
待办：主 agent 按 `summary.md` 处理 prompt 机器字段污染与 news/social provider 合同/鉴权问题。

## 01:21 [codex]
-完成：完成 `frontline_4w_after_guard_fields_fix_20260510_0035/cn_comparison_report.md`，按旧版详细格式对比 final prompt、LLM back、report、工具链和 CN 原文差异。
-决策：结论标为 `DONE_WITH_CONCERNS`，区分“运行链路通过”和“内容质量未收口”；把 final prompt 机器内容过多、news/social 增强源失败、软判断 guard 不足列为下一步重点。
待办：下一步优先修 worker 输入形态、soft-claim guard、news/social provider。

## 01:28 [codex]
-完成：回查 memory 与工具链代码，确认 final prompt 机器化回归的根因不是写入目标 prompt 模板回退，而是工具返回层仍把完整 PackEnvelope 作为 model-facing `content` 返回。
-决策：将问题归类为“model-facing content 与 audit/details 未分离”；既有测试只覆盖 prompt 模板和 `reader_brief` 干净，没有覆盖真实 provider request 中工具返回内容是否只含中文 brief。
待办：修复 `claw-trade-frontline-tools/index.js` 的工具返回形态：模型只看 `reader_brief`，完整 pack 留在 `details`/审计文件，并补 provider request 回归测试。

## 01:42 [codex]
-完成：修复 frontline 工具返回把完整 PackEnvelope 暴露给模型的问题，模型侧只看中文 brief；同时收窄 frontline guard，只保留造假/越权类硬拦截，并接入 AkShare `stock_info_global_em` 与 `news_cctv` 两个真实 news 源。
-决策：完整机器包保留在 `details` 供审计和程序使用，不再作为模型正文；软叙事词不再 hard fail，硬门继续拦无证据指标、目标价、买卖评级、新闻短线价格预测等。
待办：Bocha/Tavily/Jina/NewsNow/MiniMax 仍缺真实 key 或调用合同，不能假装补齐；下一步需要按真实 API 合同接通或明确记录缺权限/缺合同。

## 01:51 [codex]
-完成：为 Bocha/Tavily/Jina/NewsNow/MiniMax 补齐 `.env.example` 与 `.env.local` 配置入口，并让 frontline config 读取 NewsNow/MiniMax key、MiniMax 国内 base URL 和 2.7 模型口径。
-决策：缺 key 不在配置加载阶段阻断流程，交给真实 provider attempt 报 AUTH_MISSING；MiniMax 默认使用 `https://api.minimaxi.com/v1` 和 `MiniMax-M2.7`。
待办：后续实现真实 Bocha/Tavily/Jina/NewsNow/MiniMax caller 时必须使用这些配置，不得再写死国外地址或假成功。

## 02:14 [codex]
-完成：对比当前四个 CN_A 用户 prompt 与已抓取 TradingAgents-CN final prompt，确认当前模板已显著瘦身，但 provider request 仍存在 OpenClaw runtime 系统包装差异。
-决策：把剩余差异分为两类：用户 prompt 模板层已接近“资料工具+写报告”；运行层仍追加英文 runtime/system truthfulness block，需要单独处理。
待办：若继续追求“除数据工具和 Viking 工具外像 CN”，下一步应收窄 `single_worker_minimal` 系统上下文与自动追加的 `[RuntimeTarget]/[ReportSubmission]` 文本。

## 02:21 [codex]
-完成：删除/收窄 frontline prompt 口吻类合同测试，只保留资料包工具、`openviking.write_material` 和不手写机器协议字段的最低测试，并验证相关最小测试通过。
-决策：不再用测试约束目标价/评级/情绪分/报告章节等 CN prompt 文案；出 report 与不作假交给工具链、OpenViking 写入和真实证据 gate。
待办：下一步重跑 frontline 四 worker，检查 final prompt、LLM back 和 report 是否按 CN 口吻正常产出。

## 02:31 [codex]
-完成：重新按已抓取 TradingAgents-CN final prompt 改写四个 frontline CN_A prompt，并输出 `docs/evidence/frontline_prompt_feedback/cn_prompt_rewrite_comparison_20260510.md` 对比报告。
-决策：保留 CN 的工具优先、报告结构、目标价/评级/情绪分等原有口径；仅替换为 claw-trade 资料包工具、运行变量、`openviking.write_material`，并保留不编造数据的底线。
待办：下一步重跑四个 frontline worker，确认真实 provider final prompt、LLM back 和 report 是否符合预期。

## 02:54 [codex]
-完成：重跑 CN_A frontline 四个 worker，并输出 `frontline_4w_cn_prompt_rewrite_live_20260510_0235/cn_comparison_report.md`，逐项比较 final prompt、LLM 写入正文和 report。
-决策：判定执行链路与 OpenViking 写入已跑通，但 provider 摘要层未达标；market/fundamental 关键数值没有充分自然语言化，news/social 有效正文为空，social 报告出现无证据情绪与价格内容。
待办：优先修 social/news provider 摘要，再修 market/fundamental 摘要暴露具体指标；修完后重新跑 frontline 对比。

## 03:05 [codex]
-完成：回查 news/social 模型无有效数据的要因，确认直接根因是 shared runtime 返回 PackEnvelope dataclass 未经 `to_jsonable()` 转 dict，Node 侧只能给模型兜底提示；并将根因补充写入本轮 `cn_comparison_report.md`。
-决策：修正判断口径：本轮不能证明贵州茅台本身无新闻/社交数据，只能证明模型没收到 news/social reader_brief；market/fundamental 则是 reader_brief 过度摘要化。
待办：修复 news/social adapter 序列化；随后重跑以区分“真实无新闻”和“provider 能力不足”。

## 03:11 [codex]
-完成：确认“只传摘要”和“映射未闭合”的代码要因：工具层只把 `reader_brief` 给模型，当前 brief 是审计摘要；news/social 额外存在 dataclass 未转 dict 的返回错误；Bocha/Tavily/Jina/NewsNow/MiniMax 目前只有配置入口和 provider spec，真实 caller/response mapping 未闭合。
-决策：KEY 已配置不等于 provider 闭合；下一步应先修模型可见自然语言事实正文和 news/social 序列化，再逐个按真实 API 合同实现搜索源 caller 与映射。
待办：等待实施确认后修复上述两类根因，并重跑 frontline 对比。

## 03:25 [codex]
-完成：完成对“纯中文材料流+Viking 精简+后置轻量防造假”方案的 report-only 反方评审；确认 news/social dataclass 序列化 bug 为真，并指出“移除 Viking 原始 payload”会直接击穿现有证据链验收条件。
-决策：结论为 DONE_WITH_CONCERNS；支持修序列化、补真实 provider 合同、提升模型可见中文事实正文，但反对将 hard gate 退化为仅报告后轻量检查，也反对在未改验收链前去掉 raw payload 的可追溯锚点。
待办：需人类拍板两项边界——是否保留最小可追溯原始证据锚点（ref+hash），以及后置轻检与硬门禁的职责分层。

## 03:29 [codex]
-完成：确认新的 workflow 边界：worker 之间的信息传递不得使用 JSON，中转材料必须像 TradingAgents-CN/原版一样是自然语言材料或 report。
-决策：JSON/结构化数据只能作为 provider 内部临时处理或开发日志存在，不进入 Viking workflow 主材料、不进入 worker prompt、不作为 worker handoff；必要来源说明也应以自然语言脚注/来源行表达。
待办：后续实施时把 frontline 资料包主输出改为中文事实正文，并清理 Viking/worker handoff 中的 JSON 依赖。

## 03:38 [codex]
-完成：实施 workflow 自然语言材料边界：news/social 工具返回统一 `to_jsonable()`，四个 frontline pack 的模型可见正文改为中文事实材料，文档补充“worker handoff 禁止 JSON”裁决，并更新相关测试。
-决策：内部结构化记录仍只作为工具协议、provider 处理、cache/evidence/log 使用；模型与 worker handoff 只看中文材料正文，不暴露 raw payload、attempts 或机器字段。
待办：下一步重跑 frontline 四 worker，确认 final prompt、LLM back 和 report 不再出现 JSON/机器摘要，并继续补真实 news/social 搜索源映射。
## 03:55 [Codex]
-完成：重新跑 frontline 四个 worker，抽取 final prompt、LLM back、report/候选报告，并写出与 CN 基线的详细对比报告。
-决策：news/social 因未写入 OpenViking 只能作为未批准候选输出，不能当正式 artifact；当前优先修提交闭合和过度推演。
待办：修复 news/social 写入闭合，收紧 fundamental/social 报告边界后重新跑 frontline。
## 04:28 [Codex]
-完成：修复 frontline worker 生成正文但未提交 OpenViking 时的提交闭合问题，并用真实 frontline run 验证四个 worker 都拿到 write receipt。
-决策：不让 Python 写报告；只让 OpenClaw 在缺 receipt 时追加一次“只提交、不重写”的同 worker 提示，仍由模型调用 `openviking.write_material`。
待办：继续收紧 fundamental/social 的报告边界，避免无证据目标价、PEG、情绪分和交易建议。

## 04:37 [codex]
-完成：完成 T-PROMPT-FRONTLINE-CN-A-BOUNDARY-20260510，四个 frontline CN_A prompt 已改为“证据驱动结论”，移除对目标价/评级/情绪分/短期股价预测的硬性产出要求，同时保留工具调用与 `openviking.write_material` 提交流程。
-决策：保留 TradingAgents-CN 报告感与章节结构，不改代码/provider/runtime，只在 prompt 中将高风险结论改为“有证据才可写、证据不足必须明示”。
待办：后续可用真实 frontline run 复核四个 worker 输出是否稳定落在“事实+不确定性”边界内。

## 04:55 [codex]
-完成：重跑 frontline 四个 worker，抽取 final prompt、OpenViking 写入正文和 report，并写出 `frontline_4w_prompt_boundary_live_20260510_0445/cn_comparison_report.md` 详细对比报告。
-决策：本轮确认四个 worker 的 OpenViking write receipt 均 verified，news/social 已拿到自然语言材料；剩余问题收敛为 fundamental 证据外扩写、social 标题/推断偏满、market reader_brief 来源统计矛盾。
待办：下一步建议只做小修：收紧 fundamental/social 表述，并修复 market `60 个交易日` 与 `0 条可用行情记录` 的统计矛盾后再跑一轮。

## 05:03 [codex]
-完成：完成 T-FRONTLINE-PROMPT-BRIEF-TIGHTEN-20260510 的三项小修：收紧 fundamental/social CN_A prompt 边界，并修复 market reader_brief 材料正文不再写入 `accepted_count=0` 来源。
-决策：采用最小实现，仅在 market 资料正文来源摘要增加 `accepted_count > 0` 过滤；0 条来源仍保留在“来源概况”统计链路，不改 PM/hard gate/调度边界。
待办：建议按同一 run 配置再跑一轮 frontline，确认 fundamental/social 新口径在真实输出里稳定生效。

## 05:05 [codex]
-完成：总管复核三项小修，重跑 `tests/unit/test_frontline_market_data_pack.py` 全文件，16 个 focused 单测通过。
-决策：本轮只确认代码和 prompt 小修，不重跑 live frontline；下一步若要确认真实报告效果，需要单独启动 frontline live run。
待办：等待是否重跑 frontline 并重新生成 CN 对比报告。

## 10:18 [codex]
-完成：修复 L1/L2 guard 对“L1 文本与 raw_output 相同”的误杀，保留正式 L1 仅由 OpenViking receipt/readback 链路提供，并用 focused contract tests 验证通过。
-决策：删除仅凭文本相等即失败的分支，避免把真实 `openviking.write_material` 提交正文误判为 raw_output fallback；“无 receipt/readback 必须失败”继续由 approval candidate 合同测试覆盖。
待办：无。

## 10:29 [codex]
-完成：梳理当前仓库 guard/hard gate 拒绝规则，确认 20260510_0445 frontline 证据目录内四个 worker 的 `guard-results.json` 均为通过。
-决策：将规则分为运行证据、OpenViking/材料批准、claim/PM/export、fundamental/social 专用 gate 和 early-stop 类别，避免把 gstack `/guard` 安全模式与 claw-trade 业务 guard 混淆。
待办：如需定位“每次被拒绝”的真实触发点，下一步应提供失败 run 目录或 guard-results/approval-hard-gate 文件。

## 10:40 [codex]
-完成：将 fundamental claim gate 测试口径改为“软叙事词不 hard gate”，保留目标价、买卖评级、估值判断和缺证据指标的硬拦截，并跑 focused contract tests 通过。
-决策：`龙头/护城河/定价权` 这类口吻问题交给 prompt 与质量评审，不再因为咬文嚼字阻断 frontline 报告生成。
待办：后续如要继续收敛报告口吻，应改 prompt 或做开发期 lint，不要再加正式流程 hard gate。

## 10:58 [codex]
-完成：重跑 frontline 四个 worker，抽取 final prompt、LLM back、report 和 tool calls，并写出 `frontline_4w_after_guard_softening_live_20260510_1045/cn_comparison_report.md`。
-决策：本轮四个 OpenViking report 均 verified；fundamental guard 放松后流程稳定，news 退化为 `news.news_data_pack python stdout is not valid JSON`，下一步应先修 news 工具协议。
待办：修复 news 数据包 stdout 协议错误，再收 market/social 的交易口吻和关键词外推。

## 11:31 [codex]
-完成：定位并修复 news 工具失败根因：`reader_brief` 截断到 3000 后又补句号导致 3001 字符 schema 失败；补充回归测试并确认真实 news 数据包返回 complete。
-决策：只修自然语言摘要长度边界与外层误导性错误信息，不改 workflow、prompt、OpenViking 写入或 worker 间自然语言传递方式。
待办：可重跑 frontline 四个 worker，确认 news report 不再因资料包失败退化。

## 11:51 [codex]
-完成：执行 T-FRONTLINE-4W-RERUN-AFTER-NEWS-BRIEF-FIX-20260510，完成 frontline 四 worker 真实复跑、抽取 final prompt/LLM back/report/tool calls，并产出详细 CN 对比报告 `frontline_4w_after_news_brief_fix_live_20260510_1135/cn_comparison_report.md`。
-决策：保留真实运行证据口径（不做 mock/fallback）；news 修复结论仅基于本轮可验证证据（工具成功+真实新闻正文进入 prompt+L1 写入 verified），未把本轮未出现的字段名当作已复核事实。
待办：后续可优先收敛 fundamental/social 的证据外推表述，并统一 market/social 的质量状态与缺口语义一致性。

## 12:09 [codex]
-完成：完成 T-FRONTLINE-FULL-RUN-CN-COMPARISON-20260510 的完整 frontline 单 run（无 `--target-worker-id`）复验与对照分析，输出 `frontline_full_after_news_fix_live_20260510_1154/cn_comparison_report.md`，覆盖四 worker 顺序、工具状态、OpenViking hash/receipt/manifest 一致性、news 修复证据及与 CN/近几轮差异。
-决策：首次沙箱执行因 uv 缓存只读失败后按权限规则提权重跑 authoritative run；结论定为 DONE_WITH_CONCERNS，避免过度声明“完全 CN 对齐”。
待办：下一轮优先收敛 market/fundamental/social 的证据外推与口吻边界问题。

## 12:33 [codex]
-完成：重写 `frontline_full_after_news_fix_live_20260510_1154/cn_comparison_report.md`，补齐并纠正 final prompt/LLM back/report 三层抽取与 CN 逐项对比。
-决策：修正 final prompt 口径为“写报告前一轮 provider request”，不再把包含 `openviking.write_material` 的后一轮请求误当 final prompt。
待办：基于新版报告优先修 fundamental PE 口径冲突和 social 质量状态/证据缺口冲突。

## 13:29 [codex]
-完成：按 CN 基线重写 frontline 四个 worker 的 CN_A prompt，恢复 market/fundamental/news/social 的投资建议、目标价、影响评估和情绪交易时机等 CN 报告结构。
-决策：只保留当前实际数据包工具与 `openviking.write_material` 提交工具差异，不再把前期过度 guard/工程协议文案写进四个 worker 源 prompt。
待办：重跑 frontline 并对 final prompt、LLM back、report 与 CN 做三层对比。

## 13:36 [codex]
-完成：收掉 OpenClaw single-worker 运行层对 final prompt 的 RuntimeTarget/ReportSubmission 自动追加，并把默认英文占位句从有 profile prompt 的回合中移除。
-决策：运行目标与提交要求由 CN_A profile prompt 自然表达；OpenClaw 只保留极简中文 system context 和必要的上游材料目录追加，避免再次污染 analyst prompt。
待办：重启/重跑 frontline，确认 provider final prompt 不再出现运行层协议块。

## 13:43 [codex]
-完成：进一步清理 single-worker runtime 追加链路，移除旧 renderer/默认英文触发句，新增中文默认触发常量并只保留 legacy section 剥离逻辑。
-决策：`[RuntimeTarget]`、`[ReportSubmission]`、旧 OpenViking 段只作为历史脏段清理对象存在，不再由运行层生成；有 profile prompt 时默认触发句不会进入 provider prompt。
待办：重跑 frontline live，验证真实 provider payload。

## 13:46 [codex]
-完成：清理 `docs/evidence` 下历次 claw-trade/live/frontline 测试抓取文件，保留 `cn_a_*` 目录和 `tradingagents_cn_*` CN 基线文件。
-决策：采用白名单保留策略，避免误删未来对比需要的 TradingAgents-CN final prompt、LLM back、report 和执行记录。
待办：无。

## 13:49 [codex]
-完成：按用户澄清重新收窄 `docs/evidence` 保留范围，删除 `cn_a_*` 目录，仅保留 `tradingagents_cn_*` 抓取基线。
-决策：之后“CN 文件/基线”默认只指 TradingAgents-CN 抓取文件，不包括 claw-trade 的 CN_A 验收或模板证据。
待办：无。

## 14:21 [codex]
-完成：重建 OpenClaw dist 后重新跑通 frontline 四 worker，抓取 final prompt/LLM back/report，并输出 `frontline_full_cn_aligned_live_20260510_180738/cn_comparison_report.md` 详细对比。
-决策：第一次 run 因 live gateway 仍加载旧 dist，final prompt 仍含 RuntimeTarget/ReportSubmission，判为失效样本；正式比较只采用 `run-20260510-180738-9e2c8de4`。
待办：修 market 工具参数 CN_A 明示、fundamental TTM/EPS 估值口径、social 热度数据过度推断。

## 14:23 [codex]
-完成：把“改 OpenClaw src 必须重建 dist、重启 runtime、用新 provider payload 验证”的硬规则写入 `AGENTS.md`。
-决策：明确 source/unit test 不等于 live runtime proof；未 build 或未重启只能标记 `NOT VERIFIED`，不能用旧 dist 证据证明改动生效。
待办：无。

## 14:30 [codex]
-完成：优化 frontline 三处口径：market/fundamental/social 工具参数显式 `market: CN_A`，fundamental reader brief 增加 PE TTM 与 EPS 口径说明，social prompt 改为无文本证据时只写热度报告。
-决策：social 暂不新增文本 provider，本次按用户给定的第二路径先收紧 prompt；基本面资料包避免把报告期 EPS 和 PE TTM 混算。
待办：下一轮 live run 验证 final prompt、LLM back、report 是否按新口径收敛。

## 14:49 [codex]
-完成：完成 `run-20260510-183457-a34eb258` frontline live 复跑，提取四个 worker 的 final prompt、LLM back、report，并输出 `frontline_full_after_three_fixes_live_20260510_183457/cn_comparison_report.md`。
-决策：final prompt 按“生成报告正文前的 provider request”提取；submit-only repair prompt 单独保存，不冒充报告生成 prompt。
待办：修 news 工具参数 `market: CN_A`、fundamental 目标价假设边界、fundamental/news submit-only repair 触发。

## 14:54 [codex]
-完成：修复 `news_analyst` 的 CN_A prompt 中工具参数要求，明确 `news.news_data_pack` 必须传 `market="CN_A"`，并禁止写成 `CN` 或“中国A股”。
-决策：仅做 prompt 自然语言最小补丁，不改运行层、不加 guard、不改其他 worker。
待办：执行一次 frontline 定向验证，确认 news 首次工具调用不再因 `market=CN` 失败。

## 14:55 [codex]
-完成：修复 fundamental 估值口径边界：在 CN_A prompt 与 fundamentals reader brief 同步加入“事实与假设分离”要求，允许给价格区间结论但禁止把全年 EPS/行业或历史估值推演伪装成资料包事实，并补齐对应单测断言。
-决策：仅在允许范围内做最小补丁，不新增 guard、不改控制流、不压缩报告结构；reader brief 用“价格区间测算结论”表述以兼容现有 brief 结论词校验。
待办：按 `/report` 入口做一次 fundamental live 验证，确认真实报告里目标价相关语句稳定携带“情景假设”标签。

## 14:59 [codex]
-完成：完成 fundamental/news 触发 submit-only repair 的 report-only 根因调查，确认工具可见性正常、repair 触发条件命中，以及 primary provider payload 未设置 `tool_choice` 约束。
-决策：将根因归类为“模型在 primary 回合以自然语言收尾而未发写入工具调用”叠加“运行层缺少强制 tool_choice 策略入口”；repair 属于兜底机制，不是首因。
待办：等待主 agent/人类在“仅 prompt 收敛”与“OpenClaw generic seam 增加 tool_choice 控制（NEEDS_DECISION）”之间拍板。

## 15:24 [codex]
-完成：修复 OpenClaw runtime seam：在 provider payload `onPayload` 阶段识别 frontline 已有非 write 成功 `tool_result` 且尚未 write 时，直接注入 Anthropic 原生 `tool_choice={type:"tool",name:"openviking.write_material"}`，避免继续走 submit-only repair。

## 18:48 [codex]
-完成：修复 MiniMax Anthropic-compatible payload 的 thinking 覆盖逻辑，确保即使上游已设置 `thinking: {type:"enabled"}`，最终发给 MiniMax 的 payload 仍强制为 `{type:"disabled"}`，并补充 focused test。
-决策：保持修复最小化，只改 OpenClaw 通用运行层 seam（`minimax-stream-wrappers`）与对应 extra-params 聚焦回归测试，不改 provider 策略、forced-tool 策略或 workflow 逻辑。
待办：建议后续由主流程在真实 MiniMax live run 里复核 provider payload 与超时现象是否一并收敛（本任务按要求未跑大集成 live）。

## 16:56 [codex]
-完成：完成 FRONTLINE-MARKET-DAY-DELTA-001，确认 market reader brief 实际包含涨跌事实但字段名未显式写成“日涨跌额/日涨跌幅”，并最小修复为显式字段名输出，focused 单测通过。
-决策：仅修改 market reader brief 的自然语言字段名与对应断言，不改 prompt/guard、不改调度或数据源逻辑，避免扩大变更面。
待办：下次 frontline live 需复核 `extraction_summary.json` 中 `market_reader_brief_has_day_delta_amount` 是否转为 true。
-决策：保留现有 `options.toolChoice` 预注入逻辑，同时新增 payload 级判定与注入，不改 prompt、不加 guard、不改 claw-trade 业务控制流。
待办：按 OpenClaw 源码改动验收规则，后续需 build+重启 runtime 并用 fresh live run 的 provider payload 复证线上行为。

## 15:44 [codex]
-完成：完成 `run-20260510-193254-dda8cea5` frontline live 复跑，导出四 worker initial/final/post-submit prompt、LLM back、report、visible tools，并产出 `frontline_full_after_payload_pin_live_20260510_194058/extraction_summary.json` 与 `cn_comparison_report.md`。
-决策：比较口径继续使用 TradingAgents-CN 基线；final prompt 仍按“报告生成回合”提取，submit-only repair 单独落盘，不作为报告生成 prompt。
待办：继续收敛 submit-only repair（本轮四 worker 仍触发），保持 payload tool_choice 与 receipt 成功率不回退。

## 16:08 [codex]
-完成：修复 MiniMax Anthropic 兼容流把 `<minimax:tool_call><invoke ...>` 文本漏成普通 text 的问题：在 `anthropic-transport-stream` 把纯 XML 工具调用块转成结构化 `toolCall` 事件/内容，并补齐单测覆盖；随后完成 OpenClaw `pnpm build`。
-决策：仅在 OpenClaw 通用 runtime seam 做最小变更，不改 prompt/guard/business；仅当文本块是“纯 XML 工具调用”才转换，混合普通正文的示例文本保持原样。
待办：在重启 runtime 后做 fresh live run，确认 frontline 四 worker 的 write-material 回合不再落入 submit-only repair。

## 16:18 [codex]
-完成：完成 `run-20260510-200838-638d4237` frontline 四 worker live 复跑，落盘 `frontline_full_after_minimax_xml_toolcall_live_20260510_161737` 全套证据（initial/final/repair/post-submit prompt、LLM back、report、visible tools、provider requests、receipt、checks）并输出逐 worker TradingAgents-CN 详细比较报告。
-决策：final prompt 继续使用“报告生成回合（repair 前一条）”口径；重点核查 MiniMax XML 污染、`tool_choice=openviking.write_material`、submit-only repair 与 receipt，确保不把 repair prompt 冒充 final prompt。
待办：修 `market_analyst` 报告中的“涨跌幅待计算”，并继续把 `market/fundamental/social` 的 submit-only repair 清零。

## 16:26 [codex]
-完成：完成 FRONTLINE-MARKET-CHANGE-PCT-SOURCE-001：market data pack 资料源补齐日涨跌额/涨跌幅事实链路，reader brief 在有数据时输出真实数值、缺数据时输出明确缺失说明，移除“待计算”占位风险。
-决策：最小改动落在 frontline data pack 的 provider→normalizer→evidence summary，不改 prompt/guard/runtime；优先使用源字段 `pre_close/change/pct_chg`，缺失时再用前一交易日收盘回推。
待办：无。

## 16:33 [codex]
-完成：完成 FRONTLINE-MINIMAX-FORCED-TOOL-TEXT-RECOVERY-002：在 OpenClaw MiniMax Anthropic-compatible transport 增加“forced `tool_choice` + 单一 `content:string` 工具 schema + 非空 text 且无结构化 toolCall”时的文本恢复为 `openviking.write_material` toolCall，并补齐 focused tests；`pnpm build` 已完成。
-决策：保持严格触发条件且仅在 MiniMax provider 生效，不改 prompt/guard/claw-trade 业务；保留既有 MiniMax XML tool_call 恢复路径优先。
待办：按 OpenClaw 运行层验收规则重启 runtime 并做 fresh live provider payload 复证 submit-only repair 是否清零。

## 16:49 [codex]
-完成：完成 FRONTLINE-LIVE-AFTER-FORCED-TOOL-RECOVERY-AND-MARKET-PCT-001 的 runtime 重启后真实 frontline 四 worker 复跑（run-20260510-203727-2b9459d0），并产出新证据目录 `frontline_full_after_forced_tool_recovery_live_20260510_164516`（含 initial/final/post-submit/repair prompt、LLM back、report、visible tools、provider requests、tool calls、extraction summary、CN 对比报告）。
-决策：严格沿用“final prompt=首次 `openviking.write_material` 前一条 provider request；LLM back=write_material.content 原文”口径，且按证据修正结论为“market repair 清零，fundamental/news/social 仍 repair”。
待办：定位 3/4 worker 仍触发 submit-only repair 的 provider-response 级触发条件，并确认 market reader brief 缺失“日涨跌额”字段的具体来源环节。

## 17:00 [codex]
-完成：完成 FRONTLINE-RUNTIME-TOOLCALL-001：修复 MiniMax Anthropic-compatible 流在 forced `openviking.write_material` 下缺失 `content_block_stop` 时未把纯文本报告归一化为真实 toolCall 的问题，补齐 focused 回归测试并通过构建。
-决策：仅在 OpenClaw 通用运行时闭合路径做最小改动：把 block 关闭归一化逻辑复用到 `content_block_stop` 与流结束前未闭合 block，两处规则一致；不改 prompt/guard/业务控制流。
待办：按运行层验收规则重启 runtime 后做 fresh live provider payload 复证 fundamental/news/social 的 submit-only repair 是否清零。

## 17:16 [codex]
-完成：完成 FRONTLINE-LIVE-COMPARE-STREAM-CLOSE-001：fresh 跑 `run-20260510-210437-f7a4b77b`，生成四个 worker 的 final prompt/LLM back/report 与 provider/tool 证据，产出 `frontline_full_after_stream_close_recovery_live_20260510_210437/extraction_summary.json` 和 `cn_comparison_report.md`。
-决策：保持“LLM back=write_material.content 原文、report 与 receipt sha 对齐”的提取口径；本轮判定 `fundamental/social` 仍有 submit-only repair，`market/news` 已 primary direct write，且 market 日涨跌额/日涨跌幅字段均为 true。
待办：针对 `fundamental/social` repair 未清零触发条件做 provider-response 级进一步定位，并由主流程决定是否进入 runtime seam 继续收敛。

## 17:43 [codex]
-完成：修复 MiniMax forced `openviking.write_material` 恢复逻辑被同次返回中的无关 toolCall 拦截的问题，focused transport 测试 64 个通过，并完成 OpenClaw `pnpm build` 与 runtime 重启。
-决策：只调整 OpenClaw 通用 transport seam：判断重复恢复时仅检查目标 `openviking.write_material` 是否已存在，不再让无关工具块阻止文本报告归一化；不改 prompt、不改 guard。
待办：fresh 跑 frontline 四 worker，验证 `fundamental/social` 的 submit-only repair 是否清零。

## 18:02 [codex]
-完成：完成 FRONTLINE-LIVE-COMPARE-TOOLCALL-NOISE-001 fresh live 复跑（`run-20260510-214512-1a49bb2c`），产出 `frontline_full_after_toolcall_noise_recovery_live_20260510_215308` 全套四 worker 证据、`extraction_summary.json` 和 TradingAgents-CN 详细对比报告。
-决策：沿用既有取证口径（final prompt=报告生成前强制写入回合 prompt；LLM back/report=首个 write_material 正文），并按 provider-request 序列判定 repair；结论为 fundamental/social 未清零且 news 出现回归（repair=[3,4]）。
待办：主流程需决策是否按 provider-response 级证据继续定位 news 回归与 fundamental/social 未清零根因。

## 18:10 [codex]
-完成：完成 FRONTLINE-RUNTIME-MINIMAX-BOUNDARY-001：在 `stream-resolution` 中将 `minimax|minimax-portal + anthropic-messages` 的自定义 session stream 路径切换为 boundary-aware anthropic transport，补齐 focused 单测验证 `streamStrategy` 与流函数选择行为，并完成 OpenClaw `pnpm build`。
-决策：仅对 MiniMax Anthropic-compatible 例外收敛，不改其他 provider 的 explicit custom stream 语义；普通 OpenAI custom 仍保持 `session-custom`。
待办：由总管在后续 fresh live 中复证 trajectory `streamStrategy` 与 submit-only repair 收敛。

## 18:56 [codex]
-完成：完成 FRONTLINE-RUNTIME-MINIMAX-THINKING-001：修复 MiniMax Anthropic-compatible payload 在上游已设置 `thinking` 时未覆盖的问题，统一强制发送 `{type:"disabled"}`，并补齐“已有 enabled 仍覆盖”为 disabled 的 focused tests。
-决策：本次只改 OpenClaw 通用 runtime seam 和贴近单测，不改 prompt/guard/workflow/provider 策略，不引入 mock/stub/fallback。
待办：后续由主流程在 fresh live run 中复核 MiniMax forced-tool 场景下的 provider payload 与长时无返回现象是否消失（本任务按要求未跑大集成 live）。

## 19:09 [codex]
-完成：完成 FRONTLINE-RUNTIME-MINIMAX-BOUNDARY-THINKING-002：在 boundary-aware Anthropic transport 的 payload 构造阶段对 MiniMax provider 强制 `thinking={type:"disabled"}`，并补齐 onPayload 观测点 focused tests（MiniMax 强制 disabled，非 MiniMax 维持 enabled）。
-决策：保持最小改动，仅限 OpenClaw 通用 runtime seam 的 payload 参数修正；未改 forced-tool 策略、provider 策略、prompt/guard/workflow 逻辑。
待办：按运行层验收链路在后续 fresh live 中复核 provider-requests.jsonl 是否稳定呈现 MiniMax `thinking.disabled`（本任务按要求未跑大 live）。
## 19:24 [codex]
-完成：完成 MiniMax boundary-aware 路径复证，真实 provider payload 已从 `thinking: enabled` 修为 `thinking: disabled`，但 forced `tool_choice=openviking.write_material` 请求仍在 MiniMax 侧长时间无返回。
-决策：停止继续自动修改，因为后续需要在人类确认下选择提交策略（继续强制工具、改为只暴露写入工具/非强制提交、或更换 provider/接口）。
待办：等待人类对 MiniMax forced tool_choice 兼容性问题选择方向。

## 20:43 [codex]
-完成：按人类指令将 OpenClaw 源配置默认模型切换为 `deepseek/deepseek-v4-flash`，并在 `.env.local` 追加 `DEEPSEEK_API_KEY=` 空位供填写。
-决策：保留 MiniMax provider 配置但不再作为默认模型；未改 worker prompt、guard、workflow 或 OpenViking 写入逻辑。
待办：人类填写 DeepSeek key 后，重启 runtime 并重新跑 frontline 证据对比。

## 20:53 [codex]
-完成：按 memory 固定 OpenViking 启动口径并验证 runtime：`scripts/start-control-runtime.sh` 使用 trade 仓 uv 环境、1933/18789、no-sidecar，OpenViking/OpenClaw health 均通过。
-决策：不再从源码目录或临时配置猜启动；OpenViking 读取 `~/.openviking/ov.conf` 作为源配置，但运行时 config/data/cache 固定写到 `.runtime/dev-services`，避免沙箱 home 只读导致反复掉坑。
待办：用当前已启动 runtime 继续 fresh frontline 复跑，并抓取 final prompt、LLM back、report 与 TradingAgents-CN 对比。

## 21:08 [codex]
-完成：执行 `FRONTLINE-DEEPSEEK-FRESH-CN-COMPARISON-20260510` fresh live（`run-20260511-005617-28ae946b`）后四个 frontline worker 全部在 provider schema 校验失败，已停止三层对比并落盘阻断证据到 `docs/evidence/frontline_prompt_feedback/frontline_full_deepseek_fresh_cn_comparison_live_20260510_205617/`（含 provider requests/visible tools/tool calls/extraction summary/blocked report）。
-决策：按“失败即停止编造比较”口径处理，不产出伪 final prompt/LLM back/report；根因证据来自 gateway 日志 `400 Invalid 'tools[0].function.name'`，对应 DeepSeek tool name 正则不接受带点号工具名。
待办：主流程需决定是否在 OpenClaw provider seam 做工具名映射（provider 侧无点号、回调再映射原名）后，再按同口径重跑 frontline 取证并恢复 CN 三层对比。

## 22:08 [codex]
-完成：按 CN/原版路径移除 frontline 模型可见 OpenViking 写入工具、强制 tool_choice 和补提交回合；OpenViking 改为运行层保存 LLM 原文，并完成 fresh frontline run `run-20260511-020234-21f46673` 验证。
-决策：运行层自动保存只限 `frontline`，避免影响后续 PM/决策链；四个 provider payload 均只暴露各自资料包工具，未出现 `openviking_write_material`、`tool_choice`、`[RuntimeTarget]`、`[ReportSubmission]`、`message[2]`。
待办：如继续验报告质量，基于本 run 抽取 final prompt、LLM back、report，与 TradingAgents-CN 基线做三层详细对比。

## 22:26 [codex]
-完成：基于 `run-20260511-020234-21f46673` 抽取四个 frontline worker 的 final prompt、LLM back、report，并写出 `frontline_full_runtime_save_live_20260510_220234/cn_comparison_report.md` 三层 CN 对比报告。
-决策：采纳评审结论为“部分通过”：runtime/prompt 污染已清理，剩余问题集中在 news 精确短期涨幅、fundamental 工具外历史 PE 常识、news/social 首响应铺垫文本。
待办：等待人类确认 news 是否允许写精确短期涨幅；后续优先补 social 文本源和 fundamental 历史估值/同业估值数据。
## 22:25 [codex]
-完成：完成 run-20260511-020234-21f46673 与 TradingAgents-CN 基线在 final prompt / llm back / report 三层的只读对比评审并给出分级结论与失败模式。
-决策：以 provider-request 与 tool-calls/first-response 作为“真实请求链路”主证据，避免把提取用 markdown 包装头误判为模型可见污染。
待办：按评审结论优先收敛 news/fundamental 的超证据推断与 news/social 首响应非 tool_call 的流程偏差。
## 23:00 [Codex]
-完成：补充 frontline 四个 worker 实际 user prompt 对比报告，区分了源 prompt、provider role=user 内容和后续工具消息。
-决策：报告只比较真正发给 LLM 的 user prompt，不把 tool_result、thinking、runtime 保存信息混入 prompt 本体。
待办：如需进一步对齐 CN，优先补 fundamental 历史估值/行业对比数据、social 文本源，并确认 news 是否允许写短期价格区间。
## 23:00 [Codex]
-完成：将 fundamental 和 social 的 CN_A prompt 改回 CN 风格的直接完整报告型 prompt，并同步更新 user prompt 对比报告。
-决策：news prompt 已经是直接新闻影响报告型主体，未做无意义改动；不再用“资料包事实/热度报告”大段限制压报告口吻。
待办：下一轮重新跑 frontline，检查四个 worker 的 final prompt、LLM back 和 report 是否比上一轮更接近 CN。
