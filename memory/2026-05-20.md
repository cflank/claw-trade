## 06:06 [codex]
-完成：将 UI 启动脚本改为默认复用现有 dist、仅在 `--build` 或 `RESEARCH_UI_BUILD_FRONTEND=1` 时显式构建，并同步更新测试与 README。
-决策：保留 fixed runtime 启动链路和 `--run-backend` 直调防护不变，仅调整前端构建触发条件与提示文案。

## 06:14 [codex]
-完成：定位 fixed runtime/UI 启动时 `curl: (7)` 日志为健康检查轮询噪音，并将 OpenViking/OpenClaw/UI readiness 轮询改为静默预期重试。
-决策：保留状态码判定和失败出口不变，仅隐藏启动窗口内连接未开放时的 stderr；真正超时仍由脚本输出健康检查失败。
待办：无。

## 06:27 [codex]
-完成：定位 UI “等待处理中”卡住为队列未同步 workflow 进度/终态，且从 `scripts/` 启动时相对 run_dir 写入 `scripts/runs/`；已让队列快照同步真实 workflow 状态并让 UI 后端从仓库根目录运行。
-决策：不改工作流执行权、不让 Python 改写报告内容，只把已有 workflow state 映射为 UI 进度，并在成功终态读取已导出的 final-report 写入 UI 历史仓库。
待办：如需验证真实 `/report` 端到端，需要重新发起一轮报告；上一轮 BTC 现场 run 已因 `report_polisher` OpenClaw exit=1 失败并关闭 runtime。

## 06:48 [codex]
-完成：全面复查设置页，补齐顶部/设置页设备界面入口、微信通知启用/刷新/扫码入口、设置区块局部加载与模型接口超时，并构建 OpenClaw 控制台资源让设备界面可打开。
-决策：微信登录不伪装成功；当前 OpenClaw 仍未安装/启用微信通道，UI 只展示真实不可用状态和设备界面处理入口。
待办：在设备界面批准配置写权限并安装/启用微信通道后，再验证扫码登录与真实发送。

## 07:07 [codex]
-完成：设置页微信通道已接入真实二维码字段，后端通过 OpenClaw web.login.start/wait 获取 PNG data URL，前端显示二维码区域并提供“刷新二维码”。
-决策：只展示真实通道返回的二维码图片；当前运行时未返回二维码时明确显示不可用/未返回状态，不生成假二维码或假登录成功。
待办：安装并启用真实微信通道插件后，用扫描登录返回的二维码 data URL 做一轮人工扫码验证。

## 07:47 [codex]
-完成：修复设置页不可用问题，补齐模型/数据源可编辑表单、微信二维码刷新入口，并修正模型配置加载真实运行态 503/超时和重复 UI 栈干扰。
-决策：设置页初始加载只读必要配置，不做慢模型探测；连接真实性放到“测试连接”“测试数据源”“刷新二维码”按钮上。
待办：真实微信插件当前未返回二维码，仍需安装/启用后扫码实测。

## 08:03 [codex]
-完成：按要求读取 claw-invest memory 并用真实 Windows Chrome 146 连接当前 UI 做设置页实测，确认微信/模型/数据源控件可见、刷新二维码会收口、设备界面入口打开 OpenClaw Control、模型测试返回用户可读失败文案。
-决策：不杀占用 9222 的既有 Chrome；改用临时端口转发连接新开的真实 Chrome 调试端口，避免误关用户现有浏览器。
待办：微信仍缺真人扫码后的在线/媒体发送实证；数据源缺密钥测试按当前合同返回 400 并在页面显示“需要先填写密钥”。

## 08:09 [codex]
-完成：调查设置页未返回真实微信二维码根因，确认当前 fixed runtime 未加载 `openclaw-weixin`，OpenClaw live 通道快照为空，`web.login.start` 还被设备 scope approval 拦截。
-决策：把问题归类为运行态插件/授权未就绪，不归类为前端二维码展示失败；不得伪造二维码。
待办：把 `@tencent-weixin/openclaw-weixin` 安装启用到当前 UI runtime，重启 gateway 并完成 scope approval 后，再发起真实 QR 登录。

## 09:01 [codex]
-完成：修复设置页微信通道真实二维码链路，OpenClaw 网关现在能调用 `openclaw-weixin` 的二维码登录方法并把插件返回的微信二维码 URL 渲染为 PNG data URL；设置页显示真实二维码、提供刷新按钮，并把入口明确标为 OpenClaw 主界面。
-决策：二维码生成只基于真实微信插件返回值，claw-trade 不实现微信协议、不伪造登录成功；设置页二维码请求超时单独放宽，避免真实插件响应稍慢时误报不可用。
待办：用户扫码后再验证通道进入 connected 状态以及文字/文件发送能力。

## 09:50 [codex]
-完成：修复“启动微信”一直处理中：OpenClaw 二维码登录轮询透传并复用真实 `sessionKey`，设置页已有二维码时首屏直接返回缓存图，Python 调 OpenClaw CLI 增加硬超时，点击启用超时后恢复按钮，刷新二维码可重新获取真实 PNG data URL。
-决策：不把未扫码/未保存账号的状态伪装为 connected；登录超时会清理旧会话并要求用户刷新二维码，CLI 子进程不允许无限占住 UI 请求。
待办：用户扫描当前二维码后，再验证 `channels.status` 进入 connected 以及文字/文件发送能力。

## 10:36 [codex]
-完成：修复并验证微信 OpenClaw 连接、入站和回复链路；本机账号已保存并处于监听运行，微信消息进入 OpenClaw 后已发出文字回复。
-决策：将微信长轮询接入中止信号，避免热重载停止监听超时后不重启；设置页改为状态优先，并跳过当前网关不存在的能力探测接口。
待办：将 `.runtime` 中对 `openclaw-weixin` 的补丁固化到插件来源或安装流程，避免重装插件后丢失。

## 10:43 [codex]
-完成：修复 OpenClaw 主界面打不开；构建缺失的 Control UI 静态资源并重启 gateway 后，研究 UI 的“OpenClaw 主界面”入口已能跳转到真实 OpenClaw Control 页面。
-决策：不运行完整 fixed runtime 重启脚本，避免清空 `.runtime` 导致微信账号和运行态插件补丁丢失；仅重启 OpenClaw gateway 并保留现有 state。
待办：把 Control UI 构建步骤纳入启动前检查或启动脚本，避免下次 dist/control-ui 缺失时再次返回 503。

## 11:18 [codex]
-完成：新增微信文本入站到极简 UI 后端的桥接路径，`/report` 只返回确认卡，微信回复“确认”才进入现有 report workflow，普通微信聊天不被投研流程接管。
-决策：OpenClaw 微信插件只做通用转发，不写投研业务逻辑；`claw-trade` 后端保留意图识别、待确认状态和 workflow 入队权。
待办：需要用户在真实微信里发送一次 `/report TSLA` 并回复“确认”，才能完成真人微信端到端启动实证；当前验证已覆盖后端入站、网关桥接环境和真实 Chrome 页面状态。

## 11:28 [codex]
-完成：修复极简 UI 窄屏/小窗口下聊天面板被固定三列布局挤出首屏的问题，并让首页聊天/队列加载不再被微信通道状态阻塞。
-决策：工作台在 1240px 以下改为单列且聊天区优先显示；微信状态作为非阻塞信息补齐，避免慢通道让主聊天窗口长期显示加载中。
待办：前端全量测试仍有既有布局合同失败，原因是用户可见文案包含 `OpenClaw 主界面`；本次 focused 首页测试和真实 Chrome 窄屏验证已通过。

## 12:18 [codex]
-完成：按 claw-invest 真源修复极简 UI `/report BTC` 到 OpenClaw 的连接：runner 遇到本地 scope-upgrade 时读取同一 runtime state 的 pending pairing，调用 OpenClaw `approveDevicePairing` 后只重试一次；同时把 UI 等待 run 创建的窗口从 5 秒改为可配置 30 秒默认，避免真实 boot/probe 被误判失败。
-决策：放弃手动撞 gateway scope 的方案，采用 Invest 的“真实调用触发、同 state 自动批准、一次重试”模式；启动期 scope 预授权只作为 best-effort，不再阻断 UI runtime。
待办：当前验证已证明确认卡后任务进入 `frontline_running`；完整报告完成仍需等待本轮 run 后续 worker 返回。

## 12:56 [codex]
-完成：修复极简 UI 确认 `/report BTC` 后“像没反应”的问题：前端在确认成功但后端无消息时补本地任务启动消息，后端进度映射正确识别 RunStatus 枚举，右侧进度条恢复可见；真实 Chrome 验证确认后显示“报告任务已启动，正在生成。”且右侧为 BTC 生成中/前线信息采集中。
-决策：只修 UI 启动可见反馈与真实进度映射，不改变 workflow 调度权、不把普通 OpenClaw 聊天作为投研入口。
待办：继续处理完整报告链路的后续数据/导出问题时，应与“UI 能启动 workflow”分开验证。

## 13:16 [codex]
-完成：按 claw-invest 的任务栏实现口径修复极简 UI worker 进度显示：后端从真实 run evidence 的 `calls/*/call.json` 与 `calls/*/result.json` 推导执行中/已完成/等待启动，前端右栏显示逐角色中文状态；真实 5175 API 与真实 Chrome 验证 `/report BTC` 后已显示市场/基本面/新闻/社交/多空已完成，研究经理执行中。
-决策：不改 workflow 调度、不让 Python 代替 worker，只把已有 run 证据投影到 UI；`completed_workers` 继续不作为唯一依据，因为当前 state 文件里该字段未随 worker 完成更新。
待办：后续若要彻底收口，可再评估 workflow state 是否也应写入完成 worker 摘要，但这不影响当前 UI 读真实证据。

## 13:34 [codex]
-完成：新增 claw-trade 自己的 Win11 Chrome MCP 固定入口 `scripts/mcp-chrome.sh` 与说明 `docs/mcp-chrome-wsl.md`，固定走 `playwright-win-chrome -> Windows-side Playwright MCP -> 127.0.0.1:9222 -> Win11 Chrome`。
-决策：不再把 claw-invest 文档当作口头依赖；仓库内脚本复刻已验证的检查/启动逻辑，后续 UI 验收先用本仓库入口确认 ready。
待办：下次真实 UI 验收前先运行 `scripts/mcp-chrome.sh --check`，ready 后只通过 MCP 页面工具控制浏览器。

## 13:50 [codex]
-完成：通过仓库 MCP Chrome 入口启动 Win11 Chrome，并在内建 `playwright-win-chrome` transport 断开后新增 MCP client 兜底脚本，仍连接 `~/.mcp.json` 的同一个 server；真实 Chrome UI 验证 `/report BTC` 确认后任务进入运行，右栏显示前线四 worker 完成并推进到投资辩论，多头研究员执行中。
-决策：不使用 headless、raw Playwright 或临时 CDP 直连；MCP 工具不可用时只补 MCP client 调同一个 `playwright-win-chrome` server。
待办：如果继续验收到完整报告完成，可沿用 `scripts/playwright-win-chrome-mcp-call.mjs` 读取 UI 状态。

## 14:14 [codex]
-完成：定位 BTC 报告完成后首页红条原因：报告列表和队列已成功，但报告详情接口被普通 UI 禁词扫描误杀，报告正文中的合法 crypto 术语 `L2` 触发 `ASSISTANT_UNAVAILABLE`。
-决策：这不是报告生成失败，也不是微信通道 disconnected；根因在报告详情 payload 安全扫描对自然语言报告正文过宽。
待办：修复时应让禁词扫描按字段/上下文处理，允许报告正文里的合法 `L1/L2` 金融或 crypto 术语，同时继续阻断内部协议泄漏。

## 14:35 [codex]
-完成：追查普通 UI 禁词扫描来源：`src/claw_trade/ui_contracts/` 和相关 UI 合同测试是当前工作区未提交新增文件，`git blame` 无法追到提交作者；`L1/L2` 全局禁词来自该新增常量表。
-决策：该机制本质是 UI 防内部字段泄漏的合同检查，不是外部安全系统；把 `L1/L2` 作为全局禁词会误杀 crypto 报告正文，属于规则设计过宽。
待办：修复时应先收窄“防内部泄漏”边界，不应继续扩大为泛化安全扫描。

## 14:43 [codex]
-完成：移除普通 UI 自然语言禁词扫描策略：删除 `FORBIDDEN_UI_TERMS` 和 `ui_contracts/scanner.py`，路由/API/error/chat 不再扫描报告正文或聊天文本；合同测试新增/保留带 `L2` 的报告详情通过样本。
-决策：按人类反馈，原版 TradingAgents/TradingAgents-CN 没有这种全局文本安全策略；普通 UI 只保留明确 DTO/禁止字段边界，不拦自然语言报告术语。
待办：本地 5175 服务当前未运行，若要在浏览器里立即看到修复，需要重启 UI 后端加载新代码。

## 14:56 [codex]
-完成：全仓排查安全/禁止/guard 类检查，确认 UI 自然语言禁词扫描无残留；剩余检查主要分为真实性硬门、内部字段/密钥/SSRF 边界、数据包不代替投资建议，以及少量 prompt 级读者化清理。
-决策：不一刀切删除 PE/PB/ROE/目标价真实性硬门、provider payload 真实性校验、内部字段/密钥边界；把 fundamental claim gate 和 CRYPTO prompt 词面清理标为需要人类确认后再改的表达敏感项。
待办：若继续清理，优先让人类拍板是否移除 prompt 里的读者化词面禁令，以及是否重审 fundamental claim gate 对评级/目标价的 runtime 拦截。

## 15:06 [codex]
-完成：按人类批准收窄表达敏感检查：fundamental claim gate 不再拦评级、买入/卖出/持有和宽泛估值判断，只保留具体目标价与基础财务数值的真实性检查；CRYPTO 各 worker prompt 的硬禁词文案改成读者化改写指导；本地 Research UI 全栈已启动并通过队列接口健康检查。
-决策：保留防造假、密钥/路径隐藏、provider payload 真实性边界；删除/软化原版/CN 没有的报告表达词面策略。
待办：当前 fixed runtime 启动后 UI 运行态为空，旧 `task-2` 不存在；需要重新从页面发起 `/report BTC` 才能看新报告详情。

## 15:35 [codex]
-完成：确认重启后历史报告未显示的原因：实际报告文件仍在 `runs/**/reports/final-report.md`，但 UI `ReportRepository` 是进程内存仓库，重启后没有从 `runs` 目录重建历史索引，`list-saved-reports` 因此返回空列表。
-决策：这不是报告文件被删除，而是 UI 历史持久化/重建缺失；当前至少有 BTC 完成报告保留在 `runs/run-20260520-171453-edd59473` 和 `runs/run-20260520-174725-7fe4e068`。
待办：后续应实现启动时从 completed workflow state 和 final-report.md 重建普通 UI 历史。

## 15:59 [codex]
-完成：实现普通 UI 历史报告重启恢复和软删除；启动时从 `runs/*/state.json` + `reports/final-report.md` 恢复 completed 报告，删除写入 `runs/.ui-deleted-reports.json` 并仅隐藏 UI 历史，保留原始 run 证据文件；前端历史项新增删除按钮，本地 5175 已重启并显示历史报告。
-决策：删除功能不物理删除 `runs/run-*` 目录，避免误删报告证据；当前任务完成报告 ID 改用稳定 run id，避免重启后 `task-*` 临时编号失效。
待办：若未来要支持“彻底删除磁盘证据”，需要单独加危险确认和证据保留说明，不能复用当前软删除。

## 16:08 [codex]
-完成：修复网页报告 Markdown 表格乱排：前端安装并启用 `remark-gfm`，报告正文表格按 `<table>` 渲染并补齐表格/打印样式；报告详情右侧新增“保存为 PDF”按钮，调用浏览器打印保存为 PDF。
-决策：PDF 先走浏览器原生保存路径，避免把当前后端占位 renderer 伪装成真实下载文件；用户操作路径是打开报告后点击右侧“保存为 PDF”，在打印窗口选择保存为 PDF。
待办：若需要服务器端生成可下载 PDF，应另做真实 PDF renderer 和下载接口，不复用当前占位字节。

## 16:18 [codex]
-完成：修复历史报告图片不显示和阅读模式无法回聊天的问题；后端保存/恢复报告时记录 `reports/assets`，报告详情把 Markdown 图片地址改写为 `/api/ui/get-report-asset`，图片接口返回真实 PNG；前端报告阅读标题旁新增“返回聊天”按钮。
-决策：报告原始 Markdown 不改写、不落库，改写只发生在 UI 详情 payload，避免污染报告证据文件；图表状态同步按本地图片存在性显示 ready。
待办：若后续支持跨机器访问历史报告，需要把报告资产目录纳入持久化索引或对象存储设计。

## 16:26 [codex]
-完成：按人类要求复查 `memory` 解释“无 UI 多次通过、加 UI 后问题集中暴露”的原因；确认核心报告路径此前已多轮通过，UI 后续问题主要来自新增启动/队列/状态同步/历史仓库/报告 API/浏览器渲染/资产服务/微信与 OpenClaw 连接面，以及 UI 自行新增的普通文本禁词扫描。
-决策：不把所有问题归因给报告生成核心；基本面估值缺失属于 CRYPTO fundamental 数据包/指标建模问题，和表格、图片、历史、返回聊天、安全扫描这类 UI 集成问题分开处理。
待办：后续 UI 变更必须把重启恢复、历史报告、真实浏览器打开报告、图片资产、Markdown 表格、PDF/打印和通道连接纳入验收，不再只用非 UI full-chain 结果代表 UI 可用。

## 16:33 [codex]
-完成：按人类纠正重新对比脚本 BTC 报告 `run-20260519-042451-bb511eac` 与 UI BTC 报告 `run-20260520-174725-7fe4e068`；确认 fundamental provider 两次都调用成功，返回均为 partial，核心差异是 UI 构造请求时把 `startDate/endDate/currentDate` 全设为同一天且 `/report BTC` 名称解析为 `BTC`，而脚本请求为 `company_name=Bitcoin`、`currency=USDT`、`start_date=2025-05-19`、`end_date=2026-05-19`。
-决策：当前“基本面估值框架缺失很多”不能归因于 provider 没调用；更准确地说是 UI 没按脚本的报告参数配置请求，加上 fundamental prompt 只允许使用 fundamental pack，而 AHR999/SOPR 等链上估值信号在 market pack 中出现，导致 final report 同时出现 market 段有 AHR999、fundamental 段说无法计算的割裂。
待办：修复应先让 UI `/report` 请求沿用脚本/验收用的市场默认窗口和标的名称映射，并补合同测试锁住 CRYPTO `BTC -> Bitcoin`、一年或批准的默认日期窗口。

## 16:58 [codex]
-完成：修复 UI `/report` 请求不复用脚本默认参数的问题：新增共享 `workflow/report_request_factory.py`，脚本 `run_claw_trade_fresh_report.py`、CLI `run_control.py`、UI 确认流和 UI workflow bridge 均改为走同一报告请求构造函数；`/report BTC` 现在生成 `company_name=Bitcoin`、`currency=USDT`、365 天默认窗口。
-决策：不再让 UI 自己手写报告请求默认规则；UI 只传用户意图，报告请求参数由共享工厂统一解析和补全。
待办：需要重启 UI 后重新从页面跑一次 `/report BTC`，检查新 run 的 `request.json` 是否与脚本口径一致。

## 17:06 [codex]
-完成：修复 Research UI 聊天输入框被长消息/报告正文顶到整页底部的问题；工作台改为视口内固定布局，消息流和报告正文各自滚动，输入框固定留在中间工作区底部可见；补充布局契约测试并重新构建前端 dist。
-决策：只改前端布局与契约测试，不碰报告生成、provider、workflow 请求路径。
待办：Chrome DevTools 端口 9222 当前未启动，未完成真实浏览器截图验证；本地 5175 已能返回新构建的页面和 CSS。

## 17:32 [codex]
-完成：调查报告追问上下文来源；确认当前 UI 报告追问直接把完整 final-report Markdown 塞给 OpenClaw，而 OpenViking 本体有 search/find/grep/关系能力，claw-trade 当前报告运行关闭语义/vector 检索，Mongo 中保留 provider attempts/raw/normalized/crypto_lens 证据可供按需追溯。
-决策：建议报告追问改为基于 approved-manifest 的 L1 worker 材料选择、lineage-relations/Mongo 证据追溯和小片段上下文组装；不直接发送十几万字完整报告，也不把 OpenViking semantic find 当作当前已可用能力。
待办：如获批准，新增 ReportContextRetriever：先用 OpenViking 可用性探测，失败则走本地 approved manifest + worker appendix + Mongo refs 的确定性检索。

## 17:53 [codex]
-完成：按 memory 复查后重新打开 OpenViking 向量/语义队列，报告写入恢复 `vectorize=true`，UI 报告追问改为从 approved L1、OpenViking 可检索片段和 Mongo 证据索引中组装小上下文；本地 UI 已重启到 `http://127.0.0.1:5175/`。
-决策：启动脚本不再把 OpenClaw 聊天主模型当 OpenViking embedding 模型，而是继承 `~/.openviking/ov.conf` 的 embedding 配置；同时保留 `.runtime/dev-services/openviking/data`，避免重启清空 OpenViking 索引数据。
待办：当前 OpenViking 语义链路真实阻塞点是 MiniMax `embo-01` 返回 `insufficient balance`，导致 find 仍返回 500；需修复 embedding 账号/余额或显式更换 OpenViking embedding 配置后再复验语义检索成功。

## 19:46 [codex]
-完成：纠正上一条错误决策：control runtime 不再读取 `~/.openviking/ov.conf`、`~/.openclaw/.env` 或 `~/.openclaw/openclaw.json`；OpenClaw runtime LLM 配置只从项目 `.env.local` 生成 DeepSeek provider，OpenViking 无显式 `.env.local` embedding 配置时禁用 vectorize/semantic queue。
-决策：未把 `.env.local` 里的新闻/数据源 key 擅自当 OpenViking embedding；只有显式配置 `OPENVIKING_EMBEDDING_PROVIDER` 与 `OPENVIKING_EMBEDDING_MODEL` 时才会打开 OpenViking 向量化。
待办：如果要真正打开 OpenViking 语义检索，需要先由人类明确批准并写入 `.env.local` 的 OpenViking embedding provider/model/dimension/key 配置，再复验 `/api/v1/search/find`。

## 20:10 [codex]
-完成：按人类要求重新打开 OpenViking semantic/vector queue，并改为从项目 `.env.local` 的统一 LLM 配置生成 OpenViking embedding 配置；当前 runtime 生成 `litellm + deepseek/deepseek-chat`，未出现 MiniMax、disabled embedding、个人 `.openviking` 或 `.openclaw` 配置。
-决策：不再以“避免 MiniMax”为理由默认 blocked；但 runtime 探针证明当前 DeepSeek chat 经 OpenViking/LiteLLM embedding 路径会报 `Unmapped LLM provider for this endpoint` 并 requeue，说明模型来源已纠正，向量能力仍受当前 LLM embedding 兼容性限制。
待办：下一步需要决定是让 UI/项目 LLM 配置提供可 embedding 的同一 provider/model，还是为报告追问实现不依赖 vector embedding 的 DeepSeek LLM 检索编排；不能再偷换 MiniMax 或静默关闭队列。

## 19:53 [codex]
-完成：评估 `a-stock-data` 的 A 股数据集合能否改造成 claw-trade provider 层；结论是可以作为 CN_A provider adapter 候选分批接入，但不能整体搬入 worker skill 或绕过现有 attempt/raw/normalized/evidence 合同。
-决策：推荐先接行情、公告/新闻、基本面补充这类能映射到现有 `market/fundamental/news` pack 的端点；龙虎榜、研报、题材、筹码、资金面等需要先明确是否扩展资料包域或字段合同。
待办：若批准实施，先做端点矩阵和最小垂直切片，不一次性接 28 个端点。

## 20:06 [codex]
-完成：根据人类反馈重新评估 CN_A 方向：A 股特色 agent（政策、游资、解禁）和龙虎榜、解禁、资金流、研报等数据域应纳入目标范围，不应被理解为永久不做。
-决策：推荐对 CN_A 扩展 frontline worker 与资料包域，但必须保持 claw-trade 控制调度、OpenClaw 单 worker 执行、provider evidence 合同和 PM 最终决策权；不能照搬 TradingAgents-astock 的 Python/LangGraph 结构。
待办：确认是否只对 CN_A 启用扩展 15-worker 路径，US/HK/CRYPTO 继续保持现有 12-worker 路径。

## 20:35 [codex]
-完成：修复 OpenViking embedding 派生逻辑：DeepSeek 项目 LLM 不再被当作 embedding，Qwen/DashScope 项目 LLM 会同厂商派生 `text-embedding-v4`；当前真实 `.env.local` DeepSeek runtime 已重启到 UI `http://127.0.0.1:5175/`，向量索引标记为 unavailable 而不是错误重试。
-决策：不跨厂商偷换 embedding，不自动 MiniMax；当前 `.venv` OpenViking 不认 `provider=dashscope`，所以 Qwen 自动派生用 OpenViking 已支持的 `openai` provider 指向 DashScope OpenAI 兼容 `/embeddings` endpoint。
待办：如果后续 UI 设置切到 Qwen，需要用真实 Qwen key 跑一次报告写入/语义检索，确认向量队列从配置生成到实际 embedding 调用都通过。

## 21:04 [codex]
-完成：按“embedding LLM 与主 LLM 分开配置”重做 OpenViking 语义开关：启动脚本只看显式 `OPENVIKING_EMBEDDING_PROVIDER` + `OPENVIKING_EMBEDDING_MODEL`，设置页新增 embedding provider/model/key/base/dimension 字段；当前 UI 已重启到 `http://127.0.0.1:5175/`。
-决策：移除从主 LLM 自动派生 embedding 的路径；没有 embedding 配置就关闭语义检索，有完整 embedding 配置才打开，避免 DeepSeek/Qwen/Minimax 等自动猜测。
待办：用户在设置页填写真实 embedding 配置后，需要重启 runtime，让 OpenViking 用新 `.env.local` 建向量索引。

## 21:02 [codex]
-完成：将已确认的 CN_A A股扩展需求写入 `docs/A股扩展方案.md`，覆盖新增 policy/hot_money/lockup worker、3 个 OpenBB pack、a-stock-data 免费源矩阵、现有用户声明式 provider 复用和 OpenBB 唯一数据入口边界。
-决策：A股方案只针对 CN_A；`a-stock-data` 作为免费源分类和 provider adapter 参考，不作为 worker 直连脚本；用户 provider 第一版允许覆盖新增三域，但只在同一 market/domain/source_role/coverage_group 内优先且不能覆盖官方原始披露源。
待办：下一步若进入实施，先做 Phase 1 最小闭环设计/任务拆分，不直接改运行代码。

## 21:23 [codex]
-完成：根据 A股扩展方案、OpenBB 数据源设计、数据源实施方案、架构设计和本日 memory，写成 `docs/A股扩展详细设计.md`，覆盖 CN_A 7 前线 worker、三新增数据域、三新增 OpenBB pack、provider adapter、用户声明式 provider、失败策略、normalized schema、UI、证据链、测试验收、分阶段计划和人类拍板清单。
-决策：只产出设计文档，不改运行代码；把 OpenBB submodule/version/license、a-stock-data 端点矩阵、声明式 provider SSRF policy、官方源管理员模式、新 worker baseline prompt 等未决项列为实施前人类拍板，不自行替代决策。
待办：关闭人类拍板清单后，再进入 Phase 1 最小闭环实现。

## 21:28 [codex]
-完成：在 `.env.local` 追加百炼/DashScope embedding 配置骨架，预填 `text-embedding-v4`、兼容接口地址和 1024 维度，API Key 留空等用户填写。
-决策：使用 OpenViking 当前可接入的 OpenAI-compatible provider 指向 DashScope，不改主 LLM 配置，不重启 runtime。
待办：填写 `OPENVIKING_EMBEDDING_API_KEY` 后重启 runtime，让语义检索真正启用。

## 21:37 [codex]
-完成：用户填入百炼 embedding Key 后，已重启 Research UI 到 `http://127.0.0.1:5175/`；runtime 显示 OpenViking embedding/vectorize 均为 `1`，设置接口显示 embedding enabled，真实 probe 写入后 Embedding/Semantic 队列处理成功且语义检索搜回 probe 文件。
-决策：测试采用最小 OpenViking probe，不跑完整报告，避免把 embedding 验证扩大成无关 workflow/provider live run。
待办：后续正式 `/report` 生成新材料时，OpenViking 会开始为报告材料建语义索引；如出现队列错误再按 OpenViking 日志定位具体 provider 返回。

## 21:42 [codex]
-完成：按架构评审阻断项修订 `docs/A股扩展详细设计.md`：移除可执行 `report_polisher` 路径、补三新增 worker prompt 迁移合同、统一 canonical tool schema、增加 Phase 0 人类冻结门、明确 Phase 1 guard freeze、重写测试矩阵并补 provider evidence/prompt live proof 与图表 root cause 验收。
-决策：`report_polisher` 仅保留为未批准提案；active run 固定 `provider_config_version` 不热加载；Phase 1 禁止新增或收紧 runtime guard（无 Guard source + 人类批准 + 边界测试不得实施）。
待办：人类关闭 Phase 0 冻结门后再进入 Phase 1 编码。
