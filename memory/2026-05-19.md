## 21:39 [codex]
-完成：调用子代理复评并按阻塞意见修订 `docs/UI详细设计.md`，补齐普通 UI 用户 DTO、公共错误码映射、微信入站桥接待确认边界和 PDF/报告追问边界。
-决策：普通 UI/API 一律返回 `*ForUser` DTO，内部 run/config/channel/artifact/data-source 证据只留在后端日志或诊断链路，避免把 OpenClaw/Channel/credential/artifact 等内部概念暴露给普通用户。
待办：实现前仍需确认 OpenClaw 微信入站语义事件桥接、本地 PDF 文件发送能力、前端技术栈和 UI 后端传输形态。

## 22:57 [codex-doc-subagent]
-完成：新增 `docs/UI实施任务清单.md`，把 UI 详细设计 §0-§15 映射到 Phase 1-6 的可执行任务、DTO/API 红线、blocker、负向测试和 adversarial review，并在 `docs/UI详细设计.md` §14 增加清单入口链接。
-决策：将 `ScheduledReportForUser`、`PriceAlertForUser` 标为必补 DTO 缺口，并要求 scheduled/price alert 普通 API 全部返回用户 DTO；未知 OpenClaw 微信/PDF/LLM 能力全部列为 blocker，不作为已实现项。
待办：实现前仍需确认 OpenClaw 微信 Channel ID/config schema、本地 PDF 文件发送、微信入站产品语义事件、LLM 配置专用 API/桥接、UI 后端传输形态、前端框架、数据源持久化/secret store、完成摘要提取规则、图表证据来源和运行中取消能力。

## 23:05 [codex]
-完成：先用上一轮 BTC 真实全链材料执行 `report_polish` 回放，输出 `docs/evidence/cryptolens-btc-report-polish-dynamic-20260519T022309Z/exported/final_report.md`，8 段 report_polisher 与最终八章结构均通过；随后按 fixed runtime command mode 重跑 A/HK/US 三市场全链回归，证据目录 `docs/evidence/regression-three-markets-dynamic-report-polish-20260519T023114Z/`，A 股 `run-20260519-023119-9aa30f4d`、港股 `run-20260519-024315-aa5b780b`、美股 `run-20260519-025450-d153ec97` 均 `REPORT/AUDIT/OV=0`，最终报告结构 `ok=true`。
-决策：本轮验证重点是动态 report_polisher 分段和结构验收；回放不声称 fresh 数据，三市场回归使用真实 `/report` 全链 live/fresh 运行；未使用 mock/stub/fake/fallback。
待办：如需进一步收紧“信息空白”读者表达，需要单独做 prompt/措辞产品决策；本轮结构和证据链验收已通过。

## 23:08 [codex]
-完成：按“子代理修改 -> 只读复评 -> 再修改 -> 再复评”闭环修订 UI 实施清单；新增 `docs/UI实施任务清单.md`，并同步修订 `docs/UI详细设计.md` 的清单入口、`ScheduledReportForUser`/`PriceAlertForUser`、API 输出、`REPORT_CONTEXT_TOO_LONG` 和普通 UI 响应红线；第二轮只读复评结论为 `符合`、无 Critical、无 Required Changes。
-决策：把未知 OpenClaw Channel/PDF/入站/LLM 能力保持为 blocker，不写成可实现项；把 UI 设置模型到 `RunRequest` 的冻结范围补到 rounds、frontline mode、default profile/market/currency/currency symbol 和 hard limit 校验。
待办：进入实现前仍需逐项确认 blocker；本轮只做文档闭合，未运行 live/provider。

## 23:33 [codex]
-完成：按 fixed runtime command mode 重新测试 BTC/CRYPTO 全链 `/report`，run_id=`run-20260519-032016-831503dc`，证据目录 `docs/evidence/cryptolens-btc-fullchain-dynamic-report-polish-20260519T032011Z/`；OpenBB audit exit 0、OpenViking collect exit 0，但 `/report` exit 1，未导出 final_report。
-决策：不把本轮说成通过；根因是 `report_polisher` 第 6 段违反结构要求，在非首段输出了 H1 `# Bitcoin（BTC）加密资产投资研究报告`，结构 gate 正确拦截。未使用 mock/stub/fake/fallback。
待办：若用户批准，可只改 `report_polisher` 分段格式提示或增加同段重试策略，再用同一 BTC 上游材料做 focused report_polish 验证；涉及 prompt/guard 策略变化需先获确认。

## 04:03 [codex]
-完成：按用户批准实现 `report_polisher` 分段结构失败的一次真实同段重试，并收紧 CRYPTO 终稿编辑 prompt 的内部工具名读者化规则；本地相关回归 `229 passed, 1 skipped`，ruff Python 文件通过。
-决策：不放松 final report structure gate，不用 fallback；BTC 全链 fresh `/report` run `run-20260519-034550-0c4e9196` 通过 `REPORT/AUDIT/OV=0`，证据目录 `docs/evidence/cryptolens-btc-fullchain-report-polisher-retry-20260519T034446Z/`；随后用该 run 的真实 12 份上游 L1 只重跑 `report_polisher`，focused run `run-20260519-035922-dfefacc8` 输出 `docs/evidence/cryptolens-btc-report-polish-internal-clean-20260519T035814Z/exported/final_report.md`，八章结构通过且禁用内部词扫描为空。
待办：全链 fresh 报告结构与证据链已通过，但读者版内部词清理的最终干净报告来自 focused report_polish 回放；如要把干净报告也作为 fresh 全链最终包，需要再跑一次完整 BTC `/report`。

## 04:35 [codex]
-完成：按用户要求重新跑 BTC/CRYPTO 完整 `/report` 全链，run_id=`run-20260519-042451-bb511eac`，证据目录 `docs/evidence/cryptolens-btc-fullchain-internal-clean-20260519T042344Z/`；`REPORT/AUDIT/OV=0`，OpenBB audit passed，终稿 8 段 `report_polisher` 全部结构通过，final report 八章结构通过，禁用内部词扫描为空。
-决策：本轮是 fresh 全链，不是回放；仍使用 fixed runtime command mode，OpenViking MCP sidecar 未启动，未使用 mock/stub/fake/fallback。OpenViking relations 非空、ovpack export/import ok；enhanced runtime health 仍为 blocked（semantic/vector/metrics 口径残留），不影响本次 `/report`、audit、collect exit code 事实。
待办：OpenViking enhanced runtime health blocked 仍按既有 todolist 处理。

## 07:27 [codex]
-完成：整理 v1.0 提交现场，删除被最新 full-chain/回归证据替代的中间 BTC 证据目录、pytest/Python 缓存，并清理 OpenBB 子模块自动生成构建脏文件；保留最新 BTC 全链、report_polish focused、三市场回归证据。
-决策：不提交 `.env.local`、`.runtime`、`.venv`、缓存或 OpenBB 子模块自动生成文件；正式证据只保留可说明当前状态的最终/聚焦目录。
待办：提交 `完成基本功能`，打 `v1.0` 标签并推送到 GitHub。

## 07:35 [codex]
-完成：按用户五项决策修订 UI 详细设计和实施清单：首版前端/后端参考并可复用 `claw-invest` 兼容代码，PDF 模板和完成摘要规则标为已决策，数据源与 report workflow 默认值改为后端受控读写 `.env.local`。
-决策：接受“直接读写 `.env.local`”的简单实现方向，但限定为后端 allowlist 配置写入层；前端、普通 UI API、OpenClaw bridge 和 workflow controller 不直接读写 env，controller 仍只读确认时冻结的 `RunRequest`。
待办：实现前仍需确认 OpenClaw 微信 Channel ID/config schema、本地 PDF 文件发送、微信入站产品语义事件、LLM 配置桥接、图表证据来源和运行中取消能力。

## 07:59 [codex]
-完成：按用户六点补充修订 UI 需求、详细设计和实施清单：普通微信映射为 OpenClaw 外部插件 `openclaw-weixin`；LLM 设置走 OpenClaw config/models；“事件”改成人话“微信消息通知/回调”；图表状态来源限定为 `ChartAsset`/`chart_assets`、`DataGap`、`reports/export-result.json` 和已保存 Markdown 图片引用；running workflow 首版不取消，只有 queued 可取消。
-决策：PDF/微信发送不是人类决策 blocker，但仍是运行时能力检查点，必须先查 OpenClaw status/capabilities；不能把未安装、未登录、文件能力缺失或发送失败写成成功。此前 07:35 待办中的微信映射、LLM 桥接、图表来源、运行中取消能力已由本轮关闭；剩余是 PDF/微信发送和微信入站回调的运行时探测/工程接入检查。
待办：实现阶段必须保留 B-02/B-03 probe，不得用 mock/stub/fake/fallback 证明真实发送或真实入站回调。

## 08:14 [codex]
-完成：把微信官方 ClawBot 插件接入方案同步到 UI 需求、详细设计和实施清单：固定普通微信接入为 `@tencent-weixin/openclaw-weixin` / `openclaw-weixin`，补安装、启用、gateway restart、扫码登录、status/capabilities probe、微信内命令映射和失败文案。
-决策：实现基线使用 `openclaw plugins install "@tencent-weixin/openclaw-weixin"`，`npx` 只作为用户手工路径；不接入 Wechaty、逆向登录或 WeCom；产品文案不承诺“绝对不封号”，只说明使用微信 ClawBot 插件接入。
待办：实现时按文档补 `channel_bridge` 和 `channel_inbound_bridge` 测试，真实发送/入站成功仍需 OpenClaw 运行时能力证据。

## 08:34 [codex]
-完成：补做实现前真实能力探测并同步文档：本机没有全局 `openclaw`，项目应使用 `third_party/openclaw/openclaw.mjs`；npm registry 确认 `@tencent-weixin/openclaw-weixin@2.4.3` 和 `@tencent-weixin/openclaw-weixin-cli@2.1.4` 存在；独立 probe 目录中插件可安装，`plugins inspect openclaw-weixin` 显示 Channel ID `openclaw-weixin`、依赖齐全，源码声明 direct/media/blockStreaming、sendText/sendMedia、QR 登录和长轮询入站 monitor。
-决策：把微信能力从“可直接按文档实现”降级为实现 blocker：当前本机 OpenClaw CLI 对 `channels login --channel openclaw-weixin` 返回 `Unsupported channel: openclaw-weixin`，对 `channels capabilities --channel openclaw-weixin` 返回 `Unknown channel "openclaw-weixin"`；`scripts/start-control-runtime.sh` 会重建 runtime OpenClaw config 且当前不会保留外部微信插件；不得把扫码登录、文件发送、微信入站回调写成已可用。
待办：实现前先修正/确认 OpenClaw CLI 或等价 Gateway QR 登录、状态、媒体能力方法，并让 fixed runtime 能加载 `openclaw-weixin`；随后再做真实扫码、真实 status、真实 PDF 发送和真实入站回调验证。

## 08:45 [codex]
-完成：实现 UI foundation 公共骨架（`ui_contracts`、`error_translator`、4 个 UI 单测/contract 测试）并新增独立证据文档 `docs/ui_implementation_evidence.md` 与 `docs/ui_blocker_register.md`；按指定命令完成最小 pytest 验证与扫描入口执行。
-决策：只落地 P1-00/P1-01/P1-03/P1-07/P1-10/P5-02、B-01..B-10 表达、AR-01..AR-06 扫描入口、NT 公共负向入口；不改 `third_party/openclaw`，不改 UI 任务源文档，不把 B-02/B-03 未探测能力写成成功。
待办：后续 subagent 继续实现聊天/队列/报告/设置业务模块，并在真实运行时完成 B-02/B-03 能力探测闭环。

## 08:59 [I-SETTINGS-CHANNEL]
-完成：落地 P5-03~P5-07 对应后端与单测：新增 `settings_service`、`data_source_settings`、`data_source_health`、`llm_settings_bridge`、`channel_bridge`，并补齐 6 个 UI 单测文件；通过指定单测与 contract 测试。
-决策：按评审反馈把设置/Channel/LLM/数据源响应统一收敛到用户 DTO 语义，严格不返回 `providerChannelId`、`credentialRef`、`path`；B-02/B-03 维持保守不可用路径（能力未探测不宣称文件发送成功），错误统一用产品错误码。
待办：若后续需要真实“微信扫码登录/文件发送/入站回调成功”能力证明，必须走 OpenClaw 运行时探测闭环，不能由单测替代。

## 08:59 [codex]
-完成：实现 I-REPORT-PDF 负责的报告/PDF/通知链路模块（`report_repository`、`report_qa`、`summary_builder`、`chart_evidence`、`pdf_export_service`、`report_notification_service`、`channel_inbound_bridge`）并补齐对应 10 组单测/contract 测试，全部通过。
-决策：按评审反馈统一采用文档字段输出 `SavedReportForUser`、`ReportDetailForUser`、`ReportAssetForUser`、`PdfExportForUser`、`ChartEvidenceForUser`（camelCase），并把报告追问预算超限硬返回 `REPORT_CONTEXT_TOO_LONG`，不做摘要压缩兜底。
待办：等待 I-SETTINGS-CHANNEL 提供实际 Channel 发送接口后再做联调验证。

## 08:58 [codex]
-完成：按 R-FOUNDATION 反馈修复 foundation 层：`*ForUser` DTO 序列化改为设计要求的 camelCase 用户合同，移除未授权 `workflow_running` 输出；错误翻译器覆盖全量产品错误码和业务类别；AR 证据改为实际结论；B-01/B-04 补充源文档与命令证据引用。
-决策：Python 内部仍可用 snake_case dataclass 字段，但 `to_user_payload` 是唯一普通 UI 序列化出口；B-02/B-03 仍保持待运行时探测，不写成成功。
待办：聊天、队列、报告、设置、PDF、Channel 业务 subagent 继续按这些公共合同接入。

## 08:59 [I-SCHEDULE-ALERT]
-完成：实现并新增 `scheduler_service`、`price_alert_service` 及 4 组对应测试，覆盖 P4-01~P4-06/T-04 约束（daily/weekly only、到点入队、暂停不触发、软删除、价格阈值/涨跌幅、触发后关闭、requestId 幂等、API 返回 `*ForUser` DTO）。
-决策：按评审反馈把 `ScheduledReportForUser`/`PriceAlertForUser` 字段本体改为任务源要求的 camelCase，并补齐 `error_translator` 对全量产品错误码的直接/类别映射；定时与提醒 API contract 新增专门测试并通过禁词扫描。
待办：等待上层 chat/controller 接入这些 service 的实际路由调用与端到端联调。

## 09:04 [I-FRONTEND]
-完成：在 `web/research-ui` 新建 React/Vite/TypeScript 前端并落地三栏工作台、报告阅读器、右侧任务/图表/PDF/数据源状态展示、设置页三分区；前端 contracts 与 fixtures 按 `docs/UI实施任务清单.md §2` 和 `docs/UI详细设计.md §7/§8` 用户 DTO 对齐；`pnpm test`、`pnpm build` 通过，禁词扫描命中为 0。
-决策：左侧历史仅消费 `listSavedReports` 返回的 `SavedReportForUser`，中间上下文以 `ChatContextForUser` 可见化，右侧任务只展示中文 `stageLabel/roleLabel`，设置页仅“微信通知/模型/数据源”三块且不暴露内部诊断信息。
待办：等待后端 API 联调后补充组件级截图 QA 与真实交互回归。

## 09:01 [I-CHAT-QUEUE]
-完成：实现 P1-02/P1-06/P1-08/P1-09 与 P2-01~P2-08 的聊天/确认/队列/workflow-bridge/进度映射后端模块，新增对应单测与 contract 测试共 10 个文件并全部通过；普通聊天透传与报告确认卡边界、串行队列上限 10、running 不可取消、运行中插话不改 run、失败不进历史/右侧长期保留均有覆盖。
-决策：按 R-FOUNDATION 反馈不把现有 `user_dto.py` 偏差当合同；聊天与队列 API 层直接输出设计字段（`messageId/actor/contextId/taskId/statusLabel` 等 camelCase），并继续通过 `ErrorMessageTranslator` 走产品错误码。
待办：等待 foundation 最终统一 DTO 后，再做最小适配收敛，避免重复字段映射。

## 10:17 [I-FIX-INTEGRATION]
-完成：修复 R-FULL/T-FULL 指定集成缺口：移除 `ChatContextForUser` 的 `workflowRunning` 输出；`ConfirmationController.confirmIntentDraft` 对 `scheduled_report`/`price_alert` 改为真实调用 `SchedulerService`/`PriceAlertService` 并返回 `*ForUser` DTO；`SchedulerService._build_task_input` 补齐 `startDate/endDate/currentDate/workflowSettings` 等完整队列字段；`ChannelBridge` 新增 `send_text` 与 `send_report_file_via_channel`（能力不足走 `NOTIFICATION_UNAVAILABLE`/`FILE_SEND_UNSUPPORTED`，不伪造 message id）；新增 `tests/e2e/ui/test_report_user_flows.py` 覆盖 T-09 指定流程。
-决策：保持 B-02/B-03 保守边界，不在无能力证据时宣称真实发送成功；`sendReportFileViaChannel` 失败时统一返回不可用错误，不返回成功文案。
待办：后续若要声明“微信真实发送/入站可用”，仍需运行时能力探测与真实证据闭环。

## 13:37 [codex]
-完成：按官方文档和本机探测重新校正微信/OpenClaw 口径：腾讯 `@tencent-weixin/openclaw-weixin` 插件真实存在、可安装，并能被 OpenClaw plugin runtime import；当前失败限定为本地 CLI `channels login/capabilities` 未把外部 channel 正确接入。
-决策：把 `scripts/start-control-runtime.sh` 明确写成 dev/fixed 测试 runtime；它重建测试配置只说明这条测试路径不能证明微信接通，不代表生产级 UI runtime 也应每次重建配置。
待办：修复或升级 OpenClaw CLI/Gateway 的微信 channel 加载路径后，再做真实扫码、状态、媒体/PDF 发送和入站消息验证。

## 13:51 [codex]
-完成：修复 OpenClaw channel 解析：当官方/catalog 未包含已安装外部 channel 时，显式 `--channel openclaw-weixin` 会从 installed manifest owner 加载真实插件；定向测试、`pnpm build` 和构建后 `channels capabilities --channel openclaw-weixin --json` 通过。
-决策：只修通“识别 channel 和静态能力”这一层，不把它写成已扫码、已在线、已发送或已接收入站消息。
待办：继续做真实二维码登录、Gateway status、PDF/媒体发送、微信入站消息到 claw-trade 的实测。

## 14:42 [T-OPENCLAW-CLEAN-CUSTOM-CHANNEL]
-完成：在 `/tmp/openclaw-clean-custom-channel-1132436` 的干净 OpenClaw worktree（HEAD: `ad8bd6238e`）完成“无源码改动”验证：默认 profile 与隔离 profile 都执行了 `channels capabilities --channel openclaw-weixin --json`；隔离 profile 已成功安装并启用 `@tencent-weixin/openclaw-weixin@2.4.3`，但 capabilities/login 仍报 `Unknown/Unsupported channel`。
-决策：`plugins inspect --runtime` 证据显示 clean HEAD 运行态存在 `Cannot find module '@sinclair/typebox'` 注册错误，导致 channel 未进入可用能力路径；当前不能把“插件可安装”推断成“capabilities/login 可用”。
待办：以 clean 环境优先修复 OpenClaw runtime 依赖/注册链路后，再复测 `channels capabilities`、短超时 login 二维码入口、以及后续 B-02/B-03 live 能力证据。

## 15:49 [I-WEIXIN-OFFICIAL-ALIGNMENT]
-完成：已撤销 `third_party/openclaw` 两处未批准源码改动，并完成 UI 文档口径纠偏：保留腾讯官方插件安装/启用/扫码/Gateway restart 路径，移除“本机 CLI 静态能力已打通/已能返回能力”的成功表述。
-决策：把当前真实状态统一为“官方插件存在且路径明确，但本机/项目运行态尚未按官方流程完成 clean 验收”；B-02/B-03 继续作为运行时 blocker，禁止把静态能力、安装成功或 inspect 成功写成扫码成功、在线成功、PDF 发送成功或入站成功。
待办：后续仅在完全按官方流程仍失败且形成最小复现证据时，再由人类拍板是否进入 OpenClaw 源码修复路径。

## 15:56 [I-WEIXIN-STOP-GATE-DOC]
-完成：在 `docs/UI实施任务清单.md` 的 B-02/B-03 邻近位置补充“官方流程→最小复现→人类拍板”停问红线，并在 `docs/UI详细设计.md` 微信官方插件接入段落同步同义约束，明确默认不得直接进入 OpenClaw 源码修复。
-决策：本次只补 stop/ask 文档红线，不改 B-02/B-03 既有验收标准，不改任何 UI 业务代码、测试代码、OpenClaw 源码、DTO、API、状态机。
待办：后续如官方流程在同一 profile/config/Gateway 下仍失败，先提交最小复现证据，再由人类决定是否进入 OpenClaw 源码修复。

## 16:59 [codex]
-完成：按腾讯官方路径在隔离 `HOME=/tmp/openclaw-weixin-live-home-rpDFVq`、`profile=weixin-live-scan` 安装并启用 `@tencent-weixin/openclaw-weixin`；`plugins inspect --runtime` 显示 `status=loaded`/`activated=true`，`channels capabilities` 显示 `media=true` 但 `configured=false`；`channels login` 成功生成三轮二维码和备用链接，但均过期，最终返回“二维码多次失效，连接流程已停止”。
-决策：不把二维码生成、静态能力或安装成功写成已扫码、已在线、PDF 发送成功或入站成功；B-02/B-03 仍保持未验收。
待办：需要真人在二维码有效期内完成扫码/授权后，再测在线状态、真实 PDF/媒体发送和微信入站消息到 claw-trade。

## 20:00 [codex]
-完成：完成最终非微信闭环：补齐 `test_ui_boundary.py`、`test_confirmation_controller.py`、前端截图 QA 证据，并由独立测试/复评确认非微信范围通过；最终复测 `uv run pytest tests/unit/ui tests/contracts tests/e2e/ui` 为 `797 passed, 1 skipped`，前端 `pnpm test`/`pnpm build` 通过，截图证据 3 张 PNG 非空，`third_party/openclaw` 工作树为空。
-决策：非微信项可收口为已验证；微信 live 项不硬判完成，B-02/B-03 及其关联 P5-03/P6-02/P6-03/AR-04 继续等待真人扫码、在线、真实发送和入站证据。
待办：用户扫码后执行微信 live 验证并更新矩阵。

## 19:46 [I-FINAL-NONWECHAT-FIX]
-完成：补齐非微信缺口：新增 `tests/contracts/test_ui_boundary.py`（清单入口，复用既有边界合同断言）与 `tests/unit/ui/test_confirmation_controller.py`（确认卡后建 workflow、scheduled/price 返回 `*ForUser` DTO、requestId 幂等、running 取消拒绝）；补充前端截图证据目录 `docs/evidence/ui-screenshot-qa-20260519/`（desktop+mobile 截图与复现 README），并通过指定 pytest/pnpm 验证。
-决策：截图链路优先使用 Playwright；MCP DevTools 通道不可用时改走 `npx playwright screenshot` 真实生成。`--device "iPhone 13"` 因系统缺库失败，按“不可伪造”约束改为固定 mobile viewport 截图并在 README 记录失败原因。
待办：若后续要求严格设备仿真截图，需要补齐 Playwright 浏览器系统依赖后再跑 `--device` 证据。

## 19:56 [I-P2-02-PROFILE-REDLINE-FIX]
-完成：仅针对 P2-02 补齐红线覆盖：在 `tests/unit/ui/test_confirmation_controller.py` 新增 `HK/CRYPTO` 未批准策略的参数化用例，确认 `confirm_intent_draft` 抛出 `PROFILE_STRATEGY_UNAPPROVED`，且不触发 workflow、不产生排队任务。
-决策：保持实现最小化，不改产品逻辑与状态机；本轮仅补测试缺口并用全量 UI/contract/e2e 回归验证无回归。

## 21:06 [codex-ui-invest-fix]
-完成：前端 `web/research-ui` 完成 claw-invest 形态对标修复：三栏工作台拆分为 `HistoryRail/MessageStream/Composer/TaskRail/EmptyStates/ErrorStates/ReportSummaryCard`，新增 `/reports/:reportId` 阅读路由（正文+问答），并补充 `/api/ui/*` 非 JSON/HTML 中文错误转译与轮询刷新。
-决策：不改后端与 runtime 边界；只在前端实现用户可见错误净化与结构对标，避免暴露 OpenClaw/OpenViking/runId/internal 等内部词。
待办：如需进一步逼近 claw-invest 视觉细节，可在不触碰后端合同前提下补充更细粒度样式和交互动效回归截图。

## 20:59 [UI-ENTRY-FIX]
-完成：新增 `scripts/start-research-ui.sh` 作为 claw-trade UI 正式开发入口：外层先构建前端并调用 `scripts/start-control-runtime.sh -- ...` 启动 fixed runtime，内层在 runtime 中拉起真实 UI 后端并用 `/api/ui/get-report-queue-snapshot` 做健康检查；同时新增 `tests/unit/test_start_research_ui_script.py` 合同测试锁定“必须走 fixed runtime、必须有后端入口、不能只跑 Vite dev”。
-决策：默认 UI 端口选择 `5175`/`8787`（避免占用 `5173`），并在后端入口模块缺失时硬失败，不做 fallback 成功。
待办：待后端 worker 提供最终 `claw_trade.web.*` 入口实现后，补一次真实 `scripts/start-research-ui.sh` 端到端启动验收（含 URL/日志/PID 输出实跑证据）。

## 21:05 [UI-BACKEND-HTTP-SUBAGENT]
-完成：补齐 `claw_trade.web` HTTP 服务层并对齐 `claw_trade.web.app` 入口：新增 FastAPI app factory、`/api/ui/*` 路由、`/api/ui/{path}` JSON 404 兜底、SPA 静态托管、OpenClaw 网关适配与服务装配；实现并验证 `GET /healthz`、`POST /api/ui/send-chat-message`、`GET /api/ui/get-report-queue-snapshot`、`GET /api/ui/list-saved-reports`、`GET /api/ui/get-report-detail`、`POST /api/ui/ask-report-question`、`GET /api/ui/get-report-chart-evidence`、`GET /api/ui/get-channel-status`、`GET /api/ui/load-llm-settings`、`GET /api/ui/list-data-sources` 均返回 JSON，且 API 不再回落 Vite HTML。
-决策：普通聊天/OpenClaw 不可达时统一走产品错误 `ASSISTANT_UNAVAILABLE`（真实失败暴露）；未定义 `/api/ui/*` 子路径固定返回 JSON 404，避免被 SPA catch-all 吞掉；保留 `claw_trade.web.server` 兼容导出并以 `claw_trade.web.app` 作为优先启动模块。
待办：和启动脚本 worker 联调 `scripts/start-research-ui.sh` 的实际参数与启动方式，补真实 runtime 下的端到端启动证据。

## 21:13 [UI-FINAL-RUNTIME-TEST-SUBAGENT]
-完成：按要求完成独立测试采证：3 组 pytest、`web/research-ui` 的 `pnpm test/build`、`claw_trade.web.app` 直启 + 5 个路由 curl 验证、`RESEARCH_UI_BUILD_FRONTEND=0 scripts/start-research-ui.sh` 正式入口冒烟、Playwright 首页截图、禁词扫描与 mock/stub/fake/fallback 扫描；证据已写入 `docs/evidence/ui-final-runtime-20260519/`。
-决策：禁词扫描分两层输出：渲染后 DOM/HTTP 响应层为 0 命中；前端源码层命中 3 处（`web/research-ui/src/api/client.ts` 中内部词黑名单正则），按“源码也扫描”规则标记为需人工判定是否阻塞验收。
待办：如需“源码 0 命中”严格验收，需产品/实现方确认是否允许在前端源码保留内部词过滤正则，或改为不含敏感词文本的等价实现。

## 21:28 [ui-round2-invest-subagent]
-完成：完成前端 round2 invest 对齐修复：补齐确认卡渲染与确认/取消动作、报告完成卡站内打开报告、三栏工作台信息密度增强、API client 扩展（confirm/create intent + scheduled/alert/settings 相关函数）及 HTML/非 JSON 错误文案收敛，并补齐对应前端测试；额外执行 `pnpm preview` + `npx playwright screenshot` 做工作台可视检查。
-决策：保留 `/reports/:reportId` 仅作兼容入口，但主交互改为在工作台内打开报告阅读；取消动作统一走 `confirmIntentDraft(decision="cancel")`，避免新增未授权产品路径。
待办：等待后端最终路由落地后做一次联调复验，确认新增 client 端点与后端契约命名完全一致。

## 21:28 [ui-round2-backend-api-subagent]
-完成：补齐 round2 后端/API/启动脚本阻塞项：新增 create/confirm intent、scheduled/alert CRUD+立即执行、数据源 test/save、LLM save/test、报告文件发送与 PDF 导出 HTTP 路由；`state.py` 改为真实 control workflow runner 桥接并移除默认不可用占位 runner；`start-research-ui.sh` 增加 `--run-backend` 内部 marker 门禁，阻止绕过 `start-control-runtime.sh`；补充对应 contract/脚本测试并通过。
-决策：workflow 运行态缺依赖时保持真实失败映射为产品错误，不做 mock/fallback 成功；`send-report-file-via-channel` 失败按 `FILE_SEND_UNSUPPORTED/NOTIFICATION_UNAVAILABLE` 返回可验证错误，不伪造成功 message id。
待办：与前端 subagent 做最终联调，确认新增 `/api/ui/*` 端点调用面与请求字段命名完全一致。

## 21:37 [ui-round2-test-subagent]
-完成：按 UI round2 独立测试要求完成取证：指定 pytest/pnpm 命令全跑、真实入口 `scripts/start-research-ui.sh` 启动并采集 `/api/ui/*` curl 证据、price alert run-now/close、data source save/test、LLM save/test、`/api/ui/not-exists` JSON 404、首页截图 `docs/evidence/ui-round2-test-20260519/home.png`、禁词扫描与 mock/stub/fake/fallback 扫描，并在测试后清理停止服务。
-决策：浏览器 MCP 不可用（DevTools 9222 连接错误）时，采用真实入口截图 + DOM/HTML/JS/API 探针替代“前端自动点击确认/取消”证据；确认卡点击链路以真实 API 响应补证，不伪造成前端点击成功。
待办：如需补齐“前端真实点击确认/取消”证据，需先恢复可交互浏览器自动化通道后复跑 UI 操作取证。

## 21:42 [codex-ui-round3-confirm-report]
-完成：修复 UI round3 两项 FAIL：确认卡后端字段统一为 `id`（移除 `cardId`），聊天流改用统一字段，补充后端契约测试；前端移除独立报告页主入口，把 `/reports/:reportId` 改为兼容重定向到工作台并通过 `?reportId=` 在工作台内打开报告阅读。
-决策：保留旧外链兼容能力但不保留独立报告页主流程，确保“报告阅读与聊天追问共存”始终在同一工作台内完成。
待办：无

## 21:48 [ui-round3-price-datasource-subagent]
-完成：修复 P4-05/P5-05/P6-05：`state.py` 改为注入真实 `data_gateway` 行情 quote provider 与数据源 health tester，移除价格提醒 `_raise_unavailable` 固定失败路径；`DataSourceSettingsService` 默认测试器改为失败、`test-data-source/save-data-source-instance` 启用前必须服务端真实探测通过，并注入 `.env.local` allowlist writer；补充并更新相关 unit/contract/e2e 测试与 runtime TestClient 证据。
-决策：首版只对 `tushare/coingecko/coinglass` 提供真实 health probe，其他内置类型不再伪成功，统一返回 `DATASOURCE_TEST_FAILED` 并保持禁用。
待办：如需扩展其余内置数据源“可启用”路径，需要逐个补真实探测实现与验收证据。

## 21:57 [ui-round4-api-validation-subagent]
-完成：在 research UI app 内新增 `/api/ui/*` 请求体验证错误映射，`RequestValidationError` 统一返回产品错误 JSON（`code=INVALID_INPUT`、固定中文文案、`severity=error`），不再暴露 FastAPI 默认 `422 detail`；并新增 contract 测试覆盖 `create-intent-draft` 缺字段场景。
-决策：仅在 `build_research_ui_app` 注册验证异常处理器并按路径前缀限定到 `/api/ui`，非 UI 路径继续走 FastAPI 默认处理，避免影响非 UI API 行为。
待办：无

## 21:59 [ui-round3-test-subagent]
-完成：完成 round3 独立复测与采证，执行指定 pytest/pnpm 命令、真实入口启动与 API/页面探针、禁词扫描、mock/stub/fake/fallback 扫描，并输出证据报告 `docs/evidence/ui-round3-test-20260519/round3_ui_retest_report.md`；判定总状态为 FAIL（数据源缺 key 样本存在 validated+保存成功复现）。
-决策：不改产品代码，仅写 evidence 与 memory；测试结束后手动清理 `start-research-ui.sh` 拉起的运行态并确认 5175/1933/18789 端口释放。
待办：由实现方修复“缺 key 不得 validated/保存成功”后复测 round3 全链路。

## 22:05 [codex]
-完成：完成 UI round5 数据源缺 key 伪成功窄修：在 `DataSourceSettingsService` 新增“测试/启用前密钥前置校验”，新建且需 key 的实例缺 `apiKeyReplacement` 会直接返回产品错误，且不会触发探测或持久化；保留已有 `credential_ref` 的 replace-only 行为，并新增 unit/contract 探针覆盖 `/api/ui/test-data-source`、`/api/ui/save-data-source-instance`、`/api/ui/list-data-sources`。
-决策：最小改动仅落在 UI 后端服务与测试，不改前端/价格提醒/OpenClaw/启动脚本；错误码采用产品允许集合（`INVALID_INPUT` 或 `DATASOURCE_TEST_FAILED`）中的 `INVALID_INPUT`，确保“缺 key 新实例”不再被环境全局 token 伪通过。
待办：若要把同类约束扩展到更多数据源类型（除当前 `requiresKey=true` 输入路径外），需按产品定义补类型级 requires-key 真值来源再扩展验证。

## 22:10 [ui-final-independent-retest-subagent]
-完成：仅执行 UI final 独立复测与证据采集，不改产品代码；完成指定 pytest/pnpm 回归、真实入口 `RESEARCH_UI_BUILD_FRONTEND=0 scripts/start-research-ui.sh` 双轮启动与清理、`/api/ui/*` 关键接口 curl 取证、`/reports/:id` 路由复验、禁词与 mock/stub/fake/fallback 扫描，证据落盘到 `docs/evidence/ui-final-retest-20260519/`。
-决策：对“无效 key 也失败”和“/reports/:id 非独立阅读页”补采二次实证（live curl + 前端路由测试）后再给最终判定，避免仅凭单测推断。
待办：无
