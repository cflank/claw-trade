## 05:51 [codex]
-完成：回答 CRYPTO 无新闻/舆情/API 密钥时的可运行范围与 only CoinGlass 降级边界。
-决策：建议把 only CoinGlass 定位为市场结构单源模式，不作为完整基本面、新闻或舆情覆盖的兜底。
待办：若要继续实施新闻/舆情，先由人类确认真实 provider 与可用 key/免费额度。

## 05:57 [codex]
-完成：给出 CRYPTO 最小 API 申请组合与显式降级运行方案，区分 only CoinGlass、CoinGlass+CoinGecko、以及可选 Reddit/搜索 key。
-决策：推荐最小可用组合为 CoinGlass 已有 key 加 CoinGecko 免费 Demo key；新闻优先用官方/监管 RSS 零 key，舆情不伪装成全网覆盖。
待办：若人类接受该最小组合，再按此边界实施资料包和 stage 策略。

## 06:07 [codex]
-完成：只读评估 guangxiangdebizi/FinanceMCP 对 claw-trade CRYPTO 数据源方案的帮助程度。
-决策：FinanceMCP 可作为 Binance K 线/技术指标参考或 A/HK/US/Tushare 方向参考，但不适合作为 CoinGlass、新闻事实源或舆情源的兜底核心。
待办：如需试接，只能先做 stage-scoped 包装验证，不直接暴露其完整 MCP 工具集。

## 07:43 [codex]
-完成：在无法申请 Reddit app 的约束下，重新给出 CRYPTO 最小舆情/事件预期降级方案。
-决策：移除 Reddit 作为第一阶段必选项，建议用 Alternative.me 市场情绪指数与 Polymarket 事件预期做零 key 补充，但不声称全网社交舆情覆盖。
待办：若需要打开 social_analyst，需先确认是否接受“市场情绪/事件预期”而非 Reddit/X/Telegram 全网舆情。

## 07:46 [codex]
-完成：评估 D4Vinci/Scrapling 是否适合抓取 CRYPTO 新闻和舆情。
-决策：Scrapling 可作为指定官方/公开页面的显式提取工具，不适合作为新闻或舆情 provider，也不应直接把其通用 MCP 暴露给 worker。
待办：若采用 Scrapling，需先列明允许抓取的来源清单、robots/条款边界和资料包输出字段。

## 08:06 [codex]
-完成：将 Brave Search、Bocha、NewsAPI、SerpAPI 加入 CRYPTO 可选新闻搜索 key 模板与数据源方案。
-决策：接受用户可选填写 provider key 的方向，但把“没有就略过”限定为不发起请求且显式写入 provider_attempts/data_gaps，不作为静默 fallback。
待办：真实 `crypto_news_data_pack` 实现前，`news_analyst` 的 CRYPTO stage 继续关闭。

## 08:11 [codex]
-完成：按用户追问将 `BRAVE_SEARCH_API_KEY`、`BOCHA_API_KEY`、`NEWSAPI_API_KEY`、`SERPAPI_API_KEY` 空字段补入本地 `.env.local`。
-决策：只检查键名存在性，不打印 `.env.local` 中任何密钥值。
待办：用户填写真实 key 后，再实现或验证 `crypto_news_data_pack` 的对应 provider 调用。

## 08:15 [codex]
-完成：说明 `NEWSAPI_API_KEY` 在 NewsAPI.org 注册页申请，并补充免费开发档限制。
-决策：提醒 NewsAPI 免费档只适合开发测试，不能当生产稳定新闻源。
待办：用户申请后把 key 填入 `.env.local` 的 `NEWSAPI_API_KEY`。

## 08:20 [codex]
-完成：检查 `.env.local` 的 CRYPTO 相关 key 填写状态、CRYPTO stage 开启状态和工具注册状态，并汇总当前全链路缺口。
-决策：确认 key 已足够推进新闻搜索实现，但完整 CRYPTO 12-worker 链路仍缺真实 news/social 资料包、live provider payload 证据和 stage 打开。
待办：下一步优先实现 `crypto_news_data_pack`，再做 fresh OpenClaw provider payload 验证。

## 08:23 [codex]
-完成：说明 `crypto_news_data_pack` 候选数据源中哪些免费、哪些不需要 key、哪些只是免费额度但仍需 key。
-决策：把官方/RSS/交易所公告/GitHub release 归为零 key 事实源，把 Brave/Bocha/NewsAPI/SerpAPI 归为需 key 的搜索发现源。
待办：实现新闻包时优先接零 key 官方源，再接用户已填 key 的商业搜索源。

## 08:29 [codex]
-完成：回应 CRYPTO 舆情工具尚未实现的问题，并给出最小 `crypto_social_sentiment_pack` 实现边界。
-决策：建议第一版只做 Alternative.me 市场情绪、Polymarket 事件预期和搜索 API 发现到的公开讨论线索，不声称完整社交舆情。
待办：等待人类确认后再实现舆情资料包代码与注册。

## 08:33 [codex]
-完成：新增 `docs/crypto_news_social_data_pack_design.md`，沉淀 CRYPTO 新闻与舆情资料包详细设计，并在总体方案中加入入口链接。
-决策：设计明确新闻事实源、搜索发现源、市场情绪源和事件预期源边界；stage 继续关闭，不新增 runtime gate/guard。
待办：按设计先实现 `crypto_news_data_pack`，再实现 `crypto_social_sentiment_pack`。

## 08:52 [codex]
-完成：按最新人类决策打开正式 12 个 CRYPTO worker，保持 `report_polisher` 关闭，并允许 news/social 无工具运行时在 L1 报告中上报资料缺口。
-决策：只放宽 CRYPTO news/social 的空工具配置校验；CN_A/US 新闻工具校验保持，未实现的 `crypto_news_data_pack` / `crypto_social_sentiment_pack` 不写入插件合同。
待办：后续实现真实 news/social 资料包后，再做 provider payload、真实 tool call 和 L1 报告验证。

## 08:55 [codex]
-完成：复核 sub-agent 变更，移除 CRYPTO worker prompt 中的内部执行层措辞，并补跑 stage/tool/prompt/frontline 协议验证。
-决策：保留“缺工具就由 worker 报告缺失”的产品边界，但 worker 可见 prompt 只表达证据边界，不提内部执行层。
待办：如要声明 live 行为，还需启动控制 runtime 并检查 fresh OpenClaw provider payload。

## 09:13 [codex]
-完成：实现并注册第一版 `crypto_news_data_pack` / `crypto_social_sentiment_pack`，CRYPTO news/social stage 改为必须挂真实资料包；补 provider attempts / data gaps / readiness 单测、插件合同和文档边界。
-决策：第一版只把 DefiLlama 安全/融资背景、Polymarket 事件预期、Alternative.me 市场级情绪和商业搜索发现作为资料输入；官方公告、GitHub、交易所公告、监管原始源和真实社交平台 API 仍明确上报缺口，避免搜索摘要或情绪指标伪装成事实覆盖。
待办：后续扩展官方公告/GitHub/交易所/监管原始源与真实社交平台数据源，并用 live provider payload 证明 worker 可见工具集合和真实 tool call。

## 09:39 [codex]
-完成：补齐 `crypto_news_data_pack` 的官方公告/GitHub releases/交易所公告/监管源/Exa 搜索路径，以及 `crypto_social_sentiment_pack` 的 LunarCrush/X/Reddit/Telegram/Discord/Exa 路径；同步文档、prompt、skill 和测试，移除 active CRYPTO 文档与 prompt 中的“第一版”表述。
-决策：缺 credential/config 或 provider 空结果只进入 `provider_attempts`、`data_gaps` 和 readiness，不伪装成新闻、舆情或平台覆盖成功；未新增 runtime gate/guard。
待办：真实 `.env.local` provider key 与源配置齐备后，按控制 runtime preflight 做 fresh OpenClaw provider payload 和真实 tool call 验证。

## 09:45 [codex]
-完成：只读检查 CRYPTO 与 CN_A 在 12-worker prompt、workflow 调度和前线工具调用主入口上的差异。
-决策：确认 workflow/stage 主结构同构，主要差异在前线工具 intent/provider-visible 工具、CRYPTO prompt 的资产域与缺口边界；同时发现 CRYPTO 下游 approved L1 prompt material 注入路径与 CN_A 不一致，需后续修正或验证。
待办：补齐 CRYPTO 下游 prompt material 注入/测试前，不能声称 12-worker CRYPTO 下游已真实消费前线 L1 报告。

## 10:00 [codex]
-完成：打开 CRYPTO `report_polisher`，按 A 股终稿润色边界重写 CRYPTO prompt，并让 CRYPTO 下游/终稿 worker 注入已批准 L1 上游正文。
-决策：`report_polisher` 不调用工具，只整理组合经理和上游 approved reports；缺失新闻、舆情、链上或图表证据必须保留为缺口，不能补写。
待办：全量 pytest 仍有既有收集错误：未批准的 artifact_flow guard 缺失，以及 CN_A fundamental/social 裸模块名污染；未做 live provider payload 验证。

## 10:21 [codex]
-完成：按“Scrapling 暂不接入”更新 CRYPTO 数据源文档，并实现本地 `crypto_market_data_pack`，用于公开交易所 OHLCV、技术指标和 PNG 图表资产补充。
-决策：BB / CoinGlass 继续是 CRYPTO 市场结构主源；本地 market pack 不替代 OI、资金费率、清算、多空比、期权、ETF 等市场结构数据。artifact flow guard 只明确缺口，不新增未批准 runtime guard。
待办：如需修复全量 pytest 的 `src/claw_trade/guards/artifact_flow.py` 缺失，需人类明确批准该 guard；CN_A fundamental/social 裸模块名污染另行处理。

## 10:44 [codex]
-完成：按人类批准补齐 `artifact_flow` runtime guard 文件、CRYPTO 下游材料注入相关缺口、CN_A 测试隔离和 acceptance 资产缺口，并完成全量验证。
-决策：guard 仅迁移 runner 内既有 artifact flow 逻辑并登记批准 hash；测试修复只隔离运行时环境变量污染，不改生产 fallback 语义。
待办：后续若要证明 CRYPTO live 行为，还需按控制 runtime preflight 实跑 OpenClaw provider payload 和真实 tool call 取证。

## 10:57 [codex]
-完成：根据真实性评审修正 CRYPTO report_polisher 批准说明、fundamental skill 挂载和资料包 partial/insufficient 工具返回文案，并补合同测试。
-决策：只强化“工具调用完成不等于资料覆盖完成”的 worker 可见说明，不新增 runtime guard、风格 gate 或投资判断 gate。
待办：后续仍需用 live OpenClaw provider payload 验证真实 CRYPTO tool call 与 worker 可见摘要。

## 11:05 [codex]
-完成：只读对比 CN_A/A股 与 CRYPTO 在 prompt、workflow、guard、工具调度和材料注入上的差异，并跑相关合同验证。
-决策：确认 workflow 基本同构，差异主要在前线工具、CRYPTO 资料源边界和 provider prompt runtime guard 覆盖范围。
待办：如需达到 runtime prompt guard 同等覆盖，需明确批准后把 CRYPTO 纳入 provider payload 污染检查并做 live proof。

## 11:22 [codex]
-完成：按固定 preflight 用 `scripts/start-control-runtime.sh -- <run_control>` 实跑 CRYPTO BTC frontline_ready live 验证，run_id=`run-20260516-151234-6721fd58`，四个前线 worker 均 succeeded 并进入 approved manifest。
-决策：确认配置不再是主 blocker；live 证据显示 `crypto_fundamental_data_pack` ready，`crypto_news_data_pack`/`crypto_social_sentiment_pack` partial 且明确记录缺口，`crypto_market_data_pack` 在 market worker 中因 `ctx.singleWorkerCommand` 缺失报错但 BB 工具成功。
待办：下一步应调查并修复 market worker 同时暴露 BB MCP 与本地 pack 时 `crypto_market_data_pack` 缺 singleWorkerCommand 的运行时上下文问题。

## 11:39 [codex]
-完成：修复 OpenClaw 混合 core/plugin 工具路径未把 single-worker command 传给插件工具的问题，并按 BB 本地最新同步版本重跑 CRYPTO BTC market live proof，run_id=`run-20260516-153604-451cb75b`。
-决策：BB 最新改动未改变工具名或入参；claw-trade 只需修复 OpenClaw 工具上下文传递，并补 CRYPTO market prompt 红线，禁止用 readiness 代替资金费率/OI/CVD/清算点位等具体读数。
待办：GitHub 远端因无凭证无法 fetch，后续若 BB 再更新需先同步本地 `/mnt/d/src/BB` 或配置 GitHub 凭证后再复核。

## 13:03 [codex]
-完成：按人类要求调用 `gpt-5.5`/`xhigh` subagent 评审 CRYPTO 共享 provider cache/限流方案，修订到 V2 后复审通过。
-决策：最终方案明确 CRYPTO cache 不读取 OpenViking、不让 controller 预取、不把 cache_hit/stale/rate_limited/config_blocked 伪装成 success，并要求 source_role、密钥脱敏、TTL、cached empty 分支和 live proof。
待办：若进入实现，先写 `crypto_provider_cache.py` 合同和单测，再逐个迁移四个 CRYPTO 资料包。

## 13:33 [codex]
-完成：实施 CRYPTO 共享 provider cache 第一阶段：新增独立 `crypto_provider_cache.py`、Mongo `crypto_*` collection/index、market pack Binance OHLCV cache 接入、边界合同和单测。
-决策：只在 OpenClaw worker 工具调用链内使用缓存；不碰 workflow controller、不读取 OpenViking cache、不改 runtime guard；`cache_hit` 保留原状态且 cached empty 会降级为缺口。
待办：下一步按同一合同迁移 `crypto_fundamental_data_pack`、`crypto_news_data_pack`、`crypto_social_sentiment_pack`，再做 fresh live proof。

## 14:04 [codex]
-完成：实施 CRYPTO 共享 provider cache 第二阶段：fundamental/news/social 资料包接入 JSON/text 缓存，保留 source_role，并补齐 invalid config、schema_invalid、irrelevant search 和 Polymarket malformed 测试。
-决策：缓存只省 provider 请求，不改变证据身份；搜索仍只作发现线索，Alternative.me 只作市场级情绪，Polymarket 只作事件预期，OpenViking 不作 provider cache。
待办：后续如需证明真实 provider 行为，按 live runtime preflight 跑 fresh OpenClaw/tool call 取证。

## 16:21 [codex]
-完成：修复 CRYPTO 前线 worker 静默跳过资料包的真实性漏洞，OpenClaw 单 worker 首轮支持必需工具选择，并修复 CRYPTO provider cache Mongo upsert `_id` 冲突。
-决策：新增 guard 只约束 CRYPTO 前线资料包真实调用，不检查报告风格或投资判断；OpenClaw 只做单 worker provider payload/tool_choice 透传；provider cache 保持 Mongo/非 OpenViking 存储。
待办：后续若要验证完整 12-worker CRYPTO 下游报告，还需跑 full report live gate 并检查 downstream approved L1 消费。

## 16:33 [codex]
-完成：按 AGENTS 与人类明确要求补强材料流边界：CRYPTO provider prompt 也禁止机器协议字段泄漏，L1 批准拒绝整篇 JSON 文档作为 worker 报告，并补对应合同测试。
-决策：只保护 worker 间材料必须是自然语言 L1 report；JSON 仍可存在于工具返回、provider evidence、manifest、receipt、cache 和审计文件中，但不能成为 worker 交接正文。
待办：完整 12-worker CRYPTO live run 仍需验证每个 worker 的真实 `raw-output.md` 和 downstream provider payload。

## 16:46 [codex]
-完成：修复 HK provider prompt 机器协议污染检查与下游 approved L1 自然语言正文注入，补 HK 回归测试并通过合同验证。
-决策：HK `research_manager` prompt 使用中文前线报告与辩论历史占位符，按非 US 自然语言材料边界处理；只更新本次授权范围内的 `provider_request.py` guard 哈希，未新增风格或投资判断 gate。
待办：如需 runtime 证据，还需按 live runtime preflight 跑 HK fresh OpenClaw provider payload。

## 16:58 [codex]
-完成：按固定 runtime preflight 跑完 HK 腾讯控股 `00700.HK` fresh live `/report` 全链路，run_id=`run-20260516-205008-351e1cdf`，13 个 worker 均 succeeded，证据包导出到 `docs/evidence/trading_claw_trade_hk_fresh_live_run-20260516-205008-351e1cdf_md/`。
-决策：使用 `scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py ...` command mode；sandbox 内 MongoDB 启动失败后按同一固定命令提升权限重跑，不更换启动方式、不使用 mock/stub/fake/fallback。
待办：后续若评审 HK 报告质量，直接查看该证据包中的每个 worker `*_final_prompt.md`、`*_llm_back.md`、`*_report.md` 和 `final_report.md`。

## 17:09 [codex]
-完成：完成 `00700.HK` TradingAgents-CN live capture，抓取 12 个 worker 的 final prompt、LLM back、report 与 final_report，并生成 HK claw-trade 对 TradingAgents-CN 的详细对比分析。
-决策：TradingAgents-CN 使用 `/home/frank/src/TradingAgents-CN/.venv/bin/python scripts/capture_tradingagents_cn_full.py` 真实外部 LLM 模式，证据落在 `docs/evidence/tradingagents_cn_full_hk_00700_20260516_live_md/`；对比分析落在 `docs/evidence/trading_claw_trade_hk_fresh_live_run-20260516-205008-351e1cdf_md/hk_vs_tradingagents_cn_detailed_analysis.md`。
待办：如需进一步证明每条券商评级、资金流、回购、宏观新闻等事实，需要做逐 claim 原始来源审计。

## 17:15 [codex]
-完成：按人类要求将 HK claw-trade 与 TradingAgents-CN 的详细对比分析报告主体改为中文。
-决策：只改我生成的对比分析文档，不重写原始 worker 输出、LLM 回包或抓取 summary，以保持取证原文不被二次改写。
待办：若需要所有抓取 summary 也中文化，应另生成中文索引文件，而不是覆盖原始证据文件。

## 17:22 [codex]
-完成：按既有 CN_A/US 对比报告结构，补充 HK 对 TradingAgents-CN 分析中的 `final_prompt`、`llm_back`、`report` 三层指标表和逐 worker 三层解读，并在报告中标明最终报告路径。
-决策：继续只更新分析文档，不改原始 prompt、LLM back、worker report 或 final_report 取证文件。
待办：若要继续提高分析严谨度，可补逐 claim 来源审计，尤其是 TradingAgents-CN 社媒报告中的券商评级、资金流和情绪评分。

## 17:19 [codex]
-完成：修复 CRYPTO market worker 直接暴露 BB 大 JSON 导致 provider payload 截断的问题，改为 `crypto_market_data_pack` 集中读取 BB/CoinGlass 并输出自然语言简报；同时锁定四个 CRYPTO 资料包使用 runtime 日期。
-决策：BB 原始 payload 只写入 provider_raw 证据，worker 可见材料只保留紧凑摘要、readiness、provider_attempts、data_gaps、conflicts；market worker 可见工具收敛为单一资料包，避免重复调用和限流。
待办：后续按 live runtime preflight 跑 fresh BTC CRYPTO report，检查四个 frontline raw-output 与 provider payload 不再出现 BB 大包截断。

## 17:39 [codex]
-完成：核对 HK claw-trade 与 TradingAgents-CN `market_analyst` 三层证据，确认 prompt 视觉长度差异主要来自工具材料展开方式不同：claw 使用紧凑自然语言行情包，TradingAgents-CN 展开近 10 日行情、财务指标和数据统计。
-决策：不把 CN market prompt 中的 PE/PB/EPS/ROE 直接视为 HK market 缺失，因为 claw-trade 12-worker 边界下基本面数据应由 `fundamental_analyst` 承接；但 HK market 数据包过度压缩会削弱技术分析证据密度，需后续按自然语言材料边界补齐近端价格/量价细节。
待办：如要做 TradingAgents-CN market prompt parity，优先增强 HK `market_market_data_pack` 的自然语言事实材料，而不是让 Python 写分析或把 JSON 表塞进 prompt。

## 17:53 [codex]
-完成：修复 CRYPTO market pack cache-hit attempt 重复写入导致真实 OpenClaw 工具返回 `TOOL_PROTOCOL_ERROR` 的问题，并补资金费率单位边界；fresh BTC market worker live run `run-20260516-215140-1dc5178a` 显示工具成功、readiness=ready、worker 报告引用自然语言资料包。
-决策：只允许完全相同的 provider_attempt duplicate key 写入幂等通过，内容不一致仍失败；资金费率只暴露“原始值/沿用资料包单位”，禁止 worker 自行换算百分比或年化。
待办：后续如要最终验收 CRYPTO 全链路，还需跑四个 frontline 或完整 12-worker `/report`，确认 news/social/fundamental downstream 材料也全是自然语言 L1。

## 18:12 [codex]
-完成：将 CRYPTO market `reader_brief` 从字段式紧凑摘要升级为 BB/CoinGlass 详细自然语言市场材料，覆盖数据状态、多周期技术结构、指标逐项材料、衍生品/清算、链上/宏观/AHR999、条件化操作框架和 data_gaps。
-决策：操作框架标注为“BB 技术材料，不是最终投资裁决”，只由真实 BB/资料包字段转写，不让 Python 替 trader/PM 写最终结论；真实工具层验证新版 BTC brief 约 19.8KB/132 行，无截断标记。
待办：如需看 worker 报告效果，下一步按 fixed runtime preflight 重跑 CRYPTO market worker 或完整 frontline。

## 18:23 [codex]
-完成：根据 HK market prompt 对比结论与 memory 复盘，增强共享 `market_market_data_pack` 的自然语言 `reader_brief`，为 HK/CN_A worker 增加最近 10 个交易日 OHLCV 明细，并保留技术指标、图表事实和来源摘要。
-决策：CRYPTO 已在独立资料包中完成详细自然语言市场材料增强，本轮不改；CN_A/HK 走共享市场资料包，改资料包事实正文而非 prompt 或 Python 投资分析，避免让 Python 写观点或塞 JSON 给 worker。
待办：如需 runtime 证据，还需按 fixed runtime preflight 重跑 HK/CN_A market worker 或完整 `/report`，检查真实 provider final prompt 中的工具返回是否包含最近 10 日自然语言明细。

## 18:29 [codex]
-完成：按 gpt-5.5/xhigh sub-agent 评审建议补强 market 资料包 focused tests，HK/CN_A 均断言最近 10 日 OHLCV、均线、图表、来源摘要存在且不含机器协议词。
-决策：评审无阻断问题，唯一建议是测试覆盖收紧；本轮只补测试，不改生产逻辑。
待办：若进入验收阶段，仍需 fresh live provider prompt 证据证明真实 HK/CN_A worker 可见材料已更新。

## 21:03 [codex]
-完成：检查 HK claw-trade 与 TradingAgents-CN 取证目录中的 worker final prompt、LLM back、report 和 final report 是否含 JSON/机器协议字段；worker 报告与 LLM 输出未命中 JSON/机器字段，claw 前线 final prompt 仅命中工具调用参数 JSON。
-决策：将 `*_raw.json`、capture summary/state/request 等审计 JSON 与 worker 自然语言正文分开判定；工具调用参数 JSON 属于工具协议记录，不等同于下游 worker 自然语言交接正文。
待办：若要求 provider 可见文本里也不展示工具调用参数 JSON，需要另行调整 provider prompt capture/render 或工具调用展示口径。

## 18:32 [codex]
-完成：继续验证 CRYPTO market 详细自然语言包，并修复 BB/CoinGlass 清算地图字段未被压入 compact 摘要的问题；fresh BTC market live run `run-20260516-222527-e7ec86b6` 显示 worker 可见材料约 19.6KB，清算簇 82,300.2 / 247.2M 与 77,126.7 / 78.99M 已进入报告。
-决策：只修资料包字段抽取和自然语言转写，不暴露 BB 原始 JSON，不让 Python 写最终投资裁决；provider payload 仍只暴露 `crypto_market_data_pack`，无 direct BB tool、无 truncation、无 protocol error。
待办：如需最终验收 CRYPTO 全链路，应继续跑四个 frontline 或完整 12-worker `/report`，确认其他 worker 的自然语言 L1 材料也未被压缩成提纲。

## 18:52 [codex]
-完成：完成 BTC CRYPTO fresh live `/report` 全链路验收并返修 market 操作框架错位清算价；最终通过 run `run-20260516-224140-98a84127`，13 个 worker/export worker 均 succeeded，最终报告和 2 张 market 图表已导出。
-决策：首轮全链路 run `run-20260516-223104-62b07b99` 暴露 provider-visible market 资料包中“跌破 82300.2”的上下方清算价错位，因此修正为优先使用 `nearest_above/nearest_below`，补单测后重跑完整 live；最终 provider prompt 已显示偏多失效/偏空触发为 `77126.7`。
待办：新闻/社交资料包仍因部分 provider config/key/endpoint 返回缺口而为 partial，但已显式记录 data_gaps 并未冒充新闻事实源或社交共识；若要提高数据覆盖，需要补对应真实 provider 配置和源白名单。

## 19:09 [codex]
-完成：修复 report_polisher 终稿过度压缩和 CRYPTO/HK 图表落到报告末尾的问题；新 BTC CRYPTO fresh live run `run-20260516-230036-c20f1726` 产出 14,434 字终稿，图表位于“市场结构与技术指标分析”章节内，OB/POC/FVG/AMD/SMC/CVD/资金费率/OI/AHR999 等指标进入正文。
-决策：只改终稿编辑 prompt 与图表导出定位，不让 Python 重写投资判断、不新增风格 gate；同时把 CN_A/HK/US/CRYPTO report_polisher 都补上 H1 开头、禁止过程性开场、保留 market 指标推导链的约束。
待办：如需同等验收 HK/CN_A，需要按 fixed runtime preflight 分别重跑 fresh `/report`，确认终稿也不再压缩且图表在技术章节。

## 19:31 [codex]
-完成：修复 report_polisher 仍会漏掉完整 BB/market 指标材料的问题；exporter 现在在有润色稿时机械搬运 `market_analyst` approved L1 到终稿第二章，新 CRYPTO fresh live run `run-20260516-232205-121544b8` 产出 24,398 字终稿，包含完整数据质量、指标覆盖、TD、谐波、双线反转缺失说明等市场材料。
-决策：Python 只搬运已批准自然语言 L1 并降级 Markdown 标题层级，不总结、不改写、不新增投资判断；对比脚本纳入 `report_polisher`，CRYPTO 证据包与 A股 `run-20260514-113338-8a770ce9` 已生成逐 worker final prompt / llm back / report 对比。
待办：如需进一步收敛报告形态，可单独决定是否让终稿第二章保留 market 原文的同时减少 report_polisher 自己的重复市场段落。

## 20:25 [codex]
-完成：修复 CRYPTO 终稿和 worker 报告暴露内部字段名的问题，market/news/social/fundamental 资料包与对应 prompt 改为中文读者表达，最终 fresh live run `run-20260517-001630-13c4e0d7` 通过终稿和 13 个 worker report 字段泄漏扫描。
-决策：只在资料包自然语言材料和 worker/report_polisher prompt 源头修复，不在 exporter 层改写投资正文；保留技术缩写 RSI/MACD/OI/CVD/FDV/TVL 等市场通用表达。
待办：若还要进一步提升报告审美，可单独做终稿重复段落和章节语气优化，不新增 runtime 风格 gate。

## 21:24 [codex]
-完成：按 fixed runtime preflight 复跑 00700.HK 港股 claw-trade live，全 worker succeeded 但 final export 因缺图表资产失败；随后复跑同标的 TradingAgents-CN live 并采集 12 worker prompt/LLM back/report/final report，生成中文逐 worker 对比分析。
-决策：不把 claw `report_polisher_report.md` 冒充最终报告；本轮 claw 证据定位为 HK market 工具未取到行情/图表导致的失败样本，TradingAgents-CN market/fundamental 有数据但 news/social/PM 存在未证实叙事扩写。
待办：继续修 HK market/news/social/company-level 数据源覆盖后，重跑 fresh HK live 取得成功终稿与图表章节位置证据。
