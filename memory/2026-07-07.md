# 2026-07-07

- 完成：按用户要求将目标机 `192.168.1.21` 清理为新机状态，准备重新安装测试。清理前确认 `/opt/claw-trade`、`/etc/claw-trade`、`/usr/local/lib/claw-trade`、`/opt/senseshield` 存在，`claw-trade-control.service` 与 `claw-trade-rescue.service` 为 failed，`27017/1933/18789/5175` 有监听，`clawtrade/clawkiosk` 用户组存在。
- 清理范围：先用 `/home/frank/.claw-trade/license.key` 尝试解绑 Virbox 授权并返回 `succeed`；随后停止并禁用 `claw-trade-*` 服务和 timer，停止 Virbox 服务，清理 `/opt/claw-trade`、`/etc/claw-trade`、`/usr/local/lib/claw-trade`、`/etc/sudoers.d/claw-trade-update`、相关 `/tmp` 安装包和 pid/log，删除 `claw-trade*` systemd unit，purge `senseshield` 并删除 `/opt/senseshield`、`/etc/senseshield`、`/var/log/senseshield`，删除 `clawtrade/clawkiosk` 用户组和 home。
- 验证：复查显示 `/opt/claw-trade`、`/etc/claw-trade`、`/usr/local/lib/claw-trade`、`/opt/senseshield`、`/etc/senseshield`、`/var/log/senseshield` 均不存在；`systemctl list-unit-files 'claw-trade*' 'senseshield*' 'virboxlcc*'` 为 0；`systemctl --failed` 为 0；`clawtrade/clawkiosk` 用户组不存在；`27017/1933/18789/5175` 无监听；`dpkg -l | grep -E 'senseshield|claw-trade'` 无输出。
- 根因：新机重装后 ETH/CRYPTO 报告 `run-20260707-073849-79631f82` 失败，UI 显示“报告运行目录不可写”。这不是 2026-07-03 的 `pandas_ta/numba cache` 问题复发。真实 `state.json` 写明：`openviking: openviking probe prepare failed: [Errno 13] Permission denied: 'runs'`。`/opt/claw-trade/shared/runs` 实际为 `clawtrade:clawtrade 0750`，并且 `sudo -u clawtrade` 写入探针成功；失败点是 OpenViking boot probe 使用相对 receipt 路径 `runs/probe/openviking/receipt.json`，而安装脚本临时启动 control/UI 时工作目录是 root-owned release `/opt/claw-trade/current`，服务用户无法在 release 下创建 `runs`。
- 热修：目标机停止临时 runtime 后，改用 `/opt/claw-trade/shared` 作为 cwd 重新启动 control/UI。验证 UI `/healthz` 返回 `{"status":"ok"}`，OpenViking `1933/health` healthy，OpenClaw `18789/health` live，`27017/1933/18789/5175` 均监听；OpenViking production env probe 返回 `ProbeResult(ok=True, reason=None)`，相关进程 cwd 为 `/opt/claw-trade/shared`。
- 源码修复：`src/claw_trade/artifacts/openviking_client.py` 的 probe receipt 路径在设置了 `CLAW_TRADE_REPORT_RUN_DIR` 时改用该 run root；`scripts/production/install_factory_test_ubuntu.sh` 的临时 control/UI 启动 cwd 改为 `${install_root}/shared`，避免旧包安装后也把相对 `runs` 写到 release 目录。回归：`uv run pytest tests/contracts/test_openviking_client_contract.py tests/unit/production/test_production_paths_contract.py tests/unit/test_report_workflow_settings.py -q` 为 `55 passed`；`bash -n scripts/production/install_factory_test_ubuntu.sh` 和 `git diff --check` 通过。
