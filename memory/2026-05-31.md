2026-05-31：按评审意见重写《docs/数据层总体设计.md》关键执行合同，修正流程顺序、Provider 批量能力声明、SingleFlight 持久化状态机、IngestPipeline 所有权与 OpenViking 边界，并补多市场字段合同与后台维护（seed/增量/修复）路径。
2026-05-31：二次窄修，收紧 QueryPlanner/DataPlan 边界（不含 provider 选择与 batch 计划），并补全 ExecutionGate 的 cache/wait/shared_result 分支语义与流程图中 ProviderSelector 读能力后选候选 provider 的顺序。

## 02:01 [codex]
-完成：重写 `docs/数据层详细设计.md`，按总体设计落地接口、协调、仓库、执行、插件、入库证据、数据库、多市场和后台维护的伪码级合同。
-决策：目标文档不沿用旧 OpenBB 前缀命名；数据层只返回 `DataResult` 的 refs/gaps，不生成 worker 资料包，不决定 report/select 成败。
待办：后续编码时先从 `DataRequest -> Warehouse -> DataResult` 最小闭环开始，并为限流、single-flight、batch policy 增加合同测试。

## 02:45 [codex-subagent]
-完成：任务（主代理评审 8 项文档修正，无独立编号）已完成；只修改 `docs/数据层详细设计.md` 并追加本日志，未改代码。
-决策：删除未授权 `provider_configs` 目标 collection；provider 优先级回收到 `ProviderPlugin.capabilities()` / `ProviderCapability`；仓库空/过期分别使用 `warehouse_missing` / `warehouse_stale`；provider-result cache TTL 只按 `expires_at`/`stale_until` 删除；维护路径允许 `SeedInput`/本地文件入库但不改变 report/select 的 `DataRequest -> DataResult` 公共合同。
-命令：读取要求文件 `sed -n ... AGENTS.md docs/数据层总体设计.md docs/架构设计.md docs/数据层详细设计.md` exit 0；定位问题 `rg -n ... docs/数据层详细设计.md` exit 0（`provider_configs|execute_batch|src/claw_trade/data/` 禁止残留检查 exit 1，表示无命中）；行号复核 `nl -ba docs/数据层详细设计.md | sed -n ...` exit 0。
-偏差：未运行代码测试或 live runtime；本任务限定为文档修改，未触碰 runtime。
-实现状态：真实文档落地完成；mock/stub/fake/fallback 状态：未发现、未引入、未用作绕过。
-覆盖：8 项评审意见均在详细设计中有对应修正，待主代理复核。
待办：主代理对 `docs/数据层详细设计.md` 做下一轮评审。
