## 03:54 [codex]
-完成：将 13 个 `agents/<worker>/prompts/HK.md` 从失败占位改为真实港股 prompt 草稿，覆盖前线四 worker、投研辩论、研究经理、交易员、风险三人组、组合经理和终稿编辑；补充 prompt 合同测试，证明 HK prompt 是 draft 真实文本、无机器协议字段、前线只声明 HK 工具、并保留 TradingAgents-CN 报告感/辩论感。
-决策：HK profile 仍保持 `approved: false`，不打开运行入口；本轮只做 prompt 草稿迁移，不新增 runtime guard，不把“真实性”扩展成保守/memo/禁止强观点规则。
待办：继续补 HK market/news/social 数据包和 HKEXnews/公司公告原文校验；之后用真实 provider payload 与 LLM 输出验证 HK prompt，再决定是否批准 HK profile。

## 04:09 [codex]
-完成：根据 gpt-5.5/xhigh subagent 评审修正 HK 组合经理 prompt，把研究经理投资计划和交易员交易计划分开；补充 HK 全 worker prompt 风格合同、持有语义禁令回归检查、HK 工具未注册不得批准的合同测试，并为组合经理补齐货币运行时变量。
-决策：不注册 `hk_market_data`、`hk_news_data`、`hk_social_sentiment` 的不可用工具；将三者记录为 HK profile 批准前阻塞项，HK 继续保持 `approved: false`。
待办：实现真实 HK market/news/social 工具并完成 fresh provider payload 验证后，再评审是否批准 HK profile。

## 04:17 [codex]
-完成：撤销未确认的港股行情、新闻、社交 provider-visible 工具名；从 HK prompt、STAGES、工具 registry、港股设计文档和任务清单中移除这些具体名字，只保留 `hk_fundamental_data` 这个已有真实插件入口。
-决策：港股行情、新闻、社交现在只作为待定义数据能力记录；对应具体工具名、schema 和 provider 合同确认前，不写入 STAGES，不写入 prompt，不注册到 OpenClaw 插件。
待办：确认三类 HK 数据能力的真实工具合同后再实现并用 provider payload 验证。

## 04:30 [codex]
-完成：将 HK 前线改为复用现有四个 domain pack 模型可见工具名：`market_market_data_pack`、`fundamental_fundamentals_data_pack`、`news_news_data_pack`、`social_social_sentiment_pack`；移除 `hk_fundamental_data` 模型可见工具和 skill export；把港股基本面真实实现挂到 `fundamental_fundamentals_data_pack` 的 `market=HK` 内部路由。
-决策：不再引入任何 `hk_*` 前线工具名；行情、新闻、社交只先放开 OpenClaw JS 工具边界接收 `market=HK`，不把 Python 内部 CN_A provider 硬改成假港股支持。
待办：继续把 market/news/social 三个 pack 的 Python provider plan 按 `market=CN_A/HK` 做真实分流，并用 provider payload 与真实 provider attempts 验证。

## 04:56 [codex]
-完成：把 `market_market_data_pack`、`news_news_data_pack`、`social_social_sentiment_pack` 的 Python 内部链路改为支持 `market=HK`；行情主源走 Tushare `hk_daily_adj`，免费兜底包含 AkShare `stock_hk_hist` 和 EastMoney `push2his_kline`；新闻排除 A 股专用 `akshare.stock_news_em`，保留 Tushare 公告和宏观/搜索 provider attempts；社交支持 5 位港股代码匹配。
-决策：HK 仍复用现有模型可见工具名，不恢复任何 `hk_*` 前线工具；基本面 Python 直连包继续只允许 CN_A，港股基本面由插件分发到 `hk_data_pack`。
待办：执行 fresh OpenClaw provider payload/live provider run，确认真实 provider 返回、模型可见工具 schema 和 HK profile 批准条件；HK profile 继续保持 `approved: false`。

## 05:03 [codex]
-完成：按指定命令执行 HK live 控制链路测试（含 sandbox 受限后同命令 require_escalated 重跑），生成 run `run-20260515-090241-8171c277`；控制面在 worker 启动前被 `profile is not approved: HK` 阻断并失败退出。
-决策：保持 HK profile blocked，不做任何修复、绕过或 fallback；仅采集 run/state/failure 证据并回传主代理。
待办：如需继续 HK live，先由主代理/人工明确 HK profile 批准策略后再重跑。

## 05:28 [codex]
-完成：按人工指令打开 HK profile 和 13 个 worker/report_polisher 的 HK stage/prompt 批准状态，清理 agent-facing HK 未批准旧说明；修正前线 pack 工具说明为 CN_A/HK 通用；修复 provider 错误摘要中脱敏 URL 仍带协议导致 market pack schema 拒绝的问题。HK live 前线 run `run-20260515-092332-24b74364` 已完成到 `frontline_ready`，四个前线 worker 均 OpenClaw 成功返回，四个 pack 工具均真实调用成功。
-决策：不新增 guard，不绕过 provider；Tushare 仍为港股主源，Tushare 频率限制后真实尝试 AkShare/EastMoney 免费兜底。当前行情资料为空的根因是 Tushare `hk_daily_adj` 频率超限、AkShare/EastMoney 远端断连。
待办：等待 Tushare 频率窗口恢复后重跑 HK market pack；继续补强港股行情免费源可用性和新闻/社交相关性。

## 05:44 [codex]
-完成：用 `.env.local` 的正式 Tushare token 查询账号状态；项目配置地址返回 token 有效、用户 ID 6194、到期时间 2026-06-08、剩余 24天6小时，但未返回积分金额字段。
-决策：不把未返回的积分数脑补成余额；按 Tushare 官方文档，总积分需登录个人主页查看，API 本次只能证明账号有效和到期状态。
待办：如需精确总积分，需用网页个人中心的真实登录态查询。

## 06:22 [codex]
-完成：将“HK prompt 与 TradingAgents-CN 港股基准详细对比”加入 `docs/任务清单.md`，标记为等待港股接口恢复后执行；清理可再生测试缓存、runtime dev-services、TradingAgents 临时缓存和 probe 目录。
-决策：保留 `runs/run-*` live 证据目录，避免丢失 provider payload 追溯；不把当前 HK prompt 误报为已完成详细对齐。
待办：提交并推送本轮 HK profile、HK data pack、prompt 与文档更新。

## 07:00 [codex]
-完成：评估 `cflank/BB` 加密交易分析 skill 与 `bb_crypto_data` 只读 MCP 如何集成到 claw-trade；结论是应作为 CRYPTO profile 的前线数据能力和质量契约，不作为第 13 个 worker，也不替代 12-worker 工作流。
-决策：建议采用 adapter/intent 集成，保留 BB envelope 的 `readiness/data_gaps/conflicts/sources`，由 claw-trade 控制面决定 profile、stage policy、artifact approval 和 PM 最终权威。
待办：等待人工确认集成路线与是否允许写设计文档/实施计划；未改代码。

## 07:37 [codex]
-完成：将 BB/CRYPTO 数据源 key 标准化进 claw-trade `.env.example`、本地 `.env.local` 和 `docs/架构设计.md`；从 Windows 用户环境同步已存在的 `COINGLASS_API_KEY`、`COINGLASS_API_BASE`、`COINGLASS_API_HEADER_NAME`、`FRED_API_KEY`、`TAVILY_API_KEY` 到本项目本地环境。
-决策：只做数据源配置标准化，不打开 CRYPTO profile，不新增或修改任何 runtime guard/gate，不接触 worker 调度。
待办：下一步再把 `market_analyst` 的 CRYPTO 工具路由接到 BB，并用真实 provider payload 验证。

## 07:44 [codex]
-完成：按人工提供的 CoinGlass/Keystore 代理说明校准本地 CRYPTO 数据源：`.env.local` 使用 Keystore 代理根地址、`X-Api-Key` header、Pro plan 10 次/分钟限速；`.env.example` 与 `docs/架构设计.md` 同步模板和字段说明。
-决策：密钥只进入 ignored 的 `.env.local`；tracked 文件仅记录字段名、代理根地址、header 名和限速，不记录密钥明文；未新增任何 gate/guard。
待办：后续接入 `market_analyst` CRYPTO 工具时，复用该环境变量口径并做真实 provider payload 验证。

## 07:45 [codex]
-完成：读取 `D:\src\BB\memory` 并归纳 BB 接入 claw-trade 的复盘教训，重点包括不重复拉取 ready 域、尊重 readiness/data_gaps、CoinGlass 限速、MCP 重启/HTTP 持久化、指标覆盖矩阵和 PowerShell/路径/编码问题。
-决策：把 BB memory 中的 “gate” 仅理解为 skill/路由纪律，不转化为 claw-trade 新 runtime gate；继续遵守不新增 guard/gate 的人工指令。
待办：后续实现 `market_analyst` CRYPTO 接 BB 时，把这些教训写进工具路由和测试策略。

## 07:51 [codex]
-完成：根据人工修正，将 BB 定位明确为 OpenClaw worker 挂载的 skill 加 `bb_crypto_data` MCP 接入，并同步到 `docs/架构设计.md`。
-决策：初始只建议给 `market_analyst` 的 CRYPTO stage 暴露 BB skill/MCP；BB 的 `readiness/data_gaps` 只作为 worker 可解释的数据质量材料，不转成 claw-trade runtime gate。
待办：实现 `market_analyst` CRYPTO stage 的 BB skill/MCP 可见工具路由，并用真实 provider payload 验证。

## 08:24 [codex]
-完成：实现 `market_analyst` 的 CRYPTO stage 接入 BB skill + `bb_crypto_data` MCP：标准化 BB MCP 路径配置，CRYPTO market prompt/tool intent/skill manifest 生效，OpenClaw provider payload 只暴露 `bb_crypto_data__build_trade_context`，完整单 worker live run `run-20260515-122130-48b98a67` 真实调用 BB MCP 并写入 L1 报告。
-决策：只打开 `market_analyst` 的 CRYPTO，不打开其它 worker；修复 OpenClaw bundle MCP 最终工具过滤，属于 per-turn tool narrowing，不新增 gate/guard；BB `readiness/data_gaps/conflicts` 仍仅作为报告数据质量材料。
待办：继续设计并实现 CRYPTO 的 `fundamental_analyst`、`news_analyst`、`social_analyst` 等后续 worker 策略，再做完整 `/report` 加密币链路。

## 10:48 [codex]
-完成：按 CN_A prompt 骨架重写 11 个非 market worker 的 CRYPTO prompt，补齐前线报告、牛熊辩论、研究经理、交易员、风险三方和 PM 的加密资产语义；同步 AGENTS/stage skill 说明，保留 STAGES runtime 未打开。
-决策：本轮只批准 prompt strategy，不打开非 market CRYPTO stage；不新增 gate/guard，后续需逐 worker 明确工具/材料策略后再做 live provider payload 验证。
待办：为 CRYPTO fundamental/news/social 设计真实数据工具或 BB 域挂载策略，再决定是否打开对应 STAGES。

## 13:02 [codex]
-完成：新增 `docs/crypto_data_source_plan.md`，沉淀 CRYPTO 稳定数据源总体方案，覆盖 BB/CoinGlass、DefiLlama、CoinGecko、新闻搜索、LunarCrush、Polymarket、OpenViking 分工、四个资料包和 stage 打开策略；在 `docs/架构设计.md` 增加入口链接。
-决策：文档明确 CoinGlass/BB 是市场结构主源，CoinGecko/DefiLlama 只做补充；OpenViking 不当数据源；不新增 gate/guard，不引入静默 fallback。
待办：后续按文档实现 `crypto_fundamental_data_pack`、`crypto_news_data_pack`、`crypto_social_sentiment_pack` 并做 provider payload 证明。

## 16:38 [codex]
-完成：根据架构评审修正 CRYPTO 数据源方案：把当前能力收敛为 `market_analyst` 市场结构报告，明确完整 12-worker CRYPTO 报告必须等 fundamental/news/social 资料包和四份前线 approved L1 齐备后再打开下游；同时注明 BB 工具由外部 `bb_crypto_data` MCP 注册，不属于本地 frontline 插件合同。
-决策：不把 BB 工具伪加入 `claw-trade-frontline-tools` 插件合同；只在插件描述和设计文档里说明注册边界，避免静态合同假成功。未新增 runtime gate/guard，未打开新的 CRYPTO stage。
待办：实现 `crypto_fundamental_data_pack`、`crypto_news_data_pack`、`crypto_social_sentiment_pack` 后，再做 provider payload 和 L1 证据验证。

## 20:40 [codex]
-完成：按 CRYPTO 数据源方案实现 `crypto_fundamental_data_pack` 第一版，接入 CoinGecko 基础资料和 DefiLlama DeFi 经营指标，注册本地 OpenClaw 插件工具、tool intent、fundamental skill manifest，并补齐单测/合同测试。
-决策：CoinGecko 缺 `COINGECKO_DEMO_API_KEY` 或 `COINGECKO_PRO_API_KEY` 时显式记录 credential missing，不走 public no-key 静默 fallback；DefiLlama 免费 API 可无 key，Pro key 使用官方路径式鉴权。`fundamental_analyst` / CRYPTO stage 继续关闭，等待 fresh provider payload、真实 tool call 和 approved L1 报告证据。
待办：下一步对 `crypto_fundamental_data_pack` 做真实 OpenClaw turn 验证；再实现 `crypto_news_data_pack` 和 `crypto_social_sentiment_pack`。
