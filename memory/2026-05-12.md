## 04:58 [codex]
-完成：完成 FIX-MARKET-LOOKBACK-20260512：market 输入正规化新增技术历史最小 120 自然日下限，显式短 `start_date` 自动扩窗，显式更早 `start_date` 保留，`end_date` 不变；相关 focused tests 全绿。
-决策：采用最小实现放在 `frontline_data_pack/profile.py`，不改 workflow/guard/prompt，不引入 fallback；通过测试锁定“默认窗口、显式短窗扩展、显式早窗不缩短”三类行为。

## 04:59 [codex]
-完成：完成 FIX-CUSTOMER-FINAL-REPORT-EXPORT-20260512：`final-report.md` 改为客户可读结构，先输出 portfolio_manager 最终裁决原文，再按章节嵌入其余 worker 的 approved L1 原文，移除 Markdown 中机器审计字段。
-决策：追溯能力保留在 `export-claims.json`/`export-guard-results.json`，不搬进 Markdown；不新增 gate/guard，不改 PM owner 权限，不做 Python 摘要改写。
待办：后续如要进一步优化章节命名或排版，只能在“原文直出、不引入机器字段”边界内做样式级调整。

## 05:00 [codex]
-完成：完成 FIX-CNA-PROMPT-PROCESS-PROSE-20260512：收敛四个 frontline CN_A prompt，加入“正文直入、禁过程话、数据缺口仅在限制段说明”约束，并补充 prompt policy 合同测试覆盖禁过程话约束与机器协议关键词禁入。
-决策：只改 prompt 与合同测试，不改 runtime/guard/Python 后处理；保留 TradingAgents-CN 报告口吻与工具调用边界，避免工程化流程话进入 downstream 内联材料。
## 05:16 [codex]
-完成：将固定 runtime 启动脚本扩展为测试命令入口：`scripts/start-control-runtime.sh -- <command>` 会启动 OpenViking/OpenClaw、导出 runtime env、运行命令，并在退出/失败/中断时关闭本次拉起的服务。
-决策：不新建第二套启动法，沿用 memory 固定的 `scripts/start-control-runtime.sh`、1933/18789、no-sidecar、`.runtime/dev-services` 口径；将规则同步到 AGENTS 与 live runtime runbook，防止测试前手动分散启动。
待办：后续需要干净 runtime 的 focused/live 测试统一从该命令模式进入。

## 06:18 [codex]
-完成：执行任务 C4-FRESH-LIVE-AFTER-LOOKBACK-PROMPT-EXPORT-FIX-20260512，使用固定脚本命令模式完成 CN_A 600519 full12 真实 provider run（run_id=`run-20260512-100304-bba373d0`），并将 12 worker 的 final_prompt/llm_back/report 与 provider/tool/raw 证据导出到 `docs/evidence/full12_prompt_feedback/claw_trade_cn_a_600519_live_run-20260512-100304-bba373d0/`，含 `capture_summary.json/md` 与导出产物拷贝。
-决策：严格按真实链路执行（无 mock/stub/fake/fallback），`report.md` 口径沿用当前项目证据规范由同 worker `raw-output.md` 直拷；命令结束后复核 1933/18789 均已关闭。
待办：frontline 开场过程话仍残留在 `social_analyst` 首行（“数据已获取，现在基于真实材料撰写报告。”），后续如要清零需新任务处理。

## 06:24 [codex]
-完成：完成任务 B3-SOCIAL-PROCESS-PROSE-ROOT-FIX-20260512，收紧 `social_analyst` 的 CN_A prompt，明确“工具调用/重试必须静默执行、最终首行必须 `# ` 标题”，并新增 social 专属 prompt policy 合同断言防回退。
-决策：仅改 prompt 与合同测试，不改 Python 报告后处理、不改 guard/gate、不引入任何机器协议字段，保持 TradingAgents-CN 报告边界。
待办：建议由总管在下一轮 fresh live 中复核 `social_analyst` 的 provider history 与首行是否已清除过程话污染。

## 06:43 [codex]
-完成：执行任务 C5-FRESH-LIVE-AFTER-SOCIAL-PROCESS-FIX-20260512，按固定脚本命令模式完成 CN_A 600519 full12 真实 provider run（run_id=`run-20260512-102914-dcb823f1`，status=completed），并导出 12 worker 的 final_prompt/llm_back/report 与 provider/tool/raw 证据到 `docs/evidence/full12_prompt_feedback/claw_trade_cn_a_600519_live_run-20260512-102914-dcb823f1/`，含 capture_summary 与导出产物拷贝。
-决策：严格执行无 mock/stub/fake/fallback；逐项复核 social 过程话、market 扩窗、final-report 机器字段禁入与命令结束后端口关闭状态。
待办：无。

## 07:18 [codex]
-完成：执行任务 C-CLAW-CAPTURE-20260512，复用主会话已通过 preflight 的 runtime 完成 CN_A 600519 `/report` 全流程 live run（run_id=`run-20260512-110633-4e1cc0ab`），并在 `docs/evidence/claw/` 生成 12 个 worker 的 final_prompt/llm_back/report 与 `final_report.md`、`capture_summary.md`（目录仅保留 Markdown）。
-决策：首次命令因未导出 `runtime.env` 变量被 `run_control` 门禁拦截，随后在同一启动体系下改为 `set -a; source ...; set +a` 成功；`report.md` 优先取 approved L1 URI 实际下载正文，无法下载时才回退 raw_output，并在 `capture_summary.md` 逐 worker 标注口径与差异。

## 07:31 [codex]
-完成：完成用户要求的 evidence 重建、CN/本项目双侧真实抓取与 Markdown-only 对比归档；`docs/evidence/cn/` 与 `docs/evidence/claw/` 各 38 个 Markdown，`docs/evidence/compare/` 14 个 Markdown，覆盖 12 worker 三层证据和最终报告对比。
-决策：严格不把 JSON/raw/log 放入 `docs/evidence`；CN 原始 JSON 保留在 `.runtime/tmp_cn_capture_20260512/`，claw 原始 runtime 证据保留在 `runs/run-20260512-110633-4e1cc0ab/`；对 CN `news_analyst` 只有工具调用前置语的问题在 summary 与 compare 中标明为内容缺口。
待办：如要进一步缩小差异，应优先处理 CN news 原生节点未形成完整报告、以及 claw 与 CN 上游材料/工具链口径差异。

## 07:52 [codex]
-完成：修复本项目最终报告图表位置，导出器现在优先把图表资产插入“技术指标分析”小节内，并同步修正 `docs/evidence/claw/final_report.md` 与 assets 目录。
-决策：这是导出层排版问题，不改 worker 报告正文、不让 Python 补写技术指标结论；新增契约测试锁定图片不再落到文末。
待办：若要让技术指标正文更长更充分，需要调整 market analyst prompt 或市场数据包能力后重新跑 live report，不能靠导出器事后扩写。

## 08:03 [codex]
-完成：按用户要求清理工作区临时产物，删除 `.runtime/`、`runs/`、`logs/`、pytest/Python 缓存，保留并准备提交 `docs/evidence/` 下的新参考证据。
-决策：OpenClaw 子模块含运行边界源码改动，已先在子模块提交 `9b3f0cf19c`，父仓库将记录该子模块指针；保留 `node_modules/` 依赖缓存不作为临时报告证据处理。
待办：无。

## 08:33 [codex]
-完成：新增并执行 `scripts/capture_tradingagents_us.py`，调用 `/home/frank/src/TradingAgents` 原版节点，以 DeepSeek `deepseek-chat` 跑美股 `AAPL`，在 `docs/evidence/trading/` 落盘 12 worker 的 final_prompt、LLM back、report、raw 调用与 final_report。
-决策：用户未指定美股 ticker，本轮使用原版/美股常用样例 `AAPL`；数据 vendor 固定为原版 TradingAgents 的 yfinance，不需要额外行情 API，未使用 mock/stub/fake/fallback/capture-only。
待办：如需非 AAPL 标的，用同脚本指定 `--ticker` 重跑。

## 09:05 [codex]
-完成：根据 `docs/evidence/trading/` 原版美股证据与 CN prompt 迁移教训，写入 12 个美股 US worker prompt，移除运行时协议/控制面措辞，并让 PM prompt 同时接收研究经理投资计划与交易员交易提案。
-决策：只保留原版 TradingAgents 角色、报告结构、辩论/决策语气与必要的本项目工具名替换；不把 OpenClaw/OpenViking、材料协议、Python 控制权说明写进模型可见 prompt。
待办：若要证明真实 provider prompt 对齐，需要按 live runtime preflight 跑一次 fresh US `/report` 并检查 provider payload。

## 09:31 [codex]
-完成：只读核对原版 TradingAgents workflow 与本项目 workflow，确认默认 12-worker 主链后半段单轮顺序基本对齐，但存在 frontline 顺序/批处理、US downstream prompt 材料注入、US report_polisher 终稿阶段等差异。
-决策：本轮仅报告差异不改代码；判定 US full workflow 当前最需要先处理 `report_polisher` profile 未批准与 US 下游材料注入只对 CN_A 生效两个阻断。
待办：修复后应跑一次 fresh US `/report` provider payload 验证。

## 14:43 [codex]
-完成：按人类批准启用 `report_polisher` 的 US profile，并把下游 prompt 材料注入从仅 `CN_A` 扩展到 `CN_A`/`US`，确保 US bull/bear/manager/trader/risk/PM/report_polisher 能收到上游 approved L1 报告正文。
-决策：US report_polisher 只做读者版整理，不改变 PM 结论；Python 仍只搬运已批准原文，不总结、不改写、不生成投资观点。相关 prompt/workspace/stage policy/material tests 均已通过。
待办：后续需要按 live runtime preflight 跑 fresh US `/report`，检查真实 provider final prompt 与终稿输出。

## 09:06 [codex]
-完成：新增 `report_polisher` 作为 PM 之后的 A 股读者版终稿 worker，接入 `final_report` 阶段、CN_A prompt、alphaear-reporter 风格 skill、完整上游 L1 正文注入与最终报告导出优先使用逻辑。
-决策：PM 继续拥有投资决策；reporter 只改善结构、排版、措辞和专业度，不改 PM 结论；US/HK/CRYPTO 对 reporter 显式未批准，不做市场策略 fallback。
待办：后续如需验收真实 provider payload，需要按 runtime preflight 跑 CN_A fresh `/report`，确认 `report_polisher` final prompt 与最终导出报告。

## 10:00 [codex]
-完成：将 `frontline` 阶段改为四个分析员并发执行，保持后续阶段串行，结果仍按固定 worker 顺序汇总，并为 approved manifest 写入加锁。
-决策：并发只作用于同一层独立 frontline worker，不改变 claw-trade 的 workflow 决策权，也不改变 OpenClaw 单 worker 执行边界；focused 验证命令 `uv run --project /home/frank/src/claw-trade pytest tests/unit/test_control_runner.py tests/unit/test_workflow_controller.py -q` 通过，27 passed。
待办：如需证明真实 provider 侧运行耗时和证据落盘行为，需要按 runtime preflight 再跑一次 fresh `/report`。

## 10:07 [codex]
-完成：按用户要求暂不继续测试；补上 OpenClaw `openviking_write_material` 的跨进程 OpenViking 写锁，并让 `scripts/start-control-runtime.sh` 为 gateway/runtime.env 传递同一个 `OPENVIKING_WRITE_LOCK_PATH`。
-决策：frontline 并发会让四个 OpenClaw worker 同时可能写 OpenViking；Python 的 manifest 锁只能保护本地批准清单，不能保护 OpenViking server 的 temp_upload/import/readback 链路，所以在 Node 工具层串行化远端写入，保留 worker 推理并发。
待办：本轮只运行 `pnpm build` 让 OpenClaw dist 更新，未跑 pytest/vitest/live；后续需按 runtime preflight 做 fresh `/report` 验证写锁与并发表现。

## 10:51 [codex]
-完成：按固定 runtime preflight 重跑 CN_A 600519 fresh `/report`，最终 `run-20260512-143745-6aba9ea5` completed，13 个 worker/report_polisher 均 succeeded，最终报告导出 passed 且图表位于技术指标章节。
-决策：发现 OpenClaw 正式材料写锁无 timeout，但 frontline 数据工具仍有 27 次 OpenViking `resource is busy` 重试日志，已补 Python evidence client 同一写锁；仍需后续继续处理工具超时与图表/文字不一致问题。
待办：修 market/news/social/fundamental 工具 `python subprocess timed out`，让 worker 真正拿到图表与指标数据后再生成技术分析正文。

## 12:16 [codex]
-完成：将 `scripts/start-control-runtime.sh` 扩展为同一脚本启动 MongoDB、OpenViking、OpenClaw；前线资料包改为完整资料包一次性提交 OpenViking，并按 tool_call_id 隔离 L2 路径，修复新闻/社交工具父进程预算低于 Python 数据源预算的问题；fresh CN_A `/report` 复验 `run-20260512-160632-d4a6db1c` completed，未再出现 `TOOL_SUBPROCESS_TIMEOUT` 或 `resource is busy`；补充 `social_analyst` 单 worker 复验 `run-20260512-161854-39f82e35`，工具参数不再携带非法 `current_date`。
-决策：Mongo 缺失按启动层解决，不再要求人工另起；OpenViking 写入风险按“先本地完成、再批量提交、失败即工具失败”处理，不靠重试掩盖半写；同时去掉 CN_A 前线 prompt 工具参数里的非法 `current_date`，避免模型触发 schema 拒绝后自我纠错。
待办：OpenViking embedding 仍有 MiniMax 余额不足 warning，但不影响本轮 L1/L2 写入与报告导出；后续可单独决定是否关闭/替换 embedding。

## 13:51 [codex]
-完成：统一报告 runtime 的模型配置：OpenClaw 运行配置只保留 `deepseek/deepseek-chat`，OpenViking 运行配置不再继承个人 ov.conf 的 MiniMax embedding，并通过 `claw_trade.runtime.openviking_report_server` 禁用 OpenViking 语义/向量队列、默认目录向量化和 embedding 实例。
-决策：OpenViking 在本项目报告链路只做证据存储，不允许直接调用任何 LLM/embedding；误触发向量化时直接失败，不再进入后台 embedding 重试队列。
待办：后续 fresh `/report` 可继续观察 OpenViking 日志，但当前 focused runtime 验证和 L2 集成测试均未再出现 MiniMax、embedding failure 或 re-enqueue。

## 14:05 [codex]
-完成：按固定 preflight 重新完整执行 CN_A 600519.SH `/report`，run_id=`run-20260512-175657-6dfda550`，13 个 worker（含 report_polisher）全部 succeeded，最终报告导出 `runs/run-20260512-175657-6dfda550/reports/final-report.md`，导出 guard `ok=true`，图表位于技术指标章节。
-决策：本轮使用 `scripts/start-control-runtime.sh -- <run_control>` 一次性启动 MongoDB、OpenViking、OpenClaw；运行后复核 1933/18789 已关闭，日志与 run 目录未出现 MiniMax、embedding failure/requeue、resource busy、工具超时或 Mongo 不可用。
待办：无。

## 14:21 [codex]
-完成：清理工作区运行缓存和 Python 编译缓存，保留 `docs/evidence` 下 Markdown 证据与 fresh `/report` run 产物；准备提交主仓库与 OpenClaw 子模块，并将 release tag 同步到 GitHub。
-决策：用户给出的 `v 0.2` 含空格，不是合法 Git tag ref，发布 tag 使用标准名 `v0.2`；`docs/evidence/trading` raw JSON 与“只保留 md 证据”要求冲突，按临时抓取中间产物删除。
待办：完成 commit、push 与 tag push 后复核远端状态。

## 14:58 [codex]
-完成：写入 `docs/report_workflow_env_and_rounds_design.md`，明确 `.env.local` 作为 CLI/dev 默认配置入口、多轮 debate/risk debate 的 workflow 控制改造、turn 身份、manifest 多轮材料、prompt 历史和验证标准。
-决策：推荐 `.env.local -> typed settings -> RunRequest -> controller`，拒绝 controller 直接读 env；ticker/date 等每次报告输入不作为全局 env 设置，未来 UI 复用 settings schema 而不是直接编辑生产 env 文件。
待办：如进入实现，先做配置入口和 turn 身份，再改 manifest/controller/prompt 材料。

## 15:07 [codex]
-完成：新增标的市场识别层，CLI 可从 ticker/market hint 推断 `CN_A`、`US`、`HK`、`CRYPTO`，并把解析出的 profile 写入原 `RunRequest`，后续 worker 唤醒仍按原 workflow 使用对应 profile prompt。
-决策：只在控制入口识别市场，不改 worker 顺序、不改 prompt 内容；显式传入的 profile 若与识别结果冲突则阻断，避免用错市场 prompt。
待办：按用户要求，本轮未运行测试。

## 15:18 [codex]
-完成：按用户要求启动 gpt-5.3-codex/high 子 agent 评审 `docs/report_workflow_env_and_rounds_design.md`，并根据评审补强 RunRequest 回读一致性、exporter 多轮保真、manifest/capability 稳定排序、旧 run 兼容和 contract 测试覆盖。
-决策：接受子 agent 提出的两个 Critical：新增 workflow 参数必须随 `request.json` 回读保持不丢值，最终导出不能继续按 worker_id 单值去重；文档已把两者列为硬任务和验收条件。
待办：进入实现前按文档 Phase 1 从配置入口和持久化回读开始。

## 15:16 [codex]
-完成：按用户要求只测试市场自动识别，不启动 runtime、不进入完整 workflow；resolver 覆盖 A股/美股/港股/加密币样例，CLI 在省略 market/profile 时能自动填入 `RunRequest.profile`，profile 冲突会阻断。
-决策：测试范围限定为 `uv run python -c` 直接调用识别器与 CLI parse_args，不运行 pytest 或报告流程。
待办：无。

## 16:03 [codex]
-完成：实施报告 workflow 环境参数与多轮控制设计：`.env` 参数进入 `RunRequest`，debate/risk debate 支持按配置多轮串行，turn 身份贯穿 batch/call/result/material/ref，manifest、prompt 注入和最终导出都保留多轮材料而不按 worker 覆盖。
-决策：frontline 仍只允许 parallel；controller 只读 `RunRequest` 不直接读 env；PM/report_polisher 输入继续使用完整 approved L1 正文，多轮历史按 turn 顺序传入，不做 Python 摘要。
待办：如需验收真实 provider 行为，再按 runtime preflight 跑 fresh `/report`，检查 provider payload 和最终报告多轮呈现。
