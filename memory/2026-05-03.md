## 00:03 [Codex]
-完成：根据 `docs/control迁移方案.md` 生成 `docs/control迁移任务清单.md`，覆盖文件责任图、设计覆盖矩阵、T0-T10 原子任务、伪码、验证命令、预期功能检查表和最终偏离审计。
-决策：任务清单按“先合同测试、再最小实现、再阶段验证”的顺序组织；补入前线 all-success barrier、全部阶段验证停止点、market 首轮直接文本也算首轮、对比结果固定写入 `docs/market_first_turn_validation.md`。
待办：等待用户审阅任务清单后，再决定是否进入代码实现。

## 01:30 [subagent T5.1-T5.4]
-完成：实现 T5.1-T5.4 market 首轮验证的控制层骨架
-决策：先实现控制层合同验证，不依赖真实 OpenClaw 调用
待办：完善 provider payload 捕获，实现真实 OpenClaw 唤醒

## 08:33 [Codex]
-完成：对 control 迁移实施反馈进行代码检查，确认主工作区未合入 48-test 子工作区实现，且主工作区运行循环存在成功唤醒后误置失败的问题。
-决策：将“48 tests pass”归因到 `.claude/worktrees/control-migration` 未跟踪子工作区；主工作区当前只能复现 10 个本地测试通过，不能证明 T0-T5 完成。
待办：先整理合入边界和修复运行循环，再补真实 OpenClaw/provider payload 验证。

## 08:39 [Codex]
-完成：新增 OpenClaw client 合同测试并修复 `wake()` 证据链，缺 provider payload 或缺 visible tools 时显式失败，禁止从输入 allowed_tools 回填。
-决策：优先信任 OpenClaw stdout JSON 中的 `provider_request_path` / `first_response_path` / `visible_tools`，仅在缺路径时回退到 `state_dir` 查找；两处证据都缺则硬失败。
待办：继续补齐真实 provider payload 捕获链路在更多 worker/stage 的端到端验证。

## 08:39 [Codex]
-完成：新增 market_first_turn 聚焦回归测试并最小修复 run_loop 的失败分支判定，确认成功唤醒后不再被误置为 FAILED 且 provider/first response 路径保留。
-决策：仅将 `run_loop_until_stop` 的“失败分支”从 `Action` 收窄到 `FailRun`，避免改动 controller 与 OpenClaw 边界。
待办：`uv run pytest tests -q` 仍有 2 个 `openclaw_client` 合同测试失败（范围外文件），需后续按任务边界单独处理。

## 08:41 [Codex]
-完成：补齐 run_loop 对 OpenClaw `visible_tools` 的结果传递，并用同一 market 首轮回归测试证明工具证据不会被丢弃。
-决策：只在 OpenClaw wake result 返回可见工具时更新 `RunResult.visible_tools`，不在执行层猜测或回填工具列表。
待办：继续进行独立代码评审与真实 OpenClaw/provider payload 端到端验证。

## 08:47 [Codex]
-完成：处理独立评审反馈，移除 provider payload 历史路径回退和本地 run id 拼接，增加 visible tools 严格匹配与首轮证据硬失败测试。
-决策：OpenClaw CLI 当前没有阶段工具限制参数，因此本仓不宣称已控制工具暴露；只接受本次 stdout 证据并用实际 visible tools 与 allowed tools 匹配来 fail-closed。
待办：后续需要确认 OpenClaw 正式工具限制接口或获批扩展 seam 后，才能说阶段工具收敛完成。

## 08:51 [Codex]
-完成：完成最终复审与本仓回归验证，确认 17 个本仓测试通过，评审中前述证据伪成功问题已关闭。
-决策：保留 `allowed_tools` 尚未由正式 stage policy 下发这一中等级残余风险，不用 hardcoded allowlist 假装阶段工具收敛已完成。
待办：实现正式 stage_policy/工具限制 seam 后，再运行真实 US/CN_A market 首轮 live 验证。

## 09:02 [Codex coordinator]
-完成：按总管职责派出两个只读 sub-agent 审计 control 迁移覆盖和 OpenClaw seam，确认当前任务清单为部分覆盖且命中停止条件。
-决策：当前 OpenClaw CLI JSON 未提供 `provider_request_path`、`visible_tools`、`first_response_path` 等任务要求证据字段，真实 market 首轮还需要 provider 凭据；不继续派实现 agent 伪造证据或在未获批前修改 `third_party/openclaw`。
待办：等待人类决定是否批准 OpenClaw 通用接缝、调整证据口径，或提供真实 provider 配置后再继续。

## 09:28 [Codex coordinator]
-完成：根据人类授权更新 AGENTS、control 迁移方案和任务清单，删除“禁止修改 OpenClaw”的旧口径，并把首轮工作改为“完整 control 流程中的阶段验证”。
-决策：OpenClaw 修改范围限定为通用单回合运行接缝：工具收窄、真实 provider request 保存、首轮回应保存、机器可读证据返回；claw-trade 仍保留流程、材料、硬检查和报告导出权责。
待办：按更新后的 T3.3 先补 OpenClaw 通用运行接缝，再实现 claw-trade client 适配和后续 control 任务。

## 09:40 [Codex coordinator]
-完成：按“删除旧 src、重新来过”的新方向重写 `docs/control迁移方案.md`，将其改为总体架构方案，重点说明完整流程、claw-trade 与 OpenClaw 的接口、证据回传、模块划分和重建顺序。
-决策：总体方案不再沿用旧代码路径，也不再写成任务清单；`claw-trade` 保留流程、材料、硬检查、报告权责，OpenClaw 只补通用单回合真实证据能力。
待办：下一步应重新生成任务清单，删除或重写旧验证文档与旧脚本引用，避免方案和任务清单再次分叉。

## 09:50 [Codex coordinator]
-完成：新增 `docs/control详细设计方案.md`，按总体方案展开到伪码级，覆盖目录结构、状态、工作者调用、流程决策、配置、OpenClaw 接口、OpenClaw 通用接缝、证据检查、材料批准、运行循环、报告导出和测试设计。
-决策：详细设计保留少量可编程英文标识，解释和边界全部用中文；不沿用旧 `experiments` 路径，不写业务 prompt，不让 Python 替 worker 调工具。
待办：按详细设计重新生成任务清单，并删除或归档旧 `control迁移任务清单.md`、旧验证报告中与新方案冲突的内容。

## 09:55 [Codex]
-完成：阅读本仓 `AGENTS.md` 与 `memory`，并只读核对旧 `claw-invest` 中 OpenViking 的正式产物、MCP 工具面和材料治理职责。
-决策：初步建议新项目不要默认引入 OpenViking 作为运行依赖；保留 artifact authority / accepted pointer / traceability 思想，在 `claw-trade` 内重写轻量材料层，除非后续证明必须复用 OpenViking 的远端存储或检索能力。
待办：若决定重开 OpenViking 议题，应先写一页 design delta，明确它只承担材料存储/读取，不接管流程、硬检查、提示词或 PM 结论。

## 10:00 [Codex]
-完成：按功能、收益、代价重新拆解 OpenViking，而不是只从新仓约束判断是否引入。
-决策：把 OpenViking 定位为“正式材料交换与治理层”：它能带来远端 artifact 存储、worker 工具写入、权威指针、按角色读取和审计回执；主要代价是服务依赖、MCP/工具合同复杂度、L0/L1/L2 语义耦合和旧仓 compact-first 失真风险。
待办：后续若继续讨论，应围绕“本阶段是否需要远端共享材料仓库/agent 工具式产物 IO/长期记忆检索”三项需求做取舍。

## 10:06 [Codex]
-完成：基于上游 OpenViking README 与旧仓实践，形成是否引入 OpenViking 的认证式架构判断。
-决策：建议引入，但仅认证为材料/上下文底座而非流程大脑；若引入，必须前置到材料接口设计，使用 OpenViking-shaped contract，避免后续重写。
待办：如人类认可该方向，下一步应修订 `docs/control迁移方案.md` 与 `docs/control详细设计方案.md`，把正式材料接口改为 OpenViking-shaped。

## 10:10 [Codex]
-完成：检索并阅读旧 `claw-invest/memory` 中 OpenViking 相关收益、问题和后续修正共识。
-决策：旧仓证据显示 OpenViking 的真实收益是正式材料权威、回执闭账、按角色读取治理和 prompt 控重潜力；真实问题主要来自运维侧车脆弱、compact-first 接口、L2 未被消费、URI/read scope 治理复杂和过厚协议规则。
待办：把新仓 OpenViking 设计明确成“完整 L1 默认消费 + L2 按需回源 + claw-trade 批准 manifest”，避免复刻旧仓 compact-first 链路。

## 10:19 [Codex]
-完成：在 `docs/control迁移方案.md` 总体方案中加入 OpenViking，形成 `claw-trade` 管流程批准、OpenClaw 管单回合执行、OpenViking 管正式材料与上下文的三方架构。
-决策：将材料接口前置为 OpenViking-shaped contract，固定 `viking://workflow/<run_id>/<stage>/<worker>/<artifact>.md`、approved manifest、L1 默认消费、L2 按需回源，并把 compact-first、目录扫描和假回执列为禁止项。
待办：下一步需要同步修订 `docs/control详细设计方案.md` 和任务清单，否则总体方案与实施级设计会分叉。

## 10:30 [Codex]
-完成：根据评审意见补强 `docs/control迁移方案.md`，明确 L1/L2 规范语义、OpenViking 回执指纹校验、确定性 URI/approved manifest 定位、L1/L2 证据目录和 provider request/visible tools 缺失停止条件。
-决策：正式材料 URI 加入调用编号以避免重跑覆盖；回执只作为写入证据，必须经 URI、大小、内容指纹和调用身份校验后才能进入 approved manifest；禁止 compact write 冒充工作者正式材料。
待办：同步修订 `docs/control详细设计方案.md` 和 `docs/control迁移任务清单.md`，让实施级设计与总体方案保持一致。

## 10:58 [Codex]
-完成：根据总体方案整体重写 `docs/control详细设计方案.md`，将详细设计改为 `claw-trade` 管流程批准、OpenClaw 管单回合真实执行、OpenViking 管正式材料与上下文的三方架构版本。
-决策：详细设计前置 OpenViking 材料合同、L1/L2 对象、回执校验、approved manifest、OpenClaw provider evidence、OpenViking 工具收窄、运行循环和对应测试；保留本地文件仅作审计副本，不作为正式材料权威。
待办：下一步需要按新的详细设计重写 `docs/control迁移任务清单.md`，否则任务清单仍可能沿用旧本地 artifact 口径。

## 11:20 [Codex]
-完成：根据详细设计评审反馈补强 `docs/control详细设计方案.md`，覆盖运行时 OpenViking read 审计、tool calls 显式状态、provider request 身份校验、L1/L2 可执行校验、PM 结论所有权和实例级 hard gate。
-决策：approved manifest 不再只作为输入引用检查，而要生成 manifest-scoped capability 并通过 tool-calls 审计实际 read URI；L1 批准前必须与 raw output 指纹一致，claim 必须映射到 L2，PM 评级、最终结论和执行条件都要防改写。
待办：再跑一次独立评审后，若无 blocking issue，再重写 `docs/control迁移任务清单.md`。

## 11:28 [Codex]
-完成：继续处理详细设计复审反馈，补入 L2 entry 逐条 OpenViking stat/read/hash 校验、PM owner 批准路径接入、最终报告 unsupported claim guard、agent skill manifest 校验和长期记忆第一版 defer 说明。
-决策：L2 index 不能只被 claim 引用，必须逐条回源验证 URI prefix、大小和 SHA；portfolio_manager 进 manifest 前必须有评级、最终结论和执行条件；exporter 生成的高风险 claim 必须能映射到 approved materials 的 L1Claim/L2 evidence。
待办：可再做一次独立评审；若无 blocking issue，再按详细设计重写 `docs/control迁移任务清单.md`。

## 11:32 [Codex]
-完成：按最新复审反馈继续补强 `docs/control详细设计方案.md`，扩展最终报告 unsupported claim guard，并让运行时 OpenViking read 比对实际 result_sha256。
-决策：高风险 claim 类型新增投资结论、评级声明、交易动作和风险条件；OpenViking read 审计必须比对 L1 approved hash 或 L2 entry hash；导出层同时校验 PM risk_conditions 不被改写。
待办：若下一轮评审无 blocking issue，可开始重写 `docs/control迁移任务清单.md`。

## 12:10 [Codex]
-完成：根据已通过评审的总体方案和详细设计，新建 `docs/control迁移任务清单.md`，将迁移拆成 T0-T18 的实施任务和验收门槛。
-决策：任务顺序采用先纯合同测试、再 OpenViking 材料合同、再 OpenClaw 通用接缝、再单 worker/前线/12 worker 集成验收；长期记忆第一版显式封存，skill/workspace 真实加载、OpenViking read result_sha256 和最终报告高风险 claim guard 都列为不可删减验收项。
待办：执行任务清单前应再次确认 provider/OpenViking 配置可用性；若不可用，只能完成纯合同和 fake-runtime 集成任务，不能伪造 live 成功。

## 12:22 [Codex]
-完成：把 OpenClaw 真实加载 worker workspace/skill 的证据合同回灌到 `docs/control详细设计方案.md`，并同步任务清单中的 `workspace-evidence.json` 验收项。
-决策：详细设计新增 `workspace_evidence_path` 强制读取、`validate_workspace_evidence()` guard、真实单 worker wake 集成测试和反例测试；文件存在只算配置完整，不能替代 OpenClaw runtime 证据。
待办：后续实现 OpenClaw 通用接缝时必须由 OpenClaw runtime 生成 workspace evidence，不能由 `claw-trade` 重构。

## 12:31 [Codex]
-完成：根据总体方案和详细设计整体重写 `docs/control迁移任务清单.md`，改为 T00-T21 原子任务、伪码级测试、执行命令、停止条件和覆盖矩阵。
-决策：任务清单按先合同测试、再材料/证据 guard、再 OpenClaw/OpenViking 真实接缝、最后 12 worker/report gate 的顺序组织；每个详细设计章节都映射到任务，且明确无证据不得声明符合。
待办：执行 T00 前先确认当前代码基线和 provider/OpenViking 配置边界；真实集成任务不可用时只能记录缺口，不能 fake 成功。

## 12:45 [Codex]
-完成：根据任务清单评审反馈补强 `docs/control迁移任务清单.md`，拆分 OpenClaw runtime seam，新增 12 worker workspace、tool registry、OpenViking capability read/write、hard gate table tests、final report 全阶段验收和 BLOCKED 语义。
-决策：OpenClaw 改动拆为 T09A-T09G，并新增边界 sweep；真实集成不可用时只能产出 BLOCKED，不把 skip 记为完成；同时把 news 工具能力、OpenClaw 边界 sweep、最终报告全阶段要求小幅回灌到详细设计。
待办：再次评审任务清单，重点核对 T09A-T09G、T04A/T04B、T20 的原子性和覆盖矩阵。

## 12:59 [Codex]
-完成：复审 `docs/control迁移任务清单.md` 对总体方案、详细设计和 AGENTS 边界的覆盖，确认上一轮 3 个 blocking 已修复，当前仅剩非阻塞强化建议。
-决策：任务清单已具备按顺序执行的条件；后续实现仍需在真实 provider/OpenViking 不可用时产出 BLOCKED，不能用 fake runtime 冒充 live 完成。
待办：执行 T00 前确认代码基线、provider/OpenViking 配置和 sub-agent 分工边界。

## 13:04 [Codex]
-完成：再次复审 `docs/control迁移任务清单.md`，确认 CN_A OpenViking read/write scoped 测试已补齐，未发现新的 blocking 或 non-blocking 缺口。
-决策：文档层任务清单可进入执行；真实集成仍以 provider/OpenViking 证据为准，缺依赖时只能 BLOCKED。
待办：按 T00 开始实施前确认当前代码基线和执行 agent 分工。

## 12:51 [Codex]
-完成：继续处理任务清单复审的 3 个 blocking，收紧 OpenViking 写接口归属、统一 high-risk claim kind 命名，并补 T20 逐 worker OpenClaw evidence 验收伪码。
-决策：`claw-trade` approval client 只读/校验 OpenViking 材料，worker L1/L2 写入只属于 OpenClaw tool adapter 或 fake runtime；claim 类型统一为 `valuation_metric`、`news_claim`、`sentiment_claim`、`chart_claim`、`source_claim`、`tool_success_claim`。
待办：再次运行任务清单覆盖评审，确认 T06/T07/T20 不再留下歧义。

## 12:56 [Codex]
-完成：修正任务清单和详细设计对象字段的同步问题，更新 T05 claim kind、T12 receipt/l2_index 伪码、T19/T20 material 字段和 RunStatus。
-决策：批准流程伪码改用 `receipt.uri` 与 `call.material_target.l2_prefix`；完整运行成功态使用 `RunStatus.COMPLETED`；L2 空原因来自 `material.l2_index.empty_reason`。
待办：继续做任务清单复审，确认已无字段级漂移。

## 13:16 [Codex]
-完成：拉取 OpenViking 到 `third_party/openviking`，并在用户纠偏后按 `claw-invest` 既有安装方式启动 OpenViking server；健康检查 `http://127.0.0.1:1933/health` 返回 ok。
-决策：停止使用临时 `/tmp` 配置和源码猜测启动路线；正确口径是复用 `claw-invest` 已验证的 `uv run openviking-server --port 1933`、现有 `~/.openviking/ov.conf` 与 `.venv` 环境。
待办：如果需要 OpenViking MCP sidecar，应继续参考 `claw-invest/start-live-runtime.sh` 的 `ensure_local_openviking_mcp_sidecar` 路径，而不是单独猜命令。

## 13:17 [Codex]
-完成：复审 `docs/control迁移任务清单.md` 对 `docs/control详细设计方案.md`、`docs/control迁移方案.md` 和 `AGENTS.md` 的覆盖，结论为部分符合但未发现必须阻止执行的架构性 blocking。
-决策：主要缺口归类为非阻塞强化：补 CRYPTO 显式失败测试、缺失图表/指标真实原因测试、prompt 对齐验收、若干路径映射说明和关键 guard 的 table-driven 反例。
待办：执行 T00 前建议先把这些非阻塞测试补进任务清单，避免实施阶段口径漂移。

## 13:23 [Codex]
-完成：按用户确认补强 `docs/control迁移任务清单.md`，新增路径映射表、CRYPTO fail-closed、prompt 内容/人工 review、缺失图表指标根因证据、receipt identity mismatch 和 PM 改写 table-driven 测试项。
-决策：只改任务清单，不改详细设计；prompt 对齐采用结构测试加人工 review checklist，避免脆弱全文匹配。
待办：后续执行 T00 前可再做一次任务清单只读核对。

## 13:26 [Codex]
-完成：只读复审 `docs/control迁移任务清单.md` 最新版本，确认前轮多数非阻塞缺口已补为具体测试或验收项，但 T21 完成判定仍缺“缺失图表/指标/新闻/来源真实失败原因”核对项。
-决策：当前文档层结论为部分符合，可执行但建议先补 T21 最终完成判定表；实现阶段仍必须区分 fake-runtime 合同验证与 live provider/OpenViking 验收。
待办：补齐 T21 完成判定表后再进入 T00，或接受该项作为非阻塞实施前修订。

## 13:30 [Codex]
-完成：补齐 `docs/control迁移任务清单.md` 的 T07 和 T21 闭环，新增新闻/来源缺失根因测试样例，并把“缺失图表、指标、新闻或来源真实失败原因”加入最终完成判定表。
-决策：继续只改任务清单，不改详细设计；将 collect-first 判定顺延为第 19 项，保持详细设计第 18 章所有完成问题都有最终核对入口。
待办：可进入 T00 前的最终只读核对。

## 13:46 [subagent T00]
-完成：完成 T00 最小工程骨架，先创建 `tests/unit/test_imports.py` 复现包缺失失败，再补齐 `pyproject.toml`、`src/claw_trade/__init__.py`、`tests/conftest.py` 并通过同一测试命令。
-决策：在 `pyproject.toml` 加入 `pytest` 依赖，解决 `uv run pytest` 无可执行文件导致的入口失败，保持实现范围仅限 T00。
待办：等待总管分配后续任务。

## 13:46 [task-list-auditor]
-完成：完成 `AGENTS.md`、`memory/2026-05-03.md`、`docs/control迁移方案.md`、`docs/control详细设计方案.md`、`docs/control迁移任务清单.md` 的只读审计，输出任务字段完整性和冲突结论。
-决策：将字段缺口区分为“可由全局执行顺序/停止条件/覆盖矩阵推导”和“任务原子性不足需人类修清单”，避免把 live 依赖缺失误报为已完成。
待办：如要进入实施，先由人类确认 T18-T21 的失败测试口径与“非 TDD 直接实现”执行口径。

## 13:50 [T00-reviewer]
-完成：完成 T00 只读复核并运行 `uv run pytest tests/unit/test_imports.py -q`，确认测试通过且未在 T00 目标文件中发现越界实现。
-决策：按任务要求仅追加 memory 记录，不修改代码、配置、测试和文档其他内容。
待办：建议后续任务执行前明确 `uv.lock` 是否纳入版本控制，并在提交时排除无关 `__pycache__` 生成物。

## 15:00 [task-list-reviser]
-完成：按用户批准口径修订 `docs/control迁移任务清单.md` 的执行纪律、T18/T19/T21 字段，并将 T20 拆分为 T20A/T20B/T20C，同时同步覆盖矩阵与最小执行顺序。
-决策：对 live/provider/OpenViking 验收任务采用“非 TDD 可例外但强验收”写法，明确依赖缺失只能 `BLOCKED`，禁止 skip/fake/伪成功。
待办：等待总管或人类对修订版任务清单做最终确认后再继续派发实现任务。

## 15:04 [task-list-microfix]
-完成：将 `docs/control迁移任务清单.md` 覆盖矩阵第 18 行从“18 个证据问题逐项回答”同步为“19 个证据问题逐项回答”，与 T21 最终完成判定表一致。
-决策：仅做口径一致性微修，不改任务范围、验收语义和执行顺序。

## 15:07 [subagent T01]
-完成：完成 T01 的固定阶段与 worker registry 实现，新增最小 workflow 数据模型，并通过 `tests/unit/test_workflow_workers.py` 验收。
-决策：数据模型只保留 T01 后续可用的最小字段；worker 顺序和阶段映射按 AGENTS 与任务清单固定，不引入 direct LLM 或 worker class。
待办：等待分配后续任务。

## 15:08 [T01-reviewer]
-完成：完成 T01 只读复核并运行 `uv run pytest tests/unit/test_workflow_workers.py -q`，确认 3 个用例通过，目标文件内未发现 direct LLM、12 个 Python worker class、OpenClaw/OpenViking 接入或 fake 成功路径。
-决策：按要求仅追加 memory 记录，不修改代码、测试和文档其他内容；结论为“实现范围基本符合 T01 最小边界”，同时记录模型中 `status` 仍为裸字符串，后续 T02/T03 前可再确认是否需要枚举化。
待办：等待总管分配下一任务或进入 T02。

## 15:13 [subagent T02]
-完成：实现 T02 纯流程状态机（`DecisionKind`/`Decision`/`decide_next`）并新增 `tests/unit/test_workflow_controller.py`，覆盖前线唤醒、前线未全成功 WAIT、固定阶段推进和“模型输出不决定下一步”。
-决策：按任务授权在 `models.py` 最小补充 `RunStatus`，并将 `WorkflowState.status` 与 `WorkerResult.status` 收敛为 `RunStatus`，不引入 T03 字段、不接 OpenClaw/OpenViking runtime。
待办：等待总管分配后续任务（T03+）。

## 15:15 [T02-reviewer]
-完成：完成 T02 只读复核并运行 `uv run pytest tests/unit/test_workflow_controller.py -q`，确认 10 个用例通过；实现保持在纯流程状态机边界内，未发现 OpenClaw/OpenViking/runtime/store/runner 接入，也未发现 Python worker class/direct LLM/materializer/mock/stub/fallback/fake 成功路径。
-决策：将 `approved_manifest` 当前“仅占位未使用”判定为 T02 可接受（任务清单 T02 只要求 `DecisionKind`、`decide_next`、固定阶段推进与“模型输出不决定下一步”）；但按详细设计第 4 章，后续阶段需补齐基于 approved manifest 的下游入口校验，否则到材料流转任务会形成缺口。
待办：T03+ 阶段补齐 approved manifest 驱动的下游材料入口检查与对应失败用例。

## 15:19 [subagent T03]
-完成：实现 `WorkflowStore` 的 run 目录初始化、evidence 目录位点创建和 `save_call`/`save_openclaw_result`/`save_worker_result` 最小 JSON 落盘，并新增 T03 单测通过。
-决策：本地 `openviking/*.json` 初始化写入 `audit_only` 与说明字段，明确仅为审计副本，不作为正式材料权威。
待办：等待总管派发下一任务。

## 15:21 [T03-reviewer]
-完成：完成 T03 只读复核并运行 `uv run pytest tests/unit/test_workflow_store.py -q`，确认 4 个用例通过；实现范围集中在运行状态存储与 evidence 目录位点，本地 openviking 文件标记为 audit_only。
-决策：`ensure_call_dir()` 预创建空 evidence 文件属于 T03 可接受目标位点（non-blocking），但后续 T10/T11 必须以真实 OpenClaw 证据覆盖并由 guard 拒绝空壳证据。
待办：后续任务补充“空 evidence 不可通过 evidence guard”的合同测试与真实 runtime 验收，避免位点文件被误判为有效证据。

## 15:24 [subagent T03-path-fix]
-完成：补充 `init_run("../outside")` 越界反例测试，并最小修复 `WorkflowStore.init_run()`，确保 run 目录只能创建在 store root 下；`tests/unit/test_workflow_store.py` 共 5 个用例通过。
-决策：仅复用既有 `_safe_join()` 做 run_id 根路径约束，不引入额外 ID 白名单策略；保留 T03 现有 evidence tree、call_id 防护和 openviking audit_only 语义不变。
待办：空 evidence 位点风险保持不处理，按任务清单留给 T10/T11 guard。

## 15:26 [T03-final-reviewer]
-完成：完成 T03 path-fix 最终只读复核并运行 `uv run pytest tests/unit/test_workflow_store.py -q`，确认 5 个用例通过，`init_run()` 越界反例有效且 openviking 本地文件仍为 audit_only。
-决策：判定 T03 可收口（部分符合），但保留空 evidence 位点风险为 T10/T11 guard 缺口；同时记录 `call_id` 当前边界仍以 run root 为界（未越出 run root），未在本任务扩大为更严格策略。
待办：在 T10/T11 增补“空 evidence 不可通过”与 `call_id` 越界反例（如 `../`）的 guard/合同测试，避免位点文件或相对路径被误判为有效证据。

## 15:28 [subagent T03-call-path-fix]
-完成：新增 `ensure_call_dir(\"../evil\")` 越界反例并最小修复 `call_dir()` 路径基准，确保 call 目录只能落在 `run_dir/calls/<call_id>` 子树；`uv run pytest tests/unit/test_workflow_store.py -q` 6 个用例通过。
-决策：将 `call_dir()` 的 `_safe_join` 基准从 `run_dir` 收紧到 `run_dir/calls`，避免 `../` 仍落在 run 根目录内的旁路创建；保留 T03 既有落盘与 audit_only 行为不变。
待办：空 evidence 位点风险不在本任务处理范围，留给 T10/T11 guard。

## 15:34 [subagent T04]
-完成：完成 T04 最小实现与合同测试：新增 profile fail-closed、workspace 校验、stage/tool policy 解析、OpenViking 配置读取，并通过 4 个 contract 测试文件共 15 个用例。
-决策：按“非 TDD 可例外但强验收”直接最小实现；news analyst 强制要求 `company_news`+`macro_news` 双能力；长期记忆只保留只读占位常量，不进入任何执行路径。
待办：等待总管分配 T04A/T04B 或后续任务。

## 15:38 [T04-reviewer]
-完成：完成 T04 只读复核与合同测试复跑，确认当前实现聚焦 profile/workspace/stage policy/tool registry/OpenViking config，未进入 T04A/T04B 交付面。
-决策：将手写 `STAGES.yaml` / `skills/manifest.yaml` 解析判定为 T04 可接受的最小合同实现（non-blocking），但要求在 T04B/T09B/T09D 前后收敛为可证明与 OpenClaw visible tools 同源的结构化约束与证据链。
待办：补 T04A 真实 12 worker workspace 验收、T04B tool registry 与 OpenClaw tool schema 合同测试，并加“缺 tools 策略即失败”的反例覆盖。

## 15:42 [subagent T04-contract-fix]
-完成：补强 T04 合同：新增“先过 profile 校验再调用 OpenClaw spy”的测试 helper，并将 stage policy 改为缺 `tools`/空 `tools`/缺 `openviking_access` 直接失败；指定 4 份合同测试共 19 条通过。
-决策：`openviking_access` 从“缺省默认为 none”收紧为 fail-closed，避免静默放行；仅调整 T04 合同与配置解析，不改 agents，不触及 T04A/T04B。
待办：tool registry 与 OpenClaw visible tools 同源证据仍留给 T04B/T09B/T09D。

## 15:44 [T04-final-reviewer]
-完成：按 T04 final 复核要求完成只读审计并复跑四份合同测试（19/19 通过），确认 T04 已覆盖 HK/CRYPTO fail-before-OpenClaw、缺/空 tools fail、缺 openviking_access fail、unknown intent fail、news 双能力、workspace manifest、OpenViking config。
-决策：当前 T04 未进入 T04A/T04B 与 runtime 接缝实现；手写 YAML 解析与本地 `DEFAULT_TOOL_REGISTRY` 仍属 non-blocking 设计风险，需在 T04A/T04B/T09 与 T04B/T09B/T09D 收敛。
待办：进入 T04A/T04B 前补齐“真实 workspace 资产验收 + OpenClaw provider visible tools 同源证据”，并以 runtime 证据关闭本地 registry 真相偏差。

## 15:49 [subagent T04A]
-完成：补齐 12 个 worker 的 `SKILLS.md`、`skills/manifest.yaml`、`prompt-review.yaml`，新增 `tests/contracts/test_all_worker_workspaces.py` 与 workspace prompt-review/结构校验 helper，`uv run pytest tests/contracts/test_all_worker_workspaces.py -q` 48 条通过。
-决策：保持现有 US/CN_A prompt 业务文本不改写，仅做非空与结构验收；`prompt-review.yaml` 统一使用 `reviewed_by: Codex migration review`；HK/CRYPTO 资产保持未批准状态，不纳入本任务通过条件。
待办：进入 T04B，收敛 stage tool registry 与 OpenClaw tool schema 合同和同源证据。

## 15:53 [subagent T04A-stage-policy-fix]
-完成：补上 12 个真实 worker 的 US/CN_A stage policy 合同验收测试，并为 12 份 `STAGES.yaml` 的 US/CN_A profile 补齐非空 `tools` 与 `openviking_access`，`uv run pytest tests/contracts/test_all_worker_workspaces.py -q` 72 条通过。
-决策：前线分析 worker 使用业务工具 + `openviking_access: write`；下游 worker 统一使用 `tools: [openviking_read]` + `openviking_access: read_write`；HK/CRYPTO 保持未批准且未补 tools/access，继续 fail-closed。
待办：T04B/T09 继续处理“本地 registry 与 provider visible tools 真相一致性”证据链，本任务不扩展。

## 15:57 [T04A-reviewer]
-完成：完成 T04A final 只读复核，核对 AGENTS/memory/迁移方案与任务清单、12 worker workspace 资产、stage policy/tool 解析与 prompt-review，并复跑 `uv run pytest tests/contracts/test_all_worker_workspaces.py -q`（72/72 通过）。
-决策：判定 T04A 已真实覆盖 12 worker 的必需文件与 US/CN_A 合同；`tools: [openviking_read] + openviking_access: read_write` 在当前解析器下属于可接受冗余（非阻塞）；`reviewed_by: Codex migration review` 可作为内部迁移审阅标记但不构成外部人工背书证明。
待办：T04B/T09 补齐“stage tool registry ↔ OpenClaw visible tools”同源证据与 runtime 级验证，避免仅靠本地静态配置通过。

## 16:01 [subagent T04B]
-完成：完成 T04B 合同层工具注册与阶段工具策略合同，实现 `resolve_intents` hard-fail，新增 `test_tool_registry_contract.py` 与 `test_stage_tool_policy_contract.py`，并通过指定验收与 T04/T04A 回归。
-决策：明确本地 tool registry 仅作合同层约束，不宣称等于 OpenClaw provider visible tools 真相；保留真实同源证据到 T09B/T09D。
待办：后续在 T09B/T09D 用真实 OpenClaw runtime 证据闭环本地 registry 与 provider visible tools 的同源关系。

## 16:03 [T04B-reviewer]
-完成：完成 T04B final 只读复核，核对指定文档与代码测试文件，复跑 T04B 指定合同与 T04/T04A/T04B 回归；确认当前覆盖 unknown/empty intent hard fail、unknown openviking access fail、missing openviking tool fail、12 worker US/CN_A 非空工具、frontline write、downstream read_write、news 双能力。
-决策：判定 T04B 在合同层实现范围内符合任务清单；同时明确本地 registry 不是 provider visible tools 真相，OpenClaw runtime/provider evidence 同源闭环尚未接入，需在 T09B/T09D 继续完成。
待办：进入 T09B/T09D 做真实 OpenClaw tool schema 与 provider evidence 对齐验证，关闭“本地合同通过但 runtime 同源证据缺失”缺口。

## 16:08 [T04B-access-fix]
-完成：移除 `resolve_stage_tools` 的 `openviking_access` 默认值并强制调用方显式 keyword 传入，补齐相关契约测试调用并新增“漏传参数时报错”回归测试，指定合同测试集全部通过。
-决策：保持 fail-closed，避免任何路径隐式回退到默认 `none`。
待办：无。

## 16:14 [subagent T05]
-完成：新增 `artifacts/refs.py` 与 `artifacts/manifest.py`，实现 OpenViking 材料对象、URI 目标生成、approved manifest、manifest-scoped read capability，并新增 T05 合同/单测后通过指定验收命令。
-决策：`make_material_target` 采用 `run/stage/worker/call` 固定 URI 形状并让 L2 prefix 以 `/evidence/` 结尾；`ApprovedManifest.add()` 返回新对象并同时拒绝重复 material_id、call_id、worker/stage 记录，避免重跑覆盖。
待办：后续 T06/T07 继续接 OpenViking approval read client、receipt 与 L1/L2 校验闭环。

## 16:20 [subagent T05-manifest-add-fix]
-完成：将 `ApprovedManifest.add()` 改为对当前 manifest 原地追加并返回 `self`，并新增“忽略返回值仍能进入下游材料”的单测；T05 指定合同/单测与回归验收均通过。
-决策：保持现有重复 material_id、call_id、worker/stage 拒绝逻辑不变，只调整 `add()` 的状态更新语义，避免后续调用方漏接返回值导致材料丢失。
待办：无。

## 16:26 [subagent T06]
-完成：实现 OpenViking approval read client 协议、OpenVikingStat 与访问错误类型，新增 fake OpenViking store/worker tools 越权边界，并完成 T06 指定合同测试通过。
-决策：在 `refs.py` 增补最小 `MaterialReceipt`（严格按详细设计 3.3 字段）；approval client 保持只读接口，worker fake tools 仅允许 capability 读取且写入受 `MaterialTarget` 限制。
待办：后续 T07 接入 receipt 校验与 L1/L2 guard 时，可复用本次 fake store 的 receipt/stat/read 夹具。

## 16:32 [subagent T06-compact-contract-fix]
-完成：补齐 T06 follow-up 的 compact 禁止合同测试，覆盖 fake client 与 fake worker tools 的 `compact_read`、`write_compact_as_l1` 默认 fail，且不改生产代码。
-决策：当前 fake 实现已按边界抛 `OpenVikingAccessError`，因此仅新增合同用例，不扩展任何 compact 成功路径或 list/latest/裸 URI 放行行为。
待办：无。

## 16:39 [subagent T07]
-完成：实现 OpenViking receipt/runtime read 与 L1/L2/claims guard，并新增 4 份 T07 合同测试；先红后绿，指定命令全部通过。
-决策：guard 采用最小字段协议，不改 workflow/controller 权责；runtime read 严格禁止 latest/list/裸 URI/compact，且 capability、material_id、URI 与 SHA 全量对齐 approved manifest 与 L2 index。
待办：等待后续任务把这些 guard 接入完整 approval/run loop 执行链。

## 19:38 [subagent T07-fix]
-完成：修复 T07 reviewer blocking gaps，收紧 runtime tool-calls 证据形状与身份校验，新增下游阶段 `status=none` 拒绝、OpenViking read 记录完整字段校验、L2 回源校验强制门禁，并补齐对应合同测试覆盖。
-决策：`validate_l1_l2_contract` 以最小破坏新增可选 `openviking` 参数；当 L1 存在需证据声明时，先强制 `validate_l2_entries` 通过，阻断“仅靠 evidence_id 的假 L2 index”通过路径。
待办：无。

## 19:45 [Chinese-comment-sweep]
-完成：完成 comment-policy follow-up，统一 `src/claw_trade` 迁移核心模块中文注释/docstring，并在 manifest/capability、receipt/runtime read、compact-latest-list 禁止路径、L1/L2 hard gate 等关键逻辑补充简短中文注释；测试全量通过且英文注释扫描清零。
-决策：仅做注释与 docstring 收口，不改任何业务逻辑、断言语义、函数签名或架构边界。
待办：无。

## 19:56 [subagent T07-source-fix]
-完成：完成 T07 follow-up 的 tool-calls 受控来源修复：runtime read guard 将 `source` 收紧为固定 `model_tool_events`，并新增单条 tool call 缺 `tool_call_id`/缺 `action`、以及 evidence 对象 `openclaw_run_id` 不一致的失败测试。
-决策：保持最小改动范围，只修改 `openviking_access.py` 与 `test_openviking_runtime_read_guard.py`；测试 helper 默认 source 改为 `model_tool_events`，read record 默认补齐 `tool_call_id` 与 `action`。
待办：无。

## 20:06 [subagent T08]
-完成：新增 `runtime/request_builder.py` 与 `guards/artifact_flow.py`，实现 WorkerCall/OpenClawCommand 构造、默认 L1 读取策略与材料流校验；新增 T08 合同测试并通过指定与建议回归命令。
-决策：WorkerCall 仅承载运行事实、approved refs/capabilities、material target、read policy、allowed tools、evidence dir 与 stop 标记；材料流 guard 对 material_id、sha、capability、URI、worker/stage/call 逐项一致性校验，不允许仅靠 material_id 放行。
待办：后续在 runner 接入时需把 call_id 生成策略替换为调度层稳定分发编号，避免默认 call_id 规则与未来 dispatcher 漂移。

## 20:17 [subagent T08-fix]
-完成：完成 T08 review blocking 修复：`validate_artifact_flow()` 改为按 `call.stage` 强制校验固定上游 worker 集合，补齐 `l2_index_uri` 与 `l2_allowed_prefix` 防篡改校验，并新增 6 个场景合同测试（缺失必需上游、夹带非法上游、FRONTLINE 禁带上游、两类 ref 篡改失败、完整匹配通过）。
-决策：不改 `make_worker_call` 的 `call_id` 生成策略；仅在 guard 层 fail-closed，要求 `upstream_materials` 与 `openviking_read_capabilities` 同时精确匹配 `required_upstream_workers(stage)` 对应 approved manifest 结果，避免“已批准但非当前阶段合法上游”穿透。
待办：runner 阶段仍需统一分发全局唯一 `call_id`，避免并发/重跑时默认规则产生碰撞风险。

## 21:01 [codex]
-完成：补强 `docs/control详细设计方案.md` 的 OpenClaw tool call 记录说明，明确 LLM 只看到业务工具，`tool-calls.json` 由 OpenClaw 工具执行层基于 `evidence_dir` 写出，并加入函数级伪码。
-决策：只修改设计文档，不改实现；把审计挂点限定在 `tool.execute(...)` 或 harness `handleToolCall(...)` 外层，避免把写审计文件暴露成模型可见工具。
待办：后续实现 OpenClaw recorder 时对照该函数级接口落地并补测试。

## 21:12 [codex]
-完成：在 `docs/control详细设计方案.md` 的单 worker 时序图后补充参与者职责表，解释 `workflow.store`、`runtime.request_builder`、`runtime.evidence_reader` 等模块的角色、调用关系、输入输出和禁止事项。
-决策：只补设计说明，不改代码；把 `workflow.store` 定义为本地状态和审计文件柜，把 `runtime.request_builder` 定义为出发前调用参数打包员，避免读者把它们误解成正式材料仓库或 OpenClaw runner。
待办：后续如继续发现图中模块名不清楚，优先在图旁补人话职责，不只在后文伪码里解释。

## 21:45 [codex]
-完成：根据 `docs/control详细设计方案.md` 新建 `docs/control详细设计实施任务清单.md`，拆出 T00-T67 原子任务，覆盖模型、controller、OpenViking、OpenClaw 接缝、hard gate、runner、CLI、真实实验和评审收口。
-决策：不覆盖不可见的旧迁移清单；新清单显式记录当前 `src/claw_trade` 与 `src-bak/claw_trade` 路径差异为 T00 风险，避免后续实现直接漂移。
待办：执行 T00 前需先确认源码目标目录。

## 21:53 [codex]
-完成：修正 `docs/control详细设计实施任务清单.md` 的执行边界，把任务改成从零创建 `src/claw_trade` 开始，并移除“实验/现状/对齐/跑通”和旧目录名残留。
-决策：旧代码目录只保留为只读参考概念，不再以具体路径写入实现任务、命令或违规扫描范围，避免工程师误把旧代码当源码根。
待办：后续执行从 T00 创建干净 `src` 目录开始。

## 21:58 [codex]
-完成：再次修正 `docs/control详细设计实施任务清单.md` 的路径表述，明确是在当前仓库根目录重建原来的 `./src/` 子目录，并在 `./src/claw_trade/` 下重建代码包架构。
-决策：避免使用 `claw-trade/src/...` 这种容易被理解成嵌套工程目录的写法，改成当前仓库根目录下的相对路径 `./src/` 和 `./src/claw_trade/`。
待办：无。

## 22:03 [codex]
-完成：继续修正 `docs/control详细设计实施任务清单.md`，明确 `./src/` 下的目录结构参考 `./src-bak/`，目标包名 `claw_trade` 来自 `./src-bak/claw_trade/`，实现内容仍从零写。
-决策：`./src-bak/` 只作为只读结构参考，不作为实现目标、导入路径、成功路径或整目录复制来源。
待办：无。

## 21:53 [codex]
-完成：新增根目录 `.gitignore` 并忽略 `src-bak/`。
-决策：仓库原本没有 `.gitignore`，因此新建最小忽略文件；不改其他未跟踪目录状态。
待办：无。

## 22:14 [codex]
-完成：修改 `docs/control详细设计方案.md`，把设计顺序改为先在已有工程根目录下重建 `./src/` 和 `./src/claw_trade/`，并把 §3.8 改成目标目录结构。
-决策：`./src-bak/` 在详细设计中只保留为结构参考，不再描述为实现目录、导入路径、成功路径或整体复制来源；§14 改为真实执行顺序，不再要求先跑现状。
待办：如继续修任务清单，应同步引用新的详细设计顺序。

## 22:25 [codex]
-完成：根据评审意见更新 `docs/control详细设计实施任务清单.md`，补 T29A/T34A/T25A/T47A/T50A/T63A 等缺失任务，收紧 OpenViking 读取接口，并拆分 approval、runner、exporter 的过大任务。
-决策：把并行批次写入清单顶部；OpenViking 下游读取只能走 manifest capability，PM/export/claim 只能解析机器可读结构化块，approval 最终接线放到 hard gate 之后。
待办：下一轮评审重点看新增任务是否仍有跨边界或伪码不足。

## 22:50 [T00-worker]
-完成：按 T00 要求在既有工程根重建 `src/claw_trade` 包目录，确保 `import claw_trade` 指向 `/home/frank/src/claw-trade/src/claw_trade/__init__.py`。
-决策：仅做最小落地（创建 `src/claw_trade/__init__.py` 并在 `pyproject.toml` 增加 wheel 的 src 包声明），不触及 T03 模块目录和 `agents`/`third_party/openclaw`。
待办：等待总管分配后续任务（T02/T03 及以后不在本次执行范围）。

## 23:00 [T03-worker]
-完成：在 `src/claw_trade` 下按 T03 仅重建模块目录包入口，新增 `workflow/config/runtime/artifacts/guards/reports/cli` 的 `__init__.py`，未提前创建后续任务文件。
-决策：`src-bak/claw_trade` 仅作为目录层级参考；严格限制改动到 T03 允许范围，不触碰 `agents`、`third_party/openclaw`、`pyproject.toml`、测试配置等边界外文件。
待办：等待总管分配后续任务（T04 及以后未执行）。

## 23:15 [B3-T09-worker]
-完成：新增 T09 所需通用结果/证据对象与 guard 聚合函数，覆盖 `OpenClawResult`、`ProviderEvidence`、`GuardResult`、`ApprovalResult`、`BootResult`、`OpenVikingStat`、`OpenVikingReadResult`、`OpenVikingAccessRecord` 及 `guard_passed/guard_failed/combine_guard_results`。
-决策：保持最小实现范围，仅在 `runtime/evidence_reader.py`、`guards/common.py`、`artifacts/openviking_client.py` 定义对象与函数；`ProviderEvidence` 不携带业务正文；`OpenViking` 错误分类固定为 8 类并在对象构造时校验。
待办：等待后续任务接线（本任务不实现 evidence 读取逻辑和 OpenViking read/stat 成功路径）。

## 23:16 [B3-core-worker]
-完成：完成 T04-T07 的控制面数据对象与纯翻译函数，新增 `workflow/models.py`、`workflow/workers.py`、`runtime/openclaw_client.py`，并通过指定验收命令。
-决策：为避免与 T08 并行冲突，`WorkerCall` 中 OpenViking 相关字段先采用结构化鸭子类型，由 `build_openclaw_command` 做只读序列化；保持 `agent == worker_id`，不引入业务正文。
待办：等待 T08 及后续任务把 OpenViking 强类型对象接入调用链。

## 23:18 [B3-T08-worker]
-完成：完成 T08 的 OpenViking 材料/manifest 数据对象与函数实现，新增 `src/claw_trade/artifacts/refs.py`、`src/claw_trade/artifacts/manifest.py`，并通过三条验收命令。
-决策：为适配并行中的 T04-T07，`stage` 与 `call` 相关类型采用兼容字符串值/鸭子类型读取，不改 `workflow/models.py`；manifest 下游能力严格由已批准材料生成，不提供目录扫描式读取路径。
待办：等待后续任务接入运行时与 guard/approval 流程。

## 23:43 [B3-fix-subagent]
-完成：完成 B3 的 T04-T09 评审阻塞修复：收紧 WorkerCall/OpenViking 类型、补 ArtifactFlowError 与 manifest 严格去重/缺失硬失败、修复 material target 默认值与 evidence 前缀、强化 OpenClawCommand 序列化必填校验、并让 blocked_worker_result 强制使用 failure.call_id。
-决策：`tests/contracts/test_worker_call_contract.py` 的唯一失败是缺 `claw_trade.runtime.request_builder`，按人类指令判定为 future T37/T37A out-of-scope，不在本次实现。
待办：后续 T37/T37A 实现 request_builder 后，再回归 worker_call contract。

## 23:48 [B3-fix-reviewer]
-完成：完成 B3 范围 T04-T09 只读 conformance 审查与指定命令复核，确认多数验收条件满足；发现 2 个阻塞项（`openclaw_client` 仍接受 `object` 并做字符串鸭子转换、`ProviderEvidence.stage` 与详细设计类型不一致）。
-决策：按范围要求不把 `runtime/request_builder.py` 缺失作为 B3 阻塞；阻塞仅聚焦当前 T04-T09 实现与设计边界漂移。
待办：由实现 owner 最小修复上述 2 项后，复跑本次同一组命令并补一条 reviewer 复核记录。

## 23:46 [B3-fix-quality-reviewer]
-完成：完成 B3 T04-T09 当前实现与 Curie 修复后的只读质量/证据审查，确认本轮仅发现非阻塞质量风险，未发现需立刻回滚或阻断的伪造链路。
-决策：`tests/contracts/test_worker_call_contract.py` 仅因缺失 `runtime/request_builder.py` 在收集阶段失败，按任务边界归类为 T37/T37A out-of-scope，不作为 B3 阻塞。
待办：进入 T37/T37A 后补齐 `request_builder` 再回归 worker_call contract，并增加序列化类型负例防止静默字符串化。

## 23:58 [B3-type-boundary-subagent]
-完成：完成 B3 范围 T07/T09 两个阻塞修复：`OpenClawCommand` 序列化不再对任意对象做 `str()`，`ProviderEvidence.stage` 改为 `Stage` 并提供显式字符串化 helper；新增最小单测覆盖非字符串拒绝与 Stage 枚举可用性。
-决策：保持最小改动且不触发 future T37/T37A；`tests/contracts/test_worker_call_contract.py` 仍仅因缺失 `claw_trade.runtime.request_builder` 失败，按 out-of-scope 记录不修。
待办：后续进入 T37/T37A 后补 `request_builder`，再回归 `test_worker_call_contract.py`。

## 23:57 [B3-type-boundary-quality-reviewer]
-完成：完成 B3 T07/T09 补修的只读质量与证据复核，确认 `test_openclaw_client.py`/`test_evidence_reader.py` 与指定回归测试集通过，`test_worker_call_contract.py` 仅因缺失 `runtime/request_builder.py` 收集失败并按 T37/T37A 归类 out-of-scope。
-决策：本轮不要求未来任务实现；阻塞判断仅针对当前 T07/T09 的类型边界、provider evidence 序列化边界与伪造路径风险。
待办：进入 T37/T37A 后补齐 `request_builder` 并回归 worker_call contract。

## 23:57 [B3-type-boundary-reviewer]
-完成：按 B3 审查范围复核 T07/T09 最新补修，确认两项既有阻塞（OpenClawCommand 静默字符串化、ProviderEvidence.stage 类型漂移）已解除，并通过指定测试与 Stage 身份断言。
-决策：审查严格限于 `openclaw_client.py`、`evidence_reader.py`、对应单测与设计文档，不把 `runtime/request_builder.py` 缺失判为 B3 阻塞。
待办：后续进入 T37/T37A 时再补 `request_builder` 并回归 `test_worker_call_contract.py`。
