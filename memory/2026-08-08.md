# 2026-08-08

## 01:03 [codex-scheduled-wechat-notification]
-完成：查明并修复网页聊天创建的定时报告保存为站内通知、完成后不进入微信发送队列的问题；聊天创建的 `/sched` 现在统一保存为逻辑主微信 `wechat_primary`，直接 Scheduler API 显式传入 `in_app` 的行为保持不变。
-决策：在意图草稿默认值处修复根因，不改调度器显式通知配置，不新增通知抽象；新增控制器持久化回归和 UI 聊天 `/sched` 确认链回归。聚焦回归 `98 passed`，新增两项回归 `2 passed`；全量测试为 `3340 passed, 69 failed, 2 skipped`，69 项均为既有缺失文档、数据层合同或 live 环境失败，不在本任务范围内。
待办：本地真实 Chrome 9223/9224 均无可复用调试会话，且环境无 Windows 互操作命令；遵守“不启动新 Chrome”约束，真实 Chrome 验收和生产部署尚未执行。
