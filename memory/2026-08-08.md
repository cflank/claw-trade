# 2026-08-08

## 01:03 [codex-scheduled-wechat-notification]
-完成：查明并修复网页聊天创建的定时报告保存为站内通知、完成后不进入微信发送队列的问题；聊天创建的 `/sched` 现在统一保存为逻辑主微信 `wechat_primary`，直接 Scheduler API 显式传入 `in_app` 的行为保持不变。
-决策：在意图草稿默认值处修复根因，不改调度器显式通知配置，不新增通知抽象；新增控制器持久化回归和 UI 聊天 `/sched` 确认链回归。聚焦回归 `98 passed`，新增两项回归 `2 passed`；全量测试为 `3340 passed, 69 failed, 2 skipped`，69 项均为既有缺失文档、数据层合同或 live 环境失败，不在本任务范围内。
待办：无。

## 15:46 [codex-scheduled-wechat-production]
-完成：复用用户已打开的 Chrome `Profile 1`，通过 Playwright MCP 扩展和现有 token 建立单一持久连接；WSL 向 Windows 传 token 必须使用 `WSLENV=PLAYWRIGHT_MCP_EXTENSION_TOKEN/w`。真实 Chrome 打开 `http://192.168.1.21:5175/`，创建 `/sched BTC 每天 23:58` 后确认落盘为 `wechat_clawbot + wechat_primary`，随后删除测试计划；页面 0 error，6 条 warning 均来自 Playwright/MetaMask 扩展。
-完成：部署 `claw-trade-production-1.0.0-20260808T192200Z`。生产发布目录禁止用 `cp -al`：OpenClaw 会拒绝硬链接文件；本次改用普通 `cp -a`，确认旧/新 inode 不同且链接数为 1。只保留当前发布，OpenViking/OpenClaw/UI health 均为 200。
-完成：生产原有 `schedule-1`、`schedule-6` 仍是历史 `in_app`，已一次性迁移为 `wechat_clawbot + wechat_primary`；UI API 回读两条 active 任务均走微信。迁移时原子替换文件必须保持 `clawtrade:clawtrade / 600`，否则 UI 会因无权读取退出。
