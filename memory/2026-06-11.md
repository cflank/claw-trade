## 03:29 [task-a-http-rate-limit-subagent]
-完成：修复 managed_http 枚举值未触发通用 HTTP 级限流的问题，并补充 FetchEngine 与 ExecutionGate 的枚举场景聚焦测试。
-决策：统一按枚举 `.value` 或原字符串比较 `http_visibility`，避免硬编码 Coinglass 或新增 guard/hard gate。
待办：无。

## 07:35 [task-b-crypto-intraday-columnar]
-完成：确认 CRYPTO intraday 小时线在列式仓库写入时被日期化，且 manifest supersession 对同币种同粒度前缀覆盖过宽；修复为保留 `period_start`/`period_end` 完整时间，并让日期范围查询按全天边界命中小时线。
-改动：`normalized_columnar.py` 拆分周期存储文本和范围查询文本；`repository.py` 的 columnar manifest 查询/覆盖使用相同边界语义，并限制前缀覆盖只适用于 realtime；新增同日 00:00/01:00 BTC 1h 分批写入聚焦测试。
-验证：`uv run pytest tests/unit/data_gateway/test_normalized_columnar_warehouse.py -q` -> 19 passed；`uv run pytest tests/unit/data_gateway/test_warehouse.py -q` -> 39 passed；`uv run pytest tests/unit/data_gateway/test_ingest_pipeline.py -q` -> 23 passed。
-mock/stub/fake/fallback：无新增。

## 03:40 [task-c-credential-source-skip]
-完成：修复未配置/禁用需凭证 provider 的 selector skip 污染报告 `credential_missing` 缺口的问题，并补 CRYPTO/US 聚焦测试。
-决策：凭证解析器区分 `source_not_configured`、`source_disabled`、`credential_missing`；未配置/禁用只写 selector-skip attempt evidence，不生成用户可见 `DataGap`；已启用但 secret 缺失仍保留 `credential_missing` gap。
-验证：`uv run pytest tests/unit/data_gateway/test_provider_selector.py tests/unit/data_gateway/test_ingest_pipeline.py tests/unit/data_gateway/test_data_service_flow.py tests/contracts/test_data_layer_failure_semantics.py` -> 80 passed；`uv run pytest tests/contracts/test_provider_capabilities.py -k 'credential or settings_resolver or missing' tests/unit/data_gateway/test_rate_limit_policy.py` -> 13 passed。
-待办：完整 `test_provider_capabilities.py` 仍有既有 Coinglass `futures_pairs_markets` symbol 参数合同失败，需单独处理。

## 08:20 [coinglass-rate-limit-and-pairs-follow-up]
-完成：根据 live 返回 `coinglass_api_400:Required String parameter 'symbol' is not present`，修正 `futures_pairs_markets` 为传合约 symbol（如 `BTCUSDT`），并更新合同/单元测试。
-完成：DataService 接入通用的远端 429 等待后重试一次；该行为只使用用户配置的 `overflow=wait`、`window_seconds`、`wait_timeout_seconds`，不硬编码 Coinglass。没有等待策略或等待超时时，保留 attempt 证据并尝试后续 provider。
-验证：`uv run pytest tests/unit/data_gateway/test_crypto_coinglass_plugin.py tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_futures_pairs_markets_normalizes_binance_usdt_snapshot tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_data_service_flow.py -q` -> 49 passed。
-注意：`CLAW_TRADE_REPORT_PREFETCH_DOMAIN_TIMEOUT_SECONDS` 当前默认值是 `0.0`；旧 memory 中“默认 90s”是当时实现记录，不代表当前默认。

## 08:58 [btc-market-live-options-fix]
-完成：修复 Coinglass BTC 期权 OI/成交量解析。根因不是接口无数据，而是 `/api/option/exchange-oi-history` 返回 `data.time_list + data.data_map` 的交易所序列结构，旧 parser 只认普通行列表，导致误判 `empty_result`；成交量来自已验证存在的 `/api/option/exchange-vol-history`。
-完成：期权 OI/成交量按时间聚合各交易所数值，写入 `options_open_interest/options_volume`，并补 `options_open_interest_unit/options_volume_unit=USD`。
-验证：`uv run pytest tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_options_cvd_and_etf_flow_fetch_and_normalize tests/unit/data_gateway/test_crypto_coinglass_plugin.py tests/contracts/test_provider_capabilities.py::test_crypto_coinglass_futures_pairs_markets_normalizes_binance_usdt_snapshot -q` -> 3 passed；`uv run pytest tests/unit/reports/test_data_pack_bridge.py -q` -> 47 passed；`uv run pytest tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_execution_gate.py tests/unit/data_gateway/test_data_service_flow.py tests/unit/reports/test_data_pack_bridge.py -q` -> 183 passed。
-BTC market live 短链：`scripts/start-control-runtime.sh -- uv run python -`，证据 `docs/evidence/btc-market-prefetch-options-fix-20260611T084408Z.json`，17 个 market 请求：13 ready、4 partial、0 missing、0 rate_limited。期权 OI/成交量从 missing 变为 ready，365 rows。剩余 partial 为：1h 小时线 1000 rows 的全年覆盖不足、清算热力图 360 rows 的全年覆盖不足、两个链上序列的覆盖不足。

## 09:53 [btc-fullchain-data-gap-check]
-完成：按固定 runtime 入口跑 BTC 全链：`scripts/start-control-runtime.sh -- uv run python scripts/run_claw_trade_fresh_report.py --ticker BTC --company-name Bitcoin --market CRYPTO --profile CRYPTO --currency USDT --currency-symbol USDT --current-date 2026-06-11 --start-date 2025-06-11 --end-date 2026-06-11 --output-dir docs/evidence/fullchain-crypto-btc-after-options-fix-20260611/btc`。
-结果：exit code 0；run_id=`run-20260611-092752-9d3d9cd0`；final report=`docs/evidence/fullchain-crypto-btc-after-options-fix-20260611/btc/final_report.md`；manifest=`runs/run-20260611-092752-9d3d9cd0/data-layer/report-prefetch.json`。
-数据层：29 个请求，18 ready、10 partial、1 missing；缺口原因只有 `date_range_missing` 8 个和 `warehouse_missing` 2 个；没有 `rate_limited`、`cooldown_skipped`、`credential_missing`、`empty_result`。
-剩余缺口：市场 1h 小时线 1000 rows 仅覆盖 2026-04-30 18:00 至 2026-06-11 09:00；清算热力图 360 rows 覆盖 2025-06-17 至 2026-06-11，缺约 6 天；链上日频仅 1 日，大额转账事件 8 笔；基本面 DeFi 日频收入/费用 0 rows；ETF/机构资金 253 rows 覆盖交易日但不是完整自然日；新闻/宏观为搜索线索且时间覆盖不足；社交只有 Fear & Greed 单点，缺真实社交平台样本。
-报告检查：final report 未出现 `rate_limited`、`cooldown_skipped`、`credential_missing`、`empty_result`、`date_range_missing`、`warehouse_missing`、`CoinGecko`、`Coinglass`、`terminal`、`no_recent`、`no_candidate`、`bearish_reversal_countdown` 等内部词；清算地图、CVD/主动买卖量代理、资金费率、OI、多空比均已写入可分析。

## 15:22 [paid-data-source-prune]
-完成：按用户要求把需要密钥的内置数据源收窄为 6 个：Tushare、Finnhub、FRED、CoinGecko Pro、Coinglass、Glassnode。
-改动：设置页源目录、env allowlist、source probe、provider registry、provider-source mapping、rate-limit mapping、前端设置页分类、相关契约测试已同步。Alpha Vantage、LunarCrush、Token Terminal 不再有运行时插件/注册/设置入口；US daily 基础行情改由 Yahoo 公共源承担。
-验证：`uv run pytest tests/settings/test_enhanced_sources.py tests/settings/test_enhanced_source_health_probes.py tests/unit/runtime/test_settings_projection.py tests/unit/data_gateway/test_provider_registry.py tests/unit/data_gateway/test_provider_selector.py tests/unit/data_gateway/test_data_service_flow.py tests/contracts/test_provider_capabilities.py tests/contracts/test_data_layer_final_acceptance_plan.py -q` -> 198 passed；`pnpm --dir web/research-ui test -- settings-enhanced-sources.test.tsx settings-css-sections.test.tsx settings-forbidden-surface.test.tsx` -> 72 passed。
-注意：历史 docs/evidence 与 memory 中仍会出现旧源名称，那是历史记录，不代表当前运行时仍支持。

## 15:39 [official-api-raw-provider-channel]
-完成：为保留的 6 个需凭证数据源新增官方原始接口调用通道：Tushare、Finnhub、FRED、CoinGecko Pro、Coinglass、Glassnode。每个源注册 `official_api_response/official_api_call`，覆盖 CN_A/US/HK/CRYPTO 四个市场；调用时由上层显式传官方 `path` 或 Tushare `api_name`，数据层只负责打到该 provider 官方 base URL 并返回 raw payload。
-决策：旧结构化 provider 能力表继续用于报告/入库字段选择；官方原始接口通道只解决“接口层不能被旧白名单卡死”，不声称所有官方接口已完成结构化映射。
-安全边界：官方原始通道只允许 provider 自己的 base URL 下的相对路径，拒绝绝对 URL 和 `..` 路径逃逸；这不是业务白名单，是防 SSRF。
-清理：删除静态 `known_missing_adapter_capabilities` 里的旧缺口登记，避免继续用猜测表宣布 Tushare/mootdx 缺口；真实缺口以后应来自 live attempt 和 provider 返回。
-验证：`uv run pytest tests/unit/data_gateway/test_official_api_plugin.py tests/contracts/test_provider_adapter_inventory.py tests/contracts/test_provider_capabilities.py tests/unit/data_gateway/test_provider_registry.py tests/unit/data_gateway/test_provider_selector.py tests/unit/data_gateway/test_data_service_flow.py -q` -> 150 passed；`uv run pytest tests/settings/test_enhanced_sources.py tests/settings/test_enhanced_source_health_probes.py tests/unit/runtime/test_settings_projection.py tests/unit/data_gateway/test_provider_registry.py tests/unit/data_gateway/test_provider_selector.py tests/unit/data_gateway/test_data_service_flow.py tests/contracts/test_provider_capabilities.py tests/contracts/test_data_layer_final_acceptance_plan.py tests/contracts/test_provider_adapter_inventory.py tests/unit/data_gateway/test_official_api_plugin.py -q` -> 213 passed；`pnpm --dir web/research-ui test -- settings-enhanced-sources.test.tsx settings-css-sections.test.tsx settings-forbidden-surface.test.tsx` -> 72 passed。

## 16:14 [settings-data-source-flat-ui]
-完成：设置页“增强数据源”UI 不再按市场/行情/基本面/新闻/衍生品等范围拆分；改为扁平展示保留的 6 个 API 数据源，同一个源只出现一次、只编辑一份配置。
-改动：`SettingsSections.tsx` 删除市场 tab 与功能卡片逻辑，新增固定 6 源前端过滤，旧源即使出现在响应里也不渲染；CSS 从 `ct-source-category-*` 改为 `ct-source-card`；相关前端测试同步。
-验证：`pnpm --dir web/research-ui test -- settings-enhanced-sources.test.tsx settings-css-sections.test.tsx settings-page.test.tsx settings-responsive-motion.test.tsx settings-forbidden-surface.test.tsx` -> 72 passed。

## 21:06 [report-no-startup-prefetch]
-根因：`/report` 启动阶段被接入 `ReportDataPrefetcher`，会在调度 worker 前预先拉取所有报告域数据；这会把通用流程变成“先全量取数，再让 worker 写报告”，导致总耗时/限流/失败被集中放大。
-完成：移除 `ControlRunner` 的启动预取入口和前线 worker 自动注入的 `report_prefetch_manifest_path`；删除未引用的 `ReportDataPrefetcher` 包装类。前线工具在没有强制 manifest 时回到按需调用 DataAPI；显式 `report_prefetch_required=true` 的严格校验保留。
-验证：`uv run pytest tests/unit/test_control_runner.py::test_report_command_does_not_prefetch_data_before_controller_dispatch tests/unit/test_control_runner.py::test_report_frontline_worker_calls_do_not_receive_prefetch_manifest_path tests/unit/reports/test_data_pack_bridge.py::test_frontline_data_pack_fails_when_report_prefetch_manifest_missing tests/unit/reports/test_data_pack_bridge.py::test_frontline_data_pack_uses_data_api_when_optional_prefetch_manifest_missing -q` -> 4 passed；`uv run pytest tests/unit/test_control_runner.py tests/contracts/test_frontline_tool_protocol.py -q` -> 53 passed。
-注意：`uv run pytest tests/unit/reports/test_data_pack_bridge.py tests/contracts/test_frontline_tool_protocol.py -q` 仍有 2 个既有字段能力表失败，位置在 crypto fundamental request 顺序/字段与 US provider capability 覆盖，不属于本次启动预取修复。

## 22:31 [ashare-btc-fullchain-timeout-fix]
-根因1：A股 `600519.SH` live 中 `fundamental_analyst` 第一次资料包工具调用超时、第二次同工具成功，但 guard 只看见历史 error 就失败；这是“成功重试后仍失败”的 false failure。
-修复1：`tool_calls` guard 改为按同一前线资料包工具汇总结果；同一工具只要后续有 success，就不因早先 timeout 失败。Guard source 记录为 2026-06-12 CN_A live 证据；合同测试覆盖。
-根因2：BTC market pack 一次性拉日线、小时线、盘口、OI、资金费率、多空比、CVD、清算、期权、订单簿、链上等 17 类请求；直接调用没有资料包内部返回预算，导致 Python 子进程不返回，OpenClaw 最后 gateway 600s 超时。
-修复2：CRYPTO market 前线资料包默认 150s 内逐请求写进度并返回 partial；已完成数据继续给 worker，未完成请求写成真实缺口 `frontline_data_pack_domain_timeout:market:150s`。这不是启动预取，也不是总链路超时；只保证单个 worker 工具按时返回。
-工具层：market/fundamental Node 子进程窗口提高到 180s，CRYPTO market 显式走 crypto market timeout 常量；news/social 保持 20s。
-提示词：CRYPTO market worker prompt 里的旧“预取超时”口径已改为“资料包工具超时/资料包超时”，避免 worker 把现场取数超时误写成启动预取。
-验证：`uv run pytest tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_frontline_data_pack_timeout_is_enabled_by_default tests/unit/reports/test_data_pack_bridge.py::test_frontline_crypto_market_pack_returns_partial_when_one_request_times_out tests/unit/reports/test_data_pack_bridge.py::test_crypto_market_domain_timeout_text_does_not_claim_all_sources_unavailable tests/contracts/test_frontline_tool_protocol.py::test_market_pack_uses_extended_timeout_contract tests/contracts/test_tool_calls_guard.py -q` -> 14 passed；`git diff --check -- ...` -> exit 0。
-A股 live：fixed runtime `scripts/start-control-runtime.sh -- ... 600519.SH`，最终成功 run_id=`run-20260612-013702-55dcac61`，输出 `docs/evidence/fullchain-a-share-crypto-20260612/cn_a_600519/final_report.md`，workers=22。
-BTC live：fixed runtime preflight 发现旧 `runtime.env` 存在、1933 通、18789 不通，按固定脚本重启；最终成功 run_id=`run-20260612-021507-74c2f11d`，输出 `docs/evidence/fullchain-a-share-crypto-20260612/crypto_btc_after_timeout_fix/final_report.md`，workers=19，exit 0。
-BTC 数据源情况：market 工具状态 success，耗时约 158s，返回 partial；拿到日线、小时线、实时价、盘口、OI、资金费率、图表、CryptoLens；缺口为多空比、CVD/主动买卖量、强平/清算热力图、期权、交易所净流入、订单簿聚合深度、链上日频/事件/实时指标，原因是资料包 150s 内未完成。
