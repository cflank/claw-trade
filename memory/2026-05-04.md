## 00:05 [codex]
-完成：补齐 B3/T09 的负例合同缺口，在 ProviderEvidence 正式接口拒绝错误 stage 类型并新增对应单测。
-决策：在 `ProviderEvidence.__post_init__` 做最小强类型校验，抛出包含 `stage` 与 `Stage` 的 `TypeError`，保持序列化继续输出 `stage.value`。
待办：`tests/contracts/test_worker_call_contract.py` 仍因缺失 `claw_trade.runtime.request_builder` 报错，属于未来 T37/T37A 范围。

## 00:08 [codex-reviewer]
-完成：完成 B3/T09-negative-contract 的质量/证据审查；当前实现满足“显式 TypeError 拒绝非 Stage”与负例覆盖要求，未发现阻塞问题。
-决策：将 `tests/contracts/test_worker_call_contract.py` 的导入失败归类为 T37/T37A out-of-scope，不作为本次阻塞项。
待办：后续在 T37/T37A 落地后复跑 worker_call_contract 合同测试并回填证据。

## 00:08 [codex-review]
-完成：完成 B3/T09 负例补修 conformance 审查，确认 `ProviderEvidence.stage` 仍是 `Stage`，并合同化拒绝错误类型。
-决策：按 T09 与方案边界仅核对 `evidence_reader`/单测/证据日志，不把 `request_builder` 缺失视为本任务阻塞。
待办：无

## 00:14 [B4-openclaw-entry-locator]
-完成：完成 B4/T27 只读定位，确认 OpenClaw 单 worker 真实执行入口链为 `agentCliCommand -> agentCommand -> runAgentAttempt -> runEmbeddedPiAgent -> runEmbeddedAttemptWithBackend -> runAgentHarnessV2LifecycleAttempt -> runEmbeddedAttempt`，并确认当前仓库不存在 `SingleWorkerCommand`/`runSingleWorkerCommand`。
-决策：将 `packages/memory-host-sdk/src/host/openclaw-runtime*.ts`、`openclaw.mjs`、`src/agents/pi-embedded-runner.ts` 归类为包装/转发层，不作为 T28-T35/T34A 的主修改入口；后续应围绕已确认入口附近文件改动。
待办：B6(T28-T35/T34A) 仅允许在本记录确认的入口邻近文件收口，优先 `src/agents/command/attempt-execution.ts`、`src/agents/pi-embedded-runner/run.ts`、`src/agents/pi-embedded-runner/run/attempt.ts`、`src/agents/pi-embedded-runner/stream-resolution.ts`、`src/agents/pi-embedded-runner/model.ts`、`src/agents/provider-request-config.ts`、`src/agents/provider-transport-stream.ts`、`src/agents/tool-allowlist-guard.ts`。

任务编号：T27
状态：达到预期
设计引用：`docs/control详细设计实施任务清单.md` §1.1(B4/B6) 与 §7(T27)
改动文件：`memory/2026-05-04.md`
新增/修改函数：无（只读定位）
执行命令：
- `rg -n "provider request|tools|toolCall|runSingle|agent|openclaw-runtime" third_party/openclaw`（exit 0）
- `rg -n "SingleWorkerCommand|runSingleWorkerCommand" third_party/openclaw`（exit 1，无匹配）
- 多次 `sed/nl/rg` 对候选与调用链文件取证（exit 0）
关键输出：
- `runEmbeddedPiAgent` 定义：`third_party/openclaw/src/agents/pi-embedded-runner/run.ts:303`
- `runAgentAttempt -> runEmbeddedPiAgent`：`third_party/openclaw/src/agents/command/attempt-execution.ts:561`
- `runEmbeddedPiAgent -> runEmbeddedAttemptWithBackend`：`third_party/openclaw/src/agents/pi-embedded-runner/run.ts:1014`
- `runEmbeddedAttemptWithBackend -> runAgentHarnessAttemptWithFallback`：`third_party/openclaw/src/agents/pi-embedded-runner/run/backend.ts:4-7`
- `runEmbeddedAttemptWithBackend` 最终落到 `runEmbeddedAttempt`（工具/流式/prompt 提交核心）：`third_party/openclaw/src/agents/pi-embedded-runner/run/attempt.ts`
真实证据路径：
- `third_party/openclaw/src/commands/agent-via-gateway.ts:218-229`
- `third_party/openclaw/src/agents/agent-command.ts:1260-1277`
- `third_party/openclaw/src/agents/command/attempt-execution.ts:561-616`
- `third_party/openclaw/src/agents/pi-embedded-runner/run.ts:303-305,1014-1048`
- `third_party/openclaw/src/agents/harness/selection.ts:175-196`
- `third_party/openclaw/src/agents/pi-embedded-runner/run/attempt.ts:1141-1146,1583-1611,1827-1870,2977-3014`
- `third_party/openclaw/src/agents/pi-embedded-runner/model.ts:537-607`
- `third_party/openclaw/src/agents/provider-request-config.ts:694-770`
- `third_party/openclaw/src/agents/provider-transport-stream.ts:109-146`
- `third_party/openclaw/src/agents/tool-allowlist-guard.ts:8-38`
- 非入口反证：`third_party/openclaw/openclaw.mjs:306-309`、`third_party/openclaw/packages/memory-host-sdk/src/host/openclaw-runtime-cli.ts:1-17`
偏离设计：无
禁止路径扫描结果：未修改 OpenClaw 源码；未引入 12 worker DAG/批准/hard gate/报告导出逻辑
中文注释覆盖：N/A（本任务无代码实现）
下一步：B6 owner 以本记录为唯一入口白名单执行 T28-T35/T34A；若超出该范围需先回到 T27 重新定位并更新记录

## 00:17 [codex]
-完成：完成 B4/T25A 结构化 claim 合同落地，L1 仅解析 `control.claims.v1` 机器可读区块并接入 L1/L2 guard。
-决策：将 claim 来源限定为 JSON block（代码块或整段 JSON），并在 `require_l1_claim_block` 做 run/call/worker/stage 身份硬校验；高风险 claim 必须映射 L2 evidence。
待办：T25 的 L2 回源/stat/read 完整性核验与 receipt 真实性校验由后续任务继续实现。

任务编号：T25A
状态：达到预期
设计引用：`docs/control详细设计实施任务清单.md` §1.1(B4) 与 §T25A；`docs/control详细设计方案.md` §10
改动文件：
- `src/claw_trade/artifacts/claims.py`
- `src/claw_trade/guards/l1_l2.py`
- `tests/contracts/test_claims_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
新增/修改函数：
- `parse_l1_claim_block(l1_text, material=None) -> ClaimBlockResult`
- `require_l1_claim_block(l1_text, call) -> tuple[tuple[L1Claim, ...], GuardResult]`
- `validate_claim_block_identity(block, call) -> GuardResult`
- `validate_claims(claims, l2_index) -> GuardResult`
- `validate_l1_l2_contract(call, l1_text, raw_output, l2_index) -> tuple[tuple[L1Claim, ...], GuardResult]`
- `validate_l2_entries(openviking, l2_index, allowed_prefix) -> GuardResult`（仅结构字段范围）
执行命令：
- `uv run pytest tests/contracts/test_claims_guard.py tests/contracts/test_l1_l2_guard.py -q`（exit 0）
- `rg -n "heuristic|自然语言|guess|infer|fallback|direct_llm|mock|stub|fake|materializer|regex" src/claw_trade/artifacts/claims.py src/claw_trade/guards/l1_l2.py tests/contracts/test_claims_guard.py tests/contracts/test_l1_l2_guard.py`（exit 1，无命中）
关键输出：
- `20 passed in 0.11s`
- 禁止路径扫描无匹配
真实证据路径：
- `src/claw_trade/artifacts/claims.py`
- `src/claw_trade/guards/l1_l2.py`
- `tests/contracts/test_claims_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
偏离设计：无（未实现 T25 receipt/stat/read 完整性）
禁止路径扫描结果：无 `fallback/direct_llm/mock/stub/fake/materializer` 关键词命中
中文注释覆盖：
- `src/claw_trade/guards/l1_l2.py` 中注明“只从结构化 claim block 读取声明，禁止正文启发式抽取”
下一步：等待 B8/T40-T48 接入本合同对象；T25 完整性任务在专属范围补齐

## 00:17 [B4-config-implementation]
-完成：完成 B4/T17-T21 的 config 合同实现与对应 contracts 改造，US/CN_A 可用、HK/CRYPTO 明确阻断，workspace 仅做资产存在性校验，stage policy + tool policy + OpenViking 探测均按控制面边界落地并通过验收测试。
-决策：`load_stage_policy` 使用最小 YAML 解析只读取结构字段（stage/profiles/tools/access/approved），避免 Python 读取 worker 自然语言正文；`probe_openviking_contract` 采用 read/stat/read_receipt 方法可调用性作为阻断条件，禁止本地文件回退成功路径。
待办：等待后续 B5/B7 owner 接入 request_builder/runner 时按 `*Result/GuardResult/BootResult` 合同联调。

## 00:21 [B4-openviking-artifacts]
-完成：完成 B4/T22-T24：收紧 OpenViking URI/target 合同、实现 receipt/capability 双通道 client 合同、落地 ApprovedManifest + ManifestStore，并把授权合同测试改到新接口后全绿。
-决策：OpenViking client 不暴露 `read/latest/list/compact` 泛化成功接口；审批只走 receipt+target，下游只走 capability；manifest.add 增加硬校验阻断未批准材料。
待办：等待后续 owner 在 T44/T45/T53D 里把这些合同接入 guard/runner 持久化主流程。

任务编号：T22/T23/T24
状态：达到预期
设计引用：`docs/control详细设计实施任务清单.md` §T22-§T24；`docs/control详细设计方案.md` §4.8/§4.9/§4.10/§8
改动文件：
- `src/claw_trade/artifacts/refs.py`
- `src/claw_trade/artifacts/openviking_client.py`
- `src/claw_trade/artifacts/manifest.py`
- `tests/contracts/test_openviking_contract.py`
- `tests/contracts/test_openviking_client_contract.py`
- `tests/contracts/test_openviking_forbidden_access.py`
- `tests/contracts/test_openviking_receipt_guard.py`
- `tests/contracts/test_openviking_runtime_read_guard.py`
- `tests/unit/test_manifest.py`
- `memory/2026-05-04.md`
新增/修改函数：
- `make_material_target(...)`、`validate_viking_uri_shape(...)`
- `OpenVikingClient.ensure_namespace/probe_read_stat_receipt/read_receipt/stat_for_receipt_verification/read_for_receipt_verification/read_l2_index_for_receipt_verification/stat_with_capability/read_with_capability/read_l2_index_with_capability`
- `ApprovedManifest.add/required_workers_for/has_worker/for_downstream_stage/capabilities_for_downstream_stage/all_for_run`
- `ManifestStore.load/add`
执行命令：
- `uv run pytest tests/unit/test_manifest.py tests/contracts/test_openviking_contract.py tests/contracts/test_openviking_client_contract.py tests/contracts/test_openviking_forbidden_access.py tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_openviking_runtime_read_guard.py -q`（exit 0）
- `rg -n "def read\\(|def read_text|latest|compact|list\\(|fallback|direct_llm|mock|stub|fake|materializer" src/claw_trade/artifacts tests/contracts/test_openviking_contract.py tests/contracts/test_openviking_client_contract.py tests/contracts/test_openviking_forbidden_access.py tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_openviking_runtime_read_guard.py tests/unit/test_manifest.py`（exit 0）
关键输出：
- `27 passed in 0.16s`
- 关键词扫描在 `src/claw_trade/artifacts` 未出现泛化 `def read(...)` 成功接口；命中主要为测试断言/假数据命名
真实证据路径：
- `src/claw_trade/artifacts/refs.py`
- `src/claw_trade/artifacts/openviking_client.py`
- `src/claw_trade/artifacts/manifest.py`
- `tests/contracts/test_openviking_*.py`
- `tests/unit/test_manifest.py`
偏离设计：无
禁止路径扫描结果：未引入 fallback/direct_llm/materializer；未新增 latest/list/compact/裸 URI 成功接口
中文注释覆盖：
- `refs.py` URI 边界注释（run/stage/worker/call 强绑定）
- `openviking_client.py` 审批 L2 index 固定路径注释（禁止 latest/list/目录扫描）
- `manifest.py` hard gate 进入 manifest 边界注释
下一步：由后续批次把 ManifestStore 接入 runner 写序（guard/approval evidence 之后）并补 T44-T46 runtime/receipt guard 集成

## 00:26 [b4-spec-conformance-reviewer]
-完成：完成 B4（T17-T21、T22-T24、T25A、T27）只读 conformance 审查并复跑指定合同测试；发现 1 个阻塞项：T21 当前仅做方法存在性检查，未做 read/stat/receipt 真实可用性探测。
-决策：将 T21 判定为 `未达到预期`（需人类决策：是否在 B4 引入真实探测调用/健康探针，或明确把真实探测下放到后续任务并改写验收口径）；其余任务按当前范围判定 `达到预期`。
待办：由实现 owner 补充 T21 真实探测证据后，重新跑 `tests/contracts/test_openviking_config_contract.py` 及全套 B4 合同测试再闭环。

## 00:27 [codex-reviewer]
-完成：完成 B4(T17-T21/T22-T24/T25A/T27) 质量与证据复审；发现 2 个阻塞偏差：T21 探测仅做 callable 检查会假成功、T24 manifest 可接收未证明 gate-pass 的构造材料。
-决策：将 `tests.fakes` 定性为隔离测试替身（非生产成功路径），但把 `probe_openviking_contract` 与 `ManifestStore.add` 的假成功窗口标记为必须修复后再宣称达到预期。
待办：按阻塞项补强 T21 真探测与 T24 gate-pass 绑定证据，再重跑 B4 合同集。

## 00:36 [B4-T21-real-probe]
-完成：完成 B4/T21 最小修复：`probe_openviking_contract` 改为真实探测调用，优先 `probe_read_stat_receipt()`，并把异常/失败/缺能力统一阻断为 `BootResult.blocked(openviking_contract)`。
-决策：统一 probe 可用时只采信其返回；缺统一 probe 时逐个真实调用 `read/stat/read_receipt`，并拒绝 `None` 或显式 `ok=False` 返回，避免“仅 callable 即通过”的假成功。
待办：无

## 00:37 [B4-T24-gate-bound-manifest]
-完成：完成 B4/T24 hard-gate-pass 绑定：`ManifestStore.add` 写入 approved manifest 前会强制校验 `hard_gate_result_path` 指向的 JSON 文件存在且明确为 pass，否则拒绝写入。
-决策：按最小合同优先复用 `GuardResult` 语义（`ok: true`），并兼容 `status: pass|passed|ok`；`ok:false`、`status: fail`、缺字段、非 JSON、不存在文件都视为未通过。
待办：等待 T53D 将本约束接到 runner 的最终 approval->manifest 落库时序（本任务不提前实现 T26 dataflow）。

## 00:41 [b4-quality-evidence-reviewer]
-完成：完成 B4(T21/T24) 修复后质量与证据复审：T21 的抛错/统一 probe false 会阻断、统一 probe true 可通过；T24 的缺失 gate 文件/非法 JSON/`ok:false`/`status:blocked|fail` 均被拒绝，`status:pass` 才允许落库。
-决策：将 `ApprovedManifest.add()` 仍可接收“未校验 hard gate 文件真实通过”的构造 `ApprovedMaterial` 记为阻塞风险（当前仅 `ManifestStore.add()` 强制 gate-pass），建议后续接线时禁止绕过 store 直接写 manifest。
待办：由实现 owner 明确并固化“禁止直接调用 `ApprovedManifest.add` 作为成功路径”的边界（代码或合同测试二选一）。

## 00:48 [b4-spec-conformance-reviewer]
-完成：完成 B4 阻塞复审（仅核对 T21/T24）；确认 T24 已绑定 hard gate pass，但 T21 仍存在统一 probe 假成功窗口，当前结论为 `BLOCKED`。
-决策：将 `probe_openviking_contract` 对统一 probe 的“直接信任”判定为阻塞；`ManifestStore.add` 的 gate 文件存在/JSON/pass 校验判定为达到当前 T24 边界。
待办：T21 需把统一 probe 的“可调用即通过”语义替换为真实可用性证明（至少可检测失败返回并拒绝未知成功对象），修复后重跑合同集闭环。

## 00:46 [code-hygiene-subagent]
-完成：完成 Python 缓存卫生修复，删除 `src/` 与 `tests/` 下全部 `__pycache__` 目录，并在 `.gitignore` 补齐缓存忽略规则。
-决策：仅修改 `.gitignore` 与 `memory/2026-05-04.md`，不触碰任何业务源码与测试实现，避免与 B4 config/manifest 并行改动冲突。
待办：无

## 00:49 [B4-strict-gate-probe]
-完成：完成 B4/T21 与 T24 阻塞修复：统一 probe 仅接受结构化 `ok=True`，`ApprovedManifest.add()` 与 `ManifestStore.add()` 都强制 hard gate pass 证据。
-决策：无统一 probe 时保留真实 `read/stat/read_receipt` 调用探测并阻断异常；manifest 入库边界增加中文注释并前移 hard gate 校验到内存入口，避免绕过 store。
待办：`tests/contracts/test_openviking_runtime_read_guard.py` 仍需补临时 gate fixture（超出本次 ownership）。

## 00:51 [B4-regression-fixture-subagent]
-完成：完成 B4 regression fixture 修复：`test_openviking_runtime_read_guard.py` 的 `ApprovedMaterial` fixture 现在写入真实临时 hard gate result JSON，并通过 `tmp_path` 传入 3 个用例，和 T24 入 manifest 边界一致。
-决策：仅修改测试夹具输入，不放宽 `ApprovedManifest.add` 生产约束；保留原有负例语义（hash mismatch / capability mismatch）。
待办：无

## 00:55 [b4-final-spec-conformance-reviewer]
-完成：完成 B4-final（T17-T21、T22-T24、T25A、T27）终态 conformance 审查并复跑指定合同测试集，当前范围内未发现阻塞问题。
-决策：按任务清单验收口径仅审 B4 已定义任务，不把未来任务（T10-T16/T25/T26/T28+）作为阻塞；`__pycache__` 现存不作为阻塞，仅确认 `.gitignore` 已覆盖。
待办：后续批次接线时继续保持“只通过 approved manifest capability 下游读取”和“hard gate pass 证据先于 manifest 落库”两条边界。

## 00:56 [b4-final-quality-evidence-reviewer]
-完成：完成 B4-final 复核与反例验证，确认 T21/T24 仍有可复现阻塞：统一 probe 可掩盖 read/stat/receipt 实际不可用，且 manifest 仍接受非法 gate category 的“假通过”材料。
-决策：将上述两项标记为 `BLOCKED`；不修改源码，只补充真实反例证据并要求按任务口径收紧。
待办：实现 owner 需补 T21 统一 probe 真可用性验证和 T24 gate payload 合法性约束后，再复跑同批合同测试闭环。

## 01:00 [B4-T24-gate-category]
-完成：完成 B4/T24 gate category 合法性收紧：`ApprovedManifest.add` 与 `ManifestStore.add` 现在都要求 hard gate JSON 含合法 `category` 且通过状态，`illegal_gate` 与缺 category 均拒绝。
-决策：采用显式 allowlist（`hard_gate/runtime_guards/.../ok`）并保留 pass 语义（`ok=true` 或 `status=pass|passed|ok`），同时拒绝 `fail/failed/error/blocked`。
待办：无

## 01:01 [B4-T21-openviking-client-probe]
-完成：完成 B4/T21 的 OpenVikingClient 探测加固：`probe_read_stat_receipt()` 由 callable 检查改为真实调用 `receipt/stat/read` 固定探测输入，方法存在但调用抛错将返回失败并阻断 boot。
-决策：统一使用固定 probe 常量（`runs/probe/openviking/receipt.json` 与固定 probe URI）；探测 read 返回值仅接受 `bytes` 或 `OpenVikingReadResult(ok=True)`，拒绝 `None` 和无效结构，防止“返回了对象就算成功”。
待办：无

## 01:05 [b4-final-v2-quality-evidence-reviewer]
-完成：完成 B4-final-v2 质量/证据复审（只读源码 + 反例实测）；T21/T24、禁止路径、缓存卫生点均满足本次验收要求，未发现阻塞项。
-决策：按任务口径只审 B4 已实现范围，不追加未来任务要求；对“统一 probe 未结构化成功信号”与“manifest gate 非法 payload”两类历史风险用反例脚本再次实测确认已被阻断。
待办：无

## 01:05 [b4-final-v2-spec-conformance-reviewer]
-完成：完成 B4-final-v2（T17-T21、T22-T24、T25A、T27）只读规范/一致性复审并复跑指定合同测试，当前代码状态下未发现阻塞项。
-决策：以“当前源码+当前合同测试+T27 已落盘只读证据”为最终判定依据，不采纳同日旧日志中已被后续修复覆盖的中间冲突结论。
待办：后续批次接线时继续守住“hard gate pass 先于 manifest 落库”与“下游仅 capability 读取”边界。

## 01:13 [B5-controller-implementation-subagent]
-完成：完成 B5/T10-T16，实现纯 controller 决策、单 worker 目标派发、running/ready/report-exporting 状态推进，以及阶段失败归因函数与对应单测。
-决策：保持 controller 只输出 worker id 批次计划，不引入运行时调用构造；阶段失败合并保留 collect-first 归因并传递 early_stop 标记。
待办：`tests/unit/test_workflow_workers.py` 仍引用不存在的 `worker_ids`（超出 B5 ownership，需对应 owner 处理）。

## 01:17 [B5-regression-workers-api]
-完成：修复 `worker_ids` 接口不一致，给 `src/claw_trade/workflow/workers.py` 增加兼容别名 `worker_ids()`（委托 `all_worker_ids()`），使 worker 表相关单测与 B5 扩展回归恢复通过。

## 18:46 [B11-T57-CLI]
-完成：实现 `run_control` CLI（`parse_args`/`main`）、新增 CLI 单测，并验证缺少真实依赖配置时返回非 0 且输出 BLOCKED。
-决策：CLI 只接受任务清单要求参数；运行时依赖通过 `CLAW_TRADE_OPENCLAW_RUNNER` 与 `CLAW_TRADE_OPENVIKING_BACKEND` 显式注入，缺失即阻断，不提供任何绕过成功路径。
待办：等待 T51/T51A 与 T59/T60 联调时接入真实 runner/backend 实现并做里程碑实跑。

## 17:34 [B8-Round3-T48-export-claims-fix]
-完成：完成 T48 `validate_export_claims_are_supported` 逐 material/逐 claim 绑定修复，阻断跨 material claim/sha 串配，并补齐 export_claims guard 路径与串配合同测试。
-决策：在不读取 final report 正文前提下，仅基于 `reports/export-claims.json` 与 approved materials 做双向绑定校验：每个声明 SHA 必须绑定到声明 material，每个 source claim 必须绑定到声明 material 且该 material SHA 在本条声明内。
待办：无
-决策：选择最小改动且不改 12 worker 顺序/阶段映射/StagePlan；不改 controller/runner。
待办：无

## 01:19 [b5-quality-evidence-reviewer]
-完成：完成 B5(T10-T16) 只读质量/证据审查；当前实现满足纯 controller 边界、stop point 反例、collect-first 归因与 early_stop 保留要求，未发现阻塞项。
-决策：按任务口径仅审 controller/runner/workers 与对应单测，不把 B6+ 未来任务作为本轮阻塞；将 `worker_ids()` 认定为只读兼容别名且顺序由回归测试锁定。
待办：无

## 01:19 [b5-spec-conformance-reviewer]
-完成：完成 B5（T10-T16）与 `worker_ids` 窄回归补丁只读 conformance 审查，并复跑 controller/workers/imports 单测；当前范围内未发现阻塞问题。
-决策：将 `controller.py` 中 `openviking/approved-manifest.json` 判定为失败证据路径常量而非 OpenViking 读取调用，不构成 controller 越权；`worker_ids()` 兼容别名仅做 API 向后兼容，不改变 12 worker 固定顺序与 StagePlan。
待办：无

## 02:09 [B6-Implementer]
-完成：完成 B6 的 T28/T29/T29A/T30/T31/T32/T33/T34/T35（含 `failureReason` 机器可读回传）代码接缝与 focused 回归；T34A 因未确认可用的真实 OpenViking JS/HTTP worker-tool 协议保持 BLOCKED，未引入 fake adapter。
-决策：在 OpenClaw 入口链以 `singleWorkerCommand` 正式字段透传并强制 `agent==worker_id`、`allowed_tools` fail-closed；provider request/visible tools 走同源 capture，workspace evidence 固定 `source=openclaw_workspace_loader` 且只写 path+sha；`stop_after_first_response=true` 下第一事件即停并阻断工具副作用。
待办：待人类确认并提供可验证的 OpenViking worker 工具协议后，再完成 T34A 的 `openviking.write_material` / `openviking.read_with_capability` 真实适配与 receipt 全链路验收。

## 02:17 [B6-Quality-Review]
-完成：完成 B6 只读 code quality / forbidden-path 审查，核对 AGENTS/设计文档/memory 与 `third_party/openclaw` 变更，复跑新增测试文件路径并确认可执行。
-决策：结论为“需补修”：发现 first-response 证据写入未 await 的落盘竞态风险，以及新增测试文件仍为 untracked、`No test files found` 报告不能作为通过证据；T34A 继续保持 BLOCKED 合规。
待办：先补 first-response 落盘同步与测试纳管/执行口径，再做 B6 复审闭环。

## 02:28 [B6-Implementer]
-完成：完成 B6 补修（不扩范围、不做 T34A）：first-response 证据改为 `openclaw_first_model_event + event_kind` 并补 `first-response.txt` 路径；修复 first-response 落盘竞态（返回前 await）；provider request 改为捕获 priorOnPayload 处理后的最终 payload；tool-calls 顶层改 `calls` 且记录字段对齐；raw output 改为 `raw-output.md`。
-决策：删除会被当前 vitest shard 排除的测试文件，改为 `single-worker-evidence.helpers.test.ts` 并补齐 source/event_kind、tool-call intent、payload 同源、`calls` 顶层、可等待落盘的覆盖；T34A 继续 BLOCKED，未引入 fake adapter。
待办：等待人类提供真实 OpenViking worker-tool 协议后再处理 T34A/receipt 相关闭环（本次不实现）。

## 02:41 [B6-Implementer]
-完成：完成 B6 二审类型补修（仅修 3 个新增 TS 错误）：`run.ts` 用局部 `singleWorkerCommand` 保持收窄，`attempt.ts` 对 `result` 使用 `unknown` 过渡断言，`onAgentEvent` 回调保证 `data` 非空并完整透传事件。

-决策：保持最小改动，不改行为路径；随后复跑 2 个 B6 focused vitest、`attempt-execution.cli.test.ts`、Python `openclaw_client/evidence_reader` 单测、以及指定 `NODE_OPTIONS=--max-old-space-size=8192 pnpm --dir third_party/openclaw exec tsc --noEmit --pretty false`。
待办：`tsc` 仍报 3 条既有测试类型错误（`sanitize-session-history.test.ts`、`run/attempt.test.ts` 两处），不在本次 B6 补修范围。

## 03:21 [Codex]
-完成：按用户纠偏重新参考 `claw-invest` memory 与启动脚本，启动本仓库运行时依赖服务：OpenViking server 1933 健康检查 `ok`，OpenViking MCP sidecar 1944 健康检查 `ok`，OpenClaw gateway 18789 已由 systemd 运行且 RPC health `ok`。
-决策：承认 B6/T34A 先前 BLOCKED 口径不完整；服务可启动不等于 T34A 完成，仍需把旧 `openvikingArtifact__...` MCP 工具接入当前 single-worker 正式工具可见性，并产出真实 provider-visible tools、tool calls、receipt 证据后才能闭环。
待办：由后续实现 sub-agent 继续处理 OpenViking MCP/tool mapping 与 receipt 全链路验收，不能用健康检查或日志替代 provider request / visible tools / receipt。

## 03:38 [Codex]
-完成：只读核查 `claw-invest` 的 OpenViking 接入方式，确认它采用 OpenViking HTTP server + `claw_invest.mcp.openviking_mcp` sidecar + workspace `.pi/settings.json` 挂载 MCP，worker 通过 `openvikingArtifact__ov_*` 工具读写正式材料。

## 17:05 [codex]
-完成：完成 T47+T47A：新增 PM owner guard 与结构化 PM decision 合同实现，并新增合同测试覆盖缺块/非法 JSON/多块/身份不匹配/字段改写等硬失败场景，测试全绿。
-决策：`validate_pm_owner` 严格限制为“结构化块 + ProviderEvidence 身份对齐”，不做正文推断；`validate_export_does_not_rewrite_pm` 仅读取结构化 `pm_decision` 字段并逐字段精确比较。
待办：等待 T48 的 `ExportClaimMapping` 正式对象接入当前 duck-typing 接口。

任务编号：T47、T47A
是否完成：是
修改文件：
- `src/claw_trade/guards/pm_owner.py`（新文件）
- `tests/contracts/test_pm_owner_guard.py`（新文件）
- `memory/2026-05-04.md`（追加）
关键函数/对象/接口：
- `@dataclass(frozen=True) PMDecision`
- `parse_pm_decision_block(pm_l1_text, material) -> PMDecisionResult`
- `require_pm_decision_block(pm_l1_text, material) -> tuple[PMDecision, GuardResult]`
- `compare_pm_decision_fields(left, right) -> GuardResult`
- `validate_pm_decision_identity(decision, material) -> GuardResult`
- `validate_pm_owner(pm_l1_text, evidence) -> GuardResult`
- `validate_export_does_not_rewrite_pm(decision, export_mapping) -> GuardResult`
执行命令：
- `uv run pytest tests/contracts/test_pm_owner_guard.py -q`
- `uv run pytest tests/contracts/test_pm_owner_guard.py tests/contracts/test_claims_guard.py tests/contracts/test_l1_l2_guard.py -q`
- `rg -n "direct_llm|directLLM|materializer|Materializer|mock|stub|fake|fallback|heuristic|regex.*PM|rewrite" src/claw_trade/guards/pm_owner.py tests/contracts/test_pm_owner_guard.py`
命令 exit code：
- `0`
- `0`
- `0`
关键输出：
- `14 passed in 0.09s`
- `42 passed in 0.13s`
- 关键词扫描仅命中 `rewrite` 相关函数名/测试名，未出现 `direct_llm/materializer/mock/stub/fake/fallback/heuristic` 行为实现
是否达到任务预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是（非 mock/stub）
是否存在 mock/stub/fake/fallback/direct LLM/Python materializer：否
验收证据路径：
- `src/claw_trade/guards/pm_owner.py`
- `tests/contracts/test_pm_owner_guard.py`
- `tests/contracts/test_claims_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
- `memory/2026-05-04.md`
剩余风险：
- `validate_export_does_not_rewrite_pm` 目前为 T48 前置 duck-typing 兼容层；待 T48 落地后需做一次结构对齐回归。
是否需要人类决策：否

## 17:44 [B9-T26]
-完成：完成 T26 材料批准数据流骨架，实现 approval candidate 准备与 ApprovedMaterial 构造，未接入 manifest 写入。
-决策：`l1_text` 只采信 OpenViking `read_for_receipt_verification` 内容；`raw_output_path` 仅作为证据路径保存，禁止作为正式 L1 权威来源。
待办：等待 T49A/runner 在 hard gate 证据落盘后接 approval->manifest 最终写序。

任务编号：T26
是否完成：是（达到预期）
修改文件：
- `src/claw_trade/artifacts/approval.py`（新文件）
- `tests/contracts/test_approval_candidate.py`（新文件）
- `memory/2026-05-04.md`
关键函数/对象/接口：
- `ApprovalCandidate`（`call/receipt/l1_text/l2_index/raw_output_path`）
- `ApprovalCandidateResult`（`ok/candidate/category/reason/paths`）
- `prepare_approval_candidate(call, evidence, openviking)`
- `build_approved_material_from_passed_gates(call, candidate, claims)`
执行命令：
- `uv run pytest tests/contracts/test_approval_candidate.py -q`（exit code 0）
- `uv run pytest tests/contracts/test_approval_candidate.py tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_l1_l2_guard.py -q`（exit code 0）
- `rg -n "direct_llm|directLLM|materializer|Materializer|mock|stub|fake|fallback|read_latest|latest_material|compact_read|raw_output.*l1|manifest\.add|ApprovalResult\.ok_result" src/claw_trade/artifacts/approval.py tests/contracts/test_approval_candidate.py`（exit code 0）
命令 exit code：
- 0
- 0
- 0
关键输出：
- `tests/contracts/test_approval_candidate.py`: `6 passed in 0.11s`
- 回归集合：`22 passed in 0.13s`
- 禁止路径扫描仅命中测试依赖导入：`tests.fakes.openviking_store`（生产实现文件无命中）
是否达到预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是（真实实现 candidate 准备与 material 构造逻辑，失败路径返回 result，不抛给调用者）
是否存在禁止路径：否（未引入 direct_llm/materializer/fallback；未写 manifest；未返回 `ApprovalResult.ok_result`）
验收证据路径：
- `src/claw_trade/artifacts/approval.py`
- `tests/contracts/test_approval_candidate.py`
- `tests/contracts/test_openviking_receipt_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
剩余风险：
- 本任务按 T26 边界未接入 receipt/l1_l2/claims/pm_owner 的完整批准串联与 manifest 写入，最终成功接线依赖 T49A/runner。
是否需要人类决策：否

## 17:42 [B9-T49]
-完成：完成 T49 早停类别与 guard 合并补强，新增 `EARLY_STOP_CATEGORIES`、`should_early_stop`，并让 `merge_stage_failures` 用统一早停判定；补充合同测试覆盖关键场景。
-决策：仅修改 `guards/common.py`、`workflow/runner.py` 与新增 `tests/contracts/test_guard_combination.py`，不触碰 approval/OpenClaw/OpenViking/manifest 及其他 guards，避免越界并行冲突。
待办：无

## 17:27 [B8-Round3-T48]
-完成：完成 T48 export claims guard，实现 `export-claims.json` 解析、mapping 身份校验、claim 来源可追溯校验、PM 导出字段防改写校验，并补齐合同测试。
-决策：仅信任结构化 `export-claims.json` + approved materials；不从 final report 正文做任何 claim 推断；非 `other` 声明必须提供完整来源并回溯到 `ApprovedMaterial.l1_claims` 与 `l1_sha256`。
待办：等待后续任务接线 exporter/runner（本任务按要求不接线）。

任务编号：T48
是否完成：是（达到预期）
修改文件：
- `src/claw_trade/guards/export_claims.py`（新文件）
- `tests/contracts/test_export_claims_guard.py`（新文件）
- `memory/2026-05-04.md`
关键函数/对象/接口：
- `@dataclass(frozen=True) ExportClaim`
- `@dataclass(frozen=True) ExportClaimMapping`
- `@dataclass(frozen=True) ExportClaimMappingResult`
- `parse_export_claim_mapping(path: Path) -> ExportClaimMappingResult`
- `validate_export_mapping_identity(mapping: ExportClaimMapping, state: WorkflowState) -> GuardResult`
- `validate_export_claims_are_supported(mapping: ExportClaimMapping, materials: tuple[ApprovedMaterial, ...]) -> GuardResult`
- `validate_export_pm_fields(mapping: ExportClaimMapping, pm_decision: PMDecision) -> GuardResult`
执行命令：
- `uv run pytest tests/contracts/test_export_claims_guard.py -q`
- `uv run pytest tests/contracts/test_export_claims_guard.py tests/contracts/test_pm_owner_guard.py tests/contracts/test_claims_guard.py -q`
- `rg -n "direct_llm|directLLM|materializer|Materializer|mock|stub|fake|fallback|heuristic|regex|infer|guess|read_text\(|final_report" src/claw_trade/guards/export_claims.py tests/contracts/test_export_claims_guard.py`
命令 exit code：
- `uv run pytest tests/contracts/test_export_claims_guard.py -q`：首次 1（修复后复跑为 0）
- `uv run pytest tests/contracts/test_export_claims_guard.py -q`：0
- `uv run pytest tests/contracts/test_export_claims_guard.py tests/contracts/test_pm_owner_guard.py tests/contracts/test_claims_guard.py -q`：0
- `rg -n ...`：0
关键输出：
- 单测文件：`18 passed in 0.10s`
- 联跑：`68 passed in 0.16s`
- 关键词扫描仅命中 `final_report_path` 字段与 `parse` 读取 mapping 文件，无 direct_llm/materializer/mock/stub/fake/fallback 路径
是否达到任务预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是（真实新增 guard 与合同测试）
是否存在 mock/stub/fake/fallback/direct LLM/Python materializer：否
验收证据路径：
- `src/claw_trade/guards/export_claims.py`
- `tests/contracts/test_export_claims_guard.py`
- `tests/contracts/test_pm_owner_guard.py`
- `tests/contracts/test_claims_guard.py`
- `memory/2026-05-04.md`
剩余风险：
- 当前只完成 guard 与解析合同；尚未接入 exporter/runner 的运行时调用链（按任务边界属于后续任务）。
是否需要人类决策：否

## 17:19 [codex]
-完成：完成 T47/T47A 的签名收口，`validate_export_does_not_rewrite_pm` 与其内部提取函数签名移除 `| object`，收紧为 `export_mapping: ExportClaimMapping`，运行时 duck-typing 解析逻辑保持不变。
-决策：按 T48 尚未落地的现状保留 `from __future__ import annotations` + `TYPE_CHECKING` 引用，不在运行时 import 不存在模块。
待办：无

任务编号：T47/T47A（签名收口）
是否完成：是（达到预期）
修改文件：
- `src/claw_trade/guards/pm_owner.py`
- `memory/2026-05-04.md`
关键函数/接口：
- `validate_export_does_not_rewrite_pm(decision: PMDecision, export_mapping: ExportClaimMapping) -> GuardResult`
- `_extract_export_pm_decision(export_mapping: ExportClaimMapping, baseline: PMDecision) -> PMDecision | GuardResult`
执行命令：
- `uv run pytest tests/contracts/test_pm_owner_guard.py -q`
- `uv run pytest tests/contracts/test_pm_owner_guard.py tests/contracts/test_claims_guard.py tests/contracts/test_l1_l2_guard.py -q`
- `rg -n "validate_export_does_not_rewrite_pm\\(.*object|direct_llm|directLLM|materializer|Materializer|mock|stub|fake|fallback" src/claw_trade/guards/pm_owner.py tests/contracts/test_pm_owner_guard.py`
exit code：
- 0
- 0
- 1（无匹配）
关键输出：
- `33 passed in 0.13s`
- `61 passed in 0.17s`
- `rg` 无命中（签名不再含 `object`，且未引入禁止路径关键词）
是否达到预期：是
是否偏离任务清单/详细设计：否
是否真实实现：是（真实修改函数签名，非测试替身）
是否存在禁止路径：否（未新增 mock/stub/fake/fallback/direct LLM/Python materializer）
验收证据路径：
- `src/claw_trade/guards/pm_owner.py`
- `tests/contracts/test_pm_owner_guard.py`
- `tests/contracts/test_claims_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
- `memory/2026-05-04.md`
剩余风险：
- 当前仅完成类型签名收口；`ExportClaimMapping` 运行时实体仍待 T48 实现，现阶段依赖 postponed annotations + TYPE_CHECKING。
是否需要人类决策：否

## 17:14 [Codex-T47-T47A]
-完成：完成 T47/T47A 评审修复，补齐 PM owner 合同覆盖并收紧 export/PM block 校验边界。
-决策：仅在 `pm_owner.py` 与 `test_pm_owner_guard.py` 做最小改动；坚持“只解析结构化 PM block，不从正文推断、不改写 PM 决策”。

任务编号：T47/T47A
是否完成：是（达到预期）
修改文件：
- `src/claw_trade/guards/pm_owner.py`
- `tests/contracts/test_pm_owner_guard.py`
- `memory/2026-05-04.md`
关键函数/对象/接口：
- `validate_export_does_not_rewrite_pm(...)`
- `_extract_export_pm_decision(...)`
- `_extract_pm_decision_payload(...)`
- `_looks_like_pm_json(...)`
- `validate_pm_decision_identity(...)`
执行命令：
- `uv run pytest tests/contracts/test_pm_owner_guard.py -q`
- `uv run pytest tests/contracts/test_pm_owner_guard.py tests/contracts/test_claims_guard.py tests/contracts/test_l1_l2_guard.py -q`
- `rg -n "direct_llm|directLLM|materializer|Materializer|mock|stub|fake|fallback|heuristic|regex|infer|guess|rewrite" src/claw_trade/guards/pm_owner.py tests/contracts/test_pm_owner_guard.py`
命令 exit code：
- 0
- 0
- 0
关键输出：
- `33 passed in 0.11s`
- `61 passed in 0.15s`
- `rg` 命中仅为函数/测试命名（如 `validate_export_does_not_rewrite_pm`、`*_rewrite_*`、`does_not_infer_*`），无新增 mock/stub/fake/fallback/direct LLM/Python materializer 实现路径
是否达到任务预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是（真实修改 guard 逻辑与合同测试）
是否存在 mock/stub/fake/fallback/direct LLM/Python materializer：否
验收证据路径：
- `src/claw_trade/guards/pm_owner.py`
- `tests/contracts/test_pm_owner_guard.py`
- `tests/contracts/test_claims_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
- `memory/2026-05-04.md`
剩余风险：
- 当前通过 `TYPE_CHECKING` 引用了未来 `ExportClaimMapping` 类型名；待 T48 真正落地该对象后，建议再做一次类型检查以确认与最终字段契约完全一致。
是否需要人类决策：否

## 16:45 [B8-Round1-T44-codex]
-完成：完成 T44，给 receipt guard 增加 adapter verified receipt 字段硬校验，并扩展 `MaterialReceipt` 以表达 OpenViking adapter verified 字段。
-决策：`MaterialReceipt` 仅做最小 schema 扩展（新增字段默认 `None` 保持兼容），强约束逻辑全部落在 `validate_openviking_receipt`，避免影响 T40 并行改动与非 T44 路径。
待办：无。

任务编号：T44
是否完成：是
修改文件：
- `src/claw_trade/guards/openviking_receipt.py`
- `src/claw_trade/artifacts/refs.py`
- `tests/contracts/test_openviking_receipt_guard.py`
- `memory/2026-05-04.md`
关键函数/对象/接口：
- `validate_openviking_receipt`
- `MaterialReceipt`
- `MaterialReceiptVerification`
执行命令：
- `uv run pytest tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_openviking_client_contract.py tests/contracts/test_openviking_forbidden_access.py`
- `uv run pytest tests/contracts/test_workspace_evidence_guard.py tests/contracts/test_provider_request_guard.py tests/contracts/test_visible_tools_guard.py tests/contracts/test_tool_calls_guard.py tests/contracts/test_openviking_runtime_read_guard.py tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_l1_l2_guard.py tests/contracts/test_claims_guard.py`
命令 exit code：
- `0`
- `0`
关键输出：
- `tests/contracts/test_openviking_receipt_guard.py .....`
- `16 passed`
- `85 passed`
是否达到任务预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是
是否存在 mock/stub/fake/fallback/direct LLM/Python materializer：否
验收证据路径：
- `tests/contracts/test_openviking_receipt_guard.py`
- `src/claw_trade/guards/openviking_receipt.py`
- `src/claw_trade/artifacts/refs.py`
剩余风险：
- 目前 `MaterialReceipt` 新字段默认可为空，真实强校验由 guard 执行；若后续有绕过 guard 的直接消费路径，需要在接线阶段继续防止漏检。
是否需要人类决策：否

## 16:55 [B8-Round1-T40-fix]
-完成：完成 T40「workspace evidence 路径合同兼容修复」：guard 现已兼容 OpenClaw 绝对路径（限定在 `agents_root/<worker_id>/` 下），并对 identity/skills_manifest/stage_policy 增加固定目标文件约束。
-决策：保持最小改动，只校验 OpenClaw 已落盘 evidence 的 `path/sha256` 字段，不读取 worker 正文、不引入 fallback 或额外运行时路径。
待办：无

任务编号：T40
是否完成：是（达到预期）
修改文件：
- `src/claw_trade/guards/workspace_evidence.py`
- `tests/contracts/test_workspace_evidence_guard.py`
- `memory/2026-05-04.md`
关键函数/对象/接口：
- `validate_workspace_evidence(call, evidence, agents_root)`
- `_require_sha_block(...)`
- `_resolve_worker_path(path_value, expected_worker_root, worker_id)`
- `_looks_like_windows_drive(path_value)`
- `_is_within(candidate, expected_worker_root)`
执行命令：
- `uv run pytest tests/contracts/test_workspace_evidence_guard.py -q`
- `uv run pytest tests/contracts/test_workspace_evidence_guard.py tests/contracts/test_provider_request_guard.py tests/contracts/test_visible_tools_guard.py tests/contracts/test_tool_calls_guard.py tests/contracts/test_openviking_runtime_read_guard.py tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_l1_l2_guard.py tests/contracts/test_claims_guard.py -q`
命令 exit code：
- 0
- 0
关键输出：
- `17 passed in 0.13s`
- `90 passed in 0.34s`
是否达到任务预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是
是否存在 mock/stub/fake/fallback/direct LLM/Python materializer：否
验收证据路径：
- `src/claw_trade/guards/workspace_evidence.py`
- `tests/contracts/test_workspace_evidence_guard.py`
- `tests/contracts/test_provider_request_guard.py`
- `tests/contracts/test_visible_tools_guard.py`
- `tests/contracts/test_tool_calls_guard.py`
- `tests/contracts/test_openviking_runtime_read_guard.py`
- `tests/contracts/test_openviking_receipt_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
- `tests/contracts/test_claims_guard.py`
- `memory/2026-05-04.md`
剩余风险：
- 当前 guard 只校验路径归属和 sha 字段存在性，不校验路径对应文件内容是否与 sha 真值一致（属其他链路职责）。
是否需要人类决策：否

## 16:22 [B8-F]
-完成：完成 T45/T46 最小实现：L1/L2 guard 新增 compact 与 raw_output 替代拦截，L2 index/entry 改为通过 OpenViking stat/read 校验 sha/size；claims 新增 `high_risk_claim_kinds()` 并把 `target_price` 纳入高风险声明映射检查，相关合同测试通过。
-决策：仅修改授权文件（`src/claw_trade/guards/l1_l2.py`、`src/claw_trade/artifacts/claims.py`、`tests/contracts/test_l1_l2_guard.py`、`tests/contracts/test_claims_guard.py`），未触碰 T47/T47A 相关文件以避免并发冲突。
待办：等待上游把 T45/T46 接入 approval/runner 主链路后再做集成层验证。

## 16:21 [B8-C-visible-tools]
-完成：完成 T42 visible tools guard，实现同源校验与模型可见工具名校验，并新增合同测试覆盖 hard-fail 场景。
-决策：visible tools 判定严格依赖 `visible-tools.json` 与 `provider-request.json` 真实证据；同时要求 `source=provider_request`、`provider_request_path` 对齐本次 evidence、以及 provider payload 工具名与 visible tools 工具名一致，禁止以本地 allowlist/日志替代。
待办：等待后续 T43/T49 接入 runtime guard 聚合链路。

任务编号：T42
状态：达到预期
设计引用：`docs/control详细设计实施任务清单.md` §T42；`docs/control详细设计方案.md` §10/§11.5
改动文件：
- `src/claw_trade/guards/visible_tools.py`
- `tests/contracts/test_visible_tools_guard.py`
- `memory/2026-05-04.md`
新增/修改函数：
- `validate_visible_tools(call: WorkerCall, evidence: ProviderEvidence) -> GuardResult`
- `_same_provider_request_source(...)`
- `_extract_provider_tool_names(...)`
- `_normalize_tool_names(...)`
- `_tool_name_from_entry(...)`
执行命令：
- `uv run pytest tests/contracts/test_visible_tools_guard.py`（exit 0）
- `rg -n "renderer|export|reconstruct|direct_llm|materializer|fake|stub|mock|fallback" src/claw_trade/guards/visible_tools.py tests/contracts/test_visible_tools_guard.py`（exit 1，无命中）
关键输出：
- `collected 6 items`，`6 passed`
- 禁止路径扫描无命中
真实证据路径：
- `src/claw_trade/guards/visible_tools.py`
- `tests/contracts/test_visible_tools_guard.py`
偏离设计：无
禁止路径扫描结果：未引入 `direct_llm/materializer/mock/stub/fake/fallback` 成功路径
中文注释覆盖：
- `visible_tools.py` 中已标注“只能比较模型实际可见工具名，不能用 allowlist/日志/重构文本替代 provider payload”
下一步：由后续 owner 在 runtime guard 总线中按 §11.5 顺序接入并联调

## 16:20 [B8-B]
-完成：完成 T41 provider request guard：新增 `validate_provider_request(call, evidence)`，强制读取 `provider-request.json`、校验 `source=provider_request_capture`、runtime marker 身份一致、`payload.messages/tools` 非空且结构可信，并对缺失文件/坏 JSON/身份不一致/伪来源全部 hard fail。
-决策：对 `renderer_output/export_report/logs/reconstructed_*` 显式拒绝；不引入 mock/stub/fake/fallback 成功路径，不改 runtime/workflow/third_party。
待办：等待后续 T42-T43 对 visible tools/tool calls/runtime reads 接线到统一 runtime guards。

## 16:31 [B8-B]
-完成：完成 T41 Round1 NEEDS_FIX：provider request guard 改为校验 OpenClaw 真实 `runtime_markers` 对象，要求 run/call/worker/stage/profile/openclaw_run_id 与 call/evidence 一致，并新增缺失 marker、旧字符串 marker、错误 marker 负例。
-决策：以 `third_party/openclaw/src/agents/pi-embedded-runner/run/params.ts` 和 `attempt.ts` 的实际 evidence 输出为准，不再接受仅有 `runtime_marker="openclaw_single_agent_turn"` 的旧字符串 fixture。
待办：无

## 16:01 [B7-D]
-完成：完成 T50/T50A：新增 `WorkflowStore`（run/call/evidence 目录、state/decision/call/result/failure/export 持久化）并补齐 guard/receipt/access/L1-L2/export-claim 审计落盘；新增单测覆盖核心路径并通过。
-决策：`runs/<run>/openviking` 全部按本地审计副本处理，明确注释其非正式材料权威；`save_export_claim_mapping` 先按通用可序列化对象落盘到 `reports/export-claims.json`，不引入 fake material/receipt。
待办：等待后续任务接入真实 runner/exporter/approval 时继续消费这些持久化接口并补跨模块回归。

## 16:09 [B7-D]
-完成：按 B7 review 修复 T50/T50A 证据伪造风险：`save_call` 不再预创建任何 runtime evidence 占位文件，仅创建 `calls/<call_id>/` 并写 `call.json`；单测同步改为断言 evidence 文件不存在。
-决策：本地 `evidence_dir(call)` 继续指向 `runs/<run>/calls/<call_id>`，但真实 `provider-request/visible-tools/workspace-evidence/first-response/tool-calls/raw-output/openviking-receipt` 仅允许 OpenClaw runtime 真实写出。
待办：无

## 16:13 [B7-D]
-完成：补充 T50/T50A 字段保真测试：`save_call` 后直读 `call.json` 校验 `openviking_read_capabilities[].allowed_l2_index_sha256`；`save_openclaw_result/save_decision` 后直读 JSON 校验关键字段未丢。
-决策：本轮仅改测试，不新增 `load_call/load_openclaw_result/load_decision`，保持 T50 公开接口边界不扩大。
待办：无
-决策：`claw-invest` 的实现不是纯 skill；skill 只做使用约束，真实读写来自 MCP。当前旧合同是 worker 专用业务工具和 `formal-write-receipts.json` 证据，不等价于 `claw-trade` T34A 要求的通用 `openviking.write_material/read_with_capability`。
待办：若继续 T34A，应新增或批准通用 OpenViking MCP 工具合同，不能让 OpenClaw 拼装旧 `ov_*` 业务字段冒充通用工具。

## 03:34 [T34A-openclaw-openviking-adapter]
-完成：完成 T34A 阻塞核验：已实测 sidecar `/mcp` 只暴露 `ov_*` 原始工具（非 `openviking.write_material/openviking.read_with_capability`），且写工具参数是 worker 业务字段集合，不是通用材料写接口；按边界规则判定当前协议与 T34A 设计名合同不兼容。
-决策：不在 OpenClaw 内写入 `claw-invest` 的 worker 业务参数拼装桥接（这会把业务 authority 从 claw-trade 搬进 OpenClaw）；按 `AGENTS.md` 与任务 stop 条件返回 BLOCKED。
待办：需要人类确认一种通用协议路线（二选一）：A) sidecar 增加通用 `write_material/read_with_capability` 合同；B) 明确批准 OpenClaw 直连 OpenViking HTTP API 并给出正式 endpoint/auth/receipt 合同。

## 03:58 [Codex]
-完成：把 OpenViking 参数流转设计写入 `docs/control详细设计方案.md` 和 `docs/control详细设计实施任务清单.md`，明确 worker 通过本轮可读材料清单获得 `capability_id/material_id/uri`，工具层通过 command 授权表强校验。
-决策：采用 `run_id + stage + worker_id + call_id` 隔离同一 worker 不同 stage 或重跑；写入阶段不生成 `material_id/capability_id`，批准后再生成 `material_id`，下游读取前再生成 `capability_id`。
待办：后续 T34A/T37A/T38 实现时必须产出 provider request 中的可读材料清单、真实 OpenViking read/write access evidence 和 capability 校验证据。

## 04:07 [B4-T20-tool-name-contract]
-完成：完成 T20 补修：保留 `openviking_read/openviking_write` intent 名不变，但 `resolve_tools()` 产出的 provider-visible 工具名已对齐为 `openviking.read_with_capability` 与 `openviking.write_material`，并同步更新相关合同测试断言。
-决策：只在 `tool_names.py` 做 intent->provider-visible 映射边界变更，不改 OpenClaw/workflow/runtime/agents；补一处中文注释强调该边界防止占位名泄漏到 provider-visible tools。
待办：等待 T34A 后续接线在真实 provider request/visible-tools 证据里继续验证这两个设计名端到端可见。

## 04:20 [Codex]
-完成：中断 B6/T34A 实现尝试并转为阻塞归因；sub-agent 只留下 `params.ts` 中可读材料 brief 的中间改动，未完成 T34A/T35，未跑 T34A 验收命令。
-决策：OpenViking HTTP 写入 `viking://workflow/...` 被真实服务拒绝，日志显示 `Invalid scope 'workflow'`；这与当前设计里的正式材料 URI 形状冲突，属于方向性问题，不能在 OpenClaw 里用改 URI、fake receipt 或本地替身绕过。
待办：需要人类决定：扩展/配置 OpenViking 支持 `workflow` scope，或修改设计 URI 形状为 OpenViking 当前支持的 scope 后再继续 T34A。

## 05:38 [Codex]
-完成：按人类批准口径完成 T22/T23 补修：正式材料 URI 统一为 `viking://resources/workflow/...`，同步更新 refs/client、设计文档和既有测试断言，未修改 OpenViking/OpenClaw 源码。
-决策：保持最小改动，不做 TDD 和额外反面测试；仅替换旧 `viking://workflow/...` 口径并让 `validate_viking_uri_shape()` 只接受新前缀，避免继续触发 `workflow` scope 冲突。

## 06:33 [B6/T34A-1-focused-subagent]
-完成：完成 T34A-1 focused 修复：`openviking-tools` 写后校验与 read sha 改为 OpenViking 文本规范化（CRLF->LF 且去掉末尾换行），`content.write.replace` 增加 `wait: true`，focused vitest 通过。
-决策：仅接受已确认的文本接口行尾规范化，不放宽其他内容一致性；测试预期同步为规范化后的真实读回内容与 sha。
待办：`pnpm exec tsc --noEmit` 在本机 OOM（非本次改动引入的直接类型错误证据不足），如需类型闭环建议按既有口径加大 Node heap 后复跑。

## 06:53 [B6/T34A-1-type-fix]
-完成：完成 T34A-1 helper 类型修复：`openviking.write_material` 与 `openviking.read_with_capability` 均返回标准 `AgentToolResult`，`details` 保留 receipt/read payload；`Blob` 上传改为 `Uint8Array`；对应测试改为从 `.details` 取值。
-决策：保持真实 HTTP/真实 receipt 路径，不引入 mock/stub/fake/fallback/Python materializer；`tsc` 复跑后 helper 报错已清零，仅剩既有 3 处非本任务错误。
待办：无

## 07:15 [B6-T34A-2-T35-runtime-wiring]
-完成：完成 B6/T34A-2 与必要 T35 接线：single-worker runtime 现在会挂载 `registerOpenVikingTools(runtimeContext)`，并按 `singleWorkerCommand.allowed_tools` 与 `toolsAllow` 交集收窄；tool audit 改为从 `AgentToolResult.details` 读取 OpenViking receipt/read 字段，`openvikingReceiptPath` 也从 `details.receipt_path` 获取；新增 focused helper 测试并统一相关测试 URI 到 `viking://resources/workflow/...`。
-决策：保持最小边界改动，仅改 `attempt.ts` 的工具准备与审计提取，不改 OpenViking HTTP 语义、不改 DAG/批准/hard gate/报告导出逻辑。
待办：进入 T34A/T35 二次审查。

## 07:26 [Codex]
-完成：二次验收并记录 T34A-2 小修：去掉 `openviking.write_material` 成功审计中用 `material_target.target_name` 冒充 `material_id` 的逻辑，写入阶段不再生成假的 `material_id/capability_id`。
-决策：按设计保持“写入阶段只有 target/receipt，批准后才生成 material_id”；补跑 `rg` 确认 `attempt.ts` 不再出现 `material_target.target_name`，且 OpenClaw single-worker/command 范围无旧 `viking://workflow` URI。
待办：派 T34A/T35 规格审查和质量审查 sub-agent。

## 14:26 [codex]
-完成：完成 T34A/T35 corrective implementation：真实 provider request 提交前注入 `command.upstream_materials` 材料清单；OpenViking 写入证据改为明确标注“adapter verified、非原生”并保留 write/stat/read-back/sha/size/身份字段；完整运行继续返回 `raw_output_path` 与 `openviking_receipt_path`，first response 不要求 receipt。
-决策：在 `attempt.ts` 仅补真实 prompt 路径调用 `appendOpenVikingMaterialBrief`（不改 DAG/审批/hard gate）；在 `openviking-tools.ts` 仅收紧 receipt 语义字段，不新增任何 fake/mock/fallback 成功路径。
待办：如需全量类型绿灯，需由主线 owner 处理 `third_party/openclaw` 既有 `tsc --noEmit` 三处非本次改动错误。

## 14:32 [codex]
-完成：完成 T34A/T35 小收口：将设计文档中的“OpenViking 返回真实 receipt 后”旧口径改为“adapter verified receipt（非原生）在真实 write+stat/read-back 校验后生成/返回后再进入 approval 复核”；并新增 `attempt.ts` prompt 路径的轻量 focused test（纯函数）证明会注入可读材料清单。
-决策：不引入 provider/mock/fake 测试链路，采用可导出 helper `appendSingleWorkerMaterialBriefToPrompt` 直连 `attempt.ts` 实际调用点，最小成本覆盖真实路径语义。
待办：无

## 15:11 [codex]
-完成：完成 reviewer NEEDS_FIX 的 T34A/T35 三项修复：1) `read_with_capability` 新增 L2 index SHA + entry SHA 绑定校验（不再仅 prefix）；2) `write_material` 消费并对账 `fs.stat.after_write` 返回的 size/sha（sha 存在时强校验），receipt verification 保留 write/stat/read-back 证据；3) 新增 focused 测试把 `attempt.ts` 的材料清单追加 helper 与 provider payload capture helper 串起来验证最终 payload 保留材料清单。
-决策：最小协议补齐 `SingleWorkerReadCapability.allowed_l2_index_sha256`（可选字段），并复用现有 `upstream_materials.l2_index_uri` 取 index URI，避免扩大 command 协议面；放弃重型 attempt harness 测试（依赖完整 workspace 资产），改用现有 helper 测试基座并在报告里明确边界。
待办：如需端到端证明 provider capture 文件中 `messages` 文本包含该清单，需要后续在可提供完整 single-worker workspace 资产的集成环境追加 E2E 证据。

## 15:41 [codex]
-完成：完成 quality reviewer 三项修复：1) `extractStatIntegrity` 改为仅走明确 schema 路径并按目标文件名定位 stat 节点，不再递归扫描；2) 写后 stat sha mismatch 合并为单一判断分支；3) 新增 L2 负例（index 正确但 entry.sha256 与实际内容不一致时失败）。
-决策：在不扩大协议范围前提下，stat 解析要求明确目标节点且必须识别 size；sha 字段若出现但无效则失败，避免“不确定当成功”。
待办：无

## 15:58 [B7-C-T39-subagent]
-完成：完成 T39 evidence_reader 落地：补齐 EvidenceReader/EvidenceReadResult，按 run/call evidence_dir 强制路径边界校验，full run 缺 raw_output 或 openviking_receipt 失败，first response 模式允许缺 receipt/raw_output，并在 JSON 读取失败时返回明确失败对象。
-决策：只在 `runtime/evidence_reader.py` 与 `tests/unit/test_evidence_reader.py` 做最小实现，不改 request_builder/openclaw_client/store；禁止用日志/renderer/export 重构 provider request，所有证据均从 result 指定 JSON 文件只读解析。
待办：如需联调 runner 接线（T53A）由对应任务 owner 在后续批次完成。

## 16:01 [B7-A]
-完成：完成 T37/T37A：新增 `runtime/request_builder.py`，落地 `build_request_context` 与 `build_worker_call_from_context`、`RequestBuildContext/RequestBuildResult`，并补齐对应 unit/contract 测试且通过。
-决策：将 request_builder 严格限定为“运行上下文与权限边界校验 + WorkerCall 组装”，所有失败统一返回 `config_blocked`，frontline 强制空 upstream/capabilities，下游只接受 approved manifest capability 对账结果。
待办：等待 B7 其他子任务（T38/T39/T50 系列）继续接线 runner/openclaw client/store，不在本任务范围内扩改。

## 16:09 [B7-A]
-完成：完成 T37A evidence_dir 语义修正：`WorkerCall.evidence_dir` 改为 `runs/<run>/calls/<call_id>/`，并同步 unit/contract 断言，避免与 OpenViking L2 `/evidence/` URI 语义混用。
-决策：只改 `request_builder` 与两处测试，不触碰 store/openclaw/evidence_reader，保持本地证据目录与文档一致。
待办：无

## 16:13 [B7-A]
-完成：完成 T37/T37A 的 cwd 依赖修正：`_AGENTS_ROOT` 改为基于 `request_builder.py` 文件定位仓库根再拼接 `agents/`，不再依赖进程当前工作目录。
-决策：保持接口不扩大、不引入新配置系统；新增 `monkeypatch.chdir(tmp)` 用例验证在临时 cwd 下仍能通过 workspace/stage policy 校验。
待办：无
## 16:02 [B7-B-implementer]
-完成：完成 T38 `openclaw_client` 落地，新增 `OpenClawClient.probe/run_worker`、`parse_openclaw_result`、`validate_openclaw_result_shape`，并补齐 `build_openclaw_command` 对 `allowed_l2_index_sha256` 的原样序列化与单测。
-决策：按设计把 OpenClaw 调用限制为“注入 runner seam”，client 不直接触达 provider；成功结果强制校验证据路径存在且位于本 call evidence 目录；`first_response` 模式不要求 `raw_output_path/openviking_receipt_path`，full run 必须要求。
待办：等待 T37/T37A 的 `request_builder.py` 接线后，在 runner 主流程里验证与真实 OpenClaw gateway/CLI 的端到端兼容性。

## 16:13 [B7-B-implementer]
-完成：修复 T38 evidence path 解析：result 相对路径不再依赖 cwd，统一按 `evidence_dir` 解析；新增 cwd 切换回归测试与路径逃逸负例。
-决策：保留绝对路径支持；相对路径仅允许 evidence_dir-relative 语义，`../` 逃逸会被硬拒绝，防止错误地把 `runs/...` 裸相对路径当作 cwd 基准。
待办：无

## 16:21 [B8-A]
-完成：完成 T40 workspace evidence guard，实现 OpenClaw workspace 证据 JSON 的 source/身份字段/资产 path+sha 硬校验，并补齐合同测试。
-决策：只验证 OpenClaw 已写出的 `workspace-evidence.json` 字段一致性与完整性，不做 Python 侧 worker 文案读取或内容扫描。
待办：无

## 16:20 [B8-E]
-完成：完成 T44 OpenViking receipt guard：新增 `validate_openviking_receipt`，严格比对 receipt 与 `material_target` 身份字段，并强制通过 `stat_for_receipt_verification` + `read_for_receipt_verification` 双重复核后才返回 guard 通过。
-决策：将“receipt 通过不等于 approval”固化为 guard 仅返回 `GuardResult`；任何本地 receipt 但无法被 OpenViking stat/read 复核的情况都按 `openviking_receipt` hard fail。
待办：后续由 T49A owner 将该 guard 接入 approval 主流程组合校验。

## 16:23 [B8-D]
-完成：完成 T43 tool calls 与 runtime reads guard：新增 `validate_tool_calls`/`validate_openviking_runtime_reads`，并补齐对应 contracts 测试（含 source/status、read capability 越权、write 伪造字段与写入目标校验）。
-决策：runtime read 严格以 `ApprovedManifest.capabilities_for_downstream_stage` 为权威；L1 读取强制 `uri+sha` 对齐 capability，L2 读取强制命中 `allowed_l2_prefix`，write 记录禁止伪造 `material_id/capability_id` 且必须写到 `call.material_target.l1_uri`。
待办：无

## 16:33 [B8-D]
-完成：完成 T43 Round1 review 修复记录补记；本轮修复收紧 OpenViking read/write 识别、单条 tool call status 枚举，并要求 L2 read 记录携带且匹配 `l2_index_sha256`。
-决策：仅追加 memory 记录，不再修改 T43 代码或测试文件；代码修复证据沿用本轮已通过的 `test_tool_calls_guard.py` 与 `test_openviking_runtime_read_guard.py`。
待办：无

## 16:34 [B8-F]
-完成：完成 T45/T46 Round1 review 修复记录补记；本轮修复将 compact 拦截收窄为结构化/明确标记判断，并把 L2 sha/size 校验切到 OpenViking 公开 capability read/stat API。
-决策：仅追加 memory 记录，不再修改 T45/T46 代码或测试文件；代码修复证据沿用本轮已通过的 `test_l1_l2_guard.py` 与 `test_claims_guard.py`。
待办：无

## 16:45 [B8-Round1-T40]
-完成：完成 T40「workspace evidence guard 路径归属校验补强」，将 identity/skills_manifest/stage_policy/text_assets 的路径校验从字符串 `startswith` 改为基于路径段前缀匹配并拒绝路径穿越/同名前缀绕过；新增合同测试覆盖合法 `<worker_id>/...` 与 3 类绕过样例。
-决策：保持最小改动，仅修改 `workspace_evidence.py` 与 `test_workspace_evidence_guard.py`；不读取 worker 正文，不扫描 worker 文案内容，只校验 OpenClaw 已落盘 workspace evidence 字段。
待办：无

任务编号：T40
是否完成：是（达到预期）
修改文件：
- `src/claw_trade/guards/workspace_evidence.py`
- `tests/contracts/test_workspace_evidence_guard.py`
- `memory/2026-05-04.md`
关键函数/对象/接口：
- `validate_workspace_evidence(call, evidence, agents_root)`
- `_path_matches_worker_root(path_value, expected_worker_root, worker_id)`
- `_has_prefix(parts, prefix)`
执行命令：
- `uv run pytest tests/contracts/test_workspace_evidence_guard.py`（exit code 0）
- `uv run pytest tests/contracts/test_workspace_evidence_guard.py tests/contracts/test_provider_request_guard.py tests/contracts/test_visible_tools_guard.py tests/contracts/test_tool_calls_guard.py tests/contracts/test_openviking_runtime_read_guard.py tests/contracts/test_openviking_receipt_guard.py tests/contracts/test_l1_l2_guard.py tests/contracts/test_claims_guard.py`（exit code 0）
命令 exit code：
- 0
- 0
关键输出：
- `test_workspace_evidence_guard.py`：`12 passed`
- 合同回归集合：`85 passed`
是否达到任务预期：是
是否偏离任务清单：否
是否偏离详细设计：否
是否真实实现：是（真实修改 guard 校验逻辑并由合同测试验证）
是否存在 mock/stub/fake/fallback/direct LLM/Python materializer：否
验收证据路径：
- `src/claw_trade/guards/workspace_evidence.py`
- `tests/contracts/test_workspace_evidence_guard.py`
- `tests/contracts/test_provider_request_guard.py`
- `tests/contracts/test_visible_tools_guard.py`
- `tests/contracts/test_tool_calls_guard.py`
- `tests/contracts/test_openviking_runtime_read_guard.py`
- `tests/contracts/test_openviking_receipt_guard.py`
- `tests/contracts/test_l1_l2_guard.py`
- `tests/contracts/test_claims_guard.py`
剩余风险：
- 当前 T40 仅覆盖“路径归属文本校验”；不负责校验路径指向文件是否真实存在，也不负责 sha 真值校验（这属于其他 guard/runtime 证据职责）。
是否需要人类决策：否

## 17:54 [Plato the 2nd]
-完成：仅在 request_builder/evidence_reader/visible_tools/tool_calls/workspace_evidence 五个文件补齐中文边界注释，未改任何逻辑。
-决策：注释聚焦证据边界与越权防线，明确 Python 只做校验与运行参数拼装、不得接管 worker 正文或重构 provider request。
待办：无

## 18:03 [T49-T26-followup-implementer]
-完成：完成 T49-T26-FOLLOWUP：controller 阶段失败合并改为复用共享早停规则、补齐并分组注释 EARLY_STOP_CATEGORIES、并将 approval 的 L1 read 异常转为 `openviking_receipt` 失败结果；新增对应合同测试并全部通过。
-决策：保持最小改动，不实现 T49A 主体接线；仅补 shared early-stop 一致性和 T26 骨架异常封装，避免异常逃逸或绕过 hard gate。
待办：等待后续 T49A 在现有 guard/approval 基础上做最终批准接线。

## 18:15 [Volta the 2nd]
-完成：完成 B9/T49A：在 approval 实现 receipt+L2 entry+L1/L2+claim+PM owner 全量 hard gate 组合，并在 runner 增加“先写 combined_hard_gate 通过证据、再写 manifest”的最小接线 helper，同时补齐材料批准流水线 focused tests。
-决策：保持最小改动，只改 `approval.py`、`runner.py` 和合同测试文件；approval 失败时直接拒绝且不产出 approved material，不在 approval 内写 manifest。
待办：等待后续 runner 主循环 owner 把该 helper 挂到真实 `run_single_worker` 路径。

## 18:35 [B10-T51-T55-owner]
-完成：完成 B10-T51~T55：实现 `ControlRunner` 的 boot/run/apply/fail 主循环、`run_stage_batch` collect-first 与归因报告、`run_single_worker` 的 OpenClaw→evidence→guards→approval→manifest 顺序、以及 REPORT_EXPORTING 完成前置约束；新增 runner 单测与合同测并通过。
-决策：保持边界内最小修改，未改 controller/request_builder/OpenClaw/OpenViking 源码；导出未接 B11 时统一显式失败，禁止导出阶段伪成功。
待办：无

## 18:49 [B11-T56-T56B-EXPORTER]
-完成：完成 B11 的 T56/T56A/T56B 导出实现：新增 `reports/exporter.py`，落地材料装载、报告渲染、export claim mapping、export guard 接线与三类导出文件持久化；补充 exporter 合同测试并通过。
-决策：导出阶段只读取 approved material 对应 call 目录里的 `openviking_l1.md` 审计副本并解析 PM 结构化决策；`export-result.json` 不在 exporter 内写入，保持由 runner/store 单点落盘，避免双写不一致。
待办：等待 B11 CLI 侧（T57）并行分支接线，本分支不改 CLI/runner 逻辑。

## 18:56 [COMMENT-SWEEP-CHINESE-COMMENTS]
-完成：按 COMMENT-SWEEP-CHINESE-COMMENTS 仅补控制迁移关键文件中文边界注释，覆盖 controller/runner/approval/exporter/cli/OpenClaw/OpenViking 关键防线，未改执行逻辑。
-决策：注释集中在“控制权归属、证据真实性、禁止 fallback、PM owner、防止伪成功”的非显然边界，避免机械逐函数注释。
待办：无

## 19:12 [B11-FIX-T56-T57]
-完成：完成 B11 审查修复（T56/T56A/T56B/T57）：exporter 改为只走 OpenViking 正式 URI 读取 approved L1，CLI 接入真实 exporter 并补齐 completed 输出字段，相关合同与单测已通过。
-决策：`export-result.json` 继续由 runner/store 统一写入，exporter 仅写 `final-report.md`、`export-claims.json`、`export-guard-results.json`，避免双写分叉。
待办：无

## 19:18 [T58]
-完成：完成 T58 基础结构验证与最小控制面导入验证，`src/claw_trade` 存在且 `claw_trade` 导入路径位于新 `src` 目录，控制面关键模块可导入。
-决策：严格按 T58 只读取证与最小 import 验证执行，不触发 T59/live run，不改源码。
待办：进入 T59 前按任务清单继续里程碑验证。
## 19:22 [T59-PREP]
-完成：完成 T59-PREP 只读核查：已确认本仓当前缺少可用于 `CLAW_TRADE_OPENCLAW_RUNNER` / `CLAW_TRADE_OPENVIKING_BACKEND` 的真实 Python `module:attr` 适配对象；1933/1944/18789 健康检查均返回可用。
-决策：不运行 T59 live 命令；因为在 `src/` 与 `third_party/` 中未找到真实 runner/backend 注入对象（仅有 Protocol 与 tests fake），若继续会触发假成功或越权风险。
待办：补齐最小真实缺口（OpenClaw runner 注入对象 + OpenViking backend 注入对象 + 可导入路径/依赖）后再进入 T59 正式实跑。

## 19:53 [T59-UNBLOCK-CLAW-TRADE-ADAPTERS]
-完成：新增真实 `module:attr` 适配对象：`claw_trade.runtime.openclaw_local_runner:create_default_runner` 与 `claw_trade.artifacts.openviking_backend_http:create_default_backend`，并补齐 focused tests 验证失败语义与禁用路径。
-决策：OpenClaw runner 采用 `openclaw gateway call agent.runSingleWorker --json` 真实网关调用（无本地结果重构）；OpenViking backend 采用 `fs/mkdir`+`fs/stat` 固定 URI、`content/download` 优先并在 404 时仅对 `.md/.json` 回落 `content/read`。
待办：如需 T59 live gate，下一步在真实网关运行态配置上述两个环境变量并执行单 worker first response 命令验收。

## 20:00 [T59-UNBLOCK-OPENCLAW-GATEWAY-METHOD]
-完成：在 OpenClaw gateway 新增 `agent.runSingleWorker` 专用方法并接入已有 `singleWorkerCommand` 运行通路，补齐协议 schema/validator、method 注册与最小测试，完成 T59 unblock 所需 gateway 能力。
-决策：不扩展既有 `agent` 方法；新方法直接调用 `agentCommandFromIngress(...singleWorkerCommand=...)`，并对 single-worker 证据做最小必需字段校验，校验失败一律返回 `status=failed`（不构造成功）。
待办：无

## 20:12 [codex]
-完成：完成 T59-UNBLOCK-CLAW-TRADE-ADAPTERS-FIX：收紧 OpenClaw runSingleWorker 探针误判、移除 OpenViking download->read 成功 fallback、支持 `OPENCLAW_GATEWAY_CALL_BIN` 入口配置，并补齐对应单测与回归验收。
-决策：`_probe_run_single_worker_method` 仅把“runSingleWorker 参数校验失败”视为方法存在；鉴权/连接/CLI 缺失/方法缺失与其他运行错误统一失败，避免 `missing`/`command` 关键词假阳性。
待办：无

## 20:22 [T59-UNBLOCK-OPENCLAW-GATEWAY-FIX]
-完成：完成 gateway `agent.runSingleWorker` 真实 provider request id 透传修复，移除固定 `"missing"`，并补齐成功路径 evidence 映射单测（含有/无 provider id 两种场景）。
-决策：保持 OpenClaw 通用 single-agent 边界不变，仅在 gateway 读取 runtime capture（`provider-request.json`）提取 request id；未提取到真实 id 时返回机器可读状态 `not_available`/`unavailable`，不编造 id。
待办：无

## 20:39 [T59-OPENCLAW-PROVIDER-ID-FIX]
-完成：收紧 OpenClaw gateway 的 provider request id 解析：移除正文正则抽取，仅从受限结构化键位提取，并新增“content 文本含 request_id=... 不得 captured”的反例测试；指定 vitest 验收通过。
-决策：保留已有成功路径（结构化字段如 `request_id`/`provider_request_id` 仍可 captured），同时禁止从 `source/path/hash/messages/content` 这类正文文本误提取，未命中明确字段时返回 `not_available`。
待办：无

## 21:01 [T59-OPENCLAW-BUILD-RESTART]
-完成：完成 OpenClaw gateway 最小构建并重启本地 18789，确认 dist 已包含 `agent.runSingleWorker`，health HTTP 返回 200，method existence probe 命中 `agent.runSingleWorker` 参数校验（方法已存在）。
-决策：构建采用 `pnpm --dir third_party/openclaw build gatewayWatch`；method probe 通过本机配置中的 gateway token 注入调用，避免泄露凭证；本轮仅做构建/进程与运行态验证，不改任何源码。
待办：无

## 21:05 [T59-LIVE-FIRST-RESPONSE]
-完成：执行 T59 单 worker first_response live gate（`market_analyst@frontline`）并收集失败证据；运行在 boot 阶段因 OpenViking receipt probe 缺失失败，未进入 worker call。
-决策：严格按“不改代码”执行，仅做 health 检查、脱敏命令运行、run 目录与失败分类取证；不做 fallback、不推进 T60。
待办：需人类确认 OpenViking receipt probe 预置/合同期望（`runs/probe/openviking/receipt.json`）后再重跑 T59。

## 21:15 [T59-OPENVIKING-PROBE-FIX]
-完成：修复 T59 boot 失败根因：`first_response` 启动改走 receipt-free 的真实 OpenViking namespace/stat 探针；HTTP backend 兼容目录 `stat(isDir=true)` 且不再下载目录内容；补齐 runner/backend/client 对应测试并通过。
-决策：保留完整 worker 的 receipt/read/stat 正式校验路径不变，仅在 `first_response` 启动探针去除“预置 receipt 文件”前置条件；未引入 fake receipt、mock 成功路径或 fallback 成功路径。
待办：建议在真实环境重跑 T59 live 命令确认 boot 已跨过 OpenViking probe 阶段。

## 21:23 [T59-LIVE-FIRST-RESPONSE-RERUN]
-完成：执行 T59 rerun（仅 `market_analyst@frontline`、`first_response`）并完成失败取证；首轮因 gateway URL override 缺显式凭证失败，读取本机 OpenClaw 配置 token 后重跑，次轮在 OpenViking namespace probe 返回 HTTP 500 失败，未进入 worker call。
-决策：严格遵守“只执行 T59 live gate、不改代码”，仅做 health 检查、脱敏命令执行、run 目录与日志取证；不推进 T60，不引入 fallback 或 fake 结果。
待办：需人类决策是否先清理/修复 OpenViking `viking://resources/workflow/probe` 目录幂等问题后再重跑 T59。

## 21:27 [T59-OPENVIKING-NAMESPACE-IDEMPOTENT-FIX]
-完成：完成 `ensure_namespace` 幂等修复：`mkdir` 失败后必须走 `stat` 复核，只有 URI 存在且为目录才放行；并新增 500+目录成功、500+非目录失败、500+stat失败的单测覆盖。
-决策：在 `mkdir` 失败且 `stat` 不可用/非目录时回抛原始 `mkdir` 失败，避免把 namespace 误判为可用；保持目录 `stat` 场景不触发 content download/read。
待办：建议在真实环境重跑 T59 first_response live gate，验证 OpenViking `mkdir 已存在返回 500` 场景已跨过 boot。

## 21:34 [T59-LIVE-FIRST-RESPONSE-RERUN-3]
-完成：按 T59 rerun 3 执行单 worker first_response live gate（仅 `market_analyst@frontline`），运行失败并完成取证；失败原因为 OpenClaw gateway call 在 10000ms 超时，run 状态为 failed，未推进 T60。
-决策：严格执行“只跑 T59、不改代码”，仅做 health/token 检查、脱敏实跑、run/call/evidence 路径核验；不使用 mock/stub/fake/fallback/direct LLM/Python materializer。
待办：需要人类决策是否调整 gateway timeout/运行态，再次重跑 T59 以达到里程碑通过。

## 22:46 [T59-FRESH-LIVE-VERIFICATION]
-完成：完成 T59 fresh live 验证执行：重建并重启本地 OpenClaw gateway 后，运行 `first_response + market_analyst@frontline`；fresh run 产出 provider/visible/first-response/tool-calls 等证据，但 run 最终因 `single worker runtime returned no singleWorkerEvidence` 失败。
-决策：第一次失败为 gateway 鉴权缺失，读取本机 OpenClaw 配置 token（仅本地使用）后重跑；第二次失败已进入 worker 调用并产出 fresh provider/visible/first-response 证据，按“不改代码”要求只做取证不做实现修复。
待办：需人类决策是否允许进入 T59 runtime 根因修复（singleWorkerEvidence 生成/回传链路），修复后再做 fresh 重跑验收。
