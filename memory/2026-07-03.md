# 2026-07-03

- 根因：目标机 `192.168.1.21` 再次显示“助手服务暂不可用，请稍后重试”不是 LLM/OpenClaw 进程挂了。失败报告 run `run-20260703-043101-14b97829` 的四个 frontline worker OpenClaw turn 都成功，失败发生在 `claw_request_data` 工具验收；真实错误码为 `data_need_runtime_blocked`，audit message 为 `cannot cache function 'fibonacci': no locator available for file '/opt/claw-trade/current/runtime/python-site-packages/pandas_ta/utils/_math.py'`。
- 复现：用生产服务用户 `clawtrade` 加载真实 `runtime.env` 后 import `pandas_ta` 可稳定复现上述 numba cache 错误。直接原因是生产安装脚本创建 `clawtrade` 时 `--no-create-home`，但环境里 `HOME=/home/clawtrade`，该目录不存在；`pandas_ta` 触发 numba `cache=True`，库目录不可写且用户 cache 目录不可用，于是 import 崩溃。给临时 `NUMBA_CACHE_DIR=/tmp/clawtrade-numba-cache` 后同一生产包 import 成功，证明根因。
- 修复：生产 `claw-trade-control`、`claw-trade-ui` 和 `claw-trade-control-runtime` 固定导出服务用户可写缓存目录：`XDG_CACHE_HOME=/opt/claw-trade/shared/cache/xdg`、`NUMBA_CACHE_DIR=/opt/claw-trade/shared/cache/numba`、`MPLCONFIGDIR=/opt/claw-trade/shared/cache/matplotlib`；control runtime 创建这些目录并写入 `runtime.env`，UI wrapper 启动前也确保目录存在。合同测试 `tests/unit/production/test_production_paths_contract.py` 覆盖这些生产路径。
- 目标机热修验证：安装脚本热修后重启 `claw-trade-control.service` 和 `claw-trade-ui.service`，两者 active；`runtime.env` 中三项 cache 变量存在且 `clawtrade` 可写；`pandas_ta` import 成功；真实 `run_claw_request_data` 请求 `BTC/CRYPTO` 日线返回 `ok=true`、`status=ready`、`need_satisfied=true`、`chart_status=ready`、`crypto_lens_status=partial`，没有再出现 numba 或 Matplotlib home/cache 警告。UI `/healthz` 返回 ok，`/api/ui/get-channel-status` connected，OpenClaw `18789/health` 返回 `{"ok":true,"status":"live"}`。
- 根因：目标机价格提醒“任务消失但 UI/微信都没提醒”不是行情没触发。原 alert-1 已在 `2026-07-03T19:36:31Z` 触发，价格 `62198.24` 低于 `62201`；但后端列表和右栏都过滤 `closed`，所以触发后从右侧消失。站内提醒写入了 `normal-chat` 内存消息，但首页普通聊天态轮询跳过 `getChatSession(normal-chat)`，后台消息不会自动刷新到 UI；服务重启后这类内存消息还会丢。微信没收到的根因是默认微信目标解析只读旧 `*.context-tokens.json`，目标机新 OpenClaw 微信插件实际保存 `openclaw-weixin/accounts/*.json`，导致创建提醒时落成 `notification.channel=in_app`，扫描触发时根本没有走微信发送。
- 修复：`ChannelBridge` 默认微信目标解析同时支持旧 `*.context-tokens.json` 和新账号 `*.json`，仍只在唯一候选时默认发送微信；`PriceAlertService` 列表保留 `closed`，但去重匹配只匹配 active/checking/error/paused，避免已触发旧提醒挡住重建；右栏显示 `closed` 为“已触发”且只隐藏 `deleted`；首页轮询始终读取当前 chat session，普通聊天后台站内消息能刷出来。
- 验证：本地 `uv run pytest tests/unit/ui/test_channel_bridge.py tests/unit/ui/test_price_alert_service.py tests/contracts/test_scheduled_and_alert_api_contracts.py` 88 passed；`npm test -- --run src/__tests__/right-rail.test.tsx src/__tests__/home-page.test.tsx` 81 passed；`npm run build` 通过。目标机热修后真实 Windows Chrome 146 打开 `http://192.168.1.21:5175/`：右栏显示 BTC `已触发`、条件 `CRYPTO低于 62201`、最近报价 `62198.24`、扫描结果 `已触发`，console warning/error 0。真实 Chrome 页面上下文创建不会触发的 `/alert BTC 低于 1`，落盘记录为 `channel=wechat_clawbot` 且有 target/accountId，随后删除测试提醒。再创建必触发的 `/alert BTC 低于 999999` 并立即检查，结果 `triggered=true`，`lastNotificationResult.channel=wechat_clawbot`、`sent=true`、有 messageId，随后删除测试提醒。原 alert-1 是修复前创建的，所以历史记录仍显示 `notification.channel=in_app`；新建提醒会走微信。
