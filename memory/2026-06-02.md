## 20:10 [provider_capability_fix_manager]

- 完成：按用户要求执行总管 AI 模式，先用官方/可信公开文档核查 provider 能力，再修改注册并串行跑四市场 full-chain report。四个市场全部 exit 0：CN_A `run-20260602-191542-65e47877`、US `run-20260602-192924-73829953`、HK `run-20260602-193946-9f591be7`、CRYPTO `run-20260602-195506-2b53ec3f`。
- 实现：`src/claw_trade/data_gateway/providers/plugins/cn_a/provider_matrix.py` 新增 Tushare `moneyflow/moneyflow_ths/moneyflow_ind_dc/moneyflow_ind_ths/moneyflow_cnt_ths` 与 AkShare `stock_individual_fund_flow/stock_sector_fund_flow_rank` 注册和归一；Tushare 作为 paid_data 同组优先，AkShare 作为 public fallback。
- 实现：`src/claw_trade/reports/data_pack_bridge.py` 修 US FRED 宏观序列，不再把股票 ticker 当 `series_id`，改为 `FEDFUNDS/CPIAUCSL/UNRATE/DGS10`；A 股 `sector_snapshot` 最低字段改成 `sector_name/main_net/timestamp`，避免免费源因无 `sector_code` 被误排除。
- 验证：`uv run pytest tests/contracts/test_provider_capabilities.py tests/unit/reports/test_data_pack_bridge.py -q` -> `75 passed`。
- 证据：最终汇总写入 `docs/evidence/report-fullchain-provider-capability-fix-20260602/final_report.md`；四市场报告分别在 `docs/evidence/report-fullchain-provider-capability-fix-20260602/{cn_a,us,hk,crypto}/final_report.md`；统一预取证据分别在 `runs/<run_id>/data-layer/report-prefetch.json`。
- 数据结果：CN_A Tushare `moneyflow` 已调用并落 `capital_flow` 243 行；CN_A 板块资金流仍缺，原因是 Tushare 权限不足且 AkShare/EastMoney 本轮源站 JSON/502/连接失败。US FRED 已调用真实宏观序列而非 `AAPL`，`FEDFUNDS/CPIAUCSL` 有数据，`UNRATE/DGS10` 因限流/cooldown 缺。HK Tushare HK/AkShare/Yahoo 有数据，Finnhub 403，HKEXnews 空。CRYPTO Coinglass 12 个 endpoint 被调用，10 个成功，`futures_order_book` 限流、`spot_order_book` cooldown。
- 决策：本轮没有新增 runtime guard，没有改 PM/report 决策权威，没有绕过 provider 失败；免费源失败按真实失败记录，报告仍导出是因为数据缺口被如实带入。

## 20:45 [cn-a-select-dataapi-bridge]

- 完成：新增 `src/claw_trade/data_gateway/selection_batch.py`，把 archived `selection.provider_batch` facade 接到当前统一 `DataAPI.get_data_batch()`，不恢复旧 已删除数据网关/provider store 路径；`/select` 读入口仍只读 completed warehouse-marked selection run。
- 完成：`tests/unit/selection/test_provider_batch_strategy_and_trade_date.py` 更新为当前 `cn_a_*` provider id，并新增 DataAPI bridge 单测：`consumer=select`、`universe_ref=all_a_shares`、dataset refs 转 `normalized://mongo/normalized_datasets/...`、DataAPI gap 不生成 fake rows。
- 验证：`uv run pytest tests/unit/selection/test_provider_batch_strategy_and_trade_date.py -q` -> exit 0，23 passed；`uv run pytest tests/integration/selection/test_data_job_pipeline.py -q` -> exit 0，10 passed；`uv run pytest tests/integration/selection/test_select_command_chat_flow.py -q` -> exit 0，11 passed；`uv run pytest tests/contracts/test_data_gateway_cutover.py tests/contracts/test_data_layer_module_boundaries.py tests/contracts/test_unified_dataset_names.py -q` -> exit 0，10 passed；`uv run python -m py_compile src/claw_trade/data_gateway/selection_batch.py tests/unit/selection/test_provider_batch_strategy_and_trade_date.py` -> exit 0。
- Live smoke：按 fixed runtime preflight 使用 `scripts/start-control-runtime.sh -- uv run python -m claw_trade.selection.provider_batch --selection-run-id sel-data-api-bridge-20260602 --trade-date 2026-05-26 --output-json /tmp/sel_data_api_bridge_20260602.json`；沙箱内 Mongo loopback 失败后提权重跑，runtime health 通过，命令 exit 2（预期 fail closed）。
- Live smoke 结果：旧 facade 不再 `ModuleNotFoundError`；进入统一数据层并记录 11 条 provider attempts（EastMoney daily/corporate、AkShare hist、Baostock daily/adjust/dividend、mootdx daily/corporate、Tushare anns_d/valuation）；`rows=0`，`normalized_refs=[]`。
- Remaining blocker：当前 DataAPI provider batch 层尚不能把 `universe_ref=all_a_shares` 展开为真实全市场 provider 批次；provider errors 为 `symbol_required`，因此 RL-05 仍不能宣称完成，不能生成 approved candidate pack。
