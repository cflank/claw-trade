# claw-trade 迁移设计

本文档写给在 `/home/frank/src/claw-trade` 新工作区工作的 AI。

不要把旧工作区的实现当成正确答案。旧工作区只提供证据、失败样本和反面教材。你的任务是在新工作区按新的架构边界重新完成迁移。

## 0. 先读什么

进入新工作区后，先读：

1. `AGENTS.md`
2. `迁移设计.md`
3. 旧工作区的 `/home/frank/src/claw-invest/实施计划.md`
4. 旧工作区的 `/home/frank/src/claw-invest/memory/2026-05-02.md`
5. 旧工作区的真实 prompt / response / provider payload 证据文档

如果这些文档互相冲突，优先级是：

```text
新工作区 AGENTS.md
  > 本迁移设计
  > 用户最新明确决定
  > 旧工作区证据文档
  > 旧工作区实现代码
  > 旧工作区历史计划
```

旧实现不能自动成为 truth source。

## 1. Verdict

迁移方向是：

```text
claw-trade 管 TradingAgents 工作流状态机
OpenClaw 只执行单个 agent turn
agent 配置拥有 worker prompt / skill / stage policy
上下游通过已批准 artifact 传递
Python 不假扮 worker，不替 worker 调工具，不改 PM 结论
```

OpenClaw 放在：

```text
third_party/openclaw
```

OpenClaw 不应该占据 `/home/frank/src/claw-trade` 根目录。

OpenClaw 原码原则上不改。需要改 OpenClaw 原码时，必须停下来问人。

## 2. 已确认事实

### 2.1 新工作区结构

`/home/frank/src/claw-trade` 是 claw-trade 项目根目录。

目标结构：

```text
/home/frank/src/claw-trade/
  AGENTS.md
  迁移设计.md
  README.md
  pyproject.toml
  docs/
  memory/
  scripts/
  third_party/
    openclaw/
  agents/
    market_analyst/
    fundamental_analyst/
    news_analyst/
    social_analyst/
    bull_researcher/
    bear_researcher/
    research_manager/
    trader/
    risk_challenger/
    risk_guardian/
    risk_moderator/
    portfolio_manager/
  src/claw_trade/
    app/
    config/
    workflow/
    runtime/
    artifacts/
    guards/
    reporting/
    skills/
  tests/
    contracts/
    unit/
    integration/
    live/
```

### 2.2 OpenClaw 角色

OpenClaw 是 agent runtime。

它负责：

```text
接收一次 wake
加载 agent workspace / skills
形成最终 provider prompt
暴露当前 tool schema
执行 LLM turn
处理 tool call
保留 session context
```

它不负责：

```text
决定 12 个 worker 谁先谁后
自动执行完整 TradingAgents DAG
替 claw-trade 判断 hard gate
替 PM 改投资结论
```

### 2.3 claw-trade 角色

`claw-trade` 是 workflow control plane。

它负责：

```text
创建 report run
识别 market/profile/ticker/date
维护 workflow 状态机
发布 dispatch
wake OpenClaw agent
收集 artifact
执行 hard gate
把已批准 artifact refs 暴露给下一阶段
导出 reader-facing report
```

它不负责：

```text
写 worker 业务 prompt
替 worker 调工具
把后八个 worker 实现成 Python direct LLM 函数
改写 PM rating
改写 PM final decision
```

## 3. 需要主动反驳的错误方向

新 AI 必须主动防止以下方向重新出现：

1. **把 OpenClaw 当成完整 DAG 引擎。**
   - 错：OpenClaw 配一个 workflow YAML 自动跑 12 worker。
   - 对：`claw-trade` 管状态机，OpenClaw 跑单个 agent turn。

2. **把 worker 写成 Python 函数。**
   - 错：`bull_researcher()` 拼 prompt 后直接调 provider。
   - 对：`claw-trade` 发布 dispatch，OpenClaw wake `bull_researcher` agent。

3. **Python 拼业务 prompt。**
   - 错：Python 生成一大段 worker task prose。
   - 对：prompt 属于 `agents/<worker>/` 的配置/profile。

4. **Python 替 agent 调工具。**
   - 错：Python 先查数据，再把结果塞给 worker。
   - 对：agent 挂 skill，stage/profile 暴露 tool，agent 自己调用。

5. **上下游直接塞全文。**
   - 错：Python 把上个 worker 的全文塞进下个 prompt。
   - 对：上游写 canonical artifact；下游读已批准 artifact refs。

6. **fallback 假装覆盖。**
   - 错：HK 没 prompt 就套 US；CRYPTO 没 prompt 就套股票。
   - 对：未明确批准的 profile runtime fail，不自动 fallback。

7. **用测试通过替代 runtime 证据。**
   - 错：合同测试过了就说 prompt 对齐。
   - 对：provider payload 必须抓最终发给 LLM 的 prompt 和 tool schema。

## 4. 12 Worker 目标

所有 12 个 worker 都必须是 OpenClaw first-class agent：

```text
market_analyst
fundamental_analyst
news_analyst
social_analyst
bull_researcher
bear_researcher
research_manager
trader
risk_challenger
risk_guardian
risk_moderator
portfolio_manager
```

每个 worker 至少包含：

```text
agents/<worker>/IDENTITY.md
agents/<worker>/USER.md
agents/<worker>/STAGES.yaml
agents/<worker>/prompts/US.md
agents/<worker>/prompts/CN_A.md
agents/<worker>/prompts/HK.md
agents/<worker>/prompts/CRYPTO.md
agents/<worker>/skills/
```

如果某个市场 profile 还没有批准策略：

```text
fail explicitly
不要 fallback
不要用空 prompt
不要套其它市场 prompt
```

## 5. Workflow 状态机

目标状态：

```text
created
  -> frontline_running
  -> frontline_ready
  -> investment_debate_running
  -> investment_decision_running
  -> trade_decision_running
  -> risk_debate_running
  -> risk_decision_running
  -> completed
```

目标顺序：

```text
frontline 并行:
  market_analyst
  fundamental_analyst
  news_analyst
  social_analyst

investment_debate:
  bull_researcher
  bear_researcher

investment_decision:
  research_manager

trade_decision:
  trader

risk_debate:
  risk_challenger
  risk_guardian
  risk_moderator

portfolio_decision:
  portfolio_manager
```

规则：

1. LLM 不决定下一步 worker。
2. `claw-trade` 状态机决定下一步 dispatch。
3. 每个 dispatch 对应一次 OpenClaw wake。
4. wake 是 session turn，不是常驻 sleeping worker。
5. 后八个 worker 不得走 `direct_llm`。

## 6. Prompt / Profile 设计

Prompt 属于 agent 配置，不属于 Python runtime code。

规则：

1. US prompt 对齐原版 TradingAgents。
2. CN_A prompt 对齐 TradingAgents-CN。
3. HK 必须有明确策略。
4. CRYPTO 必须有明确策略。
5. 未批准 profile 必须 runtime fail。
6. 不得自动 fallback。
7. 不得把自然语言 prompt 改写成工程化 checklist。
8. Python 只传变量：
   - ticker
   - company name
   - market
   - currency
   - date range
   - run/profile metadata

## 7. Skill / Tool 设计

Agent 挂 skill，stage/profile 控制当前 turn tool。

原则：

```text
worker 总能力 = agent mounted skills
当前可见能力 = stage/profile tool policy
最终可见 schema = provider payload 中的 tool schema
```

规则：

1. Python 不替 agent 调工具。
2. Python hardcoded allowlist 不能成为最终架构。
3. news 必须覆盖 company news 和 global/macro news。
4. market 不应暴露不属于 market stage 的工具。
5. fundamental 不应被压缩成语义不清的单个 wrapper。
6. later-stage worker 应通过 artifact read/write skill 获取材料和提交结果。

## 8. Artifact Authority

上下游通过 artifact 传递。

正确流：

```text
worker A
  -> agent turn
  -> tool calls
  -> write canonical artifact

claw-trade
  -> read artifact metadata
  -> hard gate
  -> approve artifact ref

worker B
  -> receives approved artifact refs
  -> reads needed artifacts through skill/tool
  -> writes next artifact
```

规则：

1. hard-gate-failed artifact 不得进入下一阶段。
2. artifact 必须可追踪 run/stage/worker/URI/hash 或等价 integrity marker。
3. brief 可以包含必要摘要，但不能用 Python 塞满上游全文。
4. reader export 不得暴露 provider metadata、tool schema、OpenClaw protocol 噪音。

## 9. PM Owner

PM 是最终投资结论 owner。

规则：

1. `portfolio_manager` 是 OpenClaw agent。
2. Python 不改 PM rating。
3. Python 不改 PM final decision。
4. Exporter 不新增 unsupported investment conclusion。
5. Python 可以拒收 unsafe/fabricated artifact，可以请求 rerun，可以做 materialization/export。

不迁移旧规则：

```text
Mandatory alphaear-reporter workflow requirement
```

不要把 alphaear-reporter 重新写成硬性迁移要求，除非用户重新明确批准。

## 10. Truthfulness Hard Gates

不得放松：

1. unsafe investment claim hard fail。
2. fabrication hard fail。
3. unsupported PE/PB/ROE hard fail。
4. unsupported target price hard fail。
5. fake news/source/sentiment/tool success hard fail。
6. chart/indicator missing 时必须记录 root cause。
7. mock / stub / fake / fallback 不得用于通过验收。

## 11. Provider Payload Capture

必须抓真实 provider request。

必须包含：

1. final messages 原文。
2. tool schema。
3. worker_id。
4. stage。
5. run_id。
6. dispatch_id。
7. runtime marker。
8. provider/request id，若可用。

禁止：

```text
renderer output 冒充 provider payload
exporter output 冒充 provider payload
重构 prompt 冒充 provider payload
只给摘要不保留原文
```

## 12. 四市场验证

必须覆盖：

```text
US
CN_A
HK
CRYPTO
```

每个市场必须收集：

1. 12 worker provider payload。
2. 12 worker LLM raw reply。
3. tool schema。
4. final report artifact。
5. Original/CN/Claw 对比。
6. 报告感分析。
7. 图表/指标证据。
8. hard gate 结果。

验证必须 collect-first：

```text
不要第一个市场失败就停
先跑完整个已批准 batch
收集所有失败
按根因分组
再批量修
```

允许提前停止的情况：

1. provider/runtime/root alignment 不可信。
2. 继续运行会污染证据。
3. 涉及 PM 最终裁决权、Python rewrite、OpenClaw 原码修改等需人类拍板事项。
4. destructive/data-loss/security 风险。

## 13. 第一阶段任务：Inventory

新 AI 的第一步不是写实现。

第一步是维护统一后的架构入口：

```text
docs/架构设计.md
```

架构设计中的 inventory 部分必须回答：

1. 旧工作区 12-worker 状态机在哪里。
2. 旧工作区 dispatch 发布在哪里。
3. 旧工作区 OpenClaw wake 在哪里。
4. 旧工作区 prompt 生成在哪里。
5. 旧工作区 stage/tool policy 在哪里。
6. 旧工作区 artifact 写读在哪里。
7. 旧工作区 direct LLM 在哪里。
8. 旧工作区哪些内容可以保留。
9. 旧工作区哪些内容必须重写。
10. 旧工作区哪些内容必须丢弃。

Inventory 通过人工确认后，才能进入 scaffold / implementation。

## 14. 可保留的旧资产

可以保留的是证据，不是脏实现：

1. provider payload capture 思路和证据。
2. 12 worker final prompt 原文文档。
3. Original / CN / Claw 三版本 prompt 和 LLM response 对比。
4. frontend 4 worker 差异分析。
5. news global/macro 缺口分析。
6. hard gate 失败样本。
7. artifact authority 的方向。
8. PM 最终裁决权 / Python 不改结论的边界。

## 15. 不应迁移的旧内容

不要迁移：

1. Python direct LLM report path。
2. direct LLM later-stage materializer。
3. Python hand-written worker business prompt。
4. Python hardcoded tool allowlist 作为最终架构。
5. OpenViking-specific 旧实现细节，除非重新批准。
6. 旧启动脚本假设。
7. `instrument_*` / `unified_*` 旧仓预授权特例。
8. mandatory alphaear-reporter workflow requirement。
9. mock / stub / fake / fallback。

## 16. Stop-And-Ask

遇到以下情况必须停下来问人：

1. 需要修改 OpenClaw 原码。
2. 需要改变 PM 最终裁决权。
3. 需要 Python 改写 PM 投资结论或 rating。
4. 需要保留或新增 direct LLM report path。
5. 需要 fallback prompt / fallback tool。
6. 需要 mock / stub / fake / fallback。
7. 需要放松 unsafe/fabrication/chart hard gate。
8. HK 或 CRYPTO prompt 策略不明确且影响实现。
9. provider payload capture 无法证明是真实 request。
10. worker scheduling / execution authority / retry owner / retry budget 需要改变。
11. 同一 gate category 两次 focused fix 后仍失败。

## 17. 新 AI 的工作姿态

不要迎合旧结论。

每次做架构、迁移、评审、debug 判断时，先回答：

```text
Verdict
Pushback
What I Know
What I Do Not Know
Recommendation
Stop Conditions
```

不要说“已经对齐”“差不多”“应该可以”，除非有 provider payload、artifact、runtime 或测试证据。

如果一个结论只是推断，就标成推断。

如果证据不足，就说证据不足。

## 18. Completion Definition

迁移完成必须同时满足：

1. 12 worker 都是 OpenClaw agent path。
2. 后八个没有 direct LLM report path。
3. prompt/profile 从 agent config 进入 provider payload。
4. stage/profile tool schema 在 provider payload 中可证明。
5. artifact authority 可证明。
6. PM 最终裁决权可证明。
7. hard gates 可证明。
8. US/CN_A/HK/CRYPTO fresh comparison 完成。
9. profile-off/non-report 不被 report policy 污染。
10. adversarial review 通过。

未满足任何一项，不得声称完成。
