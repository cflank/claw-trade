# claw-trade 交付与升级操作指南

这篇文档只讲一件事：怎么把 claw-trade 从发布机做成可交付的软件包，并在工控机上安装、授权、升级、回滚。

## 0. 先分清三个包

```text
普通生产包：Linux 上构建出来的 tar.gz，能安装运行，但还不是 Virbox 保护包。
Virbox 保护交付包：在普通生产包基础上，替换受保护运行时，并加密 .pyc、md、js 后重新打出的 tar.gz。
远程升级包：可以被目标机在线升级使用的 tar.gz，加了签名、manifest，并上传到七牛 stable/。
```

如果要给厂家测试或量产，默认交付 Virbox 保护交付包，不交付普通生产包。

## 0.1 最常用流程：代码改了以后重新出交付包

只改了业务代码、前端、prompt 或配置，没有更换许可 ID、DS 密码、Python/Node 运行时，目标就是重新做出一个给客户的 Virbox 保护包。

```bash
# 进入项目目录
cd /home/frank/src/claw-trade

# 看当前代码是不是你要交付的版本
git status

# 确认有初始数据包
ls -lh data/current-seed-*.tar

# 如果本次数据也更新了，先重新生成 seed；只改代码可以跳过这一步。
SEED_COLUMNAR_ROOT="$(find .runtime/factory-seeds -mindepth 2 -maxdepth 2 -type d -name normalized ! -path '*-test/normalized' | sort | tail -n 1)"
scripts/start-control-runtime.sh -- uv run python scripts/production/export_current_seed.py \
  --mongo-database claw_trade_a_share_factory_seed \
  --columnar-root "$SEED_COLUMNAR_ROOT" \
  --output data/current-seed-$(date -u +%Y%m%d).tar \
  --replace-output

# 先做普通生产包。这个只是中间产物，不直接给客户。
scripts/production/build_production_package.sh

# 再把普通包加工成 Virbox 保护交付包。这个才是给客户的包。
scripts/production/build_virbox_protected_delivery_package.sh
```

最后得到的是 D 盘上的这两个文件：

```text
/mnt/d/claw-trade-virbox/delivery-output/virbox-protected-时间/claw-trade-production-版本-时间.tar.gz
/mnt/d/claw-trade-virbox/delivery-output/virbox-protected-时间/claw-trade-production-版本-时间.tar.gz.sha256
```

交付给客户的就是这两个文件。`dist/production/` 下的普通包只是中间产物，不是最终交付包。

交付前校验：

```bash
cd /mnt/d/claw-trade-virbox/delivery-output/virbox-protected-时间
sha256sum -c claw-trade-production-版本-时间.tar.gz.sha256
```

不需要重新做 Virbox Protector 保护的情况：

```text
只改代码、prompt、前端、配置。
只改同一个许可 ID 的授权日期。
```

需要回到 3.1 重新做 4 个保护文件的情况：

```text
许可 ID 变了。
DS 密码或 API 密码变了。
Python/Node runtime 换了。
保护策略变了。
```

原因很简单：

```text
普通生产包里的 Python 代码虽然是 .pyc，不是 .py 明文，但这不等于正式加密。
agents 里的 prompt md 如果不加密，厂家拿到包后能直接看。
OpenClaw 入口和插件如果不加密，也会暴露关键逻辑。
```

## 1. 发布机准备

进入仓库：

```bash
cd /home/frank/src/claw-trade
```

看代码状态：

```bash
git status
git branch --show-current
```

确认：

```text
1. 当前分支和代码就是要交付的版本。
2. 要发布的修改已经提交或明确允许带上。
3. 前端能正常构建。
4. 数据种子已经准备好。
```

生产包里会放一个“初始数据文件”。

这个文件的作用是：新机器第一次安装后，系统可以直接恢复一批历史数据，不用从零开始下载。

打包脚本只认当前初始数据文件：

```text
data/current-seed-*.tar
```

当前仓库里有：

```text
data/current-seed-20260626.tar
```

这个包是当前默认交付数据包。除非本次交付专门生成了更新的 `data/current-seed-*.tar`，否则就用它。

它包含 A 股和加密币：

```text
CN_A：daily_bar、valuation_metric、financial_statement、financial_metric、capital_flow、news/social 等。
CRYPTO：daily_bar、valuation_metric、crypto_derivative_metric、crypto_onchain_metric、news/social 等。
```

旧的 `data/a-share-cn-required-300td-20260608.tar` 已经废弃，不再作为备用包，也不再作为打包 fallback。

所以，发布前要先检查有没有最新初始数据文件：

```bash
ls -lh data/current-seed-*.tar
```

如果这里没有文件，不要继续打包。先生成本次要交付的 `current-seed`，或者确认使用现有的 `current-seed-20260626.tar`。

### 1.1 制作新的数据种子

这个动作不是重新下载数据。

它做的是：把当前机器上已经存在的数据打成一个新 seed。

当前历史数据分两部分：

```text
1. Parquet 文件：真正的历史行情、估值、财务、新闻、加密币指标等数据。
2. Mongo 元数据：告诉系统这些 Parquet 文件是什么市场、什么数据集、什么时间范围、文件在哪里。
```

所以制作新 seed 时，必须同时导出这两部分。

`.runtime/dev-services/runtime.env` 不是仓库自带文件。它只在本地固定 runtime 运行期间生成；如果由启动脚本拉起服务，脚本退出时还会清掉它。

默认做法是直接用固定 runtime 包住导出命令：

```bash
SEED_COLUMNAR_ROOT="$(find .runtime/factory-seeds -mindepth 2 -maxdepth 2 -type d -name normalized ! -path '*-test/normalized' | sort | tail -n 1)"
test -n "$SEED_COLUMNAR_ROOT"
echo "using seed columnar root: $SEED_COLUMNAR_ROOT"

scripts/start-control-runtime.sh -- uv run python scripts/production/export_current_seed.py \
  --mongo-database claw_trade_a_share_factory_seed \
  --columnar-root "$SEED_COLUMNAR_ROOT" \
  --output data/current-seed-$(date -u +%Y%m%d).tar
```

如果同名文件已经存在，并且确认要覆盖：

```bash
SEED_COLUMNAR_ROOT="$(find .runtime/factory-seeds -mindepth 2 -maxdepth 2 -type d -name normalized ! -path '*-test/normalized' | sort | tail -n 1)"
test -n "$SEED_COLUMNAR_ROOT"
echo "using seed columnar root: $SEED_COLUMNAR_ROOT"

scripts/start-control-runtime.sh -- uv run python scripts/production/export_current_seed.py \
  --mongo-database claw_trade_a_share_factory_seed \
  --columnar-root "$SEED_COLUMNAR_ROOT" \
  --output data/current-seed-$(date -u +%Y%m%d).tar \
  --replace-output
```

不要省略 `--mongo-database` 和 `--columnar-root`。默认运行时数据根是 `.runtime/dev-services/data-gateway/normalized`，那是增量运行数据，不是干净 factory seed；直接导出它可能因为 A 股日线缺 `company_name` 被完整性检查拒绝。

只有在另一个终端已经启动了本地 runtime，并且 `.runtime/dev-services/runtime.env` 正在存在时，才需要手动加：

```bash
SEED_COLUMNAR_ROOT="$(find .runtime/factory-seeds -mindepth 2 -maxdepth 2 -type d -name normalized ! -path '*-test/normalized' | sort | tail -n 1)"
test -n "$SEED_COLUMNAR_ROOT"
echo "using seed columnar root: $SEED_COLUMNAR_ROOT"

uv run python scripts/production/export_current_seed.py \
  --env-file .runtime/dev-services/runtime.env \
  --mongo-database claw_trade_a_share_factory_seed \
  --columnar-root "$SEED_COLUMNAR_ROOT" \
  --output data/current-seed-$(date -u +%Y%m%d).tar
```

脚本成功后会生成两个文件：

```text
data/current-seed-YYYYMMDD.tar
data/current-seed-YYYYMMDD.tar.sha256
```

这个 `tar` 里面必须有：

```text
seed_manifest.json
mongo/raw_payloads.jsonl
mongo/provider_attempts.jsonl
mongo/dataset_manifests.jsonl
data/normalized/**/*.parquet
```

说人话：

```text
seed_manifest.json：这个 seed 的目录。
mongo/*.jsonl：数据库元数据。
data/normalized/**/*.parquet：真正的数据文件。
```

检查：

```bash
SEED="current-seed-$(date -u +%Y%m%d).tar"
tar -tf "data/${SEED}" | head
(cd data && sha256sum -c "${SEED}.sha256")
```

如果脚本报错 `missing Parquet`，不要继续打包。

这表示 Mongo 里记录了某个数据文件，但当前 Parquet 目录里找不到它。先修数据目录或重新跑数据导入，再重新制作 seed。

## 2. 制作普通生产包

执行：

```bash
scripts/production/build_production_package.sh
```

脚本会做这些事：

```text
1. 构建前端页面。
2. 复制 agents、openclaw_plugins、OpenClaw 运行文件。
3. 复制 Python 运行环境和依赖。
4. 把 src/claw_trade 编译成 .pyc，放进 app/python/claw_trade。
5. 复制数据种子。
6. 复制本地 MongoDB 运行时，如果 .runtime/mongodb/current 存在。
7. 执行生产包检查。
8. 生成 tar.gz 和 sha256。
```

成功后会得到类似：

```text
dist/production/claw-trade-production-0.1.4-20260629T114637Z.tar.gz
dist/production/claw-trade-production-0.1.4-20260629T114637Z.tar.gz.sha256
```

这里的 `dist/production` 是 WSL 里的仓库目录，不是 D 盘目录：

```text
WSL 路径：/home/frank/src/claw-trade/dist/production/
Windows 里看：\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\dist\production\
```

包名里的版本来自：

```text
pyproject.toml
```

包名里的时间是构建时间。

检查普通包：

```bash
PKG="claw-trade-production-版本-时间.tar.gz"
python3.12 scripts/production/validate_production_archive.py "dist/production/${PKG}"
python3.12 scripts/production/audit_production_package.py "dist/production/${PKG}"
(cd dist/production && sha256sum -c "${PKG}.sha256")
```

这一步通过，只说明普通包结构合格。它还不是给厂家的保护包。

## 3. 制作 Virbox 保护交付包

实际流程是：

```text
普通生产包
-> 解包到 D 盘中转目录
-> 替换受保护 Python 和 Node
-> 用 DSProtector 加密 .pyc、OpenClaw 入口、插件 js、agents md
-> 修启动入口
-> 检查没有明文和 .ssp
-> 重新打 tar.gz
-> 上传目标 Ubuntu 真机安装验证
```

做完以后，厂家拿到的是：

```text
1. Python 解释器被 Virbox 保护。
2. Node 运行时被 Virbox 保护。
3. claw-trade Python 应用 .pyc 被 DS 加密。
4. agents 里的 prompt md 被 DS 加密。
5. OpenClaw 入口和插件 js 被 DS 加密。
6. .ssp 配置文件不进交付包。
```

### 3.1 准备 4 个保护文件

这 4 个文件不是 `claw-trade` 仓库生成的，是发布机上提前用
Windows Virbox 工具做出来的保护运行时和 DS 配置。

这一步只需要在保护配置变更、Python/Node 运行时变更、许可 ID 变更、
DS 密码变更时重新做。普通业务代码改动后，不需要重新生成这 4 个文件，
后面直接跑保护包脚本即可。

先确认当前 Virbox 保护参数：

```text
产品/许可：claw-trade 生产测试许可
许可 ID：16427
许可类型：软锁-本地许可
平台：Linux x86_64
DS：打开
DSProtector 配置：分别导出 python.ssp 和 node.ssp
```

这个许可 ID 要同时出现在三个地方：

```text
1. Virbox Protector 保护 Python 时的配置。
2. Virbox Protector 保护 Node 时的配置。
3. 目标机运行时配置 VIRBOX_LICENSE_ID。
```

如果许可 ID 换了，只改目标机 `VIRBOX_LICENSE_ID` 不够。Python、Node 都要用新许可 ID 重新保护，并重新生成 `python.ssp`、`node.ssp`。

保护配置记录名建议：

```text
claw-trade-python-linux-x86_64-ds-16427
claw-trade-node-linux-x86_64-ds-16427
```

API 密码、DS 密码、账号密码、完整授权码都只进 Virbox 工具或安全凭据库，不写进文档、不进 git、不放交付包。

不要依赖 Virbox GUI 一定有“模板”功能。不同版本可能叫“项目”“保护方案”，也可能只是一次性保护配置。按输入文件、输出位置、许可 ID、平台、DS 配置这几个字段核对即可。

#### 3.1.1 先准备普通生产包

先在 Linux/WSL 里打普通生产包：

```bash
scripts/production/build_production_package.sh
```

普通包里一定有：

```text
runtime/python/bin/python
runtime/openclaw/openclaw.mjs
```

Node 运行时要用 Linux x86_64 的 Node，版本必须满足 OpenClaw 要求。
当前 OpenClaw 入口要求 Node.js 22.12+。

如果普通包里没有 `runtime/node`，就从构建机准备一个 Node 22.12+
Linux 二进制作为 Virbox 输入。后面的保护包脚本会把保护后的
受保护 Node 放进交付包的 `runtime/node`。

检查构建机 Node：

```bash
node --version
command -v node
```

#### 3.1.2 做 Python 保护文件

在 Win11 上准备目录，例如：

```text
D:\claw-trade-virbox\resource-test\protect-python-ds\
```

从普通生产包里取这个文件：

```text
claw-trade-production-版本-时间/runtime/python/bin/python
```

把它作为 Virbox Protector 的待保护 Linux 程序。

在 Virbox Protector 里配置一次 Python 保护：

```text
配置记录名：claw-trade-python-linux-x86_64-ds-16427
输入文件：D:\claw-trade-virbox\resource-test\protect-python-ds\python
输出目录：D:\claw-trade-virbox\resource-test\protect-python-ds\protected
许可类型：软锁-本地许可
许可 ID：16427
平台：Linux x86_64
DS：打开
DS 配置输出：D:\claw-trade-virbox\resource-test\protect-python-ds\python.ssp
导入表保护：不选
压缩：不选
资源保护：不选
```

操作顺序：

```text
1. 从普通生产包复制 runtime/python/bin/python 到输入文件路径。
2. 在 Virbox Protector 里选择这个 Linux x86_64 程序。
3. 选择软锁-本地许可。
4. 填许可 ID：16427。
5. 打开 DS。
6. 填 API 密码和 DS 密码。
7. 如果 Virbox 支持保存项目或方案，就保存；不支持就记录本段参数。
8. 执行保护。
9. 确认生成 protected/python 和 python.ssp。
```

执行保护后，必须得到：

```text
D:\claw-trade-virbox\resource-test\protect-python-ds\protected\python
D:\claw-trade-virbox\resource-test\protect-python-ds\python.ssp
```

含义：

```text
protected/python：交付包里真正运行 Python 代码的解释器。
python.ssp：后面用 DSProtector 加密 .pyc 的配置文件。
```

#### 3.1.3 做 Node 保护文件

在 Win11 上准备目录，例如：

```text
D:\claw-trade-virbox\node-ds-test\
```

把 Linux Node 二进制作为 Virbox Protector 的待保护程序。

如果是在 WSL 里拿当前 Node，可以先复制出来：

```bash
mkdir -p /mnt/d/claw-trade-virbox/node-ds-test/input
cp "$(command -v node)" /mnt/d/claw-trade-virbox/node-ds-test/input/node
```

然后在 Virbox Protector 里配置一次 Node 保护。配置和 Python 保持同一套产品许可：

```text
配置记录名：claw-trade-node-linux-x86_64-ds-16427
输入文件：D:\claw-trade-virbox\node-ds-test\input\node
输出文件：D:\claw-trade-virbox\node-ds-test\input\protected\node
许可类型：软锁-本地许可
许可 ID：16427
平台：Linux x86_64
DS：打开
DS 配置输出：D:\claw-trade-virbox\node-ds-test\input\node.ssp
```

操作顺序：

```text
1. 从 WSL 复制 Linux Node 到 input/node。
2. 在 Virbox Protector 里选择这个 Linux x86_64 程序。
3. 选择软锁-本地许可。
4. 填许可 ID：16427。
5. 打开 DS。
6. 填 API 密码和 DS 密码。
7. 如果 Virbox 支持保存项目或方案，就保存；不支持就记录本段参数。
8. 执行保护。
9. 确认生成 input/protected/node 和 input/node.ssp。
```

执行保护后，必须得到：

```text
D:\claw-trade-virbox\node-ds-test\input\protected\node
D:\claw-trade-virbox\node-ds-test\input\node.ssp
```

含义：

```text
input/protected/node：交付包里真正运行 OpenClaw 的 Node。
node.ssp：后面用 DSProtector 加密 openclaw.mjs、插件 js、agents md 的配置文件。
```

#### 3.1.4 最终检查

WSL 里检查 4 个文件：

```bash
ls -lh \
  /mnt/d/claw-trade-virbox/resource-test/protect-python-ds/protected/python \
  /mnt/d/claw-trade-virbox/resource-test/protect-python-ds/python.ssp \
  /mnt/d/claw-trade-virbox/node-ds-test/input/protected/node \
  /mnt/d/claw-trade-virbox/node-ds-test/input/node.ssp
```

规则：

```text
protected/python 和 input/protected/node 会进最终交付包。
python.ssp 和 node.ssp 只留在发布机，不进 git，不进交付包，不给客户。
```

### 3.1.5 推荐：用脚本制作保护包

有了上面 4 个文件以后，不需要一条条执行 3.2 到 3.10。

第一次先复制参数文件：

```bash
mkdir -p .runtime
cp scripts/production/virbox-protected-package.env.example .runtime/virbox-protected-package.env
```

然后只改这个文件：

```bash
vim .runtime/virbox-protected-package.env
```

默认参数已经按当前验证路径写好。通常只需要确认这些值：

```text
PROTECTED_PYTHON
PYTHON_SSP
PROTECTED_NODE
NODE_SSP
DSPROTECTOR
OUTPUT_DIR
```

`INPUT_ARCHIVE` 可以留空。留空时脚本自动选择 WSL 仓库目录里的最新普通生产包：

```text
/home/frank/src/claw-trade/dist/production/claw-trade-production-*.tar.gz
```

Windows 里看到的是：

```text
\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\dist\production\claw-trade-production-*.tar.gz
```

这不是 `D:\...`，也不是 `/mnt/d/...`。如果普通生产包已经被你放到 D 盘，就显式传：

```bash
scripts/production/build_virbox_protected_delivery_package.sh \
  --input /mnt/d/你的目录/claw-trade-production-版本-时间.tar.gz
```

脚本流程：

```text
1. 从 WSL 的 dist/production 读取普通包。
2. 解到 WSL 的 .runtime/virbox-delivery-build。
3. 把要加密的 .pyc、.md、.js 临时放到 D 盘给 Windows DSProtector 处理。
4. 把加密结果拷回 WSL 解包目录。
5. 重新打 Virbox 保护包，输出到 D 盘 delivery-output。
6. 默认删除 WSL 解包目录和 D 盘临时加密目录。
```

这里没有用 `scp`，也没有网络上传。`/mnt/d/...` 是 Win11 本机 D 盘在 WSL 里的路径。

默认不会把 1.8G 普通包复制到 D 盘再解开。最终 1.8G 保护包是由 `tar` 直接写到 `/mnt/d/.../delivery-output/`，所以慢的是 WSL 写 Windows D 盘，不是 `scp`。

要保留解包目录排查问题时，加 `--keep-work`：

```bash
scripts/production/build_virbox_protected_delivery_package.sh --keep-work
```

以后制作保护包只运行：

```bash
scripts/production/build_virbox_protected_delivery_package.sh
```

校验脚本生成的 `.sha256` 时，要在输出目录里执行。`.sha256` 里面写的是相对文件名：

```bash
cd /mnt/d/claw-trade-virbox/delivery-output/virbox-protected-时间
sha256sum -c claw-trade-production-版本-时间.tar.gz.sha256
cd /home/frank/src/claw-trade
```

如果想用另一个参数文件：

```bash
scripts/production/build_virbox_protected_delivery_package.sh \
  --config /path/to/virbox-protected-package.env
```

脚本会做这些事：

```text
1. 解开普通生产包。
2. 替换受保护 Python 和 Node。
3. 用 python.ssp 加密 app/python/claw_trade 下的 .pyc。
4. 用 node.ssp 加密 OpenClaw 入口、agents md、OpenClaw 插件 js。
5. 创建 runtime/openclaw-protected。
6. 修启动脚本，让 UI/control 走受保护 OpenClaw。
7. 检查 .ssp 没进包。
8. 重新打 tar.gz 和 sha256。
```

后面的 3.2 到 3.10 是手工排查路径；正常交付优先用这个脚本。
如果你临时只想覆盖某个参数，也可以传 `--input`、`--output-dir` 等命令行参数；
日常不要这么用，太容易输错。

### 3.2 解开普通生产包

设置变量：

```bash
REL_NAME="claw-trade-production-版本-时间"
# WSL 仓库内路径：/home/frank/src/claw-trade/dist/production/${REL_NAME}.tar.gz
PKG="dist/production/${REL_NAME}.tar.gz"
# D 盘中转目录，Windows 里对应 D:\claw-trade-virbox\delivery-build\...
WORK="/mnt/d/claw-trade-virbox/delivery-build/${REL_NAME}"
RELEASE="${WORK}/${REL_NAME}"
```

解包：

```bash
rm -rf "$WORK"
mkdir -p "$WORK"
tar -xzf "$PKG" -C "$WORK"
```

确认关键文件存在：

```bash
ls -lh "$RELEASE/runtime/python/bin/python" "$RELEASE/runtime/node"
ls -lh "$RELEASE/bin/claw-trade-ui" "$RELEASE/bin/claw-trade-control"
```

### 3.3 替换 Python 和 Node

替换 Python：

```bash
cp /mnt/d/claw-trade-virbox/resource-test/protect-python-ds/protected/python \
  "$RELEASE/runtime/python/bin/python"
chmod 0755 "$RELEASE/runtime/python/bin/python"
```

替换 Node：

```bash
cp /mnt/d/claw-trade-virbox/node-ds-test/input/protected/node \
  "$RELEASE/runtime/node"
chmod 0755 "$RELEASE/runtime/node"
```

### 3.4 加密 Python .pyc

不要直接在 release 目录里边加密边覆盖。先输出到临时目录，全部成功后再覆盖。

Windows 版 DSProtector 实测不能稳定读取 `\\wsl.localhost\...` 这类 WSL UNC 路径。手工排查时，`WORK`、输入临时目录、输出临时目录都放在 `/mnt/d/...` 下，让 DSProtector 读取 Windows 盘路径。

```bash
DSP="/mnt/d/sw/senseshield/sdk/Tool/VirboxProtect/bin/dsprotector_con.exe"
PY_SSP="/mnt/d/claw-trade-virbox/resource-test/protect-python-ds/python.ssp"
PY_IN="${WORK}/_ds_python_in"
PY_TMP="${WORK}/_ds_python_out"

rm -rf "$PY_IN" "$PY_TMP"
mkdir -p "$PY_IN" "$PY_TMP"
```

加密范围：

```text
app/python/claw_trade/**/*.pyc
```

执行：

```bash
mkdir -p "$PY_IN/app/python"
cp -a "$RELEASE/app/python/claw_trade" "$PY_IN/app/python/claw_trade"
"$DSP" "$(wslpath -w "$PY_IN")" -c "$(wslpath -w "$PY_SSP")" -o "$(wslpath -w "$PY_TMP")" < /dev/null
```

覆盖回 release：

```bash
cp -a "$PY_TMP/app/python/claw_trade/." "$RELEASE/app/python/claw_trade/"
```

### 3.5 加密 OpenClaw 和 prompt 资源

```bash
NODE_SSP="/mnt/d/claw-trade-virbox/node-ds-test/input/node.ssp"
NODE_IN="${WORK}/_ds_node_in"
NODE_TMP="${WORK}/_ds_node_out"

rm -rf "$NODE_IN" "$NODE_TMP"
mkdir -p "$NODE_IN" "$NODE_TMP"
```

只加密：

```text
third_party/openclaw/openclaw.mjs
openclaw_plugins/**/*.js
agents/**/*.md
```

不要加密：

```text
agents/**/*.yaml
node_modules
package.json
普通配置文件
```

执行：

```bash
mkdir -p "$NODE_IN/third_party/openclaw"
cp -a "$RELEASE/third_party/openclaw/openclaw.mjs" "$NODE_IN/third_party/openclaw/openclaw.mjs"

find "$RELEASE/openclaw_plugins" -type f -name '*.js' | while read -r SRC; do
  REL="${SRC#"$RELEASE"/}"
  OUT="${NODE_IN}/${REL}"
  mkdir -p "$(dirname "$OUT")"
  cp -a "$SRC" "$OUT"
done

find "$RELEASE/agents" -type f -name '*.md' | while read -r SRC; do
  REL="${SRC#"$RELEASE"/}"
  OUT="${NODE_IN}/${REL}"
  mkdir -p "$(dirname "$OUT")"
  cp -a "$SRC" "$OUT"
done

"$DSP" "$(wslpath -w "$NODE_IN")" -c "$(wslpath -w "$NODE_SSP")" -o "$(wslpath -w "$NODE_TMP")" < /dev/null
```

覆盖回 release：

```bash
cp -a "$NODE_TMP/third_party/openclaw/openclaw.mjs" "$RELEASE/third_party/openclaw/openclaw.mjs"
cp -a "$NODE_TMP/openclaw_plugins/." "$RELEASE/openclaw_plugins/"
cp -a "$NODE_TMP/agents/." "$RELEASE/agents/"
```

### 3.6 创建受保护 OpenClaw 入口

```bash
cat > "$RELEASE/runtime/openclaw-protected" <<'SH'
#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT_DIR/runtime/node" "$ROOT_DIR/third_party/openclaw/openclaw.mjs" "$@"
SH
chmod 0755 "$RELEASE/runtime/openclaw-protected"
```

启动脚本要走：

```text
runtime/openclaw-protected
```

不要再直接运行：

```text
third_party/openclaw/openclaw.mjs
```

### 3.7 检查启动脚本

`bin/claw-trade-ui` 和 `bin/claw-trade-control` 要把包内 `runtime` 放进 `PATH`：

```bash
grep -n 'PATH=.*/runtime' "$RELEASE/bin/claw-trade-ui" "$RELEASE/bin/claw-trade-control"
```

OpenClaw 启动命令要指向受保护入口：

```bash
grep -n 'OPENCLAW_GATEWAY_CALL_BIN.*openclaw-protected' \
  "$RELEASE/bin/claw-trade-ui" \
  "$RELEASE/bin/claw-trade-control" \
  "$RELEASE/runtime/claw-trade-control-runtime"
```

control runtime 要用包内受保护 Python，不要用系统 `python3.12`：

```bash
grep -n 'runtime/python/bin/python' "$RELEASE/runtime/claw-trade-control-runtime"
```

### 3.8 检查数据种子校验文件

如果包里有：

```text
data/current-seed-*.tar
```

就必须有：

```text
data/current-seed-*.tar.sha256
```

检查：

```bash
ls -lh "$RELEASE"/data/current-seed-*.tar "$RELEASE"/data/current-seed-*.tar.sha256
```

如果缺 `.sha256`：

```bash
cd "$RELEASE/data"
sha256sum current-seed-*.tar > "$(ls current-seed-*.tar | head -n 1).sha256"
cd /home/frank/src/claw-trade
```

### 3.9 检查保护结果

检查 prompt md 是否还露明文：

```bash
file "$RELEASE/agents/market_analyst/AGENTS.md"
grep -a 'TradingAgents' "$RELEASE/agents/market_analyst/AGENTS.md" || true
```

合格结果：

```text
file 显示 data。
grep 找不到明文关键字。
```

检查 OpenClaw 入口：

```bash
file "$RELEASE/third_party/openclaw/openclaw.mjs"
grep -a 'openclaw' "$RELEASE/third_party/openclaw/openclaw.mjs" || true
```

检查 `.ssp` 没进交付包：

```bash
find "$RELEASE" -name '*.ssp' -print
```

这条命令应该没有输出。

### 3.10 重新打保护包

顶层目录名必须保持：

```text
claw-trade-production-版本-时间
```

重新打包：

```bash
OUT_DIR="/mnt/d/claw-trade-virbox/delivery-output/virbox-protected-时间"
mkdir -p "$OUT_DIR"
tar --dereference --hard-dereference -C "$WORK" -czf "$OUT_DIR/${REL_NAME}.tar.gz" "$REL_NAME"
```

生成 sha256：

```bash
cd "$OUT_DIR"
sha256sum "${REL_NAME}.tar.gz" > "${REL_NAME}.tar.gz.sha256"
sha256sum -c "${REL_NAME}.tar.gz.sha256"
cd /home/frank/src/claw-trade
```

本次已经验证通过的样例：

```text
D:\claw-trade-virbox\delivery-output\virbox-protected-20260629T114637Z\claw-trade-production-0.1.4-20260629T114637Z.tar.gz
SHA256: 41551663362c38e0be502533d9f051802d5ab4735f5eb4a589f361b6af32a743
目标机：192.168.1.21
```

## 4. 新 Ubuntu 机器首次安装

假设目标机：

```text
IP：192.168.1.21
用户：frank
密码：目标机自己的登录密码
```

每台机器准备一个自己的授权文件：

```text
license.key
```

文件内容只放这一台机器要用的授权码，例如：

```text
NWFY-XXXX-XXXX-XXXX
```

注意：

```text
license.key 不放进软件包。
license.key 不提交 git。
license.key 不给多台机器共用。
一台机器一个 license.key。
```

### 4.1 传文件到目标机

现在用七牛时，按这个流程走。大包不要用 `scp`。

在七牛控制台上传这两个文件：

```text
claw-trade-production-版本-时间.tar.gz
claw-trade-production-版本-时间.tar.gz.sha256
```

七牛对象路径建议填：

```text
delivery/claw-trade-production-版本-时间.tar.gz
delivery/claw-trade-production-版本-时间.tar.gz.sha256
```

下载域名分两种：

```text
临时交付：用七牛默认域名，例如 thaef8z8g.hn-bkt.clouddn.com。
长期交付：用已备案的自有域名，例如 download.cflank-trade.top。
```

七牛默认域名可能被回收，只能当临时地址。长期稳定下载不要依赖默认域名。

如果要用 `download.cflank-trade.top`：

```text
1. 先完成 ICP 备案。
2. 在七牛添加自定义域名：download.cflank-trade.top。
3. 复制七牛给的 CNAME。
4. 在 NameSilo 给 cflank-trade.top 添加 CNAME：download -> 七牛 CNAME。
```

检查解析：

```bash
dig +short download.cflank-trade.top CNAME
```

目标机下载。临时用七牛默认域名时：

```bash
cd /tmp
curl -fL -o claw-trade-production-版本-时间.tar.gz "http://thaef8z8g.hn-bkt.clouddn.com/delivery/claw-trade-production-版本-时间.tar.gz"
curl -fL -o claw-trade-production-版本-时间.tar.gz.sha256 "http://thaef8z8g.hn-bkt.clouddn.com/delivery/claw-trade-production-版本-时间.tar.gz.sha256"
sha256sum -c claw-trade-production-版本-时间.tar.gz.sha256
```

长期用自有域名时：

```bash
cd /tmp
curl -fL -o claw-trade-production-版本-时间.tar.gz "http://download.cflank-trade.top/delivery/claw-trade-production-版本-时间.tar.gz"
curl -fL -o claw-trade-production-版本-时间.tar.gz.sha256 "http://download.cflank-trade.top/delivery/claw-trade-production-版本-时间.tar.gz.sha256"
sha256sum -c claw-trade-production-版本-时间.tar.gz.sha256
```

规则：

```text
license.key 不上传云端。
安装脚本可以 scp 传，没必要放云端。
首次安装用 delivery/ 路径。
远程自动升级才用 stable/ 路径和 manifest。
```

`scp` 只传小文件：

```bash
scp scripts/production/install_factory_test_ubuntu.sh frank@192.168.1.21:/tmp/
scp scripts/production/uninstall_factory_test_ubuntu.sh frank@192.168.1.21:/tmp/
scp license.key frank@192.168.1.21:/tmp/license.key
```

不用七牛时，才在发布机开临时 HTTP：

```bash
cd /mnt/d/claw-trade-virbox/delivery-output/virbox-protected-时间
python3 -m http.server 18080 --bind 0.0.0.0
```

目标机下载：

```bash
cd /tmp
curl -fLO http://发布机IP:18080/claw-trade-production-版本-时间.tar.gz
curl -fLO http://发布机IP:18080/claw-trade-production-版本-时间.tar.gz.sha256
sha256sum -c claw-trade-production-版本-时间.tar.gz.sha256
```

### 4.1.1 海外免备案下载方案

不想备案，用 Cloudflare R2。

一次性设置：

```text
1. 在 Cloudflare 添加域名 cflank-trade.top。
2. 到 NameSilo 把 cflank-trade.top 的 nameserver 改成 Cloudflare 给的两个 nameserver。
3. 在 Cloudflare R2 创建 bucket，例如 claw-trade-delivery。
4. 在 R2 bucket 里添加自定义域名：download.cflank-trade.top。
```

每次交付：

```text
上传到 R2：
delivery/claw-trade-production-版本-时间.tar.gz
delivery/claw-trade-production-版本-时间.tar.gz.sha256
```

目标机下载：

```bash
cd /tmp
curl -fL -o claw-trade-production-版本-时间.tar.gz "https://download.cflank-trade.top/delivery/claw-trade-production-版本-时间.tar.gz"
curl -fL -o claw-trade-production-版本-时间.tar.gz.sha256 "https://download.cflank-trade.top/delivery/claw-trade-production-版本-时间.tar.gz.sha256"
sha256sum -c claw-trade-production-版本-时间.tar.gz.sha256
```

规则：

```text
不需要 ICP 备案。
下载速度取决于目标机到 Cloudflare 的线路。
license.key 不上传 R2。
```

第一次 SSH 提示：

```text
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

输入：

```text
yes
```

这是记录目标机指纹，不是报错。

### 4.2 登录目标机

```bash
ssh frank@192.168.1.21
```

### 4.3 一键安装

在目标机执行：

```bash
sudo bash /tmp/install_factory_test_ubuntu.sh \
  /tmp/claw-trade-production-版本-时间.tar.gz \
  --license-key-file /tmp/license.key
```

安装脚本会做：

```text
1. 安装系统依赖。
2. 创建 /opt/claw-trade。
3. 解压 release 到 /opt/claw-trade/releases/。
4. 把 /opt/claw-trade/current 指向当前 release。
5. 创建 /opt/claw-trade/shared/。
6. 读取 /tmp/license.key。
7. 调用目标机本机 Virbox 服务绑定授权码。
8. 启动 control runtime。
9. 恢复包内数据种子。
10. 启动 UI。
11. 检查 http://127.0.0.1:5175/。
```

成功会看到类似：

```text
[OK] claw-trade installed
UI: http://127.0.0.1:5175/
Log: /tmp/claw-trade-ui.log
Control log: /tmp/claw-trade-control.log
```

### 4.4 验证

目标机本机验证：

```bash
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5175/
```

Win11 浏览器打开：

```text
http://192.168.1.21:5175/
```

端口检查：

```bash
ss -lntp | grep -E ':(5175|1933|18789)\b'
```

合格结果：

```text
127.0.0.1:5175 返回 200。
192.168.1.21:5175 能打开。
UI 监听 0.0.0.0:5175。
OpenViking 监听 127.0.0.1:1933。
OpenClaw 监听 127.0.0.1:18789。
```

## 5. Virbox 授权

厂家机器必须在目标机上绑定授权。不能在发布机绑定后拿去另一台机器用。

原因：

```text
一机一码绑定的是目标机器指纹。
换机器就要重新激活、转移或重新发码。
```

量产推荐走自动绑定：

```bash
sudo bash /tmp/install_factory_test_ubuntu.sh \
  /tmp/claw-trade-production-版本-时间.tar.gz \
  --license-key-file /tmp/license.key
```

脚本读取：

```text
/tmp/license.key
```

然后调用目标机本机 Virbox 服务：

```text
http://127.0.0.1:12339
```

如果自动绑定失败，再人工排查。

目标机本机能访问时，打开：

```text
http://192.168.1.21:12339/#/license
```

如果 Win11 打不开，但目标机本机服务正常，用 SSH 转发：

```bash
ssh -L 12339:127.0.0.1:12339 frank@192.168.1.21
```

然后 Win11 浏览器打开：

```text
http://127.0.0.1:12339/#/license
```

## 6. 配置目标机远程升级

首次安装后，如果这台机器以后要在线升级，要配置更新源和公钥。

发布机上传公钥：

```bash
scp update-signing-public.pem frank@192.168.1.21:/tmp/
```

目标机配置更新源：

```bash
sudo mkdir -p /opt/claw-trade/shared/config
sudo tee -a /opt/claw-trade/shared/config/claw-trade.env >/dev/null <<'EOF'
CLAW_TRADE_UPDATE_BASE_URL=https://download.cflank-trade.top/stable/
CLAW_TRADE_AUTO_UPDATE_INSTALL=1
EOF
```

安装升级公钥：

```bash
sudo install -d -m 0755 /etc/claw-trade
sudo install -m 0644 /tmp/update-signing-public.pem /etc/claw-trade/update-signing-public.pem
```

重启服务：

```bash
sudo systemctl restart claw-trade-control.service claw-trade-ui.service
```

如果测试机没有 systemd 服务，就重跑安装脚本，或重启当前脚本启动的进程。

## 7. 制作远程升级包

远程升级包本质上也是生产包，但多了：

```text
1. 生产包签名。
2. manifest.json。
3. manifest.json 签名。
4. 上传到七牛 stable/。
```

如果产品要求 Virbox 保护，升级用的 tar.gz 也必须是第 3 节做出来的保护包。不要把普通生产包拿去签名发布。

### 7.1 给 tar.gz 签名

发布私钥只放发布机，不放目标工控机。

```bash
openssl pkeyutl -sign \
  -inkey update-signing-private.pem \
  -rawin \
  -in claw-trade-production-版本-时间.tar.gz \
  -out claw-trade-production-版本-时间.tar.gz.sig
```

### 7.2 生成 manifest.json

示例：

```json
{
  "product": "claw-trade",
  "channel": "stable",
  "version": "0.1.4",
  "arch": "linux-x86_64",
  "archive": "claw-trade-production-0.1.4-20260629T114637Z.tar.gz",
  "sha256": "这里填最终 tar.gz 的 sha256",
  "created_at": "2026-06-29T11:46:37Z",
  "min_current_version": "0.1.0",
  "notes": "release note"
}
```

字段要求：

```text
version：新版本号，必须高于目标机当前版本。
archive：只写文件名，不写路径。
sha256：必须是最终上传 tar.gz 的 sha256。
min_current_version：允许升级的最低当前版本。
```

计算 sha256：

```bash
sha256sum claw-trade-production-版本-时间.tar.gz
```

### 7.3 给 manifest 签名

```bash
openssl pkeyutl -sign \
  -inkey update-signing-private.pem \
  -rawin \
  -in manifest.json \
  -out manifest.json.sig
```

最终有 4 个文件：

```text
claw-trade-production-版本-时间.tar.gz
claw-trade-production-版本-时间.tar.gz.sig
manifest.json
manifest.json.sig
```

### 7.4 上传远程升级文件

上传到 Cloudflare R2：

```text
stable/claw-trade-production-版本-时间.tar.gz
stable/claw-trade-production-版本-时间.tar.gz.sig
stable/manifest.json
stable/manifest.json.sig
```

上传顺序必须是：

```text
1. 先传 tar.gz。
2. 再传 tar.gz.sig。
3. 再传 manifest.json。
4. 最后传 manifest.json.sig。
```

不要先传 `manifest.json`。否则目标机可能先看到新版本，但包还没传完。

上传后检查这些地址：

```text
https://download.cflank-trade.top/stable/manifest.json
https://download.cflank-trade.top/stable/manifest.json.sig
https://download.cflank-trade.top/stable/claw-trade-production-版本-时间.tar.gz
https://download.cflank-trade.top/stable/claw-trade-production-版本-时间.tar.gz.sig
```

打开：

```text
https://download.cflank-trade.top/stable/manifest.json
```

确认显示的是新版本、新 archive、新 sha256。

## 8. 在界面执行升级

打开设备界面：

```text
http://目标机IP:5175/
```

进入：

```text
设置 -> 远程更新
```

手动升级：

```text
1. 点击 检查更新。
2. 看到发现新版本。
3. 点击 安装更新。
4. 等待服务重启。
5. 刷新页面确认版本状态。
```

如果目标机配置了：

```text
CLAW_TRADE_AUTO_UPDATE_INSTALL=1
```

系统也可以自动检查并安装更新。

升级安装时，目标机会校验：

```text
manifest 签名
生产包 sha256
生产包签名
包名和目录结构
版本是否高于当前版本
```

校验失败不会安装。

安装后健康检查失败，会回滚到上一版本。

## 9. 回滚、救援页、恢复出厂

升级失败时，目标机会保留上一可用版本。

如果主 UI 起不来，会进入救援页。救援页仍然使用同一个地址：

```text
http://目标机IP:5175/
```

恢复出厂入口：

```text
设置 -> 恢复出厂设置
```

恢复出厂会清空：

```text
应用配置
报告
缓存
队列
会话
临时文件
```

恢复出厂会保留：

```text
Virbox 授权
更新包
当前版本
上一可用版本
更新公钥
```

恢复出厂不是重装 Ubuntu，也不会清空整机磁盘。

## 10. 卸载

只卸载程序，保留 shared 数据：

```bash
sudo bash /tmp/uninstall_factory_test_ubuntu.sh
```

卸载程序，并清空 shared 数据：

```bash
sudo bash /tmp/uninstall_factory_test_ubuntu.sh --purge-shared
```

`--purge-shared` 会删除应用共享数据，包括缓存、报告、运行状态等。客户机器上不要随便执行。

## 11. 交付前检查表

交付前至少确认：

```text
1. 普通生产包构建成功。
2. 如果给厂家，已经做 Virbox 保护包。
3. 保护包里 Python 和 Node 是受保护版本。
4. app/python/claw_trade/**/*.pyc 已加密。
5. agents/**/*.md 已加密。
6. third_party/openclaw/openclaw.mjs 已加密。
7. openclaw_plugins/**/*.js 已加密。
8. 包里没有 .ssp。
9. 包里有需要的数据种子和 sha256。
10. 每台机器有自己的 license.key。
11. 新 Ubuntu 机器能一键安装。
12. http://127.0.0.1:5175/ 返回 200。
13. http://目标机IP:5175/ 能从 Win11 打开。
14. Virbox 授权码已经在目标机绑定成功。
15. 如果要远程升级，目标机有更新源和公钥。
16. 如果发布远程升级，上传的 4 个文件和 CDN 都已验证。
```

## 12. 常见问题

### 只创建了目录，没有真正安装 release

现象：

```text
/opt/claw-trade/current 不存在
/opt/claw-trade/releases 是空的
浏览器打不开
curl 127.0.0.1:5175 失败
```

处理：

```bash
sudo bash /tmp/install_factory_test_ubuntu.sh /tmp/claw-trade-production-版本-时间.tar.gz
```

不要只手工创建 `/opt/claw-trade` 目录。

### 包能装，但 UI 起不来

检查日志：

```bash
tail -n 120 /tmp/claw-trade-ui.log
tail -n 120 /tmp/claw-trade-control.log
```

检查端口：

```bash
ss -ltnp | grep 5175
```

### Win11 打不开目标机页面

先在目标机本机检查：

```bash
curl -fsS http://127.0.0.1:5175/ | head
ss -ltnp | grep 5175
```

如果本机正常，Win11 打不开，优先查目标机防火墙、IP 是否正确、UI 是否监听 `0.0.0.0:5175`。

### Virbox 授权一台机器成功，另一台机器不能用

这是正常的。

授权码软锁绑定机器指纹。换机器就要重新激活、转移或重新发码。

### 检查更新提示未配置

检查：

```bash
grep CLAW_TRADE_UPDATE_BASE_URL /opt/claw-trade/shared/config/claw-trade.env
```

### 检查更新提示公钥不存在

检查：

```bash
ls -l /etc/claw-trade/update-signing-public.pem
```

### 七牛已经上传，但目标机还是旧版本

先刷新 CDN，再打开：

```text
https://download.cflank-trade.top/stable/manifest.json
```

确认看到的是新 `version`、新 `archive`、新 `sha256`。

### 安装更新按钮不可用

先点：

```text
检查更新
```

只有检查到可安装新版本后，`安装更新` 才可用。

### 升级包校验失败

通常是：

```text
1. manifest 里的 sha256 不是最终 tar.gz 的 sha256。
2. tar.gz 签名对应的不是最终 tar.gz。
3. manifest.json 改过后没有重新签名。
4. archive 文件名写错。
5. 包名或顶层目录名不符合 claw-trade-production-版本-时间。
```

处理：

```text
重新计算 sha256。
重新签 tar.gz。
重新签 manifest.json。
重新上传 4 个文件。
重新刷新 CDN。
```

### Virbox 状态检查 SDK 缺失

如果日志提示 Virbox 状态检查 SDK 缺失，说明 UI 授权状态显示可能不可用。

这不等于 Virbox 保护运行时失效。受保护 Python、Node、DS 加密文件仍然按 Virbox 保护逻辑运行。

如果产品要求界面显示授权状态，就把 Virbox 状态检查 SDK 补进生产包，再重新构建和验证。
