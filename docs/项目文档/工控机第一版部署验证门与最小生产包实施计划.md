# 工控机第一版部署验证门与最小生产包实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务执行。步骤使用 checkbox（`- [ ]`）跟踪。

**目标：** 先交付一个可验证的第一版竖切片：Virbox 授权验证门、`/opt/claw-trade/current` 无开发仓库启动样机、后端授权阻断报告生成和数据刷新。

**架构：** `claw-trade` 继续拥有报告工作流、数据刷新、artifact authority 和导出；授权层只提供“能否生成报告/能否刷新数据”的后端状态，不改写 worker prompt、报告结论或 PM 决策。生产入口是新的薄启动层，不能直接把开发脚本放进 systemd；第一阶段只做最小生产包和阻断竖切片，不做完整 updater、黄金镜像、深度加固。

**技术栈：** Python 3.12、FastAPI、pytest、systemd、Chromium kiosk policy、OpenViking、OpenClaw gateway、MongoDB、Virbox Linux x86 SDK/API、shell 打包脚本。

---

## 0. 范围和硬边界

本计划是第一版生产部署的**验证门和最小可启动竖切片**，不是完整第一版生产合同。签名 updater、救援页、黄金镜像和量产流程必须在本计划通过后另开计划。

### 本计划交付

- Virbox 账号申请、产品配置、测试授权码和 Linux x86 SDK/runtime 获取记录。
- Ubuntu x86_64 验证虚拟机创建记录、快照和 SSH 登录方式。
- Virbox Linux x86 SDK/API 实机验证记录。
- 后端授权状态模型和 Virbox 状态读取适配。
- 报告生成和数据刷新后端授权阻断。
- UI 授权状态查询和禁用提示，前端只展示，不拥有授权判断。
- `/opt/claw-trade/current` 最小生产启动样机。
- 生产包内容审计：不含 `.git`、`tests/`、`docs/`、`web/research-ui/src/`、开发脚本、`.env.local`、签名私钥。
- 最小 systemd unit 草案和 VM 验收命令。
- OpenClaw 边界验收：生产样机仍能证明 `/report` 通过 OpenClaw worker wake。

### 本计划不交付

- 完整签名 updater 和回滚器。
- 黄金镜像、Clonezilla 流程、硬盘克隆流程。
- `.clawpack`、native loader、Virbox Protector、Secure Boot、磁盘加密、TPM。
- 远程运维、factory/support endpoint、产线注册后台。
- Python 源码级强防护或抗 root/拆盘/动态调试。

### 停止条件

- Virbox SDK/API 不能在 Ubuntu x86 服务进程里稳定返回授权状态。
- 生产样机仍依赖 `/home/frank/src/claw-trade`、`uv run`、`pnpm build` 或 `.env.local`。
- 授权失败仍能触发报告生成、数据刷新或 OpenClaw 报告 workflow。
- 授权层开始改写 worker prompt、报告结论或 PM 决策。
- 为了让测试通过而加入 mock/stub/fake/fallback 冒充生产授权。

---

## 1. 文件结构

### 新增产品代码文件

- Create: `src/claw_trade/licensing/__init__.py`
  - 暴露授权状态读取的最小公共入口。
- Create: `src/claw_trade/licensing/status.py`
  - 定义授权状态、功能权限、用户可见错误和 fail-closed 判断。
- Create: `src/claw_trade/licensing/virbox.py`
  - 调用已验证的 Virbox Linux x86 SDK/API 包装命令或 Python binding。
  - 只做状态读取和激活码后四位脱敏，不保存明文授权码。
- Create: `src/claw_trade/licensing/service.py`
  - 根据环境构建授权服务。
  - 开发模式允许不启用授权；生产模式必须 fail closed。
- Create: `src/claw_trade/production/__init__.py`
- Create: `src/claw_trade/production/paths.py`
  - 解析 `/opt/claw-trade/current` 和 `/opt/claw-trade/shared`。
- Create: `src/claw_trade/production/preflight.py`
  - 检查生产目录、前端 dist、OpenClaw dist、配置、数据目录、日志目录。
- Create: `src/claw_trade/cli/production_preflight.py`
  - 命令行运行生产 preflight。

### 修改产品代码文件

- Modify: `src/claw_trade/web/state.py`
  - 在 `UiHttpServices` 中挂载授权服务。
  - 构建报告队列、选股刷新、定时维护时注入授权检查 callable。
- Modify: `src/claw_trade/web/routes_ui.py`
  - 新增 `/api/ui/get-license-status`。
  - 继续让现有路由只调用后端服务，不能让前端决定授权。
- Modify: `src/claw_trade/ui_backend/report_queue.py`
  - 在报告入队和启动前执行授权检查。
- Modify: `src/claw_trade/selection/refresh.py`
  - 在手动 `/select refresh`、缓存缺失补数、自动刷新前执行授权检查。
- Modify: `src/claw_trade/ui_backend/scheduled_work_runner.py`
  - 在 OpenClaw cron 唤醒的数据刷新和 data maintenance 执行前执行授权检查。
- Modify: `src/claw_trade/web/app.py`
  - 生产模式不再从 `.runtime/dev-services/runtime.env` 猜 token。
  - 保留开发模式兼容。
- Modify: `web/research-ui/src/App.tsx`
  - 加授权状态栏和授权失效时的按钮禁用提示。
- Modify: `web/research-ui/src/styles.css`
  - 加少量授权状态样式。

### 新增打包和部署文件

- Create: `packaging/production/bin/claw-trade-control`
  - 生产 control 入口模板。
- Create: `packaging/production/bin/claw-trade-ui`
  - 生产 UI 入口模板。
- Create: `packaging/production/bin/claw-trade-preflight`
  - 生产 preflight 入口模板。
- Create: `packaging/production/runtime/claw-trade-control-runtime`
  - 生产 control runtime 模板，启动 OpenViking 和 OpenClaw gateway。
- Create: `packaging/production/systemd/claw-trade-control.service`
- Create: `packaging/production/systemd/claw-trade-ui.service`
- Create: `packaging/production/systemd/claw-trade-kiosk.service`
- Create: `packaging/production/kiosk/chromium-policy.json`
- Create: `scripts/production/build_production_package.sh`
  - 构建最小 release tar。
- Create: `scripts/production/audit_production_package.py`
  - 审计 release tar 禁止内容。
- Create: `scripts/production/install_production_package.sh`
  - 安装 release tar 到测试 VM 的 `/opt/claw-trade`。

### 新增测试文件

- Create: `tests/unit/licensing/test_status.py`
- Create: `tests/unit/licensing/test_virbox.py`
- Create: `tests/unit/licensing/test_service.py`
- Create: `tests/unit/production/test_paths.py`
- Create: `tests/unit/production/test_preflight.py`
- Create: `tests/unit/production/test_package_audit.py`
- Create: `tests/e2e/ui/test_license_gate_routes.py`

### 新增证据文档

- Create: `docs/evidence/工控机第一版Virbox验证记录.md`
- Create: `docs/evidence/工控机第一版Virbox账号与授权配置记录.md`
- Create: `docs/evidence/工控机第一版Ubuntu虚拟机创建记录.md`
- Create: `docs/evidence/工控机第一版生产包审计记录.md`
- Create: `docs/evidence/工控机第一版VM启动验收记录.md`
- Create: `docs/evidence/工控机第一版OpenClaw边界验收记录.md`

---

## 2. 任务清单

### Task 0: Virbox 账号和 Ubuntu 验证虚拟机实操准备

**目的：** 先拿到真实 Virbox 账号、测试产品、测试授权码、Linux x86 SDK/runtime，并创建一台干净 Ubuntu x86_64 VM。没有这一步，后续“验证门”没有对象。

**Files:**
- Create: `docs/evidence/工控机第一版Virbox账号与授权配置记录.md`
- Create: `docs/evidence/工控机第一版Ubuntu虚拟机创建记录.md`

- [ ] **Step 1: 准备 Virbox 账号申请材料**

准备并记录以下信息，不写入仓库的敏感字段用“已准备，不入库”表示：

```text
公司/主体名称：
联系人：
手机号：
邮箱：
产品名称：claw-trade
目标平台：Ubuntu x86_64 工控机
授权模型：一机一码
续期频率：每天心跳
离线宽限期：30 天
需要 SDK/API：Linux x86_64 后端服务进程可调用
需要功能模块：report_generation, data_refresh
需要测试状态：未激活、已激活、过期、宽限期、超过宽限期、吊销、runtime 不可用、网络不可用
```

Expected: 申请材料齐全。不要把手机号、邮箱、账号密码、授权码明文提交到仓库。

- [ ] **Step 2: 申请 Virbox 账号或试用**

实际操作：

```text
1. 打开 Virbox 官方站点或联系 Virbox/深思数盾销售支持。
2. 申请企业账号或试用账号。
3. 明确要求开通：云许可/软许可、授权码、一机一码、Linux x86_64 SDK/runtime、测试授权码。
4. 要求支持方确认：Python/FastAPI 后端服务进程可以读取授权状态。
5. 要求支持方提供：SDK 下载地址、runtime 安装包、示例程序、API 文档、错误码说明、控制台登录入口。
```

Expected:

- 能登录 Virbox 控制台。
- 能下载 Linux x86_64 SDK/runtime。
- 能创建测试产品和测试授权码。
- 如果只能走销售人工开通，把工单号或联系人记录到证据文档；不要把账号密码入库。

- [ ] **Step 3: 创建 Virbox 测试产品**

在 Virbox 控制台创建测试产品：

```text
产品名称：claw-trade-prod-v1-test
平台：Linux x86_64
授权类型：云许可/软许可
授权码模式：一台设备一个唯一授权码
心跳频率：1 天
离线宽限：30 天
功能模块：
  - report_generation
  - data_refresh
```

Expected:

- 控制台有产品 id。
- 控制台有两个功能模块 id 或等价 feature 标识。
- 记录产品 id 和 feature id；不记录控制台登录凭证。

- [ ] **Step 4: 创建测试授权码矩阵**

在 Virbox 控制台创建或向支持方申请以下测试授权码：

```text
active_001：已激活，report_generation/data_refresh 均开通
not_activated_001：未激活
expired_001：已过期
revoked_001：已吊销
grace_001：离线宽限期内
over_grace_001：超过离线宽限期
feature_report_only_001：只开通 report_generation
feature_refresh_only_001：只开通 data_refresh
```

Expected:

- 每个场景都有可操作授权码或控制台可切换状态。
- 证据文档只记录授权码后 4 位或不可反推短标识。
- 如果 Virbox 不能直接创建某状态，记录替代制造方式，例如“先激活后控制台吊销”。

- [ ] **Step 5: 下载并保存 Virbox SDK/runtime**

在实施人员本机或安全共享盘保存 SDK/runtime，路径不要放在仓库内：

```bash
mkdir -p ~/secure-vendor/virbox/linux-x86_64
```

把 Virbox 提供的 SDK/runtime、示例程序、文档放到：

```text
~/secure-vendor/virbox/linux-x86_64/
  sdk/
  runtime/
  examples/
  docs/
```

记录校验信息：

```bash
export VIRBOX_VENDOR_DIR="$HOME/secure-vendor/virbox/linux-x86_64"
find "${VIRBOX_VENDOR_DIR}" -maxdepth 3 -type f -print0 | sort -z | xargs -0 sha256sum
find "${VIRBOX_VENDOR_DIR}" -maxdepth 5 -type f -perm -111 -print
find "${VIRBOX_VENDOR_DIR}/runtime" -maxdepth 4 -type f \
  \( -name '*.deb' -o -name '*.rpm' -o -name '*.run' -o -name '*.sh' -o -name '*.tar.gz' \) -print
```

Expected:

- SDK/runtime 不进入 git。
- 记录 SDK 版本、runtime 版本、sha256、下载来源。
- 记录后续探针脚本需要调用的官方 SDK、runtime 或示例程序路径。
- 记录可在干净 Ubuntu VM 安装的官方 runtime 安装包或安装脚本路径。
- 如果 Virbox 不提供 Linux x86_64 SDK/runtime，停止本计划。

- [ ] **Step 6: 写 Virbox 账号和配置证据**

在 `docs/evidence/工控机第一版Virbox账号与授权配置记录.md` 写：

```markdown
# 工控机第一版 Virbox 账号与授权配置记录

## 账号申请

- 申请日期：
- 申请方式：官网 / 销售 / 支持工单
- 工单号或联系人：
- 控制台入口：已记录到安全凭据库，不入库
- 账号密码：不入库

## 测试产品

- 产品名称：
- 产品 id：
- 平台：
- 授权类型：
- 心跳频率：
- 离线宽限：

## 功能模块

| 功能 | Virbox 标识 | 用途 |
| --- | --- | --- |
| report_generation | | 控制报告生成 |
| data_refresh | | 控制数据刷新 |

## 测试授权码矩阵

| 场景 | 授权码短标识 | 状态制造方式 | 预期 report | 预期 data_refresh |
| --- | --- | --- | --- | --- |
| active_001 | 后四位： | | allow | allow |
| not_activated_001 | 后四位： | | block | block |
| expired_001 | 后四位： | | block | block |
| revoked_001 | 后四位： | | block | block |
| grace_001 | 后四位： | | allow | allow |
| over_grace_001 | 后四位： | | block | block |
| feature_report_only_001 | 后四位： | | allow | block |
| feature_refresh_only_001 | 后四位： | | block | allow |

## SDK/runtime

- SDK 版本：
- runtime 版本：
- 本地安全路径：
- sha256 记录：
```

Expected: 证据文档能让后续执行者知道账号、产品和测试授权码已经准备好，但不能泄露凭证。

- [ ] **Step 7: 安装本机 KVM/libvirt 工具**

在创建 VM 的宿主机运行：

```bash
sudo apt-get update
sudo apt-get install -y qemu-kvm libvirt-daemon-system libvirt-clients virtinst cloud-image-utils qemu-utils openssh-client
sudo usermod -aG libvirt,kvm "$USER"
newgrp libvirt
```

Expected:

- `virsh list --all` 可运行。
- 当前用户能创建 libvirt VM。
- 如果宿主机不支持 KVM，停止并改用 VirtualBox 或真实工控机，不要继续假装 VM 验证完成。

- [ ] **Step 8: 创建 Ubuntu 24.04 x86_64 云镜像 VM**

创建目录：

```bash
mkdir -p ~/vm/claw-trade-prod-vm
cd ~/vm/claw-trade-prod-vm
```

下载 Ubuntu 24.04 LTS server cloud image：

```bash
curl -L -o ubuntu-24.04-server-cloudimg-amd64.img \
  https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-amd64.img
sha256sum ubuntu-24.04-server-cloudimg-amd64.img
```

生成 80G 磁盘：

```bash
qemu-img create -f qcow2 -F qcow2 \
  -b ubuntu-24.04-server-cloudimg-amd64.img \
  claw-trade-prod-vm.qcow2 80G
```

准备 SSH key：

```bash
test -f ~/.ssh/id_ed25519.pub || ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N ""
```

写 cloud-init：

```bash
PUBKEY="$(cat ~/.ssh/id_ed25519.pub)"
cat > user-data <<EOF
#cloud-config
users:
  - name: deploy
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: sudo
    shell: /bin/bash
    ssh_authorized_keys:
      - ${PUBKEY}
package_update: true
packages:
  - python3.12
  - python3.12-venv
  - curl
  - qemu-guest-agent
runcmd:
  - systemctl enable --now qemu-guest-agent
EOF

cat > meta-data <<'EOF'
instance-id: claw-trade-prod-vm-001
local-hostname: claw-trade-prod-vm
EOF

cloud-localds seed.iso user-data meta-data
```

创建并启动 VM：

```bash
virt-install \
  --name claw-trade-prod-vm \
  --memory 8192 \
  --vcpus 4 \
  --disk path="$PWD/claw-trade-prod-vm.qcow2",format=qcow2,bus=virtio \
  --disk path="$PWD/seed.iso",device=cdrom \
  --os-variant ubuntu24.04 \
  --import \
  --network network=default,model=virtio \
  --graphics none \
  --noautoconsole
```

Expected:

- `virsh list --all` 能看到 `claw-trade-prod-vm` running。
- VM 内存 8G、CPU 4 核、磁盘 80G。

- [ ] **Step 9: 获取 VM IP 并验证 SSH**

等待 cloud-init 完成并从 libvirt 读取 IP：

```bash
VM_IP="$(virsh domifaddr claw-trade-prod-vm | awk '/ipv4/ {split($4,a,"/"); print a[1]; exit}')"
if [ -z "${VM_IP}" ]; then
  virsh net-dhcp-leases default
  VM_IP="$(virsh net-dhcp-leases default | awk '/claw-trade-prod-vm|ipv4/ {split($5,a,"/"); print a[1]; exit}')"
fi
test -n "${VM_IP}"
printf 'VM_IP=%s\n' "${VM_IP}"
```

如果仍然没有 IP，先看 VM 是否启动、default 网络是否启动、cloud-init 是否完成，不继续后续步骤：

```bash
virsh domstate claw-trade-prod-vm
virsh net-dhcp-leases default
```

登录 VM：

```bash
ssh -o StrictHostKeyChecking=accept-new deploy@"${VM_IP}" 'uname -a && lsb_release -a || cat /etc/os-release'
```

Expected:

- `VM_IP` 已写入本次终端环境，并记录到 VM 创建证据文档。
- SSH 登录成功。
- 输出 Ubuntu 24.04 x86_64。
- 不需要密码登录。

- [ ] **Step 10: 在 VM 内安装基础依赖和创建服务用户**

Run:

```bash
ssh deploy@"${VM_IP}" '
set -euo pipefail
sudo apt-get update
sudo apt-get install -y python3.12 python3.12-venv curl nodejs chromium-browser
getent group clawtrade >/dev/null || sudo groupadd --system clawtrade
id -u clawtrade >/dev/null 2>&1 || sudo useradd --system --gid clawtrade --home /nonexistent --shell /usr/sbin/nologin clawtrade
getent group clawkiosk >/dev/null || sudo groupadd --system clawkiosk
id -u clawkiosk >/dev/null 2>&1 || sudo useradd --system --gid clawkiosk --create-home --shell /usr/sbin/nologin clawkiosk
python3.12 --version
curl --version
node --version
chromium-browser --version
id clawtrade
id clawkiosk
'
```

Expected:

- Python、curl、浏览器依赖可用。
- `clawtrade` 和 `clawkiosk` 用户存在。
- 如果 `chromium-browser` 在当前 Ubuntu 源不可安装，停止并记录原因；不要用 `|| true` 跳过 kiosk 依赖失败。

- [ ] **Step 11: 创建 VM 基线快照**

关闭 VM 并拍快照：

```bash
virsh shutdown claw-trade-prod-vm
until [[ "$(virsh domstate claw-trade-prod-vm)" == "shut off" ]]; do sleep 2; done
virsh snapshot-create-as claw-trade-prod-vm clean-ubuntu-24.04-before-claw-trade
virsh start claw-trade-prod-vm
```

Expected: 后续验收失败可以回滚到 `clean-ubuntu-24.04-before-claw-trade`。

- [ ] **Step 12: 写 Ubuntu VM 创建记录**

在 `docs/evidence/工控机第一版Ubuntu虚拟机创建记录.md` 写：

```markdown
# 工控机第一版 Ubuntu 虚拟机创建记录

- 创建日期：
- 宿主机：
- VM 名称：claw-trade-prod-vm
- Ubuntu 镜像 URL：
- Ubuntu 镜像 sha256：
- CPU：
- 内存：
- 磁盘：
- VM IP：
- SSH 用户：deploy
- 服务用户：clawtrade, clawkiosk
- 基线快照：clean-ubuntu-24.04-before-claw-trade
- 验证命令：
- 验证结果：
```

Expected: 后续 Task 12 可以直接使用这台 VM，不再临时猜 VM 创建方式。

- [ ] **Step 13: Commit**

```bash
git add docs/evidence/工控机第一版Virbox账号与授权配置记录.md docs/evidence/工控机第一版Ubuntu虚拟机创建记录.md
git commit -m "docs: record virbox account and vm preparation"
```

Expected: 前置实操证据入库，不包含账号密码、完整授权码或 SDK 私有包。

---

### Task 1: Virbox Linux x86 验证门

**目的：** 先证明授权底座可用。没有这份证据，不写生产授权逻辑。

**Files:**
- Create: `docs/evidence/工控机第一版Virbox验证记录.md`
- Create: `techlab/virbox_probe/README.md`
- Create: `techlab/virbox_probe/virbox_probe.py`

- [ ] **Step 1: 建立验证目录**

Run:

```bash
mkdir -p techlab/virbox_probe docs/evidence
```

Expected: 目录存在。

- [ ] **Step 2: 记录 Virbox SDK 来源**

在 `techlab/virbox_probe/README.md` 写入：

```markdown
# Virbox Linux x86 验证记录

- SDK 来源：
- SDK 版本：
- Ubuntu 版本：
- CPU 架构：
- Python 版本：
- 官方样例路径：
- 本机 runtime 安装路径：
```

Expected: SDK 版本和路径明确。不要提交 Virbox 私有 SDK 包、账号、授权码明文。

- [ ] **Step 3: 写最小探针脚本**

在 `techlab/virbox_probe/virbox_probe.py` 写一个只调用官方 SDK/API 的探针。文件首行必须是：

```python
#!/usr/bin/env python3
```

脚本必须从 Task 0 记录的 Virbox SDK/runtime 路径读取所需库或命令；不能写 mock、stub、固定 JSON 或手工映射状态。输出必须是 JSON，字段先固定为：

```json
{
  "raw_status": "activated",
  "normalized_status": "activated",
  "features": ["report", "data_refresh"],
  "expires_at": "2026-12-31T23:59:59Z",
  "grace_until": "2027-01-30T23:59:59Z",
  "device_id_hash": "sha256:...",
  "license_suffix": "1234",
  "error_code": null,
  "error_message": null
}
```

如果官方 SDK 不支持某字段，先写 `null`，不要编造。

让探针可执行：

```bash
chmod +x techlab/virbox_probe/virbox_probe.py
```

- [ ] **Step 4: 跑未激活状态**

Run:

```bash
python3 techlab/virbox_probe/virbox_probe.py
```

Expected: JSON 中 `normalized_status` 是 `not_activated` 或等价状态；退出码为 `0`。如果 SDK 不可调用，停止本计划。

- [ ] **Step 5: 跑已激活状态**

使用 Virbox 官方流程激活测试授权码后运行：

```bash
python3 techlab/virbox_probe/virbox_probe.py
```

Expected: JSON 中 `normalized_status=activated`，`features` 包含 `report` 和 `data_refresh`。

- [ ] **Step 6: 跑过期、吊销、宽限、runtime 不可用状态**

分别制造或向 Virbox 控制台配置：

```text
expired
revoked
grace_period
runtime_unavailable
network_unavailable
```

每种状态都运行探针并保存脱敏输出。

Expected: 每种状态都有明确 `normalized_status`、`error_code` 和可脱敏说明。

- [ ] **Step 7: 写证据文档**

在 `docs/evidence/工控机第一版Virbox验证记录.md` 写入：

```markdown
# 工控机第一版 Virbox 验证记录

## 结论

- 结论：通过 / 不通过
- 验证日期：
- Ubuntu 版本：
- Virbox SDK 版本：

## 状态矩阵

| 场景 | normalized_status | report | data_refresh | 退出码 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 未激活 | | | | | |
| 已激活 | | | | | |
| 过期 | | | | | |
| 宽限期内 | | | | | |
| 超过宽限 | | | | | |
| 吊销 | | | | | |
| runtime 不可用 | | | | | |
| 网络不可用 | | | | | |

## 停止条件

- 如果无法稳定读取状态，停止后续实施。
- 如果不能识别吊销或过期，停止后续实施。
- 如果 Python/FastAPI 服务进程不能调用，停止后续实施。
```

Expected: 文档能证明 Virbox 可作为第一版授权权威。

- [ ] **Step 8: Commit**

```bash
git add techlab/virbox_probe docs/evidence/工控机第一版Virbox验证记录.md
git commit -m "docs: record virbox linux validation gate"
```

Expected: 提交只包含 Virbox 验证材料，不包含授权码明文或 SDK 私有包。

---

### Task 2: 授权状态模型

**目的：** 把 Virbox 原始状态收敛成后端可执行的两个判断：能否生成报告、能否刷新数据。

**Files:**
- Create: `src/claw_trade/licensing/__init__.py`
- Create: `src/claw_trade/licensing/status.py`
- Test: `tests/unit/licensing/test_status.py`

- [ ] **Step 1: 写失败测试**

在 `tests/unit/licensing/test_status.py` 写：

```python
from claw_trade.licensing.status import LicenseFeature, LicenseSnapshot, LicenseStatus


def test_activated_license_allows_report_and_data_refresh() -> None:
    snapshot = LicenseSnapshot(
        status=LicenseStatus.ACTIVATED,
        features=frozenset({LicenseFeature.REPORT, LicenseFeature.DATA_REFRESH}),
    )

    assert snapshot.allows_report_generation()
    assert snapshot.allows_data_refresh()


def test_revoked_license_blocks_report_and_data_refresh() -> None:
    snapshot = LicenseSnapshot(status=LicenseStatus.REVOKED, features=frozenset())

    assert not snapshot.allows_report_generation()
    assert not snapshot.allows_data_refresh()
    assert snapshot.user_message == "设备授权已失效，请在授权页修复后重试。"


def test_runtime_unavailable_fails_closed() -> None:
    snapshot = LicenseSnapshot(status=LicenseStatus.RUNTIME_UNAVAILABLE, features=frozenset())

    assert not snapshot.allows_report_generation()
    assert not snapshot.allows_data_refresh()
```

- [ ] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/unit/licensing/test_status.py -q
```

Expected: FAIL，原因是 `claw_trade.licensing.status` 不存在。

- [ ] **Step 3: 写最小实现**

在 `src/claw_trade/licensing/status.py` 实现：

```python
from __future__ import annotations

from dataclasses import dataclass
from enum import StrEnum


class LicenseStatus(StrEnum):
    ACTIVATED = "activated"
    NOT_ACTIVATED = "not_activated"
    GRACE_PERIOD = "grace_period"
    EXPIRED = "expired"
    REVOKED = "revoked"
    RUNTIME_UNAVAILABLE = "runtime_unavailable"
    NETWORK_UNAVAILABLE = "network_unavailable"


class LicenseFeature(StrEnum):
    REPORT = "report"
    DATA_REFRESH = "data_refresh"


_ALLOWED_STATUSES = {LicenseStatus.ACTIVATED, LicenseStatus.GRACE_PERIOD}


@dataclass(frozen=True)
class LicenseSnapshot:
    status: LicenseStatus
    features: frozenset[LicenseFeature]
    expires_at: str | None = None
    grace_until: str | None = None
    device_id_hash: str | None = None
    license_suffix: str | None = None
    error_code: str | None = None
    error_message: str | None = None

    def allows_report_generation(self) -> bool:
        return self.status in _ALLOWED_STATUSES and LicenseFeature.REPORT in self.features

    def allows_data_refresh(self) -> bool:
        return self.status in _ALLOWED_STATUSES and LicenseFeature.DATA_REFRESH in self.features

    @property
    def user_message(self) -> str:
        if self.status == LicenseStatus.NOT_ACTIVATED:
            return "设备尚未激活，请在授权页激活后重试。"
        if self.status == LicenseStatus.GRACE_PERIOD:
            return "设备授权处于离线宽限期，请尽快联网续期。"
        if self.status in {LicenseStatus.EXPIRED, LicenseStatus.REVOKED}:
            return "设备授权已失效，请在授权页修复后重试。"
        if self.status == LicenseStatus.NETWORK_UNAVAILABLE:
            return "授权续期网络不可用，请检查网络后重试。"
        if self.status == LicenseStatus.RUNTIME_UNAVAILABLE:
            return "授权运行时不可用，请联系支持人员。"
        return "授权状态正常。"
```

在 `src/claw_trade/licensing/__init__.py` 导出：

```python
from claw_trade.licensing.status import LicenseFeature, LicenseSnapshot, LicenseStatus

__all__ = ["LicenseFeature", "LicenseSnapshot", "LicenseStatus"]
```

- [ ] **Step 4: 跑测试确认通过**

Run:

```bash
uv run pytest tests/unit/licensing/test_status.py -q
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/claw_trade/licensing tests/unit/licensing/test_status.py
git commit -m "feat: add production license status model"
```

Expected: 单独提交授权状态模型。

---

### Task 3: Virbox 状态读取适配

**目的：** 后端读取真实 Virbox 状态。测试可使用脱敏 fixture，但产品代码不能 fake 成功。

**Files:**
- Create: `src/claw_trade/licensing/virbox.py`
- Test: `tests/unit/licensing/test_virbox.py`

- [ ] **Step 1: 写失败测试**

在 `tests/unit/licensing/test_virbox.py` 写：

```python
from pathlib import Path

import pytest

from claw_trade.licensing.status import LicenseStatus
from claw_trade.licensing.virbox import VirboxLicenseReader, VirboxReadError


def test_virbox_reader_maps_activated_json(tmp_path: Path) -> None:
    command = tmp_path / "probe.sh"
    command.write_text(
        "#!/usr/bin/env bash\n"
        "printf '%s\\n' '{\"normalized_status\":\"activated\",\"features\":[\"report\",\"data_refresh\"],\"license_suffix\":\"1234\"}'\n",
        encoding="utf-8",
    )
    command.chmod(0o755)

    snapshot = VirboxLicenseReader(str(command)).read()

    assert snapshot.status == LicenseStatus.ACTIVATED
    assert snapshot.allows_report_generation()
    assert snapshot.allows_data_refresh()
    assert snapshot.license_suffix == "1234"


def test_virbox_reader_fails_closed_when_command_missing() -> None:
    with pytest.raises(VirboxReadError):
        VirboxLicenseReader("/no/such/command").read()


def test_virbox_reader_rejects_unknown_status_as_read_error(tmp_path: Path) -> None:
    command = tmp_path / "probe.sh"
    command.write_text(
        "#!/usr/bin/env bash\n"
        "printf '%s\\n' '{\"normalized_status\":\"mystery\",\"features\":[\"report\"]}'\n",
        encoding="utf-8",
    )
    command.chmod(0o755)

    with pytest.raises(VirboxReadError, match="unknown virbox status"):
        VirboxLicenseReader(str(command)).read()


def test_virbox_reader_rejects_unknown_feature_as_read_error(tmp_path: Path) -> None:
    command = tmp_path / "probe.sh"
    command.write_text(
        "#!/usr/bin/env bash\n"
        "printf '%s\\n' '{\"normalized_status\":\"activated\",\"features\":[\"unknown_feature\"]}'\n",
        encoding="utf-8",
    )
    command.chmod(0o755)

    with pytest.raises(VirboxReadError, match="unknown virbox feature"):
        VirboxLicenseReader(str(command)).read()
```

说明：这里的 shell fixture 只模拟已验证的探针 JSON 输出，不代表生产授权成功。

- [ ] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/unit/licensing/test_virbox.py -q
```

Expected: FAIL，原因是 `VirboxLicenseReader` 不存在。

- [ ] **Step 3: 写最小实现**

在 `src/claw_trade/licensing/virbox.py` 实现：

```python
from __future__ import annotations

import json
import subprocess
from dataclasses import dataclass

from claw_trade.licensing.status import LicenseFeature, LicenseSnapshot, LicenseStatus


class VirboxReadError(RuntimeError):
    pass


@dataclass(frozen=True)
class VirboxLicenseReader:
    status_command: str
    timeout_seconds: float = 5.0

    def read(self) -> LicenseSnapshot:
        try:
            completed = subprocess.run(
                [self.status_command],
                check=False,
                capture_output=True,
                text=True,
                timeout=self.timeout_seconds,
            )
        except OSError as exc:
            raise VirboxReadError(str(exc)) from exc
        except subprocess.TimeoutExpired as exc:
            raise VirboxReadError("virbox status command timed out") from exc
        if completed.returncode != 0:
            raise VirboxReadError((completed.stderr or completed.stdout or "virbox status command failed").strip())
        try:
            payload = json.loads(completed.stdout)
        except json.JSONDecodeError as exc:
            raise VirboxReadError("virbox status command returned invalid json") from exc
        return _snapshot_from_payload(payload)


def _snapshot_from_payload(payload: object) -> LicenseSnapshot:
    if not isinstance(payload, dict):
        raise VirboxReadError("virbox status payload must be an object")
    status = _parse_status(payload.get("normalized_status"))
    features = frozenset(_parse_feature(item) for item in payload.get("features", []) if str(item).strip())
    return LicenseSnapshot(
        status=status,
        features=features,
        expires_at=_optional_text(payload.get("expires_at")),
        grace_until=_optional_text(payload.get("grace_until")),
        device_id_hash=_optional_text(payload.get("device_id_hash")),
        license_suffix=_optional_text(payload.get("license_suffix")),
        error_code=_optional_text(payload.get("error_code")),
        error_message=_optional_text(payload.get("error_message")),
    )


def _parse_status(value: object) -> LicenseStatus:
    raw = str(value or "").strip()
    try:
        return LicenseStatus(raw)
    except ValueError as exc:
        raise VirboxReadError(f"unknown virbox status: {raw or '<empty>'}") from exc


def _parse_feature(value: object) -> LicenseFeature:
    raw = str(value or "").strip()
    try:
        return LicenseFeature(raw)
    except ValueError as exc:
        raise VirboxReadError(f"unknown virbox feature: {raw or '<empty>'}") from exc


def _optional_text(value: object) -> str | None:
    if value is None:
        return None
    text = str(value).strip()
    return text or None
```

- [ ] **Step 4: 跑测试确认通过**

Run:

```bash
uv run pytest tests/unit/licensing/test_virbox.py -q
```

Expected: PASS。

- [ ] **Step 5: 用真实 Virbox 探针跑一次**

Run:

```bash
VIRBOX_PROBE="$PWD/techlab/virbox_probe/virbox_probe.py"
test -x "${VIRBOX_PROBE}"
CLAW_TRADE_VIRBOX_STATUS_COMMAND="${VIRBOX_PROBE}" \
uv run python - <<'PY'
import os
from claw_trade.licensing.virbox import VirboxLicenseReader
print(VirboxLicenseReader(os.environ["CLAW_TRADE_VIRBOX_STATUS_COMMAND"]).read())
PY
```

Expected: 输出脱敏 `LicenseSnapshot`；状态与 `docs/evidence/工控机第一版Virbox验证记录.md` 一致。

- [ ] **Step 6: Commit**

```bash
git add src/claw_trade/licensing/virbox.py tests/unit/licensing/test_virbox.py
git commit -m "feat: read virbox license status"
```

Expected: 只提交 Virbox 状态读取适配。

---

### Task 4: 授权服务构建和生产 fail-closed

**目的：** 开发环境不被授权阻断；生产环境必须有真实 Virbox 状态，缺失时 fail closed。

**Files:**
- Create: `src/claw_trade/licensing/service.py`
- Test: `tests/unit/licensing/test_service.py`

- [ ] **Step 1: 写失败测试**

在 `tests/unit/licensing/test_service.py` 写：

```python
import pytest

from claw_trade.licensing.service import LicenseService, build_license_service
from claw_trade.licensing.status import LicenseFeature, LicenseSnapshot, LicenseStatus


def test_dev_mode_without_license_required_allows_work() -> None:
    service = build_license_service({})

    assert service.current_snapshot().allows_report_generation()
    assert service.current_snapshot().allows_data_refresh()


def test_production_mode_without_virbox_command_fails_closed() -> None:
    service = build_license_service({"CLAW_TRADE_LICENSE_REQUIRED": "1"})

    snapshot = service.current_snapshot()

    assert snapshot.status == LicenseStatus.RUNTIME_UNAVAILABLE
    assert not snapshot.allows_report_generation()


def test_assert_report_generation_raises_user_error_when_blocked() -> None:
    service = LicenseService(lambda: LicenseSnapshot(status=LicenseStatus.REVOKED, features=frozenset()))

    with pytest.raises(PermissionError, match="设备授权已失效"):
        service.assert_report_generation_allowed()
```

- [ ] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/unit/licensing/test_service.py -q
```

Expected: FAIL，原因是 `service.py` 不存在。

- [ ] **Step 3: 写最小实现**

在 `src/claw_trade/licensing/service.py` 实现：

```python
from __future__ import annotations

from collections.abc import Callable, Mapping

from claw_trade.licensing.status import LicenseFeature, LicenseSnapshot, LicenseStatus
from claw_trade.licensing.virbox import VirboxLicenseReader, VirboxReadError


class LicenseService:
    def __init__(self, reader: Callable[[], LicenseSnapshot]) -> None:
        self._reader = reader

    def current_snapshot(self) -> LicenseSnapshot:
        return self._reader()

    def assert_report_generation_allowed(self) -> None:
        snapshot = self.current_snapshot()
        if not snapshot.allows_report_generation():
            raise PermissionError(snapshot.user_message)

    def assert_data_refresh_allowed(self) -> None:
        snapshot = self.current_snapshot()
        if not snapshot.allows_data_refresh():
            raise PermissionError(snapshot.user_message)


def build_license_service(env: Mapping[str, str]) -> LicenseService:
    required = str(env.get("CLAW_TRADE_LICENSE_REQUIRED") or "").strip().lower() in {"1", "true", "yes", "on"}
    command = str(env.get("CLAW_TRADE_VIRBOX_STATUS_COMMAND") or "").strip()
    if command:
        reader = VirboxLicenseReader(command)
        return LicenseService(lambda: _read_or_fail_closed(reader))
    if required:
        return LicenseService(lambda: LicenseSnapshot(status=LicenseStatus.RUNTIME_UNAVAILABLE, features=frozenset()))
    return LicenseService(
        lambda: LicenseSnapshot(
            status=LicenseStatus.ACTIVATED,
            features=frozenset({LicenseFeature.REPORT, LicenseFeature.DATA_REFRESH}),
        )
    )


def _read_or_fail_closed(reader: VirboxLicenseReader) -> LicenseSnapshot:
    try:
        return reader.read()
    except VirboxReadError as exc:
        return LicenseSnapshot(
            status=LicenseStatus.RUNTIME_UNAVAILABLE,
            features=frozenset(),
            error_code=type(exc).__name__,
            error_message=str(exc),
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run:

```bash
uv run pytest tests/unit/licensing/test_service.py -q
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/claw_trade/licensing/service.py tests/unit/licensing/test_service.py
git commit -m "feat: build production license service"
```

Expected: 授权服务单独提交。

---

### Task 5: 后端阻断报告生成

**目的：** 所有报告任务必须通过同一个队列入口和启动入口检查授权。前端不能绕过。

**Files:**
- Modify: `src/claw_trade/ui_backend/report_queue.py`
- Modify: `src/claw_trade/web/state.py`
- Test: `tests/e2e/ui/test_license_gate_routes.py`
- Test: `tests/e2e/ui/test_report_user_flows.py`

- [ ] **Step 1: 写队列失败测试**

在 `tests/e2e/ui/test_license_gate_routes.py` 写一个最小测试 runner：

```python
import pytest

from claw_trade.ui_backend.report_queue import QueueError, ReportTaskQueue
from claw_trade.ui_backend.workflow_bridge import ReportWorkflowBridge


class _Runner:
    created = 0

    def create_run(self, request):  # type: ignore[no-untyped-def]
        self.created += 1
        return "run-1"


def _task_input() -> dict[str, object]:
    return {
        "instrumentCode": "600519",
        "instrumentName": "贵州茅台",
        "market": "CN_A",
        "companyName": "贵州茅台",
        "currencySymbol": "¥",
        "startDate": "2026-01-01",
        "endDate": "2026-06-01",
        "currentDate": "2026-06-01",
        "workflowSettings": {"defaultProfile": "CN_A"},
    }


def test_report_queue_blocks_enqueue_when_license_denied() -> None:
    def deny() -> None:
        raise PermissionError("设备授权已失效，请在授权页修复后重试。")

    runner = _Runner()
    queue = ReportTaskQueue(ReportWorkflowBridge(runner), report_permission_checker=deny)

    with pytest.raises(QueueError, match="设备授权已失效"):
        queue.enqueue_report_task(request_id="req-1", task_input=_task_input(), source="manual")

    assert runner.created == 0
```

- [ ] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/e2e/ui/test_license_gate_routes.py::test_report_queue_blocks_enqueue_when_license_denied -q
```

Expected: FAIL，原因是 `report_permission_checker` 参数不存在。

- [ ] **Step 3: 修改队列构造函数**

在 `src/claw_trade/ui_backend/report_queue.py`：

- `__init__` 增加 `report_permission_checker: Callable[[], None] | None = None`。
- 保存为 `self._report_permission_checker`。
- 新增私有方法：

```python
    def _assert_report_allowed(self) -> None:
        if self._report_permission_checker is None:
            return
        try:
            self._report_permission_checker()
        except PermissionError as exc:
            raise QueueError("LICENSE_BLOCKED", "license_blocked", str(exc)) from exc
```

- 在 `enqueue_report_task()` 获取锁前调用 `self._assert_report_allowed()`。
- 在 `start_next_report_task_if_idle()` 选出 queued task 后、改成 RUNNING 前调用 `self._assert_report_allowed()`；如果被阻断，把任务标记为 FAILED，并返回，不调用 workflow。

- [ ] **Step 4: 跑队列测试**

Run:

```bash
uv run pytest tests/e2e/ui/test_license_gate_routes.py::test_report_queue_blocks_enqueue_when_license_denied -q
```

Expected: PASS。

- [ ] **Step 5: 注入真实授权检查**

在 `src/claw_trade/web/state.py`：

- import `build_license_service`。
- `UiHttpServices` 增加 `license_service` 字段。
- `build_ui_http_services()` 里构建：

```python
license_service = build_license_service(os.environ)
```

- 创建 `ReportTaskQueue` 时传入：

```python
report_permission_checker=license_service.assert_report_generation_allowed
```

- `return UiHttpServices(...)` 增加 `license_service=license_service`。

- [ ] **Step 6: 跑相关测试**

Run:

```bash
uv run pytest tests/e2e/ui/test_license_gate_routes.py tests/e2e/ui/test_report_user_flows.py -q
```

Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add src/claw_trade/ui_backend/report_queue.py src/claw_trade/web/state.py tests/e2e/ui/test_license_gate_routes.py tests/e2e/ui/test_report_user_flows.py
git commit -m "feat: block report generation when license is invalid"
```

Expected: 授权失败不能进入报告 workflow。

---

### Task 6: 后端阻断数据刷新和维护

**目的：** 授权失效时禁止候选数据刷新、自动刷新和 data maintenance。

**Files:**
- Modify: `src/claw_trade/selection/refresh.py`
- Modify: `src/claw_trade/ui_backend/scheduled_work_runner.py`
- Modify: `src/claw_trade/web/state.py`
- Test: `tests/unit/selection/test_refresh_service.py`
- Test: `tests/integration/test_openclaw_cron_price_alert_contract.py`

- [ ] **Step 1: 写 selection refresh 授权失败测试**

在 `tests/unit/selection/test_refresh_service.py` 加：

```python
def test_selection_refresh_service_blocks_when_license_denied() -> None:
    def deny() -> None:
        raise PermissionError("设备授权已失效，请在授权页修复后重试。")

    service = _build_refresh_service(data_refresh_permission_checker=deny)
    result = service.run_automatic_refresh_once(reason="startup")

    assert result.status == "failed"
    assert result.error_code == "license_blocked"
```

如果现有 helper 不支持参数，先让测试失败。

- [ ] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/unit/selection/test_refresh_service.py::test_selection_refresh_service_blocks_when_license_denied -q
```

Expected: FAIL，原因是参数或行为不存在。

- [ ] **Step 3: 修改 `SelectionDataRefreshService`**

在 `src/claw_trade/selection/refresh.py`：

- `__init__` 增加 `data_refresh_permission_checker: Callable[[], None] | None = None`。
- 保存为 `self._data_refresh_permission_checker`。
- 新增：

```python
    def _assert_data_refresh_allowed(self) -> SelectionDataRefreshResult | None:
        if self._data_refresh_permission_checker is None:
            return None
        try:
            self._data_refresh_permission_checker()
        except PermissionError as exc:
            return SelectionDataRefreshResult(
                status="failed",
                selection_run_id=None,
                trade_date=None,
                reason=str(exc),
                error_code="license_blocked",
            )
        return None
```

- 在 `request_refresh()` 开始处调用；如果返回结果，直接返回。
- 在 `run_automatic_refresh_once()` 开始处调用；如果返回结果，直接返回。

- [ ] **Step 4: 跑 selection refresh 测试**

Run:

```bash
uv run pytest tests/unit/selection/test_refresh_service.py::test_selection_refresh_service_blocks_when_license_denied -q
```

Expected: PASS。

- [ ] **Step 5: 写 scheduled work 授权失败测试**

在 `tests/integration/test_openclaw_cron_price_alert_contract.py` 加：

```python
def test_scheduled_data_maintenance_blocks_when_license_denied() -> None:
    def deny() -> None:
        raise PermissionError("设备授权已失效，请在授权页修复后重试。")

    runner = ScheduledWorkRunner(data_maintenance_runner=object(), data_refresh_permission_checker=deny)

    with pytest.raises(ScheduledWorkRunnerError, match="设备授权已失效"):
        runner.handle_wake({"kind": "data_maintenance", "market": "CN_A", "jobKind": "kline-refresh"})
```

- [ ] **Step 6: 跑测试确认失败**

Run:

```bash
uv run pytest tests/integration/test_openclaw_cron_price_alert_contract.py::test_scheduled_data_maintenance_blocks_when_license_denied -q
```

Expected: FAIL，原因是 `data_refresh_permission_checker` 不存在。

- [ ] **Step 7: 修改 `ScheduledWorkRunner`**

在 `src/claw_trade/ui_backend/scheduled_work_runner.py`：

- `__init__` 增加 `data_refresh_permission_checker: Callable[[], None] | None = None`。
- 在 `_handle_selection_data_refresh()` 和 `_handle_data_maintenance()` 开始处调用 checker。
- 捕获 `PermissionError` 后抛出 `ScheduledWorkRunnerError("LICENSE_BLOCKED", str(exc))`。

- [ ] **Step 8: 注入真实授权检查**

在 `src/claw_trade/web/state.py`：

- 创建 `SelectionDataRefreshService` 时传入：

```python
data_refresh_permission_checker=license_service.assert_data_refresh_allowed
```

- 创建 `ScheduledWorkRunner` 时传入：

```python
data_refresh_permission_checker=license_service.assert_data_refresh_allowed
```

- [ ] **Step 9: 跑相关测试**

Run:

```bash
uv run pytest tests/unit/selection/test_refresh_service.py tests/integration/test_openclaw_cron_price_alert_contract.py -q
```

Expected: PASS。

- [ ] **Step 10: Commit**

```bash
git add src/claw_trade/selection/refresh.py src/claw_trade/ui_backend/scheduled_work_runner.py src/claw_trade/web/state.py tests/unit/selection/test_refresh_service.py tests/integration/test_openclaw_cron_price_alert_contract.py
git commit -m "feat: block data refresh when license is invalid"
```

Expected: 授权失败不能执行数据刷新或维护。

---

### Task 7: 授权状态 API 和 UI 提示

**目的：** 用户能看到授权状态；前端只展示和禁用按钮，后端仍是权威。

**Files:**
- Modify: `src/claw_trade/web/routes_ui.py`
- Modify: `web/research-ui/src/App.tsx`
- Modify: `web/research-ui/src/styles.css`
- Test: `tests/e2e/ui/test_license_gate_routes.py`

- [ ] **Step 1: 写 API 测试**

在 `tests/e2e/ui/test_license_gate_routes.py` 加：

```python
from types import SimpleNamespace

from fastapi import FastAPI
from fastapi.testclient import TestClient

from claw_trade.licensing.status import LicenseSnapshot, LicenseStatus
from claw_trade.web.routes_ui import router


class _LicenseService:
    def current_snapshot(self) -> LicenseSnapshot:
        return LicenseSnapshot(status=LicenseStatus.REVOKED, features=frozenset(), license_suffix="1234")


def test_license_status_route_returns_redacted_status() -> None:
    app = FastAPI()
    app.include_router(router, prefix="/api/ui")
    app.state.ui_services = SimpleNamespace(license_service=_LicenseService())
    client = TestClient(app)

    response = client.get("/api/ui/get-license-status")

    assert response.status_code == 200
    assert response.json()["status"] == "revoked"
    assert response.json()["licenseSuffix"] == "1234"
    assert "licenseCode" not in response.text
```

- [ ] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/e2e/ui/test_license_gate_routes.py::test_license_status_route_returns_redacted_status -q
```

Expected: FAIL，路由不存在。

- [ ] **Step 3: 新增路由**

在 `src/claw_trade/web/routes_ui.py` 添加：

```python
@router.get("/get-license-status")
def get_license_status(request: Request) -> JSONResponse:
    services = _services(request)
    try:
        snapshot = services.license_service.current_snapshot()
        return _success_response(
            {
                "status": snapshot.status.value,
                "allowsReportGeneration": snapshot.allows_report_generation(),
                "allowsDataRefresh": snapshot.allows_data_refresh(),
                "expiresAt": snapshot.expires_at,
                "graceUntil": snapshot.grace_until,
                "deviceIdHash": snapshot.device_id_hash,
                "licenseSuffix": snapshot.license_suffix,
                "message": snapshot.user_message,
            }
        )
    except Exception as exc:
        return _exception_response(exc)
```

- [ ] **Step 4: 跑 API 测试**

Run:

```bash
uv run pytest tests/e2e/ui/test_license_gate_routes.py::test_license_status_route_returns_redacted_status -q
```

Expected: PASS。

- [ ] **Step 5: 前端读取授权状态**

在 `web/research-ui/src/App.tsx`：

- 增加启动时 `fetch("/api/ui/get-license-status")`。
- 保存 `allowsReportGeneration`、`allowsDataRefresh`、`message`。
- 报告确认按钮、手动刷新按钮在对应权限为 false 时 disabled。
- 禁用文案显示后端 message。

不要在前端写“授权有效则允许生成报告”的业务判断；前端只禁用按钮改善体验。

- [ ] **Step 6: 前端样式**

在 `web/research-ui/src/styles.css` 增加授权状态条样式。保持工作台风格，不做大面积营销式卡片。

- [ ] **Step 7: 跑前端构建**

Run:

```bash
cd web/research-ui
pnpm build
```

Expected: PASS，`web/research-ui/dist/index.html` 更新。

- [ ] **Step 8: 跑 UI 相关测试**

Run:

```bash
uv run pytest tests/e2e/ui/test_license_gate_routes.py tests/e2e/ui/test_research_ui_server.py -q
```

Expected: PASS。

- [ ] **Step 9: Commit**

```bash
git add src/claw_trade/web/routes_ui.py web/research-ui/src/App.tsx web/research-ui/src/styles.css web/research-ui/dist tests/e2e/ui/test_license_gate_routes.py
git commit -m "feat: expose license status in research ui"
```

Expected: UI 能显示授权状态，但后端仍阻断真实操作。

---

### Task 8: 生产路径和 preflight

**目的：** 把生产目录约束变成可运行检查，先在开发机模拟 `/opt/claw-trade/current`。

**Files:**
- Create: `src/claw_trade/production/__init__.py`
- Create: `src/claw_trade/production/paths.py`
- Create: `src/claw_trade/production/preflight.py`
- Create: `src/claw_trade/cli/production_preflight.py`
- Test: `tests/unit/production/test_paths.py`
- Test: `tests/unit/production/test_preflight.py`

- [ ] **Step 1: 写路径测试**

在 `tests/unit/production/test_paths.py` 写：

```python
from pathlib import Path

from claw_trade.production.paths import ProductionPaths


def test_production_paths_resolve_current_and_shared(tmp_path: Path) -> None:
    root = tmp_path / "opt" / "claw-trade"
    paths = ProductionPaths.from_root(root)

    assert paths.current == root / "current"
    assert paths.shared == root / "shared"
    assert paths.config == root / "shared" / "config"
    assert paths.logs == root / "shared" / "logs"
```

- [ ] **Step 2: 写 preflight 测试**

在 `tests/unit/production/test_preflight.py` 写：

```python
from pathlib import Path

from claw_trade.production.paths import ProductionPaths
from claw_trade.production.preflight import run_preflight


def test_preflight_fails_when_frontend_dist_missing(tmp_path: Path) -> None:
    paths = ProductionPaths.from_root(tmp_path / "opt" / "claw-trade")
    (paths.current / "bin").mkdir(parents=True)
    (paths.current / "runtime" / "bin").mkdir(parents=True)
    (paths.shared / "config").mkdir(parents=True)
    result = run_preflight(paths)

    assert not result.ok
    assert any(item.code == "frontend_dist_missing" for item in result.failures)
```

- [ ] **Step 3: 跑测试确认失败**

Run:

```bash
uv run pytest tests/unit/production/test_paths.py tests/unit/production/test_preflight.py -q
```

Expected: FAIL，生产模块不存在。

- [ ] **Step 4: 实现 `ProductionPaths`**

在 `src/claw_trade/production/paths.py` 实现：

```python
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path


@dataclass(frozen=True)
class ProductionPaths:
    root: Path
    current: Path
    shared: Path
    config: Path
    data: Path
    logs: Path
    license: Path
    reports: Path
    updates: Path

    @classmethod
    def from_root(cls, root: Path) -> "ProductionPaths":
        shared = root / "shared"
        return cls(
            root=root,
            current=root / "current",
            shared=shared,
            config=shared / "config",
            data=shared / "data",
            logs=shared / "logs",
            license=shared / "license",
            reports=shared / "reports",
            updates=shared / "updates",
        )
```

- [ ] **Step 5: 实现 preflight**

在 `src/claw_trade/production/preflight.py` 实现：

```python
from __future__ import annotations

from dataclasses import dataclass

from claw_trade.production.paths import ProductionPaths


@dataclass(frozen=True)
class PreflightFailure:
    code: str
    message: str


@dataclass(frozen=True)
class PreflightResult:
    ok: bool
    failures: tuple[PreflightFailure, ...]


def run_preflight(paths: ProductionPaths) -> PreflightResult:
    failures: list[PreflightFailure] = []
    _require_dir(paths.current, "current_missing", failures)
    _require_dir(paths.current / "bin", "bin_missing", failures)
    _require_file(paths.current / "web" / "dist" / "index.html", "frontend_dist_missing", failures)
    _require_file(paths.current / "runtime" / "bin" / "claw-trade-control-runtime", "control_runtime_missing", failures)
    _require_file(paths.current / "runtime" / "openclaw" / "openclaw.mjs", "openclaw_launcher_missing", failures)
    _require_dir(paths.current / "runtime" / "openclaw" / "dist", "openclaw_dist_missing", failures)
    _require_dir(paths.config, "config_missing", failures)
    _require_dir(paths.data, "data_missing", failures)
    _require_dir(paths.logs, "logs_missing", failures)
    return PreflightResult(ok=not failures, failures=tuple(failures))


def _require_dir(path, code: str, failures: list[PreflightFailure]) -> None:
    if not path.is_dir():
        failures.append(PreflightFailure(code, f"目录不存在：{path}"))


def _require_file(path, code: str, failures: list[PreflightFailure]) -> None:
    if not path.is_file():
        failures.append(PreflightFailure(code, f"文件不存在：{path}"))
```

- [ ] **Step 6: 实现 CLI**

在 `src/claw_trade/cli/production_preflight.py` 实现：

```python
from __future__ import annotations

import argparse
from pathlib import Path

from claw_trade.production.paths import ProductionPaths
from claw_trade.production.preflight import run_preflight


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--root", default="/opt/claw-trade")
    args = parser.parse_args(argv)
    result = run_preflight(ProductionPaths.from_root(Path(args.root)))
    for failure in result.failures:
        print(f"{failure.code}: {failure.message}")
    return 0 if result.ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 7: 跑测试确认通过**

Run:

```bash
uv run pytest tests/unit/production/test_paths.py tests/unit/production/test_preflight.py -q
```

Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add src/claw_trade/production src/claw_trade/cli/production_preflight.py tests/unit/production
git commit -m "feat: add production path preflight"
```

Expected: 生产目录检查可独立运行。

---

### Task 9: 最小生产启动入口模板

**目的：** 创建生产入口模板，systemd 后续只调用这些入口，不直接调用开发脚本。

**Files:**
- Create: `packaging/production/bin/claw-trade-control`
- Create: `packaging/production/bin/claw-trade-ui`
- Create: `packaging/production/bin/claw-trade-preflight`
- Create: `packaging/production/runtime/claw-trade-control-runtime`
- Modify: `src/claw_trade/web/app.py`
- Test: `tests/unit/production/test_preflight.py`

- [ ] **Step 1: 写入口文件**

`packaging/production/bin/claw-trade-preflight`：

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="${CLAW_TRADE_HOME:-/opt/claw-trade}"
exec "${ROOT}/current/runtime/python/bin/python" -m claw_trade.cli.production_preflight --root "${ROOT}"
```

`packaging/production/bin/claw-trade-ui`：

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="${CLAW_TRADE_HOME:-/opt/claw-trade}"
CURRENT="${ROOT}/current"
SHARED="${ROOT}/shared"
export CLAW_TRADE_LICENSE_REQUIRED="${CLAW_TRADE_LICENSE_REQUIRED:-1}"
export CLAW_TRADE_REPORT_RUN_DIR="${CLAW_TRADE_REPORT_RUN_DIR:-${SHARED}/reports/runs}"
exec "${CURRENT}/runtime/python/bin/python" -m claw_trade.web.app \
  --host "${CLAW_TRADE_UI_HOST:-127.0.0.1}" \
  --port "${CLAW_TRADE_UI_PORT:-5175}" \
  --frontend-dist "${CURRENT}/web/dist" \
  --gateway-call-bin "${CURRENT}/runtime/openclaw/openclaw.mjs" \
  --gateway-ws-url "${OPENCLAW_GATEWAY_URL:-ws://127.0.0.1:18789}"
```

`packaging/production/bin/claw-trade-control`：

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="${CLAW_TRADE_HOME:-/opt/claw-trade}"
CURRENT="${ROOT}/current"
SHARED="${ROOT}/shared"
mkdir -p "${SHARED}/logs" "${SHARED}/data/openviking" "${SHARED}/data/openclaw-state"
exec "${CURRENT}/runtime/bin/claw-trade-control-runtime" "$@"
```

说明：`claw-trade-control-runtime` 在 Task 10 打包时生成；它启动 OpenViking 和 OpenClaw gateway，不调用 `scripts/start-control-runtime.sh`。

`packaging/production/runtime/claw-trade-control-runtime`：

```bash
#!/usr/bin/env bash
set -euo pipefail

ROOT="${CLAW_TRADE_HOME:-/opt/claw-trade}"
CURRENT="${ROOT}/current"
SHARED="${ROOT}/shared"
PYTHON="${CURRENT}/runtime/python/bin/python"
OPENCLAW="${CURRENT}/runtime/openclaw/openclaw.mjs"
LOG_DIR="${SHARED}/logs"

export OPENVIKING_ENDPOINT="${OPENVIKING_ENDPOINT:-http://127.0.0.1:1933}"
export OPENVIKING_BASE_URL="${OPENVIKING_BASE_URL:-${OPENVIKING_ENDPOINT}}"
export OPENVIKING_CONFIG_FILE="${OPENVIKING_CONFIG_FILE:-${SHARED}/config/openviking/ov.conf}"
export OPENVIKING_DATA_DIR="${OPENVIKING_DATA_DIR:-${SHARED}/data/openviking}"
export OPENVIKING_WRITE_LOCK_PATH="${OPENVIKING_WRITE_LOCK_PATH:-${OPENVIKING_DATA_DIR}/write.lock}"
export OPENCLAW_STATE_DIR="${OPENCLAW_STATE_DIR:-${SHARED}/data/openclaw-state}"
export OPENCLAW_CONFIG_PATH="${OPENCLAW_CONFIG_PATH:-${OPENCLAW_STATE_DIR}/openclaw.json}"
export OPENCLAW_MARKET_TOOL_PYTHON="${OPENCLAW_MARKET_TOOL_PYTHON:-${PYTHON}}"
export OPENCLAW_GATEWAY_PORT="${OPENCLAW_GATEWAY_PORT:-18789}"
export OPENCLAW_GATEWAY_URL="${OPENCLAW_GATEWAY_URL:-ws://127.0.0.1:${OPENCLAW_GATEWAY_PORT}}"

mkdir -p "${LOG_DIR}" "$(dirname "${OPENVIKING_CONFIG_FILE}")" "${OPENVIKING_DATA_DIR}" "${OPENCLAW_STATE_DIR}"

cleanup() {
  jobs -pr | xargs -r kill
}
trap cleanup EXIT INT TERM

wait_http_any() {
  local timeout="$1"
  shift
  for _ in $(seq 1 "${timeout}"); do
    for url in "$@"; do
      if curl -fsS "${url}" >/dev/null 2>&1; then
        return 0
      fi
    done
    sleep 1
  done
  return 1
}

"${PYTHON}" -m claw_trade.runtime.openviking_report_server \
  --config "${OPENVIKING_CONFIG_FILE}" \
  --host 127.0.0.1 \
  --port 1933 >"${LOG_DIR}/openviking.log" 2>&1 &

wait_http_any 90 "${OPENVIKING_ENDPOINT}/health" "${OPENVIKING_ENDPOINT}/healthz" || {
  echo "OpenViking health check failed" >&2
  exit 1
}

gateway_cmd=("${OPENCLAW}" gateway run --port "${OPENCLAW_GATEWAY_PORT}" --force --bind loopback)
if [[ -n "${OPENCLAW_GATEWAY_TOKEN:-}" ]]; then
  gateway_cmd+=(--auth token --token "${OPENCLAW_GATEWAY_TOKEN}")
else
  gateway_cmd+=(--auth none)
fi

OPENCLAW_STATE_DIR="${OPENCLAW_STATE_DIR}" \
OPENCLAW_CONFIG_PATH="${OPENCLAW_CONFIG_PATH}" \
OPENCLAW_MARKET_TOOL_PYTHON="${OPENCLAW_MARKET_TOOL_PYTHON}" \
OPENVIKING_ENDPOINT="${OPENVIKING_ENDPOINT}" \
OPENVIKING_BASE_URL="${OPENVIKING_BASE_URL}" \
OPENVIKING_WRITE_LOCK_PATH="${OPENVIKING_WRITE_LOCK_PATH}" \
  "${gateway_cmd[@]}" >"${LOG_DIR}/openclaw-gateway.log" 2>&1 &

wait_http_any 90 "http://127.0.0.1:${OPENCLAW_GATEWAY_PORT}/health" || {
  echo "OpenClaw gateway health check failed" >&2
  exit 1
}

wait -n
```

- [ ] **Step 2: 让入口可执行**

Run:

```bash
chmod +x packaging/production/bin/claw-trade-control packaging/production/bin/claw-trade-ui packaging/production/bin/claw-trade-preflight packaging/production/runtime/claw-trade-control-runtime
```

Expected: 三个入口和 control runtime 可执行。

- [ ] **Step 3: 修改 `web/app.py` 生产 token 读取**

在 `src/claw_trade/web/app.py`：

- 保留 `_runtime_env_value()` 仅开发模式使用。
- 新增判断：如果 `CLAW_TRADE_PRODUCTION=1`，`--gateway-token` 只能来自环境变量或显式参数，不能读 `.runtime/dev-services/runtime.env`。

- [ ] **Step 4: 写测试**

在 `tests/unit/production/test_preflight.py` 加一个脚本存在性测试：

```python
from pathlib import Path


def test_production_entry_templates_exist_and_are_executable() -> None:
    for path in [
        Path("packaging/production/bin/claw-trade-control"),
        Path("packaging/production/bin/claw-trade-ui"),
        Path("packaging/production/bin/claw-trade-preflight"),
        Path("packaging/production/runtime/claw-trade-control-runtime"),
    ]:
        assert path.is_file()
        assert path.stat().st_mode & 0o111
```

- [ ] **Step 5: 跑测试**

Run:

```bash
uv run pytest tests/unit/production/test_preflight.py -q
```

Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add packaging/production/bin packaging/production/runtime src/claw_trade/web/app.py tests/unit/production/test_preflight.py
git commit -m "feat: add production entrypoint templates"
```

Expected: 生产入口存在，未接 systemd。

---

### Task 10: 构建最小生产包

**目的：** 生成 release tar，内容满足第一版最小样机：无开发仓库、可安装到 `/opt/claw-trade/releases/<version>`。

**前置条件：** 虽然本任务编号是 Task 10，但执行时必须先完成 Task 11 的 systemd/kiosk 模板；生产包构建脚本会强制复制这些模板，缺失时必须失败。

**Files:**
- Create: `scripts/production/build_production_package.sh`
- Create: `scripts/production/audit_production_package.py`
- Test: `tests/unit/production/test_package_audit.py`

- [x] **Step 1: 写包审计失败测试**

在 `tests/unit/production/test_package_audit.py` 写：

```python
import tarfile
from pathlib import Path

from scripts.production.audit_production_package import audit_archive


def test_package_audit_rejects_git_directory(tmp_path: Path) -> None:
    archive = tmp_path / "bad.tar"
    payload = tmp_path / "payload"
    (payload / ".git").mkdir(parents=True)
    (payload / ".git" / "HEAD").write_text("ref: refs/heads/main\n", encoding="utf-8")
    with tarfile.open(archive, "w") as tar:
        tar.add(payload, arcname="claw-trade")

    result = audit_archive(archive)

    assert not result.ok
    assert ".git" in result.forbidden_hits[0]
```

- [x] **Step 2: 跑测试确认失败**

Run:

```bash
uv run pytest tests/unit/production/test_package_audit.py -q
```

Expected: FAIL，审计脚本不存在。

- [x] **Step 3: 写审计脚本**

在 `scripts/production/audit_production_package.py` 实现：

```python
from __future__ import annotations

import argparse
import tarfile
from dataclasses import dataclass
from pathlib import Path


FORBIDDEN_PARTS = {
    ".git",
    "tests",
    "docs",
    ".env.local",
    "web/research-ui/src",
}


@dataclass(frozen=True)
class AuditResult:
    ok: bool
    forbidden_hits: tuple[str, ...]


def audit_archive(path: Path) -> AuditResult:
    hits: list[str] = []
    with tarfile.open(path) as tar:
        for member in tar.getmembers():
            name = member.name.strip("/")
            parts = set(name.split("/"))
            if parts & {".git", "tests", "docs"}:
                hits.append(name)
            if name.endswith(".env.local") or "web/research-ui/src/" in f"{name}/":
                hits.append(name)
    return AuditResult(ok=not hits, forbidden_hits=tuple(hits))


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("archive")
    args = parser.parse_args(argv)
    result = audit_archive(Path(args.archive))
    for hit in result.forbidden_hits:
        print(f"forbidden: {hit}")
    return 0 if result.ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [x] **Step 4: 跑审计测试**

Run:

```bash
uv run pytest tests/unit/production/test_package_audit.py -q
```

Expected: PASS。

- [x] **Step 5: 写构建脚本**

在 `scripts/production/build_production_package.sh` 实现：

```bash
#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
VERSION="${CLAW_TRADE_VERSION:-$(date +%Y%m%d%H%M%S)}"
BUILD_DIR="${ROOT}/.runtime/production-build/${VERSION}"
RELEASE_DIR="${BUILD_DIR}/claw-trade-${VERSION}"
ARCHIVE="${ROOT}/.runtime/production-build/claw-trade-${VERSION}.tar"

rm -rf "${BUILD_DIR}"
mkdir -p "${RELEASE_DIR}/app" "${RELEASE_DIR}/web" "${RELEASE_DIR}/runtime/bin" "${RELEASE_DIR}/bin"

cd "${ROOT}/web/research-ui"
pnpm build

cd "${ROOT}"
python -m compileall -q src/claw_trade
python -m pip wheel . -w "${BUILD_DIR}/wheels"

python -m venv "${RELEASE_DIR}/runtime/python"
"${RELEASE_DIR}/runtime/python/bin/python" -m pip install --no-index --find-links "${BUILD_DIR}/wheels" claw-trade

cp -a web/research-ui/dist "${RELEASE_DIR}/web/dist"
cp -a packaging/production/bin/. "${RELEASE_DIR}/bin/"
cp -a packaging/production/runtime/claw-trade-control-runtime "${RELEASE_DIR}/runtime/bin/claw-trade-control-runtime"
cp -a packaging/production/systemd "${RELEASE_DIR}/runtime/systemd"
cp -a packaging/production/kiosk "${RELEASE_DIR}/runtime/kiosk"
mkdir -p "${RELEASE_DIR}/runtime/openclaw"
cp -a third_party/openclaw/openclaw.mjs "${RELEASE_DIR}/runtime/openclaw/openclaw.mjs"
cp -a third_party/openclaw/dist "${RELEASE_DIR}/runtime/openclaw/dist"

tar -C "${BUILD_DIR}" -cf "${ARCHIVE}" "claw-trade-${VERSION}"
uv run python scripts/production/audit_production_package.py "${ARCHIVE}"
echo "${ARCHIVE}"
```

如果 `third_party/openclaw/dist` 不存在，构建脚本必须失败，并提示先构建 OpenClaw。不要现场 `pnpm build` OpenClaw。

- [x] **Step 6: 让脚本可执行**

Run:

```bash
chmod +x scripts/production/build_production_package.sh scripts/production/audit_production_package.py
```

Expected: 脚本可执行。

- [x] **Step 7: 跑包构建**

Run:

```bash
scripts/production/build_production_package.sh
```

Expected:

- 退出码 `0`。
- 输出 `.runtime/production-build/claw-trade-<version>.tar`。
- 审计通过。
- 包内无 `.git`、`tests`、`docs`、`.env.local`、`web/research-ui/src`。

- [x] **Step 8: 写审计记录**

在 `docs/evidence/工控机第一版生产包审计记录.md` 写入：

```markdown
# 工控机第一版生产包审计记录

- 构建命令：
- archive 路径：
- archive sha256：
- 审计命令：
- 审计结论：
- 禁止项检查：
  - `.git`：
  - `tests/`：
  - `docs/`：
  - `.env.local`：
  - `web/research-ui/src/`：
```

- [x] **Step 9: Commit**

```bash
git add scripts/production/build_production_package.sh scripts/production/audit_production_package.py tests/unit/production/test_package_audit.py docs/evidence/工控机第一版生产包审计记录.md
git commit -m "feat: build minimal production package"
```

Expected: 可重复构建最小生产包。

---

### Task 11: 最小 systemd 和 kiosk 草案

**目的：** 先给 VM 样机用的 unit 和 kiosk policy，不做完整安全恢复模式。

**Files:**
- Create: `packaging/production/systemd/claw-trade-control.service`
- Create: `packaging/production/systemd/claw-trade-ui.service`
- Create: `packaging/production/systemd/claw-trade-kiosk.service`
- Create: `packaging/production/kiosk/chromium-policy.json`

- [ ] **Step 1: 写 control unit**

`packaging/production/systemd/claw-trade-control.service`：

```ini
[Unit]
Description=claw-trade control runtime
After=network-online.target mongod.service
Wants=network-online.target

[Service]
Type=simple
User=clawtrade
Group=clawtrade
WorkingDirectory=/opt/claw-trade/current
EnvironmentFile=-/opt/claw-trade/shared/config/runtime.env
ExecStart=/opt/claw-trade/current/bin/claw-trade-control
Restart=on-failure
RestartSec=5
NoNewPrivileges=true
PrivateTmp=true
ReadWritePaths=/opt/claw-trade/shared

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 2: 写 UI unit**

`packaging/production/systemd/claw-trade-ui.service`：

```ini
[Unit]
Description=claw-trade UI backend
After=claw-trade-control.service
Requires=claw-trade-control.service

[Service]
Type=simple
User=clawtrade
Group=clawtrade
WorkingDirectory=/opt/claw-trade/current
EnvironmentFile=-/opt/claw-trade/shared/config/runtime.env
ExecStart=/opt/claw-trade/current/bin/claw-trade-ui
Restart=on-failure
RestartSec=5
NoNewPrivileges=true
PrivateTmp=true
ReadWritePaths=/opt/claw-trade/shared

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 3: 写 kiosk unit**

`packaging/production/systemd/claw-trade-kiosk.service`：

```ini
[Unit]
Description=claw-trade kiosk browser
After=graphical.target claw-trade-ui.service
Requires=claw-trade-ui.service

[Service]
Type=simple
User=clawkiosk
Group=clawkiosk
Environment=DISPLAY=:0
ExecStart=/usr/bin/chromium --kiosk --no-first-run --disable-extensions --disable-dev-tools --disable-file-system --user-data-dir=/home/clawkiosk/.config/claw-trade-browser http://127.0.0.1:5175/
Restart=always
RestartSec=3
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=graphical.target
```

- [ ] **Step 4: 写 Chromium policy 草案**

`packaging/production/kiosk/chromium-policy.json`：

```json
{
  "DeveloperToolsAvailability": 2,
  "DownloadRestrictions": 3,
  "ExtensionInstallBlocklist": ["*"],
  "HomepageLocation": "http://127.0.0.1:5175/",
  "RestoreOnStartup": 4,
  "RestoreOnStartupURLs": ["http://127.0.0.1:5175/"]
}
```

- [ ] **Step 5: systemd 静态检查**

Run:

```bash
systemd-analyze verify packaging/production/systemd/claw-trade-control.service packaging/production/systemd/claw-trade-ui.service packaging/production/systemd/claw-trade-kiosk.service
```

Expected: 无语法错误。若本机无 systemd，记录为“未运行”，在 VM 中补跑。

- [ ] **Step 6: Commit**

```bash
git add packaging/production/systemd packaging/production/kiosk
git commit -m "feat: add first production systemd and kiosk templates"
```

Expected: unit 和 policy 模板进入生产包。

---

### Task 12: VM 安装和 `/opt` 启动验收

**目的：** 在干净 Ubuntu/VM 上证明生产包可启动，不依赖开发仓库。

**Files:**
- Create: `scripts/production/install_production_package.sh`
- Create: `docs/evidence/工控机第一版VM启动验收记录.md`

- [ ] **Step 1: 写安装脚本**

`scripts/production/install_production_package.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail

ARCHIVE="${1:?usage: install_production_package.sh <archive.tar>}"
VERSION="${2:?usage: install_production_package.sh <archive.tar> <version>}"
ROOT="${CLAW_TRADE_HOME:-/opt/claw-trade}"

sudo mkdir -p "${ROOT}/releases" "${ROOT}/shared/config" "${ROOT}/shared/data" "${ROOT}/shared/logs" "${ROOT}/shared/license" "${ROOT}/shared/reports" "${ROOT}/shared/updates"
sudo tar -C "${ROOT}/releases" -xf "${ARCHIVE}"
sudo ln -sfn "${ROOT}/releases/claw-trade-${VERSION}" "${ROOT}/current.next"
sudo mv -Tf "${ROOT}/current.next" "${ROOT}/current"
sudo chmod +x "${ROOT}/current/bin/"*
sudo chown -R clawtrade:clawtrade "${ROOT}/shared"
```

- [ ] **Step 2: 让脚本可执行**

Run:

```bash
chmod +x scripts/production/install_production_package.sh
```

Expected: 脚本可执行。

- [ ] **Step 3: 确认使用 Task 0 创建的 VM**

在宿主机运行：

```bash
VM_IP="$(virsh domifaddr claw-trade-prod-vm | awk '/ipv4/ {split($4,a,"/"); print a[1]; exit}')"
test -n "${VM_IP}"
ssh deploy@"${VM_IP}" '
set -euo pipefail
id clawtrade
id clawkiosk
python3.12 --version
curl --version
'
```

Expected: Task 0 创建的 `claw-trade-prod-vm` 可用，基础依赖和服务用户存在。若这一步失败，回到 Task 0 修 VM，不在这里临时补环境。

- [ ] **Step 4: 从宿主机复制生产包和安装脚本到 VM**

在宿主机运行：

```bash
VM_IP="$(virsh domifaddr claw-trade-prod-vm | awk '/ipv4/ {split($4,a,"/"); print a[1]; exit}')"
test -n "${VM_IP}"
ARCHIVE="$(ls -1t .runtime/production-build/claw-trade-*.tar | head -n 1)"
test -f "${ARCHIVE}"
VERSION="$(basename "${ARCHIVE}" .tar)"
VERSION="${VERSION#claw-trade-}"
printf 'ARCHIVE=%s\nVERSION=%s\n' "${ARCHIVE}" "${VERSION}"
scp "${ARCHIVE}" deploy@"${VM_IP}":/tmp/
scp scripts/production/install_production_package.sh deploy@"${VM_IP}":/tmp/
```

Expected:

- VM 的 `/tmp/claw-trade-${VERSION}.tar` 存在。
- VM 的 `/tmp/install_production_package.sh` 存在。

- [ ] **Step 5: 在 VM 内安装生产包**

在宿主机运行：

```bash
VM_IP="$(virsh domifaddr claw-trade-prod-vm | awk '/ipv4/ {split($4,a,"/"); print a[1]; exit}')"
test -n "${VM_IP}"
ssh deploy@"${VM_IP}" '
set -euo pipefail
ARCHIVE="$(ls -1t /tmp/claw-trade-*.tar | head -n 1)"
test -f "${ARCHIVE}"
VERSION="$(basename "${ARCHIVE}" .tar)"
VERSION="${VERSION#claw-trade-}"
chmod +x /tmp/install_production_package.sh
/tmp/install_production_package.sh "${ARCHIVE}" "${VERSION}"
'
```

Expected:

- `/opt/claw-trade/current` 指向 release。
- `/opt/claw-trade/shared/*` 目录存在。
- `/home/frank/src/claw-trade` 不存在。

- [ ] **Step 6: 从宿主机复制并安装 Virbox runtime/SDK 和已验证探针**

在宿主机运行：

```bash
VM_IP="$(virsh domifaddr claw-trade-prod-vm | awk '/ipv4/ {split($4,a,"/"); print a[1]; exit}')"
test -n "${VM_IP}"
VIRBOX_VENDOR_DIR="${VIRBOX_VENDOR_DIR:-$HOME/secure-vendor/virbox/linux-x86_64}"
test -d "${VIRBOX_VENDOR_DIR}/runtime"
test -d "${VIRBOX_VENDOR_DIR}/sdk"
tar -C "${VIRBOX_VENDOR_DIR}" -czf /tmp/virbox-linux-x86_64-runtime-sdk.tgz runtime sdk examples docs
VIRBOX_PROBE="$PWD/techlab/virbox_probe/virbox_probe.py"
test -x "${VIRBOX_PROBE}"
scp /tmp/virbox-linux-x86_64-runtime-sdk.tgz deploy@"${VM_IP}":/tmp/
scp "${VIRBOX_PROBE}" deploy@"${VM_IP}":/tmp/virbox_probe
```

在宿主机运行：

```bash
VM_IP="$(virsh domifaddr claw-trade-prod-vm | awk '/ipv4/ {split($4,a,"/"); print a[1]; exit}')"
test -n "${VM_IP}"
ssh deploy@"${VM_IP}" 'bash -s' <<'REMOTE'
set -euo pipefail
sudo mkdir -p /opt/virbox/linux-x86_64
sudo tar -C /opt/virbox/linux-x86_64 -xzf /tmp/virbox-linux-x86_64-runtime-sdk.tgz
sudo chown -R root:root /opt/virbox

VIRBOX_INSTALLER="$(
  find /opt/virbox/linux-x86_64/runtime -maxdepth 4 -type f \
    \( -name '*.deb' -o -name '*.run' -o -name 'install.sh' -o -name 'setup.sh' \) \
    | sort | head -n 1
)"
test -n "${VIRBOX_INSTALLER}"

case "${VIRBOX_INSTALLER}" in
  *.deb)
    sudo apt-get install -y "${VIRBOX_INSTALLER}"
    ;;
  *.run|*/install.sh|*/setup.sh)
    sudo chmod +x "${VIRBOX_INSTALLER}"
    sudo "${VIRBOX_INSTALLER}"
    ;;
  *)
    echo "unsupported Virbox runtime installer: ${VIRBOX_INSTALLER}" >&2
    exit 1
    ;;
esac

find /opt/virbox/linux-x86_64 -maxdepth 5 -type f -perm -111 -print
if command -v virbox >/dev/null 2>&1; then
  virbox --version || virbox --help
fi
sudo install -m 0750 -o root -g clawtrade /tmp/virbox_probe /opt/claw-trade/shared/license/virbox_probe
/opt/claw-trade/shared/license/virbox_probe
REMOTE
```

Expected:

- Virbox 官方 runtime/SDK 已在 VM 的 `/opt/virbox/linux-x86_64` 可见。
- Virbox 官方 runtime 安装包或安装脚本已经成功执行；如果官方安装器需要交互式界面，停止并向 Virbox 要 Linux server 非交互安装方法。
- 如果官方安装器暴露 `virbox` CLI，已打印版本或帮助信息。
- 探针存在且可执行。
- 输出脱敏 JSON。
- 输出状态与 `docs/evidence/工控机第一版Virbox验证记录.md` 中同一场景一致。
- 不输出完整授权码、API KEY、token、cookie 或 session。

- [ ] **Step 7: 写运行配置**

Run in VM:

```bash
sudo tee /opt/claw-trade/shared/config/runtime.env >/dev/null <<'EOF'
CLAW_TRADE_PRODUCTION=1
CLAW_TRADE_LICENSE_REQUIRED=1
CLAW_TRADE_VIRBOX_STATUS_COMMAND=/opt/claw-trade/shared/license/virbox_probe
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789
CLAW_TRADE_UI_HOST=127.0.0.1
CLAW_TRADE_UI_PORT=5175
EOF
```

Expected: 配置不包含客户 API KEY 明文示例。

- [ ] **Step 8: 跑 preflight**

Run in VM:

```bash
/opt/claw-trade/current/bin/claw-trade-preflight
```

Expected: exit code `0`。失败则修生产包，不绕过。

- [ ] **Step 9: 启动 control runtime**

Run in VM:

```bash
sudo sh -c 'nohup /opt/claw-trade/current/bin/claw-trade-control >/opt/claw-trade/shared/logs/claw-trade-control-wrapper.log 2>&1 & echo $! >/opt/claw-trade/shared/logs/claw-trade-control.pid'
```

Expected:

- `http://127.0.0.1:1933/health` 或 `/healthz` 返回成功。
- `http://127.0.0.1:18789/health` 返回成功。
- 日志写入 `/opt/claw-trade/shared/logs/openviking.log` 和 `/opt/claw-trade/shared/logs/openclaw-gateway.log`。

- [ ] **Step 10: 启动 UI**

Run in VM:

```bash
sudo sh -c 'nohup /opt/claw-trade/current/bin/claw-trade-ui >/opt/claw-trade/shared/logs/claw-trade-ui.log 2>&1 & echo $! >/opt/claw-trade/shared/logs/claw-trade-ui.pid'
```

Expected: `http://127.0.0.1:5175/healthz` 返回 `{"status":"ok"}`。

- [ ] **Step 11: 验证无开发依赖**

Run in VM:

```bash
test ! -d /home/frank/src/claw-trade
test ! -e /opt/claw-trade/current/.git
test ! -d /opt/claw-trade/current/tests
test ! -d /opt/claw-trade/current/docs
test ! -d /opt/claw-trade/current/web/research-ui/src
```

Expected: 全部通过。

- [ ] **Step 12: 写 VM 验收记录**

在 `docs/evidence/工控机第一版VM启动验收记录.md` 写：

```markdown
# 工控机第一版 VM 启动验收记录

- Ubuntu 版本：
- archive：
- sha256：
- 安装命令：
- preflight 结果：
- control runtime health：
- UI healthz：
- Virbox 探针路径和脱敏输出：
- 禁止目录检查：
- systemd verify：
- 已知未覆盖：
```

- [ ] **Step 13: Commit**

```bash
git add scripts/production/install_production_package.sh docs/evidence/工控机第一版VM启动验收记录.md
git commit -m "docs: record first production vm startup proof"
```

Expected: VM 证明完整记录。

---

### Task 13: 授权阻断验收

**目的：** 在生产样机上证明未授权不能生成报告或刷新数据；已授权能进入真实报告路径。

**Files:**
- Modify: `docs/evidence/工控机第一版VM启动验收记录.md`

- [ ] **Step 1: 准备未授权 Virbox 状态**

Run in VM:

```bash
/opt/claw-trade/shared/license/virbox_probe
```

Expected: 输出 `normalized_status=not_activated` 或 `revoked`。

- [ ] **Step 2: 查询 UI 授权状态**

Run in VM:

```bash
curl -s http://127.0.0.1:5175/api/ui/get-license-status
```

Expected:

- `allowsReportGeneration=false`
- `allowsDataRefresh=false`
- 不包含完整授权码。

- [ ] **Step 3: 尝试生成报告**

通过 UI 或 API 触发 `/report` 确认。

Expected:

- 后端返回 `LICENSE_BLOCKED`。
- 没有新 run 目录。
- 没有 OpenClaw worker wake。

- [ ] **Step 4: 尝试数据刷新**

通过 UI 或内部命令触发 `/select refresh` 或 scheduled data refresh。

Expected:

- 后端返回 `LICENSE_BLOCKED` 或 selection refresh `failed/license_blocked`。
- 没有启动新的数据 job。

- [ ] **Step 5: 激活授权后重复报告**

使用 Virbox 官方流程激活测试授权后：

```bash
curl -s http://127.0.0.1:5175/api/ui/get-license-status
```

Expected:

- `allowsReportGeneration=true`
- `allowsDataRefresh=true`

触发一个最小 `/report`。

Expected: 能进入报告队列并创建 workflow run。

- [ ] **Step 6: 记录验收结果**

在 `docs/evidence/工控机第一版VM启动验收记录.md` 追加：

```markdown
## 授权阻断验收

| 场景 | 预期 | 实际 | 证据 |
| --- | --- | --- | --- |
| 未授权查询状态 | 不允许 report/data_refresh | | |
| 未授权报告 | LICENSE_BLOCKED，无 worker wake | | |
| 未授权数据刷新 | LICENSE_BLOCKED，无 data job | | |
| 已授权报告 | 创建 workflow run | | |
| 已授权数据刷新 | 可启动 data refresh | | |
```

- [ ] **Step 7: Commit**

```bash
git add docs/evidence/工控机第一版VM启动验收记录.md
git commit -m "docs: record production license gate acceptance"
```

Expected: 授权阻断证据完整。

---

### Task 14: OpenClaw 边界验收

**目的：** 证明生产样机没有破坏项目核心边界。

**Files:**
- Create: `docs/evidence/工控机第一版OpenClaw边界验收记录.md`

- [ ] **Step 1: 确认 runtime profile**

生产样机记录：

```text
OpenViking endpoint:
OpenClaw gateway:
OPENCLAW_GATEWAY_URL:
OPENCLAW_GATEWAY_CALL_BIN:
CLAW_TRADE_REPORT_RUN_DIR:
CLAW_TRADE_LICENSE_REQUIRED:
```

Expected: 路径都在 `/opt/claw-trade` 或受控系统依赖中，不指向开发仓库。

- [ ] **Step 2: 执行授权有效 `/report`**

Run in VM:

```bash
curl -s http://127.0.0.1:5175/api/ui/get-license-status
```

Expected: `allowsReportGeneration=true`。

然后从 UI 触发一个小样本报告。

- [ ] **Step 3: 检查 12 worker evidence**

检查最新 run evidence：

```bash
find /opt/claw-trade/shared/reports/runs -maxdepth 4 -type f | sort | tail -200
```

Expected:

- 有 12 个 TradingAgents worker 的 dispatch/evidence。
- 后续 worker 没有 direct LLM 或 Python materializer 替代路径。

- [ ] **Step 4: 检查 provider payload 和 tool schema**

定位最新 run 的 provider payload evidence 和 tool schema evidence：

```bash
RUN_ROOT=/opt/claw-trade/shared/reports/runs
LATEST_RUN="$(find "${RUN_ROOT}" -mindepth 1 -maxdepth 1 -type d | sort | tail -1)"
find "${LATEST_RUN}" -type f \( \
  -iname '*provider*payload*' -o \
  -iname '*provider*request*' -o \
  -iname '*visible*tool*' -o \
  -iname '*tool*schema*' -o \
  -iname '*dispatch*evidence*' \
\) -print
```

Expected:

- 能看到 worker id、stage、run id、dispatch id。
- 能看到 model-visible messages。
- 能看到 visible tool schema。
- 如果上述命令找不到文件，停止并回到生产 runtime 打包或 evidence 写入路径排查；不能用 exporter 输出或重建 prompt 代替 provider payload。

- [ ] **Step 5: 写边界验收记录**

在 `docs/evidence/工控机第一版OpenClaw边界验收记录.md` 写：

```markdown
# 工控机第一版 OpenClaw 边界验收记录

## 结论

- `/report` 是否仍由 claw-trade 控制：
- 12 worker 是否仍由 OpenClaw wake：
- provider payload 是否可取得：
- visible tool schema 是否可取得：
- 是否发现 direct LLM 替代路径：

## 证据

- run id：
- dispatch ids：
- provider payload 路径：
- tool schema 路径：

## 偏差

- 无 / 有：
```

- [ ] **Step 6: Commit**

```bash
git add docs/evidence/工控机第一版OpenClaw边界验收记录.md
git commit -m "docs: record production openclaw boundary proof"
```

Expected: 生产样机边界证据完整。

---

## 3. 总体验证命令

完成全部任务后运行：

```bash
uv run pytest \
  tests/unit/licensing \
  tests/unit/production \
  tests/e2e/ui/test_license_gate_routes.py \
  tests/e2e/ui/test_research_ui_server.py \
  tests/e2e/ui/test_report_user_flows.py \
  tests/unit/selection/test_refresh_service.py \
  tests/integration/test_openclaw_cron_price_alert_contract.py \
  -q
```

Expected: PASS。

构建生产包：

```bash
scripts/production/build_production_package.sh
```

Expected: PASS，审计脚本返回 exit code `0`。

VM 手工验收，宿主机运行：

```bash
virsh domstate claw-trade-prod-vm
virsh domifaddr claw-trade-prod-vm
ssh deploy@"${VM_IP}" 'hostname && python3.12 --version'
```

VM 内运行：

```bash
/opt/claw-trade/current/bin/claw-trade-preflight
curl -s http://127.0.0.1:5175/healthz
curl -s http://127.0.0.1:5175/api/ui/get-license-status
```

Expected:

- VM running，SSH 登录成功。
- preflight exit code `0`。
- UI healthz 正常。
- 授权状态脱敏。
- 未授权阻断 report/data_refresh。
- 已授权可以进入真实 OpenClaw `/report`。

---

## 4. 执行顺序和提交边界

按以下顺序执行，不能跳过验证门：

1. Task 0：Virbox 账号、测试授权码、SDK/runtime、Ubuntu VM。
2. Task 1：Virbox Linux x86 验证门。
3. Task 2-4：授权状态模型、Virbox 读取、授权服务。
4. Task 5-7：报告阻断、数据刷新阻断、UI 状态。
5. Task 8-9：生产路径和入口模板。
6. Task 11：systemd/kiosk 草案。
7. Task 10：生产包构建，包内必须包含 systemd/kiosk 模板。
8. Task 12：VM `/opt` 启动。
9. Task 13-14：授权阻断验收、OpenClaw 边界验收。

每个 Task 结束后单独 commit。不要把 Virbox 账号申请、Virbox 验证、授权阻断、打包、UI 改动混在一个提交里。

---

## 5. 已知风险

- Virbox 账号、测试授权码或 Linux x86_64 SDK/runtime 拿不到时，Task 0 停止，不进入后续实施。
- Virbox SDK/API 细节未知。Task 1 失败时，本计划停止，不允许用本地假状态绕过。
- Python wheel 默认包含 `.py` 文件。若产品定义的“无源码”要求连 Python 文本源码也不能出现，需要单独评审 pyc-only、PyInstaller、Nuitka 或 native loader；不在本计划内。
- OpenClaw 生产包需要的最小 runtime 文件要通过 VM 验证确认。若 `openclaw.mjs + dist` 不足，补齐运行时依赖，但不得复制整个开发仓库。
- kiosk 当前只是第一版 policy 和 unit 草案，不等于防 root、拆盘或专业逃逸。
- 本计划不做签名 updater；不能拿这个样机直接承诺远程更新能力。

---

## 6. 后续计划入口

本计划通过后，再单独开三个计划：

1. `工控机第一版签名更新与回滚实施计划`
2. `工控机第一版救援页与恢复出厂实施计划`
3. `工控机第一版黄金镜像与量产验收实施计划`
