# 九十三号人工浏览器集成测试报告

测试日期：2026-06-25
执行方式：真实 Windows Chrome，通过 `playwright-win-chrome` MCP 操作页面
访问地址：`http://127.0.0.1:5175/`
Chrome：`Chrome/146.0.7680.165`
运行环境：本地真实 runtime 已启动，OpenViking `1933` 和 OpenClaw gateway `18789` 健康检查通过
本轮范围：按 `docs/项目文档/93-人工浏览器集成测试用例.md` 全部用例逐项复验
代码修改：无。本轮只新增测试证据和本报告。

## 总结

这轮不是“全部通过”。结论是：主要用户入口已经可用，`/select` 和四市场 `/report` 解析明显比上一轮好；完整新报告链路已在补测中跑通，从确认、任务启动、运行中阶段推进、完成、历史入库到报告详情都能在 Chrome 里看到。仍未通过或未覆盖的是 worker 运行证据入口、渠道发送、删除保护、外部入站消息、真实 PDF 保存这些项目。

统计：

| 结果 | 数量 | 说明 |
| --- | ---: | --- |
| 通过 | 10 | 普通聊天、`/report` 基础校验、四市场解析、完整新报告链路、报告问答、图表、worker 聊天、维护状态、API 边界 |
| 部分通过 | 14 | 页面入口或失败态可见，但没有覆盖全部成功路径或证据入口 |
| 未通过 | 2 | 正式报告运行证据、数据网关证据入口 |
| 阻塞/未执行 | 4 | 取消任务、微信/外部渠道、报告删除等会影响外部渠道或已有数据的成功路径 |

本轮最重要的三个结论：

1. `/select` 默认 A 股这次返回了 3 个进入 `/report` 候选、17 个观察候选、0 个放弃候选。
2. `/select CRYPTO` 这次返回了 3 个进入 `/report` 候选、9 个观察候选、8 个放弃候选，不再只是补数。
3. `/select US` 和 `/select 3` 没有再显示“回复失败”，但页面也没有显示明确“不支持”提示，所以这两条仍然不能算完全通过。

## 运行和证据

| 项目 | 本轮结果 |
| --- | --- |
| UI 地址 | `http://127.0.0.1:5175/` |
| OpenViking | `1933/health` 返回健康 |
| OpenClaw gateway | `18789/health` 返回健康 |
| Chrome 控制方式 | `scripts/mcp-chrome.sh --endpoint http://127.0.0.1:9223` + `scripts/playwright-win-chrome-mcp-call.mjs` |
| 证据目录 | `docs/evidence/manual-browser-93-20260625/` |
| 主要快照 | `home.snapshot.json`、`select-default-after-wait.snapshot.json`、`select-crypto-after-wait.snapshot.json`、`report-detail-open.snapshot.json`、`settings*.snapshot.json`、`diagnostics.snapshot.json` |
| 完整新报告链路补测证据 | `full-report-chain-aapl/01-home-before.result.json` 到 `full-report-chain-aapl/15-new-report-detail.snapshot.json` |

注意：MCP 的直接页面工具一开始出现 transport closed，本轮按项目文档里的 9223 兜底方式连接同一个 Windows Chrome。这个路径仍然是 `playwright-win-chrome` MCP，不是 headless 浏览器，也不是直接 API 代测。

## 逐项结果

| 用例 | 结论 | 本轮看到的页面结果 | 主要证据 |
| --- | --- | --- | --- |
| TC-01 普通聊天不触发报告 | 通过 | 普通问题返回普通解释；“帮我看看 AAPL”只提示使用 `/report AAPL`，没有创建报告任务。任务状态保持 `0 运行中 · 0 排队中`。 | `after-timeout.snapshot.json` |
| TC-02 `/report` 输入校验和确认 | 通过 | `/report` 不带标的时提示“请输入完整的 /report 指令”；`/report 600519.SH` 显示确认卡，标的是 `600519.SH`，名称是 `贵州茅台`，未确认前不创建任务。 | `after-timeout.snapshot.json` |
| TC-03 `/select` 候选发现和刷新入口 | 部分通过 | `/select` 默认 A 股成功；`/select CRYPTO` 成功；`/select US`、`/select 3` 没有出现“回复失败”，但也没有显示明确“不支持”提示。 | `select-default-after-wait.snapshot.json`、`select-crypto-after-wait.snapshot.json` |
| TC-03A `/select` 命令矩阵和 selection worker 证据 | 部分通过 | 本轮覆盖了 `/select`、`/select US`、`/select 3`、`/select CRYPTO`。候选结果可见，但页面没有可见的 selection worker 顺序、cache hash/readback、provider payload、tool schema 入口。 | `select-default-after-wait.snapshot.json`、`select-crypto-after-wait.snapshot.json`、`diagnostics.snapshot.json` |
| TC-04 `/report` 创建、队列和运行中状态 | 通过 | 补测中通过 `/report AAPL` 创建新任务，确认后页面显示“报告任务已启动，正在生成”。任务状态从 `1 运行中 · 0 排队中` 进入运行中；右侧任务区持续显示 AAPL 的阶段推进。因为本地没有排队积压，未出现非 0 的排队停留。 | `full-report-chain-aapl/02-report-command-confirm-card.result.json`、`04-after-confirm-chat-history.result.json`、`05-poll-30s.result.json`、`06-poll-90s.result.json` |
| TC-04A 四市场 `/report` 最小 Chrome 验收 | 通过 | A 股 `600519.SH` 显示 `贵州茅台`；美股 `AAPL` 显示 `Apple Inc.`；港股 `00700.HK` 显示 `腾讯控股`；加密 `BTC/USDT` 显示 `Bitcoin`。都显示确认卡，未确认前不创建任务。 | `select-crypto-after-wait.snapshot.json` |
| TC-04B 正式报告运行证据核查 | 未通过 | 报告阅读页能看正文、图表、PDF 状态，但没有 Chrome 可见入口核对每个 worker 的 provider payload、tool schema、approved material、receipt、manifest。 | `report-detail-open.snapshot.json`、`diagnostics.snapshot.json` |
| TC-05 报告任务取消 | 阻塞/未执行 | 本轮没有创建新的长运行报告任务，所以没有可取消任务可测。 | 无成功路径证据 |
| TC-06 报告失败状态和错误文案 | 部分通过 | 高级诊断页显示 provider 健康异常：“provider 健康检查失败，请检查模型服务商配置后重试。”但本轮没有创建一条失败的 `/report` 任务，所以不能算完整通过。 | `diagnostics.snapshot.json` |
| TC-07 完整报告完成和历史入库 | 通过 | 补测中新建的 AAPL 报告完成。页面显示“正式报告已完成”，任务状态回到 `0 运行中 · 0 排队中`，正式报告数量从 2 份变成 3 份；历史第一条新增 `AAPL 报告 2026/6/25 03:33:54`，点击后打开 `Apple Inc.（AAPL）投资研究报告` 详情页。 | `full-report-chain-aapl/13-poll-after-more-polisher.result.json`、`14-open-new-report-detail.result.json`、`15-new-report-detail.snapshot.json` |
| TC-08 报告详情正文、worker 附录和 PM 结论 | 部分通过 | AAPL 报告正文是中文读者版报告，PM 结论可见，评级为“卖出（Sell）”；但没有 worker 附录或阶段详情入口。 | `report-detail-open.snapshot.json` |
| TC-08A 独立报告问答不改写原报告 | 通过 | 在 AAPL 报告页提问“这份报告最大的下行风险是什么？”，组合经理回答使用了当前报告内容，没有创建新报告任务。 | `report-worker-chat-after-wait.result.json` |
| TC-09 图表证据状态 | 通过 | AAPL 报告页能看到图表状态 `ready`，正文里有图表图片节点；侧栏显示图表已生成。 | `report-detail-open.snapshot.json` |
| TC-10 PDF 导出 | 部分通过 | 页面显示 `PDF ready` 和“保存为 PDF”按钮；本轮没有实际走 Chrome 保存文件流程，也没有验证后端 PDF 下载文件。 | `report-detail-open.snapshot.json` |
| TC-11 selection 确认后交接报告 | 部分通过 | 候选结果里有“确认进入 /report”按钮，且 `/select` 没有自动创建报告；本轮没有点击候选确认创建正式报告。 | `select-default-after-wait.snapshot.json`、`select-crypto-after-wait.snapshot.json` |
| TC-12 渠道状态和报告文件发送 | 阻塞/未执行 | 页面显示微信 ClawBot 已连接，但“微信文件发送能力未验证”。本轮没有发送真实报告文件。 | `home.snapshot.json`、`settings-general.snapshot.json` |
| TC-12A 渠道配置保存和 probe | 部分通过 | 设置页能看到微信通知状态、重新连接、解除连接、刷新二维码入口；本轮没有修改配置、保存或 probe。 | `settings-general.snapshot.json` |
| TC-12B 入站渠道消息 | 阻塞/未执行 | 本轮没有外部测试渠道消息进入 Chrome 页面，因此没有入站普通消息或入站 `/report` 命令证据。 | 无成功路径证据 |
| TC-13 worker 聊天 | 通过 | 报告页里向组合经理提问后，组合经理返回基于报告的回答；任务状态仍是 `0 运行中 · 0 排队中`。 | `report-worker-chat-after-wait.result.json` |
| TC-14 定时报表管理 | 部分通过 | 首页右侧有定时报表管理，能看到 cron id、最近运行、下次运行、暂停/恢复/手动运行/删除按钮。本轮做了可逆暂停/恢复验证，最终状态已恢复；没有新建、手动运行、删除。 | `home-controls-before.result.json`、`schedule-alert-actions.result.json`、`schedule-current-buttons.result.json` |
| TC-14A 定时报表 cron wake 和幂等 | 部分通过 | 页面能看到 cron job id、scheduled-report wake id、最近运行 task id，也能看到 `sync_failed` 项；本轮没有等待新的 cron wake，也没有手动运行两次验证幂等。 | `home-controls-before.result.json`、`diagnostics.snapshot.json` |
| TC-15 价格提醒管理和立即检查 | 部分通过 | 首页右侧有价格提醒管理，能看到提醒状态、扫描 bucket、暂停/立即检查/删除按钮。真实数据源不可用时仍 fail-closed。补测用模拟报价跑通了成功路径：Chrome 点击 `alert-17` 的“立即检查”后，状态变为 `closed`，最近报价显示 `71000`，通知 key 可见。注意：`scan run id` 属于后台扫桶，不是单条“立即检查”产生。 | `schedule-alert-actions.result.json`、`home-after-actions.snapshot.json`、`price-alert-full-simulated/09-click-inapp-alert17-immediate-check.result.json` |
| TC-15A 价格提醒触发、不触发、bucket 扫描和通知去重 | 部分通过 | 模拟报价补测覆盖了触发成功、通知回执、通知去重和 bucket 扫描 run id：服务层 `wechat_clawbot` 模拟回执 `messageId=mock-message-1`；Chrome 页面使用 `in_app` 避免真实外部发送，显示通知去重 key。真实外部通知发送仍未执行。 | `price-alert-full-simulated/price-alert-simulated-service-evidence.json`、`price-alert-full-simulated/09-click-inapp-alert17-immediate-check.result.json`、`price-alert-full-simulated/10-after-inapp-check.snapshot.json` |
| TC-16 系统后台任务和维护状态 | 通过 | 高级诊断页显示维护任务明细：selection 刷新、定时报表 wake、价格扫描 bucket、数据维护、报告清理；没有把这些维护任务暴露成普通用户可新建的定时报表。 | `diagnostics.snapshot.json` |
| TC-17 报告清理保护 | 阻塞/未执行 | 历史报告里有删除按钮，但本轮没有删除报告，也没有构造发送中/导出中报告验证保护。 | `home.snapshot.json` |
| TC-18 设置和模型设置 | 部分通过 | 设置页能看到报告模型、Embedding、报告保留时间、选股刷新、微信、恢复默认；本轮没有修改、保存、取消、局部重置或全局重置。 | `settings.snapshot.json`、`settings-general.snapshot.json` |
| TC-19 数据源设置和诊断 | 部分通过 | 数据源页显示 7 个 API 源，Tushare 已验证，Finnhub/FRED/CoinGecko/Coinglass 等有状态；高级诊断只读展示 provider 健康异常和 live run 缺口。没有执行测试连接或保存。 | `settings-data.snapshot.json`、`diagnostics.snapshot.json` |
| TC-19A 数据源保存和数据网关证据 | 未通过 | 页面有数据源配置和测试按钮，但没有 Chrome 可见 provider attempts、raw store、normalized store、Mongo metadata、columnar warehouse、manifest/data gap 细节入口；本轮也没有保存/取消数据源配置。 | `settings-data.snapshot.json`、`diagnostics.snapshot.json` |
| TC-20 API 404 和 SPA fallback | 通过 | `/reports/not-real-client-route` 回到应用并提示没有找到报告；`/api/ui/not-real-api`、`/api/other` 都返回 404 JSON“接口不存在”；`/assets/not-real-file.js` 返回 404。 | `routes-api-static.result.json` |
| TC-21 权限、配置缺失和运行中状态总检 | 部分通过 | 首页、设置页、诊断页能区分空状态、运行中、配置/健康异常、fail-closed；但没有覆盖所有配置缺失动作和权限失败动作。 | `home.snapshot.json`、`settings*.snapshot.json`、`diagnostics.snapshot.json` |

## `/select` 复验细节

| 命令 | 页面结果 | 判定 |
| --- | --- | --- |
| `/select` | 返回 A 股候选。进入 `/report`：`300671.SZ 富满微`、`600961.SH 株冶集团`、`601208.SH 东材科技`，共 3 个；观察 17 个；放弃 0 个。 | 通过 |
| `/select US` | 页面没有出现“回复失败”，但也没有出现明确“不支持 US”的系统提示。 | 未完全通过 |
| `/select 3` | 页面没有出现“回复失败”，但也没有出现明确“不支持 3”的系统提示。 | 未完全通过 |
| `/select CRYPTO` | 返回加密候选。进入 `/report`：`NEARUSDT NEAR/USDT`、`XLMUSDT XLM/USDT`、`INJUSDT INJ/USDT`，共 3 个；观察 9 个；放弃 8 个。 | 通过 |

## 本轮发现的问题

| 严重度 | 问题 | 影响 | 证据 |
| --- | --- | --- | --- |
| 高 | 正式报告运行证据没有普通 Chrome 入口 | 93 号要求人工从 Chrome 追踪 provider payload、tool schema、approved material、receipt、manifest；当前报告页和诊断页没有这个入口，所以不能验收完整 live 链路。 | `report-detail-open.snapshot.json`、`diagnostics.snapshot.json` |
| 中 | `/select US`、`/select 3` 没有明确“不支持”提示 | 用户看不到这两条命令为什么没有结果；虽然不再显示“回复失败”，但反馈仍不够清楚。 | `select-crypto-after-wait.snapshot.json` |
| 中 | worker 附录/阶段详情入口缺失 | 普通报告阅读页可以没有内部证据，但 93 号验收项要求 worker 附录或阶段详情；当前 Chrome 页面无法核对。 | `report-detail-open.snapshot.json` |
| 中 | 数据网关深层证据入口缺失 | 数据源页有配置状态，高级诊断有摘要，但没有 attempts/store/manifest/data gap 的可追踪详情入口。 | `settings-data.snapshot.json`、`diagnostics.snapshot.json` |
| 中 | 价格提醒报价不可用时只能 fail-closed | 立即检查能产生新的 scan run id，但报价仍是“暂无报价”，没有触发成功或通知回执。 | `schedule-alert-actions.result.json` |
| 低 | 定时报表列表存在重复历史记录 | 右侧任务栏有多条 TSLA/BTC 定时报表记录；不一定是当前 bug，但会影响人工辨认和测试操作。 | `home-controls-before.result.json` |

## 没有执行成功路径的项目

这些不是“通过”，只是本轮没有执行成功路径：

| 项目 | 没执行的原因 |
| --- | --- |
| 报告任务取消 | 没有新的运行中报告任务可取消。 |
| 微信报告文件发送 | 会发到真实渠道；页面也明确写着文件发送能力未验证。 |
| 外部入站消息 | 本轮没有外部测试渠道消息。 |
| 删除报告 | 会修改已有报告数据；本轮不做破坏性删除。 |
| 真实 PDF 保存 | 会弹出 Chrome/系统保存流程；本轮只验证按钮和 ready 状态，没有保存文件。 |

## 定时报表操作说明

本轮为了验证暂停/恢复入口，做了一次可逆操作。过程中发现右侧任务栏 DOM 容器较大，第一次定位时把相邻定时报表也带入了匹配范围。随后我核对了具体 cron id，并把状态恢复到测试前：

| 定时报表 | 测试前 | 最终状态 |
| --- | --- | --- |
| `schedule-1` / `e61761a6-13c1-48d7-a6dd-eec532f02d6e` | paused | paused |
| `schedule-10` / `2eca05b6-6133-43a6-bd0b-aae3db60403d` | active | active |

最终状态证据：`schedule-current-buttons.result.json`。

## Collect-first compliance

| 项目 | 结果 |
| --- | --- |
| batch scope | 93 号文档全部 TC-01 到 TC-21，含 A/B 扩展用例 |
| completed items | 全部用例均给出 Chrome 证据或阻塞/未执行结论 |
| failures collected | 运行证据入口缺失、selection 负例提示不足、数据网关深层证据入口缺失、渠道/删除/PDF 成功路径未执行 |
| early-stop exception used | 否 |
| batch fix grouping | 本轮不改代码；问题按报告工作流证据、selection 负例反馈、数据网关证据、外部渠道/文件/删除成功路径分组 |
