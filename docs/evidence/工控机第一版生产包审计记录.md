# 工控机第一版生产包审计记录

- 构建时间：2026-06-26
- 构建分支：feature/virbox-delivery-implementation
- 构建版本：test-task10
- archive 路径：`.runtime/production-build/claw-trade-test-task10.tar`
- archive sha256：`50c22709827537a283b968288784057233bf5660414cc13d8bfa514271b5a1de`

## 构建命令

```bash
CLAW_TRADE_VERSION=test-task10 VITE_BIN=/home/frank/src/claw-trade/web/research-ui/node_modules/.bin/vite scripts/production/build_production_package.sh
```

说明：本次在隔离 worktree 验证时使用了主工作区已安装的 Vite 二进制文件。生产环境默认仍由 `scripts/production/build_production_package.sh` 调用 `pnpm build`。

## 审计命令

```bash
uv run python scripts/production/audit_production_package.py .runtime/production-build/claw-trade-test-task10.tar
```

审计结论：通过，命令退出码为 0，未输出禁止项。

## 禁止项检查

- `.git`：未发现
- `tests/`：未发现
- `docs/`：未发现
- `.env.local`：未发现
- `web/research-ui/src/`：未发现

## 回归测试

```bash
uv run pytest tests/unit/production/test_paths.py tests/unit/production/test_preflight.py tests/unit/production/test_package_audit.py -q
```

结果：`5 passed in 0.06s`
