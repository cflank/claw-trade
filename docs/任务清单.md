# claw-trade 任务清单

> **2026-06-13 数据层任务口径**
>
> 本文中涉及 report data-prefetch、domain 历史数据入口、历史数据入口或 候选选择逻辑 历史路由的任务描述，已被 `docs/限流重组.md` 取代。后续数据层任务必须按 `业务数据项请求 -> 内部 DataNeed -> planner -> ProviderCallSpec -> 执行闸/限流/入库` 落地；worker 不提交 `api_id` 或 `内部需求类型字段`。

本文是当前执行任务入口：它归并当前完成项、剩余项、验收项和停止条件。已删除的 legacy 任务文档不再作为产品路径或实施依据。

## 1. 当前状态

截至 2026-05-11，本地已有两个提交等待推送：

- OpenClaw 子仓库：`4fd1c1054d`，`Support claw-trade single worker runtime seams`。
- claw-trade 主仓库：当前 `HEAD`，`Add CN A frontline provider workflow`。

GitHub push 仍等待人类明确批准外部远端推送。

## 2. 已完成

### 2.1 control plane

- 固定 12-worker workflow state machine。
- StagePlan / StageBatch / WorkerCall / OpenClawCommand。
- OpenClaw client。
- request builder。
- WorkflowStore。
- runner batch。
- report export skeleton。
- runtime evidence reader。

### 2.2 OpenClaw runtime seam

- per-turn tool narrowing。
- final provider request capture。
- visible tools capture。
- first response capture。
- raw output capture。
- tool call recorder。
- OpenViking runtime tools。
- single-worker minimal system context。
- OpenClaw `pnpm build` 已执行。

### 2.3 CN_A frontline

- market prompt 对齐 CN 方向。
- fundamental prompt 对齐 CN 方向。
- news prompt 对齐 CN 方向。
- social prompt 对齐 CN 方向。
- 四 worker user prompt 已从“材料摘要型”改为“直接生成报告型”。
- 工具命名从点号收敛到下划线。

### 2.4 Provider / DataNeed data result

- CN_A market data skill。
- CN_A fundamental data skill。
- CN_A news data skill。
- CN_A social data skill。
- Tushare client 配置化。
- Tushare 私有 endpoint 配置化。
- News/Search provider key 模板。
- MiniMax 国内 endpoint 模板。
- Provider attempts 与质量状态。

### 2.5 Report / image

- 图片不进入 worker 主传递链。
- final exporter 能发现 `data-need-tool-evidence/techlab/charts-local` 下的 market 图表。
- final exporter 复制图表到 final report assets。

### 2.6 文档清理

- 顶层设计文档统一收敛为：
  - `需求设计.md`
  - `架构设计.md`
  - `详细设计.md`
  - `任务清单.md`
- `docs/evidence/` 继续保留 TradingAgents-CN 基准证据。

## 3. 待办优先级

### P0：推送 GitHub

阻塞：需要人类批准外部 GitHub push。

执行顺序：

```text
cd /home/frank/src/claw-trade/third_party/openclaw
git push cflank claw-trade-control-v0.1

cd /home/frank/src/claw-trade
git push origin main
```

验收：

- OpenClaw 子仓库远端存在 `4fd1c1054d`。
- claw-trade main 远端存在当前 `HEAD`。
- clone 后 submodule 可 checkout。

### P1：fresh runtime proof

目标：证明 pushed 代码在 fresh runtime 中真实生效。

任务：

- 重启 `scripts/start-control-runtime.sh`。
- 跑 CN_A 600519 frontline 四 worker。
- 抓取 final prompt、LLM back、report。
- 与 TradingAgents-CN 基准做详细比较。

验收：

- final prompt 不包含控制流垃圾。
- worker report 有报告感。
- tool schema stage-scoped。
- OpenViking receipt 存在。
- 图片最终进入 final report assets。

### P2：完整 12-worker report

目标：从 `/report` 语义跑完整 12-worker。

任务：

- frontline 四 worker。
- bull -> bear。
- research manager。
- trader。
- risk challenger / guardian / moderator。
- portfolio manager。
- final report export。

验收：

- PM 最终结论未被 Python 改写。
- final report reader-facing。
- 所有既有 worker 报告校验无 Critical/Important。
- 图表可见。

### P3：News provider 完整性

目标：解决 news 数据源覆盖不足和强相关新闻为空时的解释问题。

任务：

- 验证 Tushare news / announcements。
- 验证 AkShare stock news。
- 验证 Bocha / Tavily / Jina / NewsNow / MiniMax key。
- 明确强相关为空和 provider 失败的区别。

验收：

- provider 失败原因可读。
- 真实无新闻不被当成工具失败。
- 背景新闻不被写成公司事实。

### P4：Social 文本源

目标：补齐文本情绪来源，或明确只做热度报告。

任务：

- `T-TST-002`：真实集成运行仍待最后集中执行；历史故障注入口径已记录，不作为继续实施阻断 gate。
- `T-TST-003`：fresh 验收脚本已实现，真实 fresh run 仍待执行。
- 修复雪球/股吧文本源注册或 schema。
- 如果文本源不可用，prompt 和 report 改为热度报告。
- 禁止无文本源时输出情绪指数和价格预测。

验收：

- 有文本时可写情绪。
- 无文本时只写热度。
- 不编造社交结论。

### P5：Fundamental EPS / 估值口径

目标：避免目标价混算。

任务：

- 在 DataNeed 数据结果中保留 EPS 与 TTM EPS 口径。
- 在 worker 可读材料中说明估值字段来源和日期。
- fundamental report 使用一致口径。

验收：

- PE/PB/ROE 有来源。
- 目标价框架引用口径一致。
- 缺字段时报告缺口，不补假数。

### P5.1：HK Fundamental 财务指标来源

目标：港股 fundamental 主源使用 Tushare；当 Tushare 单个接口失败、无权限、空结果或缺字段时，再显式切换到可验证的免费资源补位。

已确认：

- 当前 Tushare 账号可调用 `hk_basic(00700.HK)` 和 `hk_daily(00700.HK)`。
- 当前 Tushare 账号调用 `hk_fina_indicator(00700.HK)` 返回无接口权限。
- `hk_basic`、`hk_daily` 等已验证可用接口仍作为 HK provider 的优先来源。
- `hk_fina_indicator` 仍是港股财务指标的 Tushare 主源候选；当前账号无权限时进入显式失败记录，并由免费资源补位。
- 如果未来 Tushare 账号获得 `hk_fina_indicator` 权限，该接口恢复为对应字段的主源；免费资源退回交叉校验或故障切换位置。

任务：

- 将 `hk_fina_indicator` 列为 Tushare provider contract 待解决项：确认权限、字段、币种、更新时间、频率限制和失败证据。
- 对 Tushare 已可用接口继续按第一梯队 provider 接入，不能因为存在免费补位源而绕开 Tushare。
- 实现 provider priority：每个 HK fundamental 字段先请求 Tushare；仅在 Tushare 明确失败、无权限、空结果、字段缺失或超时后，才请求免费补位源。
- 验证免费替代来源一：AkShare 东方财富港股财务指标与三大报表接口，覆盖 EPS、ROE、资产负债率、利润表、资产负债表、现金流量表。
- 验证免费替代来源二：Yahoo Finance / yfinance 港股财务表和估值字段，覆盖 income statement、balance sheet、cashflow、PE、PB、ROE、market cap、currency。
- 验证免费权威来源三：HKEXnews / 公司公告中的年报、中报、业绩公告原文，用于校验重大财务结论和监管披露。
- 形成 HK fundamental source matrix：每个字段标明来源、单位、币种、报告期、发布日期、更新时间、可追溯原文或 provider attempt。

验收：

- 至少用 `00700.HK` 和另一个非腾讯港股样本完成真实请求证明。
- 每个 provider attempt 记录 provider、函数或端点、provider symbol、请求时间、返回行数、关键字段、单位、币种和失败原因；同一字段发生故障切换时必须同时保留 Tushare 失败记录和免费源成功记录。
- AkShare / Yahoo 来源只能作为真实 provider attempt 补 Tushare 失败、无权限、空结果或缺字段的位置，不能替代已验证可用的 Tushare 接口，也不能在 Tushare 无权限时静默替换成“已完成 Tushare 财务指标”。
- 重大财报、盈利警告、停复牌、配售、回购、关联交易等结论必须追溯 HKEXnews、公司公告或等效权威原文。
- 如果所有已验证来源都缺字段，HK fundamental report 必须报告证据缺口，不能补假数或硬编码。

### P5.2：HK Frontline 复用统一数据需求工具

目标：HK 前线 prompt 和 STAGES 复用统一数据需求工具；同一模型可见工具提交业务数据项请求，由数据层按 `market=HK` 生成内部 DataNeed 和 provider 调用，不另造 HK 专用工具名。

已确认：

- HK 四个前线 worker 分别复用 `claw_request_data`、`claw_request_data`、`claw_request_data`、`claw_request_data`。
- 当前插件 JS 边界已允许这些工具接收 `market=HK`。
- 当前港股基本面实现已挂到 `claw_request_data` 的 `market=HK` 内部路由。
- `claw_request_data` 已按 `market=HK` 传递业务数据项和市场参数；具体源、接口和失败证据只在数据层 attempt/audit 中出现，不进入 worker 工具输入。
- HK profile 已按 2026-05-15 人工指令切换为 `approved: true`，用于进入真实报告路径做 live 验证。
- HK live provider payload 验证仍未完成；后续验收必须证明真实 provider 请求、模型可见工具 schema 和 provider payload，不能用静态配置或导出结果替代。

任务：

- 用真实 Tushare token 验证 `hk_daily_adj`、`anns_d`、`hk_basic`、`hk_fina_indicator` 的权限、字段和失败证据。
- 验证免费兜底源 AkShare `stock_hk_hist`、EastMoney `push2his_kline`、新闻/搜索 provider 在 HK 样本上的真实返回。
- 补齐或确认港股交易单位、交易时段、停复牌、公告原文和社交文本源的字段口径。
- 用 fresh OpenClaw provider payload 证明 HK 前线 worker 可见工具 schema 与各自 STAGES 一致，并记录真实 provider attempt 成败。

验收：

- OpenClaw 插件 manifest 不包含 `hk_*` 专用前线工具名；HK 前线复用统一数据需求工具。
- HK 前线 worker 的 provider payload 只暴露 `claw_request_data`，不暴露 US 原子工具或 provider 执行细节。
- 每个工具真实请求至少覆盖 `00700.HK` 和另一个非腾讯港股样本，并记录 provider、provider symbol、请求时间、返回行数、关键字段、单位、币种和失败原因。
- HK profile 已打开；未完成上述验收前，不得声明港股报告链路完成。

### P5.3：HK Prompt 与 TradingAgents-CN 港股基准对比

目标：等港股接口恢复后，用同一港股标的补齐 HK prompt 的正式对比验收，不能只靠静态合同测试证明 prompt 对齐。

当前阻塞：

- Tushare `hk_daily_adj` 当前触发频率限制。
- AkShare / EastMoney 港股行情免费源当前出现远端断连。
- 在行情资料缺失状态下做 prompt/report 质量对比会混入数据源失败噪音，因此先登记待办，等待接口恢复后执行。

任务：

- 在 TradingAgents-CN 中用同一港股标的抓取真实港股基准证据，优先使用 `00700.HK`，并补一个非腾讯港股样本。
- 在 claw-trade 中用同一标的跑 HK report 路径，抓取真实 OpenClaw provider final prompt、LLM back、worker report 和模型可见工具 schema。
- 对 12 个 worker 加 report_polisher 做逐项对比：角色口吻、报告结构、辩论强度、交易建议表达、币种/市场变量、上游材料边界、工具可见性和机器协议污染。
- 输出 HK vs TradingAgents-CN 港股 detailed analysis 文档，位置放在 `docs/evidence/` 下对应 fresh run 目录。

验收：

- 对比证据必须来自真实 provider payload 或 TradingAgents-CN 真实运行捕获，不能用静态 render、日志重构或导出报告替代。
- HK prompt 不得 fallback 到 CN_A prompt；只允许保留 TradingAgents-CN 风格并替换港股市场、港币和港股数据源语境。
- 后续 worker 的模型可见材料必须是已批准 L1 自然语言正文，不得暴露 artifact 协议、URI/hash、OpenViking/OpenClaw 运行协议或 provider attempt 调试字段。
- 未完成该对比前，不得声明 HK prompt 已与 TradingAgents-CN 港股基准完成详细对齐。

### P6：Guard 收敛

目标：guard 只防造假，不压扁报告；prompt 不再把真实性红线扩展成审慎、合规、memo 化或禁止强观点/交易建议的风格系统。

任务：

- 检查 hard gate 是否仍存在“报告语气误杀”。
- 删除重复 prompt 约束。
- 保留 claim/source/tool/receipt/PM 最终裁决权边界。
- 删除或改写所有没有原版依据的表达层禁令，包括禁止 final proposition、禁止证据支持的 BUY/HOLD/SELL、默认 limitation report、过度审慎、过度合规、memo 化语气。
- 为 bull/bear/risk/research manager/PM 增加 prompt 风格合同：保留原版强观点辩论室和自然语言决策风格，同时继续拦截编造事实。

验收：

- analyst 可以有观点。
- PM 仍是最终结论 owner。
- fake/fallback 仍 hard fail。
- evidence-supported final proposition、交易条件、目标情景、风险判断、直接反驳不会被 prompt 或 guard 禁掉。
- 对比报告必须明确区分“真实性增强”与“原版风格漂移”，不得把前者当作后者的借口。

## 4. 测试清单

### 4.1 Focused tests

```text
uv run pytest tests/contracts/test_final_report_exporter.py
uv run pytest tests/contracts/test_tool_registry_contract.py
uv run pytest tests/unit/test_openclaw_client.py
```

### 4.2 Frontline unit tests

```text
uv run pytest tests/unit/test_frontline_*.py
uv run pytest tests/unit/test_cn_a_fundamental_*.py
uv run pytest tests/unit/test_cn_a_social_*.py
```

### 4.3 Contract tests

```text
uv run pytest tests/contracts
```

### 4.4 Integration tests

```text
uv run pytest tests/integration
```

只在 MongoDB、OpenViking、OpenClaw runtime 环境齐全时执行。

### 4.5 OpenClaw build/test

```text
cd third_party/openclaw
pnpm build
pnpm exec vitest run \
  src/agents/pi-embedded-runner/run/params.single-worker.test.ts \
  src/agents/pi-embedded-runner/run/single-worker-system-context.test.ts \
  src/agents/pi-embedded-runner/run/openviking-runtime-tools.test.ts \
  src/agents/pi-embedded-runner/stream-resolution.test.ts
```

## 5. 停止条件

停止并问人类：

- GitHub push 远端不明确。
- 需要 force push。
- OpenClaw 子模块提交未推送但主仓库要引用它。
- provider 策略需要重新选择。
- 需要关闭矩阵源。
- 需要添加 fallback。
- live runtime 缺 MongoDB / OpenViking / OpenClaw 任一关键服务。
- 同一 gate 类别两次失败。
- 任何改动会让 Python 写 PM 投资结论。

## 6. 历史任务来源与整合边界

本文件是当前任务入口。已删除的 legacy 任务文档不再链接、不再作为运行时或实施依据；需要调整任务时，直接修改当前任务清单和对应测试。
