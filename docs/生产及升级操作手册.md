# 生产及升级操作手册

## 0. 变量

```bash
TARGET_IP="192.168.1.21"
TARGET_USER="frank"
DELIVERY_BASE="https://download.cflank-trade.top/delivery"
STABLE_BASE="https://download.cflank-trade.top/stable"
VERSION="1.0.1"
MIN_CURRENT_VERSION="1.0.0"
```

## 1. 发布机准备

```bash
cd /home/frank/src/claw-trade
scripts/production/generate_update_signing_key.sh
```

```bash
techlab/virbox_probe/build_virbox_status_sdk.sh
ls -lh \
  .runtime/virbox-status-sdk/virbox-status-sdk-linux-x86_64.tgz \
  /mnt/d/claw-trade-virbox/resource-test/protect-python-ds/protected/python \
  /mnt/d/claw-trade-virbox/resource-test/protect-python-ds/python.ssp \
  /mnt/d/claw-trade-virbox/node-ds-test/input/protected/node \
  /mnt/d/claw-trade-virbox/node-ds-test/input/node.ssp
```

不要上传：

```text
license.key
update-signing-private.pem
```

## 2. 重做 seed

只改代码就跳过。

```bash
cd /home/frank/src/claw-trade

SEED_COLUMNAR_ROOT="$(find .runtime/factory-seeds -mindepth 2 -maxdepth 2 -type d -name normalized ! -path '*-test/normalized' | sort | tail -n 1)"
test -n "$SEED_COLUMNAR_ROOT"

scripts/start-control-runtime.sh -- uv run python scripts/production/export_current_seed.py \
  --mongo-database claw_trade_a_share_factory_seed \
  --columnar-root "$SEED_COLUMNAR_ROOT" \
  --output data/current-seed-$(date -u +%Y%m%d).tar \
  --replace-output

SEED="current-seed-$(date -u +%Y%m%d).tar"
(cd data && sha256sum -c "${SEED}.sha256")
```

## 3. 打生产包

```bash
cd /home/frank/src/claw-trade

scripts/production/build_production_package.sh
scripts/production/build_virbox_protected_delivery_package.sh

OUT_DIR="$(find /mnt/d/claw-trade-virbox/delivery-output -mindepth 1 -maxdepth 1 -type d | sort | tail -n 1)"
PKG="$(ls -t "$OUT_DIR"/claw-trade-production-*.tar.gz | head -n 1)"
REL="$(basename "$PKG" .tar.gz)"

cd "$OUT_DIR"
sha256sum -c "${REL}.tar.gz.sha256"
```

产物：

```text
$OUT_DIR/$REL.tar.gz
$OUT_DIR/$REL.tar.gz.sha256
```

## 4. 上传首次安装文件

PowerShell：

```powershell
cd D:\claw-trade-virbox\r2-delivery-upload
```

第一次使用：

```powershell
.\install-aws-cli.ps1
```

重新打开 PowerShell：

```powershell
cd D:\claw-trade-virbox\r2-delivery-upload
.\setup-r2-credentials.ps1
```

每次新包上传：

```powershell
.\upload-delivery.cmd "D:\claw-trade-virbox\delivery-output\virbox-protected-时间\claw-trade-production-版本-时间.tar.gz"
.\upload-delivery.cmd "D:\claw-trade-virbox\delivery-output\virbox-protected-时间\claw-trade-production-版本-时间.tar.gz.sha256"

Set-Content -NoNewline -Path .\latest.txt -Value "claw-trade-production-版本-时间"
.\upload-delivery.cmd ".\latest.txt"
```

脚本变了再上传：

```powershell
copy "\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\scripts\production\install_factory_test_ubuntu.sh" .
copy "\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\scripts\production\install_from_r2_ubuntu.sh" .
copy "\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\scripts\production\uninstall_factory_test_ubuntu.sh" .
copy "\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\scripts\production\validate_production_archive.py" .
copy "\\wsl.localhost\Ubuntu\home\frank\src\claw-trade\update-signing-public.pem" .
copy "\\wsl.localhost\Ubuntu\home\frank\secure-vendor\virbox\linux-x86_64\runtime\senseshield-lcc-2.7.5.69040-amd64.deb" .

.\upload-delivery.cmd ".\install_factory_test_ubuntu.sh"
.\upload-delivery.cmd ".\install_from_r2_ubuntu.sh"
.\upload-delivery.cmd ".\uninstall_factory_test_ubuntu.sh"
.\upload-delivery.cmd ".\validate_production_archive.py"
.\upload-delivery.cmd ".\update-signing-public.pem"
.\upload-delivery.cmd ".\senseshield-lcc-2.7.5.69040-amd64.deb"
```

对应公开对象路径包括 `delivery/install_from_r2_ubuntu.sh`、`delivery/validate_production_archive.py` 和 `delivery/latest.txt`。

检查：

```powershell
curl.exe -I "https://download.cflank-trade.top/delivery/latest.txt"
curl.exe -I "https://download.cflank-trade.top/delivery/claw-trade-production-版本-时间.tar.gz"
```

通过标志：

```text
200 OK
```

## 5. 新机首次安装

按这个文档执行：

```text
docs/Ubuntu新机首次安装操作手册.md
```

目标机上最小命令：

```bash
BASE="https://download.cflank-trade.top/delivery"
LICENSE_KEY_FILE="$HOME/.claw-trade/license.key"
mkdir -p "$(dirname "${LICENSE_KEY_FILE}")"
read -rsp "Virbox license key: " KEY; printf '\n'
printf '%s' "${KEY}" > "${LICENSE_KEY_FILE}"
chmod 600 "${LICENSE_KEY_FILE}"
curl -fsSL "${BASE}/install_from_r2_ubuntu.sh" | bash -s -- --base-url "${BASE}" --license-key-file "${LICENSE_KEY_FILE}"
```

## 6. 发远程升级

先跑第 3 节。

```bash
cd "$OUT_DIR"

REL="$(basename "$(ls -t claw-trade-production-*.tar.gz | head -n 1)" .tar.gz)"
SHA256="$(sha256sum "${REL}.tar.gz" | awk '{print $1}')"

openssl pkeyutl -sign \
  -inkey /home/frank/src/claw-trade/update-signing-private.pem \
  -rawin \
  -in "${REL}.tar.gz" \
  -out "${REL}.tar.gz.sig"

cat > manifest.json <<EOF
{
  "product": "claw-trade",
  "channel": "stable",
  "version": "${VERSION}",
  "arch": "linux-x86_64",
  "archive": "${REL}.tar.gz",
  "sha256": "${SHA256}",
  "created_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "min_current_version": "${MIN_CURRENT_VERSION}",
  "notes": "production update"
}
EOF

openssl pkeyutl -sign \
  -inkey /home/frank/src/claw-trade/update-signing-private.pem \
  -rawin \
  -in manifest.json \
  -out manifest.json.sig
```

产物：

```text
$OUT_DIR/$REL.tar.gz
$OUT_DIR/$REL.tar.gz.sig
$OUT_DIR/manifest.json
$OUT_DIR/manifest.json.sig
```

## 7. 上传远程升级文件

打开 Cloudflare：

```text
R2 Object Storage -> claw-trade-delivery -> stable/
```

按顺序上传：

```text
stable/$REL.tar.gz
stable/$REL.tar.gz.sig
stable/manifest.json
stable/manifest.json.sig
```

检查：

```text
https://download.cflank-trade.top/stable/manifest.json
https://download.cflank-trade.top/stable/manifest.json.sig
https://download.cflank-trade.top/stable/$REL.tar.gz
https://download.cflank-trade.top/stable/$REL.tar.gz.sig
```

## 8. 目标机升级

打开：

```text
http://目标机IP:5175/
```

操作：

```text
设置 -> 远程更新 -> 检查更新 -> 安装更新
```

验证：

```bash
readlink /opt/claw-trade/current
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5175/
curl -fsS http://127.0.0.1:5175/api/ui/get-license-status
systemctl --failed --no-pager
```

通过标志：

```text
current 指向新版本。
curl 输出 200。
"status":"activated"。
systemctl --failed 输出 0 loaded units listed。
```

## 9. 延长授权

先在 Virbox 开发者平台改到期日。

目标机执行：

```bash
KEY="$(tr -d '[:space:]' < "$HOME/.claw-trade/license.key")"
/opt/senseshield/ssclt --online_refresh_license --license_key "$KEY"
```

验证：

```bash
/opt/senseshield/ssclt -l all
curl -fsS http://127.0.0.1:5175/api/ui/get-license-status
```

通过标志：

```text
LicenseKeyStatus:Normal
"status":"activated"
expiresAt 变成新日期
```

许可 ID 变了就停，先确认包里的 `VIRBOX_LICENSE_ID`。

## 10. 什么时候重做 Virbox 加密

不用重做：

```text
只改代码、prompt、前端、配置。
只改同一个许可 ID 的授权日期。
```

必须重做：

```text
许可 ID 变了。
DS 密码或 API 密码变了。
Python/Node runtime 换了。
保护策略变了。
```

## 11. 排错

缺 Virbox 状态 SDK：

```bash
cd /home/frank/src/claw-trade
techlab/virbox_probe/build_virbox_status_sdk.sh
scripts/production/build_production_package.sh
scripts/production/build_virbox_protected_delivery_package.sh
```

页面显示未授权：

```bash
curl -fsS http://127.0.0.1:5175/api/ui/get-license-status
/opt/senseshield/ssclt -l all
```

Virbox 在线绑定超时：

```bash
grep -RIn 'ChinaVBPGlobalCDN\|MSG_TIMEOUT\|operationTimeout' \
  /opt/senseshield/etc/common/ss_cloud.xml \
  /opt/senseshield/etc/ss_service/ss_config.xml \
  /opt/senseshield/ss_config.xml \
  /opt/senseshield/etc/license_control_center/virboxlcc_config.json
```

通过标志：

```text
"status":"activated"
LicenseKeyStatus:Normal
ChinaVBPGlobalCDN" Default=1
MSG_TIMEOUT>60000
operationTimeout" : 60000
```
