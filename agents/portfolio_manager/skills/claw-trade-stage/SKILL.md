---
name: claw-trade-stage
description: Stage contract for `portfolio_manager` in the claw-trade OpenClaw report path.
---

# claw-trade stage contract

Follow `STAGES.yaml` for the current worker/profile/stage.

- Read only approved artifact refs supplied by claw-trade.
- Write the canonical artifact named `portfolio_decision`.
- Submit PM structured decision fields for `rating`, `final_conclusion`, `execution_conditions`, and `risk_conditions` through the tool schema exposed in this turn.
- Keep report markdown reader-facing; do not append machine-readable PM decision blocks by hand.
- Runtime audit fields are generated by tool/control evidence, not by worker-authored markdown.
- If the PM structured decision tool is missing or fails, report it truthfully and stop this turn.
- Do not call tools through Python or assume Python already fetched evidence.
- Do not emit unsupported investment claims.
- If the current profile is HK or CRYPTO, fail explicitly until that profile strategy is approved.
